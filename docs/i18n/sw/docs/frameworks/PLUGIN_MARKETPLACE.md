# Plugin Marketplace (Kiswahili)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇹 [am](../../../am/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇿 [az](../../../az/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇰 [da](../../../da/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇪 [de](../../../de/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇷 [el](../../../el/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇸 [es](../../../es/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇪 [et](../../../et/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇱 [he](../../../he/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇩 [id](../../../id/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇹 [it](../../../it/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇭 [km](../../../km/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇲 [my](../../../my/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇴 [no](../../../no/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [or](../../../or/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇰 [si](../../../si/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [te](../../../te/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇭 [th](../../../th/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/PLUGIN_MARKETPLACE.md)

---

> **Chanzo rasmi:** `src/lib/plugins/` (`marketplace.ts`, `manager.ts`, `manifest.ts`,
> `scanner.ts`, `loader.ts`), `src/app/api/plugins/`, na
> `src/app/(dashboard)/dashboard/plugins/`
> **Ilisasishwa mwisho:** 2026-06-28 — v3.8.40

OmniRoute huja na mfumo wa programu-jalizi unaofanana na wa WordPress. Programu-jalizi ni saraka
zinazojitegemea — kila moja ikiwa na faili ya maelezo ya `plugin.json` na faili ya kuingilia — ambazo huunganishwa
kwenye mfululizo wa kushughulikia maombi (`onRequest` / `onResponse` / `onError`) na kwenye
matukio ya mzunguko wa maisha (`onInstall` / `onActivate` / `onDeactivate` / `onUninstall`).

**Soko la Programu-jalizi** ni safu ya ugunduzi iliyo juu ya mfumo huo. Linaweka wazi
katalogi inayoweza kuvinjariwa ya programu-jalizi zinazoweza kusakinishwa. Kwa chaguomsingi katalogi ni
rejista ndogo ya awali iliyojengewa ndani; mwendeshaji anaweza kuielekeza kwenye URL maalum ya
rejista ya mbali, ambapo uletaji hulindwa dhidi ya SSRF kwa kutumia kinga inayotatua DNS
(tazama [Usalama](#security)).

Kila njia ya programu-jalizi **inaruhusiwa kwenye loopback pekee** (Kiwango cha 1 — `LOCAL_ONLY`): programu-jalizi hupakia
na kutekeleza msimbo katika michakato-toto, kwa hivyo njia hizo haziwezi kufikiwa kutoka
chanzo kisicho cha loopback bila kujali uthibitishaji. Tazama
[`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md).

## Jinsi Vipengele Vinavyounganishwa

```
Dashibodi (/dashboard/plugins)
  ├─ Kichupo cha "Zilizosakinishwa" → GET /api/plugins            (listPlugins)
  │                                  POST /api/plugins/scan      (pluginManager.scan)
  │                                  POST /api/plugins/{name}/activate|deactivate
  │                                  DELETE /api/plugins/{name}   (ondoa usakinishaji)
  └─ Kichupo cha "Soko"            → GET /api/plugins/marketplace
                                       → listMarketplacePlugins()
                                         ├─ hakuna URL maalum → SEED_REGISTRY iliyojengewa ndani
                                         └─ URL maalum → kinga ya SSRF ya isSafeMarketplaceUrl()
                                                        → safeOutboundFetch(guard:"public-only")
```

- **Safu ya rejista** — `src/lib/plugins/marketplace.ts`: huorodhesha / hutafuta kwenye
  katalogi, na kurudi kwenye rejista ya awali ikiwa hitilafu yoyote itatokea.
- **Safu ya mzunguko wa maisha** — `src/lib/plugins/manager.ts` (`pluginManager` singleton):
  kusakinisha, kusasisha, kuwezesha, kulemaza, kuondoa usakinishaji, kuchanganua, kupakia wakati wa kuanza.
- **Safu ya faili ya maelezo** — `src/lib/plugins/manifest.ts`: skima ya Zod + chaguomsingi za
  `plugin.json`.
- **Kichanganuzi** — `src/lib/plugins/scanner.ts`: hugundua programu-jalizi kwenye diski chini ya
  saraka ya programu-jalizi.
- **Kipakiaji** — `src/lib/plugins/loader.ts`: huanzisha kila programu-jalizi katika
  mchakato-toto uliotengwa na kupitisha miito ya vihusishi kupitia IPC.

## Katalogi ya Soko

`listMarketplacePlugins()` (`src/lib/plugins/marketplace.ts`) hurejesha orodha ya
vipengee vya `MarketplaceEntry`:

| Sehemu        | Aina     | Maelezo                                            |
| ------------- | -------- | -------------------------------------------------- |
| `name`        | string   | Jina la programu-jalizi la kebab-case              |
| `version`     | string   | semver                                             |
| `description` | string   | Muhtasari mfupi                                    |
| `author`      | string   | Mwandishi / shirika                                |
| `license`     | string   | Kitambulisho cha leseni cha mtindo wa SPDX         |
| `downloadUrl` | string   | URL ya kupakua chanzo (inaweza kuwa tupu)          |
| `repository`  | string?  | URL ya hiari ya hazina                             |
| `tags`        | string[] | Lebo za kutafuta/kuchuja                           |
| `downloads`   | number   | Idadi ya upakuaji                                  |
| `rating`      | number   | 0–5                                                |
| `verified`    | boolean  | Ikiwa kipengee kimetiwa alama kuwa kimethibitishwa |
| `lastUpdated` | string   | Mfuatano wa tarehe unaofanana na ISO               |

Wakati hakuna URL maalum ya rejista iliyosanidiwa, katalogi huwa `SEED_REGISTRY`
iliyojengewa ndani (kwa sasa `request-logger`, `rate-limiter`, `cost-tracker`, na
`theme-manager`). Rejista ya awali hupatikana kila wakati — ikiwa rejista ya mbali
iliyosanidiwa haifikiki, inarejesha hali isiyo ya `200`, au inarejesha maudhui
yasiyotambulika, `listMarketplacePlugins()` hurekodi onyo na kurudi kwenye orodha ya awali.

> Kumbuka: **katalogi** ya soko (kuvinjari/kutafuta) imeunganishwa kikamilifu kuanzia mwanzo hadi mwisho, lakini
> **usakinishaji** wa mbofyo mmoja kutoka kwenye katalogi bado haujatekelezwa — kitufe cha
> "Sakinisha" cha dashibodi kwenye kipengee cha soko kwa sasa huonyesha arifa ya
> "inakuja hivi karibuni". Kwa sasa usakinishaji hufanywa kupitia mtiririko wa usakinishaji
> kutoka njia ya ndani (`POST /api/plugins`) na ugunduzi kwenye diski (`POST /api/plugins/scan`).

## API ya REST

Vituo vyote vya mwisho vinahitaji uthibitishaji wa usimamizi (`requireManagementAuth`) **na**
vinaweza kufikiwa kupitia loopback pekee — `/api/plugins` na `/api/plugins/` vimeorodheshwa katika
`LOCAL_ONLY_API_PREFIXES` (`src/server/authz/routeGuard.ts`).

| Kituo cha mwisho                 | Mbinu  | Maelezo                                                                        |
| -------------------------------- | ------ | ------------------------------------------------------------------------------ |
| `/api/plugins`                   | GET    | Orodhesha programu-jalizi zilizosakinishwa (kichujio cha hiari cha `?status=`) |
| `/api/plugins`                   | POST   | Sakinisha programu-jalizi kutoka kwenye njia kamili ya ndani                   |
| `/api/plugins/scan`              | POST   | Changanua saraka ya programu-jalizi na usajili mpya                            |
| `/api/plugins/marketplace`       | GET    | Orodhesha maingizo ya katalogi ya soko                                         |
| `/api/plugins/[name]`            | GET    | Pata maelezo ya programu-jalizi iliyosakinishwa                                |
| `/api/plugins/[name]`            | DELETE | Sanidua programu-jalizi                                                        |
| `/api/plugins/[name]/activate`   | POST   | Washa (pakia + sajili vishikio)                                                |
| `/api/plugins/[name]/deactivate` | POST   | Zima (anzisha `onDeactivate`, ondoa usajili wa vishikio)                       |
| `/api/plugins/[name]/config`     | GET    | Pata usanidi wa programu-jalizi + skima ya usanidi                             |
| `/api/plugins/[name]/config`     | PUT    | Sasisha usanidi wa programu-jalizi (ukithibitishwa dhidi ya skima)             |

Kichujio cha `status` cha `GET /api/plugins` kinakubali mojawapo ya
`installed` / `active` / `inactive` / `error`. Thamani batili hurejesha `400`.

### Orodhesha programu-jalizi zilizosakinishwa

```bash
curl http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..."
```

### Sakinisha kutoka kwenye njia ya ndani

```bash
curl -X POST http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "path": "/absolute/path/to/my-plugin" }'
```

`path` lazima iwe **kamili** na haiwezi kuwa na sehemu za upitiaji za `..` au
baiti batili (inatekelezwa na Zod). Saraka chanzo lazima iwe na
`plugin.json` halali (au iwe saraka mzazi wa iliyo nayo). Ikifanikiwa, jibu ni `201` pamoja na
safu ya programu-jalizi iliyosakinishwa.

### Vinjari soko

```bash
curl http://localhost:20128/api/plugins/marketplace \
  -H "Cookie: auth_token=..."
```

### Sasisha usanidi wa programu-jalizi

```bash
curl -X PUT http://localhost:20128/api/plugins/my-plugin/config \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "config": { "level": "debug", "maxItems": 100 } }'
```

`PUT .../config` huthibitisha kila thamani iliyotolewa dhidi ya
`configSchema` ya programu-jalizi (iliyotangazwa katika manifesti): sehemu za `number` huzingatia `min`/`max`,
na sehemu za `select` lazima zilingane na `enum` iliyotangazwa. Funguo ambazo hazipo kwenye skima
zinaruhusiwa kupita.

## Usanidi

### Saraka ya programu-jalizi

Programu-jalizi huhifadhiwa chini ya saraka ya data ya OmniRoute:

```
~/.omniroute/plugins/<plugin-name>/
  ├─ plugin.json
  └─ index.js          # (au chochote ambacho manifest.main inaelekeza)
```

`getDefaultPluginDir()` (`src/lib/plugins/scanner.ts`) hutambua saraka hiyo kwa
hatua tatu:

1. **`OMNIROUTE_PLUGINS_DIR`**, inapowekwa — hutumiwa jinsi ilivyo, bila kujali thamani ya `HOME`. Hiki ni
   kidhibiti dhahiri kwa Docker/K8s, ambapo mti wa programu-jalizi huunganishwa kwa bind katika njia
   ambayo kwa kawaida haihusiani kabisa na saraka ya nyumbani ya kontena (#11827).
2. `<home>/.omniroute/plugins`, ambapo `<home>` hutokana na vigezo vya mazingira vya `HOME` / `USERPROFILE`.
3. `/tmp/.omniroute/plugins`, wakati mchakato hautoi kabisa saraka ya nyumbani.

Saraka iliyotambuliwa huandikwa kwenye kumbukumbu mara moja wakati wa kuanza kama `scanner.dir_resolved`, ikitaja
ingizo lililoshinda (`OMNIROUTE_PLUGINS_DIR`, `home`, au `no-home-fallback`) — hivyo image
inayoishia kimyakimya kwenye hatua ya 3 husema hivyo, badala ya kuripoti tu orodha tupu ya programu-jalizi.
`POST /api/plugins/scan` hugundua kila saraka ndogo iliyopo humo ambayo ina
`plugin.json` halali na kuisajili; saraka hiyo hiyo ndiyo mzizi ambamo
`pluginManager.install()` hunakili programu-jalizi, hivyo ubatilishaji huhamisha ugunduzi na
usakinishaji kwa pamoja.

> **`OMNIROUTE_PLUGINS_DIR` si `OMNIROUTE_PLUGIN_PATH`.** Ya pili husomwa tu na
> kipakiaji cha programu-jalizi za amri cha CLI (`bin/cli/plugins.mjs`) ili kupata vifurushi vya npm vya `omniroute-cmd-*`
> vinavyoongeza amri ndogo za `omniroute` — haina athari kwa kichanganuzi cha wakati wa utekelezaji
> kilichoelezwa hapa. Tazama [PLUGINS.md](./PLUGINS.md) kwa upande huo.

### URL maalum ya sajili ya soko

Chanzo cha katalogi ya soko husomwa kutoka kwenye mpangilio wa `pluginMarketplaceUrl`
(`src/lib/plugins/marketplace.ts` husoma `settings.pluginMarketplaceUrl`). Inapowekwa
kuwa URL ya `http(s)`, `listMarketplacePlugins()` huchota URL hiyo na kukubali
ama safu ya kiwango cha juu ya JSON ya maingizo au kipengee chenye safu ya `plugins`;
maingizo yasiyo na `name` ya aina ya string huondolewa. Isipowekwa (au uchotaji
ukizuiwa na kinga ya SSRF / ukirejesha jibu baya), sajili chaguo-msingi iliyojengewa ndani
hutumiwa.

Kichupo cha "Marketplace" kwenye dashibodi huonyesha sehemu ya URL hii (husomwa tena kutoka
`GET /api/settings`).

> Dokezo la utekelezaji: kitendo cha "Save" kwenye dashibodi hutuma
> `pluginMarketplaceUrl` kwa `PATCH /api/settings`. Wakati hati hii ikiandikwa, ufunguo huu
> haujatangazwa katika `updateSettingsSchema`
> (`src/shared/validation/settingsSchemas.ts`), kwa hivyo thibitisha uhifadhi wake katika
> toleo lako kabla ya kuutegemea — njia ya **kusoma** (`getSettings()` →
> `listMarketplacePlugins()`) huzingatia ufunguo huo mara tu unapokuwepo kwenye hifadhi ya
> mipangilio.

## Usalama

### Daraja la njia — loopback pekee

Programu-jalizi hutekeleza msimbo katika michakato-toto iliyoanzishwa, kwa hivyo eneo lote la `/api/plugins`
limeainishwa kuwa `LOCAL_ONLY` (Daraja la 1). Utekelezaji wa lazima wa loopback huendeshwa
bila masharti **kabla** ya ukaguzi wowote wa uthibitishaji, kwa hivyo tokeni ya usimamizi iliyovuja na kufikia
mashine kupitia handaki bado haiwezi kusakinisha, kuwezesha, au kusanidua programu-jalizi.
Angalia [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) na
Kanuni Madhubuti #15 / #17.

### Kinga ya SSRF ya sajili ya soko

URL maalum ya sajili ni usanidi unaoweza kuathiriwa na mshambuliaji, kwa hivyo kabla ya
kuichota, `listMarketplacePlugins()` huipitisha katika safu mbili:

1. **`isSafeMarketplaceUrl(url)`** (`src/lib/plugins/marketplace.ts`):
   - Hukataa chochote ambacho si `http:` / `https:`.
   - Hukataa seva pangishi halisi za faragha/loopback/link-local/ULA (IPv4 **na** IPv6,
     ikijumuisha IPv4 iliyowekwa ramani) kupitia `isPrivateHost` ya kikanoni
     (`src/shared/network/outboundUrlGuard.ts`).
   - Husuluhisha rekodi **zote mbili** za `A` na `AAAA` na hukataa ikiwa anwani
     **yoyote** iliyosuluhishwa ni ya faragha — hivyo kufunga njia ya kukwepa ya jina la seva pangishi la umma → IP ya faragha.
   - **Hukataa kwa usalama**: hitilafu ya usuluhishaji wa DNS husababisha URL kukataliwa.
2. **`safeOutboundFetch(url, { guard: "public-only", timeoutMs: 5000 })`**
   (`src/shared/network/safeOutboundFetch.ts`): hutumia tena kinga ya URL ya umma pekee
   wakati wa uchotaji na **huzuia uelekezaji upya** (hakuna mpito wa umma → faragha wa `30x`).

URL inayoshindwa katika safu yoyote kati ya hizo haisitishi ombi — soko
hurudi kimyakimya kwenye sajili ya mbegu iliyojengewa ndani na kurekodi onyo.

> Kinga hii iliimarishwa katika PR #3774 mahsusi ili kusuluhisha A + AAAA na kutumia
> `isPrivateHost` ya kikanoni badala ya ukaguzi wa IPv4 pekee.

### Utengaji wa utekelezaji wa programu-jalizi

- **Utengaji wa michakato** — `loadPlugin()` (`src/lib/plugins/loader.ts`) huanzisha
  kila programu-jalizi katika mchakato-toto tofauti wa Node.js na kuwasiliana kupitia IPC.
  Miito ya hook ina muda wa kuisha wenye ongezeko la hatua kutoka `SIGTERM` → `SIGKILL`.
- **Orodha ruhusiwa ya mazingira** — mchakato-toto hupokea tu seti iliyoruhusiwa ya vigezo vya
  mazingira; seti pana zaidi hutolewa tu wakati manifesti inapoomba ruhusa ya
  `env`.
- **Uzuiaji ndani ya njia** — usakinishaji/uboreshaji/usaniduaji huhakikisha kwamba saraka ya programu-jalizi
  na `manifest.main` zinasuluhishwa **ndani** ya mzizi wa programu-jalizi unaosimamiwa
  kabla ya kunakili au kufuta kwa kujirudia (hulinda dhidi ya njia za DB zilizochezewa na
  upitishaji wa `../` katika `manifest.main`). Uwezeshaji husuluhisha viungo vya kiishara kupitia
  `realpath` na hukataa kupakia sehemu ya kuingilia inayotoka nje ya saraka ya
  programu-jalizi.
- **Kibandiko cha hiari cha uadilifu** — manifesti inaweza kutangaza uga wa `integrity`
  (`sha256-<base64>`, umbizo la SRI). Ukiwepo, kipakiaji huthibitisha heshi ya
  faili ya kuingilia wakati wa upakiaji na hukataa kuwezesha ikiwa hazilingani. Huu ni
  ugunduzi wa uchezeaji wa hiari, **si** mpaka wa usalama — uelekezaji wa loopback pekee
  na modeli ya ruhusa ndiyo mipaka halisi.

## Manifesti (`plugin.json`)

Huthibitishwa na `PluginManifestSchema` (`src/lib/plugins/manifest.ts`):

| Uga                | Aina      | Maelezo                                                                  |
| ------------------ | --------- | ------------------------------------------------------------------------ |
| `name`             | string    | Inahitajika; kebab-case (`^[a-z0-9-]+$`), vibambo 1–100                  |
| `version`          | string    | Inahitajika; semver (`MAJOR.MINOR.PATCH`)                                |
| `description`      | string?   | Vibambo ≤ 500                                                            |
| `author`           | string?   | Vibambo ≤ 200                                                            |
| `license`          | string?   | Chaguo-msingi ni `MIT`                                                   |
| `main`             | string?   | Faili ya kuingilia; chaguo-msingi ni `index.js`                          |
| `source`           | enum?     | `local` \| `marketplace` (chaguo-msingi ni `local`)                      |
| `tags`             | string[]? | Lebo za utafutaji                                                        |
| `requires`         | object?   | `{ omniroute?, permissions[] }`                                          |
| `hooks`            | object?   | Thamani za Boolean zinazotangaza hook zinazotekelezwa na programu-jalizi |
| `skills`           | object[]? | Ufafanuzi wa hiari wa ujuzi                                              |
| `enabledByDefault` | boolean?  | Wezesha kiotomatiki wakati wa usakinishaji                               |
| `configSchema`     | object?   | Ramani ya sehemu za usanidi (`string`/`number`/`boolean`/`select`)       |
| `integrity`        | string?   | Kibandiko cha hiari cha faili ya kuingilia cha `sha256-<base64>`         |

Ruhusa huchukuliwa kutoka kwenye enum
`network` / `file-read` / `file-write` / `env` / `exec`.

## Mtiririko wa Mzunguko wa Maisha

```
install (POST /api/plugins, njia)
  → changanua/thibitisha manifest → nakili hadi staging → hakikisha main ipo ndani ya saraka
  → badilisha jina atomiki kwenda ~/.omniroute/plugins/<name> → ingiza safu ya DB
  → anzisha onInstall → ikiwa enabledByDefault: activate

activate (POST /api/plugins/{name}/activate)
  → ukaguzi wa realpath kuhakikisha ipo ndani → loadPlugin() (anzisha mchakato mtoto)
  → sajili hooks zilizotangazwa → status = "active" → anzisha onActivate

deactivate (POST /api/plugins/{name}/deactivate)
  → anzisha onDeactivate (KABLA ya kufuta usajili) → futa usajili wa hooks
  → sitisha mchakato mtoto → status = "inactive"

uninstall (DELETE /api/plugins/{name})
  → deactivate ikiwa active → anzisha onUninstall
  → ufutaji wa kujirudia uliokaguliwa kuhakikisha unabaki ndani ya saraka ya programu-jalizi → futa safu ya DB
```

Kuendesha tena `install` dhidi ya saraka ambayo toleo la manifest ni **jipya
kabisa** kuliko toleo lililosakinishwa husasisha kiotomatiki (usakinishaji upya safi; usanidi huwekwa upya
kwenye chaguomsingi). Toleo sawa au la zamani zaidi hukataliwa.

## Hifadhidata

Jedwali `plugins` (uhamishaji `076_create_plugins.sql`):

| Safu            | Aina    | Maelezo                                                           |
| --------------- | ------- | ----------------------------------------------------------------- |
| `id`            | TEXT PK | UUID                                                              |
| `name`          | TEXT    | Ya kipekee                                                        |
| `version`       | TEXT    | semver; chaguomsingi `1.0.0`                                      |
| `description`   | TEXT    | Si lazima                                                         |
| `author`        | TEXT    | Si lazima                                                         |
| `license`       | TEXT    | Chaguomsingi `MIT`                                                |
| `main`          | TEXT    | Faili ya kuingilia; chaguomsingi `index.js`                       |
| `source`        | TEXT    | Chaguomsingi `local`                                              |
| `tags`          | TEXT    | Safu ya JSON; chaguomsingi `[]`                                   |
| `status`        | TEXT    | `installed` \| `active` \| `inactive` \| `error`                  |
| `enabled`       | INT     | 0/1; chaguomsingi 0                                               |
| `manifest`      | TEXT    | JSON kamili ya manifest                                           |
| `config`        | TEXT    | JSON; chaguomsingi `{}`                                           |
| `config_schema` | TEXT    | JSON; chaguomsingi `{}`                                           |
| `hooks`         | TEXT    | Safu ya JSON ya majina ya hooks yaliyotangazwa; chaguomsingi `[]` |
| `permissions`   | TEXT    | Safu ya JSON; chaguomsingi `[]`                                   |
| `plugin_dir`    | TEXT    | Saraka kamili ya usakinishaji                                     |
| `error_message` | TEXT    | Huwekwa wakati `status = "error"`                                 |
| `installed_at`  | TEXT    | `datetime('now')`                                                 |
| `updated_at`    | TEXT    | `datetime('now')`                                                 |
| `activated_at`  | TEXT    | Huwekwa wakati wa uanzishaji                                      |

Vipimo/uchanganuzi wa programu-jalizi hufuatiliwa katika majedwali ya ziada
(`090_plugin_metrics.sql`, `091_plugin_analytics.sql`).

## Dashibodi

Ukurasa wa dashibodi uliopo `/dashboard/plugins`
(`src/app/(dashboard)/dashboard/plugins/page.tsx`) una vichupo viwili:

- **Zilizosakinishwa** — huorodhesha programu-jalizi zilizosakinishwa pamoja na hooks zake zilizotangazwa, swichi ya
  kuanzisha/kuzima, kitufe cha kusanidua, na kitendo cha "Changanua programu-jalizi"
  (`POST /api/plugins/scan`).
- **Soko** — huonyesha katalogi kutoka `GET /api/plugins/marketplace` pamoja na uga wa
  kuweka URL maalum ya registry.

Ukurasa wa usanidi wa kila programu-jalizi unapatikana `/dashboard/plugins/[name]/config`
(`src/app/(dashboard)/dashboard/plugins/[name]/config/page.tsx`).

## Tazama Pia

- [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) —
  kwa nini `/api/plugins` inapatikana tu kupitia loopback (Tier 1)
- [`docs/frameworks/SKILLS.md`](./SKILLS.md) — mfumo unaohusiana wa ujuzi
  (`src/lib/skills/`); programu-jalizi zinaweza kutangaza ujuzi katika manifest yao
- [`docs/frameworks/WEBHOOKS.md`](./WEBHOOKS.md) — miunganisho ya nje
  inayoendeshwa na matukio
- [`docs/security/ERROR_SANITIZATION.md`](../security/ERROR_SANITIZATION.md) —
  muundo wa `buildErrorBody()` ambao kila njia ya programu-jalizi hutumia kwa majibu ya hitilafu
