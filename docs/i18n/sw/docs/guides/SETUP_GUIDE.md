# 📖 Setup Guide — OmniRoute (Kiswahili)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/SETUP_GUIDE.md) · 🇪🇹 [am](../../../am/docs/guides/SETUP_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/guides/SETUP_GUIDE.md) · 🇦🇿 [az](../../../az/docs/guides/SETUP_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/guides/SETUP_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/guides/SETUP_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/guides/SETUP_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/guides/SETUP_GUIDE.md) · 🇩🇰 [da](../../../da/docs/guides/SETUP_GUIDE.md) · 🇩🇪 [de](../../../de/docs/guides/SETUP_GUIDE.md) · 🇬🇷 [el](../../../el/docs/guides/SETUP_GUIDE.md) · 🇪🇸 [es](../../../es/docs/guides/SETUP_GUIDE.md) · 🇪🇪 [et](../../../et/docs/guides/SETUP_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/guides/SETUP_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/guides/SETUP_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/guides/SETUP_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/guides/SETUP_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/guides/SETUP_GUIDE.md) · 🇮🇱 [he](../../../he/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/guides/SETUP_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/guides/SETUP_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/guides/SETUP_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/guides/SETUP_GUIDE.md) · 🇮🇩 [id](../../../id/docs/guides/SETUP_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/guides/SETUP_GUIDE.md) · 🇮🇹 [it](../../../it/docs/guides/SETUP_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/guides/SETUP_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/guides/SETUP_GUIDE.md) · 🇰🇭 [km](../../../km/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/guides/SETUP_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/guides/SETUP_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/guides/SETUP_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/guides/SETUP_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/guides/SETUP_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/guides/SETUP_GUIDE.md) · 🇲🇲 [my](../../../my/docs/guides/SETUP_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/guides/SETUP_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/guides/SETUP_GUIDE.md) · 🇳🇴 [no](../../../no/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [or](../../../or/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/guides/SETUP_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/guides/SETUP_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/guides/SETUP_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/guides/SETUP_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/SETUP_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/guides/SETUP_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/guides/SETUP_GUIDE.md) · 🇱🇰 [si](../../../si/docs/guides/SETUP_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/guides/SETUP_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/guides/SETUP_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/guides/SETUP_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [te](../../../te/docs/guides/SETUP_GUIDE.md) · 🇹🇭 [th](../../../th/docs/guides/SETUP_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/guides/SETUP_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/SETUP_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/guides/SETUP_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/guides/SETUP_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/guides/SETUP_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/guides/SETUP_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/SETUP_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/SETUP_GUIDE.md)

---

> Marejeleo kamili ya usanidi wa OmniRoute. Kwa toleo la haraka, angalia [Mwongozo wa Kuanza Haraka katika README](../README.md#-quick-start).

## Jedwali la Yaliyomo

- [Mbinu za Usakinishaji](#install-methods)
- [Usanidi wa Zana ya CLI](#cli-tool-configuration)
- [Usanidi wa Itifaki (MCP + A2A)](#protocol-setup-mcp--a2a)
- [Usanidi wa Muda wa Kuisha](#timeout-configuration)
- [Hali ya Kugawanya Bandari](#split-port-mode)
- [Void Linux (xbps-src)](#void-linux-xbps-src-template)
- [Kuondoa Usakinishaji](#uninstalling)

---

## Mbinu za Usakinishaji

### npm (inapendekezwa)

```bash
npm install -g omniroute
omniroute
```

Dashibodi hufunguka kwenye `http://localhost:20128` na URL ya msingi ya API ni `http://localhost:20128/v1`.

### pnpm

```bash
pnpm add -g omniroute@latest --allow-build=better-sqlite3 --allow-build=@swc/core
omniroute
```

> **Watumiaji wa pnpm:** alama ya `--allow-build` inahitajika ili kuwezesha hati asilia za ujenzi kwa `better-sqlite3` na `@swc/core`. Amri ya `pnpm approve-builds -g` haitumiki kwa usakinishaji wa kimataifa kwenye pnpm v11.

### Arch Linux (AUR)

```bash
yay -S omniroute-bin
systemctl --user enable --now omniroute.service
```

[Kifurushi cha AUR](https://aur.archlinux.org/packages/omniroute-bin) husakinisha OmniRoute na kutoa huduma ya mtumiaji ya systemd.

### Kutoka kwenye Msimbo Chanzo

```bash
npm install
PORT=20128 DASHBOARD_PORT=20129 NEXT_PUBLIC_BASE_URL=http://localhost:20129 npm run dev
```

> **Dokezo la Windows:** Kwa chaguo-msingi, OmniRoute hutumia `%APPDATA%\omniroute` wakati saraka ya zamani ya `%USERPROFILE%\.omniroute` haipo. Weka `DATA_DIR` ili kuchagua eneo tofauti la saraka ya data.

> **Dokezo:** `npm install` huzalisha kiotomatiki `.env` kutoka `.env.example` wakati wa uendeshaji wa kwanza. Usakinishaji unaofuata hautaandika juu ya `.env` iliyopo, kwa hivyo marekebisho yanahifadhiwa. Ili kuizalisha upya, futa `.env` kabla ya kuiendesha tena.

### Docker

Angalia [Mwongozo wa Docker](./DOCKER_GUIDE.md) kwa usanidi kamili wa Docker, ikijumuisha wasifu wa Compose na Caddy HTTPS.

### Programu ya Kompyuta ya Mezani (Electron)

OmniRoute huja na programu ya kompyuta ya mezani iliyojengwa kwa Electron 41 + electron-builder 26.10. Hati zinazopatikana (mzizi wa workspace):

```bash
npm run electron:dev          # Endesha programu ya kompyuta ya mezani kwa upakiaji upya wa moja kwa moja
npm run electron:build        # Jenga kwa OS ya sasa (hugunduliwa kiotomatiki)
npm run electron:build:win    # Kisakinishaji cha Windows (NSIS + portable)
npm run electron:build:mac    # macOS (dmg + zip, arm64+x64)
npm run electron:build:linux  # Linux (AppImage + deb + rpm)
npm run electron:smoke:packaged  # Jaribu kwa ufupi kifurushi kilichojengwa
```

Matoleo ya visakinishaji vya programu ya kompyuta ya mezani huambatishwa kwenye GitHub Releases. Kwa maelezo kamili kuhusu Electron (utiasaini, daraja la IPC, distros), angalia [`ELECTRON_GUIDE.md`](./ELECTRON_GUIDE.md) _(iliundwa katika hatua ya baadaye)_.

### Seva isiyo na kiolesura (CI/otomatiki)

Kwa usanidi usiohitaji usimamizi (Docker, Kubernetes, CI), tumia:

```bash
omniroute setup --non-interactive
omniroute providers test-batch
```

Ikiunganishwa na vigeu vya mazingira (`INITIAL_PASSWORD`, `OMNIROUTE_WS_BRIDGE_SECRET`, n.k.), hii hukuwezesha kuanzisha tukio la OmniRoute linaloweza kuendeshwa kikamilifu kwa hati.

### Chaguo za CLI

| Amri                    | Maelezo                                                                                    |
| ----------------------- | ------------------------------------------------------------------------------------------ |
| `omniroute`             | Anzisha seva (`PORT=20128`, API na dashibodi kwenye bandari moja)                          |
| `omniroute setup`       | Mwongozo wa CLI wa kuweka nenosiri na mtoa huduma wa kwanza                                |
| `omniroute doctor`      | Endesha ukaguzi wa afya wa ndani bila kuanzisha seva                                       |
| `omniroute providers`   | Gundua, orodhesha, thibitisha na ujaribu watoa huduma kupitia CLI                          |
| `omniroute config`      | Usanidi wa zana ya CLI — orodhesha, pata, weka na uthibitishe usanidi                      |
| `omniroute status`      | Dashibodi ya hali nje ya mtandao — toleo, DB, zana na usanidi                              |
| `omniroute logs`        | Tiririsha kumbukumbu za matumizi kutoka API (inaauni `--follow`)                           |
| `omniroute update`      | Angalia au tekeleza masasisho ya OmniRoute                                                 |
| `omniroute provider`    | Dhibiti miunganisho ya watoa huduma — ongeza, orodhesha, ondoa, jaribu, weka chaguo-msingi |
| `omniroute --port 3000` | Weka bandari kanoni/API kuwa 3000                                                          |
| `omniroute --mcp`       | Anzisha seva ya MCP (usafirishaji wa stdio)                                                |
| `omniroute --no-open`   | Usifungue kivinjari kiotomatiki                                                            |
| `omniroute --help`      | Onyesha msaada                                                                             |

Usanidi usio na kiolesura unaweza kuendeshwa kwa hati kwa kutumia alama au vigeu vya mazingira:

```bash
omniroute setup --non-interactive --password "$OMNIROUTE_PASSWORD"
omniroute setup --non-interactive --add-provider --provider openai --api-key "$OPENAI_API_KEY"
omniroute setup --non-interactive --add-provider --provider openai --api-key "$OPENAI_API_KEY" --test-provider
```

Endesha uchunguzi wa ndani bila kufungua dashibodi:

```bash
omniroute doctor
omniroute doctor --json
omniroute doctor --no-liveness
```

Dhibiti watoa huduma kupitia SSH au hati bila kufungua dashibodi:

```bash
omniroute providers available
omniroute providers available --search openai
omniroute providers available --category api-key
omniroute providers list
omniroute providers test <id-or-name>
omniroute providers test-all
omniroute providers validate
```

---

## Usanidi wa Zana ya CLI

### 1) Unganisha Watoa Huduma na Uunde Ufunguo wa API

1. Fungua Dashboard → `Providers` na uunganishe angalau mtoa huduma mmoja (OAuth au ufunguo wa API).
2. Fungua Dashboard → `Endpoints` na uunde ufunguo wa API.
3. (Si lazima) Fungua Dashboard → `Combos` na uweke mnyororo wako wa mbadala.

### 2) Elekeza Zana Yako ya Uandishi wa Msimbo

```txt
URL ya Msingi: http://localhost:20128/v1
Ufunguo wa API:  [nakili kutoka ukurasa wa Endpoint]
Modeli:          if/qwen3.8-max-preview (au kiambishi awali chochote cha mtoa huduma/modeli)
```

Ikiwa kihariri chako hakiwezi kutuma `Authorization: Bearer ...`, tumia anwani ya msingi ya uoanifu yenye tokeni badala yake:

```txt
URL ya Msingi: http://localhost:20128/api/v1/vscode/YOUR_KEY/
URL ya Modeli: http://localhost:20128/api/v1/vscode/YOUR_KEY/models
URL ya Gumzo: http://localhost:20128/api/v1/vscode/YOUR_KEY/chat/completions
URL ya Lebo za Ollama: http://localhost:20128/api/v1/vscode/YOUR_KEY/api/tags
```

Hufanya kazi na Claude Code, Codex CLI, Cursor, Cline, OpenClaw, OpenCode, na SDK zinazooana na OpenAI.

#### Sanidi kiotomatiki kwa `setup-*`

Badala ya kubandika URL ya msingi na ufunguo mwenyewe, ruhusu OmniRoute kuandika usanidi wa kila zana kutoka kwenye katalogi hai ya modeli. Amri moja kwa kila zana:

```bash
omniroute setup-codex        # wasifu wa ~/.codex/<name>.config.toml
omniroute setup-claude       # ~/.claude/profiles/<name>/settings.json
omniroute setup-opencode     # ~/.config/opencode/opencode.json (inaoana na OpenAI)
omniroute setup-cline        # Mipangilio ya Cline CLI + kiendelezi cha VS Code
omniroute setup-kilo         # Kilo Code
omniroute setup-continue     # ~/.continue/config.yaml (Continue / cn)
omniroute setup-cursor       # huchapisha hatua za ndani ya programu ya Cursor
omniroute setup-roo          # Uingizaji wa Roo Code + kielekezi cha autoImport
omniroute setup-crush        # ~/.config/crush/crush.json
omniroute setup-goose        # ~/.config/goose/config.yaml
omniroute setup-aider        # ~/.aider.conf.yml
omniroute setup-qwen         # ~/.qwen/settings.json + ~/.qwen/.env
```

Kila moja hukubali `--remote <url> --api-key <key>` ili kusanidi zana ya ndani dhidi ya OmniRoute ya **mbali**, pamoja na `--dry-run` ili kuona hakikisho. Ili kuanzisha CLI ikiwa mazingira yanayofaa yameingizwa bila kuandika usanidi wowote, tumia kizinduzi cha jumla `omniroute run <target>` (claude, codex, aider, goose, opencode, qwen, gemini); vizinduzi vya zamani vya kila zana `omniroute launch` (Claude Code) na `omniroute launch-codex` (Codex) bado vinapatikana.

Kwa jedwali kamili (kile ambacho kila amri huandika, kila alama, ya ndani dhidi ya ya mbali, na kanuni za `/v1` za URL ya msingi), tazama **[Miunganisho ya CLI](./CLI-INTEGRATIONS.md)**.

Kwa usanidi wa kina wa kila zana (Claude Code, Codex CLI, Cursor, Cline, OpenClaw, Kilo Code, Copilot, na nyinginezo), tazama **[Mwongozo wa Zana za CLI](../reference/CLI-TOOLS.md)** uliotengwa.

---

## Usanidi wa Itifaki (MCP + A2A)

### Usanidi wa MCP (Itifaki ya Muktadha wa Modeli)

Anzisha usafirishaji wa MCP katika modi ya stdio:

```bash
omniroute --mcp
```

Mtiririko unaopendekezwa wa uthibitishaji:

```bash
# 1. Anzisha seva ya MCP
omniroute --mcp

# 2. Kutoka kwa kiteja chako cha MCP, ita:
omniroute_get_health        # Inapaswa kurejesha hali ya mfumo
omniroute_list_combos       # Inapaswa kurejesha combos zinazotumika

# 3. Au endesha mkusanyiko kamili wa E2E:
npm run test:protocols:e2e
```

#### Usanidi wa Kiteja cha MCP

**Claude Code:**

```bash
claude mcp add-server omniroute --type http --url http://localhost:20128/api/mcp/stream
```

**Cursor / Cline:**

Ongeza kwenye mipangilio yako ya MCP:

```json
{
  "mcpServers": {
    "omniroute": {
      "command": "omniroute",
      "args": ["--mcp"],
      "env": {}
    }
  }
}
```

**Nyaraka kamili za MCP:** [README ya Seva ya MCP](../../open-sse/mcp-server/README.md) — zana 110, usanidi wa IDE, viteja vya Python/TS/Go.

### Usanidi wa A2A (Itifaki ya Wakala-kwa-Wakala)

Thibitisha Kadi ya Wakala:

```bash
curl http://localhost:20128/.well-known/agent.json
```

Tuma jukumu:

```bash
curl -X POST http://localhost:20128/a2a \
  -H 'content-type: application/json' \
  -d '{"jsonrpc":"2.0","id":"quickstart","method":"message/send","params":{"skill":"quota-management","messages":[{"role":"user","content":"Give me a short quota summary."}]}}'
```

**Nyaraka kamili za A2A:** [README ya Seva ya A2A](../../src/lib/a2a/README.md) — JSON-RPC 2.0, ujuzi, utiririshaji, mzunguko wa maisha wa jukumu.

---

## Usanidi wa Muda wa Kuisha

### Muda wa Msingi wa Kuisha

Kwa usambazaji mwingi, unahitaji vigeu hivi viwili pekee:

| Kigeu                    | Chaguo-msingi                | Madhumuni                                                                                                                                                          |
| ------------------------ | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `REQUEST_TIMEOUT_MS`     | `600000`                     | Msingi wa pamoja wa muda wa kuisha kwa kuanza kwa jibu la upstream, muda fiche wa Undici, maombi ya alama ya utambulisho ya TLS, na maombi/proksi za daraja la API |
| `STREAM_IDLE_TIMEOUT_MS` | hurithi `REQUEST_TIMEOUT_MS` | Pengo la juu zaidi kati ya vipande vya mtiririko kabla OmniRoute haijakatisha mtiririko wa SSE                                                                     |

Utangamano wa nyuma umehifadhiwa: `FETCH_TIMEOUT_MS`, `API_BRIDGE_PROXY_TIMEOUT_MS`, na vigeu vingine vilivyopo vya muda wa kuisha kwa kila safu bado vinafanya kazi na vinabatilisha msingi wa pamoja.

### Vidokezo Mahususi kwa Watoa Huduma

Kwa upstream zinazooana na Claude Code (`anthropic-compatible-cc-*`), OmniRoute hutengeneza kichwa cha nje cha `X-Stainless-Timeout` kutokana na muda wa kuisha wa fetch uliotatuliwa ili muda wa kuisha wa usomaji upande wa mtoa huduma uendelee kuambatana na usanidi wako wa mazingira.

Kwa proksi geuzi za wahusika wengine zinazooana na Claude Code, OmniRoute huweka mkusanyiko chaguo-msingi wa `anthropic-beta` kuwa wa tahadhari na, wakati `Client Cache Control` imeachwa kwenye `Auto`, husambaza tu viashirio vya `cache_control` vilivyotolewa na mteja. Washa kigeuzi cha kila muunganisho cha "Enable redact-thinking beta" tu wakati upstream inahitaji mahususi mitiririko ya fikra iliyofichwa ya Claude.

### Ubatilishaji wa Kina wa Muda wa Kuisha

| Kigeu                                    | Chaguo-msingi                              | Madhumuni                                                                                   |
| ---------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------- |
| `FETCH_TIMEOUT_MS`                       | hurithi `REQUEST_TIMEOUT_MS`               | Muda wa kuisha wa kuanza kwa jibu la upstream unaotumika hadi vichwa vya jibu vifike        |
| `FETCH_HEADERS_TIMEOUT_MS`               | hurithi `FETCH_TIMEOUT_MS`                 | Kikomo cha muda cha Undici cha kupokea vichwa vya jibu la upstream                          |
| `FETCH_BODY_TIMEOUT_MS`                  | hurithi `FETCH_TIMEOUT_MS`                 | Kikomo cha muda cha Undici kati ya vipande vya mwili wa upstream (`0` hukizima)             |
| `FETCH_CONNECT_TIMEOUT_MS`               | `30000`                                    | Muda wa kuisha wa muunganisho wa TCP wa Undici                                              |
| `FETCH_KEEPALIVE_TIMEOUT_MS`             | `4000`                                     | Muda wa kuisha wa soketi ya keep-alive ya Undici ikiwa haitumiki                            |
| `TLS_CLIENT_TIMEOUT_MS`                  | hurithi `FETCH_TIMEOUT_MS`                 | Muda wa kuisha kwa maombi ya alama ya utambulisho ya TLS yaliyofanywa kupitia `wreq-js`     |
| `API_BRIDGE_PROXY_TIMEOUT_MS`            | hurithi `REQUEST_TIMEOUT_MS` au `600000`   | Muda wa kuisha wa usambazaji wa proksi ya `/v1` kutoka porti ya API hadi porti ya dashibodi |
| `API_BRIDGE_SERVER_REQUEST_TIMEOUT_MS`   | `max(API_BRIDGE_PROXY_TIMEOUT_MS, 300000)` | Muda wa kuisha wa ombi linaloingia kwenye seva ya daraja la API                             |
| `API_BRIDGE_SERVER_HEADERS_TIMEOUT_MS`   | `60000`                                    | Muda wa kuisha wa vichwa vinavyoingia kwenye seva ya daraja la API                          |
| `API_BRIDGE_SERVER_KEEPALIVE_TIMEOUT_MS` | `5000`                                     | Muda wa kuisha wa keep-alive kwenye seva ya daraja la API                                   |
| `API_BRIDGE_SERVER_SOCKET_TIMEOUT_MS`    | `0`                                        | Muda wa kuisha kwa kutotumika kwa soketi kwenye seva ya daraja la API (`0` hukizima)        |

> **Kumbuka:** Kwa maombi ya mtiririko, `FETCH_TIMEOUT_MS` hushughulikia tu kuanzisha muunganisho / kusubiri jibu la kwanza la upstream. Mtiririko ukishaanza kufanya kazi, OmniRoute itakatisha tu kunapotokea kusimama halisi (`STREAM_IDLE_TIMEOUT_MS`) au kutotumika kwa mwili wa Undici (`FETCH_BODY_TIMEOUT_MS`).

### Utangamano wa Proksi Geuzi

Ukiendesha OmniRoute nyuma ya Nginx, Caddy, Cloudflare, au proksi nyingine geuzi, hakikisha muda wa kuisha wa proksi pia ni mrefu kuliko muda wa kuisha wa mtiririko/fetch wa OmniRoute.

---

## Hali ya Milango Iliyotenganishwa

Endesha API na Dashibodi kwenye milango tofauti kwa hali za kina (proksi ya kinyume, mitandao ya kontena):

```bash
PORT=20128 DASHBOARD_PORT=20129 omniroute
# API:       http://localhost:20128/v1
# Dashibodi: http://localhost:20129
```

---

## Kiolezo cha Void Linux (xbps-src)

Kwa watumiaji wa Void Linux, unaweza kuunda kifurushi asilia ukitumia `xbps-src`. Hifadhi sehemu hii kama `srcpkgs/omniroute/template`:

```bash
# Faili ya kiolezo cha 'omniroute'
pkgname=omniroute
version=3.8.0
revision=1
hostmakedepends="nodejs python3 make"
depends="openssl"
short_desc="Lango la AI la jumla lenye uelekezaji mahiri kwa watoa huduma wengi wa LLM"
maintainer="zenobit <zenobit@disroot.org>"
license="MIT"
homepage="https://github.com/diegosouzapw/OmniRoute"
distfiles="https://github.com/diegosouzapw/OmniRoute/archive/refs/tags/v${version}.tar.gz"
# Tengeneza upya checksum kwa kila toleo kwa kutumia:
#   curl -L -o /tmp/omniroute.tar.gz "https://github.com/diegosouzapw/OmniRoute/archive/refs/tags/v${version}.tar.gz" && sha256sum /tmp/omniroute.tar.gz
checksum=PLACEHOLDER_REGENERATE_PER_RELEASE
system_accounts="_omniroute"
omniroute_homedir="/var/lib/omniroute"
export NODE_ENV=production
export npm_config_engine_strict=false
export npm_config_loglevel=error
export npm_config_fund=false
export npm_config_audit=false

do_build() {
	local _gyp_arch
	case "$XBPS_TARGET_MACHINE" in
		aarch64*) _gyp_arch=arm64 ;;
		armv7*|armv6*) _gyp_arch=arm ;;
		i686*) _gyp_arch=ia32 ;;
		*) _gyp_arch=x64 ;;
	esac

	NODE_ENV=development npm ci --ignore-scripts
	npm run build
	cp -r .next/static .next/standalone/.next/static
	[ -d public ] && cp -r public .next/standalone/public || true

	local _node_gyp=/usr/lib/node_modules/npm/node_modules/node-gyp/bin/node-gyp.js
	(cd node_modules/better-sqlite3 && node "$_node_gyp" rebuild --arch="$_gyp_arch")

	local _bs3_release=.next/standalone/node_modules/better-sqlite3/build/Release
	mkdir -p "$_bs3_release"
	cp node_modules/better-sqlite3/build/Release/better_sqlite3.node "$_bs3_release/"

	rm -rf .next/standalone/node_modules/@img

	for _mod in pino-abstract-transport split2 process-warning; do
		cp -r "node_modules/$_mod" .next/standalone/node_modules/
	done
}

do_check() {
	npm run test:unit
}

do_install() {
	vmkdir usr/lib/omniroute/.next
	vcopy .next/standalone/. usr/lib/omniroute/.next/standalone

	for _d in \
		.next/standalone/.next/server/app/dashboard \
		.next/standalone/.next/server/app/dashboard/settings \
		.next/standalone/.next/server/app/dashboard/providers; do
		touch "${DESTDIR}/usr/lib/omniroute/${_d}/.keep"
	done

	cat > "${WRKDIR}/omniroute" <<'EOF'
#!/bin/sh
export PORT="${PORT:-20128}"
export DATA_DIR="${DATA_DIR:-${XDG_DATA_HOME:-${HOME}/.local/share}/omniroute}"
export APP_LOG_TO_FILE="${APP_LOG_TO_FILE:-false}"
mkdir -p "${DATA_DIR}"
exec node /usr/lib/omniroute/.next/standalone/server.js "$@"
EOF
	vbin "${WRKDIR}/omniroute"
}

post_install() {
	vlicense LICENSE
}
```

---

## Kuondoa Usakinishaji

| Amri                     | Kitendo                                                                                  |
| ------------------------ | ---------------------------------------------------------------------------------------- |
| `npm run uninstall`      | Huondoa programu ya mfumo lakini **huhifadhi DB na usanidi wako** katika `~/.omniroute`. |
| `npm run uninstall:full` | Huondoa programu NA **hufuta kabisa usanidi, funguo na hifadhidata zote**.               |

> Kwa maagizo ya kina ya kuondoa usakinishaji kwa mbinu zote, angalia [UNINSTALL.md](./UNINSTALL.md).
