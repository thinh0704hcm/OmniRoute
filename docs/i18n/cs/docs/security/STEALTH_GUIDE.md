# Stealth Guide (Čeština)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇩🇰 [da](../../../da/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇮🇩 [id](../../../id/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇮🇹 [it](../../../it/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Zdroj pravdy:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Poslední aktualizace:** 2026-09-02 — v3.8.51
> **Cílová skupina:** Vývojáři spravující stealth integrace specifické pro jednotlivé poskytovatele.

OmniRoute se integruje s poskytovateli, jejichž hraniční infrastruktura aktivně vytváří otisky neoficiálních klientů (TLS JA3/JA4, pořadí hlaviček, struktura těla JSON, tokeny integrity). Tato stránka dokumentuje stealth prvky, které OmniRoute zpřístupňuje, a místa jejich implementace.

## Právní a etické upozornění

Stealth funkce existují proto, aby OmniRoute mohla fungovat jako vrstva kompatibility mezi oficiálními účty vlastněnými uživateli (Claude Code CLI, Codex, Antigravity, Cursor atd.) a jednotným API OmniRoute. **Nejsou** určeny k obcházení detekce podvodů, sdílení přihlašovacích údajů ani porušování podmínek poskytovatelů. Správci očekávají, že provozovatelé budou dodržovat podmínky upstreamových služeb, které přijali při vytváření účtů.

---

## Vrstva vytváření otisků TLS

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Trvalé relace `wreq-js` se vytvářejí líně pro každý rozsah účtu a přeložený proxy server. Celoprocesní fond `TlsClient` obsahuje nejvýše 128 relací, které pro upstreamy chráněné službou Cloudflare napodobují **Chrome 124 v systému macOS**. `TlsClient.fetch()` bezpečně selže, když nativní běhové prostředí není dostupné; volající může mimo tento obal explicitně vybrat záložní řešení.

- Profil relace: `browser: "chrome_124", os: "macos"`
- Překlad proxy (podle priority): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (také varianty s malými písmeny)
- Časový limit: `TLS_CLIENT_TIMEOUT_MS` (dědí se z `FETCH_TIMEOUT_MS`, výchozí hodnota 600000)
- Response z `wreq-js` je kompatibilní s fetch (`headers`, `text()`, `json()`, `clone()`, `body`).
- Kontrolní mechanismus prvního bajtu (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()` se vyhodnotí, jakmile dorazí upstreamové hlavičky, takže samotný `TLS_CLIENT_TIMEOUT_MS` nemůže omezit tělo, které nikdy neposkytne první bajt. `guardTlsFirstByte()` nechá soupeřit první `read()` těla s `TLS_FIRST_BYTE_WATCHDOG_MS` (výchozí hodnota `10000`, hodnota `0` jej deaktivuje); správně fungující tělo není ovlivněno, zatímco uvízlé tělo zruší čtečku wreq a umožní existující logice záložního TLS v `proxyFetch` přejít na přímý/proxy dispečer (požadavek, který nelze bezpečně zopakovat, například POST s tělem, přesto vyvolá výjimku namísto tichého opakování).

### Transport poskytovatelů s webovými cookies — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts` je sdílený adaptér pro pět níže uvedených specializovaných transportů s webovými cookies. Každý tenký obal poskytovatele vybírá profil prohlížeče/OS. Adaptér používá jediný zavaděč běhového prostředí wreq a fond transportů v `open-sse/utils/tlsClient.ts`, klíčovaný podle profilu + OS + přeloženého proxy serveru, přičemž každý požadavek používá `cookieMode: "ephemeral"`. Účty a požadavky tedy sdílejí připojení na úrovni transportu, nikdy však relaci wreq ani úložiště cookies.

| Poskytovatel | Profil        | Emulovaný OS | Zásady pro EOF streamu                           |
| ------------ | ------------- | ------------ | ------------------------------------------------ |
| Claude       | `chrome_146`  | Linux        | zahrnout `[DONE]`                                |
| Perplexity   | `firefox_148` | macOS        | zahrnout `event: end_of_stream`                  |
| Grok         | `chrome_146`  | Linux        | vyloučit `[DONE]`                                |
| Notion       | `chrome_146`  | Windows      | zahrnout `[DONE]`                                |
| LMArena      | `chrome_146`  | Windows      | bez sentinelové hodnoty; zavřít při nativním EOF |

- Streamování spotřebovává nativní `ReadableStream` odpovědi přímo; nevytváří se žádný dočasný soubor ani pomocný proces.
- Před zpřístupněním streamu se kontroluje až 256 počátečních bajtů. Poskytovatelé SSE ukládají chyby mimo SSE do vyrovnávací paměti; Grok/LMArena mapují výzvy Cloudflare na `403` a mezilehlé stránky HTML na `502`.
- Nativní časový limit požadavku zůstává ohraničen absolutní pevnou lhůtou v JS. Uvíznutí zneplatní a zavře pouze dotčený transport profilu/OS/proxy, než jej další požadavek znovu vytvoří.
- Priorita překladu proxy je `proxyUrl` pro konkrétní volání → kontext účtu/ovládacího panelu omezený na požadavek → `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (včetně variant s malými písmeny). Chyby překladu vedou k bezpečnému selhání, aby nedošlo k úniku přímého připojení. LMArena záměrně provádí překlad vůči `arena.ai`.
- `byteResponse` vrací URL `data:` s uvedeným typem obsahu bez poškození UTF-8.
- Chyby jsou `TlsClientUnavailableError` (balíček/doplněk není dostupný), `TlsClientHangError` (překročena lhůta) a `WreqTransportCapacityError` (sdílený chybový kód kapacity relací), když je všech 128 omezených slotů profilu/OS/proxy aktivních nebo se zavírá.

Výše uvedená obecná relace `TlsClient` zůstává specializovaná na trvalý stav cookies spravovaný prohlížečem. Obě cesty opakovaně používají jeden zavaděč modulu wreq s mezipamětí a jeden hook životního cyklu procesu; jejich fondy zůstávají oddělené, protože jejich životnosti cookies se záměrně liší.

Profily jsou podporovány připnutým balíčkem, ale skutečné přijetí systémem WAF se může měnit nezávisle na lokálních testech kontraktu. Před deklarováním shody s upstreamovým prohlížečem ověřte změny otisku pomocí explicitně autorizovaného živého účtu.

---

## Balíček utajení Claude Code

Když je zapnutý režim `cliCompatMode`, OmniRoute upravuje odchozí požadavky Claude tak, aby byly nerozeznatelné od provozu `claude-cli`. Spolupracují na tom tři moduly:

### `claudeCodeFingerprint.ts`

Vypočítává tříznakový otisk `cc_version` vložený do fakturační hlavičky:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (pevně zakódováno; odpovídá oficiálnímu klientovi)
- Vstupy: znaky na indexech 4, 7 a 20 textu první uživatelské zprávy + řetězec verze
- Výstup: tříznakový hexadecimální prefix

### `claudeCodeCCH.ts` (hash obsahu klienta)

Kontrola integrity na straně serveru, kterou oficiální CLI Claude Code počítá prostřednictvím Bun/Zig. OmniRoute ji znovu implementuje pomocí `xxhash-wasm`:

1. Serializovat tělo se zástupným symbolem `cch=00000;`
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. Pětiznaková hexadecimální hodnota malými písmeny doplněná zleva nulami
4. Nahradit `cch=00000;` vypočítaným tokenem

Konstanty:

- Seed: `0x6e52736ac806831e`
- Vzor: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Vkládá znak Unicode **spojovník s nulovou šířkou** (`U+200D`) za první znak „citlivých“ názvů klientů, aby je upstreamové filtry nemohly vyhledat pomocí grep. Výchozí seznam slov:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Používá se na: bloky `system`, veškerý obsah `messages[].content` a `tools[].description` / `tools[].function.description`. Operátor může seznam přepsat pomocí `setSensitiveWords()`.

### `claudeCodeCompatible.ts` — poskytovatelé `anthropic-compatible-cc-*`

Pro relé Anthropic třetích stran, která přijímají pouze provoz „skutečného Claude Code“:

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- Výchozí hodnota `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"`
- Přepínač „Enable redact-thinking beta“ pro jednotlivá připojení přidá `redact-thinking-2026-02-12`, pokud konkrétní upstream kompatibilní s CC vyžaduje proudy uvažování s redigovaným obsahem
- Přepínač „Enable summarized thinking display“ pro jednotlivá připojení ukládá `providerSpecificData.requestDefaults.summarizeThinking` a přidává `display: "summarized"` do požadavků na uvažování kompatibilních s CC, které dosud nemají nastavený režim zobrazení
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (rodina Opus/Sonnet 4.x)
- Výchozí cesta: `/v1/messages?beta=true`

Sesterské moduly ve stejném balíčku:

- `claudeCodeConstraints.ts` — pravidla pro teplotu + řízení mezipaměti
- `claudeCodeToolRemapper.ts` — přemapování názvů nástrojů
- `claudeCodeExtraRemap.ts` — dodatečná normalizace datové části

---

## Utajení Antigravity

Požadavky Antigravity zachovávají text volajícího přesně na úrovni jednotlivých bajtů. OmniRoute nevkládá do promptů znaky s nulovou šířkou ani nepřejmenovává či nevkládá nástroje za účelem napodobení klienta IDE.

### `antigravityHeaderScrub.ts`

Před předáním požadavku odstraňuje značky SDK Stainless (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`).

### ⚠️ Riziko: `ANTIGRAVITY_CREDITS=always` (častý důvod zablokování účtu)

`ANTIGRAVITY_CREDITS=always` (zpracovávané souborem `open-sse/executors/antigravity.ts`) směruje **každý** požadavek přes placené překročení kreditů Antigravity AI (placené kredity Google), místo aby využití omezovala kvóta bezplatné úrovně Google. Tato možnost je zdokumentována jako funkce, ale představuje **zdaleka nejčastější hlášené porušení podmínek služby, se kterým se setkáváme** — po několika hodinách provozu s nastavením `=always` bylo několik účtů Google Ultra zablokováno s odpovědí `403 / "service disabled for ToS violation" / insufficient_quota`.

Vynucování probíhá na **straně společnosti Google** a OmniRoute mu nemůže nijak zabránit. Název proměnné prostředí a stávající dokumentace budí dojem, že jde o bezpečný přepínač; není tomu tak.

**Proč toto nastavení spouští detekci zneužití agresivněji než používání omezené pouze na bezplatnou úroveň:**

- Trvalé automatizované výdaje na jednom účtu Google jsou vyhodnocovány jinak než provoz v bezplatné úrovni, který po vyčerpání kvóty skončí.
- Překročení kreditů nemá žádný limit rychlosti, takže chybně nakonfigurovaný klient může během několika minut spotřebovat několik stovek USD a působit jako přeprodej API klíčů nebo provoz botů.
- Pokud více uživatelů OmniRoute souběžně čerpá kredity nad limit ze stejné externí IP adresy, signál se dále zesiluje.

**Doporučený přístup:**

1. Ponechte výchozí nastavení `ANTIGRAVITY_CREDITS=off`, pokud operátor výslovně nepřijme riziko placených kreditů a postihu účtu. Režim `retry` nejprve odešle běžný požadavek a kredity vloží nejvýše jednou po odpovídající chybě kvóty 429; režim `always` vloží kredity již při prvním požadavku.
2. **Rozložte zátěž mezi poskytovatele pomocí Auto-Combo** (`model: "auto"` nebo kombinace `kr/glm/etc`) namísto přetěžování jediného účtu Antigravity.
3. **Nastavte limity RPM pro jednotlivá připojení** na stránce úprav poskytovatele Antigravity (Dashboard → Providers → Antigravity → connection → rate limit). Pro trvalé používání představuje 30–60 RPM obhajitelnou horní hranici.
4. **Používejte stabilní upstreamovou síť spravovanou operátorem** a nesdílejte jeden účet mezi nesouvisejícími uživateli nebo pracovními úlohami.
5. **V případě zablokování**: podejte odvolání prostřednictvím `support.google.com` → „Restore Workspace/Account access“ a uveďte přesné tělo odpovědi `quota_exceeded` / `service disabled`, které společnost Google odeslala. Obnovení není zaručeno.

Referenční dokumentace proměnných prostředí popisuje dopady jednotlivých režimů kreditů na účet a výdaje.

Dotčená místa:

- `open-sse/executors/antigravity.ts` — čte `process.env.ANTIGRAVITY_CREDITS`
- `src/lib/oauth/providers/antigravity.ts` — zpracování přihlašovacích údajů
- Původní hlášení incidentu: diskuse [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## Registr otisků CLI — `open-sse/config/cliFingerprints.ts`

Tabulka pro jednotlivé poskytovatele, která pevně určuje **přesné** pořadí hlaviček a pořadí polí těla JSON zachycené ze záznamů mitmproxy oficiálních CLI. Aktuálně jsou registrovány: `codex`, `claude` a také profily odvozené za běhu v `providerHeaderProfiles.ts` pro `antigravity` a `github`.

```ts
interface CliFingerprint {
  headerOrder: string[]; // rozlišuje velká a malá písmena
  bodyFieldOrder: string[]; // klíče JSON nejvyšší úrovně
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Přepíná se pro každého poskytovatele prostřednictvím proměnných prostředí (viz níže). Když je tato funkce vypnutá, hlavičky a klíče těla se zobrazí v pořadí, v jakém je poskytly Node/JSON — takže je lze snadno identifikovat podle otisku.

---

## MITM proxy (Antigravity, Linux/macOS/Windows)

Pro CLI, jejichž binární soubory nelze přesměrovat pomocí `OPENAI_BASE_URL`, spouští OmniRoute lokální proxy ukončující TLS. Koncové body se nacházejí pod `src/app/api/cli-tools/antigravity-mitm/`.

| Metoda | Koncový bod                             | Účel                                               |
| ------ | --------------------------------------- | -------------------------------------------------- |
| GET    | `/api/cli-tools/antigravity-mitm`       | Stav — spuštěno, pid, dnsConfigured, certExists    |
| POST   | `/api/cli-tools/antigravity-mitm`       | Spuštění MITM (vyžaduje `apiKey` + `sudoPassword`) |
| DELETE | `/api/cli-tools/antigravity-mitm`       | Zastavení MITM                                     |
| GET    | `/api/cli-tools/antigravity-mitm/alias` | Výpis aliasů modelů                                |
| PUT    | `/api/cli-tools/antigravity-mitm/alias` | Uložení aliasů modelů pro nástroj                  |

Cílový zachytávaný hostitel: **`daily-cloudcode-pa.googleapis.com`** (upstream služby Antigravity).

### Sekvence spuštění (`src/mitm/manager.ts::startMitm`)

1. Vygenerování certifikátu podepsaného svým držitelem prostřednictvím `selfsigned` (RSA-2048, SHA-256, 1 rok) — `cert/generate.ts`
2. Instalace certifikátu do systémového úložiště důvěryhodných certifikátů — `cert/install.ts`
3. Přidání záznamu `127.0.0.1 daily-cloudcode-pa.googleapis.com` do souboru hosts — `dns/dnsConfig.ts`
4. Spuštění `src/mitm/server.cjs` s `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (výchozí hodnota `443`)
5. Uložení PID do `<DATA_DIR>/mitm/.mitm.pid`

### Dynamická detekce úložiště důvěryhodných certifikátů v Linuxu — `cert/install.ts`

`getLinuxCertConfig()` prochází seznam priorit a vybere první existující adresář:

| Rodina distribucí        | Adresář                                     | Příkaz pro aktualizaci   |
| ------------------------ | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu          | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS   | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                 | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Název souboru certifikátu: `omniroute-mitm.crt`. Porovnání otisku prostřednictvím `getCertFingerprint()` (SHA-1 dat DER).

Funkce `updateNssDatabases()` navíc instaluje certifikát do uživatelských databází NSS, pokud je k dispozici `certutil`: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb`, všechny profily Firefoxu (včetně snap), pod názvem **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** PowerShell se zvýšenými oprávněními → `certutil -addstore Root`

### Ověřování

Všechny koncové body MITM vyžadují ověření správy (`requireCliToolsAuth`). Heslo sudo se ukládá do mezipaměti v rámci modulu (nikdy v `globalThis`) a při `stopMitm()` se vymaže.

---

## Přepsání User-Agentu — proměnné prostředí (`.env.example`, sekce 12)

| Proměnná                 | Výchozí hodnota                                                 |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.149.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

Používá je `open-sse/executors/base.ts::buildHeaders()` prostřednictvím dynamického vyhledávání. **Aktualizujte je, když poskytovatelé vydají nové verze CLI** — zastaralé řetězce UA začnou být odmítány jako neaktuální klienti.

## Přepínače režimu kompatibility CLI (`.env.example`, sekce 13)

| Proměnná                   | Účinek                                |
| -------------------------- | ------------------------------------- |
| `CLI_COMPAT_CODEX=1`       | Otisk Codexu                          |
| `CLI_COMPAT_CLAUDE=1`      | Otisk claude-cli                      |
| `CLI_COMPAT_GITHUB=1`      | Otisk GitHub Copilot Chat             |
| `CLI_COMPAT_ANTIGRAVITY=1` | Otisk Antigravity                     |
| `CLI_COMPAT_KIRO=1`        | Kiro                                  |
| `CLI_COMPAT_CURSOR=1`      | Cursor                                |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                           |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                              |
| `CLI_COMPAT_CLINE=1`       | Cline                                 |
| `CLI_COMPAT_ALL=1`         | Povolit všechny výše uvedené možnosti |

IP adresa poskytovatele je **vždy zachována** — přepínač pouze mění podobu požadavku na úrovni přenosu, nepřepíná výstupní IP adresu.

---

## Čištění příchozích hlaviček

OmniRoute před předáním odstraňuje příchozí hlavičky klienta, aby požadavek přijatý z Cursoru neprozradil nadřazené službě Claude hlavičku `User-Agent: Cursor/X.Y.Z`. Seznam zakázaných hlaviček najdete v `src/shared/constants/upstreamHeaders.ts`; je udržován v přesném souladu se schématy Zod a jednotkovými testy.

---

## Aktualizace otisků při jejich změně poskytovatelem

1. Zachyťte provoz oficiálního CLI pomocí `mitmproxy` (zachycení TLS + výpis)
2. Extrahujte JA3/JA4 a doslovné pořadí hlaviček
3. Aktualizujte odpovídající položku `CLI_FINGERPRINTS[...]`
4. Aktualizujte odpovídající výchozí hodnotu `*_USER_AGENT` v `.env.example`
5. Pokud se změnil samotný handshake TLS, aktualizujte odpovídající obalovou vrstvu poskytovatele nebo volbu `browser:` knihovny wreq-js
6. Spusťte testy TLS specifické pro daného poskytovatele a ruční ověřovací test vůči živé službě poskytovatele
7. Vydejte změnu v opravné verzi a zdokumentujte ji v `CHANGELOG.md`

---

## Testy

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — chování sdílené obalové vrstvy TLS
- `tests/unit/anthropic-cache-fingerprint.test.ts` — determinismus otisku
- `tests/unit/chatgpt-web-source-retirement.test.ts` — společný skrytý zdroj ChatGPT Web zůstává nepřítomný, zatímco Codex Web zůstává přítomný

---

## Viz také

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — co se stane, když skrytá cesta obdrží odpověď `403`
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — kompletní přehled proměnných prostředí
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — pohled operátora na pracovní postup MITM
