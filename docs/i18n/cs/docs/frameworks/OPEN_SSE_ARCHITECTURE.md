# open-sse Architecture (Čeština)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **TL;DR**: `open-sse/` je hlavní streamovací engine, který zajišťuje všechny požadavky na LLM v OmniRoute. Obsahuje přibližně 1 400 souborů implementujících zpracování požadavků, exekutory, služby, server MCP a překladovou vrstvu. Tato příručka vysvětluje, jak jednotlivé části zapadají do sebe.

**Zdroj:** `open-sse/` (balíček pracovního prostoru, přibližně 1 440 netestovacích souborů `.ts`)

---

## Proč samostatný balíček pracovního prostoru?

`open-sse/` je **samostatný pracovní prostor** v monorepozitáři OmniRoute, a to z několika důvodů:

1. **Opakovaná použitelnost** — `open-sse` je publikován na npm jako `@omniroute/open-sse`, takže jej mohou nezávisle používat i jiné projekty
2. **Jasné hranice** — streamovací engine je oddělen od vrstvy uživatelského rozhraní/databáze specifické pro OmniRoute
3. **Výkon** — engine nemá žádné závislosti na Next.js, což umožňuje rychlejší studené starty v prostředích CLI/serverless
4. **Verzování** — `open-sse` může vydávat verze vlastním tempem

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Struktura nejvyšší úrovně

```
open-sse/
├── index.ts              # Veřejný vstupní bod
├── types.d.ts            # Veřejné exporty typů
├── package.json          # @omniroute/open-sse
├── config/               # Konfigurace poskytovatelů, konstanty, registry
├── executors/            # HTTP exekutory pro jednotlivé poskytovatele (67 + base.ts/index.ts)
├── handlers/             # Obslužné rutiny požadavků (chatCore, responses atd.)
├── lib/                  # Interní nástroje
├── mcp-server/           # Server Model Context Protocol
├── services/             # Přibližně 298 modulů služeb
├── transformer/          # Transformátor formátu Responses API
├── translator/           # Překlad formátů (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Sdílené nástroje (protokolování, chyby, streamy atd.)
```

### Počty modulů

| Adresář | Soubory | Účel |
| `executors/` | 167 | HTTP exekutory pro jednotlivé poskytovatele (sjednocené prostřednictvím továrny DefaultExecutor) |
| `handlers/` | 157 | Vstupní body požadavků (chatCore, responses, embeddings) |
| `services/` | ~536 | Směrování, ukládání do mezipaměti, omezování rychlosti, obnovování atd. |
| `translator/` | 56 | Převod formátů (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Nástroje a transporty MCP |
| `utils/` | ~108 | Průřezové nástroje (protokolování, chyby, streamy) |
| `config/` | ~339 | Konfigurace poskytovatelů, konstanty, registry |

---

## Zpracování požadavku

Každý požadavek na LLM prochází **5fázovým zpracováním**:

```
                              ┌──────────────┐
   Požadavek HTTP              │  1. SMĚROVAT │   vyhodnocení kombinace, výběr modelu
   (trasa Next.js)             └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. PŘELOŽIT │   převod formátu (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. PROVÉST  │   exekutor poskytovatele, HTTP, opakování, jistič
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAMOVAT│   transformace SSE, zpětný tlak
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. ZAZNAMENAT│   sledování využití, protokol volání, klasifikace chyb
                              └──────┬───────┘
                                     │
                                     ▼
                              Odpověď HTTP (SSE nebo JSON)
```

### Fáze 1: Směrování (services/combo.ts)

**Vstupní bod**: `handleComboChat()` v `services/combo.ts`

Převede požadavek na konkrétní n-tici `(provider, model, account, credentials)`:

- Vyhledá kombinaci podle ID (nebo vytvoří virtuální kombinaci pro modely `auto/*`)
- Použije strategii směrování (prioritní, váženou, round-robin atd.)
- Odfiltruje poskytovatele, kteří nejsou v pořádku (jistič)
- Vybere další životaschopný cíl

Pro modely `auto/*` tato fáze také:

- Spouští **16faktorový bodovací** algoritmus (`services/autoCombo/`)
- Vybírá dvojici `provider+model` na základě stavu, ceny, latence atd.

### Fáze 2: Překlad (translator/)

Pokud se zdrojový formát (např. OpenAI) liší od cílového formátu (např. Claude), požadavek je **přeložen**:

- Systémový prompt → systémová zpráva
- Definice nástrojů → formát nástrojů specifický pro poskytovatele
- Parametry uvažování/přemýšlení → ekvivalenty specifické pro poskytovatele
- Normalizace rolí zpráv (`developer` → `system` pro jiné poskytovatele než OpenAI)

`translator/index.ts` zpřístupňuje:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Fáze 3: Provedení (executors/)

**Vstupní bod**: `getExecutor(providerId).execute(request, options)`

Všichni poskytovatelé používají `DefaultExecutor` (`executors/default.ts`) prostřednictvím záložního mechanismu továrny `getExecutor()`. Exekutor:

- Sestaví upstreamovou URL (`buildUrl()`)
- Přidá hlavičky specifické pro poskytovatele (`buildHeaders()`)
- Transformuje tělo požadavku (`transformRequest()`)
- Odešle požadavek HTTP s opakováním a exponenciálním prodlužováním prodlevy
- V případě potřeby zpracuje obnovení ověření (poskytovatelé OAuth)

Všechny exekutory rozšiřují `BaseExecutor` (`executors/base.ts`, 1170 řádků kódu), který poskytuje:

- Společnou logiku opakování
- Integraci proxy
- Integraci jističe
- Hooky pro zaznamenávání využití

### Fáze 4: Streamování (utils/stream.ts)

U streamovaných odpovědí exekutor vrací **ReadableStream**. Obslužná rutina:

- Provede průchod transformací SSE (`createSSETransformStreamWithLogger`)
- Používá heartbeat pingy k detekci neaktivních připojení
- Korektně zpracuje odpojení klienta (`pipeWithDisconnect`)
- Transformuje SSE → JSON pro klienty bez podpory streamování

U nestreamovaných odpovědí exekutor vrací analyzovaný objekt JSON, který je beze změny předán dál.

### Fáze 5: Záznam (services/usage.ts)

Po obdržení odpovědi (úspěšné či neúspěšné) se zaznamenají údaje o využití:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` z odpovědi
- `cost_usd` vypočítané z cenových údajů
- `latency_ms`, `status`, `error_class` v případě selhání
- Uloženo do tabulky `usage_history`

Artefakty protokolu volání (pokud jsou povoleny) se zapisují do `${DATA_DIR}/call_logs/`.

---

## Podrobný rozbor klíčových souborů

### chatCore.ts (5977 řádků)

**Hlavní obsluha požadavků**. Navzdory své velikosti má jasnou strukturu:

```ts
// Pseudostruktura souboru chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Autentizace + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Ověření těla požadavku
  const body = await parseRequestBody(request);

  // 3. Detekce formátu + překlad
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Směrování komba
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Pokračovat k dalšímu cíli
    }
  }

  // 5. Nouzový záložní mechanismus
  return await emergencyFallback(body);
}
```

Přestože jde o jednu obří funkci, je uspořádána do **okomentovaných sekcí**, které odpovídají pěti fázím zpracování.

### combo.ts (4456 řádků kódu)

**Směrovací jádro**, které převádí kombo na uspořádané cíle.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Podporuje **19 strategií směrování** (viz `src/shared/constants/routingStrategies.ts`):

| Strategie           | Chování                                                                          |
| ------------------- | -------------------------------------------------------------------------------- |
| `priority`          | Uspořádaný seznam s prvním cílem jako prioritním                                 |
| `weighted`          | Pravděpodobnostní výběr podle váhy jednotlivých cílů                             |
| `round-robin`       | Cyklické procházení cílů v daném pořadí                                          |
| `context-relay`     | Předávání kontextu mezi cíli                                                     |
| `fill-first`        | Vyčerpání kvóty před přechodem k dalšímu cíli                                    |
| `p2c`               | Volba ze dvou možností                                                           |
| `random`            | Rovnoměrný náhodný výběr                                                         |
| `least-used`        | Výběr cíle s nejnižším počtem nedávných použití                                  |
| `cost-optimized`    | Nejprve nejlevnější dostupný cíl                                                 |
| `reset-aware`       | Zohlednění časových oken obnovení poskytovatele                                  |
| `reset-window`      | Směrování založené na časovém okně obnovení                                      |
| `headroom`          | Nejprve cíl s největší zbývající rezervou kvóty                                  |
| `strict-random`     | Skutečně rovnoměrný výběr (bez vážení podle kvality)                             |
| `auto`              | Použití 16faktorového hodnocení (`autoCombo/`)                                   |
| `lkgp`              | Nejprve poslední známý funkční poskytovatel                                      |
| `context-optimized` | Nejvhodnější pro požadavky s dlouhým kontextem                                   |
| `fusion`            | Paralelní distribuce panelu a následná syntéza pomocí posuzovatele (`fusion.ts`) |

### base.ts (1170 řádků kódu)

**Abstraktní vykonavatel**, od kterého je odvozeno všech 107 vykonavatelů. Obsahuje:

- `buildUrl()` — výchozí sestavení URL (podtřídy jej přepisují pro vlastní potřeby)
- `buildHeaders()` — výchozí hlavičky (autentizace, typ obsahu)
- `transformRequest()` — ve výchozím nastavení předává požadavek beze změny
- `execute()` — hlavní smyčka HTTP s opakováním, exponenciálním prodlužováním prodlev a jističem

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Obsluhuje všechny poskytovatele kompatibilní s OpenAI/Anthropic
  // Poskytovatelé registrují konfigurace (URL, autentizaci, hlavičky), ale sdílejí logiku vykonavatele
}
```

Chování specifické pro jednotlivé poskytovatele (autentizační hlavičky, základní URL, hlavičky verze) se konfiguruje prostřednictvím registru poskytovatelů, nikoli pomocí samostatných tříd vykonavatelů.

````

---

## Služby (117 modulů)

Služby jsou **specializované jednoúčelové moduly**, které handlery skládají dohromady. Hlavní kategorie:

### Směrování a kombinace

- `combo.ts` — vstupní bod pro požadavky směrované pomocí combo
- `services/autoCombo/` — hodnocení podle 16 faktorů, 8 strategií automatického směrování
- `wildcardRouter.ts` — vyhledává odpovídající zástupné trasy (`gpt-*`)
- `modelFamilyFallback.ts` — záložní řešení v rámci rodiny T5

### Omezení rychlosti a kvóty

- `rateLimitManager.ts` — token bucket pro každý klíč a poskytovatele
- `usage.ts` — zaznamenávání využití
- `quotaCache.ts` — snímky kvót v paměti

### Účty a tokeny

- `tokenRefresh.ts` — obnovení OAuth při 401
- `accountFallback.ts` — přepnutí na alternativní účet
- `sessionManager.ts` — stav vícekolové relace

### Inteligence

- `intentClassifier.ts` — klasifikace záměru požadavku
- `taskAwareRouter.ts` — směrování podle typu úlohy
- `thinkingBudget.ts` — přidělování tokenů pro přemýšlení
- `contextManager.ts` — vkládání kontextu směrování

### Odolnost

- `resilience.ts` — orchestrace opakovaných pokusů, prodlev a jističe
- `emergencyFallback.ts` — záložní řešení poslední instance
- `modelDeprecation.ts` — automatické směrování na nástupnické modely

### Stav

- `signatureCache.ts` — deduplikace podle podpisu požadavku
- `volumeDetector.ts` — odlehčování zátěže
- `contextHandoff.ts` — serializace relace

### Komprese

- `compression/` (podadresář) — kompletní kompresní pipeline
- 39 souborů zahrnujících enginy, balíčky pravidel a adaptéry

### Dovednosti

- (popsáno v [SKILLS.md](./SKILLS.md))

### Paměť

- (popsáno v [MEMORY.md](./MEMORY.md))

---

## Exekutory (více než 75 souborů)

Jeden soubor pro každého poskytovatele. Všechny rozšiřují `BaseExecutor` a přepisují odlišné části.

### Běžné vzory

Poskytovatelé se vyhledávají prostřednictvím `getExecutor(providerId)`, který vrací nakonfigurovaný exekutor. Poskytovatelé kompatibilní s OpenAI/Anthropic používají `DefaultExecutor` (`executors/default.ts`). Chování specifické pro poskytovatele (základní URL, autentizační hlavičky, verze API) se konfiguruje v `open-sse/config/providers/`, zatímco transformace těla požadavku zpracovává `open-sse/translator/`.

**Vlastní URL** se nastavuje prostřednictvím konfigurace poskytovatele:

```ts
// Konfigurace poskytovatele v open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Vlastní autentizace** se zpracovává prostřednictvím konfigurace autentizace v registru poskytovatelů (klíč API, OAuth, profily hlaviček).

Transformace **vlastního těla požadavku** (např. oddělení `system` od `messages` u Anthropic) se registrují pro jednotlivé poskytovatele v `open-sse/translator/`.

````

### Továrna exekutorů

`executors/index.ts` exportuje `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Vyhledání probíhá prostřednictvím `ExecutorRegistry` (`executors/registry.ts`): každý specializovaný exekutor je deklarován ve vestavěné tabulce v `executors/index.ts` a při načtení modulu zaregistrován pomocí `registerExecutor(alias, instance)`; `getExecutor()` vyhledá exekutor v registru a u každého poskytovatele bez specializované položky použije jako záložní řešení memoizovaný `DefaultExecutor`. Kompletní mapování alias → exekutor charakterizuje referenční test `tests/unit/executor-map-golden.test.ts`.

---

## Překladače

Překládejte mezi **3 formáty**: OpenAI, Anthropic, Gemini a také nové Responses API.

### Kdy dochází k překladu

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Běžné překlady:

- `OpenAI → Anthropic`: samostatné pole `system`, hlavička `x-api-key`
- `OpenAI → Gemini`: `contents` namísto `messages`, `systemInstruction`
- `OpenAI → Responses API`: pole `input`, stav `previous_response_id`

### Ošetřené okrajové případy

- Role `developer` → `system` pro jiné poskytovatele než OpenAI
- Role `system` → sloučena do první uživatelské zprávy pro GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` v Gemini
- `tools` → formát nástrojů specifický pro poskytovatele
- Parametry uvažování (o1, Claude) → ekvivalenty specifické pro poskytovatele

---

## Server MCP

`open-sse/mcp-server/` implementuje server **Model Context Protocol**:

- **110 nástrojů** (správa poskytovatelů, kombinace, paměť, mezipaměť, komprese, proxy, dovednosti, gamifikace, pluginy, Notion, Obsidian, místní korpus)
- **3 transporty**: stdio, SSE, Streamable HTTP
- **33 rozsahů oprávnění** pro podrobné řízení přístupu

### Registrace nástrojů

Nástroje jsou registrovány jako samostatné soubory v `open-sse/mcp-server/tools/`, přičemž každý exportuje název, schéma, obslužnou funkci a rozsah oprávnění:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Získat snímek stavu systému",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transporty

```ts
// stdio (použití v CLI)
startMcpStdio(server);

// SSE (streamování založené na HTTP)
startMcpSse(server, port);

// Streamable HTTP (moderní MCP)
startMcpStreamable(server, port);
```

### Autorizace

Každé volání nástroje prochází kontrolami rozsahů oprávnění (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Nedostatečný rozsah oprávnění");
}
```

---

## Transformátory

`open-sse/transformer/` převádí mezi formáty **Chat Completions** a **Responses API**.

### Proč samostatný transformátor?

Responses API je nový formát OpenAI se **stavovými konverzacemi** (`previous_response_id`). Když klient odešle požadavek Responses, OmniRoute:

1. Interně převede Responses → Chat Completions
2. Odešle jej poskytovateli (libovolnému poskytovateli, který podporuje Chat Completions)
3. Převede odpověď zpět do formátu Responses
4. Streamuje převedenou odpověď klientovi

Transformátor (`transformer/responsesTransformer.ts`) poskytuje:

```ts
createResponsesApiTransformStream(): TransformStream
```

Zpracovává:

- Události `response.output_item.added`
- Události `response.output_text.delta`
- Událost `response.completed`
- Mapování volání nástrojů (`function_call` ↔ `tool_calls`)

---

## Konfigurace

`open-sse/config/` obsahuje konfigurační vrstvu:

| Soubor                        | Účel                                                       |
| ----------------------------- | ---------------------------------------------------------- |
| `providerRegistry.ts`         | Registr chatovacích modelů nad katalogem 352 poskytovatelů |
| `providerModels.ts`           | Aliasy modelů, mapování formátů                            |
| `constants.ts`                | Časové limity, omezení, stavové kódy                       |
| `defaultThinkingSignature.ts` | Výchozí signatura uvažování Claude                         |
| `modelStrip.ts` (ve službách) | Odstraňování polí podle poskytovatele                      |

### Schéma registru poskytovatelů

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Validace pomocí Zod při načtení modulu zajišťuje platnost všech konfigurací poskytovatelů.

---

## Výkonnostní omezení

Směrovací modul má striktní výkonnostní limity:

| Operace                                            | Cíl   | Měření                      |
| -------------------------------------------------- | ----- | --------------------------- |
| Vyhodnocení kombinace                              | <10ms | Pro 50 cílů                 |
| Kontrola omezení rychlosti                         | <1ms  | Token bucket v paměti       |
| Přechod na záložní rodinu modelů                   | <5ms  | Definice rodin v mezipaměti |
| Předání požadavku ke směrování                     | <2ms  | Kritická cesta              |
| **Žádné blokující I/O v kritické cestě směrování** | —     | Vše asynchronně             |

---

## Antivzory

❌ **Synchronní volání databáze v `combo.ts`** — předpočítejte je a uložte do mezipaměti
❌ **Logika opakovaných pokusů v handlerech** — použijte `retry()` ze služby odolnosti
❌ **Přímý přístup ke konfiguraci poskytovatele** — použijte gettery `providerRegistry`
❌ **Natvrdo definované řetězce záložních variant** — definujte je v `modelFamilyFallback.ts`
❌ **Mutace stavu napříč souběžnými požadavky** — používejte pouze kontext s rozsahem požadavku

---

## Přidání nové komponenty

### Přidání nové služby

1. Vytvořte `open-sse/services/[serviceName].ts` s jasně vymezenou odpovědností
2. Exportujte hlavní obslužnou funkci a všechny konstanty
3. Přidejte jednotkové testy do `tests/unit/services/[serviceName].test.mjs`
4. Integrujte službu do zpracování požadavků v `handlers/chatCore.ts` (pokud souvisí se směrováním)
5. Pokud služba ovlivňuje výběr cíle, aktualizujte logiku směrování v `combo.ts`
6. Zdokumentujte ji v tomto souboru

### Přidání nového exekutoru

1. Vytvořte `open-sse/executors/[provider].ts` rozšiřující `BaseExecutor`
2. Zaregistrujte jej v `config/providerRegistry.ts`
3. Přidejte jej do továrny v `executors/index.ts`
4. Přidejte jednotkové testy pro exekutor
5. Zdokumentujte jej v `docs/architecture/ARCHITECTURE.md`

### Přidání nového nástroje MCP

1. Vytvořte nebo aktualizujte `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definujte schéma Zod pro vstupy
3. Zaregistrujte nástroj v `mcp-server/index.ts`
4. Přidejte jej do matice rozsahů v `mcp-server/auth/`
5. Přidejte jednotkové testy

---

## Viz také

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — vysokoúrovňová architektura
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — technická referenční dokumentace
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — popis jednotlivých adresářů
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — hodnocení podle 16 faktorů
- [MCP-SERVER.md](./MCP-SERVER.md) — server MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — server A2A
- Zdrojový kód: `open-sse/` (více než 400 souborů, přibližně 143 tisíc řádků kódu)
