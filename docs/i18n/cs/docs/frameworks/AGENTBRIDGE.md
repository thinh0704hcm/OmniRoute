# AgentBridge (Čeština)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENTBRIDGE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENTBRIDGE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENTBRIDGE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENTBRIDGE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENTBRIDGE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENTBRIDGE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/AGENTBRIDGE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENTBRIDGE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENTBRIDGE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENTBRIDGE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENTBRIDGE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENTBRIDGE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENTBRIDGE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENTBRIDGE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENTBRIDGE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENTBRIDGE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENTBRIDGE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENTBRIDGE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENTBRIDGE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENTBRIDGE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENTBRIDGE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENTBRIDGE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENTBRIDGE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENTBRIDGE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENTBRIDGE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENTBRIDGE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENTBRIDGE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENTBRIDGE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENTBRIDGE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENTBRIDGE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENTBRIDGE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENTBRIDGE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENTBRIDGE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENTBRIDGE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENTBRIDGE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENTBRIDGE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENTBRIDGE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENTBRIDGE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENTBRIDGE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENTBRIDGE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENTBRIDGE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENTBRIDGE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENTBRIDGE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENTBRIDGE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENTBRIDGE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENTBRIDGE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENTBRIDGE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENTBRIDGE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENTBRIDGE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENTBRIDGE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENTBRIDGE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENTBRIDGE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENTBRIDGE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENTBRIDGE.md)

---

AgentBridge je MITM (Man-in-the-Middle) proxy OmniRoute, které zachytává provoz HTTPS z AI agentů v IDE a přesměrovává jej přes jednotný směrovací modul OmniRoute. Podporuje **10 agentů v IDE** — Antigravity, Kiro, GitHub Copilot, GHE Copilot, OpenAI Codex, Cursor, Zed, Claude Code, Open Code a Trae (ve fázi zkoumání) — díky čemuž je OmniRoute MITM proxy s nejširší podporou AI asistentů pro programování na trhu.

**Umístění v řídicím panelu:** `/dashboard/tools/agent-bridge`
**Skupina v postranním panelu:** Nástroje (za Cloud Agents)
**Viz také:** [`TRAFFIC_INSPECTOR.md`](./TRAFFIC_INSPECTOR.md) — monitorování veškerého zachyceného provozu v reálném čase; `docs/security/MITM-TPROXY-DECRYPT.md` (git; není zahrnut do `/docs`) — režim transparentního dešifrování a zachytávání pomocí Linux TPROXY, řízený trasou `/api/tools/agent-bridge/tproxy`.

---

## §1 Přehled

### Co je AgentBridge?

Když agent v IDE (např. GitHub Copilot, Cursor nebo Claude Code) provede volání API, připojí se přímo k nadřazenému poskytovateli AI (OpenAI, Anthropic atd.). AgentBridge toto připojení transparentně zachytí na úrovni TLS — aniž by bylo nutné měnit konfiguraci agenta — a přesměruje požadavek přes OmniRoute.

To znamená, že můžete:

- **Přesměrovat libovolného agenta k libovolnému poskytovateli**: Komunikuje Copilot s OpenAI? Přesměrujte jej na Anthropic Claude, Gemini nebo kteréhokoli z 352 poskytovatelů OmniRoute.
- **Používat mapování modelů**: `gemini-3-flash` → `claude-sonnet-4.7` transparentně na úrovni obslužné rutiny.
- **Sledovat veškerý provoz agentů**: každý zachycený požadavek je publikován do nástroje [Traffic Inspector](./TRAFFIC_INSPECTOR.md).
- **Využívat odolnost OmniRoute**: kombinované směrování, jističe, záložní mechanismy a sledování nákladů fungují také pro provoz agentů v IDE.

### Pozice vůči konkurenci

| Funkce                                   | 9router | anti-api | llm-interceptor | **OmniRoute AgentBridge** |
| ---------------------------------------- | :-----: | :------: | :-------------: | :-----------------------: |
| Antigravity                              |    ✓    |    ✓     |        —        |             ✓             |
| GitHub Copilot                           |    ✓    |    ✓     |        —        |             ✓             |
| Kiro (AWS)                               |    ✓    |    ✓     |        —        |             ✓             |
| OpenAI Codex                             |    —    |    ✓     |        —        |             ✓             |
| Cursor IDE                               |    ✓    |    ✓     |        —        |             ✓             |
| Zed Industries                           |    —    |    ✓     |        —        |             ✓             |
| Claude Code                              |    —    |    —     |        ✓        |             ✓             |
| Open Code                                |    —    |    —     |        ✓        |             ✓             |
| Trae                                     |    —    |    —     |        —        |      🔍 Ve zkoumání       |
| Uživatelské rozhraní řídicího panelu     |    ✓    |    ✗     |        ✗        |             ✓             |
| Inspektor provozu                        |    ✗    |    ✗     |        ✓        |             ✓             |
| Směrování OmniRoute                      |    ✗    |    ✗     |        ✗        |             ✓             |
| Uživatelské rozhraní pro mapování modelů |    ✗    |    ✗     |        ✗        |             ✓             |
| Seznam výjimek                           |    ✗    |    ✗     |        ✓        |             ✓             |
| Certifikát nadřazené CA                  |    ✗    |    ✗     |        ✓        |             ✓             |

---

## §2 Architektura

### 2.1 Přehled komponent

```
Agent IDE (VS Code / Cursor / atd.)
    │  HTTPS (port 443)
    ▼
/etc/hosts — 127.0.0.1 api.githubcopilot.com   ← přesměrování DNS
    │
    ▼
src/mitm/server.cjs  (port 443, podřízený proces CJS)
    │  překládá cíl podle SNI z hlavičky Host
    │  generuje certifikát TLS pro každé SNI podepsaný CA AgentBridge
    ├── Shoda se seznamem výjimek? → přímý přenos TCP (bez dešifrování)
    ├── Shoda s cílem? → fetch → směrovač OmniRoute (port 20128)
    │       └── handler.intercept() — TypeScript
    │               ├── maskSecrets() v těle/hlavičkách požadavku
    │               ├── TrafficBuffer.push() — publikuje do Inspektoru provozu
    │               └── fetchRouter() → /v1/chat/completions
    └── Bez shody? → přímý přenos TCP (bez dešifrování)
```

### 2.2 Server MITM (`src/mitm/server.cjs`)

Jádro serveru MITM běží jako podřízený proces Node.js CJS (aby nebylo nutné přepisovat existující kódovou základnu CJS). Provádí následující:

- Naslouchá na portu 443 (vyžaduje oprávnění nebo `authbind`/`setcap`)
- Přijímá tunely CONNECT z operačního systému (prostřednictvím přesměrování DNS v `/etc/hosts`)
- Generuje certifikáty TLS pro jednotlivá SNI podepsané CA AgentBridge (`DATA_DIR/mitm/ca.crt`)
- Překládá cílového agenta podle hlavičky Host prostřednictvím registru `targets/index.ts`
- Předává požadavky vrstvě obslužných rutin v TypeScriptu přes HTTP na `http://127.0.0.1:20128`

`TARGET_HOSTS` se načítá z `DATA_DIR/mitm/targets.json` (soubor při spuštění zapisuje `targets/index.ts`), což umožňuje dynamické aktualizace bez restartování serveru CJS.

> **Model kořenové CA (#6684).** Výše uvedený popis certifikátů pro jednotlivá SNI podepsaných CA
> představuje trvalý model kořenové CA přidaný v #6684 (`src/mitm/cert/rootCa.ts` +
> `src/mitm/_internal/rootCaShim.cjs`, který znovu využívá kryptografii CA/koncových certifikátů již
> ověřenou pro TPROXY v `src/mitm/tproxy/dynamicCert.ts`) — nahrazuje
> starší jediný statický koncový certifikát podepsaný sám sebou (`src/mitm/cert/generate.ts`, stále
> omezený pouze na hostitele antigravity), který indikuje pouhá dvojice `server.crt`/`server.key`
> na disku. **Chování při migraci**: nová instalace (bez předchozího
> `server.crt`) získá model kořenové CA automaticky; instalace, která již
> důvěřovala starému statickému koncovému certifikátu, jej používá i nadále, dokud provozovatel nenastaví
> `MITM_ROOT_CA_ENABLED=true` a nerestartuje bridge (`src/mitm/cert/migration.ts`
> je čistá rozhodovací funkce — důvěryhodná MITM CA, která může podepsat koncový certifikát pro
> **libovolného** hostitele, je výrazně mocnější než starý koncový certifikát s pevně danými SAN, takže
> k přepnutí u instalace, která mu již důvěřuje, nikdy nedojde bez upozornění). Certifikát CA se instaluje
> do stejného umístění `omniroute-mitm.crt` v úložišti důvěryhodných certifikátů, které používal starý koncový certifikát
> (`cert/install.ts::installCaCert`) — není potřeba čistit dvojí důvěryhodnost.

### 2.3 Základ obslužných rutin (`src/mitm/handlers/base.ts`)

Všechny obslužné rutiny agentů rozšiřují `MitmHandlerBase`:

```ts
export abstract class MitmHandlerBase {
  abstract readonly agentId: AgentId;

  abstract intercept(
    req: IncomingMessage,
    res: ServerResponse,
    body: Buffer,
    mappedModel: string
  ): Promise<void>;

  // Chráněné pomocné funkce: fetchRouter, pipeSSE, hookBufferStart, hookBufferUpdate
}
```

Každá obslužná rutina před předáním požadavku proxy volá `hookBufferStart()` a po dokončení volá `hookBufferUpdate()`. Tyto funkce vkládají položky `InterceptedRequest` do `globalTrafficBuffer` (viz [Inspektor provozu](./TRAFFIC_INSPECTOR.md) §4).

### 2.4 Registr cílů (`src/mitm/targets/`)

Každý agent má deklarativní cílový soubor:

```ts
// src/mitm/targets/copilot.ts
export const COPILOT_TARGET: MitmTarget = {
  id: "copilot",
  name: "GitHub Copilot",
  hosts: ["api.githubcopilot.com", "copilot-proxy.githubusercontent.com"],
  port: 443,
  endpointPatterns: ["/chat/completions", "/v1/chat/completions"],
  defaultModels: [{ id: "gpt-4o", name: "GPT-4o", alias: "gpt-4o" }],
  handler: () => import("../handlers/copilot"),
  riskNoticeKey: "providers.riskNotice.oauth",
};
```

Registr (`targets/index.ts`) exportuje `ALL_TARGETS` a při spuštění generuje `DATA_DIR/mitm/targets.json`.

### 2.5 Přímý přenos a seznam výjimek (`src/mitm/passthrough.ts`)

**Seznam výjimek** (kontroluje se jako první a má přednost před shodou s cílem):

- Výchozí vzory: hostitelé bank, `.gov.`, poskytovatelé OAuth/SSO (Okta, Auth0) atd.
- Uživatelské vzory: uložené v databázové tabulce `agent_bridge_bypass`
- Hostitelé ve výjimkách obdrží transparentní tunel TCP — TLS se **nikdy nedešifruje**

**Výchozí přímý přenos** (žádná shoda s cílem a žádná shoda se seznamem výjimek):

- Také obdrží tunel TCP — spojení nejsou nikdy přerušena
- Zabraňuje tomu, aby AgentBridge narušoval obecný systémový provoz HTTPS

Priorita směrování:

```
seznam výjimek → shoda s cílem → přímý přenos
```

### 2.6 Certifikát nadřazené CA (`src/mitm/upstreamTrust.ts`)

Pro firemní síťová prostředí s vlastní CA:

```bash
AGENTBRIDGE_UPSTREAM_CA_CERT=/path/to/corporate-ca.pem
```

Pokud je tato proměnná nastavena, nakonfiguruje globální dispatcher knihovny `undici` s dodatečným certifikátem CA, což umožní AgentBridge přistupovat k nadřazeným poskytovatelům prostřednictvím firemních proxy ukončujících TLS.

### 2.7 Maskování tajných údajů (`src/mitm/maskSecrets.ts`)

Nezávislý skener vytvořený metodou clean-room se aplikuje na těla požadavků a hlavičky s přihlašovacími údaji
**předtím**, než vstoupí do vyrovnávací paměti Inspektoru provozu nebo jakéhokoli protokolu. Provádí jediný lineární průchod:

- Tokeny s předponou `sk-` / `ak-` / `pk-` (ve stylu OpenAI/Anthropic)
- Přihlašovací údaje RFC 6750 `Authorization: Bearer <token>`, přičemž celý token má přednost
- Obecné dlouhé neprůhledné tokeny (≥40 znaků), včetně forem s tečkami a odsazením

`sanitizeHeaders()` převádí zachované názvy na malá písmena, deterministicky spojuje hodnoty polí, odstraňuje
sdílený seznam zakázaných hlaviček mezi jednotlivými uzly/rámců (včetně ověřování proxy), zcela rediguje `cookie` a
`set-cookie` a předává hodnoty přihlašovacích údajů skeneru.

---

## §3 Nastavení

### 3.1 Spuštění/zastavení serveru MITM

Použijte kartu serveru AgentBridge na adrese `/dashboard/tools/agent-bridge`:

| Akce                  | Popis                                                                          |
| --------------------- | ------------------------------------------------------------------------------ |
| Spustit server        | Spustí `src/mitm/server.cjs` na portu 443                                      |
| Zastavit server       | Řízeně ukončí podřízený proces                                                 |
| Restartovat server    | Zastaví a znovu spustí server (načte změny cíle)                               |
| Důvěřovat certifikátu | Nainstaluje `DATA_DIR/mitm/ca.crt` do úložiště důvěryhodných certifikátů OS    |
| Stáhnout certifikát   | Stáhne `ca.crt` pro ruční instalaci                                            |
| Obnovit certifikát    | Vytvoří nový pár klíčů CA (všechny existující certifikáty agentů se zneplatní) |

### 3.2 Nastavení certifikátu jako důvěryhodného

Než budou IDE přijímat připojení MITM, musí operační systém důvěřovat certifikátu CA služby AgentBridge.

**Linux (NSS — Chrome/Firefox):**

```bash
certutil -A -d sql:$HOME/.pki/nssdb -n "OmniRoute AgentBridge" -t CT,, -i ~/.omniroute/mitm/ca.crt
```

**macOS (Klíčenka):**

```bash
sudo security add-trusted-cert -d -r trustRoot \
  -k /Library/Keychains/System.keychain ~/.omniroute/mitm/ca.crt
```

**Windows (certmgr):**

```powershell
certutil -addstore -f Root $env:USERPROFILE\.omniroute\mitm\ca.crt
```

Případně použijte tlačítko „Důvěřovat certifikátu“ na řídicím panelu (spustí odpovídající příkaz pro váš operační systém a v případě potřeby zobrazí výzvu sudo).

#### IDE založená na Electronu ignorují úložiště důvěryhodných certifikátů OS (`NODE_EXTRA_CA_CERTS`)

Některá IDE — zejména **Antigravity IDE** a další aplikace odvozené od Electronu / VS Code — obsahují
vlastní běhové prostředí Node.js, které při odchozích požadavcích `fetch`/HTTPS **nepoužívá úložiště
důvěryhodných certifikátů OS**. Nastavení důvěryhodnosti CA na úrovni OS/NSS stačí pro nativní **backend**
IDE (např. jazykový server v Go, který používá sadu CA operačního systému), ale **frontend Electronu**
bude při TLS nadále selhávat — projeví se to tak, že aplikace bude _odhlášená_ nebo zobrazí
_"chybu připojení"_, přestože protokol MITM ukazuje, že inicializační volání backendu vracejí `200`. Jsou
vyžadovány dva kroky a oba jsou důležité:

1. Explicitně nasměrujte běhové prostředí na CA:
   ```bash
   export NODE_EXTRA_CA_CERTS=/path/to/omniroute-agentbridge-ca.crt
   ```
2. **Spusťte IDE z tohoto shellu.** Spuštění pomocí ikony na ploše / Docku / nabídky Start
   **nepřevezme** exportované proměnné shellu a `~/.config/environment.d/*.conf` se použije až po
   novém grafickém přihlášení. Nejprve IDE zcela ukončete — zámek jediné instance Electronu způsobí,
   že druhé spuštění pouze aktivuje existující proces a nové prostředí bude ignorováno.

Výše uvedený krok nastavení důvěryhodnosti v OS + NSS je i nadále nezbytný (síťová vrstva Chromia
používaná některými ověřovacími procesy čte uživatelské úložiště NSS a má vlastní statické vazby pro
`*.googleapis.com`, které lokálně důvěryhodná CA přepíše). `NODE_EXTRA_CA_CERTS` navíc pokrývá cestu
Node `fetch`.

### 3.3 Směrování DNS

Pro každého agenta, jehož provoz chcete zachytávat, se musí jeho hostitelé API překládat na `127.0.0.1`. AgentBridge automaticky spravuje záznamy v `/etc/hosts`, když v průvodci nastavením přepnete DNS daného agenta.

Příklad záznamů v `/etc/hosts` pro GitHub Copilot:

```
127.0.0.1 api.githubcopilot.com
127.0.0.1 copilot-proxy.githubusercontent.com
```

### 3.4 Mapování modelů

Pomocí tabulky mapování modelů na kartě každého agenta definujte mapování zdroj → cíl:

| Zdrojový model (nativní pro agenta) | Cílový model (OmniRoute) |
| ----------------------------------- | ------------------------ |
| `gpt-4o`                            | `claude-sonnet-4.7`      |
| `*` (zástupný znak)                 | `claude-haiku-4.7`       |

Zástupný znak `*` mapuje jakýkoli nerozpoznaný model na zadaný cíl. Mapování se ukládá v tabulce `agent_bridge_mappings`.

> **Tip — zjistěte skutečná ID modelů agenta.** IDE může odesílat názvy modelů, které se liší od
> popisků v jeho uživatelském rozhraní a mění se mezi hlavními verzemi. Například **Antigravity 2**
> odesílá `gemini-3.1-pro-low`, `gemini-pro-agent` a `gemini-3.1-flash-lite` — nikoli
> `gemini-2.5-pro` uváděný ve starší dokumentaci. Odešlete jednu chatovou zprávu bez odpovídajícího
> mapování: MITM zaznamená přesnou příchozí hodnotu `model:` a požadavek propustí dál. Namapujte tuto
> doslovnou hodnotu a následující požadavek již bude zachycen a směrován na váš cíl.

### 3.5 Upozornění na rizika

AgentBridge zachytává přihlašovací údaje (tokeny OAuth, klíče API), které IDE používá k ověřování u upstreamových poskytovatelů. Ty jsou **před zaznamenáním maskovány** (viz §2.7), ale jsou viditelné pro vrstvu MITM služby OmniRoute. Při první aktivaci každého agenta se zobrazí modální okno s upozorněním na rizika, které lze zavřít.

### 3.6 Údržba a diagnostika

Řídicí panel obsahuje kartu **Údržba a diagnostika** (`AgentBridgeMaintenanceCard`, v `src/app/(dashboard)/dashboard/tools/agent-bridge/components/`), která zpřístupňuje provozní trasy MITM, jež dříve neměly uživatelské rozhraní. Její podtitul zní: _„Otestujte kanál zachytávání, odstraňte zbývající změny systémového stavu a přeneste své nastavení mezi počítači.“_ Klientské pomocné funkce této karty se nacházejí v `src/lib/inspector/agentBridgeMaintenanceApi.ts`.

| Tlačítko                   | Trasa                                  | Co dělá                                                                                                                                                                                                                    |
| -------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Diagnostikovat**         | `GET /api/tools/agent-bridge/diagnose` | Spustí samočinný test zpracování zachytávání a zobrazí zprávu pro každou kontrolu (✓/✗ + doporučení k nápravě).                                                                                                            |
| **Opravit**                | `POST /api/tools/agent-bridge/repair`  | Vrátí zpět osiřelý systémový stav MITM (záznamy podvržení DNS, kořenová CA, systémový proxy server), který zůstal po pádu nebo signálu SIGKILL. Je idempotentní — pokud je stav čistý, oznámí „Není co opravit“.           |
| **Odebrat CA**             | `DELETE /api/tools/agent-bridge/cert`  | Zruší důvěryhodnost kořenové CA MITM a odebere ji z úložiště důvěryhodných certifikátů OS (explicitně, idempotentně). Zobrazuje se pouze tehdy, když je CA aktuálně důvěryhodná; vyžaduje vložené potvrzení „Odebrat CA?“. |
| **Exportovat konfiguraci** | `GET /api/tools/agent-bridge/config`   | Stáhne přenositelnou konfiguraci ve formátu JSON (viz §3.7).                                                                                                                                                               |
| **Importovat konfiguraci** | `POST /api/tools/agent-bridge/config`  | Nahraje dříve exportovanou konfiguraci ve formátu JSON (viz §3.7).                                                                                                                                                         |

**Diagnostické kontroly** (`summarizeDiagnostics()` v `src/mitm/inspector/diagnostics.ts`). Trasa pro každou kontrolu spustí sondu s vedlejšími účinky a předá logické hodnoty čistému sumarizátoru; vrátí se jediný výsledek `healthy` spolu s doporučením pro každé selhání:

| Název kontroly     | Co ověřuje                                                  | Doporučení při selhání                                                                                                                                             |
| ------------------ | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `server-running`   | Proces serveru MITM je aktivní                              | „Server MITM není spuštěný. Spusťte jej na kartě AgentBridge.“                                                                                                     |
| `server-reachable` | Server MITM přijímá připojení na svém portu (sonda TCP)     | „Server MITM nepřijímá připojení na svém portu. Zkontrolujte, zda je port volný a zda máte oprávnění jej navázat.“                                                 |
| `cert-exists`      | Certifikát MITM byl vygenerován na disku                    | „Dosud nebyl vygenerován žádný certifikát MITM. Vygenerujte jej na kartě AgentBridge.“                                                                             |
| `cert-trusted`     | Kořenová CA MITM je v úložišti důvěryhodných certifikátů OS | „Kořenová CA MITM není v úložišti OS důvěryhodná, takže zachytávání TLS selže. Nastavte certifikát jako důvěryhodný na kartě AgentBridge.“                         |
| `dns-configured`   | Cílové názvy hostitelů jsou podvrženy v `/etc/hosts`        | „Cílové názvy hostitelů nejsou podvrženy v /etc/hosts, takže provoz se nikdy nedostane k proxy serveru. Povolte DNS pro agenty, jejichž provoz chcete zachytávat.“ |

**Banner osiřelého stavu:** když stránka zjistí stav, který zůstal po pádu (podvržení DNS / CA / systémový proxy server), zobrazí karta oranžový banner — _„Předchozí relace zanechala systémový stav (podvržení DNS, CA nebo systémový proxy server). Spusťte opravu, abyste jej vyčistili.“_ — a zvýrazní tlačítko **Opravit**. `Repair` je aplikační obdobou příznaku `--cleanup` nástroje ProxyBridge (deleguje na `repairMitm()` v `src/mitm/manager.ts`).

> Kořenová CA MITM zůstává mezi zastavením a spuštěním nainstalovaná, aby se předešlo
> opakovaným výzvám sudo (stejné chování jako u mitmproxy/Charles), takže její odebrání
> je explicitní akce **Odebrat CA**, nikoli něco, co se při zastavení provede automaticky.

### 3.7 Import/export přenositelné konfigurace

AgentBridge dokáže serializovat stav **nastavitelný operátorem** do verzovaného objektu JSON, aby bylo možné stejné nastavení replikovat na více počítačích. Serializátor se nachází v `src/lib/inspector/configPortability.ts` (`exportConfig()` / `importConfig()`) a je validován pomocí `AgentBridgeConfigSchema`.

Export obsahuje přesně tři části (vestavěné výchozí hodnoty se záměrně **NEEXPORTUJÍ**, takže při importu nikdy nedojde k jejich duplikaci ani ke konfliktu s nimi):

| Pole             | Zdroj                                                           | Poznámky                                                                    |
| ---------------- | --------------------------------------------------------------- | --------------------------------------------------------------------------- |
| `bypassPatterns` | uživatelem definované vzory obcházení (`agent_bridge_bypass`)   | výchozí vzory bank/gov/okta jsou vyloučeny                                  |
| `customHosts`    | vlastní hostitelé Traffic Inspectoru (`inspector_custom_hosts`) | každý: `{ host, kind: "llm"\|"app"\|"custom", label? }`                     |
| `agentMappings`  | mapování modelů pro jednotlivé agenty (`agent_bridge_mappings`) | `{ [agentId]: [{ source, target }] }` pro každého agenta, který má mapování |

```jsonc
// GET /api/tools/agent-bridge/config
{
  "version": 1,
  "bypassPatterns": ["*.internal.example.com"],
  "customHosts": [{ "host": "api.example.com", "kind": "llm", "label": null }],
  "agentMappings": {
    "copilot": [{ "source": "gpt-4o", "target": "claude-sonnet-4.7" }],
  },
}
```

**Chování při importu** (`POST /api/tools/agent-bridge/config`): vzory obcházení a mapování jednotlivých agentů se **zcela nahradí**; vlastní hostitelé se přidají **idempotentně** (`INSERT OR IGNORE`). Odpověď uvádí, kolik položek jednotlivých typů bylo použito:

```jsonc
{ "ok": true, "bypassPatterns": 1, "customHosts": 1, "agents": 1 }
```

Co v konfiguraci **NENÍ**: stav spuštění serveru, cesty k certifikátům, stav DNS jednotlivých agentů, cesta k nadřazené certifikační autoritě a nastavení TPROXY — jedná se o stav hostitele/běhového prostředí, nikoli o přenositelné předvolby.

---

## §4 Reference pro jednotlivé agenty

| #   | Agent              | Stav           | Zachytávaní hostitelé                                              | Typ ověřování  |
| --- | ------------------ | -------------- | ------------------------------------------------------------------ | -------------- |
| 1   | **Antigravity**    | ✅ Podporováno | `daily-cloudcode-pa.googleapis.com`, `cloudcode-pa.googleapis.com` | Firebase OAuth |
| 2   | **Kiro (AWS)**     | ✅ Podporováno | `prod.kiro.aws`, `dev.kiro.aws`                                    | AWS SigV4      |
| 3   | **GitHub Copilot** | ✅ Podporováno | `api.githubcopilot.com`, `copilot-proxy.githubusercontent.com`     | GitHub OAuth   |
| 4   | **OpenAI Codex**   | ✅ Podporováno | `api.openai.com` (cesty Codexu), `chatgpt.com`                     | Klíč OpenAI    |
| 5   | **Cursor IDE**     | ✅ Podporováno | `api2.cursor.sh`, `api.cursor.sh`                                  | Cursor OAuth   |
| 6   | **Zed Industries** | ✅ Podporováno | `api.zed.dev`, `llm.zed.dev`                                       | Zed OAuth      |
| 7   | **Claude Code**    | ✅ Podporováno | `api.anthropic.com` (volitelné)                                    | Klíč Anthropic |
| 8   | **Open Code**      | ✅ Podporováno | `openrouter.ai`, `api.openai.com` (cesty zenu)                     | Klíč API       |
| 9   | **Trae**           | 🔍 Ve zkoumání | Bude upřesněno — viz §8                                            | Bude upřesněno |

### Kroky průvodce nastavením (pro jednotlivé agenty)

Každá karta agenta obsahuje průvodce nastavením se 3 kroky:

1. **Ověření předpokladů** — Je server spuštěný? Je certifikát důvěryhodný? Je IDE nainstalováno (automaticky detekováno)?
2. **Povolení DNS** — Přidá záznamy do `/etc/hosts` (vyžaduje sudo). Přesně zobrazí, které řádky budou přidány.
3. **Mapování modelů** — Volitelná tabulka mapování modelů. Zástupné znaky jsou podporovány.

### Detekce agentů

U agentů 1–8 se AgentBridge pokouší automaticky detekovat instalaci IDE:

```ts
export async function detectAgent(agentId: AgentId): Promise<DetectionResult>;
// Vrací: { installed: boolean, version?: string, path?: string }
```

Detekce používá cesty specifické pro daný operační systém a kontroly binárních souborů (např. `code --list-extensions | grep github.copilot` pro Copilot, `~/.config/antigravity/` pro Antigravity).

---

## §5 Zabezpečení

### Použitá pevná pravidla

| Pravidlo                           | Použití                                                                                                   |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`     | Všechny chyby obslužných rutin jsou před odpovědí nebo zápisem do vyrovnávací paměti očištěny             |
| **#13** Předávání prostředí shellu | Úpravy `/etc/hosts` používají volbu `env` — bez řetězcové interpolace cest                                |
| **#15 + #17** `isLocalOnlyPath()`  | `/api/tools/agent-bridge/` je LOCAL_ONLY + SPAWN_CAPABLE — přístup přes loopback je vynucen před ověřením |

### Seznam výjimek pro citlivé hostitele

Seznam výjimek zajišťuje, že finanční instituce, poskytovatelé OAuth/SSO a další citliví hostitelé nejsou **nikdy dešifrováni**. Jejich provoz TLS prochází jako transparentní tunel TCP — OmniRoute nikdy nevidí data v otevřené podobě.

Výchozí vzory výjimek zahrnují:

- `*.bank.*`, `*.gov.*` (finance/státní správa)
- `*.okta.com`, `*.auth0.com`, `*.microsoft.com` (SSO/identita)
- `*.apple.com`, `*.icloud.com` (systémové služby Apple)

Vzory výjimek přidané uživatelem jsou uloženy v tabulce `agent_bridge_bypass` a mají přednost před vším ostatním.

### Maskování tajných údajů

Používá se `maskSecrets()` ze souboru `src/mitm/maskSecrets.ts`:

- Na každé tělo požadavku před voláním `TrafficBuffer.push()`
- Na každou hlavičku před protokolováním nebo vysíláním

Vzory: tokeny s předponou `sk-`/`ak-`/`pk-`, tokeny `Bearer` a obecné tokeny o délce ≥40 znaků.

### Certifikát nadřazené CA

Když je nastavena proměnná `AGENTBRIDGE_UPSTREAM_CA_CERT`, soubor se načte při spuštění. Pokud cesta existuje, ale soubor nelze přečíst, AgentBridge zaznamená srozumitelnou chybu a odmítne se spustit (zabrání tím tichým selháním TLS v podnikových prostředích).

### Známá omezení

- **Port 443 vyžaduje oprávnění**: V Linuxu potřebuje AgentBridge na binárním souboru Node nastavit `setcap 'cap_net_bind_service=+ep'` nebo musí být spuštěn prostřednictvím `authbind`. Průvodce nastavením zobrazuje pokyny specifické pro daný operační systém.
- **Je vyžadován restart IDE**: Po přesměrování DNS je nutné IDE restartovat, aby se nové překládání hostitele projevilo.
- **Pevně zakódované tokeny OAuth**: Někteří agenti (Kiro, Antigravity) ukládají obnovovací tokeny OAuth lokálně. Pro AgentBridge jsou transparentní — v každém požadavku vidí token Bearer, který je před protokolováním maskován.
- **Frontendové aplikace Electron vyžadují `NODE_EXTRA_CA_CERTS`**: IDE, jejichž frontend běží v přibaleném běhovém prostředí Node/Electron, ignorují úložiště důvěryhodných certifikátů operačního systému/NSS a musí být spuštěna ze shellu s nastavenou proměnnou `NODE_EXTRA_CA_CERTS` (viz §3.2). Příznak chybějícího nastavení: backend IDE se ověří (MITM zobrazuje odpovědi `200`), ale uživatelské rozhraní zůstává odhlášené.
- **Více instalací stejného IDE je na sobě nezávislých**: systémová instalace (např. `/usr/share/antigravity/antigravity`) a uživatelská lokální instalace „Full“ (např. `~/AntigravityIDE_Full/antigravity-ide`) jsou samostatné procesy s vlastními běhovými prostředími — každý z nich musí být znovu spuštěn s vloženou CA. Před opětovným spuštěním určete podle cesty k binárnímu souboru, který z nich je spuštěný.
- **Identitu určuje systémová výzva agenta, nikoli směrovaný model**: když přemapujete model agenta na jiného poskytovatele, odpověď se stále hlásí k nativní identitě agenta (např. Antigravity odpoví „Používám Gemini“), protože ji IDE vkládá do systémové výzvy. Skutečný backend ověřte v `call_logs` / `proxy_logs` (`provider`, `model`, `target_format`), nikoli dotazem modelu na jeho identitu.

---

## §6 Řešení problémů

### Konflikt na portu 443

Pokud již na portu 443 naslouchá jiný proces (webový server, VPN apod.):

```bash
lsof -i :443          # vyhledání procesu
sudo fuser -k 443/tcp  # vynucené ukončení (používejte opatrně)
```

Případně v nastavení AgentBridge nakonfigurujte neprivilegovaný port a nastavte pravidla přesměrování `iptables` / `pf`.

### Certifikát není důvěryhodný

Pokud IDE po spuštění AgentBridge zobrazuje chyby TLS:

1. Ověřte, že byl certifikát nainstalován: `security find-certificate -c "OmniRoute AgentBridge"` (macOS) nebo `certutil -L -d sql:$HOME/.pki/nssdb` (Linux/NSS)
2. Některé aplikace používají vlastní úložiště důvěryhodných certifikátů (Firefox, Chrome v Linuxu). Spusťte znovu „Trust Cert“ a zkontrolujte úložiště certifikátů specifické pro NSS/Firefox.
3. Po nastavení důvěryhodnosti IDE restartujte — probíhající relace TLS používají původní stav důvěryhodnosti.

### IDE je odhlášené / zobrazuje „chybu připojení“ navzdory důvěryhodné CA

Příznak: po přesměrování DNS a nastavení důvěryhodnosti CA se IDE založené na Electronu (např. Antigravity)
otevře **odhlášené** nebo zobrazí chybu ověřování/připojení, přestože protokol MITM ukazuje, že
inicializační volání (`loadCodeAssist`, `fetchAvailableModels`, …) vracejí `200`.

Příčina: **přibalené běhové prostředí Node/Electron v IDE ignoruje systémové úložiště důvěryhodných certifikátů**. Nativní
backend (jazykový server v Go) důvěřuje systémové CA a ověří se, ale frontend Electron
nikoli — uživatelské rozhraní se proto domnívá, že je offline.

Řešení (oba kroky): exportujte `NODE_EXTRA_CA_CERTS=<ca.crt>` **a znovu spusťte IDE z tohoto
shellu**, nikoli pomocí ikony na ploše. Nejprve IDE zcela ukončete — zámek jediné instance Electronu způsobí,
že druhé spuštění pouze přepne pozornost na existující proces a nové prostředí se ignoruje. Viz §3.2.
To odpovídá otevřenému hlášení v upstream projektu, podle kterého samostatný agent přes MITM funguje, ale varianta
IDE ve stejném prostředí selhává.

### Změny DNS se neprojevily

Zkontrolujte, zda byl aktualizován soubor `/etc/hosts`:

```bash
grep "omniroute\|127.0.0.1.*github\|127.0.0.1.*cursor" /etc/hosts
```

Vyprázdněte mezipaměť DNS:

```bash
# macOS
sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder
# Linux (systemd-resolved)
sudo systemctl restart systemd-resolved
# Windows
ipconfig /flushdns
```

### IDE nebylo rozpoznáno

Automatická detekce používá běžná umístění instalace. Pokud se detekce nezdaří, ale IDE je nainstalováno:

- Zkontrolujte, zda se binární soubor IDE nenachází v nestandardním umístění
- Průvodce nastavením bude i nadále fungovat — selhání detekce pouze znamená, že odznak nezobrazí cestu k instalaci

### Chyby obslužné rutiny (načtení z upstream serveru selže)

Pokud AgentBridge zachytává požadavky, ale všechny selhávají:

1. Ověřte, že je na `/dashboard/providers` připojen alespoň jeden poskytovatel
2. Zkontrolujte protokoly serveru OmniRoute: `APP_LOG_LEVEL=debug` v `.env`
3. Ověřte, že `OMNIROUTE_BASE_URL` odkazuje na správný koncový bod směrovače (výchozí: `http://127.0.0.1:20128`)

---

## §7 Referenční příručka API

Všechny trasy jsou `LOCAL_ONLY` (pouze pro místní smyčku, vynuceno před ověřením) a `SPAWN_CAPABLE`. Viz `src/server/authz/routeGuard.ts`.

Základní cesta: `/api/tools/agent-bridge/`

| Metoda              | Cesta                                          | Popis                                                                                                                               |
| ------------------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| GET                 | `/api/tools/agent-bridge/state`                | Globální stav serveru + detekce/stav jednotlivých agentů                                                                            |
| GET                 | `/api/tools/agent-bridge/agents`               | Seznam registrovaných agentů (id, název, hostitelé, použitelnost, stav)                                                             |
| GET                 | `/api/tools/agent-bridge/agents/{id}`          | Stav jednoho agenta (cílová konfigurace + detekce + uložený stav)                                                                   |
| PATCH               | `/api/tools/agent-bridge/agents/{id}`          | Aktualizace `setup_completed` pro agenta                                                                                            |
| GET                 | `/api/tools/agent-bridge/agents/{id}/detect`   | Spuštění detekční sondy pro agenta (`installed`, `version?`, `path?`)                                                               |
| POST                | `/api/tools/agent-bridge/agents/{id}/dns`      | Povolení/zakázání DNS pro agenta (`{enabled: boolean}`)                                                                             |
| GET                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Mapování modelů pro agenta                                                                                                          |
| PUT                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Nahrazení mapování modelů                                                                                                           |
| POST                | `/api/tools/agent-bridge/server`               | Spuštění/zastavení/restartování serveru (`action: "start"\|"stop"\|"restart"\|"trust-cert"\|"regenerate-cert"`)                     |
| GET                 | `/api/tools/agent-bridge/cert`                 | Stav certifikátu (`exists`, `trusted`, `path`)                                                                                      |
| POST                | `/api/tools/agent-bridge/cert`                 | Přidání kořenové certifikační autority MITM mezi důvěryhodné (instalace)                                                            |
| DELETE              | `/api/tools/agent-bridge/cert`                 | Odebrání kořenové certifikační autority MITM z důvěryhodných (odinstalace) — idempotentní (viz §3.6)                                |
| POST                | `/api/tools/agent-bridge/cert/regenerate`      | Opětovné vygenerování certifikátu MITM podepsaného svým držitelem                                                                   |
| GET                 | `/api/tools/agent-bridge/cert/download`        | Streamování certifikátu PEM ke stažení                                                                                              |
| GET                 | `/api/tools/agent-bridge/bypass`               | Seznam vzorů pro obcházení (`default` + `user`)                                                                                     |
| POST                | `/api/tools/agent-bridge/bypass`               | Úplné nahrazení uživatelem definovaných vzorů pro obcházení                                                                         |
| DELETE              | `/api/tools/agent-bridge/bypass?pattern=...`   | Odebrání jednoho uživatelem definovaného vzoru pro obcházení                                                                        |
| GET                 | `/api/tools/agent-bridge/diagnose`             | Samočinný test kanálu zachytávání (viz §3.6)                                                                                        |
| POST                | `/api/tools/agent-bridge/repair`               | Vrácení osiřelého systémového stavu MITM zpět (viz §3.6)                                                                            |
| GET                 | `/api/tools/agent-bridge/config`               | Export přenositelné konfigurace JSON (viz §3.7)                                                                                     |
| POST                | `/api/tools/agent-bridge/config`               | Import přenositelné konfigurace JSON (viz §3.7)                                                                                     |
| GET                 | `/api/tools/agent-bridge/upstream-ca`          | Získání nakonfigurované cesty k nadřazené certifikační autoritě                                                                     |
| POST                | `/api/tools/agent-bridge/upstream-ca`          | Ověření + trvalé uložení cesty k nadřazené certifikační autoritě                                                                    |
| POST                | `/api/tools/agent-bridge/upstream-ca/test`     | Pouze ověření (zkušební běh) cesty k nadřazené certifikační autoritě — změny se trvale neukládají                                   |
| GET / POST / DELETE | `/api/tools/agent-bridge/tproxy`               | Režim zachytávání TPROXY s transparentním dešifrováním — viz `docs/security/MITM-TPROXY-DECRYPT.md` (git; nezahrnuje se do `/docs`) |

Úplná schémata OpenAPI: `docs/openapi.yaml` → značka `AgentBridge`.

---

## §8 Plán vývoje

### Průzkum Trae

Trae je relativně nový AI asistent pro programování. Před implementací handleru:

1. Identifikujte binární soubor/rozšíření v tržištích VS Code / JetBrains nebo samostatnou aplikaci
2. Zachyťte provoz pomocí mitmproxy, abyste zjistili hostitele API a strukturu endpointů
3. Určete mechanismus autentizace
4. Na základě smluvních podmínek a zjistitelnosti API rozhodněte, zda pokračovat

Dokud nebude průzkum dokončen, karta Trae na řídicím panelu zobrazuje odznak „Probíhá průzkum“ s odkazem „Nahlásit životaschopnost“. Zástupný handler v `src/mitm/handlers/trae.ts` vyvolá strukturovanou chybu `Dosud neimplementováno`.

### Agenti v backlogu (vyžadován MITM — bez podpory vlastní základní adresy URL)

Následující nástroje ve svých aktuálních verzích nepodporují vlastní základní adresy URL, takže MITM je jedinou možností zachytávání. Posouzení životaschopnosti zatím nebylo dokončeno:

- **Windsurf** (Codeium/Cognition)
- **Amp** (Sourcegraph)
- **Amazon Q / Kiro CLI** (AWS Bedrock — oddělené od Kiro IDE)
- **Cowork** (desktopová aplikace Anthropic)

Poznámka: GitHub Copilot CLI ≥v1.0.19 podporuje `COPILOT_PROVIDER_BASE_URL` — pro tento nástroj použijte místo MITM přímou konfiguraci.
