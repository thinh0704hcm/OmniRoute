# Claude Code CLI — Configuration with OmniRoute (Čeština)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇦 [bs](../../../bs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

Nasměrujte CLI **Claude Code** (`claude`) na OmniRoute — lokální nebo na vzdáleném VPS —
s profily pro jednotlivé modely, obdobně jako v nastavení Codexu.

---

## Rychlý start

```bash
# Spuštění Claude Code s lokálním OmniRoute (automaticky zjistí aktivní kontext)
omniroute launch

# Se vzdáleným OmniRoute (po `omniroute connect <host>` je toto automatické)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Vygenerování profilů pro jednotlivé modely a následné spuštění jednoho z nich
omniroute setup-claude            # zapíše ~/.claude/profiles/<name>/settings.json
omniroute launch --profile glm52  # Claude Code používající glm/glm-5.2 přes OmniRoute
```

---

## Jak se Claude Code připojuje k bráně

Claude Code komunikuje prostřednictvím **Anthropic Messages API** a na vlastní
koncový bod se směruje pomocí proměnných prostředí (nemá příznak `--base-url`):

| Proměnná                                     | Účel                                                                                                   |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `ANTHROPIC_BASE_URL`                         | Kořenová URL brány (Claude Code připojí `/v1/messages`). **Bez přípony `/v1`.**                        |
| `ANTHROPIC_AUTH_TOKEN`                       | Odesílá se jako `Authorization: Bearer …` — použijte svůj přístupový token / API klíč OmniRoute        |
| `ANTHROPIC_API_KEY`                          | Alternativa: odesílá se jako `x-api-key`. Pokud jsou nastaveny obě, má přednost `ANTHROPIC_AUTH_TOKEN` |
| `ANTHROPIC_MODEL`                            | Vynutí konkrétní model (přepíše výchozí volbu nástroje `/model`)                                       |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | `1` → nativní nástroj `/model` zobrazí modely `claude*`/`anthropic*` z `/v1/models`                    |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | Omezí počet výstupních tokenů na odpověď (např. `65536`)                                               |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | Prahová hodnota tokenů pro automatickou kompakci                                                       |

> Proměnné prostředí se načítají **pouze jednou při spuštění** — po jejich změně Claude Code restartujte.

`omniroute launch` nastaví všechny tyto proměnné za vás: zjistí základní URL a token
z aktivního kontextu (takže po `omniroute connect <vps>` stačí spustit
`omniroute launch`), zkontroluje stav serveru a spustí `claude`.

---

## Aliasy pro zjišťování modelů — zobrazení modelů jiných než Claude v nástroji `/model`

Zjišťování modelů brány v Claude Code zobrazuje pouze ID začínající na `claude`
nebo `anthropic`, takže při nastavení `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`
nativní nástroj `/model` běžně zobrazuje **pouze** modely Claude/Anthropic z OmniRoute —
`kimi/kimi-k2.6` ani `glm/glm-5.2` se nikdy nezobrazí, přestože směrování funguje správně.

OmniRoute může zrcadlit libovolný povolený model (i kombinaci) pod ID `claude/…`,
aby prošel tímto filtrem a zobrazil se ve výběru:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<kombinace "custo-otimizado"> →  claude/combo/custo-otimizado
```

Když v Claude Code vyberete některý z těchto modelů, OmniRoute před směrováním
odebere obal `claude/` a obnoví skutečné ID — pravé ID `claude/<real-claude-model>`
(skutečného poskytovatele Claude OAuth) vždy ponechá beze změny.

**Ve výchozím nastavení je tato funkce vypnutá** a řídí ji tříúrovňové nastavení
(nejkonkrétnější úroveň má přednost), takže běžný OmniRoute nezdvojuje svůj katalog
pro klienty, kteří Claude Code nepoužívají:

| Úroveň       | Kde                                                                                                  |
| ------------ | ---------------------------------------------------------------------------------------------------- |
| Model        | Stránka podrobností poskytovatele → přepínač „Zobrazit v Claude Code“ pro každý model                |
| Poskytovatel | Stránka podrobností poskytovatele → přepínač na úrovni poskytovatele (platí pro všechny jeho modely) |
| Globální     | Nastavení → Příznaky funkcí → `EXPOSE_CC_DISCOVERY_ALIASES` (ve výchozím nastavení vypnuto)          |

Proměnná prostředí `EXPOSE_CC_DISCOVERY_ALIASES` vynutí zapnutí globální úrovně
a má přednost před nastavením v ovládacím panelu (obrazovka Příznaky funkcí zobrazí
poznámku „aktivní prostřednictvím proměnné prostředí“, pokud je takto aktivována).
Přepínače pro jednotlivé poskytovatele a modely toto nastavení dále upřesňují —
například globálně vypnuto + zapnutý poskytovatel Kimi zobrazí pouze modely Kimi.

> ⚠️ **Neshoda velikosti okna u modelů jiných než Claude.** Claude Code předpokládá
> kontextové okno o velikosti 200K pro každé ID, které nerozpozná (skutečnou velikost
> okna nedokáže z `/v1/models` načíst). Pro model s větším oknem (např. 256K u Kimi K2)
> nastavte `CLAUDE_CODE_AUTO_COMPACT_WINDOW` na hodnotu nižší, než je skutečná velikost
> okna modelu, aby se automatická kompakce nespouštěla předčasně. Výše uvedené
> vygenerované profily toto již nastavují pro každý model.

---

## Úvodní blok na řídicím panelu

Karta nástroje Claude (**Řídicí panel → CLI Code**) zobrazuje přesný fragment `settings.json`
pro tuto instanci vedle informačního tlačítka aliasu pro zjišťování modelů a obsahuje tlačítko pro kopírování:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<váš OmniRoute>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<váš API klíč OmniRoute>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

Základní URL je adresa, kterou karta vyhodnotila (včetně vlastního přepsání, které jste zadali), a je již
normalizovaná — bez přípony `/v1` a bez koncového lomítka. **Klíč se nikdy nezobrazuje**: blok obsahuje
zástupný text, takže jej nelze vyzradit snímkem obrazovky ani vloženým úryvkem. Nahraďte jej svým klíčem.

Pro každý model, jehož skutečné kontextové okno nemá velikost 200K, přidejte do stejného bloku `env`
proměnnou `CLAUDE_CODE_AUTO_COMPACT_WINDOW` — Claude Code předpokládá velikost 200K u každého ID,
které nerozpozná, takže automatická kompakce by se jinak spustila v nesprávný okamžik (viz upozornění
v předchozí části). Generátor úryvku tuto hodnotu rovněž přijímá, takže ji volající, který zná velikost
kontextového okna cílového modelu, může přímo vygenerovat.

Zdroj: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (čistý
generátor pokrytý jednotkovými testy), vykreslovaný komponentou `ClaudeGatewayOnboardingBlock`.

---

## Profily (`CLAUDE_CONFIG_DIR`)

Claude Code **nemá nativní soubory profilů** (na rozdíl od souboru `~/.codex/<name>.config.toml`
nástroje Codex). Standardním mechanismem je `CLAUDE_CONFIG_DIR` — samostatný konfigurační adresář
pro každý profil, přičemž každý má vlastní soubor `settings.json`, přihlašovací údaje, historii a mezipaměť.

Příkaz `omniroute setup-claude` načte aktuální katalog `/v1/models` a zapíše jeden
profil pro každý model do `~/.claude/profiles/<name>/settings.json`, přičemž znovu použije
**stejné názvy jako `setup-codex`** (`glm52`, `kimi-k27`, `deepseek-pro`, …):

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **Ověřovací token se do profilu nikdy nezapisuje.** Spusťte profil pomocí
> `omniroute launch --profile <name>` (příkaz vloží `ANTHROPIC_AUTH_TOKEN` z aktivního
> kontextu), nebo sami exportujte `ANTHROPIC_AUTH_TOKEN` a spusťte
> `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude`.

**Automatická synchronizace po zjištění modelů (volitelná).** OmniRoute může stejné soubory
`~/.claude/profiles/<name>/settings.json` automaticky znovu vygenerovat pokaždé, když synchronizace
modelů poskytovatele změní aktuální katalog — nové nebo přejmenované modely tak získají profily bez
nutnosti znovu spouštět příkaz. Ve výchozím nastavení je tato funkce **vypnutá**: zapněte ji na
**řídicím panelu CLI Code** („Automatická synchronizace profilů CLI“ → Claude Code) nebo nastavte
`OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` (respektuje také `CLI_ALLOW_CONFIG_WRITES`, které je ve
výchozím nastavení zapnuté). Je-li funkce povolena, zapisuje pouze soubory profilů; nikdy nemění vaši
aktivní/výchozí konfiguraci Claude, ověřování ani soubor `~/.claude/settings.json`.

### Generování a používání profilů

```bash
# Místní OmniRoute
omniroute setup-claude

# Vzdálený VPS (vloží URL VPS do každého profilu)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Pouze někteří poskytovatelé
omniroute setup-claude --only glm,kimi

# Náhled bez zápisu
omniroute setup-claude --dry-run

# Spuštění profilu
omniroute launch --profile kimi-k27
```

---

## Úrovně modelů (volitelné)

Claude Code směruje požadavky podle úrovní schopností. Pokud chcete pro jednotlivé úrovně používat různé poskytovatele, namapujte každou z nich na model OmniRoute prostřednictvím proměnných prostředí /
nastavení:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

Jinak se pro vše použije jediný model `ANTHROPIC_MODEL` (nastavený profily).

---

## Vzdálený režim

Jakmile spustíte `omniroute connect <host>` (viz
[Vzdálený režim](./REMOTE-MODE.md)), příkazy `omniroute launch` a `omniroute setup-claude`
se automaticky zaměří na daný vzdálený server a použijí jeho přístupový token s příslušným rozsahem oprávnění — nejsou
potřeba žádné další příznaky. Pro jednotlivá spuštění lze toto nastavení přepsat pomocí `--remote` / `--api-key`.

---

## Řešení problémů

**Claude Code ignoruje bránu** — ověřte, že `ANTHROPIC_BASE_URL` **neobsahuje
`/v1`**, a restartujte `claude` (proměnné prostředí se načítají pouze jednou při spuštění). Příkaz `omniroute launch`
se o to postará za vás.

**Výběr `/model` je prázdný / chybí modely brány** — je vyžadován Claude Code
v2.1.219+ a `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`. Ve výběru se zobrazují pouze ID modelů `claude*` /
`anthropic*`; jakýkoli jiný model vynuťte pomocí
`ANTHROPIC_MODEL=<id>` (právě to dělají profily).

**`400 Ambiguous model 'claude-…'`** — Claude Code vždy odesílá **ID modelů bez předpony**
(např. `claude-opus-4-8`), takže pokud jsou připojeni jak poskytovatel Claude Code (`cc/…`), tak
Claude (`claude/…`), odpovídá samotné ID dvěma trasám a
OmniRoute odmítne hádat. Opravit to lze dvěma způsoby: nastavte ID s předponou pomocí
`ANTHROPIC_MODEL=cc/claude-opus-4-8`, nebo povolte možnost **Upřednostňovat Claude Code pro
modely Claude bez předpony** — přepínačem na stránce poskytovatele Claude nebo pomocí
`OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` (ve výchozím nastavení vypnuto;
viz [Prostředí](../reference/ENVIRONMENT.md)) — čímž se ID `claude-*` bez předpony
nasměrují do Claude Code. Explicitní předpony poskytovatelů mají vždy přednost.

**Chyby ověřování** — profil neobsahuje žádný token. Použijte `omniroute launch --profile`
(token vloží) nebo exportujte `ANTHROPIC_AUTH_TOKEN`.

**Profily nejsou izolované** — každý profil představuje samostatný adresář `CLAUDE_CONFIG_DIR`;
ověřte, že `echo $CLAUDE_CONFIG_DIR` uvnitř relace odkazuje na
`~/.claude/profiles/<name>`.
