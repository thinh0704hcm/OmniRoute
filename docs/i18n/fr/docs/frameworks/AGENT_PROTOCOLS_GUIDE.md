# Agent Protocols Guide (Français)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md)

---

> **Source :** `src/lib/{a2a,acp,cloudAgent}/`, `src/app/api/{a2a,acp,cloud}/`, `src/app/api/v1/agents/`
> **Dernière mise à jour :** 2026-06-28 — v3.8.40

OmniRoute expose trois interfaces différentes liées aux agents. Elles semblent similaires à première vue, mais répondent à des besoins différents. Utilisez cette page pour choisir celle qui convient.

## En bref

| Interface                       | Idéale pour                                                                                                                                                          | Transport                          | Standard                         |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------- |
| **A2A — Agent à agent**         | La collaboration entre agents avec des agents pairs prenant en charge le protocole A2A                                                                               | JSON-RPC 2.0 sur HTTP              | A2A v0.3 (spécification ouverte) |
| **ACP — Registre d’agents CLI** | La détection, l’enregistrement et le lancement d’agents de codage CLI installés sur la machine de l’utilisateur (Cursor, Cline, Codex CLI, Claude Code, Aider, etc.) | HTTP REST                          | Spécifique à OmniRoute           |
| **Agents cloud**                | La soumission de tâches de codage de longue durée à des services cloud externes (Codex Cloud, Devin, Jules, Cursor Cloud)                                            | HTTP REST + tâches stockées en BDD | Spécifique à OmniRoute           |

Les trois sont indépendantes — vous pouvez en choisir n’importe quelle combinaison.

## Arbre de décision

```
Avez-vous besoin qu’un service cloud effectue un travail en dehors de cette machine (Codex Cloud / Cursor Cloud / Devin / Jules) ?
├─ OUI → Agents cloud (POST /api/v1/agents/tasks)
└─ NON → Continuer
    │
    Avez-vous un agent pair qui prend en charge A2A et souhaite collaborer ?
    ├─ OUI → A2A (POST /a2a)
    └─ NON → Continuer
        │
        Avez-vous besoin de répertorier ou de configurer des agents de codage CLI installés localement ?
        ├─ OUI → ACP (GET /api/acp/agents)
        └─ NON → Utiliser simplement /v1/chat/completions
```

## 1. A2A — Agent à agent

**Spécification :** [A2A v0.3](https://a2a-protocol.org)
**Point de terminaison OmniRoute :** `POST /a2a` (JSON-RPC 2.0)
**Fiche de l’agent :** `GET /.well-known/agent.json`

### Quand l’utiliser

- Créer un système multi-agent dans lequel OmniRoute est l’un des pairs
- Exposer l’intelligence de routage d’OmniRoute (`smart-routing`, `quota-management`, etc.) aux agents de frameworks tels que Google ADK ou de réseaux d’agents génériques
- Encapsuler OmniRoute derrière une interface standard de découverte et d’invocation

### Méthodes

- `message/send` — soumettre un message et recevoir une réponse synchrone
- `message/stream` — soumettre un message et recevoir des événements de progression diffusés via SSE
- `tasks/get` — lire une tâche à partir de son ID
- `tasks/cancel` — annuler une tâche en cours d’exécution

### Compétences intégrées (6)

- `smart-routing` — acheminer une requête via la combinaison optimale
- `quota-management` — indiquer l’état des quotas pour chaque fournisseur
- `provider-discovery` — répertorier les fournisseurs installés avec leurs capacités
- `cost-analysis` — estimer le coût d’une requête ou d’une conversation
- `health-report` — agréger l’état du disjoncteur, du délai de récupération et du verrouillage pour chaque fournisseur
- `list-capabilities` — énumérer les compétences disponibles de l’agent et leurs métadonnées

### Présentation détaillée

Consultez [A2A-SERVER.md](./A2A-SERVER.md) pour obtenir des détails sur le transport, la structure de la fiche de l’agent, la configuration de la durée de vie des tâches et le modèle permettant d’ajouter de nouvelles compétences.

## 2. ACP — Registre des agents CLI

**Endpoint OmniRoute :** `GET /api/acp/agents`
**Source :** `src/lib/acp/{index,manager,registry}.ts`

### Présentation

ACP est l’**inventaire local des agents CLI** d’OmniRoute. Il détecte les CLI de programmation installées sur l’hôte (Cursor, Cline, Claude Code, Codex CLI, Continue, etc.), détermine leurs versions et les affiche dans le tableau de bord afin que l’utilisateur puisse configurer chaque CLI pour qu’elle pointe vers OmniRoute.

Il ne s’agit PAS d’un protocole externe, mais d’un registre interne qui alimente l’interface « CLI Tools » et le suivi des empreintes des CLI (voir [CLI-TOOLS.md](../reference/CLI-TOOLS.md)).

### Fonctionnalités

- Recherche sur l’hôte les binaires CLI installés (utilise `which` / `where` selon le système d’exploitation)
- Lit la version de chaque CLI (appelle `<bin> --version`)
- Accepte éventuellement des agents personnalisés définis par l’utilisateur (chemin du binaire + commande de détection de version + arguments de lancement)
- Enregistre les agents personnalisés dans les paramètres
- Renvoie la liste unifiée au tableau de bord

### API REST

| Endpoint          | Méthode | Description                                                                   | Authentification |
| ----------------- | ------- | ----------------------------------------------------------------------------- | ---------------- |
| `/api/acp/agents` | GET     | Répertorie les agents détectés et personnalisés (nombres installés/total)     | Clé API          |
| `/api/acp/agents` | POST    | Ajoute/modifie/supprime un agent personnalisé (action indiquée dans le corps) | Clé API          |

Structure du corps pour POST (`customAgentBodySchema` dans `src/app/api/acp/agents/route.ts`) :

```json
{
  "action": "add|update|remove",
  "id": "cursor",
  "name": "Cursor",
  "binary": "/usr/local/bin/cursor",
  "versionCommand": "--version",
  "providerAlias": "cursor",
  "spawnArgs": ["--api-base", "http://localhost:20128"],
  "protocol": "stdio"
}
```

### Cas d’utilisation

- La page « CLI Tools » du tableau de bord répertorie les outils installés et vous aide à configurer chacun d’eux pour qu’il pointe vers OmniRoute
- Les agents personnalisés permettent aux utilisateurs avancés d’enregistrer des CLI internes/propriétaires qu’OmniRoute ne connaît pas par défaut
- Le résultat de la détection alimente la matrice d’empreintes `cli-tools`

### Quand ne PAS utiliser ACP

- ACP n’_exécute_ pas de tâches. Il se contente de détecter et de configurer les CLI. Pour appeler réellement une CLI, vous devez la lancer vous-même avec les variables d’environnement fournies par OmniRoute (`OPENAI_BASE_URL`, `OPENAI_API_KEY`, etc.).

## 3. Agents cloud

**Endpoints OmniRoute :** `/api/v1/agents/tasks/*` (cycle de vie) + `/api/cloud/*` (infrastructure)
**Source :** `src/lib/cloudAgent/`

### Présentation

Une interface uniforme pour les agents de programmation cloud tiers. Vous soumettez une invite et l’URL d’un dépôt ; OmniRoute transmet la requête à l’agent cloud approprié, interroge son état et renvoie les résultats.

### Agents pris en charge (4, tous confirmés dans `src/lib/cloudAgent/agents/`)

- `codex-cloud` — OpenAI Codex Cloud
- `devin` — Cognition Devin
- `jules` — Google Jules

### Cycle de vie

```
POST /api/v1/agents/tasks
  → BaseAgent.createTask() pour chaque classe d’agent
  → le service externe commence le travail
  → une ligne de tâche est créée dans la base de données (cloud_agent_tasks)
  ↓
GET /api/v1/agents/tasks/[id]
  → synchronisation différée de l’état depuis le fournisseur
  → renvoie l’état actuel + le plan + le journal d’activité
  ↓
POST /api/v1/agents/tasks/[id]   (action : "approve" | "message" | "cancel")
  → transmet l’action au fournisseur (ou marque la tâche comme annulée localement)
  ↓
DELETE /api/v1/agents/tasks/[id]
  → annulation locale
```

### Authentification

⚠️ **Tous les endpoints `/api/v1/agents/tasks/*` nécessitent une authentification de gestion** (commit `588a0333`). Depuis la v3.8.0, les appelants utilisant uniquement un jeton Bearer reçoivent une erreur 401.

### Approfondissement

Consultez [CLOUD_AGENT.md](./CLOUD_AGENT.md) pour le contrat `CloudAgentBase`, les spécificités de chaque agent, les détails du schéma et les endpoints d’infrastructure des identifiants.

## Comparaison : A2A et Cloud Agents

Tous deux proposent des « tâches de longue durée », mais à des niveaux différents :

| Aspect                     | A2A                                                                                         | Cloud Agents                                                 |
| -------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Standard                   | Open A2A v0.3                                                                               | Propre à OmniRoute                                           |
| Lieu d’exécution du calcul | Dans OmniRoute (utilise les combinaisons configurées)                                       | Externe (serveurs Codex / Devin / Jules)                     |
| Durée des tâches           | TTL par défaut de 5 min (configurable dans `TaskManager`)                                   | De quelques minutes à plusieurs heures                       |
| Prise en compte du dépôt   | Non (transmet uniquement les prompts)                                                       | Oui (URL du dépôt + branche)                                 |
| Cas d’utilisation          | Collaboration entre agents, routage intelligent en tant que service                         | Déléguer « implémenter la fonctionnalité X dans le dépôt Y » |
| Authentification           | `OMNIROUTE_API_KEY` facultative pour `/a2a` ; gestion pour les assistants REST `/api/a2a/*` | Toujours via la gestion                                      |

## Exemples d’intégration

### Découvrir les fonctionnalités A2A d’OmniRoute

```bash
curl http://localhost:20128/.well-known/agent.json
```

Renvoie l’Agent Card avec les 6 compétences, les transports et la version.

### Appeler OmniRoute en tant qu’agent A2A

```bash
curl -X POST http://localhost:20128/a2a \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "message/send",
    "params": {
      "messages": [{"role": "user", "content": "Route this prompt"}],
      "skill": "smart-routing"
    },
    "id": 1
  }'
```

### Répertorier les agents CLI installés via ACP

```bash
curl http://localhost:20128/api/acp/agents \
  -H "Authorization: Bearer <api-key>"
```

### Ajouter un agent CLI personnalisé

```bash
curl -X POST http://localhost:20128/api/acp/agents \
  -H "Authorization: Bearer <api-key>" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "add",
    "id": "my-custom-cli",
    "name": "My Custom CLI",
    "binary": "/opt/mycli/bin/mycli",
    "versionCommand": "--version",
    "providerAlias": "openai"
  }'
```

### Soumettre une tâche Cloud Agent

```bash
curl -X POST http://localhost:20128/api/v1/agents/tasks \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{
    "providerId": "devin",
    "prompt": "Implement feature X in repo Y",
    "source": {
      "repoName": "user/repo",
      "repoUrl": "https://github.com/user/repo",
      "branch": "main"
    }
  }'
```

### Interroger l’état d’une tâche cloud

```bash
curl http://localhost:20128/api/v1/agents/tasks/<task-id> \
  -H "Cookie: auth_token=..."
```

## Quel outil utiliser et dans quel cas

- **Interface de chatbot / copilote** → `/v1/chat/completions` (compatible avec OpenAI — il ne s’agit pas d’un protocole d’agent)
- **Collaboration multi-agents** → A2A
- **Affichage des CLI locales dans le tableau de bord** → ACP
- **Délégation de tâches de programmation de longue durée à des services cloud** → Cloud Agents

## Architecture interne

```
                ┌─────────────────────┐
                │   Cœur OmniRoute    │
                └─────────────────────┘
                  ↑       ↑        ↑
        ┌─────────┘       │        └─────────┐
        │                 │                  │
    ┌───────┐        ┌─────────┐       ┌────────────┐
    │  A2A  │        │   ACP   │       │  Agents   │
    │ (/a2a)│        │ (/acp)  │       │  cloud    │
    └───────┘        └─────────┘       │ (/v1/agents│
        │                 │            │  /tasks)   │
        ↓                 ↓            └────────────┘
 Agents pairs       Binaires CLI             │
 externes utilisant locaux sur               ↓
 A2A v0.3           l’hôte              Codex Cloud, Cursor,
                                        Devin, Jules
```

## Voir aussi

- [A2A-SERVER.md](./A2A-SERVER.md) — Analyse approfondie d’A2A
- [CLOUD_AGENT.md](./CLOUD_AGENT.md) — Analyse approfondie des agents cloud
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — Intégrations CLI externes (utilise ACP)
- [SKILLS.md](./SKILLS.md) — Framework de compétences (différent des compétences A2A — bac à sable d’exécution locale)
- [API_REFERENCE.md](../reference/API_REFERENCE.md#agents-protocol) — Référence des points de terminaison
- Source : `src/lib/{a2a,acp,cloudAgent}/`
