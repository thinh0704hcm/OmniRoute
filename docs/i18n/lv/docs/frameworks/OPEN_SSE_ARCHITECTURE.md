# open-sse Architecture (Latviešu)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **ĪSUMĀ**: `open-sse/` ir galvenais straumēšanas dzinējs, kas apkalpo katru LLM pieprasījumu OmniRoute sistēmā. Tajā ir aptuveni 1 400 failu, kuros īstenota pieprasījumu apstrādes ķēde, izpildītāji, servisi, MCP serveris un tulkošanas slānis. Šajā ceļvedī izskaidrots, kā šīs daļas darbojas kopā.

**Avots:** `open-sse/` (darbvietas pakotne, aptuveni 1 440 ar testiem nesaistītu `.ts` failu)

---

## Kāpēc atsevišķa darbvietas pakotne?

`open-sse/` ir **patstāvīga darbvieta** OmniRoute monorepozitorijā vairāku iemeslu dēļ:

1. **Atkārtota izmantojamība** — `open-sse` tiek publicēts npm kā `@omniroute/open-sse`, tādēļ citi projekti to var izmantot neatkarīgi
2. **Skaidras robežas** — straumēšanas dzinējs ir nodalīts no OmniRoute specifiskā lietotāja saskarnes/DB slāņa
3. **Veiktspēja** — dzinējam nav Next.js atkarību, tādēļ CLI/serverless kontekstos tiek nodrošināta ātrāka aukstā palaišana
4. **Versiju pārvaldība** — `open-sse` var izlaist jaunās versijas pēc sava grafika

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Augstākā līmeņa struktūra

```
open-sse/
├── index.ts              # Publiskais ieejas punkts
├── types.d.ts            # Publisko tipu eksports
├── package.json          # @omniroute/open-sse
├── config/               # Pakalpojumu sniedzēju konfigurācijas, konstantes, reģistri
├── executors/            # Katra pakalpojumu sniedzēja HTTP izpildītāji (67 + base.ts/index.ts)
├── handlers/             # Pieprasījumu apstrādātāji (chatCore, responses u.c.)
├── lib/                  # Iekšējās utilītas
├── mcp-server/           # Model Context Protocol serveris
├── services/             # Aptuveni 298 servisu moduļi
├── transformer/          # Responses API formāta pārveidotājs
├── translator/           # Formātu tulkošana (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Koplietojamās utilītas (žurnalēšana, kļūdas, straumes u.c.)
```

### Moduļu skaits

| Direktorijs | Faili | Nolūks |
| `executors/` | 167 | Katra pakalpojumu sniedzēja HTTP izpildītāji (vienādošanai izmantota DefaultExecutor fabrika) |
| `handlers/` | 157 | Pieprasījumu ieejas punkti (chatCore, responses, embeddings) |
| `services/` | ~536 | Maršrutēšana, kešatmiņa, ātruma ierobežošana, atsvaidzināšana u.c. |
| `translator/` | 56 | Formātu pārveidošana (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP rīki un transporti |
| `utils/` | ~108 | Dažādās komponentēs izmantotās utilītas (žurnalēšana, kļūdas, straumes) |
| `config/` | ~339 | Pakalpojumu sniedzēju konfigurācijas, konstantes, reģistri |

---

## Pieprasījuma apstrādes ķēde

Katrs LLM pieprasījums iziet cauri **5 posmu apstrādes ķēdei**:

```
                              ┌──────────────┐
   HTTP pieprasījums           │  1. MARŠRUTĒT│   kombinācijas atrisināšana, modeļa atlase
   (Next.js maršruts)          └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TULKOT   │   formāta pārveidošana (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. IZPILDĪT │   pakalpojumu sniedzēja izpildītājs, HTTP, atkārtošana, atslēdzējs
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STRAUMĒT │   SSE pārveidošana, pretspiediens
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. REĢISTRĒT│   lietojuma uzskaite, izsaukumu žurnāls, kļūdu klasificēšana
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP atbilde (SSE vai JSON)
```

### 1. posms: Maršrutēšana (services/combo.ts)

**Ieejas punkts**: `handleComboChat()` failā `services/combo.ts`

Pieprasījums tiek sasaistīts ar konkrētu `(provider, model, account, credentials)` kortežu:

- Atrast kombināciju pēc ID (vai izveidot virtuālu kombināciju `auto/*` modeļiem)
- Lietot maršrutēšanas stratēģiju (prioritāte, svērta atlase, cikliska atlase u.c.)
- Izfiltrēt neveselīgos pakalpojumu sniedzējus (ķēdes atslēdzējs)
- Izvēlēties nākamo derīgo mērķi

`auto/*` modeļiem šajā posmā arī:

- Tiek izpildīts **16 faktoru vērtēšanas** algoritms (`services/autoCombo/`)
- Tiek atlasīts `provider+model` pāris, ņemot vērā darbspēju, izmaksas, latentumu u.c.

### 2. posms: Tulkošana (translator/)

Ja avota formāts (piemēram, OpenAI) atšķiras no mērķa formāta (piemēram, Claude), pieprasījums tiek **tulkots**:

- Sistēmas uzvedne → sistēmas ziņojums
- Rīku definīcijas → pakalpojumu sniedzējam specifisks rīku formāts
- Spriešanas/domāšanas parametri → pakalpojumu sniedzējam specifiski ekvivalenti
- Ziņojumu lomu normalizēšana (`developer` → `system` pakalpojumu sniedzējiem, kas nav OpenAI)

`translator/index.ts` eksportē:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### 3. posms: Izpilde (executors/)

**Ieejas punkts**: `getExecutor(providerId).execute(request, options)`

Visi pakalpojumu sniedzēji izmanto `DefaultExecutor` (`executors/default.ts`), izmantojot `getExecutor()` fabrikas atkāpšanās mehānismu. Izpildītājs:

- Izveido augšupstraumes URL (`buildUrl()`)
- Pievieno pakalpojumu sniedzējam specifiskas galvenes (`buildHeaders()`)
- Pārveido pieprasījuma pamatdaļu (`transformRequest()`)
- Nosūta HTTP pieprasījumu, izmantojot atkārtošanu un eksponenciālu aizturi
- Vajadzības gadījumā apstrādā autentifikācijas atsvaidzināšanu (OAuth pakalpojumu sniedzējiem)

Visi izpildītāji paplašina `BaseExecutor` (`executors/base.ts`, 1170 koda rindas), kas nodrošina:

- Kopīgu atkārtošanas loģiku
- Starpniekservera integrāciju
- Ķēdes atslēdzēja integrāciju
- Lietojuma reģistrēšanas āķus

### 4. posms: Straumēšana (utils/stream.ts)

Straumēšanas atbildēm izpildītājs atgriež **ReadableStream**. Apstrādātājs:

- Novada datus caur SSE pārveidotāju (`createSSETransformStreamWithLogger`)
- Izmanto periodiskus kontrolsignālus, lai noteiktu pārtrauktus savienojumus
- Korekti apstrādā klienta atvienošanos (`pipeWithDisconnect`)
- Pārveido SSE → JSON klientiem, kuri neizmanto straumēšanu

Atbildēm bez straumēšanas izpildītājs atgriež parsētu JSON objektu, kas tiek nodots tālāk bez izmaiņām.

### 5. posms: Reģistrēšana (services/usage.ts)

Pēc atbildes (veiksmīgas vai neveiksmīgas) tiek reģistrēti lietojuma dati:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` no atbildes
- `cost_usd`, kas aprēķināts, izmantojot cenu datus
- `latency_ms`, `status`, `error_class`, ja pieprasījums neizdevās
- Saglabāti tabulā `usage_history`

Izsaukumu žurnālu artefakti (ja iespējoti) tiek ierakstīti `${DATA_DIR}/call_logs/`.

---

## Galveno failu padziļināts apskats

### chatCore.ts (5977 rindas)

**Galvenais pieprasījumu apstrādātājs**. Neraugoties uz tā apjomu, tam ir skaidra struktūra:

```ts
// chatCore.ts pseidostruktūra
export async function handleChat(request: NextRequest) {
  // 1. Autentifikācija + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Pieprasījuma pamatteksta validācija
  const body = await parseRequestBody(request);

  // 3. Formāta noteikšana + pārveidošana
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Kombinācijas maršrutēšana
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Turpināt ar nākamo mērķi
    }
  }

  // 5. Ārkārtas atkāpšanās mehānisms
  return await emergencyFallback(body);
}
```

Lai gan tā ir viena milzīga funkcija, tā ir sadalīta **komentētās sadaļās**, kas atbilst 5 posmu konveijeram.

### combo.ts (4456 koda rindas)

**Maršrutēšanas dzinis**, kas kombināciju pārveido sakārtotā mērķu sarakstā.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Atbalsta **19 maršrutēšanas stratēģijas** (skatiet `src/shared/constants/routingStrategies.ts`):

| Stratēģija          | Darbība                                                                                                       |
| ------------------- | ------------------------------------------------------------------------------------------------------------- |
| `priority`          | Sakārtots saraksts ar pirmā mērķa prioritāti                                                                  |
| `weighted`          | Varbūtību sadalījums atbilstoši katra mērķa svaram                                                            |
| `round-robin`       | Secīga cikliska pārslēgšanās starp mērķiem                                                                    |
| `context-relay`     | Konteksta nodošana starp mērķiem                                                                              |
| `fill-first`        | Kvotas aizpildīšana pirms pāriešanas pie nākamā mērķa                                                         |
| `p2c`               | Divu izvēļu spēka metode                                                                                      |
| `random`            | Vienmērīgi nejauša izvēle                                                                                     |
| `least-used`        | Izvēlēties mērķi ar vismazāko neseno lietojumu skaitu                                                         |
| `cost-optimized`    | Vispirms lētākais darbspējīgais mērķis                                                                        |
| `reset-aware`       | Ņem vērā nodrošinātāja atiestatīšanas intervālus                                                              |
| `reset-window`      | Maršrutēšana, kuras pamatā ir atiestatīšanas intervāls                                                        |
| `headroom`          | Vispirms mērķis ar lielāko atlikušo kvotas rezervi                                                            |
| `strict-random`     | Patiesi vienmērīga izvēle (bez kvalitātes svēršanas)                                                          |
| `auto`              | Izmanto 16 faktoru vērtēšanu (`autoCombo/`)                                                                   |
| `lkgp`              | Vispirms pēdējais zināmais darbspējīgais nodrošinātājs                                                        |
| `context-optimized` | Vispiemērotākais pieprasījumiem ar garu kontekstu                                                             |
| `fusion`            | Paralēli nosūta pieprasījumu ekspertu grupai un pēc tam sintezē rezultātu, izmantojot vērtētāju (`fusion.ts`) |

### base.ts (1170 koda rindas)

**Abstraktais izpildītājs**, kuru paplašina visi 107 izpildītāji. Tas ietver:

- `buildUrl()` — noklusējuma URL izveide (apakšklases to pārraksta pielāgotām vajadzībām)
- `buildHeaders()` — noklusējuma galvenes (autentifikācija, satura tips)
- `transformRequest()` — pēc noklusējuma pārsūta bez izmaiņām
- `execute()` — galvenais HTTP cikls ar atkārtotiem mēģinājumiem, eksponenciālo aizturi un ķēdes pārtraucēju

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Apstrādā visus ar OpenAI/Anthropic saderīgos nodrošinātājus
  // Nodrošinātāji reģistrē konfigurācijas (URL, autentifikāciju, galvenes), taču koplieto izpildītāja loģiku
}
```

Nodrošinātājam specifiskā darbība (autentifikācijas galvenes, pamata URL, versiju galvenes) tiek konfigurēta nodrošinātāju reģistrā, nevis atsevišķās izpildītāju klasēs.

````

---

## Pakalpojumi (117 moduļi)

Pakalpojumi ir **specializēti viena mērķa moduļi**, kurus apvieno apstrādātāji. Galvenās kategorijas:

### Maršrutēšana un kombinēšana

- `combo.ts` — ieejas punkts kombinēti maršrutētiem pieprasījumiem
- `services/autoCombo/` — 16 faktoru vērtēšana, 8 automātiskās maršrutēšanas stratēģijas
- `wildcardRouter.ts` — atrod atbilstības aizstājējzīmju maršrutiem (`gpt-*`)
- `modelFamilyFallback.ts` — T5 atkāpšanās vienas modeļu saimes ietvaros

### Ātruma ierobežošana un kvotas

- `rateLimitManager.ts` — marķieru grozs katrai atslēgas un nodrošinātāja kombinācijai
- `usage.ts` — lietojuma reģistrēšana
- `quotaCache.ts` — kvotu momentuzņēmumi atmiņā

### Konti un pilnvaras

- `tokenRefresh.ts` — OAuth pilnvaras atsvaidzināšana pēc 401 atbildes
- `accountFallback.ts` — pārslēgšanās uz alternatīvu kontu
- `sessionManager.ts` — vairāku mijiedarbības kārtu sesijas stāvoklis

### Intelekts

- `intentClassifier.ts` — pieprasījuma nolūka klasificēšana
- `taskAwareRouter.ts` — maršrutēšana pēc uzdevuma veida
- `thinkingBudget.ts` — spriešanas marķieru piešķiršana
- `contextManager.ts` — maršrutēšanas konteksta ievietošana

### Noturība

- `resilience.ts` — atkārtotu mēģinājumu, aiztures un ķēdes pārtraucēja orķestrēšana
- `emergencyFallback.ts` — pēdējās iespējas atkāpšanās mehānisms
- `modelDeprecation.ts` — automātiska maršrutēšana uz modeļu pēctečiem

### Stāvoklis

- `signatureCache.ts` — dublikātu novēršana pēc pieprasījuma paraksta
- `volumeDetector.ts` — slodzes samazināšana
- `contextHandoff.ts` — sesijas serializācija

### Saspiešana

- `compression/` (apakšdirektorijs) — pilns saspiešanas konveijers
- 39 faili, kas ietver dzinējus, noteikumu pakotnes un adapterus

### Prasmes

- (aprakstītas dokumentā [SKILLS.md](./SKILLS.md))

### Atmiņa

- (aprakstīta dokumentā [MEMORY.md](./MEMORY.md))

---

## Izpildītāji (vairāk nekā 75 faili)

Katram nodrošinātājam ir viens fails. Tie visi paplašina `BaseExecutor` un pārraksta atšķirīgo funkcionalitāti.

### Izplatītākie modeļi

Nodrošinātāji tiek atrasti, izmantojot `getExecutor(providerId)`, kas atgriež konfigurēto izpildītāju. Ar OpenAI/Anthropic saderīgie nodrošinātāji izmanto `DefaultExecutor` (`executors/default.ts`). Nodrošinātājam specifiskā darbība (pamata URL, autentifikācijas galvenes, API versija) tiek konfigurēta direktorijā `open-sse/config/providers/`, savukārt pieprasījuma pamatdaļas transformācijas tiek apstrādātas direktorijā `open-sse/translator/`.

**Pielāgots URL** tiek iestatīts nodrošinātāja konfigurācijā:

```ts
// Nodrošinātāja konfigurācija direktorijā open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Pielāgota autentifikācija** tiek apstrādāta, izmantojot nodrošinātāju reģistra autentifikācijas konfigurāciju (API atslēgu, OAuth, galveņu profilus).

**Pielāgotas pieprasījuma pamatdaļas** transformācijas (piemēram, Anthropic atdala `system` no `messages`) tiek reģistrētas katram nodrošinātājam direktorijā `open-sse/translator/`.

````

### Izpildītāju fabrika

`executors/index.ts` eksportē `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Meklēšana notiek, izmantojot `ExecutorRegistry` (`executors/registry.ts`): katrs specializētais izpildītājs ir deklarēts `executors/index.ts` iebūvētajā tabulā un moduļa ielādes laikā reģistrēts ar `registerExecutor(alias, instance)`; `getExecutor()` pārbauda reģistru un jebkuram nodrošinātājam bez specializēta ieraksta izmanto kešotu `DefaultExecutor`. Pilnā aizstājvārdu → izpildītāju kartējuma atbilstību raksturo etalontests `tests/unit/executor-map-golden.test.ts`.

---

## Tulkotāji

Tulko starp **3 formātiem**: OpenAI, Anthropic, Gemini, kā arī jauno Responses API.

### Kad notiek tulkošana

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Biežākie tulkojumi:

- `OpenAI → Anthropic`: atsevišķs `system` lauks, `x-api-key` galvene
- `OpenAI → Gemini`: `contents`, nevis `messages`, un `systemInstruction`
- `OpenAI → Responses API`: `input` masīvs, `previous_response_id` stāvoklis

### Apstrādātie robežgadījumi

- `developer` loma → `system` pakalpojumu sniedzējiem, kas nav OpenAI
- `system` loma → apvienota ar pirmo lietotāja ziņojumu GLM/ERNIE gadījumā
- `json_schema` → Gemini `responseMimeType` + `responseSchema`
- `tools` → pakalpojumu sniedzējam specifisks rīku formāts
- Domāšanas parametri (o1, Claude) → pakalpojumu sniedzējam specifiski ekvivalenti

---

## MCP serveris

`open-sse/mcp-server/` realizē **Model Context Protocol** serveri:

- **110 rīki** (pakalpojumu sniedzēju pārvaldība, kombinācijas, atmiņa, kešatmiņa, saspiešana, starpniekserveris, prasmes, spēliskošana, spraudņi, Notion, Obsidian, lokālais korpuss)
- **3 transporti**: stdio, SSE, Streamable HTTP
- **33 tvērumi** detalizētai autorizācijai

### Rīku reģistrēšana

Rīki ir reģistrēti kā atsevišķi faili mapē `open-sse/mcp-server/tools/`, un katrs no tiem eksportē nosaukumu, shēmu, apstrādātāju un tvērumu:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transporti

```ts
// stdio (CLI lietojums)
startMcpStdio(server);

// SSE (HTTP balstīta straumēšana)
startMcpSse(server, port);

// Streamable HTTP (modernais MCP)
startMcpStreamable(server, port);
```

### Autorizācija

Katrs rīka izsaukums tiek pakļauts tvēruma pārbaudēm (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Transformatori

`open-sse/transformer/` veic pārveidošanu starp **Chat Completions** un **Responses API** formātiem.

### Kāpēc nepieciešams atsevišķs transformators?

Responses API ir OpenAI jaunais formāts ar **sarunām, kas saglabā stāvokli** (`previous_response_id`). Kad klients nosūta Responses pieprasījumu, OmniRoute:

1. Iekšēji pārveido Responses → Chat Completions
2. Nosūta to pakalpojumu sniedzējam (jebkuram pakalpojumu sniedzējam, kas atbalsta Chat Completions)
3. Pārveido atbildi atpakaļ Responses formātā
4. Straumē pārveidoto atbildi klientam

Transformators (`transformer/responsesTransformer.ts`) nodrošina:

```ts
createResponsesApiTransformStream(): TransformStream
```

Tas apstrādā:

- `response.output_item.added` notikumus
- `response.output_text.delta` notikumus
- `response.completed` notikumu
- Rīku izsaukumu kartēšanu (`function_call` ↔ `tool_calls`)

---

## Konfigurācija

`open-sse/config/` satur konfigurācijas slāni:

| Fails                          | Nolūks                                                         |
| ------------------------------ | -------------------------------------------------------------- |
| `providerRegistry.ts`          | Tērzēšanas modeļu reģistrs 352 pakalpojumu sniedzēju katalogam |
| `providerModels.ts`            | Modeļu aizstājvārdi, formātu kartēšana                         |
| `constants.ts`                 | Taimauti, ierobežojumi, statusa kodi                           |
| `defaultThinkingSignature.ts`  | Noklusējuma Claude domāšanas paraksts                          |
| `modelStrip.ts` (pakalpojumos) | Katram pakalpojumu sniedzējam specifiska lauku noņemšana       |

### Pakalpojumu sniedzēju reģistra shēma

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Zod validācija moduļa ielādes laikā nodrošina, ka visas pakalpojumu sniedzēju konfigurācijas ir derīgas.

---

## Veiktspējas ierobežojumi

Maršrutēšanas dzinējam ir stingri veiktspējas limiti:

| Operācija                                                                  | Mērķis | Mērījums                      |
| -------------------------------------------------------------------------- | ------ | ----------------------------- |
| Kombinācijas atrisināšana                                                  | <10ms  | 50 mērķiem                    |
| Ātruma ierobežojuma pārbaude                                               | <1ms   | Atmiņā glabāts žetonu spainis |
| Modeļu saimes atkāpšanās                                                   | <5ms   | Kešotas saimju definīcijas    |
| Pieprasījuma maršrutēšanas nosūtīšana                                      | <2ms   | Kritiskais izpildes ceļš      |
| **Nekādas bloķējošas I/O darbības maršrutēšanas kritiskajā izpildes ceļā** | —      | Viss ir asinhrons             |

---

## Antiparaugi

❌ **Sinhroni DB izsaukumi failā `combo.ts`** — veiciet iepriekšēju aprēķināšanu un kešošanu
❌ **Atkārtotu mēģinājumu loģika apstrādātājos** — izmantojiet noturības pakalpojuma `retry()`
❌ **Tieša piekļuve nodrošinātāja konfigurācijai** — izmantojiet `providerRegistry` getterus
❌ **Cieti kodētas atkāpšanās ķēdes** — definējiet tās failā `modelFamilyFallback.ts`
❌ **Stāvokļa izmaiņas starp vienlaicīgiem pieprasījumiem** — izmantojiet tikai pieprasījuma tvēruma kontekstu

---

## Jauna komponenta pievienošana

### Jauna pakalpojuma pievienošana

1. Izveidojiet `open-sse/services/[serviceName].ts` ar skaidri noteiktu atbildību
2. Eksportējiet galveno apstrādātāja funkciju un visas konstantes
3. Pievienojiet vienībtestus failā `tests/unit/services/[serviceName].test.mjs`
4. Integrējiet pieprasījumu konveijerā failā `handlers/chatCore.ts` (ja tas ir saistīts ar maršrutēšanu)
5. Atjauniniet maršrutēšanas loģiku failā `combo.ts`, ja pakalpojums ietekmē mērķa izvēli
6. Dokumentējiet šajā failā

### Jauna izpildītāja pievienošana

1. Izveidojiet `open-sse/executors/[provider].ts`, paplašinot `BaseExecutor`
2. Reģistrējiet failā `config/providerRegistry.ts`
3. Pievienojiet `executors/index.ts` fabrikai
4. Pievienojiet izpildītāja vienībtestus
5. Dokumentējiet failā `docs/architecture/ARCHITECTURE.md`

### Jauna MCP rīka pievienošana

1. Izveidojiet vai atjauniniet `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definējiet ievades Zod shēmu
3. Reģistrējiet rīku failā `mcp-server/index.ts`
4. Pievienojiet tvērumu matricai direktorijā `mcp-server/auth/`
5. Pievienojiet vienībtestus

---

## Skatiet arī

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — augsta līmeņa arhitektūra
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — inženiertehniskā uzziņa
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — direktoriju pārskats
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 faktoru vērtēšana
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP serveris
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A serveris
- Avota kods: `open-sse/` (vairāk nekā 400 failu, ~143K koda rindu)
