# Traffic Inspector (Latviešu)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇹 [am](../../../am/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇿 [az](../../../az/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇰 [da](../../../da/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇪 [de](../../../de/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇷 [el](../../../el/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇸 [es](../../../es/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇪 [et](../../../et/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇱 [he](../../../he/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇩 [id](../../../id/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇹 [it](../../../it/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇭 [km](../../../km/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇲 [my](../../../my/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇴 [no](../../../no/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [or](../../../or/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇰 [si](../../../si/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [te](../../../te/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇭 [th](../../../th/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/TRAFFIC_INSPECTOR.md)

---

Traffic Inspector ir OmniRoute iebūvētais HTTPS datplūsmas atkļūdotājs — Charles Proxy / mitmweb / HTTP Toolkit līdzīgs rīks, kas **izprot LLM** un **aģentus**. Tas atrodas `/dashboard/tools/traffic-inspector` un saņem datplūsmu reāllaikā no līdz pat 5 vienlaicīgiem tveršanas avotiem.

**Atrašanās vieta informācijas panelī:** `/dashboard/tools/traffic-inspector`
**Sānjoslas grupa:** Rīki (pēc AgentBridge)
**Skatiet arī:** [`AGENTBRIDGE.md`](./AGENTBRIDGE.md) — AgentBridge ir 1. tveršanas režīms.

---

## §1 Pārskats

### Ar ko Traffic Inspector ir unikāls

| Funkcija                                                                    | mitmweb | Charles | Fiddler | **OmniRoute Traffic Inspector** |
| --------------------------------------------------------------------------- | :-----: | :-----: | :-----: | :-----------------------------: |
| Tīmekļa risinājums                                                          |    ✓    |    ✗    |    ✗    |                ✓                |
| Atvērtais pirmkods                                                          |    ✓    |    ✗    | daļēji  |                ✓                |
| **Izprot aģentus** (zina, vai pieprasījums nāk no Antigravity/Copilot/u.c.) |    ✗    |    ✗    |    ✗    |                ✓                |
| **Izprot LLM** (parsē OpenAI/Anthropic/Gemini struktūru, tokenus, modeli)   |    ✗    |    ✗    |    ✗    |                ✓                |
| **Redzama modeļu kartēšana** (gemini-3-flash → claude-sonnet-4.7)           |    ✗    |    ✗    |    ✗    |                ✓                |
| **Atsevišķi starpniekservera un augšupstraumes latentuma rādītāji**         | daļēji  |    ✗    |    ✗    |                ✓                |
| **Integrēts ar OmniRoute** maršrutēšanu, rezerves mehānismu un izmaksām     |    ✗    |    ✗    |    ✗    |                ✓                |
| **Sistēmas mēroga starpniekservera atkļūdošana** (jebkura lietotne datorā)  |    ✓    |    ✓    |    ✓    |                ✓                |
| **Pielāgotu resursdatoru tveršana** (DNS novirzīšana katram resursdatoram)  |    ✓    |    ✓    |    ✓    |                ✓                |
| **HTTP_PROXY vides režīms**                                                 |    ✓    |    ✓    |    ✓    |                ✓                |
| **Sarunas skats** (vairāku kārtu burbuļi, tool_use/tool_result)             |    ✗    |    ✗    |    ✗    |                ✓                |
| **SSE straumes apvienotājs** (rekonstruē no delta notikumiem)               |    ✗    |    ✗    |    ✗    |                ✓                |
| **Sesijas ierakstīšana** (nosaucama, eksportējama .har/.jsonl formātā)      |    ✗    |    ✓    |    ✓    |                ✓                |

### Arhitektūra vienā rindkopā

`TrafficBuffer` (`src/mitm/inspector/buffer.ts`) ir koplietots atmiņā glabāts gredzenbuferis (pēc noklusējuma 1000 ierakstu; konfigurējams, izmantojot `INSPECTOR_BUFFER_SIZE`). Visi tveršanas avoti tajā raksta, izmantojot `push()`. Bufera klase klasificē katru ierakstu, izmantojot `kindDetector.ts` (nosaka, vai tas ir LLM pieprasījums), aprēķina `contextKey` (sistēmas uzvednes SHA-256 digitālnospiedumu) un veic apraidi visiem WebSocket abonentiem, izmantojot `globalTrafficBuffer.subscribe()`. Informācijas panelis izveido savienojumu, izmantojot `GET /api/tools/traffic-inspector/ws`, savienojuma izveides brīdī saņem momentuzņēmumu un pēc tam — `new`/`update`/`clear` notikumus.

---

## §2 Tveršanas režīmi

Traffic Inspector atbalsta **5 vienlaicīgus datplūsmas tveršanas avotus**. Katru no tiem var neatkarīgi ieslēgt vai izslēgt. Katra `InterceptedRequest` (`src/mitm/inspector/types.ts`) lauks `source` satur vienu no šīm vērtībām: `"agent-bridge"`, `"custom-host"`, `"http-proxy"`, `"system-proxy"` vai `"tproxy"`.

### 1. režīms — AgentBridge (noklusējuma režīms, vienmēr ieslēgts)

**Avots:** AgentBridge apstrādātāji (`src/mitm/handlers/base.ts`)
**Mehānisms:** Katrs `intercept()` izsaukums klasē `MitmHandlerBase` pirms pārsūtīšanas izsauc `hookBufferStart()`, bet pēc pabeigšanas — `hookBufferUpdate()`. Papildu konfigurācija nav nepieciešama — tas darbojas, tiklīdz ir palaists AgentBridge.
**Tvērums:** 9 IDE aģenti, kas konfigurēti AgentBridge
**Piezīme:** `InterceptedRequest` lauks `source` = `"agent-bridge"`

### 2. režīms — Pielāgoti resursdatori (DNS novirzīšana)

**Avots:** Lietotāja definēts resursdatoru saraksts (`inspector_custom_hosts` tabula)
**Mehānisms:** Pievienojot resursdatoru lietotāja saskarnē, failam `/etc/hosts` tiek pievienots `127.0.0.1 <host>` (nepieciešams sudo). Esošais AgentBridge MITM serveris (ports 443) jaunajam resursdatoram dinamiski ģenerē SNI sertifikātu.
**Tvērums:** Jebkura lietojumprogramma, kas izmanto pievienoto resursdatoru — lietojumprogrammas konfigurācija nav jāmaina
**Piezīme:** `source` = `"custom-host"`

Lietošanas piemēri:

- `api.openai.com` pārraudzīšana no Python skriptiem
- `my-internal-llm.company.com` atkļūdošana
- Datplūsmas tveršana no mobilajām ierīcēm tajā pašā tīklā (izmantojot ARP viltošanu — papildu iespēja)

### 3. režīms — HTTP_PROXY klausītājs (ports 8080)

**Avots:** Lietojumprogrammas, kas izmanto `HTTP_PROXY`/`HTTPS_PROXY` vides mainīgos
**Mehānisms:** Sekundārs klausītājs portā 8080 (`src/mitm/inspector/httpProxyServer.ts`), kas darbojas kā standarta tiešais HTTP/HTTPS starpniekserveris. Pieņem `CONNECT` tuneļus (HTTPS) un tiešus HTTP pieprasījumus.
**Tvērums:** Jebkura lietojumprogramma, kas ņem vērā `HTTP_PROXY` vides mainīgo — nav jāmaina DNS un nav nepieciešams sudo
**Piezīme:** `source` = `"http-proxy"`

```bash
# Ātra datplūsmas tveršana vienai komandai:
HTTPS_PROXY=http://127.0.0.1:8080 curl https://api.openai.com/v1/models

# Pastāvīga datplūsmas tveršana čaulas sesijā:
export HTTP_PROXY=http://127.0.0.1:8080
export HTTPS_PROXY=http://127.0.0.1:8080
```

**TLS ierobežojums:** HTTPS `CONNECT` tuneļiem pēc noklusējuma tiek tverti tikai metadati (resursdators, ports un laiks) — TLS saturs netiek atšifrēts. Lai pilnībā pārbaudītu saturu, ieslēdziet opciju "Atšifrēt HTTPS starpniekservera režīmā" (jāiespējo apzināti; AgentBridge sertifikātam jābūt uzticamam).

**Porta konflikts:** Ja ports 8080 jau tiek izmantots, AgentBridge atgriež statusu 409 ar strukturētu kļūdu. Mainiet portu, izmantojot vides mainīgo `INSPECTOR_HTTP_PROXY_PORT`.

### 4. režīms — Sistēmas mēroga starpniekserveris (papildu iespēja, jāiespējo apzināti)

**Avots:** Operētājsistēmas līmeņa starpniekservera iestatījumi (attiecas uz visām ierīcē esošajām lietojumprogrammām)
**Mehānisms:** Izmanto operētājsistēmas API, lai visu HTTP/HTTPS datplūsmu novirzītu caur HTTP_PROXY klausītāju:

- **macOS:** `networksetup -setwebproxy / -setsecurewebproxy`
- **Linux:** `gsettings set org.gnome.system.proxy` + `/etc/environment`
- **Windows:** `netsh winhttp set proxy 127.0.0.1:8080`
  **Tvērums:** Katra ierīcē esošā lietojumprogramma, kas ņem vērā sistēmas starpniekservera iestatījumus
  **Piezīme:** `source` = `"system-proxy"`

**Drošības mehānismi:**

- Automātiskas atspējošanas taimeris (pēc noklusējuma 30 min.; konfigurējams, izmantojot `INSPECTOR_SYSTEM_PROXY_GUARD_MINUTES`)
- Iepriekšējais sistēmas starpniekservera stāvoklis tiek saglabāts datubāzē un atjaunots, atceļot izmaiņas
- Ja lietotājs atstāj lapu, kamēr šis režīms ir aktīvs, informācijas panelī tiek parādīts aicinājums "Notiek sistēmas starpniekservera iestatījumu atjaunošana"
- Lietotāja saskarnē tiek rādīta emblēma `⚠ Papildu iespēja` un nepārprotamas apstiprināšanas izvēles rūtiņa

### 5. režīms — TPROXY caurspīdīgā atšifrēšana (Linux, root, jāiespējo apzināti)

**Avots:** Kodola TPROXY un uz politikām balstīta maršrutēšana (`src/mitm/tproxy/`)
**Mehānisms:** Jaunie lokālie izejošie TCP savienojumi ar mērķa portu (pēc noklusējuma `443`) tiek marķēti ķēdē `mangle OUTPUT`; `ip rule` novirza marķētās paketes uz lokālu piegādi, un ķēdes `mangle PREROUTING` mērķis `TPROXY` tās nodod caurspīdīgam (**IP_TRANSPARENT**) klausītājam (pēc noklusējuma ports `8443`). Klausītājs pārtrauc TLS savienojumu, izmantojot dinamiskas CA iestādes pēc pieprasījuma izsniegtu gala sertifikātu **katram SNI resursdatora nosaukumam**, tver atšifrēto datu apmaiņu un atkārtoti šifrētu pieprasījumu pārsūta sākotnējam galamērķim.
**Tvērums:** **Patvaļīgi** galamērķa resursdatori mērķa portā — nav nepieciešama `/etc/hosts` viltošana, `HTTP_PROXY` vides mainīgais vai sistēmas mēroga starpniekservera iestatījumu mainīšana. Pārtvertā procesa konfigurācija nav jāmaina, taču tam ir jāuzticas dinamiskajai CA iestādei.
**Piezīme:** `source` = `"tproxy"`

**Prasības:** Tikai Linux (**IP_TRANSPARENT** ir pieejams tikai Linux), **CAP_NET_ADMIN** iespēja (root) un vietējais N-API papildinājums, kas jābūvē ar C rīku ķēdi (`npm run build:native:tproxy`). Ja nepieciešamie komponenti nav pieejami, informācijas paneļa slēdzis tiek atspējots un tiek parādīts paskaidrojums "TPROXY atšifrēšanai nepieciešams Linux + root + vietējais papildinājums". Ugunsmūra kārtulas tiek lietotas un atsauktas transakcionāli (avārija nekad neatstāj `mangle` kārtulu), un tās tiek dzēstas pārstartēšanas laikā. Uz SO_MARK balstīts pretcilpas mehānisms novērš starpniekservera paša atkārtoti šifrētās pārsūtīšanas atkārtotu pārtveršanu.

Šī ir apjomīga apakšsistēma ar atsevišķu operatora rokasgrāmatu — pilnu ugunsmūra konfigurāciju, dinamisko CA katram SNI un uzticamības krātuves instalētāju, tikai lokālo maršrutu, pretcilpas mehānisma informāciju un konfigurācijas shēmu skatiet failā `docs/security/MITM-TPROXY-DECRYPT.md` (git; netiek kompilēts sadaļā `/docs`). Slēdzi vada `GET / POST / DELETE /api/tools/agent-bridge/tproxy` (piezīme: maršruts atrodas zem AgentBridge prefiksa, nevis Traffic Inspector prefiksa).

### Tveršanas režīmu salīdzinājums

| Režīms                | Iestatīšana                                |            Sudo?             | Tvērums                                | Piezīmes                                                                                                                               |
| --------------------- | ------------------------------------------ | :--------------------------: | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 1. AgentBridge        | Automātiska                                | Vienreiz (sertifikāts+hosts) | 9 IDE aģenti                           | Ieslēgts pēc noklusējuma                                                                                                               |
| 2. Pielāgoti hosts    | Ievade katram hostam                       |       Jā (hosts fails)       | Jebkura lietotne, kas izmanto šo hostu | Saglabāts DB                                                                                                                           |
| 3. HTTP_PROXY         | `export HTTPS_PROXY=...`                   |              Nē              | Lietotnes, kas ievēro vides mainīgos   | Ports 8080, pēc noklusējuma bez TLS atšifrēšanas                                                                                       |
| 4. Sistēmas mēroga    | Pārslēgt + apstiprināt                     |              Jā              | Visas lietotnes ierīcē                 | Automātiski atspējo pēc 30 min                                                                                                         |
| 5. TPROXY atšifrēšana | Pārslēgt (Linux + vietējais papildinājums) |  Jā (root + CA instalēšana)  | Jebkurš hosts mērķa portā              | Atšifrē patvaļīgus hostus; pēc noklusējuma izslēgts — skatiet `docs/security/MITM-TPROXY-DECRYPT.md` (git; nav kompilēts mapē `/docs`) |

---

## §3 Lietotāja saskarne

### 3.1 Izkārtojums

```
┌─ Trafika inspektors ───────────────────────────────────────────────────┐
│ ┌─ Tveršanas avotu rīkjosla ───────────────────────────────────────┐   │
│ │ [✓ AgentBridge] [✓ Pielāgoti resursdatori (3)] [○ HTTP_PROXY]    │   │
│ │ [○ Sistēma]                                                     │   │
│ └──────────────────────────────────────────────────────────────────┘   │
│ ┌─ Filtru/vadības josla ───────────────────────────────────────────┐   │
│ │ Profils: (●) Tikai LLM  (○) Pielāgots  (○) Viss                 │   │
│ │ [⎉ Pauzēt] [🗑 Notīrīt] [⬇ .har] [● IERAKSTĪT sesiju]           │   │
│ │ ● tiešraidē 482/1k                                               │   │
│ └──────────────────────────────────────────────────────────────────┘   │
├══◀▶══════════════════════════════╬══════════════════════════════════════╤╡
│ PIEPRASĪJUMU SARAKSTS            ║ DETALIZĒTAS INFORMĀCIJAS RŪTS      ▲ │
│ (maināms izmērs)                 ║ [Saruna][Galvenes][Pieprasījums]    │ │
│ ────────────────────────────── │ ║ [Atbilde][Laiks][LLM][Statistika] ▼ │
│ ▎ 14:32 POST 200 12k AG openai ║                                       │
│ ▎ 14:31 POST 200 8k  CP openai ║                                       │
│ ▎ 14:31 POST 503 ⚠   KR ...   ║                                       │
│ ▎ 14:30 GET  200 3k  🌐 pielāgots ║                                    │
└══════════════════════════════════╝══════════════════════════════════════╝
```

### 3.2 Pieprasījumu saraksts (kreisais panelis)

- **Virtualizēts** (`useVirtualList` + `ResizeObserver`): apstrādā 1000 vienumu bez saskarnes sastingšanas
- **Automātiska ritināšana** ar pārslēgu, lai pārbaudes laikā to apturētu
- **Ar krāsām kodēts statuss**: zaļš (2xx), dzeltens (3xx), sarkans (4xx/5xx), pelēks (izpildē)
- **Aģenta emocijzīme**: 🔵 Antigravity, 🟢 Copilot, 🟠 Kiro, 🟣 Codex, 🔷 Cursor, 🟤 Zed, 🟡 Claude Code, ⚫ Open Code, 🌐 pielāgots resursdators
- **Konteksta krāsu josla**: 1px kreisā apmale, kas iekrāsota atbilstoši `contextKey` (sistēmas uzvednes SHA-256) — vizuāli grupē saistītās sarunas
- **Atliktā pamatdaļas ielāde**: detalizētās informācijas cilnēs tiek materializēta tikai atlasītā pieprasījuma pamatdaļa (novērš 1000 × 1MB pamatdaļu renderēšanu)

### 3.3 Detalizētās informācijas rūts — 7 cilnes

| Cilne               | Saturs                                                                                              | Piezīmes                                                                                                      |
| ------------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| **Saruna**          | Vairāku kārtu tērzēšanas burbuļi (sistēma/lietotājs/asistents + tool_use/tool_result)               | Normalizēts no jebkura nodrošinātāja formāta; tiek rādīts tikai tad, ja `detectedKind === "llm"`              |
| **Galvenes**        | Pieprasījuma un atbildes galveņu tabulas                                                            | Sensitīvās galvenes (Authorization, Cookie, api-key) pēc noklusējuma ir maskētas; pārslēgs „Rādīt noslēpumus” |
| **Pieprasījums**    | Neapstrādāta pamatdaļa, JSON koka skats, modeļa lauka emblēma                                       | Skaisti formatēts JSON vai neapstrādāts teksts                                                                |
| **Atbilde**         | Neapstrādāta pamatdaļa vai SSE notikumu saraksts; pārslēgs „Neapstrādāts ↔ Apvienots”               | SSE apvienotājs rekonstruē gala ziņojumu no izmaiņu notikumiem                                                |
| **Laiks**           | Ūdenskrituma diagramma: starpniekservera pieskaitāmās izmaksas pret augšupstraumes latentumu        | Kopējais laiks, TTFB un izmērs                                                                                |
| **LLM informācija** | Nodrošinātājs, modelis, ziņojumu skaits, ievades/izvades marķieri, izmaksu aplēse, kartētais mērķis | Tiek rādīts tikai LLM pieprasījumiem                                                                          |
| **Statistika**      | Recharts: latentuma laika skala, marķieru joslu diagramma, rīku izsaukumu izkliedes diagramma       | Tiek rādīts tikai tad, kad ir ielādēta ierakstīta sesija                                                      |

### 3.4 Rīkjoslas vadīklas

| Vadīkla               | Darbība                                                                                 |
| --------------------- | --------------------------------------------------------------------------------------- |
| ⎉ Pauzēt              | Pārtrauc jaunu pieprasījumu renderēšanu; uzkrājas emblēma „X jauni”                     |
| 🗑 Notīrīt             | Notīra lietotāja saskarnes sarakstu (servera buferis netiek ietekmēts)                  |
| ⬇ Eksportēt .har      | Lejupielādē pašreiz filtrēto sarakstu kā HAR failu                                      |
| ● Ierakstīt sesiju    | Sāk nosauktu ierakstīšanas sesiju                                                       |
| Profila atlasītājs    | Tikai LLM / Pielāgoti resursdatori / Viss                                               |
| Resursdatora filtrs   | Apakšvirknes atbilstība laukā `host`                                                    |
| Aģenta filtrs         | Nolaižamais saraksts: Visi / katram aģentam                                             |
| Statusa filtrs        | Visi / 2xx / 3xx / 4xx / 5xx / kļūda                                                    |
| Avota filtrs          | Visi / agent-bridge / custom-host / http-proxy / system-proxy / tproxy                  |
| **Tiešraides** filtrs | Rāda tikai izpildē esošos (atvērtos) pieprasījumus — `liveOnly` pārslēgs (skatiet §4.6) |

### 3.5 Paneļi ar maināmu izmēru

- Sarakstu un detalizētās informācijas rūti atdala vilkšanas rokturis
- Saraksta platums: vismaz 280px, ne vairāk kā 720px; tiek saglabāts `localStorage` (`inspector.listWidth`)
- Var sakļaut līdz 48px joslai (tikai ikonas); noklikšķiniet uz rindas joslā, lai to izvērstu

---

## §4 LLM apzinātas funkcijas

### 4.1 Veida noteicējs (`src/mitm/inspector/kindDetector.ts`)

Klasificē katru pieprasījumu kā `"llm"`, `"app"` vai `"unknown"`, izmantojot 4 signālus:

1. **Resursdatoru reģistrs** — ~18 zināmi LLM API resursdatoru nosaukumi (OpenAI, Anthropic, Gemini, Groq, Mistral, Together, Fireworks, Cohere, Perplexity, Hugging Face, OpenRouter, xAI, Moonshot u.c.)
2. **Ceļu paraugi** — `/v1/chat/completions`, `/v1/messages`, `/generateContent`, `/v1/responses` u.c.
3. **Pamatteksta struktūra** — nosaka laukus `messages[]` (OpenAI/Claude), `contents[]` (Gemini), `prompt`, `input`
4. **Lietotāja aģenta norādes** — `codex`, `claude`, `gemini`, `antigravity`, `kiro`, `copilot`, `cursor` UA virknē

Pielāgoti resursdatori, kas pievienoti, izmantojot 2. režīmu, pārmanto savu `kind` no veidlapas ievades (pēc noklusējuma `"custom"`).

### 4.2 SSE apvienotājs (`src/mitm/inspector/sseMerger.ts`)

**Neatkarīga tīrtelpas implementācija.** Notikumu parsēšana atbilst
[WHATWG servera sūtīto notikumu algoritmam](https://html.spec.whatwg.org/multipage/server-sent-events.html#parsing-an-event-stream),
savukārt rekonstrukcija atbilst publiskajām [OpenAI](https://platform.openai.com/docs/api-reference/chat/create),
[Anthropic](https://platform.claude.com/docs/en/build-with-claude/streaming) un
[Gemini](https://ai.google.dev/api/generate-content#method:-models.streamgeneratecontent)
straumēšanas shēmām.

Rekonstruē galīgo asistenta ziņojumu no neapstrādātiem SSE izmaiņu notikumiem:

- **Anthropic**: uzkrāj `content_block_delta` pēc indeksa; apstrādā `text_delta`, `input_json_delta` (rīku izsaukumus), `thinking_delta`
- **OpenAI**: uzkrāj Chat Completions izvēles/rīku izsaukumus un Responses API izvades elementus
  pēc indeksa
- **Gemini**: uzkrāj `candidates[i].content.parts`
- **Nezināms**: atgriež neapstrādātos notikumus bez izmaiņām

Cilnē Response tiek rādīts pārslēgs: **"Neapstrādāti notikumi ↔ Apvienoti"**.

### 4.3 Sarunas normalizētājs (`src/mitm/inspector/conversationNormalizer.ts`)

**Neatkarīga tīrtelpas implementācija.** Normalizāciju nosaka lokāli melnās kastes
kontrakti un publiskās OpenAI, Anthropic un Gemini ziņojumu shēmas; netiek izmantots
neviens augšupstraumes implementācijas pirmkods.

Pirms renderēšanas pārveido OpenAI, Anthropic un Gemini ziņojumu formātus vienotā `NormalizedConversation`:

```ts
interface NormalizedConversation {
  request: NormalizedTurn[]; // ziņojumi / saturs / uzvedne no pieprasījuma pamatteksta
  response: NormalizedTurn[]; // asistenta atbilde (apvienota, izmantojot sseMerger)
  contextKey: string | null; // sistēmas uzvednes SHA-256 digitālnospiedums
}
```

Bloku veidi: `text`, `tool_use`, `tool_result`. Cilne Conversation izmanto šo struktūru neatkarīgi no pakalpojumu sniedzēja.

### 4.4 Konteksta atslēgas iekrāsošana (`src/mitm/inspector/contextKey.ts`)

- Aprēķina sistēmas uzvednes `SHA-256` (pirmais ziņojums ar `role:system`, lauks `system` vai Gemini `systemInstruction`)
- Atgriež 12 rakstzīmju heksadecimālu prefiksu (`"a3f9c2..."`)
- Priekšgals kartē atslēgu uz deterministisku HSL krāsu kreisās malas joslai
- **Filtrs "tas pats konteksts"**: noklikšķinot uz `ctx #a3f` mikroshēmas, tiek pievienots filtrs, lai rādītu tikai pieprasījumus ar tādu pašu digitālnospiedumu

Tas ļauj viegli vizuāli atšķirt dažādas "personas" vai uzdevumus, kas darbojas vienā un tajā pašā aģenta sesijā.

### 4.5 LLM metadatu izgūšana

LLM pieprasījumiem cilne LLM Details izgūst:

```ts
interface LlmMetadata {
  provider: string | null; // "openai" | "anthropic" | "gemini" | ...
  apiKind: string | null; // "chat.completions" | "messages" | "embeddings" | ...
  model: string | null; // no pieprasījuma pamatteksta vai atbildes
  messages: number; // dialoga kārtu skaits
  tokensIn: number | null; // usage.prompt_tokens / usage.input_tokens
  tokensOut: number | null; // usage.completion_tokens / usage.output_tokens
  streamed: boolean; // true, ja tā ir SSE atbilde
  mappedTo: string | null; // x-omniroute-mapped galvene
  costEstimateUsd: number | null; // aprēķinātās izmaksas, pamatojoties uz OmniRoute cenām
}
```

### 4.6 Aktīvo pieprasījumu filtrs reāllaikā

Pieprasījuma `status` lauks ir `number | "in-flight" | "error"` — ieraksts tiek
pievienots kā `"in-flight"` pieprasījuma sākšanas brīdī un **atjaunināts turpat**,
kad tiek saņemta atbilde (vai kļūda). Rīkjoslas pārslēgs **"Reāllaiks"**
(`liveOnly`, i18n atslēga `trafficInspector.liveOnly`) ierobežo sarakstu līdz ierakstiem,
kuru `status === "in-flight"`, ļaujot reāllaikā vērot atvērtos savienojumus.

Filtrs ir tīrs klienta puses predikāts failā
`src/lib/inspector/matchesTrafficFilter.ts`:

```ts
if (f.liveOnly && req.status !== "in-flight") return false;
```

Pārslēga stāvoklis atrodas `useTrafficFilters` (inspektora informācijas paneļa āķos) un
tiek kombinēts ar pārējiem filtriem (profils, resursdators, aģents, avots, statuss, konteksts).

### 4.7 Procesa attiecināšana (Linux)

Linux sistēmā katru pārtverto pieprasījumu var attiecināt uz **vietējo procesu,
kas to iniciējis**. `InterceptedRequest` tiek pievienoti divi neobligāti lauki:

```ts
pid?: number;          // iniciējošā procesa identifikators (tikai Linux)
processName?: string;  // iniciējošā procesa nosaukums (tikai Linux)
```

`src/mitm/inspector/processAttribution.ts` kartē savienojuma _klienta_
īslaicīgo portu uz PID un nosaukumu, veicot šādas darbības:

1. Nolasa `/proc/net/tcp` un `/proc/net/tcp6`, lai atrastu porta ligzdas inode
   (`parseProcNetTcpForInode` — tīrs parsētājs, ko var testēt ar testa datiem).
2. Pārmeklē `/proc/<pid>/fd/`, meklējot simbolisku saiti uz `socket:[<inode>]`.
3. Nolasa procesa nosaukumu no `/proc/<pid>/comm`.

Kešatmiņa ar 1 sekundes TTL ierobežo procfs skenēšanas izmaksas slodzes apstākļos. Attiecināšana tiek veikta,
izmantojot **labāko iespējamo pieeju** — jebkuras kļūmes gadījumā tiek atgriezts `null`, un tveršana nekad netiek bloķēta.
macOS/Windows sistēmās funkcija atgriež `null` (aizstājējimplementācija; `lsof`/`GetExtendedTcpTable`
atbalsts tiks pievienots turpmāk).

---

## §5 Sesijas

### 5.1 Sesijas ierakstīšana

1. Rīkjoslā noklikšķiniet uz **"● Ierakstīt sesiju"** → ievadiet nosaukumu (neobligāti)
2. Reāllaika plūsma turpinās kā parasti; sarkans pulsējošs indikators rāda `◉ IER. · <nosaukums> · 00:42 · 23 piepr.`
3. Noklikšķiniet uz **"⏹ Apturēt"** → sesijas momentuzņēmums tiek saglabāts tabulās `inspector_sessions` + `inspector_session_requests`

### 5.2 Ierakstītas sesijas skatīšana

Rīkjoslas nolaižamajā izvēlnē **Sesijas** ir uzskaitītas saglabātās sesijas. Atlasot kādu no tām:

- Tiek ielādēts sesijas momentuzņēmums (fiksēts stāvoklis)
- Reklāmkarogā tiek parādīts: `Tiek skatīta ierakstītā sesija "<nosaukums>" — [Atgriezties tiešraides režīmā]`
- Kļūst pieejama cilne Statistika ar Recharts apkopojumiem

### 5.3 Eksporta formāti

Katru sesiju var eksportēt kā:

| Formāts                    | Lietojums                                                                          |
| -------------------------- | ---------------------------------------------------------------------------------- |
| **HAR** (HTTP Archive 1.2) | Saderīgs ar Chrome DevTools, Charles un Fiddler — importēšanai bezsaistes analīzei |
| **JSONL**                  | Viens `InterceptedRequest` katrā rindā — saderīgs ar `llm-interceptor` formātu     |

Eksportējiet, izmantojot `GET /api/tools/traffic-inspector/sessions/{id}/export.har` vai pogu ⬇ nolaižamajā izvēlnē Sesijas.

---

## §6 Drošība

Traffic Inspector rāda **visu pārtverto HTTPS datplūsmu**, tostarp autorizācijas galvenes un pieprasījumu ķermeņus. Ir ieviesti šādi drošības pasākumi:

| Drošības pasākums                    | Detalizēta informācija                                                                                                                                                                                                           |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **LOCAL_ONLY**                       | Visi maršruti un WebSocket galapunkts ir pieejami tikai no atgriezeniskās cilpas (to `routeGuard.ts` nodrošina pirms autentifikācijas)                                                                                           |
| **Noslēpumu maskēšana**              | Lineārais `maskSecret()` skeneris pirms `TrafficBuffer.push()` aizklāj RFC 6750 Bearer akreditācijas datus, atslēgas ar pakalpojumu sniedzēja prefiksu un garus nejaušus marķierus                                               |
| **Ķermeņa izmēra ierobežojums**      | Ķermeņi, kuru izmērs pārsniedz `INSPECTOR_MAX_BODY_KB` (pēc noklusējuma 1024 KB), tiek saīsināti ar paziņojumu `"(saīsināts veiktspējas dēļ)"`                                                                                   |
| **Galveņu sanitizēšana**             | Nosaukumi tiek pārveidoti par mazajiem burtiem; kadrēšanas, starpniekmezglu un starpniekservera autentifikācijas galvenes tiek atmestas; sīkfaili tiek pilnībā aizklāti; akreditācijas datu vērtības tiek nodotas `maskSecret()` |
| **CSP**                              | Traffic Inspector lapās tiek lietota stingra satura drošības politika, lai novērstu XSS, ko var izraisīt ievadīti atbilžu ķermeņi                                                                                                |
| **Pēc noklusējuma bez persistences** | `TrafficBuffer` tiek glabāts atmiņā un zaudēts pēc servera restartēšanas. Sesijas tiek pastāvīgi saglabātas tikai tad, ja to ierakstīšana ir skaidri sākta                                                                       |

### Piemērotie stingrie noteikumi

| Noteikums                         | Piemērojums                                                                                        |
| --------------------------------- | -------------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`    | Visas HTTP kļūdu atbildes no Traffic Inspector maršrutiem tiek sanitizētas                         |
| **#15 + #17** `isLocalOnlyPath()` | `/api/tools/traffic-inspector/` ir LOCAL_ONLY + SPAWN_CAPABLE (sistēmas starpniekservera komandas) |

### Zināmie ierobežojumi

- **Sistēmas mēroga starpniekservera režīms** ietekmē visas datorā esošās lietotnes, tostarp VPN klientus un SSO. Vienmēr izmantojiet to kopā ar automātiskās atspējošanas taimeri. Neizmantojiet koplietojamos datoros.
- **CONNECT tuneļa HTTPS**: 3. režīms (HTTP_PROXY) HTTPS galamērķiem uztver tikai tuneļa metadatus, ja vien nav iespējota TLS pārtveršana. Tas ir paredzēti — caurspīdīga uztveršana bez uzticības AgentBridge sertifikātam pārtrauktu TLS verifikāciju šajās lietotnēs.
- **Dažos komponentos iekodētas virknes**: dažos lietotāja saskarnes komponentos (F7/F8) ir neliels skaits iekodētu virkņu, kuras vēl neaptver i18n atslēgas. Tās i18n trūkumu pārskatā ir dokumentētas kā zināms ierobežojums; tās tiks migrētas nākamajā darba posmā. Ietekmētās virknes ir dekoratīvas lietotāja saskarnes etiķetes, kuru tulkojums nav nepieciešams funkcionālai lietošanai.

---

## §7 Problēmu novēršana

### WebSocket savienojuma pārtraukums

Ja tiešraides plūsmā tiek rādīts "Atvienots":

1. Pārbaudiet, vai serveris joprojām darbojas: `GET /api/tools/traffic-inspector/capture-modes`
2. Pārlādējiet lapu — WebSocket atkārtoti izveido savienojumu un saņem jaunu momentuzņēmumu
3. Ja serveris tika restartēts, atmiņas buferis tika iztīrīts — vecie ieraksti ir zuduši, ja vien netika ierakstīta sesija

### Porta 8080 konflikts

Ja HTTP_PROXY režīmu neizdodas palaist:

```bash
lsof -i :8080    # atrast procesu
```

Mainiet portu:

```bash
# .env
INSPECTOR_HTTP_PROXY_PORT=8888
```

### Sistēmas starpniekservera iestatījumi netika atjaunoti

Ja OmniRoute avarē, kamēr ir aktīvs visas sistēmas starpniekservera režīms:

**macOS:**

```bash
networksetup -setwebproxystate Wi-Fi off
networksetup -setsecurewebproxystate Wi-Fi off
```

**Linux (GNOME):**

```bash
gsettings set org.gnome.system.proxy mode 'none'
```

**Windows:**

```cmd
netsh winhttp reset proxy
```

Nākamajā ielādes reizē informācijas panelis piedāvās arī opciju "Atjaunot sistēmas starpniekservera iestatījumus", ja tas konstatēs, ka DB stāvoklis norāda uz iepriekš aktīvu starpniekserveri.

### Buferis ir pilns

Kad buferis sasniedz `INSPECTOR_BUFFER_SIZE` (noklusējuma vērtība ir 1000), jaunie ieraksti izspiež vecākos. Ja svarīgi pieprasījumi tiek zaudēti:

- Palieliniet `INSPECTOR_BUFFER_SIZE` (piemēram, līdz 5000) — lielāks atmiņas patēriņš apmaiņā pret ilgāku saglabāšanu
- Ierakstiet sesiju, lai saglabātu attiecīgo laika intervālu DB

---

## §8 API atsauce

Visi maršruti ir `LOCAL_ONLY` (pieejami tikai no lokālās atgriezeniskās cilpas) un `SPAWN_CAPABLE` (sistēmas starpniekservera komandas). Skatiet `src/server/authz/routeGuard.ts`.

Bāzes ceļš: `/api/tools/traffic-inspector/`

### Pieprasījumu pārvaldība

| Metode | Ceļš                        | Apraksts                                                                                     |
| ------ | --------------------------- | -------------------------------------------------------------------------------------------- |
| GET    | `/requests`                 | Uzskaitīt pieprasījumus (filtrējami: `?profile=llm&host=&agent=&status=&source=&sessionId=`) |
| GET    | `/requests/{id}`            | Atsevišķa pieprasījuma informācija                                                           |
| DELETE | `/requests`                 | Iztīrīt atmiņas buferi                                                                       |
| POST   | `/requests/{id}/replay`     | Atkārtoti izpildīt to pašu pieprasījumu, izmantojot OmniRoute maršrutētāju                   |
| PUT    | `/requests/{id}/annotation` | Saglabāt vai atjaunināt pieprasījuma piezīmi                                                 |

### WebSocket

| Metode | Ceļš  | Apraksts                                                                                                 |
| ------ | ----- | -------------------------------------------------------------------------------------------------------- |
| GET    | `/ws` | Tiešraides WebSocket plūsma. Savienojoties nosūta `snapshot`, pēc tam — `new`/`update`/`clear` notikumus |

### Eksportēšana

| Metode | Ceļš          | Apraksts                                               |
| ------ | ------------- | ------------------------------------------------------ |
| GET    | `/export.har` | Eksportēt pašreizējo filtrēto sarakstu HAR 1.2 formātā |

### Pielāgotie resursdatori

| Metode | Ceļš            | Apraksts                                                 |
| ------ | --------------- | -------------------------------------------------------- |
| GET    | `/hosts`        | Uzskaitīt pielāgotos resursdatorus                       |
| POST   | `/hosts`        | Pievienot resursdatoru (automātiski rediģē `/etc/hosts`) |
| DELETE | `/hosts/{host}` | Noņemt resursdatoru                                      |
| PATCH  | `/hosts/{host}` | Pārslēgt `enabled`                                       |

### Tveršanas režīmi

| Metode | Ceļš                           | Apraksts                                                                                                                  |
| ------ | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| GET    | `/capture-modes`               | AgentBridge / pielāgoto resursdatoru / HTTP_PROXY / sistēmas starpniekservera režīmu stāvoklis un `tls-intercept` slēdzis |
| POST   | `/capture-modes/http-proxy`    | Palaist/apturēt HTTP_PROXY klausītāju (`{action: "start"\|"stop"}`)                                                       |
| POST   | `/capture-modes/system-proxy`  | Lietot/atjaunot visas sistēmas starpniekservera iestatījumus (`{action: "apply"\|"revert"}`)                              |
| POST   | `/capture-modes/tls-intercept` | Pārslēgt HTTPS satura atšifrēšanu starpniekservera režīmā (`{enabled: boolean}`)                                          |

> **TPROXY atšifrēšanu** (5. tveršanas režīms) pārvalda **atsevišķs** maršruts ar
> AgentBridge prefiksu — `GET / POST / DELETE /api/tools/agent-bridge/tproxy` —, nevis
> zem `/api/tools/traffic-inspector/`. Skatiet
> `docs/security/MITM-TPROXY-DECRYPT.md` (git; nav kompilēts sadaļā `/docs`).

### Sesijas

| Metode | Ceļš                        | Apraksts                                                           |
| ------ | --------------------------- | ------------------------------------------------------------------ |
| POST   | `/sessions`                 | Sākt ierakstīšanu (`{name?: string}`)                              |
| PATCH  | `/sessions/{id}`            | Apturēt vai pārdēvēt (`{action: "stop"\|"rename", name?: string}`) |
| GET    | `/sessions`                 | Uzskaitīt visas saglabātās sesijas                                 |
| GET    | `/sessions/{id}`            | Sesijas momentuzņēmums (visi pieprasījumi)                         |
| DELETE | `/sessions/{id}`            | Dzēst sesiju                                                       |
| GET    | `/sessions/{id}/export.har` | Eksportēt sesiju HAR 1.2 formātā                                   |

### Iekšējā uzņemšana (D4 atkāpšanās mehānisms)

| Metode | Ceļš               | Apraksts                                                                                                            |
| ------ | ------------------ | ------------------------------------------------------------------------------------------------------------------- |
| POST   | `/internal/ingest` | Pieņem pārtverto pieprasījumu no `server.cjs` tranzīta ceļa; nepieciešama `INSPECTOR_INTERNAL_INGEST_TOKEN` galvene |

Pilnas OpenAPI shēmas: `docs/openapi.yaml` → tags `Traffic Inspector`.
