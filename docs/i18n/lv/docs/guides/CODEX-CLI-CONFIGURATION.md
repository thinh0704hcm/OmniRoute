# Codex CLI — Configuration with OmniRoute (Latviešu)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CODEX-CLI-CONFIGURATION.md)

---

Pilnīga rokasgrāmata par Codex CLI izmantošanu ar OmniRoute kā OpenAI saderīgu aizmugursistēmu.

---

> **TOML ir vienīgais efektīvais formāts.** Mūsdienu Codex lasa tikai
> `~/.codex/config.toml` (pārbaudīts ar codex-cli 0.147.0: `codex --help`
> dokumentē `-c/--config` pārrakstīšanas opcijas, kas ir „ielādētas no
> `~/.codex/config.toml`”). Vecais `~/.codex/config.yaml` piederēja mantotajam
> npm CLI un tiek klusējot ignorēts. Informācijas paneļa ģenerators
> (`/api/cli-tools/apply`, rīks `codex`) raksta TOML, izmantojot konservatīvu
> sapludināšanu — esošās atslēgas un citu nodrošinātāju bloki tiek saglabāti,
> API atslēga paliek `OMNIROUTE_API_KEY` (nekad failā), un atlikušais mantotais
> `config.yaml` tiek norādīts migrācijas piezīmē, to nemainot.

## Ievietošanai gatavs config.toml

Aizstājiet `<YOUR_HOST>` un `<YOUR_KEY>` ar savām vērtībām:

```toml
# ~/.codex/config.toml
model                          = "cx/gpt-5.5"
model_provider                 = "omniroute"
model_reasoning_effort         = "xhigh"
model_context_window           = 400000
model_auto_compact_token_limit = 350000
tool_output_token_limit        = 32768    # vēstures glabāšanas ierobežojums katram rīka izsaukumam

[model_providers.omniroute]
name                 = "OmniRoute"
base_url             = "http://<YOUR_HOST>:20128/v1"
env_key              = "OMNIROUTE_API_KEY"
requires_openai_auth = false
wire_api             = "responses"
```

```bash
# ~/.bashrc vai ~/.zshrc — faktiskā atslēgas vērtība, nekad config.toml failā
export OMNIROUTE_API_KEY="<YOUR_KEY>"
```

### macOS: ChatGPT lietotnē iekļautais Codex

Ja instalējāt Codex, izmantojot ChatGPT darbvirsmas lietotni, `codex` binārais
fails var atrasties tikai lietotnes pakotnē un vēl nebūt pieejams čaulas `PATH`.
Pievienojiet resursu direktoriju čaulas startēšanas failam:

```bash
export PATH="/Applications/ChatGPT.app/Contents/Resources:$PATH"
```

Atveriet jaunu čaulu un pēc tam pārbaudiet:

```bash
command -v codex
codex --version
```

### Lokāls OmniRoute bez autentifikācijas: pietiek ar viettura atslēgu

Codex pārbauda, vai `env_key` norādītais vides mainīgais pastāv, **pirms**
pirmais pieprasījums atstāj CLI. Ja jūsu **lokālajai** OmniRoute instancei
nav nepieciešama autentifikācija, der jebkurš netukšs vietturis:

```bash
export OMNIROUTE_API_KEY="${OMNIROUTE_API_KEY:-local}"
```

Ja OmniRoute serveris ir aizsargāts vai attālināts, tā vietā izmantojiet īstu atslēgu.

> **Biežākās resursdatora opcijas**
>
> | Piekļuve            | URL                           |
> | ------------------- | ----------------------------- |
> | Lokālais tīkls      | `http://192.168.0.1:20128/v1` |
> | Tailscale           | `http://100.x.x.x:20128/v1`   |
> | Atgriezeniskā cilpa | `http://localhost:20128/v1`   |

---

## `wire_api = "responses"` — kāpēc tas darbojas ar visiem modeļiem

Codex CLI 2026. gada februārī atzīmēja `wire_api = "chat"` (Chat Completions) kā novecojušu un tagad **pieprasa** `wire_api = "responses"` (OpenAI Responses API). Kopš v0.138 opcijas `wire_api = "chat"` iestatīšana izraisa tūlītēju avāriju palaišanas laikā.

Daudzi nodrošinātāji, tostarp GLM un Kimi, joprojām piedāvā tikai Chat Completions galapunktu. DeepSeek V4 tagad piedāvā gan vietēju Responses API, gan ar Anthropic saderīgu galapunktu; OmniRoute pēc noklusējuma izmanto Responses un ļauj katram DeepSeek savienojumam izvēlēties Anthropic saderību.

**OmniRoute to atrisina nemanāmi:**

```
Codex CLI
  → wire_api = "responses"
  → POST /v1/responses (OmniRoute)
    → OmniRoute izvēlas nodrošinātāja vietējo protokolu un vajadzības gadījumā to pārveido
    → POST /responses (DeepSeek V4) vai /chat/completions (Mistral / GLM / Kimi / citi)
```

Izmantojot OmniRoute, jums nekad nav nepieciešams atsevišķs tulkošanas starpniekserveris. **Visi modeļi izmanto `wire_api = "responses"`** — par pārējo parūpējas OmniRoute.

> **`wire_api` ir noklusējuma iestatījums** — lauka noklusējuma vērtība ir `"responses"`, un to var pilnībā izlaist no `config.toml`. Iestatiet to tieši tikai tad, ja dokumentējat nolūku.

---

## Konteksta logs un sablīvēšana

### Tokenu konfigurācijas lauki

| Lauks                            | Apraksts                                                                                                                                                                                                 |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `model_context_window`           | Aktīvā modeļa kopējais tokenu budžets. Iestatiet to atbilstoši modeļa norādītajam ierobežojumam.                                                                                                         |
| `model_auto_compact_token_limit` | Slieksnis, kas aktivizē automātisku vēstures sablīvēšanu. **Maksimums: 90% no `model_context_window`** — vērtības virs 90% tiek nemanāmi ignorētas.                                                      |
| `tool_output_token_limit`        | Vēsturē saglabājamo tokenu ierobežojums katra rīka izsaukuma izvadei. Neļauj vienai lielai rīka atbildei aizpildīt logu. **Tas nav maksimālais izvades apjoms** — tas ir vēstures krātuves ierobežojums. |
| `compact_prompt`                 | Iekļauts sablīvēšanas laikā izmantotās sistēmas uzvednes pārrakstījums (v0.138+).                                                                                                                        |

> **Piezīme par `model_max_output_tokens`**: šis lauks **nav daļa no Codex CLI konfigurācijas shēmas** (tas nav atrodams Codex Rust kodu bāzē). Ja tas ir iestatīts, tas tiek nemanāmi ignorēts. Nepaļaujieties uz to — izmantojiet `tool_output_token_limit`, lai kontrolētu, cik liela rīka izvades daļa tiek saglabāta vēsturē.

### Konteksta logi pēc modeļa

| Modelis                              | OmniRoute ID                         | Konteksta logs           | `auto_compact` | `tool_output_limit` |
| ------------------------------------ | ------------------------------------ | ------------------------ | -------------- | ------------------- |
| GPT-5.5                              | `cx/gpt-5.5`                         | 400k uzticami (1M maks.) | 350,000        | 32,768              |
| Kimi K2.7 (spriešana)                | `kmc/kimi-k2.7`                      | 131,072                  | 112,000        | 32,768              |
| Kimi K2.6                            | `kmc/kimi-k2.6`                      | 131,072                  | 112,000        | 32,768              |
| GLM-5.2 / 5.2-max (spriešana)        | `glm/glm-5.2`                        | 131,072                  | 112,000        | 32,768              |
| MiMo V2.5 Pro (spriešana)            | `opencode-go/mimo-v2.5-pro`          | 131,072                  | 112,000        | 32,768              |
| Qwen 3.7 Plus (spriešana)            | `opencode-go/qwen3.7-plus`           | 32,768                   | 28,000         | 16,384              |
| DeepSeek V4 Pro (OllamaCloud)        | `ollamacloud/deepseek-v4-pro`        | 131,072                  | 112,000        | 32,768              |
| DeepSeek V4 Pro                      | `ds/deepseek-v4-pro`                 | 1,000,000                | 900,000        | 65,536              |
| MiMo V2.5                            | `opencode-go/mimo-v2.5`              | 131,072                  | 112,000        | 32,768              |
| Gemma 4 31B (OllamaCloud)            | `ollamacloud/gemma4:31b`             | 32,768                   | 28,000         | 16,384              |
| Nemotron 3 Super (OllamaCloud)       | `ollamacloud/nemotron-3-super`       | 32,768                   | 28,000         | 16,384              |
| GPT-OSS 20B (OllamaCloud)            | `ollamacloud/gpt-oss:20b`            | 32,768                   | 28,000         | 16,384              |
| DeepSeek V4 Flash (OllamaCloud)      | `ollamacloud/deepseek-v4-flash`      | 65,536                   | 56,000         | 16,384              |
| Gemini 3 Flash Preview (OllamaCloud) | `ollamacloud/gemini-3-flash-preview` | 1,000,000                | 850,000        | 32,768              |
| GLM-5 Turbo                          | `glm/glm-5-turbo`                    | 131,072                  | 112,000        | 16,384              |
| GLM-4.7 Flash                        | `glm/glm-4.7-flash`                  | 131,072                  | 112,000        | 16,384              |
| Mistral Large Latest                 | `mistral/mistral-large-latest`       | 262,144                  | 220,000        | 16,384              |

> **Sablīvēšanas formula:** `effective_window = model_context_window - min(tool_output_token_limit, 20000)`. Vērtības virs 20k nemaina sablīvēšanas aktivizēšanas slieksni.

> **Praktisks ieteikums:** iestatiet `model_auto_compact_token_limit` uz 85–88% no `model_context_window`. Nekad nepārsniedziet 90% — šāda vērtība tiek nemanāmi ignorēta.

---

## Modeļa prefikss: `cx/`

Visi Codex modeļi platformā OmniRoute izmanto prefiksu `cx/`:

| Codex CLI nosaukums     | OmniRoute modelis  |
| ----------------------- | ------------------ |
| `cx/gpt-5.5`            | GPT-5.5 standarta  |
| `cx/gpt-5.4`            | GPT-5.4 standarta  |
| `cx/gpt-5.4-mini`       | GPT-5.4 mini       |
| `cx/gpt-5.1-codex-mini` | GPT-5.1 Codex mini |

Citi pakalpojumu sniedzēji izmanto savus prefiksus (`kmc/`, `glm/`, `ds/`, `ollamacloud/`, `opencode-go/`, `mistral/`) — prefikss atbilst OmniRoute pakalpojumu sniedzēja aizstājvārdam.

---

## Spriešanas intensitāte

Nosaka, cik daudz modelis „domā” pirms atbildes sniegšanas.

| Vērtība  | Izmantojiet šādiem uzdevumiem                              |
| -------- | ---------------------------------------------------------- |
| `none`   | Bez spriešanas — tieša atbilde                             |
| `low`    | Triviāli uzdevumi (pārdēvēšana, formatēšana)               |
| `medium` | **Servera noklusējums**, ja nav norādīts                   |
| `high`   | Vidējas sarežģītības uzdevumi (refaktorēšana, atkļūdošana) |
| `xhigh`  | Arhitektūra, padziļināta analīze, sarežģītas problēmas     |

```bash
# Pārrakstīšana katram izsaukumam
codex -c model_reasoning_effort=low "pārdēvē mainīgo x par count"
codex -c model_reasoning_effort=xhigh "izstrādā autentifikācijas moduli"
```

Iestatiet arī spriešanas **kopsavilkumu**, lai Desktop varētu attēlot domāšanas tekstu (ne tikai šifrētus datu blokus):

```toml
# ~/.codex/config.toml
model_reasoning_effort = "xhigh"   # vai ultra, ja tas tiek atbalstīts
model_reasoning_summary = "detailed"  # auto | concise | detailed | none
```

### OmniRoute domāšanas budžets (servera iestatījums)

OmniRoute resursdatorā opcijai **Settings → AI → Thinking Budget** jābūt iestatītai uz **`passthrough`**, lai Codex intensitātes un kopsavilkuma iestatījumi sasniegtu augšupējo pakalpojumu. Režīms **`auto` strips** noņem visus klienta laukus `reasoning` / `reasoning_effort` un atstāj domāšanas paneļus tukšus pat tad, ja Codex ir konfigurēts pareizi.

Pilna rokasgrāmata: [THINKING_BUDGET.md](./THINKING_BUDGET.md).

Saspiešana un uzvedņu kešatmiņa ir neatkarīgas un turpina darboties režīmā `passthrough`.

---

## Profili — nosauktas konfigurācijas katram modelim/darbplūsmai

Profili ļauj pārslēgt modeli un konteksta logu ar vienu karodziņu. Katrs profils ir plakans
`~/.codex/<name>.config.toml` fails, kura iestatījumi tiek uzklāti virs pamata `config.toml`.

> **Nosaukšanas noteikums (Codex CLI v0.137+):** failam jābūt `~/.codex/<name>.config.toml` — **bez prefiksa `profile-`**.
> CLI atrisina `-p kimi-k27` → `~/.codex/kimi-k27.config.toml`. Ja fails netiek atrasts, klusām tiek lietoti noklusējuma iestatījumi.

```bash
codex --profile kimi-k27 "analizē 10 tūkstošus šīs kodu bāzes rindu"
codex -p glm52 "arhitektūras pārskats"
codex --profile deepseek-flash "pārdēvē mainīgo"   # ātrs, lēts
```

### Intensitātes profili (tas pats modelis, atšķirīga intensitāte)

```bash
codex -p low      # cx/gpt-5.5, intensitāte=low
codex -p medium   # cx/gpt-5.5, intensitāte=medium
codex -p high     # cx/gpt-5.5, intensitāte=high
codex -p xhigh    # cx/gpt-5.5, intensitāte=xhigh (noklusējums)
codex -p chat     # cx/gpt-5.5, intensitāte nav iestatīta (servera noklusējums)
```

### Domāšanas modeļi (intensīva domāšana) — xhigh + detalizēts kopsavilkums

| Profils      | Modelis                     | Konteksts | Izmantojiet šādiem uzdevumiem     |
| ------------ | --------------------------- | --------- | --------------------------------- |
| `kimi-k27`   | `kmc/kimi-k2.7`             | 128k      | Labākā domāšanas kvalitāte (Kimi) |
| `glm52`      | `glm/glm-5.2`               | 128k      | GLM domāšana                      |
| `glm52max`   | `glm/glm-5.2-max`           | 128k      | Maksimāla GLM domāšana            |
| `mimo-pro`   | `opencode-go/mimo-v2.5-pro` | 128k      | MiMo domāšana                     |
| `qwen37plus` | `opencode-go/qwen3.7-plus`  | 32k       | Qwen domāšana                     |

### Labi modeļi — augsta intensitāte

| Profils        | Modelis                       | Konteksts | Izmantojiet šādiem uzdevumiem                     |
| -------------- | ----------------------------- | --------- | ------------------------------------------------- |
| `kimi-k26`     | `kmc/kimi-k2.6`               | 128k      | Vispārējam lietojumam (Kimi)                      |
| `deepseek-pro` | `ollamacloud/deepseek-v4-pro` | 128k      | DeepSeek Pro, izmantojot OllamaCloud              |
| `deepseek`     | `ds/deepseek-v4-pro`          | 1M        | Tiešs DeepSeek Pro savienojums, milzīgs konteksts |
| `mimo`         | `opencode-go/mimo-v2.5`       | 128k      | MiMo vispārējam lietojumam                        |

### Vienkārši modeļi — bez spriešanas intensitātes

| Profils    | Modelis                        | Konteksts | Izmantojiet šādiem uzdevumiem    |
| ---------- | ------------------------------ | --------- | -------------------------------- |
| `gemma4`   | `ollamacloud/gemma4:31b`       | 32k       | Izmaksu ziņā efektīvs un spējīgs |
| `nemotron` | `ollamacloud/nemotron-3-super` | 32k       | NVIDIA Nemotron                  |
| `gptoss`   | `ollamacloud/gpt-oss:20b`      | 32k       | Atvērtā pirmkoda GPT             |

### Ātri modeļi — zema intensitāte

| Profils          | Modelis                              | Konteksts | Izmantojiet šādiem uzdevumiem |
| ---------------- | ------------------------------------ | --------- | ----------------------------- |
| `deepseek-flash` | `ollamacloud/deepseek-v4-flash`      | 64k       | Ātriem uzdevumiem             |
| `gemini-flash`   | `ollamacloud/gemini-3-flash-preview` | 1M        | Ļoti ātrs, milzīgs konteksts  |
| `glm5turbo`      | `glm/glm-5-turbo`                    | 128k      | GLM Turbo                     |
| `glm47flash`     | `glm/glm-4.7-flash`                  | 128k      | GLM Flash                     |
| `mistral`        | `mistral/mistral-large-latest`       | 256k      | Mistral Large                 |

### Ātrās izvēles tabula

| Uzdevums                                | Ieteicamais profils                               |
| --------------------------------------- | ------------------------------------------------- |
| Pārdēvēšana, formatēšana, sagataves     | `--profile deepseek-flash` vai `-p low`           |
| Skaidrošana, vienkārša pārskatīšana     | `-p chat` vai `-p gemini-flash`                   |
| Atkļūdošana, mērena refaktorēšana       | `-p medium` vai `-p kimi-k26`                     |
| Jauna funkcija, sarežģīti testi         | `-p high` vai `-p mimo`                           |
| Arhitektūra, padziļināta analīze        | `-p kimi-k27` vai `-p glm52` vai `-p xhigh`       |
| Koda bāzes analīze (vajag 1M kontekstu) | `--profile deepseek` vai `--profile gemini-flash` |
| Maksimāla spriešanas kvalitāte          | `-p glm52max` vai `-p mimo-pro`                   |
| Izmaksu ziņā ekonomisks                 | `-p gemma4` vai `-p gptoss`                       |

---

## Profilu automātiska ģenerēšana ar `omniroute setup-codex`

Ja palaižat OmniRoute VPS serverī, varat automātiski ģenerēt profilu failus no aktuālā modeļu kataloga:

```bash
# No VPS (izmanto lokālo OmniRoute portā 20128)
omniroute setup-codex

# No jebkuras ierīces — norādiet savu VPS
omniroute setup-codex --remote http://100.x.x.x:20128 --api-key sk-xxx

# Priekšskatījums, neierakstot failus
omniroute setup-codex --remote http://100.x.x.x:20128 --dry-run

# Ģenerēt tikai GLM un Kimi profilus
omniroute setup-codex --only glm,kimi

# Ierakstīt pielāgotā direktorijā
omniroute setup-codex --codex-home /path/to/.codex
```

Komanda izgūst `/v1/models`, zināmajiem modeļiem izmanto optimizētus profilus, citiem saderīgiem teksta modeļiem izmanto kataloga metadatus un katram izveido `~/.codex/<name>.config.toml`. Komanda ir idempotenta — to var droši palaist atkārtoti.

OmniRoute var arī **automātiski sinhronizēt** šos pašus profilu failus pēc tam, kad veiksmīga nodrošinātāja modeļu atklāšana/importēšana maina aktuālo katalogu. Šī funkcija ir **jāiespējo, un pēc noklusējuma tā ir izslēgta**: ieslēdziet to **CLI Code informācijas panelī** ("CLI profile auto-sync" → Codex) vai iestatiet `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES=true` (tiek ņemts vērā arī `CLI_ALLOW_CONFIG_WRITES`, kas pēc noklusējuma ir ieslēgts). Kad funkcija ir iespējota, tā ieraksta tikai atsevišķus `~/.codex/*.config.toml` profilu failus; tā nekad nemaina aktīvo/noklusējuma `~/.codex/config.toml`, Codex-lb iestatījumus, autentifikāciju vai nodrošinātāja izvēli.

---

## Codex palaišana ar `omniroute launch-codex`

Pirms Codex palaišanas tiek pārbaudīts jūsu OmniRoute instances darbspējas stāvoklis:

```bash
# Palaist ar lokālo OmniRoute (noklusējuma ports 20128)
omniroute launch-codex

# Palaist ar konkrētu profilu
omniroute launch-codex --profile kimi-k27

# Palaist ar attālu VPS
omniroute launch-codex --remote http://100.x.x.x:20128/v1 --api-key sk-xxx

# Nodot papildu argumentus programmai codex
omniroute launch-codex --profile glm52 -- --yolo "fix this bug"
```

Codex ir arī mērķis abiem vispārīgajiem, uz manifestu balstītajiem ieejas punktiem
(`bin/cli/cli-manifest.mjs`):

```bash
# Interaktīva modeļa izvēle → izveido ~/.codex/<name>.config.toml (TOML, env_key)
omniroute configure codex

# Palaist codex, ievadot omniroute nodrošinātāju ar -c karodziņiem (konfigurācija netiek ierakstīta)
omniroute run codex
```

---

## Jaunas Codex CLI funkcijas (v0.138–v0.141)

| Versija | Funkcija                                                                                                                                                                                                          |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v0.138  | Pāreja uz darbvirsmas lietotni (`/app`), v2 personiskās piekļuves pilnvaras, `--profile` kā vienīgais profila atlasītājs (mantotās failā iekļautās `[profiles]` tabulas izraisa avāriju palaišanas laikā)         |
| v0.139  | `web_search = "live"` — vietējā meklēšana tīmeklī no koda režīma; `oneOf`/`allOf` MCP rīku shēmās; `codex doctor` vides diagnostika                                                                               |
| v0.140  | `/usage` pilnvaru lietojuma skats sesijas laikā; `/import` no Claude Code sesijām; apakškomanda `codex delete <SESSION_ID>`; Amazon Bedrock autentifikācija, izmantojot `aws` objektu nodrošinātāja konfigurācijā |
| v0.141  | Pilnīgi šifrēts Noise retranslators attāliem izpildītājiem; SQLite WAL labojums; P-521 TLS atbalsts                                                                                                               |

### Jauni `config.toml` lauki (pēc v0.137)

```toml
# Vietējā meklēšana tīmeklī (v0.139)
web_search = "live"   # "disabled" | "cached" | "live"

# Atsevišķa izstrādātāja sistēmas uzvedne (v0.138)
developer_instructions = "Vienmēr dod priekšroku funkcionālajam stilam."

# Pielāgota saspiešanas uzvedne
compact_prompt = "Apkopo iepriekš minēto aizzīmju sarakstā."

# Novirzīt /review uz lētāku modeli
review_model = "glm/glm-5-turbo"

# OpenAI pakalpojuma līmenis
service_tier = "fast"   # "fast" | "flex"
```

### Jauni `[model_providers.<id>]` lauki

```toml
[model_providers.omniroute]
base_url             = "http://100.x.x.x:20128/v1"
env_key              = "OMNIROUTE_API_KEY"
requires_openai_auth = false

# Statiski papildu virsraksti katram pieprasījumam
[model_providers.omniroute.http_headers]
"X-Custom-Header" = "value"

# No vides mainīgajiem nolasīti virsraksti
[model_providers.omniroute.env_http_headers]
"X-Trace-Id" = "TRACE_ID"

# Papildu URL vaicājuma parametri (noderīgi Azure api-version)
[model_providers.omniroute.query_params]
"api-version" = "2024-12-01-preview"
```

### Amazon Bedrock autentifikācija (v0.140)

```toml
[model_providers.bedrock]
base_url = "https://bedrock-runtime.us-east-1.amazonaws.com"

[model_providers.bedrock.aws]
profile = "default"   # ~/.aws/credentials profils
region  = "us-east-1"
```

---

## Vairāki serveri

```toml
[model_providers.omniroute-main]
base_url = "http://192.168.0.1:20128/v1"
env_key  = "OMNIROUTE_API_KEY"

[model_providers.omniroute-tailscale]
base_url = "http://100.x.x.x:20128/v1"
env_key  = "OMNIROUTE_API_KEY"
```

---

## Claude Code — ekvivalenta konfigurācija

| Codex CLI (`config.toml`)         | Claude Code (vides mainīgais)         | Ietekme                                 |
| --------------------------------- | ------------------------------------- | --------------------------------------- |
| `tool_output_token_limit = 32768` | _(nav tieši pieejams)_                | Katra rīka vēstures ierobežojums        |
| `model_context_window = 400000`   | _(nosaka modelis)_                    | Konteksta logs                          |
| —                                 | `CLAUDE_CODE_MAX_OUTPUT_TOKENS=65536` | Maksimālais žetonu skaits vienā atbildē |

```bash
# ~/.bashrc — Claude Code žetonu ierobežojums
export CLAUDE_CODE_MAX_OUTPUT_TOKENS=65536
```

---

## Īsā uzziņa — CLI karodziņi

| Karodziņš             | Īsais | Ietekme                                                   |
| --------------------- | ----- | --------------------------------------------------------- |
| `--model <id>`        | `-m`  | Šim izsaukumam ignorē `model` iestatījumu                 |
| `--profile <name>`    | `-p`  | Ielādē `~/.codex/<name>.config.toml`                      |
| `--config key=value`  | `-c`  | Ignorē jebkuru config.toml lauku (var izmantot atkārtoti) |
| `--enable <feature>`  | —     | Piespiedu kārtā iespējo funkcijas karodziņu               |
| `--disable <feature>` | —     | Piespiedu kārtā atspējo funkcijas karodziņu               |
| `--search`            | —     | Šim izsaukumam iespējo meklēšanu tīmeklī reāllaikā        |

Jaunums versijā v0.140:

```bash
codex delete <SESSION_ID>          # dzēst sesiju
codex delete <SESSION_ID> --force  # izlaist apstiprināšanu
codex debug models --bundled       # uzskaitīt iekļauto modeļu katalogu JSON formātā
```

Interaktīvā sesijā:

| Komanda   | Ietekme                                      |
| --------- | -------------------------------------------- |
| `/model`  | Atver modeļa izvēlni                         |
| `/usage`  | Parāda šīs sesijas žetonu lietojumu (v0.140) |
| `/app`    | Nodod darbu darbvirsmas lietotnei (v0.138)   |
| `/import` | Importē Claude Code sesiju (v0.140)          |
| `/help`   | Uzskaita visas slīpsvītras komandas          |

---

## Ilgstoši uzdevumi

Divi OmniRoute noklusējuma iestatījumi var nemanāmi traucēt vairākas stundas ilgām Codex CLI sesijām. Neviens no tiem nav Codex CLI iestatījums — abi atrodas OmniRoute pusē. Lietotāji, kuri migrē konfigurāciju no augšupstraumes starpniekserveriem, kas piesaista kontus un atspējo dīkstāves pārtraukumus, bieži saskaras ar abām problēmām un secina, ka OmniRoute „nespēj uzturēt ilgstošu sesiju”.

| Simptoms                                                                                        | Iespējamais cēlonis                                                               | Iestatījums              |
| ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------ |
| Sesija turpina pārslēgt kontus / starp gājieniem tiek zaudēta uzvedņu kešatmiņas nepārtrauktība | Sesijas piesaistes TTL ir `0` (atspējots)                                         | `sessionAffinityTtlMs`   |
| Savienojums pārtrūkst spriešanas laikā bez klientam parādītas uzvednes                          | Straumes dīkstāves uzraugs nostrādāja pēc 10 minūtēm bez augšupstraumes fragmenta | `STREAM_IDLE_TIMEOUT_MS` |

Saistītās diskusijas: [#7126](https://github.com/diegosouzapw/OmniRoute/discussions/7126) (ilgstošu uzdevumu pārtrūkšana), [#5718](https://github.com/diegosouzapw/OmniRoute/discussions/5718) (kāpēc piesaiste pēc noklusējuma ir izslēgta). Izsekošana: [#7287](https://github.com/diegosouzapw/OmniRoute/issues/7287).

### 1. Sesijas piesaiste — piesaistiet vienu sarunu vienam kontam

**Noklusējums:** `sessionAffinityTtlMs = 0` (atspējots).

**Kur to iestatīt**

- Informācijas panelis → **Iestatījumi → Maršrutēšana** → **Sesijas piesaiste** → **Piesaistes TTL (sekundes)** (`ComboDefaultsTab`)
- Vai modificējiet iestatījumus ar PATCH, norādot `sessionAffinityTtlMs` **milisekundēs** (Zod diapazons `0`–`86_400_000`, t. i., līdz 24 stundām)

> Lauks #7274 ietvaros pārdēvēts no tikai Codex paredzētā `codexSessionAffinityTtlMs`. Mantotā atslēga joprojām tiek pieņemta kā tikai lasāms aizstājvārds; jaunās konfigurācijās jāizmanto `sessionAffinityTtlMs`. Tagad piesaiste attiecas uz **jebkuru** nodrošinātāju, tiklīdz TTL pārsniedz `0`, nevis tikai uz Codex — skatiet [`docs/architecture/RESILIENCE_GUIDE.md`](../architecture/RESILIENCE_GUIDE.md) → Sesijas piesaiste.

**Kas nedarbojas, ja vērtība paliek 0**

Katrs vairāku gājienu Codex sarunas gājiens tiek maršrutēts neatkarīgi atbilstoši aktīvajai kombinācijas stratēģijai un katrā gājienā var nonākt pie **cita konta**. Tas izjauc augšupstraumes sesijas / uzvedņu kešatmiņas nepārtrauktību. OmniRoute ņem vērā Codex sesijas galvenes (`x-codex-session-id` / `x-session-id` / `x-omniroute-session`) un pamatteksta laukus, piemēram, `prompt_cache_key` / `session_id`, tikai tad, ja TTL ir lielāks par `0` (`extractSessionAffinityKey` failā `src/sse/services/auth.ts`).

**Ieteikums vienam vairākas stundas ilgam uzdevumam**

Iestatiet TTL **virs paredzētā uzdevuma reālā ilguma** (lietotāja saskarnes maksimums ir **86400 sekundes** = 24 stundas):

| Paredzamais uzdevuma ilgums | Piesaistes TTL (lietotāja saskarnē, sekundēs) | `sessionAffinityTtlMs` |
| --------------------------- | --------------------------------------------- | ---------------------- |
| Dažas stundas               | `14400` (4 h)                                 | `14400000`             |
| Pa nakti / ~12 h            | `43200` (12 h)                                | `43200000`             |
| Pilna diena                 | `86400` (24 h, maksimums)                     | `86400000`             |

Apzināta izvēle ir obligāta: piesaistes atspējošana veicina slodzes līdzsvarošanu starp kontiem; tās iespējošana veicina vienas ilgstošas aģenta sesijas nepārtrauktību. Šī rokasgrāmata **nemaina** noklusējumu — operatoriem, kuri izpilda ilgstošus Codex uzdevumus, tas ir jāiespējo pašiem.

### 2. Straumes dīkstāves taimauts — nepārtrauciet klusus spriešanas gājienus

**Noklusējums:** `STREAM_IDLE_TIMEOUT_MS = 600000` (10 minūtes). Ja tas nav iestatīts, tas manto vērtību no `REQUEST_TIMEOUT_MS`; kopīgā bāzes vērtība arī ir 600000. Skatiet [`docs/guides/SETUP_GUIDE.md`](SETUP_GUIDE.md) → Taimauti.

**Kas nedarbojas ar noklusējuma iestatījumu**

Codex spriešanas / rīka darbība, kas klusē ilgāk par 10 minūtēm un nesaņem **nevienu īstu augšupstraumes fragmentu**, tiek piespiedu kārtā pārtraukta ar SSE dīkstāves uzraugu (`open-sse/utils/stream.ts`). Klients bieži redz tikai savienojuma pārtrūkšanu — tas atbilst situācijai “automātiski apstājās bez jebkāda paziņojuma”.

Būtiska nianse: OmniRoute sintētiskais SSE **sirdspuksts neatiestata** dīkstāves taimeri. `lastChunkTime` atjaunina tikai īsts augšupstraumes atbildes satura fragments. Kluss modelis, kas joprojām “domā”, no uzrauga skatpunkta izskatās tieši tāpat kā iestrēgusi augšupstraume.

Saistītais Undici atbildes satura neaktivitātes iestatījums: `FETCH_BODY_TIMEOUT_MS` (arī pēc noklusējuma izmanto to pašu 10 minūšu bāzes vērtību; `0` to atspējo). Straumēšanas gadījumā `FETCH_TIMEOUT_MS` attiecas tikai uz savienojuma izveidi / pirmajām galvenēm — tiklīdz straume ir aktīva, tās iestrēgšanu pārvalda `STREAM_IDLE_TIMEOUT_MS` un `FETCH_BODY_TIMEOUT_MS`.

**Ieteikums vienam vairākas stundas ilgam uzdevumam**

OmniRoute procesa vidē (`.env` / compose / systemd):

```bash
# Atspējot straumes dīkstāves un atbildes satura neaktivitātes ierobežojumus ilgiem spriešanas posmiem
STREAM_IDLE_TIMEOUT_MS=0
FETCH_BODY_TIMEOUT_MS=0
```

Vai palieliniet tos virs ilgākā paredzamā klusuma perioda (vērtības ir milisekundēs):

```bash
# Piemērs: atļaut līdz 2 stundām ilgu klusumu starp augšupstraumes fragmentiem
STREAM_IDLE_TIMEOUT_MS=7200000
FETCH_BODY_TIMEOUT_MS=7200000
```

Pēc šo vides mainīgo maiņas restartējiet OmniRoute.

### Konkrēta procedūra — vairākas stundas ilgs Codex uzdevums

1. **Piesaistiet kontu:** Informācijas panelis → Iestatījumi → Maršrutēšana → Sesijas piesaiste → Piesaistes TTL = `43200` (12 h) vai `86400` (maks. 24 h).
2. **Palieliniet / atspējojiet dīkstāves ierobežojumus** OmniRoute vidē:

```bash
STREAM_IDLE_TIMEOUT_MS=0
FETCH_BODY_TIMEOUT_MS=0
```

3. Saglabājiet ierasto Codex `config.toml` (`wire_api = "responses"`, pareizs `base_url`, `OMNIROUTE_API_KEY`) — šīm abām darbībām nav Codex puses piesaistes/dīkstāves iestatījumu.
4. Restartējiet OmniRoute un pēc tam palaidiet ilgo Codex uzdevumu.

### Lēmums par noklusējuma vērtībām (#7287)

| Iestatījums              | Piegādes noklusējums | Vai mainīt šajā rokasgrāmatā?                                                                                |
| ------------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------ |
| `sessionAffinityTtlMs`   | `0` (izslēgts)       | **Nē** — joprojām jāieslēdz atsevišķi (slodzes līdzsvarošana pret nepārtrauktību; skatiet diskusiju #5718)   |
| `STREAM_IDLE_TIMEOUT_MS` | `600000` (10 min)    | **Nē** — vispārējai datplūsmai paliek 10 minūtes; ilgstošu Codex uzdevumu operatori to palielina vai atspējo |

Jebkuras noklusējuma vērtības globāla maiņa izmainītu darbību visiem instances klientiem, ne tikai Codex. Dokumentējiet iestatījumus; nemainiet noklusējuma vērtības, kamēr nav pieņemts skaidrs operatora lēmums.

### Dīkstāves izraisītu pārtraukumu diagnosticēšana

Kad nostrādā dīkstāves uzraugs, OmniRoute reģistrē aptuveni šāda veida žurnāla rindu:

```text
[STREAM] Dīkstāves noildze: 600000ms nav datu no codex (modelis: cx/gpt-5.5)
```

Meklējiet ar grep `Idle timeout: no data from` (vai kodu `stream_idle_timeout` / kļūdas nosaukumu `StreamIdleTimeoutError`). Pakalpojuma sniedzēja segments ir tas, kuru OmniRoute izmantoja attiecīgajam pieprasījumam (`codex`, cita pakalpojuma sniedzēja identifikators vai `provider`, ja tas nav zināms) — tā ne vienmēr ir burtiskā virkne `codex`.

---

## Problēmu novēršana

**`Error: wire_api = "chat" vairs netiek atbalstīts`**
Noņemiet `wire_api = "chat"` no konfigurācijas. Iestatiet `wire_api = "responses"` vai izlaidiet šo lauku (kopš v0.138 noklusējuma vērtība ir `"responses"`).

**`Error: model not found`**
Pārbaudiet, vai modelis pastāv OmniRoute ar pareizo prefiksu. Izmantojiet `omniroute models list` vai atveriet `/dashboard/providers/<provider>`.

**`Authentication error`**
Pārliecinieties, ka `OMNIROUTE_API_KEY` ir eksportēts: `echo $OMNIROUTE_API_KEY`.

**`ERROR: Missing environment variable: OMNIROUTE_API_KEY`**
Codex pirms pirmā pieprasījuma veikšanas pārbauda, vai vides mainīgais pastāv. Eksportējiet
īstu atslēgu aizsargātiem serveriem vai netukšu vietturi, piemēram,
`OMNIROUTE_API_KEY=local`, ja jūsu **lokālajai** OmniRoute instancei nav
nepieciešama autentifikācija. Restartējiet čaulu, ja pievienojāt to failam `~/.bashrc` vai `~/.zshrc`.

**`Connection refused`**
Pārbaudiet, vai OmniRoute darbojas un vai `base_url` resursdators/ports ir pareizs jūsu tīklam (lokālais, Tailscale vai VPS).

**Sesija avarē, tuvojoties konteksta ierobežojumam**
Skaidri iestatiet `model_context_window` un `model_auto_compact_token_limit`. Skatiet iepriekš esošo konteksta loga tabulu.

**Kompaktēšana tiek aktivizēta pārāk vēlu**
Samaziniet `model_auto_compact_token_limit` līdz 80–85% no loga lieluma. Nekad neiestatiet vērtību virs 90%.

**Profils netiek ielādēts (`-p <name>` tiek klusi ignorēts)**
Pārliecinieties, ka fails pastāv vietā `~/.codex/<name>.config.toml` (bez prefiksa `profile-`). Izpildiet `ls ~/.codex/*.config.toml`.

**Ilgstošs Codex uzdevums tiek pārtraukts izpildes laikā / starp darbībām tiek pārslēgti konti**
Skatiet sadaļu [Ilgstoši uzdevumi](#long-running-tasks). Iespējojiet sesijas piesaisti (TTL jābūt ilgākam par uzdevuma ilgumu) un palieliniet vai atspējojiet `STREAM_IDLE_TIMEOUT_MS` / `FETCH_BODY_TIMEOUT_MS`. Meklējiet OmniRoute žurnālos tekstu `Idle timeout: no data from`.
