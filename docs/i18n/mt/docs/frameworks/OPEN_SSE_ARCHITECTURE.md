# open-sse Architecture (Malti)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Fil-qosor**: `open-sse/` hija l-magna ewlenija tal-istreaming li tħaddem kull talba LLM f’OmniRoute. Fiha madwar 1,400 fajl li jimplimentaw il-pipeline tat-talbiet, l-eżekuturi, is-servizzi, is-server MCP, u s-saff tat-traduzzjoni. Din il-gwida tispjega kif il-komponenti jaħdmu flimkien.

**Sors:** `open-sse/` (pakkett tal-workspace, madwar 1,440 fajl `.ts` mhux tat-testijiet)

---

## Għaliex Pakkett Separat tal-Workspace?

`open-sse/` huwa **workspace awtonomu** fil-monorepo ta’ OmniRoute għal diversi raġunijiet:

1. **Użu mill-ġdid** — `open-sse` jiġi ppubblikat bħala `@omniroute/open-sse` fuq npm, għalhekk proġetti oħra jistgħu jużawh b’mod indipendenti
2. **Konfini ċari** — il-magna tal-istreaming hija diżakkoppjata mis-saff tal-UI/DB speċifiku għal OmniRoute
3. **Prestazzjoni** — il-magna m’għandha l-ebda dipendenza fuq Next.js, u b’hekk tippermetti bidu kiesaħ aktar rapidu f’kuntesti CLI/serverless
4. **Verżjonar** — `open-sse` jista’ joħroġ rilaxxi bir-ritmu tiegħu stess

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Struttura tal-Ogħla Livell

```
open-sse/
├── index.ts              # Punt tad-dħul pubbliku
├── types.d.ts            # Esportazzjonijiet pubbliċi tat-tipi
├── package.json          # @omniroute/open-sse
├── config/               # Konfigurazzjonijiet tal-fornituri, kostanti, reġistri
├── executors/            # Eżekuturi HTTP għal kull fornitur (67 + base.ts/index.ts)
├── handlers/             # Handlers tat-talbiet (chatCore, responses, eċċ.)
├── lib/                  # Utilitajiet interni
├── mcp-server/           # Server tal-Model Context Protocol
├── services/             # Madwar 298 modulu tas-servizzi
├── transformer/          # Transformer tal-format tal-Responses API
├── translator/           # Traduzzjoni tal-format (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Utilitajiet kondiviżi (logging, żbalji, streaming, eċċ.)
```

### Għadd tal-Moduli

| Direttorju | Fajls | Għan |
| `executors/` | 167 | Eżekuturi HTTP għal kull fornitur (unifikati permezz tal-factory DefaultExecutor) |
| `handlers/` | 157 | Punti tad-dħul tat-talbiet (chatCore, responses, embeddings) |
| `services/` | ~536 | Routing, caching, limitazzjoni tar-rata, aġġornament, eċċ. |
| `translator/` | 56 | Konverżjoni tal-format (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Għodod u transports MCP |
| `utils/` | ~108 | Utilitajiet trasversali (logging, żbalji, streaming) |
| `config/` | ~339 | Konfigurazzjonijiet tal-fornituri, kostanti, reġistri |

---

## Il-Pipeline tat-Talbiet

Kull talba LLM tgħaddi minn **pipeline ta’ 5 stadji**:

```
                              ┌──────────────┐
   Talba HTTP                  │  1. ROUTE    │   riżoluzzjoni tal-combo, għażla tal-mudell
   (route ta’ Next.js)         └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TRANSLATE│   konverżjoni tal-format (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. EXECUTE  │   eżekutur tal-fornitur, HTTP, tentattiv mill-ġdid, breaker
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAM   │   trasformazzjoni SSE, backpressure
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. RECORD   │   traċċar tal-użu, reġistru tas-sejħiet, klassifikazzjoni tal-iżbalji
                              └──────┬───────┘
                                     │
                                     ▼
                              Rispons HTTP (SSE jew JSON)
```

### Stadju 1: Routing (services/combo.ts)

**Punt tad-dħul**: `handleComboChat()` f’`services/combo.ts`

Jirriżolvi t-talba għal tuple konkret `(provider, model, account, credentials)`:

- Fittex il-combo permezz tal-ID (jew ibni combo virtwali għall-mudelli `auto/*`)
- Applika l-istrateġija tar-routing (prijorità, ponderata, round-robin, eċċ.)
- Iffiltra l-fornituri li mhumiex f’saħħithom (circuit breaker)
- Agħżel il-mira vijabbli li jmiss

Għall-mudelli `auto/*`, dan l-istadju wkoll:

- Iħaddem l-algoritmu ta’ **punteġġ ibbażat fuq 16-il fattur** (`services/autoCombo/`)
- Jagħżel par `provider+model` abbażi tas-saħħa, l-ispiża, il-latenza, eċċ.

### Stadju 2: Traduzzjoni (translator/)

Jekk il-format tas-sors (eż., OpenAI) ikun differenti mill-format fil-mira (eż., Claude), it-talba tiġi **tradotta**:

- Prompt tas-sistema → messaġġ tas-sistema
- Definizzjonijiet tal-għodod → format tal-għodod speċifiku għall-fornitur
- Parametri tar-raġunament/ħsieb → ekwivalenti speċifiċi għall-fornitur
- Normalizzazzjoni tar-rwoli tal-messaġġi (`developer` → `system` għal dawk li mhumiex OpenAI)

`translator/index.ts` jesponi:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Stadju 3: Eżekuzzjoni (executors/)

**Punt tad-dħul**: `getExecutor(providerId).execute(request, options)`

Il-fornituri kollha jużaw `DefaultExecutor` (`executors/default.ts`) permezz tal-fallback tal-factory `getExecutor()`. L-eżekutur:

- Jibni l-URL upstream (`buildUrl()`)
- Iżid headers speċifiċi għall-fornitur (`buildHeaders()`)
- Jittrasforma l-body tat-talba (`transformRequest()`)
- Jibgħat it-talba HTTP b’tentattivi mill-ġdid + dewmien esponenzjali
- Jittratta l-aġġornament tal-awtentikazzjoni meta jkun meħtieġ (fornituri OAuth)

L-eżekuturi kollha jestendu `BaseExecutor` (`executors/base.ts`, 1170 LOC), li jipprovdi:

- Loġika komuni għat-tentattivi mill-ġdid
- Integrazzjoni ma’ proxy
- Integrazzjoni ma’ circuit breaker
- Hooks għar-reġistrazzjoni tal-użu

### Stadju 4: Streaming (utils/stream.ts)

Għal risponsi bi streaming, l-eżekutur jirritorna **ReadableStream**. Il-handler:

- Jgħaddi minn trasformazzjoni SSE (`createSSETransformStreamWithLogger`)
- Japplika pings heartbeat biex jinduna b’konnessjonijiet inattivi
- Jittratta l-iskonnessjoni tal-klijent b’mod xieraq (`pipeWithDisconnect`)
- Jittrasforma SSE → JSON għal klijenti li ma jużawx streaming

Għal risponsi mingħajr streaming, l-eżekutur jirritorna oġġett JSON parsed li jgħaddi mingħajr tibdil.

### Stadju 5: Reġistrazzjoni (services/usage.ts)

Wara r-rispons (kemm jekk jirnexxi kif ukoll jekk ifalli), jiġi rreġistrat l-użu:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` mir-rispons
- `cost_usd` ikkalkulat mid-data tal-ipprezzar
- `latency_ms`, `status`, `error_class` jekk ifalli
- Jinżamm fit-tabella `usage_history`

L-artefatti tar-reġistru tas-sejħiet (jekk attivati) jinkitbu f’`${DATA_DIR}/call_logs/`.

---

## Analiżi Approfondita tal-Fajls Ewlenin

### chatCore.ts (5977 linja)

Il-**maniġġur ewlieni tat-talbiet**. Minkejja d-daqs tiegħu, għandu struttura ċara:

```ts
// Psewdo-struttura ta’ chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Awtentikazzjoni + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Validazzjoni tal-korp
  const body = await parseRequestBody(request);

  // 3. Individwazzjoni tal-format + traduzzjoni
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Rotot tal-combo
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Kompli għall-mira li jmiss
    }
  }

  // 5. Alternattiva ta’ emerġenza
  return await emergencyFallback(body);
}
```

Minkejja li hija funzjoni waħda enormi, hija organizzata f’**sezzjonijiet bil-kummenti** li jikkorrispondu mal-pipeline ta’ 5 stadji.

### combo.ts (4456 LOC)

Il-**magna tar-rotot** li tirriżolvi combo f’miri ordnati.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Tappoġġja **19-il strateġija tar-rotot** (ara `src/shared/constants/routingStrategies.ts`):

| Strateġija          | Imġiba                                                                                      |
| ------------------- | ------------------------------------------------------------------------------------------- |
| `priority`          | Lista ordnata bl-ewwel mira l-ewwel                                                         |
| `weighted`          | Probabbilistika skont il-piż ta’ kull mira                                                  |
| `round-robin`       | Iddur mal-miri fl-ordni                                                                     |
| `context-relay`     | Tgħaddi l-kuntest bejn il-miri                                                              |
| `fill-first`        | Timla l-kwota qabel tgħaddi għal-li jmiss                                                   |
| `p2c`               | Il-qawwa ta’ żewġ għażliet                                                                  |
| `random`            | Għażla każwali uniformi                                                                     |
| `least-used`        | Tagħżel dik bl-inqas użi reċenti                                                            |
| `cost-optimized`    | L-ewwel l-orħos mira li tkun qed taħdem tajjeb                                              |
| `reset-aware`       | Tqis il-perjodi ta’ reset tal-fornitur                                                      |
| `reset-window`      | Rotot ibbażati fuq il-perjodu ta’ reset                                                     |
| `headroom`          | L-ewwel dik bl-akbar kwota li għadha disponibbli                                            |
| `strict-random`     | Tassew uniformi (mingħajr ponderazzjoni tal-kwalità)                                        |
| `auto`              | Tuża punteġġar b’16-il fattur (`autoCombo/`)                                                |
| `lkgp`              | L-ewwel l-aħħar fornitur magħruf li ħadem tajjeb                                            |
| `context-optimized` | L-aħjar għal talbiet b’kuntest twil                                                         |
| `fusion`            | Tqassam lil bord b’mod parallel, imbagħad tissintetizza permezz ta’ ġudikatur (`fusion.ts`) |

### base.ts (1170 LOC)

L-**eżekutur astratt** li jestendu l-107 eżekuturi kollha. Fih:

- `buildUrl()` — kostruzzjoni predefinita tal-URL (is-sottoklassijiet jissostitwixxuha għal imġiba personalizzata)
- `buildHeaders()` — headers predefiniti (awtentikazzjoni, tip ta’ kontenut)
- `transformRequest()` — jgħaddi t-talba mingħajr tibdil b’mod predefinit
- `execute()` — il-loop HTTP ewlieni b’tentattivi mill-ġdid/backoff/breaker

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Jimmaniġġja l-fornituri kollha kompatibbli ma’ OpenAI/Anthropic
  // Il-fornituri jirreġistraw konfigurazzjonijiet (URL, awtentikazzjoni, headers) iżda jaqsmu l-loġika tal-eżekutur
}
```

L-imġiba speċifika għall-fornitur (headers tal-awtentikazzjoni, URL bażi, headers tal-verżjoni) tiġi kkonfigurata permezz tar-reġistru tal-fornituri, mhux permezz ta’ klassijiet ta’ eżekuturi separati.

````

---

## Servizzi (117-il modulu)

Is-servizzi huma **moduli ffukati bi skop wieħed** li l-handlers jikkomponu flimkien. Il-kategoriji ewlenin:

### Routing u Kombinazzjonijiet

- `combo.ts` — punt tad-dħul għal talbiet b’routing ikkombinat
- `services/autoCombo/` — punteġġ ibbażat fuq 16-il fattur, 8 strateġiji ta’ routing awtomatiku
- `wildcardRouter.ts` — iqabbel rotot wildcard (`gpt-*`)
- `modelFamilyFallback.ts` — fallback T5 fi ħdan l-istess familja

### Limitazzjoni tar-Rata u Kwota

- `rateLimitManager.ts` — token bucket għal kull ċavetta+fornitur
- `usage.ts` — reġistrazzjoni tal-użu
- `quotaCache.ts` — snapshots tal-kwota fil-memorja

### Kont u Token

- `tokenRefresh.ts` — aġġornament OAuth meta jingħata 401
- `accountFallback.ts` — jaqleb għal kont alternattiv
- `sessionManager.ts` — stat ta’ sessjoni b’diversi skambji

### Intelliġenza

- `intentClassifier.ts` — jikklassifika l-intenzjoni tat-talba
- `taskAwareRouter.ts` — jagħmel routing skont it-tip ta’ kompitu
- `thinkingBudget.ts` — jalloka tokens għall-ħsieb
- `contextManager.ts` — jinjetta l-kuntest tar-routing

### Reżiljenza

- `resilience.ts` — orkestrazzjoni ta’ tentattivi mill-ġdid, backoff u breaker
- `emergencyFallback.ts` — fallback tal-aħħar għażla
- `modelDeprecation.ts` — jagħmel routing awtomatiku lejn mudelli suċċessuri

### Stat

- `signatureCache.ts` — jelimina d-duplikati skont il-firma tat-talba
- `volumeDetector.ts` — tnaqqis tat-tagħbija
- `contextHandoff.ts` — serjalizzazzjoni tas-sessjoni

### Kompressjoni

- `compression/` (sottodirettorju) — pipeline sħiħ tal-kompressjoni
- 39 fajl li jkopru engines, pakketti ta’ regoli u adapters

### Ħiliet

- (koperti f’[SKILLS.md](./SKILLS.md))

### Memorja

- (koperta f’[MEMORY.md](./MEMORY.md))

---

## Eżekuturi (75+ fajl)

Fajl wieħed għal kull fornitur. Kollha jestendu `BaseExecutor` u jagħmlu override ta’ dak li jvarja.

### Mudelli Komuni

Il-fornituri jiġu riżolti permezz ta’ `getExecutor(providerId)`, li jirritorna l-eżekutur ikkonfigurat. Fornituri kompatibbli ma’ OpenAI/Anthropic jużaw `DefaultExecutor` (`executors/default.ts`). L-imġiba speċifika għall-fornitur (URL bażi, headers tal-awtentikazzjoni, verżjoni tal-API) tiġi kkonfigurata f’`open-sse/config/providers/`, filwaqt li t-trasformazzjonijiet tal-body tat-talba jiġu mmaniġġjati f’`open-sse/translator/`.

**URL personalizzat** jiġi stabbilit permezz tal-konfigurazzjoni tal-fornitur:

```ts
// Konfigurazzjoni tal-fornitur f’open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Awtentikazzjoni personalizzata** tiġi mmaniġġjata permezz tal-konfigurazzjoni tal-awtentikazzjoni fir-reġistru tal-fornituri (ċavetta tal-API, OAuth, profili tal-headers).

It-trasformazzjonijiet tal-**body personalizzat tat-talba** (eż., Anthropic jissepara `system` minn `messages`) jiġu rreġistrati għal kull fornitur f’`open-sse/translator/`.

````

### Il-Fabbrika tal-Eżekuturi

`executors/index.ts` jesporta `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Ir-riżoluzzjoni tgħaddi mill-`ExecutorRegistry` (`executors/registry.ts`): kull eżekutur speċjalizzat jiġi ddikjarat fit-tabella integrata ta’ `executors/index.ts` u rreġistrat permezz ta’ `registerExecutor(alias, instance)` waqt it-tagħbija tal-modulu; `getExecutor()` jikkonsulta r-reġistru u, għal kull fornitur mingħajr entrata speċjalizzata, juża bħala fallback `DefaultExecutor` memorizzat. Il-mapping sħiħ alias → eżekutur huwa kkaratterizzat mit-test golden `tests/unit/executor-map-golden.test.ts`.

---

## Tradutturi

Ittraduċi bejn **3 formats**: OpenAI, Anthropic, Gemini, flimkien mal-Responses API l-ġdida.

### Meta Sseħħ it-Traduzzjoni

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Traduzzjonijiet komuni:

- `OpenAI → Anthropic`: field `system` separat, header `x-api-key`
- `OpenAI → Gemini`: `contents` minflok `messages`, `systemInstruction`
- `OpenAI → Responses API`: array `input`, stat `previous_response_id`

### Każijiet Speċjali Ttrattati

- Ir-rwol `developer` → `system` għal dawk li mhumiex OpenAI
- Ir-rwol `system` → jingħaqad mal-ewwel messaġġ tal-utent għal GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` ta' Gemini
- `tools` → format tal-għodod speċifiku għall-fornitur
- Parametri tal-ħsieb (o1, Claude) → ekwivalenti speċifiċi għall-fornitur

---

## Server MCP

`open-sse/mcp-server/` jimplimenta s-server tal-**Model Context Protocol**:

- **110 għodod** (ġestjoni tal-fornituri, combos, memorja, cache, kompressjoni, proxy, ħiliet, gamification, plugins, Notion, Obsidian, corpus lokali)
- **3 transports**: stdio, SSE, Streamable HTTP
- **33 scopes** għal awtorizzazzjoni granulari

### Reġistrazzjoni tal-Għodod

L-għodod jiġu rreġistrati bħala files awtonomi f'`open-sse/mcp-server/tools/`, u kull wieħed jesporta isem, schema, handler, u scope:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transports

```ts
// stdio (użu tas-CLI)
startMcpStdio(server);

// SSE (streaming ibbażat fuq HTTP)
startMcpSse(server, port);

// Streamable HTTP (MCP modern)
startMcpStreamable(server, port);
```

### Awtorizzazzjoni

Kull sejħa lil għodda tgħaddi minn kontrolli tal-scope (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Transformers

`open-sse/transformer/` jikkonverti bejn il-formats **Chat Completions** u **Responses API**.

### Għaliex Transformer Separat?

Ir-Responses API hija l-format il-ġdid ta' OpenAI b'**konverżazzjonijiet bi stat** (`previous_response_id`). Meta klijent jibgħat talba Responses, OmniRoute:

1. Jikkonverti Responses → Chat Completions internament
2. Jibgħat lill-fornitur (kwalunkwe fornitur li jappoġġja Chat Completions)
3. Jikkonverti t-tweġiba lura għall-format Responses
4. Jibgħat it-tweġiba kkonvertita bħala stream lill-klijent

It-transformer (`transformer/responsesTransformer.ts`) jipprovdi:

```ts
createResponsesApiTransformStream(): TransformStream
```

Dan jittratta:

- Avvenimenti `response.output_item.added`
- Avvenimenti `response.output_text.delta`
- Avveniment `response.completed`
- Immappjar tas-sejħiet tal-għodod (`function_call` ↔ `tool_calls`)

---

## Konfigurazzjoni

`open-sse/config/` fih is-saff tal-konfigurazzjoni:

| File                           | Għan                                                          |
| ------------------------------ | ------------------------------------------------------------- |
| `providerRegistry.ts`          | Reġistru tal-mudelli taċ-chat fuq il-katalgu ta' 352 fornitur |
| `providerModels.ts`            | Aliases tal-mudelli, immappjar tal-formats                    |
| `constants.ts`                 | Timeouts, limiti, kodiċijiet tal-istatus                      |
| `defaultThinkingSignature.ts`  | Firma predefinita tal-ħsieb ta' Claude                        |
| `modelStrip.ts` (fis-services) | Tneħħija ta' fields għal kull fornitur                        |

### Schema tar-Reġistru tal-Fornituri

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Il-validazzjoni b'Zod waqt it-tagħbija tal-module tiżgura li l-konfigurazzjonijiet kollha tal-fornituri jkunu validi.

---

## Limitazzjonijiet tal-Prestazzjoni

Il-magna tar-routing għandha baġits stretti tal-prestazzjoni:

| Operazzjoni                                                | Mira  | Kejl                                   |
| ---------------------------------------------------------- | ----- | -------------------------------------- |
| Riżoluzzjoni tal-combo                                     | <10ms | Għal 50 mira                           |
| Verifika tal-limitu tar-rata                               | <1ms  | Token bucket fil-memorja               |
| Fallback tal-familja tal-mudelli                           | <5ms  | Definizzjonijiet tal-familji fil-cache |
| Distribuzzjoni tar-routing tat-talba                       | <2ms  | Mogħdija kritika                       |
| **Ebda I/O li jimblokka fil-mogħdija kritika tar-routing** | —     | Kollox asinkronu                       |

---

## Anti-Patterns

❌ **Sejħiet sinkroniċi lid-DB f’`combo.ts`** — ikkalkula minn qabel u aħżen fil-cache
❌ **Loġika ta’ tentattiv mill-ġdid fil-handlers** — uża `retry()` mis-servizz tar-reżiljenza
❌ **Aċċess dirett għall-konfigurazzjoni tal-fornitur** — uża l-getters ta’ `providerRegistry`
❌ **Ktajjen ta’ fallback miktuba direttament fil-kodiċi** — iddefinixxihom f’`modelFamilyFallback.ts`
❌ **Mutazzjonijiet tal-istat bejn talbiet konkorrenti** — uża biss kuntest speċifiku għat-talba

---

## Żieda ta’ Komponent Ġdid

### Żieda ta’ Servizz Ġdid

1. Oħloq `open-sse/services/[serviceName].ts` b’responsabbiltà ffukata
2. Esporta l-funzjoni prinċipali tal-handler u kwalunkwe kostanti
3. Żid testijiet unitarji f’`tests/unit/services/[serviceName].test.mjs`
4. Integrah fil-pipeline tat-talbiet f’`handlers/chatCore.ts` (jekk ikun relatat mar-routing)
5. Aġġorna l-loġika tar-routing f’`combo.ts` jekk is-servizz jaffettwa l-għażla tal-mira
6. Iddokumentah f’dan il-fajl

### Żieda ta’ Executor Ġdid

1. Oħloq `open-sse/executors/[provider].ts` li jestendi `BaseExecutor`
2. Irreġistrah f’`config/providerRegistry.ts`
3. Żidu mal-factory ta’ `executors/index.ts`
4. Żid testijiet unitarji għall-executor
5. Iddokumentah f’`docs/architecture/ARCHITECTURE.md`

### Żieda ta’ Għodda MCP Ġdida

1. Oħloq jew aġġorna `open-sse/mcp-server/tools/[category]Tools.ts`
2. Iddefinixxi schema Zod għall-inputs
3. Irreġistra l-għodda f’`mcp-server/index.ts`
4. Żidha mal-matriċi tal-ambiti f’`mcp-server/auth/`
5. Żid testijiet unitarji

---

## Ara Wkoll

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — arkitettura ta’ livell għoli
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — referenza tal-inġinerija
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — direttorju b’direttorju
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — punteġġ ibbażat fuq 16-il fattur
- [MCP-SERVER.md](./MCP-SERVER.md) — server MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — server A2A
- Sors: `open-sse/` (aktar minn 400 fajl, madwar 143K linja ta’ kodiċi)
