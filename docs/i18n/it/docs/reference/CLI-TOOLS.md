# CLI Tools — OmniRoute (Italiano)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/CLI-TOOLS.md) · 🇪🇹 [am](../../../am/docs/reference/CLI-TOOLS.md) · 🇸🇦 [ar](../../../ar/docs/reference/CLI-TOOLS.md) · 🇦🇿 [az](../../../az/docs/reference/CLI-TOOLS.md) · 🇧🇬 [bg](../../../bg/docs/reference/CLI-TOOLS.md) · 🇧🇩 [bn](../../../bn/docs/reference/CLI-TOOLS.md) · 🇧🇦 [bs](../../../bs/docs/reference/CLI-TOOLS.md) · 🇨🇿 [cs](../../../cs/docs/reference/CLI-TOOLS.md) · 🇩🇰 [da](../../../da/docs/reference/CLI-TOOLS.md) · 🇩🇪 [de](../../../de/docs/reference/CLI-TOOLS.md) · 🇬🇷 [el](../../../el/docs/reference/CLI-TOOLS.md) · 🇪🇸 [es](../../../es/docs/reference/CLI-TOOLS.md) · 🇪🇪 [et](../../../et/docs/reference/CLI-TOOLS.md) · 🇮🇷 [fa](../../../fa/docs/reference/CLI-TOOLS.md) · 🇫🇮 [fi](../../../fi/docs/reference/CLI-TOOLS.md) · 🇫🇷 [fr](../../../fr/docs/reference/CLI-TOOLS.md) · 🇮🇪 [ga](../../../ga/docs/reference/CLI-TOOLS.md) · 🇮🇳 [gu](../../../gu/docs/reference/CLI-TOOLS.md) · 🇳🇬 [ha](../../../ha/docs/reference/CLI-TOOLS.md) · 🇮🇱 [he](../../../he/docs/reference/CLI-TOOLS.md) · 🇮🇳 [hi](../../../hi/docs/reference/CLI-TOOLS.md) · 🇭🇷 [hr](../../../hr/docs/reference/CLI-TOOLS.md) · 🇭🇺 [hu](../../../hu/docs/reference/CLI-TOOLS.md) · 🇦🇲 [hy](../../../hy/docs/reference/CLI-TOOLS.md) · 🇮🇩 [id](../../../id/docs/reference/CLI-TOOLS.md) · 🇳🇬 [ig](../../../ig/docs/reference/CLI-TOOLS.md) · 🇯🇵 [ja](../../../ja/docs/reference/CLI-TOOLS.md) · 🇬🇪 [ka](../../../ka/docs/reference/CLI-TOOLS.md) · 🇰🇭 [km](../../../km/docs/reference/CLI-TOOLS.md) · 🇮🇳 [kn](../../../kn/docs/reference/CLI-TOOLS.md) · 🇰🇷 [ko](../../../ko/docs/reference/CLI-TOOLS.md) · 🇱🇹 [lt](../../../lt/docs/reference/CLI-TOOLS.md) · 🇱🇻 [lv](../../../lv/docs/reference/CLI-TOOLS.md) · 🇮🇳 [ml](../../../ml/docs/reference/CLI-TOOLS.md) · 🇮🇳 [mr](../../../mr/docs/reference/CLI-TOOLS.md) · 🇲🇾 [ms](../../../ms/docs/reference/CLI-TOOLS.md) · 🇲🇹 [mt](../../../mt/docs/reference/CLI-TOOLS.md) · 🇲🇲 [my](../../../my/docs/reference/CLI-TOOLS.md) · 🇳🇵 [ne](../../../ne/docs/reference/CLI-TOOLS.md) · 🇳🇱 [nl](../../../nl/docs/reference/CLI-TOOLS.md) · 🇳🇴 [no](../../../no/docs/reference/CLI-TOOLS.md) · 🇮🇳 [or](../../../or/docs/reference/CLI-TOOLS.md) · 🇮🇳 [pa](../../../pa/docs/reference/CLI-TOOLS.md) · 🇵🇭 [phi](../../../phi/docs/reference/CLI-TOOLS.md) · 🇵🇱 [pl](../../../pl/docs/reference/CLI-TOOLS.md) · 🇵🇹 [pt](../../../pt/docs/reference/CLI-TOOLS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/CLI-TOOLS.md) · 🇷🇴 [ro](../../../ro/docs/reference/CLI-TOOLS.md) · 🇷🇺 [ru](../../../ru/docs/reference/CLI-TOOLS.md) · 🇱🇰 [si](../../../si/docs/reference/CLI-TOOLS.md) · 🇸🇰 [sk](../../../sk/docs/reference/CLI-TOOLS.md) · 🇸🇮 [sl](../../../sl/docs/reference/CLI-TOOLS.md) · 🇷🇸 [sr](../../../sr/docs/reference/CLI-TOOLS.md) · 🇸🇪 [sv](../../../sv/docs/reference/CLI-TOOLS.md) · 🇰🇪 [sw](../../../sw/docs/reference/CLI-TOOLS.md) · 🇮🇳 [ta](../../../ta/docs/reference/CLI-TOOLS.md) · 🇮🇳 [te](../../../te/docs/reference/CLI-TOOLS.md) · 🇹🇭 [th](../../../th/docs/reference/CLI-TOOLS.md) · 🇹🇷 [tr](../../../tr/docs/reference/CLI-TOOLS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/CLI-TOOLS.md) · 🇵🇰 [ur](../../../ur/docs/reference/CLI-TOOLS.md) · 🇺🇿 [uz](../../../uz/docs/reference/CLI-TOOLS.md) · 🇻🇳 [vi](../../../vi/docs/reference/CLI-TOOLS.md) · 🇳🇬 [yo](../../../yo/docs/reference/CLI-TOOLS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/CLI-TOOLS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/CLI-TOOLS.md)

---

Ultimo aggiornamento: 2026-08-23

OmniRoute si integra con tre categorie di strumenti CLI distribuite su tre pagine dedicate della dashboard:

| Pagina         | Percorso                | Concetto                                                                                  | Numero        |
| -------------- | ----------------------- | ----------------------------------------------------------------------------------------- | ------------- |
| **CLI Code's** | `/dashboard/cli-code`   | Strumenti di programmazione indirizzati a OmniRoute (Client → CLI → OmniRoute → Provider) | 26            |
| **CLI Agents** | `/dashboard/cli-agents` | Agenti autonomi indirizzati a OmniRoute (stesso flusso, ambito più ampio)                 | 10            |
| **ACP Agents** | `/dashboard/acp-agents` | CLI che OmniRoute avvia come backend tramite stdio/ACP (flusso inverso)                   | vedi registro |

I percorsi legacy eseguono un reindirizzamento 308: `/dashboard/cli-tools` → `/dashboard/cli-code`, `/dashboard/agents` → `/dashboard/acp-agents`.

---

## Come funziona

```
CLI Code's / CLI Agents (flusso di utilizzo):
Claude / Codex / OpenCode / Cline / KiloCode / Continue / Hermes Agent / Goose / ...
           │
           ▼  (tutti puntano a OmniRoute)
    http://YOUR_SERVER:20128/v1
           │
           ▼  (OmniRoute instrada verso il provider corretto)
    Anthropic / OpenAI / Gemini / DeepSeek / Groq / Mistral / ...

ACP Agents (flusso inverso di avvio):
    Richiesta del client → OmniRoute → avvia la CLI tramite stdio/ACP → risposta
```

**Vantaggi:**

- Un'unica chiave API per gestire tutti gli strumenti
- Monitoraggio dei costi di tutte le CLI nella dashboard
- Cambio di modello senza riconfigurare ogni strumento
- Funziona in locale e su server remoti (VPS, Docker, Akamai, Cloudflare Tunnel)

---

## Configurazione automatica con `setup-*`

Non è necessario scrivere manualmente la configurazione di ogni strumento. OmniRoute fornisce un comando `setup-*` per ogni CLI supportata che legge il catalogo modelli **live** da un'istanza OmniRoute in esecuzione (locale o remota) e scrive la configurazione dello strumento sulla tua macchina:

```bash
omniroute setup-codex        omniroute setup-claude       omniroute setup-opencode
omniroute setup-cline        omniroute setup-kilo         omniroute setup-continue
omniroute setup-cursor       omniroute setup-roo          omniroute setup-crush
omniroute setup-goose        omniroute setup-qwen         omniroute setup-aider
omniroute setup-5dive
```

Ognuno accetta `--remote <url> --api-key <key>` (per configurare uno strumento locale rispetto a un OmniRoute remoto), `--dry-run` (anteprima senza scrittura) e `--port`. Gli strumenti senza auto-scoperta del modello (Cline, Kilo, Roo, Goose, Aider, Qwen, 5dive) accettano `--model <id>` (e `--yes` per esecuzioni non interattive). `setup-5dive` è l'unica ricetta che non scrive sotto `$HOME`: configura un fleet di agenti 5dive scrivendo un profilo di autenticazione di proprietà di root sull'host del fleet, quindi si riesegue tramite `sudo` e non ha una propria modalità remota. Per avviare una CLI con l'ambiente corretto iniettato e senza alcuna configurazione scritta, usa il launcher generico `omniroute run <target>` (claude, codex, aider, goose, opencode, qwen, gemini — i target e gli alias provengono da `bin/cli/cli-manifest.mjs`); i launcher legacy per strumento `omniroute launch` (Claude Code) e `omniroute launch-codex` (Codex) rimangono disponibili. La CLI Gemini è solo per l'avvio: è un target di `omniroute run` ma non ha una ricetta `setup-*`/`configure`.

> **Riferimento completo:** la tabella principale — cosa scrive ogni comando, ogni flag, locale vs remoto e quali strumenti richiedono un suffisso `/v1` — si trova in **[CLI Integrations](../guides/CLI-INTEGRATIONS.md)**.

### Esecuzione all'interno di un container

Un comando `setup-*` eseguito all'interno del container OmniRoute scrive nella home del container stesso, che nessuna CLI dell'host legge e che scompare con il container. OmniRoute lo rileva ed esce con codice `2` fornendo istruzioni anziché scrivere. Due modi supportati per procedere — installare la CLI sull'host e `omniroute connect` al container, oppure montare le directory di configurazione tramite bind-mount e impostare `CLI_CONFIG_HOME` (il profilo `host` di compose). Ogni comando `setup-*`, più `omniroute configure` e `omniroute config set`, accetta `--allow-container-write` quando la configurazione delle CLI del container è ciò che intendevi; `OMNIROUTE_ALLOW_CONTAINER_CONFIG_WRITE=true` fa lo stesso per il server. Vedi [Guida Docker → Configurazione degli strumenti CLI dell'host](../guides/DOCKER_GUIDE.md#configuring-host-cli-tools-when-omniroute-runs-in-docker).

L'**endpoint di applicazione** della dashboard (`POST /api/cli-tools/apply`) applica la stessa protezione: in un container, una scrittura il cui target non è montato tramite bind-mount dall'host risponde con **`422`** e `containerEphemeralTarget: true`, il testo di errore sicuro e — per gli strumenti con una ricetta host (claude, codex, opencode, cline, kilo, continue) — un `hostSetupCommand` (es. `omniroute setup-opencode`) da eseguire sull'host; nulla viene scritto. `dryRun: true` continua a funzionare in modalità container e restituisce un'anteprima redatta + percorso target senza toccare il disco. Il contenuto dell'anteprima non è una configurazione contenente credenziali da copiare o importare. Applica con lo strumento originale/URL di base/chiave API/input del modello sull'host, oppure usa il comando di setup lato host indicato. Vedi [Sicurezza della configurazione CLI](../security/CLI-CONFIGURATION.md) per l'intestazione dell'anteprima e il contratto di richiesta. Questo comportamento è intenzionale e protetto da regressioni tramite `tests/unit/api/cli-tools/apply-container-guard.test.ts` — non "correggere" mai un 422 rimuovendo la protezione.

---

## Fonte di verità

Il catalogo unificato si trova in `src/shared/constants/cliTools.ts` come `CLI_TOOLS: Record<string, CliCatalogEntry>`.

Ogni voce contiene questi campi (definiti in `src/shared/schemas/cliCatalog.ts`):

| Campo                                           | Tipo                                                         | Descrizione                                                              |
| ----------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------ |
| `category`                                      | `"code" \| "agent"`                                          | La pagina in cui appare lo strumento                                     |
| `vendor`                                        | `string`                                                     | Origine dello strumento ("Anthropic", "OSS (P. Gauthier)")               |
| `acpSpawnable`                                  | `boolean`                                                    | Utilizzabile anche come agente ACP (viene mostrato un badge)             |
| `baseUrlSupport`                                | `"full" \| "partial" \| "none"`                              | Livello di supporto per endpoint personalizzati. `"none"` = backlog MITM |
| `configType`                                    | `"env" \| "custom" \| "guide" \| "custom-builder" \| "mitm"` | Meccanismo di configurazione                                             |
| `id`, `name`, `color`, `description`, `docsUrl` | standard                                                     | Campi principali di visualizzazione                                      |

Le voci con `baseUrlSupport: "none"` **non vengono mostrate** nelle pagine della dashboard: sono registrate nel backlog MITM per il piano 11 (vedere `_tasks/features-v3.8.6/refactorpages/_orchestration/_plan11-mitm-backlog.md`).

### Livelli di funzionalità (catalogato × rilevabile × configurabile × avviabile)

Non tutti gli strumenti catalogati sono rilevabili, configurabili o avviabili. Ogni livello ha una
singola fonte dichiarativa e un test di divergenza ne mantiene l'allineamento:

| Livello           | Significato                                                                                      | Dichiarato in                                                         |
| ----------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| **Catalogato**    | Appare nel catalogo della dashboard (nome, fornitore, documentazione, tipo di configurazione)    | `src/shared/constants/cliTools.ts` (`CLI_TOOLS`)                      |
| **Rilevabile**    | Rilevamento del binario/della configurazione, controlli di integrità, percorsi di configurazione | `src/shared/services/cliRuntime.ts` (catalogo di runtime `CLI_TOOLS`) |
| **Configurabile** | Supportato da `omniroute configure <cli>` (esiste una procedura di configurazione)               | `bin/cli/cli-manifest.mjs` (`configure: true`)                        |
| **Avviabile**     | Supportato da `omniroute run <target>` (definita l'iniezione di variabili d'ambiente/argomenti)  | `bin/cli/cli-manifest.mjs` (`run: true`)                              |

`bin/cli/cli-manifest.mjs` è il manifest eseguibile canonico per le interfacce dei comandi CLI:
`run`, `configure` e i generatori di completamento della shell derivano tutti da esso
gli elenchi delle destinazioni, la risoluzione degli alias (ad esempio `kilocode`/`kilo-code`/`kilo_cli` → `kilo`)
e il collegamento del flag `--model`. Il controllo di divergenza
`tests/unit/cli/cli-manifest-drift.test.ts` verifica che il manifest, il catalogo di runtime,
il catalogo dell'interfaccia utente e ogni interfaccia che li utilizza rimangano sincronizzati: una destinazione aggiunta a
un'interfaccia senza essere aggiunta alle altre causa il fallimento della suite anziché generare silenziosamente una divergenza.

---

## 1. Catalogo di CLI Code (26 strumenti)

Tutti gli strumenti visualizzati in `/dashboard/cli-code`. Quelli con `baseUrlSupport: none` vengono configurati tramite MITM o una guida manuale anziché mediante un URL di base personalizzato:

| id           | nome                    | fornitore           | supporto URL di base | tipo di configurazione    | avviabile tramite ACP |
| ------------ | ----------------------- | ------------------- | -------------------- | ------------------------- | --------------------- |
| claude       | Claude Code             | Anthropic           | completo             | variabile d'ambiente      | sì                    |
| codex        | OpenAI Codex CLI        | OpenAI              | completo             | personalizzato            | sì                    |
| zcode        | ZCode (GLM Coding Plan) | Z.ai                | nessuno              | personalizzato            | no                    |
| cline        | Cline                   | OSS (ex-Claude Dev) | completo             | personalizzato            | sì                    |
| kilo         | Kilo Code               | Kilo-Org            | completo             | personalizzato            | no                    |
| roo          | Roo Code                | Roo (OSS)           | completo             | guida                     | no                    |
| continue     | Continue                | continue.dev        | completo             | guida                     | no                    |
| aider        | Aider                   | OSS (P. Gauthier)   | completo             | guida                     | sì                    |
| forge        | ForgeCode               | Antinomy HQ         | completo             | personalizzato            | sì                    |
| jcode        | jcode                   | 1jehuang (OSS)      | completo             | personalizzato            | no                    |
| deepseek-tui | DeepSeek TUI            | Hunter Bown (OSS)   | completo             | personalizzato            | no                    |
| codewhale    | CodeWhale               | Hmbown (OSS)        | completo             | personalizzato            | no                    |
| opencode     | OpenCode                | Anomaly (ex-SST)    | completo             | guida                     | sì                    |
| droid        | Factory Droid           | Factory AI          | parziale             | guida                     | no                    |
| copilot      | GitHub Copilot CLI      | GitHub/MS           | completo             | personalizzato            | no                    |
| cursor-cli   | Cursor CLI              | Anysphere           | parziale             | guida                     | sì                    |
| smelt        | Smelt                   | leonardcser (OSS)   | completo             | personalizzato            | no                    |
| pi           | Pi (pi-coding-agent)    | M. Zechner (OSS)    | completo             | personalizzato            | no                    |
| grok-build   | Grok Build              | xAI                 | completo             | personalizzato            | no                    |
| crush        | Crush                   | OSS (Charm)         | completo             | personalizzato            | no                    |
| qwen         | Qwen Code               | Alibaba             | completo             | guida                     | sì                    |
| cursor       | Cursor                  | Anysphere           | nessuno              | guida                     | no                    |
| antigravity  | Antigravity             | Google              | nessuno              | mitm                      | no                    |
| hermes       | Hermes                  | Nous Research       | nessuno              | guida                     | no                    |
| kiro         | Kiro AI                 | Amazon              | nessuno              | mitm                      | no                    |
| custom       | CLI personalizzata      | —                   | completo             | generatore personalizzato | no                    |

Gli strumenti con `baseUrlSupport: "partial"` mostrano il badge "⚠ URL di base parziale" nella scheda della dashboard.
---

## 2. Catalogo Agenti CLI (10 strumenti)

Agenti autonomi che appaiono in `/dashboard/cli-agents`:

| id           | name             | vendor                   | baseUrlSupport | acpSpawnable |
| ------------ | ---------------- | ------------------------ | -------------- | ------------ |
| hermes-agent | Hermes Agent     | Nous Research            | completo       | falso        |
| openclaw     | OpenClaw         | OSS (P. Steinberger)     | completo       | vero         |
| goose        | Goose            | Block / Linux Foundation | completo       | vero         |
| interpreter  | Open Interpreter | OSS                      | completo       | vero         |
| warp         | Warp AI          | Warp Inc.                | parziale       | vero         |
| agent-deck   | Agent Deck       | asheshgoplani (OSS)      | completo       | falso        |
| omp          | Oh My Pi         | OSS                      | completo       | vero         |
| letta        | Letta CLI        | Letta                    | completo       | falso        |
| prime-agent  | Prime Agent      | Prime Intellect (OSS)    | completo       | falso        |
| 5dive        | 5dive            | OSS (5dive-ai)           | completo       | falso        |

---

## 3. Agenti ACP (/dashboard/acp-agents)

Questa pagina (rinominata da `/dashboard/agents`) mostra le CLI che OmniRoute può **avviare** come motori di esecuzione backend tramite il protocollo stdio/ACP. Il catalogo viene gestito separatamente in `src/lib/acp/registry.ts` e **non** coincide con `CLI_TOOLS`.

---

## 4. Backlog MITM (non visualizzato nella dashboard)

Le seguenti CLI non supportano nativamente un URL di base personalizzato e **non sono elencate** nelle pagine CLI Code o CLI Agents. Sono candidate all'intercettazione MITM nel piano 11:

| CLI                 | Motivo                                                           |
| ------------------- | ---------------------------------------------------------------- |
| windsurf            | BYOK limitato a determinati modelli Claude + URL/token aziendale |
| amp                 | Ecosistema chiuso (Sourcegraph)                                  |
| amazon-q / kiro-cli | Autenticazione AWS SSO, nessun URL personalizzato                |
| cowork              | Anthropic Desktop, nessun endpoint configurabile                 |

Consultare `_tasks/features-v3.8.6/refactorpages/_orchestration/_plan11-mitm-backlog.md` per il riferimento incrociato completo.

---

## 5. API di rilevamento in batch

Il rilevamento di tutti gli strumenti viene aggregato tramite un singolo endpoint:

**`GET /api/cli-tools/all-statuses`**

- Autenticazione: `requireCliToolsAuth(request)` (come per le altre route `/api/cli-tools/`)
- Restituisce: `Record<toolId, ToolBatchStatus>` (tipo: `src/shared/types/cliBatchStatus.ts`)
- Strategia: `Promise.all` su tutti gli strumenti, timeout di 5s per strumento
- Cache: LRU in memoria indicizzata in base a `mtime` del file di configurazione. La cache viene invalidata quando `mtime` cambia. Viene reimpostata al riavvio del server.

Struttura della risposta per ciascuno strumento:

```ts
interface ToolBatchStatus {
  detection: {
    installed: boolean;
    runnable: boolean;
    version?: string;
    command?: string;
    commandPath?: string;
    reason?: string;
  };
  config: {
    status: "configured" | "not_configured" | "not_installed" | "unknown" | "other";
    endpoint?: string | null;
    lastConfiguredAt?: string | null;
  };
  error?: string; // sanitizzato, senza tracce dello stack
}
```

---

## 6. Gestori delle impostazioni per i nuovi strumenti

I nuovi strumenti con `configType: "custom"` dispongono di route API dedicate per le impostazioni:

| Route                                       | Strumento                                                                         |
| ------------------------------------------- | --------------------------------------------------------------------------------- |
| `POST /api/cli-tools/forge-settings`        | ForgeCode (.forge.toml)                                                           |
| `POST /api/cli-tools/jcode-settings`        | jcode (flag --base-url)                                                           |
| `POST /api/cli-tools/deepseek-tui-settings` | DeepSeek TUI (OPENAI_BASE_URL, legacy)                                            |
| `POST /api/cli-tools/codewhale-settings`    | CodeWhale (OPENAI_BASE_URL, sincronizzazione primaria + legacy con `~/.deepseek`) |
| `POST /api/cli-tools/smelt-settings`        | Smelt                                                                             |
| `POST /api/cli-tools/pi-settings`           | Agente di coding Pi                                                               |
| `POST /api/cli-tools/grok-build-settings`   | Grok Build (~/.grok/config.toml, `[model.omniroute]`)                             |
| `POST /api/cli-tools/qwen-settings`         | Qwen Code (`~/.qwen/settings.json` + chiave `.env` dedicata)                      |

Tutte le route utilizzano `sanitizeErrorMessage()` per le risposte di errore (Regola vincolante n. 12).

---

## 7. Architettura delle pagine della dashboard

### Codici CLI (`/dashboard/cli-code`)

- `src/app/(dashboard)/dashboard/cli-code/page.tsx` — componente server
- `src/app/(dashboard)/dashboard/cli-code/CliCodePageClient.tsx` — griglia client
- `src/app/(dashboard)/dashboard/cli-code/[id]/page.tsx` — pagina dei dettagli dello strumento
- `src/app/(dashboard)/dashboard/cli-code/components/` — 12 schede specializzate per gli strumenti + `ToolDetailClient.tsx`

### Agenti CLI (`/dashboard/cli-agents`)

- `src/app/(dashboard)/dashboard/cli-agents/page.tsx` — componente server
- `src/app/(dashboard)/dashboard/cli-agents/CliAgentsPageClient.tsx` — griglia client
- `src/app/(dashboard)/dashboard/cli-agents/[id]/page.tsx` — riutilizza `ToolDetailClient`

### Agenti ACP (`/dashboard/acp-agents`)

- `src/app/(dashboard)/dashboard/acp-agents/page.tsx` — componente server (spostato da `agents/`)

### Componenti UI condivisi (`src/shared/components/cli/`)

| File                    | Scopo                                                                     |
| ----------------------- | ------------------------------------------------------------------------- |
| `CliToolCard.tsx`       | Scheda intelligente dello stato (rilevamento + configurazione + endpoint) |
| `CliConceptCard.tsx`    | Scheda esplicativa dei concetti specifica per pagina                      |
| `CliComparisonCard.tsx` | Confronto su tre colonne tra i tipi di CLI                                |
| `BaseUrlSelect.tsx`     | Menu a discesa degli endpoint (Locale/Cloud/Personalizzato)               |
| `ApiKeySelect.tsx`      | Selettore della chiave API                                                |
| `ManualConfigModal.tsx` | Finestra modale con frammento di configurazione copiabile                 |

### Hook condiviso (`src/shared/hooks/cli/`)

| File                      | Scopo                                                                                   |
| ------------------------- | --------------------------------------------------------------------------------------- |
| `useToolBatchStatuses.ts` | Recupera `/api/cli-tools/all-statuses` e gestisce lo stato di caricamento/aggiornamento |

---

## 8. i18n

Nuovi namespace aggiunti nel piano 14 F9:

| Namespace   | Scopo                                                                                                         |
| ----------- | ------------------------------------------------------------------------------------------------------------- |
| `cliCommon` | Stringhe condivise (etichette delle schede, testi di concetti/confronti, etichette delle pagine di dettaglio) |
| `cliCode`   | Stringhe della pagina CLI Code                                                                                |
| `cliAgents` | Stringhe della pagina CLI Agents                                                                              |
| `acpAgents` | Stringhe della pagina ACP Agents                                                                              |

Sono fornite traduzioni complete in PT-BR e EN. Le altre 39 impostazioni locali utilizzano automaticamente EN come fallback tramite l'unione a livello di namespace in `src/i18n/request.ts`.

---

## 9. Avvio rapido

### Passaggio 1 — Ottieni una chiave API OmniRoute

1. Apri `/dashboard/api-manager` → **Crea chiave API**
2. Assegnale un nome (ad es. `cli-tools`) e seleziona tutte le autorizzazioni
3. Copia la chiave: ti servirà per ogni CLI riportata di seguito

> La chiave ha questo aspetto: `sk-xxxxxxxxxxxxxxxx-xxxxxxxxx`

---

### Passaggio 2 — Installa gli strumenti CLI

Tutti gli strumenti basati su npm richiedono Node.js 22.22.2+ o 24.x:

```bash
# Claude Code (Anthropic)
npm install -g @anthropic-ai/claude-code

# OpenAI Codex
npm install -g @openai/codex

# OpenCode
npm install -g opencode-ai

# Cline
npm install -g cline

# KiloCode
npm install -g kilocode

# Qwen Code
npm install -g @qwen-code/qwen-code

# Google Gemini CLI (avviabile tramite `omniroute run gemini` → endpoint /v1beta)
npm install -g @google/gemini-cli

# Aider
pip install aider-chat

# Smelt
cargo install smelt  # Basato su Rust

# Agente di programmazione Pi
# consulta https://github.com/zechnerj/pi-coding-agent per l'installazione

# jcode
# consulta https://github.com/1jehuang/jcode per l'installazione
```

---

### Passaggio 3 — Configura tramite la dashboard

1. Vai a `http://localhost:20128/dashboard/cli-code`
2. Trova il tuo strumento nella griglia
3. Fai clic sulla scheda per aprire la pagina dei dettagli dello strumento
4. Seleziona la chiave API e l'URL di base
5. Fai clic su **Applica configurazione** oppure copia il frammento di configurazione manuale

---

### Passaggio 4 — Imposta le variabili di ambiente globali

```bash
# Endpoint universale OmniRoute
export OPENAI_BASE_URL="http://localhost:20128/v1"
export OPENAI_API_KEY="sk-your-omniroute-key"
export ANTHROPIC_BASE_URL="http://localhost:20128"
export ANTHROPIC_AUTH_TOKEN="sk-your-omniroute-key"
# Gemini CLI legge GOOGLE_GEMINI_BASE_URL dalla RADICE (il suo SDK aggiunge autonomamente /v1beta/...)
export GOOGLE_GEMINI_BASE_URL="http://localhost:20128"
export GEMINI_API_KEY="sk-your-omniroute-key"
```

> Per un **server remoto**, sostituisci `localhost:20128` con l'IP o il dominio del server,
> ad es. `http://<your-server-ip>:20128`.

---

### Passaggio 4 — Configura ogni strumento

#### Claude Code

```bash
# Crea ~/.claude/settings.json:
mkdir -p ~/.claude && cat > ~/.claude/settings.json << EOF
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://localhost:20128",
    "ANTHROPIC_AUTH_TOKEN": "sk-your-omniroute-key"
  }
}
EOF
```

Usa la radice unificata del gateway Anthropic per Claude Code. Non aggiungere `/v1` qui.

**Test:** `claude "say hello"`

---

#### OpenAI Codex

Le versioni moderne di Codex (v0.137+) leggono soltanto `~/.codex/config.toml`: il vecchio
`config.yaml` appartiene alla CLI npm precedente e viene ignorato senza alcun avviso. La chiave
API rimane nella variabile di ambiente `OMNIROUTE_API_KEY` (`env_key`) e non viene mai
inserita nel file:

```bash
mkdir -p ~/.codex && cat > ~/.codex/config.toml << EOF
model_provider = "omniroute"

[model_providers.omniroute]
name                 = "OmniRoute"
base_url             = "http://localhost:20128/v1"
env_key              = "OMNIROUTE_API_KEY"
requires_openai_auth = false
EOF
export OMNIROUTE_API_KEY="sk-your-omniroute-key"
```

Riferimento completo (profili, `wire_api`, finestre di contesto): [CODEX-CLI-CONFIGURATION.md](../guides/CODEX-CLI-CONFIGURATION.md).

**Test:** `codex "what is 2+2?"`

---

#### OpenCode

```bash
mkdir -p ~/.config/opencode && cat > ~/.config/opencode/opencode.json << EOF
{
  "\$schema": "https://opencode.ai/config.json",
  "provider": {
    "omniroute": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "OmniRoute",
      "options": {
        "baseURL": "http://localhost:20128/v1",
        "apiKey": "sk-your-omniroute-key"
      },
      "models": {
        "claude-sonnet-4-5": { "name": "claude-sonnet-4-5" },
        "claude-sonnet-4-5-thinking": { "name": "claude-sonnet-4-5-thinking" },
        "gemini-3-flash": { "name": "gemini-3-flash" }
      }
    }
  }
}
EOF
```

**Test:** `opencode`

> Usa `opencode run "your prompt" --model omniroute/claude-sonnet-4-5-thinking --variant high`
> per inviare varianti con ragionamento.

---

#### Cline (CLI o VS Code)

**Modalità CLI:**

```bash
mkdir -p ~/.cline/data && cat > ~/.cline/data/globalState.json << EOF
{
  "apiProvider": "openai",
  "openAiBaseUrl": "http://localhost:20128/v1",
  "openAiApiKey": "sk-your-omniroute-key"
}
EOF
```

**Modalità VS Code:**
Impostazioni dell'estensione Cline → Provider API: `OpenAI Compatible` → URL di base: `http://localhost:20128/v1`

In alternativa, usa la dashboard di OmniRoute → **Strumenti CLI → Cline → Applica configurazione**.

---

#### KiloCode (CLI o VS Code)

**Modalità CLI:**

```bash
kilocode --api-base http://localhost:20128/v1 --api-key sk-your-omniroute-key
```

**Impostazioni di VS Code:**

```json
{
  "kilo-code.openAiBaseUrl": "http://localhost:20128/v1",
  "kilo-code.apiKey": "sk-your-omniroute-key"
}
```

In alternativa, usa la dashboard di OmniRoute → **Strumenti CLI → KiloCode → Applica configurazione**.

---

#### Continue (estensione VS Code)

Modifica `~/.continue/config.yaml`:

```yaml
models:
  - name: OmniRoute
    provider: openai
    model: auto
    apiBase: http://localhost:20128/v1
    apiKey: sk-your-omniroute-key
    default: true
```

Riavvia VS Code dopo la modifica.

---

#### VS Code Insiders (`chatLanguageModels.json`)

Usa questa configurazione quando VS Code Insiders è configurato per modelli con endpoint personalizzati e vuoi che OmniRoute funzioni senza un campo di intestazione personalizzato.

**Percorso consigliato:**

- Linux: `~/.config/Code - Insiders/User/chatLanguageModels.json`
- Windows: `%APPDATA%/Code - Insiders/User/chatLanguageModels.json`

**Esempio di utilizzo dell'alias OmniRoute con token:**

```json
[
  {
    "vendor": "customendpoint",
    "id": "auto",
    "name": "OmniRoute Auto",
    "family": "gpt-4",
    "version": "1.0.0",
    "url": "http://localhost:20128/api/v1/vscode/sk-your-omniroute-key/chat/completions",
    "modelsUrl": "http://localhost:20128/api/v1/vscode/sk-your-omniroute-key/models",
    "requestFormat": "openai-chat-completions",
    "contextWindow": 256000,
    "maxOutputTokens": 32768,
    "auth": {
      "type": "none"
    }
  }
]
```

**Note:**

- Sostituisci `sk-your-omniroute-key` con una chiave API creata in OmniRoute.
- Il campo `url` deve puntare a `/api/v1/vscode/{token}/chat/completions`.
- Il campo `modelsUrl` deve puntare a `/api/v1/vscode/{token}/models`.
- Preferisci il normale flusso `/v1` con intestazione Bearer quando il client supporta intestazioni personalizzate.
- I token incorporati negli URL rappresentano una soluzione di compatibilità alternativa e possono comparire nei log dell'editor o nella cronologia del proxy.

---

#### Kiro CLI (Amazon)

```bash
# Accedi al tuo account AWS/Kiro:
kiro-cli login

# La CLI utilizza la propria autenticazione: OmniRoute non è necessario come backend per Kiro CLI.
# Usa kiro-cli insieme a OmniRoute per altri strumenti.
kiro-cli status
```

Per l'app desktop **Kiro IDE**, utilizza l'endpoint MITM esposto da OmniRoute
in `/dashboard/cli-tools → Kiro`.

---

## 10. CLI interna di OmniRoute

Il binario `omniroute` fornisce comandi per il ciclo di vita del server, la configurazione, la diagnostica e la gestione dei provider. Punto di ingresso: `bin/omniroute.mjs`.

```bash
omniroute                              # Avvia il server (porta predefinita 20128)
omniroute setup                        # Procedura guidata di configurazione interattiva
omniroute doctor                       # Controlla configurazione, DB, porte e runtime
omniroute providers list               # Connessioni ai provider configurate
omniroute providers test-all           # Verifica ogni connessione attiva
omniroute reset-password               # Reimposta la password dell'amministratore
omniroute logs                         # Trasmette i log delle richieste
omniroute health                       # Stato dettagliato (circuit breaker, cache, memoria)
omniroute --version                    # Mostra la versione
omniroute --help                       # Mostra tutti i comandi
```

### Configurazione e inizializzazione

```bash
omniroute setup                        # Procedura guidata di configurazione interattiva
omniroute setup --non-interactive      # Modalità CI/automazione (legge variabili di ambiente + flag)
omniroute setup --password '<value>'   # Imposta direttamente la password dell'amministratore
omniroute setup --add-provider \
  --provider openai \
  --api-key '<value>' \
  --test-provider                      # Aggiunge e verifica un provider in un'unica operazione
```

Variabili di ambiente riconosciute per la configurazione non interattiva:

| Var                 | Scopo                                                                           |
| ------------------- | ------------------------------------------------------------------------------- |
| `OMNIROUTE_API_KEY` | Chiave API del provider (associata a `--api-key` tramite `.env()` di Commander) |
| `DATA_DIR`          | Sostituisce la directory dei dati di OmniRoute                                  |

Tutti gli altri input non interattivi vengono passati come flag, non come variabili di ambiente:
`--password`, `--provider`, `--provider-name`, `--provider-base-url`, `--default-model`
(vedere le opzioni di `omniroute setup` riportate sopra).

### Diagnostica

```bash
omniroute doctor                       # Controlla configurazione, DB, porte, runtime, memoria e operatività
omniroute doctor --json                # JSON leggibile dalla macchina
omniroute doctor --no-liveness         # Ignora il controllo di integrità HTTP
omniroute doctor --host 0.0.0.0        # Sostituisce l'host del controllo di operatività
omniroute doctor --liveness-url <url>  # Sostituisce l'URL completo dell'endpoint di integrità
```

Il comando doctor esegue questi controlli: `Config`, `Database`, `Storage/encryption`,
`Port availability`, `Node runtime`, `Native binary` (better-sqlite3),
`Memory` e `Server liveness`. Termina con un codice diverso da zero se un controllo qualsiasi restituisce `fail`.

### Gestione dei provider

```bash
omniroute providers available                       # Catalogo dei provider OmniRoute
omniroute providers available --search openai       # Filtra il catalogo per ID/nome/alias/categoria
omniroute providers available --category api-key    # Filtra per categoria (api-key, oauth, free, ...)
omniroute providers available --json                # JSON leggibile dalla macchina

omniroute providers list                            # Connessioni ai provider configurate
omniroute providers list --json

omniroute providers test <id|name>                  # Verifica una connessione configurata
omniroute providers test-all                        # Verifica ogni connessione attiva
omniroute providers validate                        # Convalida strutturale esclusivamente locale
omniroute providers add <provider> --credential-env PROVIDER_KEY
omniroute providers import ./providers.json --dry-run --json
omniroute providers auth <provider>                 # Flusso OAuth esistente
omniroute providers edit <id|name> --default-model <model>
omniroute providers remove <id|name> --yes
```

`providers add/import/auth/edit/remove` privilegiano l'API e funzionano pertanto con
il contesto locale o remoto attivo. Per l'inserimento delle credenziali è consigliabile usare
`--credential-stdin` o `--credential-env`; `--dry-run --json` restituisce soltanto
presenza e struttura oscurate. `providers available` legge il catalogo OmniRoute;
`providers list/test/test-all/validate` mantengono il proprio comportamento SQLite locale e
non richiedono che il server sia in esecuzione.

### Ripristino e reimpostazione

```bash
omniroute reset-password                # Reimposta la password dell'amministratore (anche: omniroute-reset-password)
omniroute reset-encrypted-columns       # Mostra un avviso + una simulazione della reimpostazione delle credenziali cifrate
omniroute reset-encrypted-columns --force  # Imposta effettivamente su null le credenziali cifrate in SQLite
```

### Esportazione delle credenziali (⚠ maneggiare con cautela)

```bash
omniroute auth export                                 # Mostra un avviso + richiesta di conferma — nessun accesso al DB
omniroute auth export --force                          # Esporta su stdout in formato JSON le credenziali DECIFRATE di TUTTE le connessioni
omniroute auth export --force --id <id>                 # Esporta solo la connessione corrispondente
omniroute auth export --force --format env               # Emette righe OMNIROUTE_<PROVIDER>_<FIELD>=<value>
omniroute auth export --force --out creds.json           # Scrive in un file (creato con autorizzazioni 0600)
```

`auth export` opera **esclusivamente in locale** (lettura diretta di SQLite, nessuna route HTTP) e stampa/scrive intenzionalmente
i valori **in testo normale** di `apiKey`/`accessToken`/`refreshToken`/`idToken`: è una funzionalità, non un
bug. Senza `--force`, nulla viene letto dal database e nulla viene decifrato. Prima che venga emesso qualsiasi testo normale,
su stderr viene sempre visualizzato un banner di avviso. Richiede che `STORAGE_ENCRYPTION_KEY` sia
impostata. Un campo che non può essere decifrato (chiave obsoleta, testo cifrato danneggiato) viene segnalato come
`<field>DecryptFailed: true`, anziché interrompere l'intera esportazione o esporre l'errore sottostante.

### Altri sottocomandi

Questi presuppongono che sia in esecuzione un server OmniRoute, salvo diversa indicazione:

```bash
omniroute status                       # Stato completo del runtime
omniroute logs                         # Visualizza i log delle richieste in streaming (--json, --search, --follow)
omniroute config list                  # Mostra gli strumenti CLI configurati

omniroute provider list                # Elenca i provider disponibili (alias di providers list)
omniroute provider add                 # Registra OmniRoute come provider in uno strumento
omniroute keys add | list | remove     # Gestisce le chiavi API
omniroute models [provider]            # Elenca i modelli (--json, --search)
omniroute combo list | switch | create | delete

omniroute backup                       # Crea uno snapshot della configurazione e del DB
omniroute restore                      # Ripristina da uno snapshot precedente

omniroute health                       # Stato dettagliato (circuit breaker, cache, memoria)
omniroute quota                        # Utilizzo delle quote dei provider
omniroute cache                        # Stato della cache
omniroute cache clear                  # Svuota le cache semantiche e delle firme

omniroute mcp status | restart         # Stato/riavvio del server MCP
omniroute a2a status | card            # Stato del server A2A/scheda dell'agente

omniroute tunnel list | create | stop  # Gestisce i tunnel (cloudflare/tailscale/ngrok)
omniroute env show | get <k> | set <k> <v>  # Visualizza/imposta le variabili di ambiente (temporanee)

omniroute test                         # Test rapido della connettività dei provider
omniroute update                       # Verifica la disponibilità di aggiornamenti
omniroute completion                   # Genera il completamento della shell
```

### Opzioni comuni

| Opzione             | Descrizione                                                   |
| ------------------- | ------------------------------------------------------------- |
| `--no-open`         | Non apre automaticamente il browser all'avvio                 |
| `--port <n>`        | Sostituisce la porta API (valore predefinito: 20128)          |
| `--mcp`             | Esegue come server MCP tramite stdio (per gli IDE)            |
| `--non-interactive` | Modalità CI (nessuna richiesta; legge da ambiente/opzioni)    |
| `--json`            | Output JSON leggibile dalla macchina (doctor, provider, ecc.) |
| `--help`, `-h`      | Mostra la guida specifica del comando                         |
| `--version`, `-v`   | Mostra la versione installata                                 |

---

## Endpoint API disponibili

| Endpoint                   | Descrizione                      | Utilizzo                                   |
| -------------------------- | -------------------------------- | ------------------------------------------ |
| `/v1/chat/completions`     | Chat standard (tutti i provider) | Tutti gli strumenti moderni                |
| `/v1/responses`            | API Responses (formato OpenAI)   | Codex, flussi di lavoro agentici           |
| `/v1/completions`          | Completamenti di testo legacy    | Strumenti meno recenti che usano `prompt:` |
| `/v1/embeddings`           | Incorporamenti di testo          | RAG, ricerca                               |
| `/v1/images/generations`   | Generazione di immagini          | GPT-Image, Flux, ecc.                      |
| `/v1/audio/speech`         | Sintesi vocale                   | ElevenLabs, OpenAI TTS                     |
| `/v1/audio/transcriptions` | Trascrizione vocale              | Deepgram, AssemblyAI                       |

Esempi pronti da incollare con un URL OmniRoute contenente un token:

```txt
Esempio di token: sk-a3ab3c080beaee3a-69f4a4-070d71af

Base OpenAI standard: http://localhost:20128/v1
Modelli VS Code: http://localhost:20128/api/v1/vscode/sk-a3ab3c080beaee3a-69f4a4-070d71af/models
Chat VS Code: http://localhost:20128/api/v1/vscode/sk-a3ab3c080beaee3a-69f4a4-070d71af/chat/completions
Risposte VS Code: http://localhost:20128/api/v1/vscode/sk-a3ab3c080beaee3a-69f4a4-070d71af/responses
Tag Ollama: http://localhost:20128/api/v1/vscode/sk-a3ab3c080beaee3a-69f4a4-070d71af/api/tags
Chat Ollama: http://localhost:20128/api/v1/vscode/sk-a3ab3c080beaee3a-69f4a4-070d71af/api/chat
```

---

## Risoluzione dei problemi

| Errore                                                  | Causa                                  | Soluzione                                                     |
| ------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------- |
| `Connection refused`                                    | OmniRoute non è in esecuzione          | `omniroute serve`                                             |
| `401 Unauthorized`                                      | Chiave API errata                      | Verifica in `/dashboard/api-manager`                          |
| `No combo configured`                                   | Nessuna combinazione di routing attiva | Configurala in `/dashboard/combos`                            |
| La CLI mostra "not installed"                           | Il file binario non è nel PATH         | Controlla con `which <command>`                               |
| La dashboard mostra "not detected" dopo l'installazione | Cache non aggiornata                   | Fai clic su "⟳ Refresh detection" nella dashboard             |
| Vecchio link `/dashboard/cli-tools`                     | Segnalibro precedente alla v3.8.6      | Reindirizzato automaticamente a `/dashboard/cli-code` (308)   |
| Vecchio link `/dashboard/agents`                        | Segnalibro precedente alla v3.8.6      | Reindirizzato automaticamente a `/dashboard/acp-agents` (308) |
