# Cost & Spend Tracking (Italiano)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/COST_TRACKING.md) · 🇪🇹 [am](../../../am/docs/guides/COST_TRACKING.md) · 🇸🇦 [ar](../../../ar/docs/guides/COST_TRACKING.md) · 🇦🇿 [az](../../../az/docs/guides/COST_TRACKING.md) · 🇧🇬 [bg](../../../bg/docs/guides/COST_TRACKING.md) · 🇧🇩 [bn](../../../bn/docs/guides/COST_TRACKING.md) · 🇧🇦 [bs](../../../bs/docs/guides/COST_TRACKING.md) · 🇨🇿 [cs](../../../cs/docs/guides/COST_TRACKING.md) · 🇩🇰 [da](../../../da/docs/guides/COST_TRACKING.md) · 🇩🇪 [de](../../../de/docs/guides/COST_TRACKING.md) · 🇬🇷 [el](../../../el/docs/guides/COST_TRACKING.md) · 🇪🇸 [es](../../../es/docs/guides/COST_TRACKING.md) · 🇪🇪 [et](../../../et/docs/guides/COST_TRACKING.md) · 🇮🇷 [fa](../../../fa/docs/guides/COST_TRACKING.md) · 🇫🇮 [fi](../../../fi/docs/guides/COST_TRACKING.md) · 🇫🇷 [fr](../../../fr/docs/guides/COST_TRACKING.md) · 🇮🇪 [ga](../../../ga/docs/guides/COST_TRACKING.md) · 🇮🇳 [gu](../../../gu/docs/guides/COST_TRACKING.md) · 🇳🇬 [ha](../../../ha/docs/guides/COST_TRACKING.md) · 🇮🇱 [he](../../../he/docs/guides/COST_TRACKING.md) · 🇮🇳 [hi](../../../hi/docs/guides/COST_TRACKING.md) · 🇭🇷 [hr](../../../hr/docs/guides/COST_TRACKING.md) · 🇭🇺 [hu](../../../hu/docs/guides/COST_TRACKING.md) · 🇦🇲 [hy](../../../hy/docs/guides/COST_TRACKING.md) · 🇮🇩 [id](../../../id/docs/guides/COST_TRACKING.md) · 🇳🇬 [ig](../../../ig/docs/guides/COST_TRACKING.md) · 🇯🇵 [ja](../../../ja/docs/guides/COST_TRACKING.md) · 🇬🇪 [ka](../../../ka/docs/guides/COST_TRACKING.md) · 🇰🇭 [km](../../../km/docs/guides/COST_TRACKING.md) · 🇮🇳 [kn](../../../kn/docs/guides/COST_TRACKING.md) · 🇰🇷 [ko](../../../ko/docs/guides/COST_TRACKING.md) · 🇱🇹 [lt](../../../lt/docs/guides/COST_TRACKING.md) · 🇱🇻 [lv](../../../lv/docs/guides/COST_TRACKING.md) · 🇮🇳 [ml](../../../ml/docs/guides/COST_TRACKING.md) · 🇮🇳 [mr](../../../mr/docs/guides/COST_TRACKING.md) · 🇲🇾 [ms](../../../ms/docs/guides/COST_TRACKING.md) · 🇲🇹 [mt](../../../mt/docs/guides/COST_TRACKING.md) · 🇲🇲 [my](../../../my/docs/guides/COST_TRACKING.md) · 🇳🇵 [ne](../../../ne/docs/guides/COST_TRACKING.md) · 🇳🇱 [nl](../../../nl/docs/guides/COST_TRACKING.md) · 🇳🇴 [no](../../../no/docs/guides/COST_TRACKING.md) · 🇮🇳 [or](../../../or/docs/guides/COST_TRACKING.md) · 🇮🇳 [pa](../../../pa/docs/guides/COST_TRACKING.md) · 🇵🇭 [phi](../../../phi/docs/guides/COST_TRACKING.md) · 🇵🇱 [pl](../../../pl/docs/guides/COST_TRACKING.md) · 🇵🇹 [pt](../../../pt/docs/guides/COST_TRACKING.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/COST_TRACKING.md) · 🇷🇴 [ro](../../../ro/docs/guides/COST_TRACKING.md) · 🇷🇺 [ru](../../../ru/docs/guides/COST_TRACKING.md) · 🇱🇰 [si](../../../si/docs/guides/COST_TRACKING.md) · 🇸🇰 [sk](../../../sk/docs/guides/COST_TRACKING.md) · 🇸🇮 [sl](../../../sl/docs/guides/COST_TRACKING.md) · 🇷🇸 [sr](../../../sr/docs/guides/COST_TRACKING.md) · 🇸🇪 [sv](../../../sv/docs/guides/COST_TRACKING.md) · 🇰🇪 [sw](../../../sw/docs/guides/COST_TRACKING.md) · 🇮🇳 [ta](../../../ta/docs/guides/COST_TRACKING.md) · 🇮🇳 [te](../../../te/docs/guides/COST_TRACKING.md) · 🇹🇭 [th](../../../th/docs/guides/COST_TRACKING.md) · 🇹🇷 [tr](../../../tr/docs/guides/COST_TRACKING.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/COST_TRACKING.md) · 🇵🇰 [ur](../../../ur/docs/guides/COST_TRACKING.md) · 🇺🇿 [uz](../../../uz/docs/guides/COST_TRACKING.md) · 🇻🇳 [vi](../../../vi/docs/guides/COST_TRACKING.md) · 🇳🇬 [yo](../../../yo/docs/guides/COST_TRACKING.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/COST_TRACKING.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/COST_TRACKING.md)

---

Come OmniRoute stima, registra e riporta il costo di ogni richiesta — e perché il
numero nella dashboard è un **indicatore dei risparmi**, non una fattura.

Vedi anche: [Guida utente](./USER_GUIDE.md) · [Galleria delle funzionalità](./FEATURES.md)

---

## Che cos'è (e che cosa non è)

OmniRoute attribuisce a ogni completamento un costo in USD per richiesta, moltiplicando il
numero di token per le tariffe del modello. Questi valori alimentano la dashboard **Costi**,
i comandi CLI `omniroute cost` / `omniroute usage`, le esportazioni CSV/JSON e i budget
per chiave API.

> **Il "costo" nella dashboard è un indicatore dei risparmi, non una fattura.** OmniRoute
> non ti addebita mai nulla: instrada le tue richieste verso provider che hai già collegato
> (i tuoi abbonamenti, piani gratuiti e chiavi API). Un "costo totale di $290" maturato
> interamente su modelli gratuiti indica che, approssimativamente, **non hai pagato $290**
> a un'API a pagamento. La cifra è una _stima_ di quanto sarebbe costato lo stesso traffico
> ai prezzi di listino standard, così puoi vedere dove si concentra il tuo utilizzo e quanto
> stai risparmiando grazie all'instradamento verso provider più economici o gratuiti.

Questa interpretazione è dichiarata direttamente nel [README](../../README.md) del progetto
("il 'costo' nella dashboard è un indicatore dei risparmi, non una fattura").

Poiché il valore è una stima:

- Dipende dalla tabella dei prezzi di cui OmniRoute dispone per ciascun modello. Un modello
  senza una voce di prezzo contribuisce con un costo pari a `0` (nell'esploratore viene
  visualizzato come riga "Legacy / Free").
- Il traffico incluso in piani gratuiti e abbonamenti genera comunque un costo _stimato_:
  si tratta dell'importo che stai risparmiando, non di un importo dovuto.

---

## Come vengono stimati i costi

### La fonte dei prezzi

I costi provengono da una tabella dei prezzi determinata secondo il seguente ordine di
precedenza ([`src/lib/pricingSync.ts`](../../src/lib/pricingSync.ts)):

1. **Sostituzioni dell'utente** — i prezzi impostati nella dashboard / tramite `PATCH /api/pricing`.
2. **Prezzi esterni sincronizzati** — recuperati dal file pubblico
   `model_prices_and_context_window.json` di LiteLLM quando la sincronizzazione è abilitata
   (memorizzati in uno spazio dei nomi `pricing_synced` separato, in modo da non sovrascrivere
   mai le tue sostituzioni).
3. **Valori predefiniti codificati** — inclusi in OmniRoute.

La sincronizzazione dei prezzi esterni è **facoltativa** e disabilitata per impostazione
predefinita. Variabili d'ambiente pertinenti
(vedi [`.env.example`](../../.env.example)):

| Variabile d'ambiente    | Valore predefinito | Scopo                                                                                     |
| ----------------------- | ------------------ | ----------------------------------------------------------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`            | Abilita la sincronizzazione in background dei prezzi LiteLLM all'avvio.                   |
| `PRICING_SYNC_INTERVAL` | `86400`            | Intervallo di sincronizzazione in **secondi** (giornaliero per impostazione predefinita). |
| `PRICING_SYNC_SOURCES`  | `litellm`          | Elenco di fonti separate da virgole (attualmente è supportato solo `litellm`).            |

### La formula del costo

Il costo viene calcolato per ogni richiesta a partire dal numero di token e dalle tariffe
per milione di token definite in
[`src/lib/usage/costCalculator.ts`](../../src/lib/usage/costCalculator.ts)
(`computeCostFromPricing` / `calculateCost`):

- **Token di input** (escluse le letture dalla cache e i token di creazione della cache) × tariffa `input`.
- **Token letti dalla cache** × tariffa `cached` (in mancanza, viene usata la tariffa di input).
- **Token di creazione della cache** × tariffa `cache_creation` (in mancanza, viene usata la tariffa di input).
- **Token di output** × tariffa `output`.
- **Token di ragionamento** × tariffa `reasoning` (in mancanza, viene usata la tariffa di output).

Tutte le tariffe sono interpretate come USD per 1.000.000 di token. Un livello di servizio
Codex "fast"/"priority" o "flex" applica un moltiplicatore di costo
(`getCodexFastCostMultiplier`): ad esempio, con flex i token vengono fatturati con uno
sconto del 50%, mostrato nella dashboard come **risparmio flex**.

I nomi dei modelli vengono prima normalizzati (i prefissi del percorso del provider, come
`openai/` o `accounts/fireworks/models/`, vengono rimossi), così anche le righe storiche
continuano a corrispondere a un prezzo.

### Come viene registrata la spesa

- Il costo per richiesta viene calcolato dopo la risposta e registrato in modalità
  fire-and-forget, così da non aggiungere mai latenza per il client. Il consumo della quota
  condivisa viene pianificato al tick successivo dell'event loop tramite
  [`src/lib/quota/spendRecorder.ts`](../../src/lib/quota/spendRecorder.ts).
- La spesa per chiave API viene memorizzata in un buffer e salvata in batch da
  [`SpendBatchWriter`](../../src/lib/spend/batchWriter.ts) (intervallo di salvataggio
  predefinito di 60 secondi, buffer da 1.000 voci). È configurabile tramite:

  | Variabile d'ambiente                | Valore predefinito | Scopo                                                    |
  | ----------------------------------- | ------------------ | -------------------------------------------------------- |
  | `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` | `60000`            | Intervallo di salvataggio in millisecondi.               |
  | `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`   | `1000`             | Numero massimo di voci nel buffer prima del salvataggio. |

Le cifre relative ai costi nella dashboard **non** vengono lette da un importo in dollari
memorizzato per ogni riga: vengono ricalcolate dinamicamente a partire dal numero di token
e dalla tabella dei prezzi corrente ogni volta che viene eseguito l'endpoint di analisi.
Ciò significa che la correzione di un prezzo errato (e una nuova sincronizzazione)
aggiorna retroattivamente le stime dei costi storici.

---

## Dashboard: la pagina Costi

La pagina **Costi** si trova in `/dashboard/costs`
(`src/app/(dashboard)/dashboard/costs/`).
La sua vista principale è la scheda **Panoramica dei costi**
(`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`),
che carica tutti i dati da `GET /api/usage/analytics`.

Cosa mostra:

- **Riquadri di spesa** — spesa stimata per _Oggi (1g)_, _7g_, _30g_ e per
  l'intervallo selezionato. Selettore dell'intervallo: `7d`, `30d`, `90d`, `all`.
- **Metriche principali** — richieste nell'intervallo, provider attivi, modelli attivi,
  costo medio per richiesta.
- **Esplora costi** — una tabella ordinabile e filtrabile, raggruppata per **provider**,
  **modello**, **chiave API**, **account** o **livello di servizio**, con costo,
  richieste, token, costo medio per richiesta e percentuale sul totale.
- **Utilizzo dei token** — token totali / di input / di output e rapporto input:output.
- **Efficienza dell'instradamento** — numero di fallback, tasso di fallback e copertura
  del modello richiesto.
- **Previsione mensile** — proietta la spesa di fine mese a partire dalla media
  giornaliera recente.
- **Confronto tra periodi** — variazione percentuale tra la prima e la seconda metà
  dell'intervallo.
- **Grafici** — andamento giornaliero dei costi, quota per provider (grafico a torta),
  provider principali, modelli principali, costo per chiave API, costo per account,
  andamento settimanale dell'utilizzo e una mappa di calore delle attività.
- **Esportazione** — scarica l'intervallo corrente in formato **CSV** o **JSON** (i
  pulsanti vengono visualizzati quando sono presenti dati di costo diversi da zero).

Quando non è presente traffico con tariffazione, le righe mostrano l'etichetta
"Legacy / Gratuito" invece di `$0`, in linea con il modello di monitoraggio dei risparmi.

### Sottopagine correlate dei Costi

L'area Costi include inoltre (tutte sotto `/dashboard/costs/`):

- **Prezzi** (`/dashboard/costs/pricing`) — visualizza e sostituisce i prezzi per
  modello (mostra la scheda Prezzi condivisa).
- **Budget** (`/dashboard/costs/budget`) — imposta limiti di spesa per ambito (mostra
  la scheda Budget condivisa).
- **Condivisione quota** (`/dashboard/costs/quota-share`) — pool di quote condivise e
  viste del tasso di consumo.

---

## Endpoint API

Tutti questi endpoint richiedono l'autenticazione di gestione (loopback/JWT, tramite
`requireManagementAuth`), salvo dove diversamente indicato.

### Analisi di utilizzo e costi

| Metodo | Endpoint                 | Scopo                                                                                                                                                                                  |
| ------ | ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/analytics`   | Analisi complete di costi/utilizzo: riepilogo, andamento giornaliero, per provider/modello/chiave API/account/livello. Query: `range`, `startDate`, `endDate`, `apiKeyIds`, `presets`. |
| `GET`  | `/api/usage/utilization` | Utilizzo nel tempo della quota per provider. Query: `range` (`1h`/`24h`/`7d`/`30d`), `provider`.                                                                                       |
| `GET`  | `/api/usage/history`     | Righe della cronologia di utilizzo non elaborate.                                                                                                                                      |
| `GET`  | `/api/usage/call-logs`   | Log delle chiamate per richiesta (modello, token, costo, latenza, stato).                                                                                                              |
| `GET`  | `/api/usage/quota`       | Stato della quota del provider.                                                                                                                                                        |
| `GET`  | `/api/usage/proxy-logs`  | Log delle richieste proxy.                                                                                                                                                             |

### Budget

| Metodo | Endpoint                 | Scopo                                                                                                      |
| ------ | ------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/budget`      | Riepilogo dei costi + verifica del budget per una chiave API (parametro di query `apiKeyId` obbligatorio). |
| `POST` | `/api/usage/budget`      | Imposta limiti giornalieri/settimanali/mensili in USD + soglia di avviso per una chiave API.               |
| `GET`  | `/api/usage/budget/bulk` | Riepiloghi aggregati dei budget tra più chiavi API.                                                        |

> L'API del budget è circoscritta per **chiave API** (`apiKeyId`). I limiti restituiti da
> `GET /api/usage/budget` includono `dailyLimitUsd`, `weeklyLimitUsd`, `monthlyLimitUsd`,
> una `warningThreshold` e i totali progressivi (`totalCostToday`, `totalCostMonth`, …).

### Prezzi

| Metodo   | Endpoint                | Scopo                                                                                                                    |
| -------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `GET`    | `/api/pricing`          | Prezzi combinati correnti (utente + sincronizzati + predefiniti). `?includeSources=1` per vedere l'origine di ogni voce. |
| `PATCH`  | `/api/pricing`          | Sostituisce i prezzi per `{ provider: { model: { input, output, cached, … } } }`.                                        |
| `DELETE` | `/api/pricing`          | Ripristina i prezzi predefiniti (facoltativamente limitato tramite `?provider=&model=`).                                 |
| `GET`    | `/api/pricing/defaults` | Mostra le tariffe di fallback predefinite per 1 milione.                                                                 |
| `GET`    | `/api/pricing/models`   | Prezzi indicizzati per modello.                                                                                          |
| `POST`   | `/api/pricing/sync`     | Avvia una sincronizzazione manuale da fonti esterne (LiteLLM).                                                           |
| `GET`    | `/api/pricing/sync`     | Stato corrente della sincronizzazione.                                                                                   |
| `DELETE` | `/api/pricing/sync`     | Cancella tutti i dati dei prezzi sincronizzati.                                                                          |

### Altri endpoint rilevanti per i costi

| Metodo | Endpoint                      | Scopo                                                                                     |
| ------ | ----------------------------- | ----------------------------------------------------------------------------------------- |
| `GET`  | `/api/free-tier/summary`      | Totali dei token del modello gratuito, utilizzo mensile e disponibilità gratuita residua. |
| `GET`  | `/api/quota/pools/[id]/usage` | Utilizzo per un pool di quota condivisa.                                                  |

---

## CLI

La CLI di OmniRoute espone comandi per costi, utilizzo e prezzi (registrati in
[`bin/cli/commands/registry.mjs`](../../bin/cli/commands/registry.mjs)).

### `omniroute cost`

Un report dei costi aggregato da `/api/usage/analytics`.

```bash
omniroute cost                          # ultimi 30 giorni, raggruppati per provider
omniroute cost --period 7d              # ultimi 7 giorni
omniroute cost --group-by model         # raggruppa per provider | model | combo | api-key | day
omniroute cost --since 2026-06-01 --until 2026-06-13
omniroute cost --api-key <key> --limit 50
```

Colonne: gruppo, richieste, token in entrata/uscita, costo (USD) e percentuale del totale. Alla fine
viene visualizzata una riga con il totale complessivo (omessa con `--quiet` o `--output json`).

### `omniroute usage`

```bash
omniroute usage analytics --period 30d [--provider <id>]   # riepilogo dei costi per provider
omniroute usage logs [--limit 100] [--follow] [--api-key <k>] [--search <q>]
omniroute usage quota [--provider <id>] [--check]
omniroute usage utilization [--api-key <k>]
omniroute usage history [--limit 100]
omniroute usage proxy-logs [--limit 100]

# Budget
omniroute usage budget list
omniroute usage budget get [scope]
omniroute usage budget set <amount> [--scope global] [--period monthly]
omniroute usage budget reset [scope]
```

### `omniroute pricing`

```bash
omniroute pricing list [--provider <p>] [--model <m>] [--limit 200]
omniroute pricing get <model>
omniroute pricing sync [--provider <p>] [--force]   # POST /api/pricing/sync
omniroute pricing diff [--model <m>]
omniroute pricing defaults show
omniroute pricing defaults set [--input <p>] [--output <p>] [--cache-read <p>] [--cache-write <p>]
```

> `pricing defaults show` legge `GET /api/pricing/defaults`. Per modificare invece i prezzi dei singoli modelli,
> utilizza la pagina **Pricing** della dashboard o `PATCH /api/pricing`.

---

## Risoluzione dei problemi

- **Tutti i costi risultano pari a $0 / "Legacy / Free".** I modelli in uso non hanno una voce di prezzo.
  Abilita la sincronizzazione esterna (`PRICING_SYNC_ENABLED=true`) ed esegui `omniroute pricing sync`, oppure
  imposta manualmente i prezzi tramite la pagina Pricing / `PATCH /api/pricing`.
- **Il prezzo di un modello storico è errato.** Correggi il prezzo (tramite override o nuova sincronizzazione): il costo viene
  ricalcolato a partire dal conteggio dei token a ogni lettura dei dati analitici, quindi le stime vengono aggiornate retroattivamente.
- **La spesa è in ritardo rispetto al tempo reale.** La spesa per chiave viene elaborata in batch; riduci
  `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` se hai bisogno di dati più aggiornati.

---

Per capire come si inserisce nel contesto più ampio della dashboard, consulta la [Guida utente](./USER_GUIDE.md) e
la [Galleria delle funzionalità](./FEATURES.md).
