# Feature Flags (Русский)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇮🇷 [fa](../../../fa/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> Переключатели среды выполнения, которые изменяют поведение OmniRoute **без повторного развертывания**.
> Каждый перечисленный здесь флаг определен в
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> — единственном источнике истины. Панель управления и REST API считывают данные
> из этого файла, поэтому приведенная ниже таблица сформирована в точном соответствии с ним.

---

## Что такое флаги функций

Флаг функции — это именованный переключатель (логический или перечисляемый), значение которого можно изменять
во время выполнения и сохранять в базе данных без необходимости повторного развертывания процесса. Каждый
флаг описывается объектом `FeatureFlagDefinition` с полями `key`, `label`,
`description`, `category`, `defaultValue`, `type` и подсказкой `requiresRestart`.

### Порядок определения значения

**Эффективное значение** флага определяется функцией
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) со следующим
приоритетом (побеждает значение с наивысшим приоритетом):

1. **Переопределение в БД** — значение, хранящееся в таблице `key_value` в пространстве имен
   `feature_flags` (задается через панель управления или REST API).
2. **Переменная окружения** — `process.env[<KEY>]`, если она задана и не пуста.
3. **Значение по умолчанию из определения** — `defaultValue` из `featureFlagDefinitions.ts`.

Логический флаг считается **включенным**, если его эффективное значение равно `"true"`,
`"1"` или `"yes"` (см. `isFeatureFlagEnabled()`).

> [!NOTE]
> Большинство флагов также имеют соответствующую переменную окружения **с тем же именем**,
> задокументированную в [`ENVIRONMENT.md`](./ENVIRONMENT.md). Переопределение флага
> в БД имеет приоритет над этой переменной окружения. Флаг с
> `requiresRestart: true` сохраняется немедленно, но повторно считывается только при запуске
> процесса — его переключение приводит к появлению баннера **«Перезапустить сервер»** в панели управления.

---

## Каталог флагов

72 флага в 6 категориях. **По умолчанию** — значение по умолчанию из определения,
используемое, когда отсутствуют как переопределение в БД, так и переменная окружения.

### Безопасность (10)

| Ключ                                    | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                                                                             |
| --------------------------------------- | ------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | boolean | `false`      | Требовать API-ключ для всех входящих запросов.                                                                                                                                                                                                                                                                       |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`       | Включить очистку входных данных для всех запросов.                                                                                                                                                                                                                                                                   |
| `INJECTION_GUARD_MODE`                  | enum    | `off`        | Режим защиты от инъекций в промпты. Значения: `off`, `warn`, `block`, `redact`.                                                                                                                                                                                                                                      |
| `PII_REDACTION_ENABLED`                 | boolean | `false`      | Удалять персональные данные из запросов (независимо от `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                                                      |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`      | Очищать ответы провайдеров от персональных данных.                                                                                                                                                                                                                                                                   |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact`     | Режим очистки ответов от персональных данных. Значения: `redact`, `warn`, `block`, `off`.                                                                                                                                                                                                                            |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`       | Блокировать исходящие запросы к частным/внутренним диапазонам IP-адресов.                                                                                                                                                                                                                                            |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`      | Разрешить аутентифицированным пользователям панели управления просматривать сохраненные API-ключи целиком вместо отображения только замаскированных значений.                                                                                                                                                        |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`      | Включать префикс учетной записи в строки журнала AUTH (например, «Используется учетная запись <provider>: abc12345...»). По умолчанию отключено, чтобы идентификаторы учетных записей скрывались в общих/мультитенантных журналах процесса. Не зависит от режима отладки; его переключение не раскрывает эти данные. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`      | Когда OIDC включен, отключить вход по паролю, чтобы пользователи могли проходить аутентификацию только через единый вход OIDC. Когда параметр отключен (по умолчанию), доступны как вход по паролю, так и OIDC.                                                                                                      |

### Сеть (15)

| Ключ                                            | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------- | ------- | ------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false`      | ✓          | Включить скрытый режим цифрового отпечатка TLS.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false`      |            | Разрешить маршрутам /v1/audio/* использовать совместимые с OpenAI узлы провайдеров, размещённые вне localhost. По умолчанию отключено — маршрутизация аудио на удалённый хост изменяет исходящую сетевую идентичность и должна быть явным решением оператора. Узлы loopback разрешены всегда, и эта настройка на них не влияет.                                                                                                                                                                                                                                                                                                   |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false`      |            | Если соединению не назначен прокси, автоматически выбирать первый работоспособный прокси из реестра. По умолчанию отключено (иначе любой прокси из реестра становится глобальным резервным вариантом — #3332).                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false`      |            | Разрешить потокам OAuth и проверки провайдера обходить закреплённый прокси и подключаться напрямую, если предварительные проверки доступности прокси завершаются неудачно. По умолчанию отключено, поскольку это может изменить исходящий IP-адрес.                                                                                                                                                                                                                                                                                                                                                                               |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`       |            | При сетевой ошибке (тайм-аут, отказ/сброс соединения) в исполнителе ротации нескольких учётных записей, если у учётной записи, вызвавшей ошибку, нет выделенного прокси, применять короткий период ожидания и пропускать другие учётные записи без прокси до конца запроса вместо повторной попытки для каждой из них. По умолчанию включено (безопасно: исходящий IP-адрес не меняется; лишь снижается риск задержек и периодов ожидания для учётных записей с общим исходящим подключением). Отключите, чтобы восстановить немедленное распространение первой ошибки от учётной записи без прокси.                              |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false`      |            | Пулы прокси и ротация opencode для каждой учётной записи перестают повторно предоставлять прокси, который только что завершился ошибкой (отклонённая TCP-проверка или полученный через него ответ 429), на период в рамках процесса, удваивающийся при каждом повторении вплоть до предельного значения. Статус прокси не записывается; если отложены все кандидаты, выбор остаётся неизменным. По умолчанию отключено.                                                                                                                                                                                                           |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false`      |            | Показывать на панели мониторинга под пулом прокси, сколько наблюдаемых исходящих IP-адресов обслуживало его участников за последние 24 ч и сколько соединений их использовало. Данные доступны только для чтения, вычисляются из журнала прокси и никогда не используются для маршрутизации. По умолчанию отключено.                                                                                                                                                                                                                                                                                                              |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false`      |            | Для исполнителя OpenCode отслеживать первый байт тела потокового ответа Responses (окно: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`, по умолчанию `15000`). Поток Responses с кодом 2xx, не передавший данные в течение этого окна, считается зависшим: для учётной записи активируется период ожидания, а запрос однократно перенаправляется на следующую учётную запись; повторное зависание приводит к немедленной ошибке. По умолчанию отключено: зависшие потоки продолжают ожидать в соответствии с текущим тайм-аутом готовности потока.                                                                                            |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false`      |            | Исполнитель OpenCode: при ответе 403/451 с отказом `user_blocked` (не связанным с географическим ограничением или отклонением цифрового отпечатка Cloudflare) активировать для отклонённой учётной записи период ожидания и не более одного раза за запрос переключаться на следующую учётную запись; повторный отказ возвращается без изменений и без отметки об успехе. По умолчанию отключено: обход блокировки пользователя на стороне вышестоящего сервиса может выглядеть как уклонение и распространить отметку на весь парк учётных записей.                                                                              |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false`      |            | Ротация OpenCode: после двух последовательных временных сбоев вышестоящего сервиса (5xx или 400 с пустым телом) приостанавливать обработку перед переходом к следующему аккаунту — 1,5 с с удвоением после каждого последующего сбоя, но не более 6 с на паузу и 10 с на запрос; пауза пропускается при отключении клиента. Тело неудачного ответа освобождается до начала ожидания. По умолчанию отключено: переключение при сбое выполняется немедленно.                                                                                                                                                                        |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false`      |            | Ротация OpenCode: останавливать цикл обхода аккаунтов при первом ответе 429, классифицированном как фактическое ограничение частоты запросов (разбираемый `Retry-After` или тело ответа с упоминанием ограничения частоты запросов либо использования), и возвращать этот ответ 429 от вышестоящего сервиса без изменений. При неклассифицированных ответах 429 ротация продолжается. По умолчанию отключено: бесплатный тариф ограничен для каждого исходящего IP-адреса (#9611), поэтому при каждом ответе 429 выполняется ротация, а после исчерпания всех аккаунтов возвращается последний ответ 429 от вышестоящего сервиса. |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false`      | ✓          | Отключить проверку TLS-сертификатов для MITM-прокси. **Опасно.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false`      |            | Разрешить URL-адреса провайдеров, указывающие на частные/внутренние сети.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`       |            | Разрешить добавление и проверку провайдеров по локальным/частным адресам (127.0.0.1, localhost, LAN). Включено по умолчанию (приоритет локальных ресурсов); отключите для строгой блокировки всех адресов, кроме общедоступных. Доступ к метаданным облачной среды остаётся заблокированным.                                                                                                                                                                                                                                                                                                                                      |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false`      | ✓          | Включить режим провайдера, совместимого с Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### Политики (5)

| Ключ                            | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                                                      |
| ------------------------------- | ------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled`   | Режим применения политики использования инструментов. Значения: `disabled`, `warn`, `block`.                                                                                                                                                                                                  |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`      | Автоматически включать ограничение частоты запросов на основе шаблонов использования.                                                                                                                                                                                                         |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`      | Пропускать локальную проверку OmniRoute размера контекстного окна / максимального количества входных токенов для прямых запросов к одной модели. Ограничения вышестоящего сервиса по-прежнему применяются.                                                                                    |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`      | Отклонять запросы до их отправки, если целевая модель не поддерживает необходимые возможности (обработку изображений, инструменты, структурированный вывод, требуемое контекстное окно). Защищает прямые запросы к одному провайдеру, которые обходят фильтр совместимости уровня комбинаций. |
| `RADAR_ENABLED`                 | boolean | `false`      | Включить модуль OmniRoute Radar (экраны ленты каталога и синхронизации). По умолчанию отключено; включение лишь открывает доступ к пользовательскому интерфейсу — синхронизация данных по-прежнему требует отдельного явного согласия.                                                        |

### Среда выполнения (32)

| Ключ                                        | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------- | ------- | ------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | boolean | `true`       |            | Создавать и внедрять сводки разговоров, когда комбинированная маршрутизация переключает модели. Отключите, чтобы обрабатывать переключения моделей независимо и предотвратить фоновые запросы передачи контекста для всех существующих и будущих комбинаций.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | boolean | `true`       |            | Удалять внутренние элементы вывода этапа комментариев из сквозных потоков Responses API перед их передачей клиентам. Отключите, чтобы получать необработанные комментарии от вышестоящего сервиса.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | boolean | `true`       |            | Применять ограничения областей доступа к инструментам MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | boolean | `false`      |            | Сжимать описания инструментов MCP, чтобы сократить использование токенов.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | boolean | `false`      |            | Включить обработку фоновых задач во время выполнения.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | boolean | `false`      | ✓          | Отключить все фоновые службы (обновление квоты, синхронизацию и т. д.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false`      |            | Доверять фильтрам RTK на уровне проекта без проверки.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`       | ✓          | Запускать WebSocket-сервер панели мониторинга в реальном времени при импорте (по умолчанию порт 20132).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`       |            | Разрешить Codex использовать транспорт Responses-over-WebSocket. Если отключено, Codex переключается на HTTP Responses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`       |            | Разрешить Codex использовать локальный транспорт app-server WebSocket JSON-RPC (codexTransport=app-server). Если отключено, соединения, настроенные на использование app-server, переключаются на другие транспорты Codex.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`       |            | Направлять запросы с исчерпанным бюджетом к аварийному бесплатному резервному провайдеру/модели. (См. раздел [Аварийный резерв при исчерпании бюджета](#emergency-budget-fallback) ниже.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false`      |            | Включить прозрачную раннюю повторную попытку для усечённых вышестоящих потоков SSE до того, как какие-либо байты ответа достигнут клиента.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false`      |            | Разрешить восстановлению потока повторно запрашивать и объединять ответ после того, как байты уже достигли клиента.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false`      |            | Обеспечить безопасность вызовов инструментов при продолжении в середине потока: никогда не возобновлять прерванный поток после отправки вызова инструмента (выполняющегося или уже завершённого с finish_reason tool_calls) и завершать работу после одного пустого продолжения вместо расходования всего бюджета. Если отключено: поведение версии релиза.                                                                                                                                                                                                                                                                                                    |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false`      |            | Однократно переключаться на параллельное соединение, если поток SSE закрывается до отправки какого-либо полезного кадра и лимит повторных попыток через то же соединение исчерпан; если подходящего параллельного соединения нет, возвращается исходная ошибка `STREAM_EARLY_EOF` 502. По умолчанию отключено: после повторной попытки через то же соединение ранний EOF остаётся окончательной ошибкой.                                                                                                                                                                                                                                                       |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`       |            | Включать удобные для отображения поля имён в ответы `/v1/models`. Отключите для клиентов, ожидающих только идентификаторы моделей.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`       |            | Управляет добавлением префиксов к идентификаторам моделей в /v1/models. Значение 'dual' (по умолчанию) выдаёт префиксы как псевдонима, так и канонического идентификатора провайдера для обратной совместимости. Значение 'alias' выдаёт только короткий префикс псевдонима (например, ds-web/model, а не deepseek-web/model). Значение 'canonical' выдаёт только полный префикс идентификатора провайдера. Значения: `dual`, `alias`, `canonical`.                                                                                                                                                                                                            |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`       |            | Включить периодическую синхронизацию рейтинга ELO из таблицы лидеров Arena AI для ранжирования моделей по интеллектуальным возможностям.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false`      |            | Публиковать идентификаторы зеркал `claude/<provider>/<model>` в `/v1/models`, чтобы при обнаружении моделей шлюза Claude Code отображались модели, отличные от Claude. Глобальный уровень трёхуровневого механизма управления (переменная среды имеет приоритет над переопределением в панели управления). См. [Конфигурация Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker).                                                                                                                                                                                                            |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`       |            | Главный переключатель псевдонимов шлюза no-think/<provider>/<model>. Включено (по умолчанию): /v1/models публикует вариант без рассуждений для каждой подходящей модели Claude с поддержкой рассуждений, а идентификатор no-think/, переданный в запросе, преобразуется обратно в реальную модель с отключёнными рассуждениями. Выключено: варианты не публикуются, а идентификатор no-think/ обрабатывается как любой другой неизвестный идентификатор модели. Пока этот переключатель включён, по-прежнему учитывается индивидуальное для каждой модели включение или отключение ModelSpec.noThinkingAlias.                                                  |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false`      |            | Отключить создание вариантов уровней рассуждений (например, -low, -medium, -high) в каталоге /v1/models.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false`      | ✓          | Включить адаптивные виртуальные полосы допуска для каждого арендатора при отправке запросов провайдеру (#9654): всплеск запросов одного арендатора больше не приводит к ошибкам 503 у другого. Переменная среды OMNIROUTE_CHAT_VIRTUAL_LANES имеет приоритет над этим переопределением в панели управления; изменения вступают в силу после перезапуска сервера.                                                                                                                                                                                                                                                                                               |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false`      |            | Публиковать идентификаторы зеркал <gateway-alias>/<model> в /v1/models для моделей, у канонического владельца которых нет активных учётных данных, но они маршрутизируются через сквозной шлюз с активными учётными данными. Предупреждение: при глобальном включении добавляет записи каталога для всех клиентов.                                                                                                                                                                                                                                                                                                                                             |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false`      |            | Включить определение баланса для узлов-агрегаторов, совместимых с New-API / One-API / Sub2API. Если параметр включён, совместимые узлы с установленным флагом агрегатора будут показывать свой баланс в панели управления и использовать его при предварительной проверке квоты маршрутизации.                                                                                                                                                                                                                                                                                                                                                                 |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false`      |            | Продолжать принадлежащие серверу нев потоковые вызовы инструментов, пока модель не вернёт ответ, пригодный для использования клиентом.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false`      |            | В статистике поиска и недавних поисковых запросах учитывать только провайдеров, у которых всё ещё есть активное подключение (провайдеры без ключей, такие как duckduckgo-free, учитываются всегда). В выключенном состоянии сохраняется прежнее поведение: учитывается каждая сохранённая строка поиска с идентификатором провайдера.                                                                                                                                                                                                                                                                                                                          |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false`      |            | Страницы провайдеров в панели управления: показывать значок «Бесплатно» только при наличии признаков, которые поддерживает провайдер, — не учитывать эвристику по отображаемому имени, небулевы бесплатные поля и суффиксы :free у зарегистрированных провайдеров без документированного бесплатного тарифа. В выключенном состоянии сохраняется прежнее правило отображения значка.                                                                                                                                                                                                                                                                           |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false`      |            | В агрегированных ответах 429/503 о недоступности не указывать `Retry-After`, если конкретное будущее время повторной попытки неизвестно (вместо синтетического значения 1s), добавлять `error.retry_after_provenance` (`signal` \| `none`) и разрешать путям опустошения комбинаций считывать текстовые подсказки о повторной попытке из JSON и обычных текстовых тел ответов вышестоящих сервисов. Поле появляется только в ответах, созданных функцией `unavailableResponse()`; остальные тела ответов 429/503 не изменяются.                                                                                                                                |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false`      |            | Если цель комбинации `priority`, помеченная как резервная только при исчерпании квоты, останавливает комбинацию по причине, которая заведомо не связана с квотой (разомкнутый автоматический выключатель провайдера, прогнозируемый пропуск из-за задержки), возвращать 502 вместо похожего на ошибку квоты кода 503. Остановки из-за блокировки, периода ожидания, недоступности, исчерпания и ограничения параллелизма продолжают возвращать 503.                                                                                                                                                                                                            |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false`      |            | Простой ответ Mistral 401 (`{"detail":"Unauthorized"}`, без явного сигнала об ошибке аутентификации) выглядит одинаково как для отозванного ключа, так и для исчерпанной квоты. Если параметр включён, соединение переводится в период ожидания вместо пометки `expired`, но не более 3 раз в час для каждого соединения; при следующем таком ответе оно помечается как `expired`, поэтому состояние отозванного ключа всё равно в итоге определяется корректно. По умолчанию выключено: каждый простой ответ Mistral 401 помечает соединение как `expired`, как и раньше.                                                                                     |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false`      |            | Получать актуальный каталог моделей xAI для соединений `xai-oauth` с `https://api.x.ai/v1/models`, используя токен OAuth Bearer, вместо зафиксированного статического исходного списка. По умолчанию выключено: `xai-oauth` продолжает предоставлять статический исходный список без изменений. При любой ошибке разрешения обнаружение моделей возвращается к исходному списку (не проверено, принимает ли x.ai токен OAuth Bearer в этой конечной точке).                                                                                                                                                                                                    |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false`      |            | Разрешить автоматической очистке удалять задания Batch API в конечном состоянии (завершённые/неудачные/отменённые/истёкшие), возраст которых превышает `OMNIROUTE_BATCH_RETENTION_DAYS`, вместе с их контрольными точками для каждой строки, а также очищать содержимое BLOB загруженных файлов после наступления их собственного `expires_at`. По умолчанию выключено: каждая существующая установка продолжает хранить эти данные без изменений, пока оператор явно не включит эту функцию. Маршрут `DELETE /api/v1/batches/delete-completed`, запускаемый оператором, не зависит от этой настройки — это отдельный безусловный контракт общедоступного API. |

### CLI (5)

| Ключ                                  | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                             |
| ------------------------------------- | ------- | ------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `CLI_COMPAT_ALL`                      | boolean | `false`      | ✓          | Включить режим совместимости для всех клиентов CLI.                                                                                                                                                                                                                                  |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false`      |            | Включить слой совместимости псевдонимов моделей.                                                                                                                                                                                                                                     |
| `PRICING_SYNC_ENABLED`                | boolean | `false`      |            | Включить автоматическую синхронизацию данных о ценах (также требуется переменная окружения `PRICING_SYNC_ENABLED`).                                                                                                                                                                  |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false`      |            | После синхронизации моделей провайдера автоматически (пере)записывать файлы профилей ~/.codex/*.config.toml на основе актуального каталога. Активная конфигурация Codex или конфигурация Codex по умолчанию никогда не изменяется. По умолчанию выключено.                           |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false`      |            | После синхронизации моделей провайдера автоматически (пере)записывать профили Claude Code в ~/.claude/profiles/<name>/settings.json на основе актуального каталога. Активная конфигурация Claude или конфигурация Claude по умолчанию никогда не изменяется. По умолчанию выключено. |

### Работоспособность (5)

| Ключ                                      | Тип     | Значение по умолчанию | Описание                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------- | ------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false`               | Отключает конечную точку проверки работоспособности локального экземпляра.                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false`               | Отключает проверку работоспособности для валидации токена.                                                                                                                                                                                                                                                                                      |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false`               | Включает доступ к сети в изолированной среде навыков.                                                                                                                                                                                                                                                                                           |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false`               | При проверке работоспособности прокси отказ целевого сервера на запрос проверки (401/403/429) сбрасывает счётчик последовательных сбоев прокси. По умолчанию отключено: отказ остаётся нейтральным (#10654). Ответ 5xx в любом случае остаётся неопределённым; отказ никогда не приводит к удалению, отключению или повторной активации прокси. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false`               | Запускает проверку целостности и работоспособности БД при запуске после того, как сервер начнёт принимать запросы (через `setImmediate`), вместо блокировки запуска до завершения проверки (#13717). По умолчанию отключено: запуск блокируется точно так же, как и до этого PR.                                                                |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` и его устаревший псевдоним
> `INJECTION_GUARD_BLOCK_THRESHOLD` настраивают режим `block` параметра
> `INJECTION_GUARD_MODE`, однако это обычные переменные окружения, считываемые
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts),
> а не флаги функций: для них нельзя задать переопределение в БД, и у них нет переключателя на панели управления. См.
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> Столбец `Restart` отмечает флаги с `requiresRestart: true` — значение
> сохраняется мгновенно, но вступает в силу только после перезапуска процесса. Флаги
> перечислений отклоняют любые значения, не входящие в разрешённый набор (проверка выполняется на стороне сервера
> как в `setFeatureFlagOverride()`, так и в обработчике REST `PUT`).

---

## Переключение флагов

### Панель управления

Перейдите в раздел **Панель управления → Настройки → Флаги функций**
(`/dashboard/settings/feature-flags`). Таблица
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
поддерживает:

- **Поиск** по ключу или описанию и **фильтрацию** по категории (а также синтетическое
  представление **Требуется перезапуск**).
- **Переключатель** для логических флагов и **выпадающий список** для флагов-перечислений
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- **Значок источника** для каждого флага — `DB`, `ENV` или `DEF`, — показывающий, откуда
  было получено фактическое значение.
- Кнопку **Сбросить** (отображается только для флагов с источником `DB`) для удаления переопределения,
  а также кнопку **Сбросить все переопределения** в нижней части.
- Баннер **Перезапустить сервер**, когда изменяется флаг `requiresRestart`.

### REST API

Все операции выполняются через единый маршрут:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
Для каждого метода требуется аутентифицированный сеанс панели управления (в противном случае возвращается `401`).

#### `GET /api/settings/feature-flags`

Возвращает каждый флаг с его фактическим значением, источником и сводной информацией.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Require API Key",
      "description": "Require an API key for all incoming requests",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... все 72 флага
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

Задаёт или удаляет отдельное переопределение. Тело: `{ key: string; value?: string }`.
Если `value` не указано, переопределение удаляется (восстанавливается значение из переменной окружения / значение по умолчанию).

```bash
# Задать переопределение в БД
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# Удалить переопределение (без "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

Ответ содержит новые значения `effectiveValue`/`source`, предыдущие значения `previousValue`/
`previousSource` и `requiresRestart`. Неизвестные ключи и значения перечислений вне допустимого
диапазона отклоняются с кодом `400`.

#### `DELETE /api/settings/feature-flags`

Удаляет **все** переопределения из БД одновременно, восстанавливая для каждого флага значение
из переменной окружения / значение по умолчанию. Возвращает `{ cleared: <count>, message: "..." }`.

> [!NOTE]
> Флаги с `requiresRestart: true` начинают действовать только после перезапуска процесса.
> Процедура перезапуска из панели управления вызывает `POST /api/restart`, а затем опрашивает
> `GET /api/health/ping`, пока сервер снова не станет доступен.

---

## Аварийный резерв при исчерпании бюджета

`OMNIROUTE_EMERGENCY_FALLBACK` (категория `runtime`, значение по умолчанию `true`) управляет
аварийным путём бесплатного резервирования в
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts).
Когда он включён, запросы, исчерпавшие свой бюджет, направляются к бесплатному резервному
провайдеру/модели вместо немедленного завершения с ошибкой. Установите значение `false` (или `0`) — через
переключатель на панели управления, переопределение в БД или переменную окружения
`OMNIROUTE_EMERGENCY_FALLBACK` — чтобы отключить это поведение и позволить запросам с исчерпанным
бюджетом завершаться с ошибкой. (Добавлено как переключатель на панели управления в PR #3741 / #3752.)

---

## См. также

- [Справочник переменных окружения](./ENVIRONMENT.md) — для большинства флагов
  там задокументирована одноимённая переменная окружения (переопределение в БД
  имеет над ней приоритет).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — единый источник достоверной информации обо всех флагах.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — логика разрешения (`resolveFeatureFlag`, `isFeatureFlagEnabled`,
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — хранение
  переопределений БД в пространстве имён `feature_flags` таблицы `key_value`.
