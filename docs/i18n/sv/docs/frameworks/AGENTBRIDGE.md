# AgentBridge (Svenska)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENTBRIDGE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENTBRIDGE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENTBRIDGE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENTBRIDGE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENTBRIDGE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENTBRIDGE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/AGENTBRIDGE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENTBRIDGE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENTBRIDGE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENTBRIDGE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENTBRIDGE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENTBRIDGE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENTBRIDGE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENTBRIDGE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENTBRIDGE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENTBRIDGE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENTBRIDGE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENTBRIDGE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENTBRIDGE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENTBRIDGE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENTBRIDGE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENTBRIDGE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENTBRIDGE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENTBRIDGE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENTBRIDGE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENTBRIDGE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENTBRIDGE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENTBRIDGE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENTBRIDGE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENTBRIDGE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENTBRIDGE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENTBRIDGE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENTBRIDGE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENTBRIDGE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENTBRIDGE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENTBRIDGE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENTBRIDGE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENTBRIDGE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENTBRIDGE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENTBRIDGE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENTBRIDGE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENTBRIDGE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENTBRIDGE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENTBRIDGE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENTBRIDGE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENTBRIDGE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENTBRIDGE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENTBRIDGE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENTBRIDGE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENTBRIDGE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENTBRIDGE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENTBRIDGE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENTBRIDGE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENTBRIDGE.md)

---

AgentBridge är OmniRoutes MITM-proxy (Man-in-the-Middle) som avlyssnar HTTPS-trafik från AI-agenter i IDE:er och omdirigerar den genom OmniRoutes enhetliga routningsmotor. Den stöder **10 IDE-agenter** — Antigravity, Kiro, GitHub Copilot, GHE Copilot, OpenAI Codex, Cursor, Zed, Claude Code, Open Code och Trae (under utredning) — vilket gör OmniRoute till den MITM-proxy för AI-kodningsassistenter som har bredast täckning på marknaden.

**Plats i instrumentpanelen:** `/dashboard/tools/agent-bridge`
**Grupp i sidofältet:** Verktyg (efter Molnagenter)
**Se även:** [`TRAFFIC_INSPECTOR.md`](./TRAFFIC_INSPECTOR.md) — övervaka all avlyssnad trafik i realtid; `docs/security/MITM-TPROXY-DECRYPT.md` (git; kompileras inte till `/docs`) — Linux-baserat TPROXY-läge för transparent dekrypteringsinsamling som styrs via routen `/api/tools/agent-bridge/tproxy`.

---

## §1 Översikt

### Vad är AgentBridge?

När en IDE-agent (t.ex. GitHub Copilot, Cursor eller Claude Code) gör ett API-anrop ansluter den direkt till den överordnade AI-leverantören (OpenAI, Anthropic osv.). AgentBridge avlyssnar anslutningen transparent på TLS-nivå — utan att agentens konfiguration behöver ändras — och skriver om begäran så att den går genom OmniRoute.

Det innebär att du kan:

- **Omdirigera valfri agent till valfri leverantör**: Kommunicerar Copilot med OpenAI? Omdirigera den till Anthropic Claude, Gemini eller någon av OmniRoutes 352 leverantörer.
- **Tillämpa modellmappningar**: `gemini-3-flash` → `claude-sonnet-4.7` transparent på hanterarnivå.
- **Observera all agenttrafik**: varje avlyssnad begäran publiceras i [Traffic Inspector](./TRAFFIC_INSPECTOR.md).
- **Tillämpa OmniRoutes motståndskraftsfunktioner**: kombinationsroutning, kretsbrytare, reservlösningar och kostnadsspårning fungerar även för trafik från IDE-agenter.

### Positionering jämfört med marknaden

| Funktion                      | 9router | anti-api | llm-interceptor | **OmniRoute AgentBridge** |
| ----------------------------- | :-----: | :------: | :-------------: | :-----------------------: |
| Antigravity                   |    ✓    |    ✓     |        —        |             ✓             |
| GitHub Copilot                |    ✓    |    ✓     |        —        |             ✓             |
| Kiro (AWS)                    |    ✓    |    ✓     |        —        |             ✓             |
| OpenAI Codex                  |    —    |    ✓     |        —        |             ✓             |
| Cursor IDE                    |    ✓    |    ✓     |        —        |             ✓             |
| Zed Industries                |    —    |    ✓     |        —        |             ✓             |
| Claude Code                   |    —    |    —     |        ✓        |             ✓             |
| Open Code                     |    —    |    —     |        ✓        |             ✓             |
| Trae                          |    —    |    —     |        —        |    🔍 Under utredning     |
| Instrumentpanel               |    ✓    |    ✗     |        ✗        |             ✓             |
| Trafikinspektör               |    ✗    |    ✗     |        ✓        |             ✓             |
| OmniRoute-routning            |    ✗    |    ✗     |        ✗        |             ✓             |
| Gränssnitt för modellmappning |    ✗    |    ✗     |        ✗        |             ✓             |
| Förbikopplingslista           |    ✗    |    ✗     |        ✓        |             ✓             |
| Överordnat CA-certifikat      |    ✗    |    ✗     |        ✓        |             ✓             |

---

## §2 Arkitektur

### 2.1 Komponentöversikt

```
IDE-agent (VS Code / Cursor / osv.)
    │  HTTPS (port 443)
    ▼
/etc/hosts — 127.0.0.1 api.githubcopilot.com   ← DNS-omdirigering
    │
    ▼
src/mitm/server.cjs  (port 443, CJS-underprocess)
    │  matchar målet utifrån SNI i Host-headern
    │  genererar TLS-certifikat per SNI, signerat av AgentBridge CA
    ├── Matchning i undantagslistan? → TCP-vidarekoppling (ingen dekryptering)
    ├── Målmatchning? → fetch → OmniRoute-router (port 20128)
    │       └── handler.intercept() — TypeScript
    │               ├── maskSecrets() på begärans body/headers
    │               ├── TrafficBuffer.push() — publicerar till Traffic Inspector
    │               └── fetchRouter() → /v1/chat/completions
    └── Ingen matchning? → TCP-vidarekoppling (ingen dekryptering)
```

### 2.2 MITM-server (`src/mitm/server.cjs`)

MITM-kärnservern körs som en Node.js CJS-underprocess (för att undvika att skriva om den befintliga CJS-kodbasen). Den:

- Lyssnar på port 443 (kräver behörighet eller `authbind`/`setcap`)
- Tar emot CONNECT-tunnlar från operativsystemet (via DNS-omdirigering i `/etc/hosts`)
- Genererar TLS-certifikat per SNI, signerade av AgentBridge CA (`DATA_DIR/mitm/ca.crt`)
- Matchar målagenten utifrån Host-headern via registret `targets/index.ts`
- Vidarebefordrar till TypeScript-hanterarlagret via HTTP till `http://127.0.0.1:20128`

`TARGET_HOSTS` läses in från `DATA_DIR/mitm/targets.json` (som skrivs av `targets/index.ts` vid start), vilket möjliggör dynamiska uppdateringar utan att CJS-servern behöver startas om.

> **Rot-CA-modell (#6684).** Beskrivningen ovan med certifikat per SNI, signerat av en CA,
> är den beständiga rot-CA-modell som lades till i #6684 (`src/mitm/cert/rootCa.ts` +
> `src/mitm/_internal/rootCaShim.cjs`, som återanvänder CA-/slutcertifikatskryptografin som redan
> verifierats för TPROXY i `src/mitm/tproxy/dynamicCert.ts`) — den ersätter det
> äldre, enskilda statiska självsignerade slutcertifikatet (`src/mitm/cert/generate.ts`, fortfarande
> begränsat till endast antigravity-värdarna) som indikeras av ett ensamt `server.crt`/`server.key`-par
> på disk. **Migreringsbeteende**: en ny installation (utan tidigare
> `server.crt`) får rot-CA-modellen automatiskt; en installation som redan
> litade på det gamla statiska slutcertifikatet fortsätter att använda det tills operatören anger
> `MITM_ROOT_CA_ENABLED=true` och startar om bryggan (`src/mitm/cert/migration.ts`
> är den rena beslutsfunktionen — en betrodd MITM-CA som kan signera ett slutcertifikat för
> **vilken** värd som helst är väsentligt kraftfullare än det gamla slutcertifikatet med fast SAN, så
> bytet sker aldrig i det tysta för en installation som redan är betrodd). CA-certifikatet installeras
> i samma `omniroute-mitm.crt`-plats i betroddhetsarkivet som det gamla slutcertifikatet använde
> (`cert/install.ts::installCaCert`) — ingen rensning av dubbla betrodda certifikat behövs.

### 2.3 Bashanterare (`src/mitm/handlers/base.ts`)

Alla agenthanterare utökar `MitmHandlerBase`:

```ts
export abstract class MitmHandlerBase {
  abstract readonly agentId: AgentId;

  abstract intercept(
    req: IncomingMessage,
    res: ServerResponse,
    body: Buffer,
    mappedModel: string
  ): Promise<void>;

  // Skyddade hjälpmetoder: fetchRouter, pipeSSE, hookBufferStart, hookBufferUpdate
}
```

Varje hanterare anropar `hookBufferStart()` före proxyförmedlingen och `hookBufferUpdate()` när den är klar. Dessa skickar `InterceptedRequest`-poster till `globalTrafficBuffer` (se [Traffic Inspector](./TRAFFIC_INSPECTOR.md) §4).

### 2.4 Målregister (`src/mitm/targets/`)

Varje agent har en deklarativ målfil:

```ts
// src/mitm/targets/copilot.ts
export const COPILOT_TARGET: MitmTarget = {
  id: "copilot",
  name: "GitHub Copilot",
  hosts: ["api.githubcopilot.com", "copilot-proxy.githubusercontent.com"],
  port: 443,
  endpointPatterns: ["/chat/completions", "/v1/chat/completions"],
  defaultModels: [{ id: "gpt-4o", name: "GPT-4o", alias: "gpt-4o" }],
  handler: () => import("../handlers/copilot"),
  riskNoticeKey: "providers.riskNotice.oauth",
};
```

Registret (`targets/index.ts`) exporterar `ALL_TARGETS` och genererar `DATA_DIR/mitm/targets.json` vid start.

### 2.5 Vidarekoppling och undantagslista (`src/mitm/passthrough.ts`)

**Undantagslista** (kontrolleras först och har företräde framför målmatchning):

- Standardmönster: bankvärdar, `.gov.`, OAuth-/SSO-leverantörer (Okta, Auth0) osv.
- Användarmönster: lagras i databastabellen `agent_bridge_bypass`
- Undantagna värdar får en transparent TCP-tunnel — TLS dekrypteras **aldrig**

**Standardvidarekoppling** (ingen målmatchning och inte med i undantagslistan):

- Får också en TCP-tunnel — anslutningar bryts aldrig
- Förhindrar att AgentBridge stör allmän HTTPS-trafik i systemet

Prioritetsordning för routning:

```
undantagslista → målmatchning → vidarekoppling
```

### 2.6 Uppströms CA-certifikat (`src/mitm/upstreamTrust.ts`)

För företagsnätverksmiljöer med en anpassad CA:

```bash
AGENTBRIDGE_UPSTREAM_CA_CERT=/path/to/corporate-ca.pem
```

När den är angiven konfigureras `undici`:s globala dispatcher med det extra CA-certifikatet, vilket gör att AgentBridge kan nå uppströmsleverantörer genom företagsproxyservrar för TLS-terminering.

### 2.7 Maskering av hemligheter (`src/mitm/maskSecrets.ts`)

Den oberoende clean-room-skannern tillämpas på begärandekroppar och autentiseringsheaders
**innan** de hamnar i Traffic Inspectors buffert eller någon logg. Den utför en enda linjär genomgång:

- Token med prefixen `sk-` / `ak-` / `pk-` (OpenAI-/Anthropic-stil)
- RFC 6750-autentiseringsuppgifter av typen `Authorization: Bearer <token>`, där hela token har företräde
- Generiska, långa, ogenomskinliga token (≥40 tecken), inklusive punktseparerade och utfyllda former

`sanitizeHeaders()` konverterar bibehållna namn till gemener, sammanfogar arrayvärden deterministiskt, tar bort den
delade blockeringslistan för hopp-för-hopp-/ramindelningsheaders (inklusive proxyautentisering), maskerar `cookie` och
`set-cookie` fullständigt och delegerar autentiseringsvärden till skannern.

---

## §3 Konfiguration

### 3.1 Starta/stoppa MITM-servern

Använd AgentBridge-serverkortet på `/dashboard/tools/agent-bridge`:

| Åtgärd               | Beskrivning                                                                          |
| -------------------- | ------------------------------------------------------------------------------------ |
| Starta server        | Startar `src/mitm/server.cjs` på port 443                                            |
| Stoppa server        | Stänger ned underprocessen kontrollerat                                              |
| Starta om server     | Stoppar + startar (läser in ändringar av mål)                                        |
| Betro certifikat     | Installerar `DATA_DIR/mitm/ca.crt` i operativsystemets certifikatarkiv               |
| Ladda ned certifikat | Laddar ned `ca.crt` för manuell installation                                         |
| Återskapa certifikat | Skapar ett nytt CA-nyckelpar (alla befintliga certifikat per agent ogiltigförklaras) |

### 3.2 Betro certifikatet

AgentBridge-CA-certifikatet måste vara betrott av operativsystemet innan IDE:er accepterar MITM-anslutningen.

**Linux (NSS — Chrome/Firefox):**

```bash
certutil -A -d sql:$HOME/.pki/nssdb -n "OmniRoute AgentBridge" -t CT,, -i ~/.omniroute/mitm/ca.crt
```

**macOS (Nyckelhanterare):**

```bash
sudo security add-trusted-cert -d -r trustRoot \
  -k /Library/Keychains/System.keychain ~/.omniroute/mitm/ca.crt
```

**Windows (certmgr):**

```powershell
certutil -addstore -f Root $env:USERPROFILE\.omniroute\mitm\ca.crt
```

Du kan också använda knappen "Betro certifikat" på instrumentpanelen (den kör lämpligt kommando för ditt operativsystem och visar en sudo-prompt vid behov).

#### Electron-baserade IDE:er ignorerar operativsystemets certifikatarkiv (`NODE_EXTRA_CA_CERTS`)

Vissa IDE:er — i synnerhet **Antigravity IDE** och andra appar baserade på Electron/VS Code — paketerar
sin egen Node.js-körmiljö som **inte använder operativsystemets certifikatarkiv** för utgående
`fetch`/HTTPS. Att betro CA:n på OS-/NSS-nivå räcker för IDE:ns inbyggda **backend**
(t.ex. en Go-språkserver som använder operativsystemets CA-paket), men **Electron-frontenddelen**
kommer fortfarande att misslyckas med TLS — detta visar sig genom att appen verkar vara _utloggad_
eller visar ett _"anslutningsfel"_, trots att MITM-loggen visar att backendens initieringsanrop returnerar `200`. Två steg
krävs, och båda är viktiga:

1. Peka uttryckligen körmiljön mot CA:n:
   ```bash
   export NODE_EXTRA_CA_CERTS=/path/to/omniroute-agentbridge-ca.crt
   ```
2. **Starta IDE:n från det skalet.** Om du startar den via skrivbordsikonen, Dock eller Start-menyn
   ärvs **inte** skalets exporterade variabler, och `~/.config/environment.d/*.conf` börjar gälla först efter
   en ny grafisk inloggning. Avsluta först IDE:n helt — Electrons singleton-lås innebär att en andra
   start bara fokuserar den befintliga processen och att den nya miljön ignoreras.

Steget ovan med operativsystemets certifikatarkiv + NSS är fortfarande nödvändigt (Chromium-nätverksstacken som används av vissa autentiseringsflöden
läser användarens NSS-arkiv och har egna statiska pinnade certifikat för `*.googleapis.com` som en
lokalt betrodd CA åsidosätter). `NODE_EXTRA_CA_CERTS` täcker dessutom Nodes `fetch`-sökväg.

### 3.3 DNS-dirigering

För varje agent som du vill avlyssna måste dess API-värdnamn matcha `127.0.0.1`. AgentBridge hanterar automatiskt poster i `/etc/hosts` när du aktiverar eller inaktiverar DNS för en agent i konfigurationsguiden.

Exempel på poster i `/etc/hosts` för GitHub Copilot:

```
127.0.0.1 api.githubcopilot.com
127.0.0.1 copilot-proxy.githubusercontent.com
```

### 3.4 Modellmappning

Använd modellmappningstabellen på varje agentkort för att definiera mappningar från källa → mål:

| Källmodell (agentens inbyggda) | Målmodell (OmniRoute) |
| ------------------------------ | --------------------- |
| `gpt-4o`                       | `claude-sonnet-4.7`   |
| `*` (jokertecken)              | `claude-haiku-4.7`    |

Jokertecknet `*` mappar alla okända modeller till det angivna målet. Sparas i tabellen `agent_bridge_mappings`.

> **Tips — identifiera agentens verkliga modell-ID:n.** En IDE kan skicka modellnamn som skiljer sig från
> dess etiketter i användargränssnittet och som ändras mellan större versioner. **Antigravity 2** skickar till exempel
> `gemini-3.1-pro-low`, `gemini-pro-agent` och `gemini-3.1-flash-lite` över nätverket — inte
> `gemini-2.5-pro` som visas i äldre dokumentation. Skicka ett chattmeddelande utan någon matchande mappning: MITM
> loggar det exakta inkommande värdet för `model:` och vidarebefordrar begäran. Mappa det exakta värdet, så
> avlyssnas nästa begäran och dirigeras till ditt mål.

### 3.5 Riskmeddelande

AgentBridge avlyssnar autentiseringsuppgifter (OAuth-token, API-nycklar) som IDE:n använder för att autentisera sig hos externa leverantörer. Dessa **maskeras före loggning** (se §2.7), men är synliga för OmniRoutes MITM-lager. Vid den första aktiveringen av varje agent visas en modal riskvarning som kan stängas.

### 3.6 Underhåll och diagnostik

Instrumentpanelen innehåller ett kort för **Underhåll och diagnostik** (`AgentBridgeMaintenanceCard`, i `src/app/(dashboard)/dashboard/tools/agent-bridge/components/`) som visar operativa MITM-rutter som tidigare saknade användargränssnitt. Dess underrubrik: _"Självtesta insamlingsflödet, återställ kvarvarande systemtillstånd och flytta konfigurationen mellan datorer."_ Kortets klienthjälpfunktioner finns i `src/lib/inspector/agentBridgeMaintenanceApi.ts`.

| Knapp                       | Rutt                                   | Vad den gör                                                                                                                                                                                                |
| --------------------------- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Diagnostisera**           | `GET /api/tools/agent-bridge/diagnose` | Kör självtestet för insamlingspipelinen och visar en rapport för varje kontroll (✓/✗ + åtgärdstips).                                                                                                       |
| **Reparera**                | `POST /api/tools/agent-bridge/repair`  | Återställer kvarlämnat MITM-systemtillstånd (DNS-spoofposter, rot-CA, systemproxy) efter en krasch eller SIGKILL. Idempotent — rapporterar "Inget att reparera" när tillståndet är rent.                   |
| **Ta bort CA**              | `DELETE /api/tools/agent-bridge/cert`  | Tar bort förtroendet för och raderar MITM-rot-CA:n från operativsystemets betrodda certifikatarkiv (explicit, idempotent). Visas endast när CA:n är betrodd; kräver en infogad bekräftelse: "Ta bort CA?". |
| **Exportera konfiguration** | `GET /api/tools/agent-bridge/config`   | Hämtar den portabla konfigurationen som JSON (se §3.7).                                                                                                                                                    |
| **Importera konfiguration** | `POST /api/tools/agent-bridge/config`  | Laddar upp en tidigare exporterad konfiguration som JSON (se §3.7).                                                                                                                                        |

**Diagnostikkontroller** (`summarizeDiagnostics()` i `src/mitm/inspector/diagnostics.ts`). Rutten kör den effektfulla kontrollen för var och en och matar de booleska värdena till den rena sammanfattningsfunktionen; ett enda `healthy`-utlåtande samt ett tips för varje fel returneras:

| Kontrollnamn       | Vad den verifierar                                               | Tips vid fel                                                                                                                                                     |
| ------------------ | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server-running`   | MITM-serverprocessen är aktiv                                    | "MITM-servern körs inte. Starta den från fliken AgentBridge."                                                                                                    |
| `server-reachable` | MITM-servern accepterar anslutningar på sin port (TCP-kontroll)  | "MITM-servern accepterar inte anslutningar på sin port. Kontrollera att porten är ledig och att du har behörighet att binda den."                                |
| `cert-exists`      | MITM-certifikatet har genererats på disken                       | "Inget MITM-certifikat har genererats ännu. Generera ett från fliken AgentBridge."                                                                               |
| `cert-trusted`     | MITM-rot-CA:n finns i operativsystemets betrodda certifikatarkiv | "MITM-rot-CA:n är inte betrodd av operativsystemets certifikatarkiv, så TLS-avlyssning kommer att misslyckas. Gör certifikatet betrott från fliken AgentBridge." |
| `dns-configured`   | Målvärdnamnen är spoofade i `/etc/hosts`                         | "Målvärdnamnen är inte spoofade i /etc/hosts, så trafiken når aldrig proxyn. Aktivera DNS för den eller de agenter vars trafik du vill samla in."                |

**Banderoll för kvarlämnat tillstånd:** när sidan upptäcker tillstånd som lämnats kvar efter en krasch (DNS-spoofning/CA/systemproxy) visar kortet en bärnstensfärgad banderoll — _"En tidigare session lämnade kvar systemtillstånd (DNS-spoofning, CA eller systemproxy). Kör Reparera för att rensa det."_ — och framhäver knappen **Reparera**. `Repair` är motsvarigheten till ProxyBridges flagga `--cleanup` på applikationsnivå (den delegerar till `repairMitm()` i `src/mitm/manager.ts`).

> MITM-rot-CA:n förblir installerad mellan stopp och start för att undvika upprepade sudo-
> uppmaningar (samma beteende som mitmproxy/Charles), så borttagningen är en explicit
> **Ta bort CA**-åtgärd i stället för något som sker automatiskt vid stopp.

### 3.7 Import/export av portabel konfiguration

AgentBridge kan serialisera det **operatörsjusterbara** tillståndet till ett versionshanterat JSON-objekt så att en konfiguration kan replikeras mellan datorer. Serialiseraren är `src/lib/inspector/configPortability.ts` (`exportConfig()`/`importConfig()`), validerad av `AgentBridgeConfigSchema`.

Exporten innehåller exakt tre delar (inbyggda standardvärden exporteras avsiktligt **INTE**, så importen duplicerar dem aldrig eller orsakar konflikter med dem):

| Fält             | Källa                                                             | Anmärkningar                                                             |
| ---------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `bypassPatterns` | användardefinierade förbikopplingsmönster (`agent_bridge_bypass`) | standardmönster för bank/gov/okta exkluderas                             |
| `customHosts`    | anpassade värdar i Traffic Inspector (`inspector_custom_hosts`)   | varje post: `{ host, kind: "llm"\|"app"\|"custom", label? }`             |
| `agentMappings`  | modellsamband per agent (`agent_bridge_mappings`)                 | `{ [agentId]: [{ source, target }] }` för varje agent som har mappningar |

```jsonc
// GET /api/tools/agent-bridge/config
{
  "version": 1,
  "bypassPatterns": ["*.internal.example.com"],
  "customHosts": [{ "host": "api.example.com", "kind": "llm", "label": null }],
  "agentMappings": {
    "copilot": [{ "source": "gpt-4o", "target": "claude-sonnet-4.7" }],
  },
}
```

**Importbeteende** (`POST /api/tools/agent-bridge/config`): förbikopplingsmönster och mappningar per agent **ersätts i sin helhet**; anpassade värdar läggs till **idempotent** (`INSERT OR IGNORE`). Svaret rapporterar hur många av varje som tillämpades:

```jsonc
{ "ok": true, "bypassPatterns": 1, "customHosts": 1, "agents": 1 }
```

Det som **INTE** finns i konfigurationen: serverns körningstillstånd, certifikatsökvägar, DNS-tillstånd per agent, sökväg till överordnad CA och TPROXY-inställningar — dessa är värd-/körningstillstånd, inte portabla inställningar.

---

## §4 Referens per agent

| #   | Agent              | Status       | Avlyssnade värdar                                                  | Autentiseringstyp |
| --- | ------------------ | ------------ | ------------------------------------------------------------------ | ----------------- |
| 1   | **Antigravity**    | ✅ Stöds     | `daily-cloudcode-pa.googleapis.com`, `cloudcode-pa.googleapis.com` | Firebase OAuth    |
| 2   | **Kiro (AWS)**     | ✅ Stöds     | `prod.kiro.aws`, `dev.kiro.aws`                                    | AWS SigV4         |
| 3   | **GitHub Copilot** | ✅ Stöds     | `api.githubcopilot.com`, `copilot-proxy.githubusercontent.com`     | GitHub OAuth      |
| 4   | **OpenAI Codex**   | ✅ Stöds     | `api.openai.com` (Codex-sökvägar), `chatgpt.com`                   | OpenAI-nyckel     |
| 5   | **Cursor IDE**     | ✅ Stöds     | `api2.cursor.sh`, `api.cursor.sh`                                  | Cursor OAuth      |
| 6   | **Zed Industries** | ✅ Stöds     | `api.zed.dev`, `llm.zed.dev`                                       | Zed OAuth         |
| 7   | **Claude Code**    | ✅ Stöds     | `api.anthropic.com` (valfritt)                                     | Anthropic-nyckel  |
| 8   | **Open Code**      | ✅ Stöds     | `openrouter.ai`, `api.openai.com` (zen-sökvägar)                   | API-nyckel        |
| 9   | **Trae**           | 🔍 Undersöks | Fastställs senare — se §8                                          | Fastställs senare |

### Steg i installationsguiden (per agent)

Varje agentkort har en installationsguide i tre steg:

1. **Verifiera förutsättningarna** — Körs servern? Är certifikatet betrott? Är IDE:n installerad (identifieras automatiskt)?
2. **Aktivera DNS** — Lägger till poster i `/etc/hosts` (kräver sudo). Visar exakt vilka rader som kommer att läggas till.
3. **Mappa modeller** — Valfri tabell för modellmappning. Jokertecken accepteras.

### Agentidentifiering

För agenterna 1–8 försöker AgentBridge automatiskt identifiera IDE-installationen:

```ts
export async function detectAgent(agentId: AgentId): Promise<DetectionResult>;
// Returnerar: { installed: boolean, version?: string, path?: string }
```

Identifieringen använder operativsystemspecifika sökvägar och kontroller av binärfiler (t.ex. `code --list-extensions | grep github.copilot` för Copilot, `~/.config/antigravity/` för Antigravity).

---

## §5 Säkerhet

### Tillämpade strikta regler

| Regel                             | Tillämpning                                                                                  |
| --------------------------------- | -------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`    | Alla hanterarfel saneras före svar eller post i bufferten                                    |
| **#13** Överföring via skalmiljö  | Ändringar i `/etc/hosts` använder alternativet `env` — ingen stränginterpolering av sökvägar |
| **#15 + #17** `isLocalOnlyPath()` | `/api/tools/agent-bridge/` är LOCAL_ONLY + SPAWN_CAPABLE — loopback krävs före autentisering |

### Förbikopplingslista för känsliga värdar

Förbikopplingslistan säkerställer att finansinstitut, OAuth-/SSO-leverantörer och andra känsliga värdar **aldrig dekrypteras**. Deras TLS-trafik passerar genom en transparent TCP-tunnel — OmniRoute ser aldrig klartexten.

Standardmönstren för förbikoppling omfattar:

- `*.bank.*`, `*.gov.*` (finans/myndigheter)
- `*.okta.com`, `*.auth0.com`, `*.microsoft.com` (SSO/identitet)
- `*.apple.com`, `*.icloud.com` (Apples systemtjänster)

Förbikopplingsmönster som användaren lägger till lagras i tabellen `agent_bridge_bypass` och har företräde framför allt annat.

### Maskering av hemligheter

`maskSecrets()` från `src/mitm/maskSecrets.ts` tillämpas:

- På varje begärandetext före `TrafficBuffer.push()`
- På varje rubrik före loggning eller utsändning

Mönster: token med prefixet `sk-`/`ak-`/`pk-`, `Bearer`-token och generiska token med minst 40 tecken.

### Uppströms CA-certifikat

När `AGENTBRIDGE_UPSTREAM_CA_CERT` har angetts läses filen vid start. Om sökvägen finns men filen inte kan läsas loggar AgentBridge ett tydligt fel och vägrar att starta (förhindrar tysta TLS-fel i företagsmiljöer).

### Kända begränsningar

- **Port 443 kräver behörighet**: På Linux behöver AgentBridge `setcap 'cap_net_bind_service=+ep'` på Node-binärfilen eller köras via `authbind`. Installationsguiden visar operativsystemspecifika instruktioner.
- **IDE:n måste startas om**: Efter DNS-omdirigeringen måste IDE:n startas om för att den nya värdnamnsupplösningen ska börja gälla.
- **Hårdkodade OAuth-token**: Vissa agenter (Kiro, Antigravity) lagrar OAuth-uppdateringstoken lokalt. Dessa är transparenta för AgentBridge — den ser `Bearer`-token i varje begäran, som maskeras före loggning.
- **Electron-gränssnitt behöver `NODE_EXTRA_CA_CERTS`**: IDE:er vars gränssnitt körs i en medföljande Node-/Electron-körmiljö ignorerar operativsystemets/NSS betrodda certifikatarkiv och måste startas från ett skal där `NODE_EXTRA_CA_CERTS` har angetts (se §3.2). Symptom när detta saknas: IDE:ns serverdel autentiseras (MITM visar `200`-svar), men användargränssnittet förblir utloggat.
- **Flera installationer av samma IDE är oberoende**: en systeminstallation (t.ex. `/usr/share/antigravity/antigravity`) och en användarlokal Full-installation (t.ex. `~/AntigravityIDE_Full/antigravity-ide`) är separata processer med egna körmiljöer — var och en måste startas om med CA-certifikatet injicerat. Identifiera vilken som körs via dess binärsökväg före omstart.
- **Identiteten anges av agentens systemprompt, inte den dirigerade modellen**: när du mappar om en agents modell till en annan leverantör hävdar svaret fortfarande agentens ursprungliga identitet (t.ex. svarar Antigravity ”I am powered by Gemini”) eftersom IDE:n infogar detta i systemprompten. Bekräfta den verkliga serverdelen i `call_logs` / `proxy_logs` (`provider`, `model`, `target_format`), inte genom att fråga modellen vem den är.

---

## §6 Felsökning

### Konflikt på port 443

Om en annan process redan lyssnar på port 443 (webbserver, VPN osv.):

```bash
lsof -i :443          # hitta processen
sudo fuser -k 443/tcp  # tvångsavsluta (använd med försiktighet)
```

Alternativt kan du konfigurera en icke-privilegierad port i AgentBridge-inställningarna och skapa omdirigeringsregler med `iptables` / `pf`.

### Certifikatet är inte betrott

Om IDE:n visar TLS-fel efter att AgentBridge har startats:

1. Kontrollera att certifikatet installerades: `security find-certificate -c "OmniRoute AgentBridge"` (macOS) eller `certutil -L -d sql:$HOME/.pki/nssdb` (Linux/NSS)
2. Vissa appar använder ett eget betrott certifikatarkiv (Firefox, Chrome på Linux). Kör "Trust Cert" igen och kontrollera det NSS-/Firefox-specifika certifikatarkivet.
3. Starta om IDE:n efter att certifikatet har gjorts betrott — pågående TLS-sessioner använder den tidigare betrodda statusen.

### IDE:n är utloggad / visar "anslutningsfel" trots en betrodd CA

Symptom: efter att DNS har omdirigerats och CA:n gjorts betrodd öppnas en Electron-baserad IDE (t.ex. Antigravity)
**utloggad** eller visar ett autentiserings-/anslutningsfel, trots att MITM-loggen visar att
bootstrap-anropen (`loadCodeAssist`, `fetchAvailableModels`, …) returnerar `200`.

Orsak: IDE:ns **medföljande Node-/Electron-körmiljö ignorerar operativsystemets betrodda certifikatarkiv**. Den inbyggda
backend-komponenten (en Go-språkserver) litar på operativsystemets CA och autentiserar sig, men Electron-gränssnittet
gör det inte — därför tror användargränssnittet att det är offline.

Lösning (båda stegen): exportera `NODE_EXTRA_CA_CERTS=<ca.crt>` **och starta om IDE:n från samma
skal**, inte från skrivbordsikonen. Avsluta först IDE:n helt — Electrons singleton-lås innebär att
en andra start bara fokuserar den befintliga processen och att den nya miljön ignoreras. Se §3.2.
Detta motsvarar en öppen rapport uppströms där en fristående agent fungerar via en MITM, men IDE-
varianten misslyckas med samma konfiguration.

### DNS-ändringen har inte slagit igenom

Kontrollera att `/etc/hosts` har uppdaterats:

```bash
grep "omniroute\|127.0.0.1.*github\|127.0.0.1.*cursor" /etc/hosts
```

Töm DNS-cachen:

```bash
# macOS
sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder
# Linux (systemd-resolved)
sudo systemctl restart systemd-resolved
# Windows
ipconfig /flushdns
```

### IDE:n identifieras inte

Automatisk identifiering använder vanliga installationssökvägar. Om identifieringen misslyckas trots att IDE:n är installerad:

- Kontrollera om IDE-binärfilen finns på en icke-standardiserad plats
- Installationsguiden fungerar ändå — misslyckad identifiering innebär bara att märket inte visar installationssökvägen

### Hanterarfel (hämtning uppströms misslyckas)

Om AgentBridge fångar upp förfrågningar men alla misslyckas:

1. Kontrollera att minst en leverantör är ansluten på `/dashboard/providers`
2. Kontrollera OmniRoute-serverloggarna: `APP_LOG_LEVEL=debug` i `.env`
3. Kontrollera att `OMNIROUTE_BASE_URL` pekar på rätt routerslutpunkt (standard: `http://127.0.0.1:20128`)

---

## §7 API-referens

Alla rutter är `LOCAL_ONLY` (endast loopback, framtvingas före autentisering) och `SPAWN_CAPABLE`. Se `src/server/authz/routeGuard.ts`.

Bassökväg: `/api/tools/agent-bridge/`

| Metod               | Sökväg                                         | Beskrivning                                                                                                                        |
| ------------------- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| GET                 | `/api/tools/agent-bridge/state`                | Global serverstatus + identifiering/status per agent                                                                               |
| GET                 | `/api/tools/agent-bridge/agents`               | Lista registrerade agenter (id, namn, värdar, användbarhet, status)                                                                |
| GET                 | `/api/tools/agent-bridge/agents/{id}`          | Status för en agent (målkonfiguration + identifiering + lagrad status)                                                             |
| PATCH               | `/api/tools/agent-bridge/agents/{id}`          | Uppdatera `setup_completed` för agenten                                                                                            |
| GET                 | `/api/tools/agent-bridge/agents/{id}/detect`   | Kör identifieringskontroll för agenten (`installed`, `version?`, `path?`)                                                          |
| POST                | `/api/tools/agent-bridge/agents/{id}/dns`      | Aktivera/inaktivera DNS för agenten (`{enabled: boolean}`)                                                                         |
| GET                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Modellmappningar för agenten                                                                                                       |
| PUT                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Ersätt modellmappningar                                                                                                            |
| POST                | `/api/tools/agent-bridge/server`               | Starta/stoppa/starta om servern (`action: "start"\|"stop"\|"restart"\|"trust-cert"\|"regenerate-cert"`)                            |
| GET                 | `/api/tools/agent-bridge/cert`                 | Certifikatstatus (`exists`, `trusted`, `path`)                                                                                     |
| POST                | `/api/tools/agent-bridge/cert`                 | Betro (installera) MITM-rotcertifikatutfärdaren                                                                                    |
| DELETE              | `/api/tools/agent-bridge/cert`                 | Sluta betro (ta bort) MITM-rotcertifikatutfärdaren — idempotent (se §3.6)                                                          |
| POST                | `/api/tools/agent-bridge/cert/regenerate`      | Generera om det självsignerade MITM-certifikatet                                                                                   |
| GET                 | `/api/tools/agent-bridge/cert/download`        | Strömma PEM-certifikatet för nedladdning                                                                                           |
| GET                 | `/api/tools/agent-bridge/bypass`               | Lista undantagsmönster (`default` + `user`)                                                                                        |
| POST                | `/api/tools/agent-bridge/bypass`               | Ersätt alla användardefinierade undantagsmönster                                                                                   |
| DELETE              | `/api/tools/agent-bridge/bypass?pattern=...`   | Ta bort ett enskilt användardefinierat undantagsmönster                                                                            |
| GET                 | `/api/tools/agent-bridge/diagnose`             | Självtest av insamlingspipelinen (se §3.6)                                                                                         |
| POST                | `/api/tools/agent-bridge/repair`               | Återställ kvarlämnat MITM-systemtillstånd (se §3.6)                                                                                |
| GET                 | `/api/tools/agent-bridge/config`               | Exportera portabel konfiguration som JSON (se §3.7)                                                                                |
| POST                | `/api/tools/agent-bridge/config`               | Importera portabel konfiguration som JSON (se §3.7)                                                                                |
| GET                 | `/api/tools/agent-bridge/upstream-ca`          | Hämta sökvägen till den konfigurerade överordnade certifikatutfärdaren                                                             |
| POST                | `/api/tools/agent-bridge/upstream-ca`          | Validera + spara sökvägen till den överordnade certifikatutfärdaren                                                                |
| POST                | `/api/tools/agent-bridge/upstream-ca/test`     | Validera endast (testkörning) en sökväg till en överordnad certifikatutfärdare — sparas inte                                       |
| GET / POST / DELETE | `/api/tools/agent-bridge/tproxy`               | TPROXY-insamlingsläge för transparent dekryptering — se `docs/security/MITM-TPROXY-DECRYPT.md` (git; kompileras inte till `/docs`) |

Fullständiga OpenAPI-scheman: `docs/openapi.yaml` → taggen `AgentBridge`.

---

## §8 Färdplan

### Undersökning av Trae

Trae är en relativt ny AI-kodningsassistent. Innan en hanterare implementeras:

1. Identifiera binärfilen/tillägget på marknadsplatserna för VS Code/JetBrains eller som en fristående app
2. Samla in trafik med mitmproxy för att upptäcka API-värdar och slutpunkternas struktur
3. Fastställ autentiseringsmekanismen
4. Bedöm om arbetet ska fortsätta eller avbrytas baserat på användarvillkoren och API:ets upptäckbarhet

Tills undersökningen är slutförd visar Trae-kortet på instrumentpanelen märket "Undersöks" med länken "Rapportera genomförbarhet". Hanterarstubben i `src/mitm/handlers/trae.ts` genererar ett strukturerat `Ännu inte implementerat`-fel.

### Agenter i backloggen (MITM krävs — inget stöd för anpassad bas-URL)

Följande verktyg stöder inte anpassade bas-URL:er i sina nuvarande versioner, vilket gör MITM till den enda vägen för avlyssning. Bedömning av genomförbarheten återstår:

- **Windsurf** (Codeium/Cognition)
- **Amp** (Sourcegraph)
- **Amazon Q / Kiro CLI** (AWS Bedrock — separat från Kiro IDE)
- **Cowork** (Anthropics skrivbordsapp)

Obs! GitHub Copilot CLI ≥v1.0.19 stöder `COPILOT_PROVIDER_BASE_URL` — använd direkt konfiguration i stället för MITM för det verktyget.
