# Codex CLI — Configuration with OmniRoute (Bahasa Melayu)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇦 [bs](../../../bs/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CODEX-CLI-CONFIGURATION.md)

---

Panduan lengkap untuk menggunakan Codex CLI yang dihalakan ke OmniRoute sebagai bahagian belakang serasi OpenAI.

---

> **TOML ialah satu-satunya format yang berkesan.** Codex moden membaca `~/.codex/config.toml`
> secara eksklusif (disahkan dengan codex-cli 0.147.0: `codex --help` mendokumenkan
> penggantian `-c/--config` yang "dimuatkan daripada `~/.codex/config.toml`"). Fail lama
> `~/.codex/config.yaml` adalah milik CLI npm legasi dan diabaikan secara senyap.
> Penjana papan pemuka (`/api/cli-tools/apply`, alat `codex`) menulis TOML dengan
> penggabungan konservatif — kekunci sedia ada dan blok penyedia lain dikekalkan,
> kekunci API kekal dalam `OMNIROUTE_API_KEY` (tidak pernah dalam fail), dan baki fail legasi
> `config.yaml` dilaporkan sebagai nota migrasi tanpa disentuh.

## config.toml yang sedia untuk ditampal

Gantikan `<YOUR_HOST>` dan `<YOUR_KEY>` dengan nilai anda:

```toml
# ~/.codex/config.toml
model                          = "cx/gpt-5.5"
model_provider                 = "omniroute"
model_reasoning_effort         = "xhigh"
model_context_window           = 400000
model_auto_compact_token_limit = 350000
tool_output_token_limit        = 32768    # had storan sejarah bagi setiap panggilan alat

[model_providers.omniroute]
name                 = "OmniRoute"
base_url             = "http://<YOUR_HOST>:20128/v1"
env_key              = "OMNIROUTE_API_KEY"
requires_openai_auth = false
wire_api             = "responses"
```

```bash
# ~/.bashrc atau ~/.zshrc — nilai kekunci sebenar, tidak pernah dalam config.toml
export OMNIROUTE_API_KEY="<YOUR_KEY>"
```

### macOS: Codex yang disertakan dalam aplikasi ChatGPT

Jika anda memasang Codex melalui aplikasi desktop ChatGPT, binari `codex` mungkin
hanya wujud dalam berkas aplikasi dan belum tersedia dalam `PATH` shell anda. Tambahkan
direktori sumber pada fail permulaan shell anda:

```bash
export PATH="/Applications/ChatGPT.app/Contents/Resources:$PATH"
```

Buka shell baharu, kemudian sahkan:

```bash
command -v codex
codex --version
```

### OmniRoute setempat tanpa pengesahan: kekunci pemegang tempat sudah memadai

Codex mengesahkan bahawa pemboleh ubah persekitaran yang dinamakan oleh `env_key` wujud
**sebelum** permintaan pertama meninggalkan CLI. Jika tika OmniRoute **setempat** anda
tidak memerlukan pengesahan, sebarang pemegang tempat yang tidak kosong boleh digunakan:

```bash
export OMNIROUTE_API_KEY="${OMNIROUTE_API_KEY:-local}"
```

Sebaliknya, gunakan kekunci sebenar apabila pelayan OmniRoute anda dilindungi atau berada di lokasi jauh.

> **Pilihan hos yang lazim**
>
> | Akses              | URL                           |
> | ------------------ | ----------------------------- |
> | Rangkaian setempat | `http://192.168.0.1:20128/v1` |
> | Tailscale          | `http://100.x.x.x:20128/v1`   |
> | Gelung balik       | `http://localhost:20128/v1`   |

---

## `wire_api = "responses"` — sebab ia berfungsi untuk semua model

Codex CLI menghentikan sokongan `wire_api = "chat"` (Chat Completions) pada Februari 2026 dan kini **memerlukan** `wire_api = "responses"` (OpenAI Responses API). Menetapkan `wire_api = "chat"` menyebabkan ranap serta-merta semasa permulaan sejak v0.138.

Banyak penyedia, termasuk GLM dan Kimi, masih hanya menyediakan titik akhir Chat Completions. DeepSeek V4 kini menyediakan Responses API natif serta titik akhir serasi Anthropic; OmniRoute menggunakan Responses secara lalai dan membolehkan setiap sambungan DeepSeek memilih keserasian Anthropic.

**OmniRoute menyelesaikan perkara ini secara telus:**

```
Codex CLI
  → wire_api = "responses"
  → POST /v1/responses (OmniRoute)
    → OmniRoute memilih protokol natif penyedia dan menterjemah apabila diperlukan
    → POST /responses (DeepSeek V4) atau /chat/completions (Mistral / GLM / Kimi / lain-lain)
```

Anda tidak memerlukan proksi terjemahan berasingan apabila menggunakan OmniRoute. **Semua model menggunakan `wire_api = "responses"`** — OmniRoute mengendalikan selebihnya.

> **`wire_api` ialah nilai lalai** — medan ini menggunakan `"responses"` secara lalai dan boleh ditinggalkan sepenuhnya daripada `config.toml`. Tetapkannya secara eksplisit hanya jika anda mendokumenkan tujuan penggunaannya.

---

## Tetingkap konteks dan pemadatan

### Medan konfigurasi token

| Medan                            | Penerangan                                                                                                                                                                                                  |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `model_context_window`           | Jumlah belanjawan token untuk model aktif. Tetapkan kepada had yang diiklankan bagi model tersebut.                                                                                                         |
| `model_auto_compact_token_limit` | Ambang yang mencetuskan pemadatan sejarah secara automatik. **Maksimum: 90% daripada `model_context_window`** — nilai melebihi 90% diabaikan secara senyap.                                                 |
| `tool_output_token_limit`        | Had token yang disimpan bagi setiap output panggilan alat dalam sejarah. Menghalang satu respons alat yang besar daripada memenuhi tetingkap. **Ini bukan output maksimum** — ini ialah had storan sejarah. |
| `compact_prompt`                 | Penggantian sebaris bagi gesaan sistem yang digunakan semasa pemadatan (v0.138+).                                                                                                                           |

> **Nota tentang `model_max_output_tokens`**: Medan ini **bukan sebahagian daripada skema konfigurasi Codex CLI** (tiada dalam pangkalan kod Codex Rust). Medan ini diabaikan secara senyap jika ditetapkan. Jangan bergantung padanya — gunakan `tool_output_token_limit` untuk mengawal jumlah output alat yang disimpan dalam sejarah.

### Tetingkap konteks mengikut model

| Model                                | ID OmniRoute                         | Tetingkap konteks        | `auto_compact` | `tool_output_limit` |
| ------------------------------------ | ------------------------------------ | ------------------------ | -------------- | ------------------- |
| GPT-5.5                              | `cx/gpt-5.5`                         | 400k andal (maksimum 1M) | 350,000        | 32,768              |
| Kimi K2.7 (penaakulan)               | `kmc/kimi-k2.7`                      | 131,072                  | 112,000        | 32,768              |
| Kimi K2.6                            | `kmc/kimi-k2.6`                      | 131,072                  | 112,000        | 32,768              |
| GLM-5.2 / 5.2-max (penaakulan)       | `glm/glm-5.2`                        | 131,072                  | 112,000        | 32,768              |
| MiMo V2.5 Pro (penaakulan)           | `opencode-go/mimo-v2.5-pro`          | 131,072                  | 112,000        | 32,768              |
| Qwen 3.7 Plus (penaakulan)           | `opencode-go/qwen3.7-plus`           | 32,768                   | 28,000         | 16,384              |
| DeepSeek V4 Pro (OllamaCloud)        | `ollamacloud/deepseek-v4-pro`        | 131,072                  | 112,000        | 32,768              |
| DeepSeek V4 Pro                      | `ds/deepseek-v4-pro`                 | 1,000,000                | 900,000        | 65,536              |
| MiMo V2.5                            | `opencode-go/mimo-v2.5`              | 131,072                  | 112,000        | 32,768              |
| Gemma 4 31B (OllamaCloud)            | `ollamacloud/gemma4:31b`             | 32,768                   | 28,000         | 16,384              |
| Nemotron 3 Super (OllamaCloud)       | `ollamacloud/nemotron-3-super`       | 32,768                   | 28,000         | 16,384              |
| GPT-OSS 20B (OllamaCloud)            | `ollamacloud/gpt-oss:20b`            | 32,768                   | 28,000         | 16,384              |
| DeepSeek V4 Flash (OllamaCloud)      | `ollamacloud/deepseek-v4-flash`      | 65,536                   | 56,000         | 16,384              |
| Gemini 3 Flash Preview (OllamaCloud) | `ollamacloud/gemini-3-flash-preview` | 1,000,000                | 850,000        | 32,768              |
| GLM-5 Turbo                          | `glm/glm-5-turbo`                    | 131,072                  | 112,000        | 16,384              |
| GLM-4.7 Flash                        | `glm/glm-4.7-flash`                  | 131,072                  | 112,000        | 16,384              |
| Mistral Large Latest                 | `mistral/mistral-large-latest`       | 262,144                  | 220,000        | 16,384              |

> **Formula pemadatan:** `effective_window = model_context_window - min(tool_output_token_limit, 20000)`. Nilai melebihi 20k tidak mengubah pencetus pemadatan.

> **Panduan umum:** tetapkan `model_auto_compact_token_limit` kepada 85–88% daripada `model_context_window`. Jangan sekali-kali melebihi 90% — ia akan diabaikan secara senyap.

---

## Awalan model: `cx/`

Semua model Codex dalam OmniRoute menggunakan awalan `cx/`:

| Nama Codex CLI          | Model OmniRoute    |
| ----------------------- | ------------------ |
| `cx/gpt-5.5`            | GPT-5.5 standard   |
| `cx/gpt-5.4`            | GPT-5.4 standard   |
| `cx/gpt-5.4-mini`       | GPT-5.4 mini       |
| `cx/gpt-5.1-codex-mini` | GPT-5.1 Codex mini |

Penyedia lain menggunakan awalan masing-masing (`kmc/`, `glm/`, `ds/`, `ollamacloud/`, `opencode-go/`, `mistral/`) — awalan tersebut sepadan dengan alias penyedia OmniRoute.

---

## Usaha Penaakulan

Mengawal sejauh mana model "berfikir" sebelum memberikan respons.

| Nilai    | Gunakan untuk                                       |
| -------- | --------------------------------------------------- |
| `none`   | Tiada penaakulan — respons langsung                 |
| `low`    | Tugas remeh (menamakan semula, memformat)           |
| `medium` | **Lalai pelayan** apabila tidak dinyatakan          |
| `high`   | Tugas pertengahan (pemfaktoran semula, nyahpepijat) |
| `xhigh`  | Seni bina, analisis mendalam, masalah kompleks      |

```bash
# Penggantian bagi setiap pemanggilan
codex -c model_reasoning_effort=low "namakan semula pemboleh ubah x kepada count"
codex -c model_reasoning_effort=xhigh "reka bentuk modul pengesahan"
```

Tetapkan juga **ringkasan** penaakulan supaya Desktop boleh memaparkan teks pemikiran (bukan hanya blob yang disulitkan):

```toml
# ~/.codex/config.toml
model_reasoning_effort = "xhigh"   # atau ultra apabila disokong
model_reasoning_summary = "detailed"  # auto | concise | detailed | none
```

### Belanjawan Pemikiran OmniRoute (tetapan pelayan)

Pada hos OmniRoute, **Settings → AI → Thinking Budget** mestilah **`passthrough`** agar usaha/ringkasan Codex sampai ke huluan. Mod **`auto` mengalih keluar** semua medan `reasoning` / `reasoning_effort` klien dan akan mengosongkan panel pemikiran walaupun Codex dikonfigurasikan dengan betul.

Panduan penuh: [THINKING_BUDGET.md](./THINKING_BUDGET.md).

Pemampatan dan cache gesaan adalah bebas dan terus berfungsi di bawah `passthrough`.

---

## Profil — konfigurasi bernama bagi setiap model/aliran kerja

Profil membolehkan anda menukar model + tetingkap konteks dengan satu bendera. Setiap profil ialah fail rata
`~/.codex/<name>.config.toml` yang ditindihkan di atas `config.toml` asas.

> **Peraturan penamaan (Codex CLI v0.137+):** fail mestilah `~/.codex/<name>.config.toml` — **tanpa awalan `profile-`**.
> CLI menyelesaikan `-p kimi-k27` → `~/.codex/kimi-k27.config.toml`. Jika fail tidak ditemukan, tetapan lalai digunakan secara senyap.

```bash
codex --profile kimi-k27 "analisis 10k baris pangkalan kod ini"
codex -p glm52 "semakan seni bina"
codex --profile deepseek-flash "namakan semula pemboleh ubah"   # pantas, murah
```

### Profil usaha (model sama, usaha berbeza)

```bash
codex -p low      # cx/gpt-5.5, usaha=low
codex -p medium   # cx/gpt-5.5, usaha=medium
codex -p high     # cx/gpt-5.5, usaha=high
codex -p xhigh    # cx/gpt-5.5, usaha=xhigh (lalai)
codex -p chat     # cx/gpt-5.5, tiada usaha ditetapkan (lalai pelayan)
```

### Model pemikiran (pemikiran tinggi) — xhigh + ringkasan terperinci

| Profil       | Model                       | Konteks | Gunakan untuk                    |
| ------------ | --------------------------- | ------- | -------------------------------- |
| `kimi-k27`   | `kmc/kimi-k2.7`             | 128k    | Kualiti pemikiran terbaik (Kimi) |
| `glm52`      | `glm/glm-5.2`               | 128k    | Pemikiran GLM                    |
| `glm52max`   | `glm/glm-5.2-max`           | 128k    | Pemikiran maksimum GLM           |
| `mimo-pro`   | `opencode-go/mimo-v2.5-pro` | 128k    | Pemikiran MiMo                   |
| `qwen37plus` | `opencode-go/qwen3.7-plus`  | 32k     | Pemikiran Qwen                   |

### Model yang bagus — usaha tinggi

| Profil         | Model                         | Konteks | Gunakan untuk                               |
| -------------- | ----------------------------- | ------- | ------------------------------------------- |
| `kimi-k26`     | `kmc/kimi-k2.6`               | 128k    | Kegunaan umum (Kimi)                        |
| `deepseek-pro` | `ollamacloud/deepseek-v4-pro` | 128k    | DeepSeek Pro melalui OllamaCloud            |
| `deepseek`     | `ds/deepseek-v4-pro`          | 1M      | DeepSeek Pro secara langsung, konteks besar |
| `mimo`         | `opencode-go/mimo-v2.5`       | 128k    | MiMo kegunaan umum                          |

### Model ringkas — tanpa usaha penaakulan

| Profil     | Model                          | Konteks | Gunakan untuk             |
| ---------- | ------------------------------ | ------- | ------------------------- |
| `gemma4`   | `ollamacloud/gemma4:31b`       | 32k     | Kos efektif, berkeupayaan |
| `nemotron` | `ollamacloud/nemotron-3-super` | 32k     | NVIDIA Nemotron           |
| `gptoss`   | `ollamacloud/gpt-oss:20b`      | 32k     | GPT sumber terbuka        |

### Model pantas — usaha rendah

| Profil           | Model                                | Konteks | Gunakan untuk                |
| ---------------- | ------------------------------------ | ------- | ---------------------------- |
| `deepseek-flash` | `ollamacloud/deepseek-v4-flash`      | 64k     | Tugas pantas                 |
| `gemini-flash`   | `ollamacloud/gemini-3-flash-preview` | 1M      | Sangat pantas, konteks besar |
| `glm5turbo`      | `glm/glm-5-turbo`                    | 128k    | GLM Turbo                    |
| `glm47flash`     | `glm/glm-4.7-flash`                  | 128k    | GLM Flash                    |
| `mistral`        | `mistral/mistral-large-latest`       | 256k    | Mistral Large                |

### Jadual keputusan pantas

| Tugas                                          | Profil yang disyorkan                              |
| ---------------------------------------------- | -------------------------------------------------- |
| Penamaan semula, pemformatan, templat          | `--profile deepseek-flash` atau `-p low`           |
| Penjelasan, semakan ringkas                    | `-p chat` atau `-p gemini-flash`                   |
| Penyahpepijatan, pemfaktoran sederhana         | `-p medium` atau `-p kimi-k26`                     |
| Ciri baharu, ujian kompleks                    | `-p high` atau `-p mimo`                           |
| Seni bina, analisis mendalam                   | `-p kimi-k27` atau `-p glm52` atau `-p xhigh`      |
| Analisis pangkalan kod (memerlukan konteks 1M) | `--profile deepseek` atau `--profile gemini-flash` |
| Kualiti penaakulan maksimum                    | `-p glm52max` atau `-p mimo-pro`                   |
| Mengutamakan penjimatan kos                    | `-p gemma4` atau `-p gptoss`                       |

---

## Menjana profil secara automatik dengan `omniroute setup-codex`

Jika anda menjalankan OmniRoute pada VPS, anda boleh menjana fail profil secara automatik daripada katalog model langsung:

```bash
# Daripada VPS (menggunakan OmniRoute setempat pada port 20128)
omniroute setup-codex

# Daripada mana-mana mesin — halakan ke VPS anda
omniroute setup-codex --remote http://100.x.x.x:20128 --api-key sk-xxx

# Pratonton tanpa menulis fail
omniroute setup-codex --remote http://100.x.x.x:20128 --dry-run

# Hanya jana profil GLM dan Kimi
omniroute setup-codex --only glm,kimi

# Tulis ke direktori tersuai
omniroute setup-codex --codex-home /path/to/.codex
```

Perintah tersebut mendapatkan `/v1/models`, menggunakan profil yang ditala untuk model yang diketahui, beralih kepada metadata katalog untuk model teks serasi yang lain, dan menulis `~/.codex/<name>.config.toml` bagi setiap model. Idempoten — selamat untuk dijalankan semula.

OmniRoute juga boleh **menyegerakkan secara automatik** fail profil yang sama selepas penemuan/import model penyedia yang berjaya mengubah katalog langsung. Ciri ini **perlu diaktifkan dan dimatikan secara lalai**: togolkannya daripada **papan pemuka CLI Code** ("Penyegerakan automatik profil CLI" → Codex), atau tetapkan `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES=true` (ia turut mematuhi `CLI_ALLOW_CONFIG_WRITES`, yang diaktifkan secara lalai). Apabila diaktifkan, ia hanya menulis fail profil `~/.codex/*.config.toml` yang berasingan; ia tidak pernah mengubah `~/.codex/config.toml` aktif/lalai, tetapan Codex-lb, pengesahan atau pemilihan penyedia.

---

## Melancarkan Codex dengan `omniroute launch-codex`

Melakukan pemeriksaan kesihatan terhadap tika OmniRoute anda sebelum melancarkan Codex:

```bash
# Lancarkan menggunakan OmniRoute setempat (port lalai 20128)
omniroute launch-codex

# Lancarkan dengan profil tertentu
omniroute launch-codex --profile kimi-k27

# Lancarkan menggunakan VPS jauh
omniroute launch-codex --remote http://100.x.x.x:20128/v1 --api-key sk-xxx

# Hantar argumen tambahan kepada codex
omniroute launch-codex --profile glm52 -- --yolo "fix this bug"
```

Codex juga merupakan sasaran bagi dua titik masuk generik dipacu manifes
(`bin/cli/cli-manifest.mjs`):

```bash
# Pemilih model interaktif → menulis ~/.codex/<name>.config.toml (TOML, env_key)
omniroute configure codex

# Lancarkan codex dengan penyedia omniroute disuntik melalui bendera -c (tiada konfigurasi ditulis)
omniroute run codex
```

---

## Ciri baharu Codex CLI (v0.138–v0.141)

| Versi  | Ciri                                                                                                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v0.138 | Penyerahan kepada aplikasi desktop (`/app`), token akses peribadi v2, `--profile` sebagai pemilih profil eksklusif (jadual `[profiles]` dalam fail legasi menyebabkan ranap semasa permulaan) |
| v0.139 | `web_search = "live"` — carian web natif daripada mod kod; `oneOf`/`allOf` dalam skema alat MCP; diagnostik persekitaran `codex doctor`                                                       |
| v0.140 | Paparan token `/usage` dalam sesi; `/import` daripada sesi Claude Code; subperintah `codex delete <SESSION_ID>`; pengesahan Amazon Bedrock melalui objek `aws` dalam konfigurasi penyedia     |
| v0.141 | Geganti Noise disulitkan E2E untuk pelaksana jauh; pembaikan SQLite WAL; sokongan TLS P-521                                                                                                   |

### Medan `config.toml` baharu (selepas v0.137)

```toml
# Carian web natif (v0.139)
web_search = "live"   # "disabled" | "cached" | "live"

# Prom sistem pembangun yang berasingan (v0.138)
developer_instructions = "Always prefer functional style."

# Prom pemadatan tersuai
compact_prompt = "Summarise the above as bullet points."

# Halakan /review kepada model yang lebih murah
review_model = "glm/glm-5-turbo"

# Tahap perkhidmatan OpenAI
service_tier = "fast"   # "fast" | "flex"
```

### Medan `[model_providers.<id>]` baharu

```toml
[model_providers.omniroute]
base_url             = "http://100.x.x.x:20128/v1"
env_key              = "OMNIROUTE_API_KEY"
requires_openai_auth = false

# Pengepala tambahan statik pada setiap permintaan
[model_providers.omniroute.http_headers]
"X-Custom-Header" = "value"

# Pengepala yang dibaca daripada pemboleh ubah persekitaran
[model_providers.omniroute.env_http_headers]
"X-Trace-Id" = "TRACE_ID"

# Parameter pertanyaan URL tambahan (berguna untuk api-version Azure)
[model_providers.omniroute.query_params]
"api-version" = "2024-12-01-preview"
```

### Pengesahan Amazon Bedrock (v0.140)

```toml
[model_providers.bedrock]
base_url = "https://bedrock-runtime.us-east-1.amazonaws.com"

[model_providers.bedrock.aws]
profile = "default"   # Profil ~/.aws/credentials
region  = "us-east-1"
```

---

## Berbilang pelayan

```toml
[model_providers.omniroute-main]
base_url = "http://192.168.0.1:20128/v1"
env_key  = "OMNIROUTE_API_KEY"

[model_providers.omniroute-tailscale]
base_url = "http://100.x.x.x:20128/v1"
env_key  = "OMNIROUTE_API_KEY"
```

---

## Claude Code — konfigurasi setara

| Codex CLI (`config.toml`)         | Claude Code (pemboleh ubah persekitaran) | Kesan                              |
| --------------------------------- | ---------------------------------------- | ---------------------------------- |
| `tool_output_token_limit = 32768` | _(tidak didedahkan secara langsung)_     | Had sejarah bagi setiap alat       |
| `model_context_window = 400000`   | _(ditentukan oleh model)_                | Tetingkap konteks                  |
| —                                 | `CLAUDE_CODE_MAX_OUTPUT_TOKENS=65536`    | Token maksimum bagi setiap respons |

```bash
# ~/.bashrc — had token Claude Code
export CLAUDE_CODE_MAX_OUTPUT_TOKENS=65536
```

---

## Rujukan pantas — bendera CLI

| Bendera               | Ringkas | Kesan                                                 |
| --------------------- | ------- | ----------------------------------------------------- |
| `--model <id>`        | `-m`    | Mengatasi `model` untuk pemanggilan ini               |
| `--profile <name>`    | `-p`    | Memuatkan `~/.codex/<name>.config.toml`               |
| `--config key=value`  | `-c`    | Mengatasi mana-mana medan config.toml (boleh diulang) |
| `--enable <feature>`  | —       | Mengaktifkan bendera ciri secara paksa                |
| `--disable <feature>` | —       | Menyahaktifkan bendera ciri secara paksa              |
| `--search`            | —       | Mendayakan carian web langsung untuk pemanggilan ini  |

Baharu dalam v0.140:

```bash
codex delete <SESSION_ID>          # padam sesi
codex delete <SESSION_ID> --force  # langkau pengesahan
codex debug models --bundled       # senaraikan katalog model terbina dalam sebagai JSON
```

Dalam sesi interaktif:

| Perintah  | Kesan                                                |
| --------- | ---------------------------------------------------- |
| `/model`  | Membuka pemilih model                                |
| `/usage`  | Menunjukkan penggunaan token untuk sesi ini (v0.140) |
| `/app`    | Menyerahkan kepada aplikasi desktop (v0.138)         |
| `/import` | Mengimport sesi Claude Code (v0.140)                 |
| `/help`   | Menyenaraikan semua perintah garis miring            |

---

## Tugas jangka panjang

Dua tetapan lalai OmniRoute boleh menjejaskan sesi Codex CLI berbilang jam secara senyap. Kedua-duanya bukan tetapan Codex CLI — kedua-duanya berada pada bahagian OmniRoute. Pengguna yang memindahkan konfigurasi daripada proksi huluan yang menetapkan akaun dan menyahaktifkan had masa melahu sering menghadapi kedua-dua masalah ini lalu membuat kesimpulan bahawa OmniRoute “tidak dapat mengekalkan sesi yang panjang.”

| Gejala                                                                            | Punca berkemungkinan                                                   | Tetapan                  |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------ |
| Sesi terus bertukar akaun / kesinambungan cache gesaan hilang antara giliran      | TTL afiniti sesi ialah `0` (dinyahaktifkan)                            | `sessionAffinityTtlMs`   |
| Sambungan terputus ketika penaakulan tanpa gesaan yang kelihatan pada pihak klien | Pemantau melahu strim dicetuskan selepas 10 minit tanpa cebisan huluan | `STREAM_IDLE_TIMEOUT_MS` |

Perbincangan berkaitan: [#7126](https://github.com/diegosouzapw/OmniRoute/discussions/7126) (tugas panjang terputus), [#5718](https://github.com/diegosouzapw/OmniRoute/discussions/5718) (sebab afiniti dinyahaktifkan secara lalai). Penjejakan: [#7287](https://github.com/diegosouzapw/OmniRoute/issues/7287).

### 1. Afiniti sesi — tetapkan satu perbualan kepada satu akaun

**Lalai:** `sessionAffinityTtlMs = 0` (dinyahaktifkan).

**Tempat untuk menetapkannya**

- Papan pemuka → **Tetapan → Penghalaan** → **Afiniti sesi** → **TTL afiniti (saat)** (`ComboDefaultsTab`)
- Atau PATCH tetapan dengan `sessionAffinityTtlMs` dalam **milisaat** (julat Zod `0`–`86_400_000`, iaitu sehingga 24 jam)

> Dinamakan semula dalam #7274 daripada `codexSessionAffinityTtlMs` yang khusus untuk Codex. Kekunci legasi masih diterima sebagai alias baca sahaja; konfigurasi baharu harus menggunakan `sessionAffinityTtlMs`. Afiniti kini digunakan untuk **mana-mana** penyedia apabila TTL melebihi `0`, bukan hanya Codex — lihat [`docs/architecture/RESILIENCE_GUIDE.md`](../architecture/RESILIENCE_GUIDE.md) → Afiniti sesi.

**Perkara yang terjejas apabila nilainya kekal pada 0**

Setiap giliran dalam perbualan Codex berbilang giliran dihalakan secara berasingan oleh strategi kombo yang aktif dan boleh dihantar kepada **akaun yang berbeza bagi setiap giliran**. Hal ini menjejaskan kesinambungan sesi / cache gesaan huluan. OmniRoute hanya merujuk pengepala sesi Codex (`x-codex-session-id` / `x-session-id` / `x-omniroute-session`) dan medan isi seperti `prompt_cache_key` / `session_id` apabila TTL lebih besar daripada `0` (`extractSessionAffinityKey` dalam `src/sse/services/auth.ts`).

**Disyorkan untuk satu tugas berbilang jam**

Tetapkan TTL **melebihi tempoh masa sebenar tugas yang dijangkakan** (maksimum UI ialah **86400 saat** = 24 jam):

| Tempoh tugas yang dijangkakan | TTL afiniti (UI, saat)  | `sessionAffinityTtlMs` |
| ----------------------------- | ----------------------- | ---------------------- |
| Beberapa jam                  | `14400` (4j)            | `14400000`             |
| Semalaman / ~12j              | `43200` (12j)           | `43200000`             |
| Sehari penuh                  | `86400` (24j, maksimum) | `86400000`             |

Penyertaan secara pilihan adalah disengajakan: menyahaktifkan afiniti mengutamakan pengimbangan beban merentas akaun; mengaktifkannya mengutamakan kesinambungan bagi satu sesi ejen yang panjang. Panduan ini **tidak** mengubah tetapan lalai — pengendali yang menjalankan tugas Codex yang panjang mesti mengaktifkannya sendiri.

### 2. Tamat masa melahu strim — jangan hentikan giliran penaakulan yang senyap

**Lalai:** `STREAM_IDLE_TIMEOUT_MS = 600000` (10 minit). Nilai ini diwarisi daripada `REQUEST_TIMEOUT_MS` apabila tidak ditetapkan; garis dasar bersama juga ialah 600000. Lihat [`docs/guides/SETUP_GUIDE.md`](SETUP_GUIDE.md) → Tamat masa.

**Perkara yang terjejas pada tetapan lalai**

Giliran penaakulan / alat Codex yang kekal senyap selama lebih daripada 10 minit dengan **tiada ketulan huluan sebenar** akan ditutup secara paksa oleh pemantau melahu SSE (`open-sse/utils/stream.ts`). Klien selalunya hanya melihat sambungan terputus tanpa sebarang mesej — sepadan dengan keadaan “berhenti secara automatik tanpa sebarang pemberitahuan.”

Butiran penting: **degupan jantung SSE** sintetik OmniRoute tidak menetapkan semula jam melahu. Hanya ketulan isi huluan sebenar yang mengemas kini `lastChunkTime`. Model senyap yang masih “berfikir” kelihatan sama seperti huluan yang tersekat dari sudut pandangan pemantau.

Ketidakaktifan isi Undici yang berkaitan: `FETCH_BODY_TIMEOUT_MS` (juga menggunakan garis dasar 10 minit yang sama secara lalai; `0` menyahaktifkannya). Untuk penstriman, `FETCH_TIMEOUT_MS` hanya meliputi persediaan sambungan / pengepala pertama — selepas strim aktif, keadaan tersekat dikawal oleh `STREAM_IDLE_TIMEOUT_MS` dan `FETCH_BODY_TIMEOUT_MS`.

**Disyorkan untuk satu tugasan berbilang jam**

Dalam persekitaran proses OmniRoute (`.env` / compose / systemd):

```bash
# Nyahaktifkan had masa melahu strim + ketidakaktifan isi untuk giliran penaakulan yang panjang
STREAM_IDLE_TIMEOUT_MS=0
FETCH_BODY_TIMEOUT_MS=0
```

Atau tingkatkan nilainya melebihi tempoh senyap terpanjang yang anda jangkakan (nilai dalam milisaat):

```bash
# Contoh: benarkan sehingga 2 jam senyap antara ketulan huluan
STREAM_IDLE_TIMEOUT_MS=7200000
FETCH_BODY_TIMEOUT_MS=7200000
```

Mulakan semula OmniRoute selepas mengubah pemboleh ubah persekitaran ini.

### Resipi khusus — tugasan Codex berbilang jam

1. **Tetapkan akaun:** Dashboard → Settings → Routing → Session affinity → Affinity TTL = `43200` (12j) atau `86400` (maksimum 24j).
2. **Tingkatkan / nyahaktifkan had masa melahu** dalam persekitaran OmniRoute:

```bash
STREAM_IDLE_TIMEOUT_MS=0
FETCH_BODY_TIMEOUT_MS=0
```

3. Kekalkan `config.toml` Codex yang biasa (`wire_api = "responses"`, `base_url` yang betul, `OMNIROUTE_API_KEY`) — tiada tetapan afiniti/melahu pada pihak Codex untuk kedua-dua tingkah laku ini.
4. Mulakan semula OmniRoute, kemudian mulakan tugasan Codex yang panjang itu.

### Keputusan nilai lalai (#7287)

| Tetapan                  | Nilai lalai       | Diubah dalam panduan ini?                                                                                            |
| ------------------------ | ----------------- | -------------------------------------------------------------------------------------------------------------------- |
| `sessionAffinityTtlMs`   | `0` (dimatikan)   | **Tidak** — kekal sebagai pilihan ikut serta (pengimbangan beban berbanding kesinambungan; lihat Perbincangan #5718) |
| `STREAM_IDLE_TIMEOUT_MS` | `600000` (10 min) | **Tidak** — kekal 10 minit untuk trafik umum; pengendali Codex menaikkan atau menyahaktifkannya                      |

Mengubah mana-mana nilai lalai secara global akan mengubah tingkah laku untuk setiap klien sesuatu tika, bukan Codex sahaja. Dokumentasikan tetapan tersebut; biarkan nilai lalai tidak berubah sehingga terdapat keputusan jelas daripada pengendali yang menyatakan sebaliknya.

### Mendiagnosis pemotongan akibat melahu

Apabila pemantau melahu dicetuskan, OmniRoute merekodkan baris berbentuk seperti:

```text
[STREAM] Idle timeout: no data from codex for 600000ms (model: cx/gpt-5.5)
```

Gunakan grep untuk `Idle timeout: no data from` (atau kod `stream_idle_timeout` / nama ralat `StreamIdleTimeoutError`). Segmen penyedia ialah apa-apa yang digunakan OmniRoute untuk permintaan tersebut (`codex`, id penyedia lain, atau `provider` jika tidak diketahui) — ia tidak semestinya rentetan literal `codex`.

---

## Penyelesaian Masalah

**`Error: wire_api = "chat" tidak lagi disokong`**
Alih keluar `wire_api = "chat"` daripada konfigurasi anda. Tetapkan `wire_api = "responses"` atau abaikan medan tersebut (lalai kepada `"responses"` sejak v0.138).

**`Error: model tidak ditemukan`**
Sahkan bahawa model tersebut wujud dalam OmniRoute dengan awalan yang betul. Gunakan `omniroute models list` atau buka `/dashboard/providers/<provider>`.

**`Ralat pengesahan`**
Pastikan `OMNIROUTE_API_KEY` telah dieksport: `echo $OMNIROUTE_API_KEY`.

**`ERROR: Pemboleh ubah persekitaran tiada: OMNIROUTE_API_KEY`**
Codex mengesahkan bahawa pemboleh ubah persekitaran tersebut wujud sebelum membuat permintaan pertama. Eksport
kunci sebenar untuk pelayan yang dilindungi, atau ruang letak yang tidak kosong seperti
`OMNIROUTE_API_KEY=local` apabila tika OmniRoute **setempat** anda tidak
memerlukan pengesahan. Mulakan semula shell jika anda menambahkannya pada `~/.bashrc` atau `~/.zshrc`.

**`Sambungan ditolak`**
Sahkan bahawa OmniRoute sedang berjalan dan hos/port `base_url` adalah betul untuk rangkaian anda (setempat berbanding Tailscale berbanding VPS).

**Sesi ranap apabila menghampiri had konteks**
Tetapkan `model_context_window` dan `model_auto_compact_token_limit` secara eksplisit. Lihat jadual tetingkap konteks di atas.

**Pemadatan dicetuskan terlalu lewat**
Turunkan `model_auto_compact_token_limit` kepada 80–85% daripada tetingkap. Jangan sekali-kali menetapkannya melebihi 90%.

**Profil tidak dimuatkan (`-p <name>` diabaikan secara senyap)**
Pastikan fail tersebut wujud di `~/.codex/<name>.config.toml` (tanpa awalan `profile-`). Jalankan `ls ~/.codex/*.config.toml`.

**Tugas Codex yang panjang terputus semasa berjalan / bertukar akaun antara giliran**
Lihat [Tugas yang berjalan lama](#long-running-tasks). Dayakan afiniti sesi (TTL melebihi tempoh tugas) dan tingkatkan atau nyahdayakan `STREAM_IDLE_TIMEOUT_MS` / `FETCH_BODY_TIMEOUT_MS`. Gunakan grep pada log OmniRoute untuk `Idle timeout: no data from`.
