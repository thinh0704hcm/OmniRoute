# Usage, Quota & Spend Tracking (Bahasa Melayu)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/USAGE_QUOTA_GUIDE.md) · 🇪🇹 [am](../../../am/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇦🇿 [az](../../../az/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇩🇰 [da](../../../da/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇩🇪 [de](../../../de/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇬🇷 [el](../../../el/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇪🇸 [es](../../../es/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇪🇪 [et](../../../et/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇱 [he](../../../he/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇩 [id](../../../id/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇹 [it](../../../it/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇭 [km](../../../km/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇲 [my](../../../my/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇴 [no](../../../no/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [or](../../../or/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇰 [si](../../../si/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [te](../../../te/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇭 [th](../../../th/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/USAGE_QUOTA_GUIDE.md)

---

> **TL;DR**: OmniRoute menjejaki penggunaan token bagi setiap permintaan, mengira kos, menguatkuasakan kuota bagi setiap kunci API dan memaparkan analitik dalam papan pemuka. Panduan ini menerangkan cara semuanya berfungsi.

**Sumber:**

- `open-sse/services/usage.ts` (~70KB) — penjejakan penggunaan utama
- `src/lib/usageAnalytics.ts` (~10KB) — pengagregatan untuk papan pemuka
- `src/lib/db/quotaSnapshots.ts` — data kuota sejarah
- `src/lib/db/usage*.ts` — berbilang modul DB berkaitan penggunaan

---

## Gambaran Keseluruhan

Setiap permintaan yang melalui OmniRoute menjana **rekod penggunaan** yang merekodkan:

- **Identiti**: kunci API, penyedia, model dan kombo yang digunakan
- **Token**: token gesaan, token pelengkapan, token dicache dan jumlah keseluruhan
- **Kos**: amaun USD (dikira daripada data harga)
- **Pemasaan**: kependaman, cap masa mula/tamat
- **Status**: berjaya, ralat, dihadkan kadar dan sebagainya

Rekod ini diagregatkan menjadi **analitik**, disimpan sebagai **petikan kuota** dan digunakan untuk menguatkuasakan **had belanjawan bagi setiap kunci**.

```
Permintaan ──▶ chatCore ──▶ usage.record() ──▶ SQLite
                                  │
                          ┌───────┼───────┐
                          ▼       ▼       ▼
                     analitik  kuota  pengebilan
                  (papan pemuka) (kuat kuasa) (eksport)
```

---

## Perkara yang Direkodkan

Perkhidmatan `usage.ts` merekodkan **peristiwa penggunaan** bagi setiap permintaan:

| Medan              | Jenis   | Sumber                                                      |
| ------------------ | ------- | ----------------------------------------------------------- |
| `id`               | string  | UUID yang dijana semasa merekodkan                          |
| `apiKeyId`         | string  | Kunci API yang memulakan permintaan                         |
| `provider`         | string  | ID penyedia (openai, anthropic dan sebagainya)              |
| `model`            | string  | ID model (gpt-5, claude-opus-4-6 dan sebagainya)            |
| `comboId`          | string? | ID kombo jika dihalakan melalui kombo                       |
| `promptTokens`     | number  | Daripada respons huluan                                     |
| `completionTokens` | number  | Daripada respons huluan                                     |
| `cachedTokens`     | number  | Token padanan cache (cache gesaan Anthropic dan sebagainya) |
| `totalTokens`      | number  | gesaan + pelengkapan                                        |
| `costUsd`          | number  | Dikira daripada data harga                                  |
| `latencyMs`        | number  | Tempoh permintaan dari hujung ke hujung                     |
| `status`           | enum    | `success`, `error`, `rate_limited`, `timeout`, `cancelled`  |
| `errorClass`       | string? | Kelas ralat jika status != success                          |
| `timestamp`        | string  | ISO 8601 UTC                                                |
| `metadata`         | object  | Data tersuai yang disuntik oleh pemalam                     |

### Sumber Token

Token diekstrak daripada respons penyedia huluan dalam **pengendali respons**:

```ts
// Daripada open-sse/handlers/chatCore.ts
const response = await providerExecutor.execute(provider, request);
const usage = response.usage || {
  prompt_tokens: 0,
  completion_tokens: 0,
  cached_tokens: 0,
};
```

Bagi penyedia yang tidak mengembalikan penggunaan (sesetengah penyedia kuki web), OmniRoute **menganggarkan** token menggunakan heuristik `~4 aksara bagi setiap token` (lihat `open-sse/services/autoCombo/pipelineRouter.ts`).

### Token Dicache

OmniRoute menjejaki `cached_tokens` secara berasingan daripada `prompt_tokens` kerana:

- Cache gesaan Anthropic mengenakan kadar yang lebih rendah untuk token dicache (10% daripada kadar biasa)
- Sesetengah penyedia mengembalikan `cache_read_input_tokens` yang perlu ditetapkan harga secara berbeza
- Analitik boleh menunjukkan **kadar padanan cache** = `cached_tokens / prompt_tokens`

---

## Pengiraan Kos

Kos dikira daripada **data harga** yang disegerakkan daripada LiteLLM (`src/lib/pricingSync.ts`):

| Model             | Input $/1M | Output $/1M | Cache $/1M |
| ----------------- | ---------- | ----------- | ---------- |
| gpt-5             | $2.50      | $10.00      | —          |
| claude-opus-4-6   | $15.00     | $75.00      | $1.50      |
| claude-sonnet-4-5 | $3.00      | $15.00      | $0.30      |
| gemini-2.5-pro    | $1.25      | $10.00      | —          |

Formula kos (`src/lib/usage/costCalculator.ts`):

```ts
cost =
  (prompt_tokens - cached_tokens) * input_price +
  cached_tokens * cached_price +
  completion_tokens * output_price;
```

> **Mengapakah token cache ditolak daripada prom?** Bahagian cache mempunyai harga berasingan; mengenakan harga input pada keseluruhan prom akan menyebabkan pengiraan berlebihan.

### Penyegerakan Harga

Data harga disegerakkan secara automatik daripada LiteLLM melalui titik akhir `/api/pricing/sync` (dicetuskan oleh tugas cron terbina dalam, bukan pemboleh ubah persekitaran untuk pengguna):

```bash
# Pencetus manual
curl -X POST http://localhost:20128/api/pricing/sync
```

Bagi model tanpa data harga, OmniRoute akan **menganggarkan kos** menggunakan kadar purata dalaman (bersumberkan data harga LiteLLM).

---

## Pengagregatan Julat Tarikh

Modul `usageAnalytics.ts` mengira widget papan pemuka daripada data penggunaan mentah. Modul ini menyokong 7 julat masa:

| Julat    | Tempoh                                     | Kes penggunaan                       |
| -------- | ------------------------------------------ | ------------------------------------ |
| `1d`     | 24 jam terakhir                            | Pengesanan lonjakan kos mengikut jam |
| `7d`     | 7 hari terakhir                            | Semakan mingguan                     |
| `30d`    | 30 hari terakhir                           | Pengebilan bulanan                   |
| `90d`    | 90 hari terakhir                           | Analisis suku tahunan                |
| `ytd`    | Sejak 1 Jan tahun semasa                   | Penjejakan belanjawan tahunan        |
| `all`    | Sepanjang masa                             | Statistik sepanjang hayat            |
| `custom` | Tarikh mula/tamat yang ditentukan pengguna | Audit, pertanyaan ad hoc             |

### Widget Papan Pemuka yang Dikira

Bagi sebarang julat tarikh, lapisan analitik mengira:

| Widget                 | Penerangan                                                               |
| ---------------------- | ------------------------------------------------------------------------ |
| **Kad ringkasan**      | Jumlah permintaan, jumlah kos, jumlah token, kadar kejayaan              |
| **Carta trend harian** | Kos + token setiap hari, ditindan mengikut model                         |
| **Peta haba aktiviti** | Grid jam dalam sehari × hari dalam seminggu, warna = bilangan permintaan |
| **Pecahan model**      | Carta pai kos mengikut model                                             |
| **Pecahan penyedia**   | Carta bar permintaan mengikut penyedia                                   |
| **Kunci API teratas**  | Jadual 10 kunci teratas mengikut kos                                     |
| **Analisis ralat**     | Kadar ralat dari semasa ke semasa, kelas ralat teratas                   |

### Akses Secara Pengaturcaraan

````ts
import { computeAnalytics } from "@/lib/usageAnalytics";

const analytics = await computeAnalytics(
  history,              // rekod sejarah penggunaan
  "7d",                 // julat masa: "1d" | "7d" | "30d" | "90d" | "ytd" | "all" | "custom"
  connectionMap,        // peta sambungan penyedia (connectionId → nama akaun)
  {
    startDate: "2025-01-01",  // pilihan: untuk julat "custom"
    endDate: "2025-06-01",   // pilihan: untuk julat "custom"
  }
);

console.log(analytics.summary.totalCost);   // 12.34 (sen)
console.log(analytics.byModel[0]);           // { model, cost, requests, promptTokens, completionTokens }

---

## Penguatkuasaan Kuota

Kuota bagi setiap kunci API dikuatkuasakan di dua tempat:

1. **Had lembut** (`quotaWarnAt`): amaran papan pemuka apabila penggunaan melebihi ambang
2. **Had keras** (`quotaLimit`): permintaan ditolak dengan HTTP 429 apabila had dilebihi

### Konfigurasi

```ts
// Bagi setiap kunci API
await updateApiKey(keyId, {
  quotaWarnAt: 5_00,    // $5.00 — paparkan amaran
  quotaLimit: 10_00,    // $10.00 — hentian keras
  quotaWindow: "month", // "day" | "week" | "month" | "all"
});
````

### Aliran Penguatkuasaan

```
Permintaan ──▶ quotaCheck()
                 │
                 ├── Dalam had?  ──▶ benarkan
                 │
                 └── Melebihi had?  ──▶ 429 Too Many Requests
                                          dengan pengepala Retry-After
```

### Syot Kilat Kuota

Jadual `quotaSnapshots` menyimpan **keadaan kuota bersejarah** untuk analisis trend:

| Medan | Penerangan |
| ----------- | -------------------------------- | ------ | ------- |
| `apiKeyId` | Kunci yang dijejaki |
| `window` | "day" | "week" | "month" |
| `used` | Kos yang digunakan dalam tetingkap ini (sen) |
| `limit` | Had (sen) |
| `resetAt` | Masa tetingkap ditetapkan semula |
| `createdAt` | Masa syot kilat diambil |

Syot kilat diambil **pada setiap permintaan** yang menggunakan kos > 0, dan digunakan untuk:

- Memaparkan bar kemajuan kuota dalam papan pemuka
- Menunjukkan carta trend kuota 30 hari
- Mencetuskan amaran apabila penggunaan menghampiri had

---

## API REST

### Senaraikan Rekod Penggunaan

```bash
GET /api/usage?range=7d&limit=100
GET /api/usage?apiKeyId=key-123&range=30d
GET /api/usage?provider=openai&range=1d
```

Respons:

```json
{
  "records": [
    {
      "id": "uuid",
      "apiKeyId": "key-123",
      "provider": "openai",
      "model": "gpt-5",
      "promptTokens": 1234,
      "completionTokens": 567,
      "totalTokens": 1801,
      "costUsd": 0.005,
      "latencyMs": 1234,
      "status": "success",
      "timestamp": "2026-06-08T12:00:00Z"
    }
  ],
  "total": 1234,
  "nextCursor": "..."
}
```

### Dapatkan Ringkasan Analitik

```bash
GET /api/usage/analytics?range=7d&groupBy=model
```

Respons:

```json
{
  "summary": {
    "totalCost": 12.34,
    "totalRequests": 5678,
    "totalTokens": 12345678,
    "successRate": 0.987,
    "avgLatencyMs": 1234
  },
  "models": [
    { "model": "gpt-5", "cost": 8.5, "requests": 1234, "tokens": 4567890 },
    {
      "model": "claude-opus-4-6",
      "cost": 3.84,
      "requests": 234,
      "tokens": 234567
    }
  ],
  "daily": [
    { "date": "2026-06-01", "cost": 1.5, "requests": 800 },
    { "date": "2026-06-02", "cost": 2.0, "requests": 1000 }
  ]
}
```

### Kueri Analitik Penggunaan

Data penggunaan diakses melalui papan pemuka atau alatan MCP, bukan titik akhir eksport REST secara langsung. Analitik yang tersedia:

- **`/api/usage/analytics`** — metrik penggunaan teragregat (dikumpulkan mengikut model, penyedia, kunci)
- **`/api/usage/quota`** — status kuota semasa bagi setiap kunci API
- **`/api/usage/history`** — log sejarah permintaan

---

## Alatan MCP

Dua alatan MCP mendedahkan data penggunaan kepada ejen (lihat `open-sse/mcp-server/tools/`):

| Alat                    | Penerangan                                                  |
| ----------------------- | ----------------------------------------------------------- |
| `omniroute_cost_report` | Menjana laporan kos bagi setiap kunci untuk tempoh tertentu |
| `omniroute_check_quota` | Mengembalikan status kuota semasa untuk kunci API           |

Contoh pemanggilan ejen:

```json
{
  "tool": "omniroute_cost_report",
  "args": { "period": "week" }
}
```

---

## Pengekalan dan Pembersihan

Data penggunaan bertambah ~1-10KB bagi setiap permintaan. Pada skala besar, jumlah ini boleh menjadi ketara.

### Tetapan Pengekalan

Pengekalan sejarah penggunaan dikonfigurasikan melalui Tetapan Pangkalan Data dalam UI atau melalui `/api/settings/database`.

Secara lalai, sejarah penggunaan dikekalkan selama **90 hari**.

### Pembersihan

Rekod lama dibersihkan oleh `src/lib/db/cleanup.ts`:

- Dicetuskan oleh proses cron latar belakang
- Memadamkan rekod daripada `usage_history` yang lebih lama daripada tetapan pengekalan `usageHistory` yang dikonfigurasikan

### Anggaran Storan

| Kadar permintaan        | Storan 30 hari | Storan 90 hari |
| ----------------------- | -------------- | -------------- |
| 100 permintaan/hari     | ~3MB           | ~9MB           |
| 1,000 permintaan/hari   | ~30MB          | ~90MB          |
| 10,000 permintaan/hari  | ~300MB         | ~900MB         |
| 100,000 permintaan/hari | ~3GB           | ~9GB           |

Untuk trafik yang sangat tinggi, pertimbangkan:

- Mengurangkan tempoh pengekalan melalui Tetapan Pangkalan Data
- Menggunakan `aggregated_metrics` dan bukannya rekod mentah (hanya untuk analitik)

---

## Petua Pengoptimuman Kos

### 1. Gunakan Model yang Sesuai

```bash
# Jawapan pantas — gunakan yang murah + pantas
curl -d '{"model":"auto/fast","messages":[...]}'

# Tugasan kompleks — utamakan kualiti
curl -d '{"model":"auto/smart","messages":[...]}'
```

### 2. Dayakan Cache

Cache gesaan Anthropic menjimatkan **90% pada konteks berulang**:

```ts
// Cache adalah automatik — hanya sertakan gesaan sistem besar yang sama
const response = await openai.chat({
  model: "claude-sonnet-4-5",
  system: longSystemPrompt, // Akan dicache secara automatik
  messages: [{ role: "user", content: "..." }],
});
```

### 3. Gunakan Pemampatan

Pemampatan RTK + Caveman menjimatkan **15-95% pada sesi yang banyak menggunakan alat**:

```ts
const config = {
  compression: {
    engine: "rtk",
    intensity: "aggressive",
  },
};
```

### 4. Tetapkan Kuota bagi Setiap Kunci

Sentiasa tetapkan `quotaLimit` untuk mengelakkan kos tidak terkawal:

```ts
await updateApiKey(keyId, { quotaLimit: 10_00 }); // Had $10/bulan
```

### 5. Audit Pengguna Terbesar

Gunakan papan pemuka atau **`/api/usage/analytics`** untuk mengumpulkan mengikut kunci API dan mengisih mengikut kos:

```bash
GET /api/usage/analytics?groupBy=apiKey
```

---

## Penyelesaian Masalah

### "Kos lebih tinggi daripada yang dijangkakan"

1. Semak **`/api/usage/analytics?groupBy=model`** — cari model yang mahal
2. Semak **`/api/usage/analytics?groupBy=apiKey`** — cari pengguna yang menggunakan paling banyak sumber
3. Sahkan data harga adalah terkini: `POST /api/pricing/sync`

### "Rekod tiada"

- Semak tetapan pengekalan DB di bawah Papan Pemuka → Pangkalan Data → Pembersihan — rekod lama dipadamkan oleh tugas pembersihan berkala (`src/lib/db/cleanup.ts`)
- Semak ralat dalam `src/lib/db/usage*.ts` — kegagalan penulisan DB dilog tetapi tidak dipaparkan
- Sahkan permintaan benar-benar sampai ke `chatCore` — semak penghalaan kombo

### "Kuota tidak dikuatkuasakan"

- Semak tetapan `quotaLimit` bagi kunci tersebut
- Sahkan `quotaWindow` ditetapkan dengan betul
- Cari rekod `quotaSnapshots` — rekod tersebut sepatutnya dicipta pada setiap permintaan

---

## Lihat Juga

- [DATABASE_GUIDE.md](../ops/DATABASE_GUIDE.md) — Skema untuk jadual penggunaan
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md#18-pricing-sync) — pemboleh ubah persekitaran penyegerakan harga
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — Cara `auto/fast`, `auto/cheap` mengurangkan kos
- [API_REFERENCE.md](../reference/API_REFERENCE.md) — Rujukan lengkap `/api/usage/*`
- Sumber: `open-sse/services/usage.ts`, `src/lib/usageAnalytics.ts`, `src/lib/db/usage*.ts`
