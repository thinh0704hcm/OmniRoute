# open-sse Architecture (Bahasa Melayu)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **TL;DR**: `open-sse/` ialah enjin penstriman teras yang mengendalikan setiap permintaan LLM dalam OmniRoute. Ia mengandungi ~1,400 fail yang melaksanakan saluran permintaan, pelaksana, perkhidmatan, pelayan MCP dan lapisan penterjemahan. Panduan ini menerangkan cara semua komponen tersebut saling berfungsi.

**Sumber:** `open-sse/` (pakej ruang kerja, ~1,440 fail `.ts` bukan ujian)

---

## Mengapa Pakej Ruang Kerja Berasingan?

`open-sse/` ialah **ruang kerja kendiri** dalam monorepo OmniRoute atas beberapa sebab:

1. **Kebolehgunaan semula** — `open-sse` diterbitkan sebagai `@omniroute/open-sse` di npm, jadi projek lain boleh menggunakannya secara bebas
2. **Sempadan yang jelas** — enjin penstriman dipisahkan daripada lapisan UI/DB khusus OmniRoute
3. **Prestasi** — enjin ini tidak mempunyai kebergantungan Next.js, membolehkan permulaan sejuk yang lebih pantas dalam konteks CLI/tanpa pelayan
4. **Pengurusan versi** — `open-sse` boleh membuat keluaran mengikut jadualnya sendiri

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Struktur Peringkat Atas

```
open-sse/
├── index.ts              # Titik masuk awam
├── types.d.ts            # Eksport jenis awam
├── package.json          # @omniroute/open-sse
├── config/               # Konfigurasi penyedia, pemalar, pendaftar
├── executors/            # Pelaksana HTTP bagi setiap penyedia (67 + base.ts/index.ts)
├── handlers/             # Pengendali permintaan (chatCore, responses dan sebagainya)
├── lib/                  # Utiliti dalaman
├── mcp-server/           # Pelayan Model Context Protocol
├── services/             # ~298 modul perkhidmatan
├── transformer/          # Pengubah format Responses API
├── translator/           # Penterjemahan format (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Utiliti dikongsi (pengelogan, ralat, strim dan sebagainya)
```

### Bilangan Modul

| Direktori | Fail | Tujuan |
| `executors/` | 167 | Pelaksana HTTP bagi setiap penyedia (disatukan melalui kilang DefaultExecutor) |
| `handlers/` | 157 | Titik masuk permintaan (chatCore, responses, embeddings) |
| `services/` | ~536 | Penghalaan, caching, pengehadaran kadar, penyegaran dan sebagainya |
| `translator/` | 56 | Penukaran format (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Alat dan pengangkutan MCP |
| `utils/` | ~108 | Utiliti rentas fungsi (pengelogan, ralat, strim) |
| `config/` | ~339 | Konfigurasi penyedia, pemalar, pendaftar |

---

## Saluran Permintaan

Setiap permintaan LLM mengalir melalui **saluran 5 peringkat**:

```
                              ┌──────────────┐
   Permintaan HTTP             │  1. HALAKAN  │   peleraian kombo, pemilihan model
   (laluan Next.js)            └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 2. TERJEMAH  │   penukaran format (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 3. LAKSANAKAN│   pelaksana penyedia, HTTP, cuba semula, pemutus
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STRIM    │   transformasi SSE, tekanan balik
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. REKOD    │   penjejakan penggunaan, log panggilan, pengelasan ralat
                              └──────┬───────┘
                                     │
                                     ▼
                              Respons HTTP (SSE atau JSON)
```

### Peringkat 1: Halakan (services/combo.ts)

**Titik masuk**: `handleComboChat()` dalam `services/combo.ts`

Meleraikan permintaan kepada tupel `(provider, model, account, credentials)` yang konkrit:

- Cari kombo mengikut ID (atau bina kombo maya untuk model `auto/*`)
- Gunakan strategi penghalaan (keutamaan, berwajaran, round-robin dan sebagainya)
- Tapis keluar penyedia yang tidak sihat (pemutus litar)
- Pilih sasaran berdaya maju yang seterusnya

Untuk model `auto/*`, peringkat ini turut:

- Menjalankan algoritma **pemarkahan 16 faktor** (`services/autoCombo/`)
- Memilih pasangan `provider+model` berdasarkan kesihatan, kos, kependaman dan sebagainya

### Peringkat 2: Terjemah (translator/)

Jika format sumber (contohnya, OpenAI) berbeza daripada format sasaran (contohnya, Claude), permintaan tersebut akan **diterjemahkan**:

- Prom sistem → mesej sistem
- Takrif alat → format alat khusus penyedia
- Parameter penaakulan/pemikiran → padanan khusus penyedia
- Penyeragaman peranan mesej (`developer` → `system` untuk selain OpenAI)

`translator/index.ts` mendedahkan:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Peringkat 3: Laksanakan (executors/)

**Titik masuk**: `getExecutor(providerId).execute(request, options)`

Semua penyedia menggunakan `DefaultExecutor` (`executors/default.ts`) melalui sandaran kilang `getExecutor()`. Pelaksana tersebut:

- Membina URL huluan (`buildUrl()`)
- Menambahkan pengepala khusus penyedia (`buildHeaders()`)
- Mengubah badan permintaan (`transformRequest()`)
- Menghantar permintaan HTTP dengan percubaan semula + undur eksponen
- Mengendalikan penyegaran pengesahan jika diperlukan (penyedia OAuth)

Semua pelaksana melanjutkan `BaseExecutor` (`executors/base.ts`, 1170 LOC), yang menyediakan:

- Logik percubaan semula umum
- Penyepaduan proksi
- Penyepaduan pemutus litar
- Cangkuk perekodan penggunaan

### Peringkat 4: Strim (utils/stream.ts)

Bagi respons penstriman, pelaksana mengembalikan **ReadableStream**. Pengendali tersebut:

- Menyalurkan melalui transformasi SSE (`createSSETransformStreamWithLogger`)
- Menggunakan ping denyutan jantung untuk mengesan sambungan terputus
- Mengendalikan pemutusan sambungan klien dengan baik (`pipeWithDisconnect`)
- Mengubah SSE → JSON untuk klien bukan penstriman

Bagi respons bukan penstriman, pelaksana mengembalikan objek JSON yang telah dihuraikan dan diteruskan tanpa perubahan.

### Peringkat 5: Rekod (services/usage.ts)

Selepas respons (berjaya atau gagal), penggunaan direkodkan:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` daripada respons
- `cost_usd` dikira berdasarkan data harga
- `latency_ms`, `status`, `error_class` jika gagal
- Disimpan dalam jadual `usage_history`

Artifak log panggilan (jika didayakan) ditulis ke `${DATA_DIR}/call_logs/`.

---

## Penelitian Mendalam Fail Utama

### chatCore.ts (5977 baris)

**Pengendali permintaan utama**. Walaupun bersaiz besar, ia mempunyai struktur yang jelas:

```ts
// Struktur pseudo bagi chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Pengesahan + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Pengesahan isi
  const body = await parseRequestBody(request);

  // 3. Pengesanan format + penterjemahan
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Penghalaan kombo
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Teruskan ke sasaran seterusnya
    }
  }

  // 5. Sandaran kecemasan
  return await emergencyFallback(body);
}
```

Walaupun merupakan satu fungsi yang sangat besar, ia disusun kepada **bahagian berulasan** yang sepadan dengan saluran paip 5 peringkat.

### combo.ts (4456 LOC)

**Enjin penghalaan** yang menyelesaikan kombo kepada sasaran tersusun.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Menyokong **19 strategi penghalaan** (lihat `src/shared/constants/routingStrategies.ts`):

| Strategi            | Tingkah laku                                                                               |
| ------------------- | ------------------------------------------------------------------------------------------ |
| `priority`          | Senarai tersusun dengan sasaran pertama diutamakan                                         |
| `weighted`          | Kebarangkalian berdasarkan pemberat setiap sasaran                                         |
| `round-robin`       | Mengitar sasaran mengikut turutan                                                          |
| `context-relay`     | Menyerahkan konteks merentas sasaran                                                       |
| `fill-first`        | Memenuhi kuota sebelum beralih ke sasaran seterusnya                                       |
| `p2c`               | Kuasa dua pilihan                                                                          |
| `random`            | Rawak seragam                                                                              |
| `least-used`        | Memilih sasaran dengan penggunaan terkini paling sedikit                                   |
| `cost-optimized`    | Sasaran sihat paling murah didahulukan                                                     |
| `reset-aware`       | Mengambil kira tetingkap tetapan semula penyedia                                           |
| `reset-window`      | Penghalaan berdasarkan tetingkap tetapan semula                                            |
| `headroom`          | Baki ruang kuota terbesar didahulukan                                                      |
| `strict-random`     | Benar-benar seragam (tanpa pemberatan kualiti)                                             |
| `auto`              | Menggunakan pemarkahan 16 faktor (`autoCombo/`)                                            |
| `lkgp`              | Penyedia baik terakhir yang diketahui didahulukan                                          |
| `context-optimized` | Terbaik untuk permintaan berkonteks panjang                                                |
| `fusion`            | Mengedarkan kepada panel secara selari, kemudian mensintesis melalui penilai (`fusion.ts`) |

### base.ts (1170 LOC)

**Pelaksana abstrak** yang dilanjutkan oleh kesemua 107 pelaksana. Ia mengandungi:

- `buildUrl()` — pembinaan URL lalai (subkelas mengatasi untuk penyesuaian)
- `buildHeaders()` — pengepala lalai (pengesahan, jenis kandungan)
- `transformRequest()` — laluan terus secara lalai
- `execute()` — gelung HTTP utama dengan percubaan semula/undur/pemutus

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Mengendalikan semua penyedia yang serasi dengan OpenAI/Anthropic
  // Penyedia mendaftarkan konfigurasi (URL, pengesahan, pengepala) tetapi berkongsi logik pelaksana
}
```

Tingkah laku khusus penyedia (pengepala pengesahan, URL asas, pengepala versi) dikonfigurasikan melalui daftar penyedia, bukannya kelas pelaksana yang berasingan.

````

---

## Perkhidmatan (117 modul)

Perkhidmatan ialah **modul berfokus dengan satu tujuan** yang digabungkan oleh pengendali. Kategori utamanya:

### Penghalaan & Kombo

- `combo.ts` — titik masuk untuk permintaan yang dihalakan melalui kombo
- `services/autoCombo/` — pemarkahan 16 faktor, 8 strategi penghalaan automatik
- `wildcardRouter.ts` — memadankan laluan kad bebas (`gpt-*`)
- `modelFamilyFallback.ts` — sandaran dalam keluarga T5

### Pengehadan Kadar & Kuota

- `rateLimitManager.ts` — baldi token bagi setiap kunci+penyedia
- `usage.ts` — perekodan penggunaan
- `quotaCache.ts` — petikan kuota dalam memori

### Akaun & Token

- `tokenRefresh.ts` — penyegaran OAuth apabila menerima 401
- `accountFallback.ts` — beralih kepada akaun alternatif
- `sessionManager.ts` — keadaan sesi berbilang giliran

### Kecerdasan

- `intentClassifier.ts` — mengelaskan niat permintaan
- `taskAwareRouter.ts` — menghalakan mengikut jenis tugas
- `thinkingBudget.ts` — memperuntukkan token pemikiran
- `contextManager.ts` — menyuntik konteks penghalaan

### Ketahanan

- `resilience.ts` — pengorkestraan percubaan semula, sela balik dan pemutus litar
- `emergencyFallback.ts` — sandaran pilihan terakhir
- `modelDeprecation.ts` — menghalakan secara automatik kepada model pengganti

### Keadaan

- `signatureCache.ts` — menyahduplikasi mengikut tandatangan permintaan
- `volumeDetector.ts` — pelepasan beban
- `contextHandoff.ts` — pensirian sesi

### Pemampatan

- `compression/` (subdirektori) — saluran paip pemampatan penuh
- 39 fail yang merangkumi enjin, pek peraturan dan penyesuai

### Kemahiran

- (dibincangkan dalam [SKILLS.md](./SKILLS.md))

### Memori

- (dibincangkan dalam [MEMORY.md](./MEMORY.md))

---

## Pelaksana (75+ fail)

Satu fail bagi setiap penyedia. Kesemuanya melanjutkan `BaseExecutor` dan mengatasi bahagian yang berbeza.

### Corak Lazim

Penyedia diselesaikan melalui `getExecutor(providerId)`, yang mengembalikan pelaksana yang dikonfigurasikan. Penyedia yang serasi dengan OpenAI/Anthropic menggunakan `DefaultExecutor` (`executors/default.ts`). Tingkah laku khusus penyedia (URL asas, pengepala pengesahan, versi API) dikonfigurasikan dalam `open-sse/config/providers/`, manakala transformasi isi permintaan dikendalikan dalam `open-sse/translator/`.

**URL tersuai** ditetapkan melalui konfigurasi penyedia:

```ts
// Konfigurasi penyedia dalam open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Pengesahan tersuai** dikendalikan melalui konfigurasi pengesahan pendaftaran penyedia (kunci API, OAuth, profil pengepala).

Transformasi **isi permintaan tersuai** (contohnya, Anthropic memisahkan `system` daripada `messages`) didaftarkan bagi setiap penyedia dalam `open-sse/translator/`.

````

### Kilang Pelaksana

`executors/index.ts` mengeksport `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Penyelesaian dilakukan melalui `ExecutorRegistry` (`executors/registry.ts`): setiap pelaksana khusus diisytiharkan dalam jadual terbina dalam `executors/index.ts` dan didaftarkan melalui `registerExecutor(alias, instance)` semasa modul dimuatkan; `getExecutor()` merujuk pendaftaran tersebut dan kembali kepada `DefaultExecutor` yang dimemo untuk mana-mana penyedia tanpa entri khusus. Pemetaan penuh alias → pelaksana dicirikan oleh ujian piawai emas `tests/unit/executor-map-golden.test.ts`.

---

## Penterjemah

Terjemahkan antara **3 format**: OpenAI, Anthropic, Gemini, serta Responses API yang baharu.

### Bila Terjemahan Berlaku

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Terjemahan lazim:

- `OpenAI → Anthropic`: medan `system` berasingan, pengepala `x-api-key`
- `OpenAI → Gemini`: `contents` menggantikan `messages`, `systemInstruction`
- `OpenAI → Responses API`: tatasusunan `input`, keadaan `previous_response_id`

### Kes Pinggir yang Dikendalikan

- Peranan `developer` → `system` untuk penyedia bukan OpenAI
- Peranan `system` → digabungkan ke dalam mesej pengguna pertama untuk GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` Gemini
- `tools` → format alat khusus penyedia
- Parameter penaakulan (o1, Claude) → padanan khusus penyedia

---

## Pelayan MCP

`open-sse/mcp-server/` melaksanakan pelayan **Model Context Protocol**:

- **110 alat** (pengurusan penyedia, gabungan, memori, cache, pemampatan, proksi, kemahiran, gamifikasi, pemalam, Notion, Obsidian, korpus setempat)
- **3 pengangkutan**: stdio, SSE, HTTP Boleh Strim
- **33 skop** untuk kebenaran terperinci

### Pendaftaran Alat

Alat didaftarkan sebagai fail kendiri dalam `open-sse/mcp-server/tools/`, dengan setiap fail mengeksport nama, skema, pengendali dan skop:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Dapatkan petikan keadaan sistem",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Pengangkutan

```ts
// stdio (penggunaan CLI)
startMcpStdio(server);

// SSE (penstriman berasaskan HTTP)
startMcpSse(server, port);

// HTTP Boleh Strim (MCP moden)
startMcpStreamable(server, port);
```

### Kebenaran

Setiap panggilan alat melalui semakan skop (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Skop tidak mencukupi");
}
```

---

## Pengubah

`open-sse/transformer/` menukar antara format **Chat Completions** dan **Responses API**.

### Mengapa Pengubah Berasingan?

Responses API ialah format baharu OpenAI dengan **perbualan berkeadaan** (`previous_response_id`). Apabila klien menghantar permintaan Responses, OmniRoute:

1. Menukar Responses → Chat Completions secara dalaman
2. Menghantar kepada penyedia (mana-mana penyedia yang menyokong Chat Completions)
3. Menukar respons kembali kepada format Responses
4. Menstrim respons yang ditukar kepada klien

Pengubah (`transformer/responsesTransformer.ts`) menyediakan:

```ts
createResponsesApiTransformStream(): TransformStream
```

Ini mengendalikan:

- Peristiwa `response.output_item.added`
- Peristiwa `response.output_text.delta`
- Peristiwa `response.completed`
- Pemetaan panggilan alat (`function_call` ↔ `tool_calls`)

---

## Konfigurasi

`open-sse/config/` mengandungi lapisan konfigurasi:

| Fail                                 | Tujuan                                         |
| ------------------------------------ | ---------------------------------------------- |
| `providerRegistry.ts`                | Daftar model sembang bagi katalog 352 penyedia |
| `providerModels.ts`                  | Alias model, pemetaan format                   |
| `constants.ts`                       | Tamat masa, had, kod status                    |
| `defaultThinkingSignature.ts`        | Tandatangan penaakulan Claude lalai            |
| `modelStrip.ts` (dalam perkhidmatan) | Pembuangan medan bagi setiap penyedia          |

### Skema Daftar Penyedia

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Pengesahan Zod semasa pemuatan modul memastikan semua konfigurasi penyedia adalah sah.

---

## Kekangan Prestasi

Enjin penghalaan mempunyai belanjawan prestasi yang ketat:

| Operasi                                              | Sasaran | Pengukuran                        |
| ---------------------------------------------------- | ------- | --------------------------------- |
| Penyelesaian kombo                                   | <10ms   | Untuk 50 sasaran                  |
| Semakan had kadar                                    | <1ms    | Baldi token dalam memori          |
| Sandaran keluarga model                              | <5ms    | Takrifan keluarga yang dicachekan |
| Penghantaran penghalaan permintaan                   | <2ms    | Laluan panas                      |
| **Tiada I/O menyekat dalam laluan panas penghalaan** | —       | Semuanya tak segerak              |

---

## Antipola

❌ **Panggilan DB segerak dalam `combo.ts`** — prahitung dan cache
❌ **Logik cuba semula dalam pengendali** — gunakan `retry()` daripada perkhidmatan daya tahan
❌ **Akses terus kepada konfigurasi penyedia** — gunakan pengambil `providerRegistry`
❌ **Rantaian sandaran yang dikodkan keras** — takrifkan dalam `modelFamilyFallback.ts`
❌ **Mutasi keadaan merentas permintaan serentak** — gunakan konteks berskop permintaan sahaja

---

## Menambahkan Komponen Baharu

### Menambahkan Perkhidmatan Baharu

1. Cipta `open-sse/services/[serviceName].ts` dengan tanggungjawab yang terfokus
2. Eksport fungsi pengendali utama dan sebarang pemalar
3. Tambahkan ujian unit dalam `tests/unit/services/[serviceName].test.mjs`
4. Sepadukan ke dalam saluran paip permintaan dalam `handlers/chatCore.ts` (jika berkaitan dengan penghalaan)
5. Kemas kini logik penghalaan dalam `combo.ts` jika perkhidmatan mempengaruhi pemilihan sasaran
6. Dokumentasikan dalam fail ini

### Menambahkan Pelaksana Baharu

1. Cipta `open-sse/executors/[provider].ts` yang melanjutkan `BaseExecutor`
2. Daftarkan dalam `config/providerRegistry.ts`
3. Tambahkan pada kilang `executors/index.ts`
4. Tambahkan ujian unit untuk pelaksana
5. Dokumentasikan dalam `docs/architecture/ARCHITECTURE.md`

### Menambahkan Alat MCP Baharu

1. Cipta atau kemas kini `open-sse/mcp-server/tools/[category]Tools.ts`
2. Takrifkan skema Zod untuk input
3. Daftarkan alat dalam `mcp-server/index.ts`
4. Tambahkan pada matriks skop dalam `mcp-server/auth/`
5. Tambahkan ujian unit

---

## Lihat Juga

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — seni bina peringkat tinggi
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — rujukan kejuruteraan
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — direktori demi direktori
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — pemarkahan 16 faktor
- [MCP-SERVER.md](./MCP-SERVER.md) — pelayan MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — pelayan A2A
- Sumber: `open-sse/` (400+ fail, ~143K LOC)
