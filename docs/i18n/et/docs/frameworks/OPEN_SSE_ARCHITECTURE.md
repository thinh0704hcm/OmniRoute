# open-sse Architecture (Eesti)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Lühidalt**: `open-sse/` on keskne voogedastusmootor, mis käitab OmniRoute'is iga LLM-päringut. See sisaldab ligikaudu 1400 faili, mis teostavad päringukonveieri, täiturid, teenused, MCP-serveri ja tõlkekihi. See juhend selgitab, kuidas osad omavahel kokku sobituvad.

**Lähtekood:** `open-sse/` (tööruumipakett, ligikaudu 1440 testivälist `.ts`-faili)

---

## Miks eraldi tööruumipakett?

`open-sse/` on OmniRoute'i monorepos **iseseisev tööruum** mitmel põhjusel:

1. **Taaskasutatavus** — `open-sse` avaldatakse npm-is paketina `@omniroute/open-sse`, seega saavad teised projektid seda iseseisvalt kasutada
2. **Selged piirid** — voogedastusmootor on OmniRoute'i-spetsiifilisest kasutajaliidese/andmebaasi kihist lahutatud
3. **Jõudlus** — mootoril puuduvad Next.js-i sõltuvused, mis võimaldab CLI- ja serverivabades keskkondades kiiremat külmkäivitust
4. **Versioonihaldus** — `open-sse` saab versioone avaldada oma ajakava järgi

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Tipptaseme struktuur

```
open-sse/
├── index.ts              # Avalik sisenemispunkt
├── types.d.ts            # Avalikud tüübiekspordid
├── package.json          # @omniroute/open-sse
├── config/               # Pakkujate konfiguratsioonid, konstandid, registrid
├── executors/            # Pakkujapõhised HTTP-täiturid (67 + base.ts/index.ts)
├── handlers/             # Päringutöötlejad (chatCore, responses jne)
├── lib/                  # Sisemised utiliidid
├── mcp-server/           # Model Context Protocoli server
├── services/             # Ligikaudu 298 teenusemoodulit
├── transformer/          # Responses API vormingu teisendaja
├── translator/           # Vormingute tõlkimine (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Jagatud utiliidid (logimine, vead, voog jne)
```

### Moodulite arv

| Kataloog | Faile | Otstarve |
| `executors/` | 167 | Pakkujapõhised HTTP-täiturid (ühtlustatud `DefaultExecutor`-i tehase kaudu) |
| `handlers/` | 157 | Päringute sisenemispunktid (`chatCore`, vastused, põimingud) |
| `services/` | ~536 | Marsruutimine, vahemällu salvestamine, kiiruse piiramine, värskendamine jne |
| `translator/` | 56 | Vormingute teisendamine (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP-tööriistad ja transpordid |
| `utils/` | ~108 | Läbivad utiliidid (logimine, vead, vood) |
| `config/` | ~339 | Pakkujate konfiguratsioonid, konstandid, registrid |

---

## Päringukonveier

Iga LLM-päring läbib **5-etapilise konveieri**:

```
                              ┌──────────────┐
   HTTP-päring                 │  1. MARSRUUT │   kombinatsiooni lahendamine, mudeli valimine
   (Next.js-i marsruut)        └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TÕLGI    │   vormingu teisendamine (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. KÄIVITA  │   pakkuja täitur, HTTP, korduskatsed, kaitselüliti
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. VOOG     │   SSE teisendamine, vastusurve
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. SALVESTA │   kasutuse jälgimine, kõnelogi, vigade liigitamine
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP-vastus (SSE või JSON)
```

### 1. etapp: marsruutimine (services/combo.ts)

**Sisenemispunkt**: `handleComboChat()` failis `services/combo.ts`

Lahendab päringu konkreetseks `(provider, model, account, credentials)` ennikuks:

- Otsib kombinatsiooni ID järgi (või koostab `auto/*`-mudelite jaoks virtuaalse kombinatsiooni)
- Rakendab marsruutimisstrateegiat (prioriteet, kaalutud, tsükliline jne)
- Filtreerib välja mittetöötavad pakkujad (kaitselüliti)
- Valib järgmise sobiva sihtmärgi

`auto/*`-mudelite puhul teeb see etapp ka järgmist:

- Käitab **16 teguriga hindamisalgoritmi** (`services/autoCombo/`)
- Valib tervise, maksumuse, latentsuse jne põhjal `provider+model` paari

### 2. etapp: tõlkimine (translator/)

Kui lähtevorming (nt OpenAI) erineb sihtvormingust (nt Claude), siis päring **tõlgitakse**:

- Süsteemiviip → süsteemisõnum
- Tööriistade definitsioonid → pakkujapõhine tööriistavorming
- Arutlus-/mõtlemisparameetrid → pakkujapõhised vasted
- Sõnumirollide normaliseerimine (`developer` → `system` muude kui OpenAI pakkujate puhul)

`translator/index.ts` ekspordib:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### 3. etapp: käivitamine (executors/)

**Sisenemispunkt**: `getExecutor(providerId).execute(request, options)`

Kõik pakkujad kasutavad `getExecutor()` tehase varumehhanismi kaudu `DefaultExecutor`-it (`executors/default.ts`). Täitur:

- Koostab ülesvoolu URL-i (`buildUrl()`)
- Lisab pakkujapõhised päised (`buildHeaders()`)
- Teisendab päringu keha (`transformRequest()`)
- Saadab HTTP-päringu koos korduskatsete ja eksponentsiaalse ooteaja pikendamisega
- Vajaduse korral värskendab autentimist (OAuthi pakkujad)

Kõik täiturid laiendavad `BaseExecutor`-it (`executors/base.ts`, 1170 koodirida), mis pakub järgmist:

- Ühine korduskatsete loogika
- Puhverserveri integratsioon
- Kaitselüliti integratsioon
- Kasutuse salvestamise konksud

### 4. etapp: voogedastus (utils/stream.ts)

Voogedastusega vastuste korral tagastab täitur **ReadableStream**-i. Töötleja:

- Suunab voo läbi SSE-teisenduse (`createSSETransformStreamWithLogger`)
- Rakendab südamelöögipäringuid katkenud ühenduste tuvastamiseks
- Käsitleb kliendi ühenduse katkemist sujuvalt (`pipeWithDisconnect`)
- Teisendab SSE → JSON klientide jaoks, mis voogedastust ei kasuta

Voogedastuseta vastuste korral tagastab täitur parsitud JSON-objekti, mis edastatakse muudatusteta.

### 5. etapp: salvestamine (services/usage.ts)

Pärast vastust (õnnestunud või nurjunud) salvestatakse kasutusandmed:

- vastusest pärinevad `prompt_tokens`, `completion_tokens`, `cached_tokens`
- hinnateabe põhjal arvutatud `cost_usd`
- `latency_ms`, `status`, nurjumise korral `error_class`
- salvestatakse tabelisse `usage_history`

Kõnelogi artefaktid (kui need on lubatud) kirjutatakse asukohta `${DATA_DIR}/call_logs/`.

---

## Põhifailide süvaanalüüs

### chatCore.ts (5977 rida)

**Peamine päringutöötleja**. Vaatamata suurusele on sellel selge struktuur:

```ts
// Faili chatCore.ts pseudostruktuur
export async function handleChat(request: NextRequest) {
  // 1. Autentimine + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Keha valideerimine
  const body = await parseRequestBody(request);

  // 3. Vormingu tuvastamine + teisendamine
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Kombinatsiooni marsruutimine
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Jätka järgmise sihtmärgiga
    }
  }

  // 5. Hädaolukorra varulahendus
  return await emergencyFallback(body);
}
```

Vaatamata sellele, et tegu on ühe hiiglasliku funktsiooniga, on see jaotatud **kommenteeritud osadeks**, mis vastavad viieetapilisele konveierile.

### combo.ts (4456 koodirida)

**Marsruutimismootor**, mis teisendab kombinatsiooni järjestatud sihtmärkideks.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Toetab **19 marsruutimisstrateegiat** (vt `src/shared/constants/routingStrategies.ts`):

| Strateegia          | Käitumine                                                                                 |
| ------------------- | ----------------------------------------------------------------------------------------- |
| `priority`          | Järjestatud loend, kus esimest sihtmärki eelistatakse                                     |
| `weighted`          | Tõenäosuslik valik iga sihtmärgi kaalu põhjal                                             |
| `round-robin`       | Sihtmärkide järjestikune tsükliline läbimine                                              |
| `context-relay`     | Konteksti edastamine sihtmärkide vahel                                                    |
| `fill-first`        | Kvoodi täitmine enne järgmise sihtmärgi juurde liikumist                                  |
| `p2c`               | Kahe valiku meetod                                                                        |
| `random`            | Ühtlane juhuslik valik                                                                    |
| `least-used`        | Valitakse viimati kõige vähem kasutatud sihtmärk                                          |
| `cost-optimized`    | Esmalt odavaim töökorras sihtmärk                                                         |
| `reset-aware`       | Arvestab teenusepakkuja lähtestamisakendega                                               |
| `reset-window`      | Lähtestamisaknal põhinev marsruutimine                                                    |
| `headroom`          | Esmalt suurima allesjäänud kvoodivaruga sihtmärk                                          |
| `strict-random`     | Täiesti ühtlane juhuslik valik (ilma kvaliteedipõhise kaalumiseta)                        |
| `auto`              | Kasutab 16 teguriga hindamist (`autoCombo/`)                                              |
| `lkgp`              | Esmalt viimane teadaolevalt töötav teenusepakkuja                                         |
| `context-optimized` | Sobib kõige paremini pika kontekstiga päringutele                                         |
| `fusion`            | Saadab päringu paralleelselt paneelile ja sünteesib seejärel kohtuniku abil (`fusion.ts`) |

### base.ts (1170 koodirida)

**Abstraktne täitur**, mida laiendavad kõik 107 täiturit. See sisaldab järgmist:

- `buildUrl()` — vaikimisi URL-i koostamine (alamklassid kirjutavad selle erijuhtudeks üle)
- `buildHeaders()` — vaikimisi päised (autentimine, sisutüüp)
- `transformRequest()` — vaikimisi muutmata edastamine
- `execute()` — peamine HTTP-tsükkel koos korduskatsete, eksponentsiaalse viivituse ja kaitselülitiga

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Töötleb kõiki OpenAI/Anthropicuga ühilduvaid teenusepakkujaid
  // Teenusepakkujad registreerivad konfiguratsioonid (URL, autentimine, päised), kuid jagavad täituri loogikat
}
```

Teenusepakkujapõhine käitumine (autentimispäised, baas-URL, versioonipäised) konfigureeritakse teenusepakkujate registri kaudu, mitte eraldi täituriklassidega.

````

---

## Teenused (117 moodulit)

Teenused on **kindla fookusega üheotstarbelised moodulid**, mida käsitlejad kombineerivad. Peamised kategooriad:

### Marsruutimine ja kombinatsioonid

- `combo.ts` — kombinatsioonmarsruutimisega päringute sisenemispunkt
- `services/autoCombo/` — 16 teguriga hindamine, 8 automaatse marsruutimise strateegiat
- `wildcardRouter.ts` — leiab metamärgiga marsruutide vasted (`gpt-*`)
- `modelFamilyFallback.ts` — T5 mudelipere sisene varuvariant

### Sageduse piiramine ja kvoot

- `rateLimitManager.ts` — loenduriga piiramine võtme ja teenusepakkuja kombinatsiooni kohta
- `usage.ts` — kasutuse salvestamine
- `quotaCache.ts` — mälusisesed kvooditõmmised

### Konto ja pääsutõend

- `tokenRefresh.ts` — OAuthi värskendamine vastuse 401 korral
- `accountFallback.ts` — alternatiivsele kontole lülitumine
- `sessionManager.ts` — mitmevoorulise seansi olek

### Intelligentsus

- `intentClassifier.ts` — päringu kavatsuse liigitamine
- `taskAwareRouter.ts` — marsruutimine ülesande tüübi järgi
- `thinkingBudget.ts` — mõtlemistõendite eraldamine
- `contextManager.ts` — marsruutimiskonteksti lisamine

### Tõrkekindlus

- `resilience.ts` — korduskatsete, viivituse ja katkesti orkestreerimine
- `emergencyFallback.ts` — viimase võimaluse varuvariant
- `modelDeprecation.ts` — automaatne marsruutimine järglasmudelitele

### Olek

- `signatureCache.ts` — duplikaatide eemaldamine päringu signatuuri alusel
- `volumeDetector.ts` — koormuse vähendamine
- `contextHandoff.ts` — seansi serialiseerimine

### Tihendamine

- `compression/` (alamkataloog) — täielik tihenduskonveier
- 39 faili, mis hõlmavad mootoreid, reeglikogumeid ja adaptereid

### Oskused

- (käsitletud failis [SKILLS.md](./SKILLS.md))

### Mälu

- (käsitletud failis [MEMORY.md](./MEMORY.md))

---

## Täiturid (75+ faili)

Üks fail teenusepakkuja kohta. Kõik need laiendavad klassi `BaseExecutor` ja kirjutavad erinevad osad üle.

### Levinud mustrid

Teenusepakkujad lahendatakse funktsiooniga `getExecutor(providerId)`, mis tagastab konfigureeritud täituri. OpenAI/Anthropicuga ühilduvad teenusepakkujad kasutavad täiturit `DefaultExecutor` (`executors/default.ts`). Teenusepakkujapõhine käitumine (baas-URL, autentimispäised, API versioon) konfigureeritakse kataloogis `open-sse/config/providers/`, samal ajal kui päringu keha teisendusi käsitletakse kataloogis `open-sse/translator/`.

**Kohandatud URL** määratakse teenusepakkuja konfiguratsiooni kaudu:

```ts
// Teenusepakkuja konfiguratsioon kataloogis open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Kohandatud autentimist** käsitletakse teenusepakkujate registri autentimiskonfiguratsiooni kaudu (API võti, OAuth, päiseprofiilid).

**Kohandatud päringukeha** teisendused (nt Anthropicu `system`-välja eraldamine väljast `messages`) registreeritakse iga teenusepakkuja jaoks eraldi kataloogis `open-sse/translator/`.

````

### Täiturite tehas

`executors/index.ts` ekspordib funktsiooni `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Lahendamine toimub registri `ExecutorRegistry` (`executors/registry.ts`) kaudu: iga spetsialiseeritud täitur deklareeritakse faili `executors/index.ts` sisseehitatud tabelis ja registreeritakse mooduli laadimisel funktsiooniga `registerExecutor(alias, instance)`; `getExecutor()` kontrollib registrit ja kasutab spetsialiseeritud kirjeta teenusepakkujate puhul varuvariandina memoiseeritud täiturit `DefaultExecutor`. Täielikku pseudonüümi → täituri vastendust iseloomustab etalontest `tests/unit/executor-map-golden.test.ts`.

---

## Tõlkijad

Tõlkige **3 vormingu** vahel: OpenAI, Anthropic, Gemini ning uus Responses API.

### Millal tõlkimine toimub

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Levinud teisendused:

- `OpenAI → Anthropic`: eraldi `system` väli, `x-api-key` päis
- `OpenAI → Gemini`: `messages` asemel `contents`, `systemInstruction`
- `OpenAI → Responses API`: `input` massiiv, `previous_response_id` olek

### Käsitletavad erijuhud

- `developer` roll → `system` mitte-OpenAI vormingute jaoks
- `system` roll → GLM-i/ERNIE puhul liidetakse esimese kasutaja sõnumiga
- `json_schema` → Gemini `responseMimeType` + `responseSchema`
- `tools` → teenusepakkujapõhine tööriistavorming
- Mõtlemisparameetrid (o1, Claude) → teenusepakkujapõhised vasted

---

## MCP-server

`open-sse/mcp-server/` teostab **Model Context Protocoli** serveri:

- **110 tööriista** (teenusepakkujate haldus, kombinatsioonid, mälu, vahemälu, tihendamine, puhverserver, oskused, mängustamine, pistikprogrammid, Notion, Obsidian, kohalik korpus)
- **3 transpordiviisi**: stdio, SSE, voogedastatav HTTP
- **33 ulatust** detailseks autoriseerimiseks

### Tööriistade registreerimine

Tööriistad registreeritakse eraldiseisvate failidena kataloogis `open-sse/mcp-server/tools/`, millest igaüks ekspordib nime, skeemi, töötleja ja ulatuse:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Süsteemi seisundi hetktõmmise hankimine",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transpordiviisid

```ts
// stdio (CLI-s kasutamine)
startMcpStdio(server);

// SSE (HTTP-põhine voogedastus)
startMcpSse(server, port);

// Voogedastatav HTTP (tänapäevane MCP)
startMcpStreamable(server, port);
```

### Autoriseerimine

Iga tööriistakutse läbib ulatusekontrolli (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Ebapiisav ulatus");
}
```

---

## Teisendajad

`open-sse/transformer/` teisendab **Chat Completionsi** ja **Responses API** vormingute vahel.

### Miks eraldi teisendaja?

Responses API on OpenAI uus vorming, mis toetab **olekuga vestlusi** (`previous_response_id`). Kui klient saadab Responses-päringu, teeb OmniRoute järgmist:

1. Teisendab Responses-vormingu sisemiselt Chat Completionsi vorminguks
2. Saadab selle teenusepakkujale (mis tahes teenusepakkujale, kes toetab Chat Completionsi)
3. Teisendab vastuse tagasi Responses-vormingusse
4. Voogedastab teisendatud vastuse kliendile

Teisendaja (`transformer/responsesTransformer.ts`) pakub järgmist:

```ts
createResponsesApiTransformStream(): TransformStream
```

See käsitleb järgmist:

- `response.output_item.added` sündmused
- `response.output_text.delta` sündmused
- `response.completed` sündmus
- Tööriistakutsete vastendamine (`function_call` ↔ `tool_calls`)

---

## Konfiguratsioon

`open-sse/config/` sisaldab konfiguratsioonikihti:

| Fail                          | Otstarve                                                    |
| ----------------------------- | ----------------------------------------------------------- |
| `providerRegistry.ts`         | Vestlusmudelite register 352 teenusepakkuja kataloogi jaoks |
| `providerModels.ts`           | Mudelite aliased, vormingute vastendus                      |
| `constants.ts`                | Ajalõpud, piirangud, olekukoodid                            |
| `defaultThinkingSignature.ts` | Claude'i vaikimisi mõtlemissignatuur                        |
| `modelStrip.ts` (teenustes)   | Väljade teenusepakkujapõhine eemaldamine                    |

### Teenusepakkuja registri skeem

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Zodi valideerimine mooduli laadimisel tagab, et kõik teenusepakkujate konfiguratsioonid on kehtivad.

---

## Jõudluspiirangud

Marsruutimismootoril on ranged jõudluspiirid:

| Toiming                                                                  | Siht  | Mõõtmine                             |
| ------------------------------------------------------------------------ | ----- | ------------------------------------ |
| Kombinatsiooni lahendamine                                               | <10ms | 50 sihtmärgi korral                  |
| Päringusageduse piirangu kontroll                                        | <1ms  | Mälusisene loenduripõhine ämber      |
| Mudeliperekonna varuvariandile lülitumine                                | <5ms  | Vahemällu salvestatud definitsioonid |
| Päringu marsruutimise edastamine                                         | <2ms  | Kriitiline täitmistee                |
| **Marsruutimise kriitilisel täitmisteel ei tohi olla blokeerivat I/O-d** | —     | Kõik on asünkroonne                  |

---

## Antimustrid

❌ **Sünkroonsed andmebaasikutsed failis `combo.ts`** — eelarvutage ja salvestage vahemällu
❌ **Korduskatsete loogika töötlejates** — kasutage töökindlusteenuse funktsiooni `retry()`
❌ **Otsene juurdepääs teenusepakkuja konfiguratsioonile** — kasutage `providerRegistry` getter-meetodeid
❌ **Püsikodeeritud varuahelad** — määratlege need failis `modelFamilyFallback.ts`
❌ **Oleku muutmine paralleelsete päringute vahel** — kasutage ainult päringupõhist konteksti

---

## Uue komponendi lisamine

### Uue teenuse lisamine

1. Looge selgelt piiritletud vastutusalaga fail `open-sse/services/[serviceName].ts`
2. Eksportige peamine töötleja funktsioon ja kõik konstandid
3. Lisage üksustestid faili `tests/unit/services/[serviceName].test.mjs`
4. Integreerige teenus päringukonveierisse failis `handlers/chatCore.ts` (kui see on seotud marsruutimisega)
5. Kui teenus mõjutab sihtmärgi valimist, värskendage marsruutimisloogikat failis `combo.ts`
6. Dokumenteerige selles failis

### Uue täituri lisamine

1. Looge klassi `BaseExecutor` laiendav fail `open-sse/executors/[provider].ts`
2. Registreerige see failis `config/providerRegistry.ts`
3. Lisage see faili `executors/index.ts` tehasesse
4. Lisage täituri üksustestid
5. Dokumenteerige failis `docs/architecture/ARCHITECTURE.md`

### Uue MCP-tööriista lisamine

1. Looge või värskendage faili `open-sse/mcp-server/tools/[category]Tools.ts`
2. Määratlege sisendite Zod-skeem
3. Registreerige tööriist failis `mcp-server/index.ts`
4. Lisage see kataloogis `mcp-server/auth/` asuvasse õiguste maatriksisse
5. Lisage üksustestid

---

## Vaadake ka

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — üldtaseme arhitektuur
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — tehniline teatmematerjal
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — kataloogide kaupa ülevaade
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 teguriga hindamine
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP-server
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A-server
- Lähtekood: `open-sse/` (üle 400 faili, ~143K koodirida)
