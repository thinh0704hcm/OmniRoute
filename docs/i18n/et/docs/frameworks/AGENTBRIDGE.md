# AgentBridge (Eesti)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENTBRIDGE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENTBRIDGE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENTBRIDGE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENTBRIDGE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENTBRIDGE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENTBRIDGE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENTBRIDGE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENTBRIDGE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENTBRIDGE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENTBRIDGE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENTBRIDGE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENTBRIDGE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENTBRIDGE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENTBRIDGE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENTBRIDGE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENTBRIDGE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENTBRIDGE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENTBRIDGE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENTBRIDGE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENTBRIDGE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENTBRIDGE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENTBRIDGE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENTBRIDGE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENTBRIDGE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENTBRIDGE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENTBRIDGE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENTBRIDGE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENTBRIDGE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENTBRIDGE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENTBRIDGE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENTBRIDGE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENTBRIDGE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENTBRIDGE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENTBRIDGE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENTBRIDGE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENTBRIDGE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENTBRIDGE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENTBRIDGE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENTBRIDGE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENTBRIDGE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENTBRIDGE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENTBRIDGE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENTBRIDGE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENTBRIDGE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENTBRIDGE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENTBRIDGE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENTBRIDGE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENTBRIDGE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENTBRIDGE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENTBRIDGE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENTBRIDGE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENTBRIDGE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENTBRIDGE.md)

---

AgentBridge on OmniRoute'i MITM-puhverserver (vahendusrünne ehk Man-in-the-Middle), mis püüab kinni IDE tehisintellektiagentide HTTPS-liikluse ja suunab selle ümber OmniRoute'i ühtse marsruutimismootori kaudu. See toetab **10 IDE agenti** — Antigravity, Kiro, GitHub Copilot, GHE Copilot, OpenAI Codex, Cursor, Zed, Claude Code, Open Code ja Trae (uurimisel) — tehes OmniRoute'ist turu kõige laiema katvusega MITM-puhverserveri tehisintellektipõhistele programmeerimisabilistele.

**Juhtpaneeli asukoht:** `/dashboard/tools/agent-bridge`
**Külgriba rühm:** Tööriistad (pärast jaotist Pilveagendid)
**Vaata ka:** [`TRAFFIC_INSPECTOR.md`](./TRAFFIC_INSPECTOR.md) — jälgi kogu kinnipüütud liiklust reaalajas; `docs/security/MITM-TPROXY-DECRYPT.md` (git; ei kompileerita kataloogi `/docs`) — Linuxi TPROXY läbipaistev dekrüptimisega hõiverežiim, mida juhib marsruut `/api/tools/agent-bridge/tproxy`.

---

## §1 Ülevaade

### Mis on AgentBridge?

Kui IDE agent (nt GitHub Copilot, Cursor või Claude Code) teeb API-kutse, loob see ühenduse otse ülesvoolu tehisintellektiteenuse pakkujaga (OpenAI, Anthropic jne). AgentBridge püüab selle ühenduse TLS-i tasandil läbipaistvalt kinni — ilma et agendi konfiguratsiooni oleks vaja muuta — ja kirjutab päringu ümber nii, et see läbiks OmniRoute'i.

See tähendab, et saad:

- **Suuna mis tahes agent mis tahes teenusepakkujale ümber**: kas Copilot suhtleb OpenAI-ga? Suuna see ümber Anthropic Claude'i, Gemini või mõne muu OmniRoute'i 352 teenusepakkuja juurde.
- **Rakenda mudelite vastendusi**: `gemini-3-flash` → `claude-sonnet-4.7` läbipaistvalt töötleja tasandil.
- **Jälgi kogu agentide liiklust**: iga kinnipüütud päring avaldatakse [liiklusinspektoris](./TRAFFIC_INSPECTOR.md).
- **Rakenda OmniRoute'i töökindlusfunktsioone**: kombineeritud marsruutimine, kaitselülitid, varuvariandid ja kulude jälgimine toimivad ka IDE agentide liikluse puhul.

### Positsioneerimine võrreldes turuga

| Funktsioon                           | 9router | anti-api | llm-interceptor | **OmniRoute AgentBridge** |
| ------------------------------------ | :-----: | :------: | :-------------: | :-----------------------: |
| Antigravity                          |    ✓    |    ✓     |        —        |             ✓             |
| GitHub Copilot                       |    ✓    |    ✓     |        —        |             ✓             |
| Kiro (AWS)                           |    ✓    |    ✓     |        —        |             ✓             |
| OpenAI Codex                         |    —    |    ✓     |        —        |             ✓             |
| Cursor IDE                           |    ✓    |    ✓     |        —        |             ✓             |
| Zed Industries                       |    —    |    ✓     |        —        |             ✓             |
| Claude Code                          |    —    |    —     |        ✓        |             ✓             |
| Open Code                            |    —    |    —     |        ✓        |             ✓             |
| Trae                                 |    —    |    —     |        —        |       🔍 Uurimisel        |
| Juhtpaneeli kasutajaliides           |    ✓    |    ✗     |        ✗        |             ✓             |
| Liiklusinspektor                     |    ✗    |    ✗     |        ✓        |             ✓             |
| OmniRoute'i marsruutimine            |    ✗    |    ✗     |        ✗        |             ✓             |
| Mudelite vastendamise kasutajaliides |    ✗    |    ✗     |        ✗        |             ✓             |
| Möödumisloend                        |    ✗    |    ✗     |        ✓        |             ✓             |
| Ülesvoolu CA-sertifikaat             |    ✗    |    ✗     |        ✓        |             ✓             |

---

## §2 Arhitektuur

### 2.1 Komponentide ülevaade

```
IDE agent (VS Code / Cursor / jne)
    │  HTTPS (port 443)
    ▼
/etc/hosts — 127.0.0.1 api.githubcopilot.com   ← DNS-i ümbersuunamine
    │
    ▼
src/mitm/server.cjs  (port 443, CJS-i alamprotsess)
    │  tuvastab sihtkoha Host-päise SNI järgi
    │  genereerib iga SNI jaoks AgentBridge'i CA allkirjastatud TLS-serdi
    ├── Möödaviiguloendi vaste? → TCP otseühendus (dekrüptimist ei toimu)
    ├── Sihtkoha vaste? → fetch → OmniRoute'i marsruuter (port 20128)
    │       └── handler.intercept() — TypeScript
    │               ├── maskSecrets() päringu kehal/päistel
    │               ├── TrafficBuffer.push() — avaldab liiklusinspektorile
    │               └── fetchRouter() → /v1/chat/completions
    └── Vastet pole? → TCP otseühendus (dekrüptimist ei toimu)
```

### 2.2 MITM-server (`src/mitm/server.cjs`)

Põhiline MITM-server töötab Node.js-i CJS-i alamprotsessina (et vältida olemasoleva CJS-koodibaasi ümberkirjutamist). See:

- Kuulab porti 443 (vajab eriõigusi või `authbind`/`setcap`)
- Võtab operatsioonisüsteemilt vastu CONNECT-tunneleid (`/etc/hosts`-i DNS-i ümbersuunamise kaudu)
- Genereerib iga SNI jaoks AgentBridge'i CA (`DATA_DIR/mitm/ca.crt`) allkirjastatud TLS-serdid
- Tuvastab sihtagendi Host-päise järgi registri `targets/index.ts` kaudu
- Edastab päringud TypeScripti töötlejakihile HTTP kaudu aadressil `http://127.0.0.1:20128`

`TARGET_HOSTS` laaditakse failist `DATA_DIR/mitm/targets.json` (mille `targets/index.ts` käivitamisel kirjutab), võimaldades dünaamilisi uuendusi ilma CJS-serverit taaskäivitamata.

> **Juur-CA mudel (#6684).** Eespool kirjeldatud iga SNI jaoks CA allkirjastatud serdi
> mudel on muudatuses #6684 lisatud püsiv juur-CA mudel (`src/mitm/cert/rootCa.ts` +
> `src/mitm/_internal/rootCaShim.cjs`, mis taaskasutab TPROXY jaoks failis
> `src/mitm/tproxy/dynamicCert.ts` juba tõestatud CA-/lõppserdi krüptograafiat) — see asendab
> varasema ühe staatilise iseallkirjastatud lõppserdi (`src/mitm/cert/generate.ts`, mille
> ulatus piirdub endiselt ainult antigravity hostidega), mille olemasolule viitab kettal olev
> failipaar `server.crt`/`server.key`. **Migreerimiskäitumine**: värske install (varasema
> `server.crt`-ita) saab juur-CA mudeli automaatselt; install, mis juba
> usaldas vana staatilist lõppserti, jätkab selle kasutamist seni, kuni operaator määrab
> `MITM_ROOT_CA_ENABLED=true` ja taaskäivitab silla (`src/mitm/cert/migration.ts`
> on puhas otsustusfunktsioon — usaldatud MITM-i CA, mis saab allkirjastada lõppserdi
> **mis tahes** hostile, on oluliselt võimsam kui vana fikseeritud SAN-idega lõppsert, mistõttu
> juba usaldatud installi puhul ei toimu üleminek kunagi vaikimisi). CA-sert installitakse
> samasse usaldusväärsuse hoidla pessa `omniroute-mitm.crt`, mida kasutas vana lõppsert
> (`cert/install.ts::installCaCert`) — topeltusalduse puhastamist pole vaja.

### 2.3 Töötleja baasklass (`src/mitm/handlers/base.ts`)

Kõik agentide töötlejad laiendavad klassi `MitmHandlerBase`:

```ts
export abstract class MitmHandlerBase {
  abstract readonly agentId: AgentId;

  abstract intercept(
    req: IncomingMessage,
    res: ServerResponse,
    body: Buffer,
    mappedModel: string
  ): Promise<void>;

  // Kaitstud abimeetodid: fetchRouter, pipeSSE, hookBufferStart, hookBufferUpdate
}
```

Iga töötleja kutsub enne proksimist välja `hookBufferStart()` ja pärast lõpetamist `hookBufferUpdate()`. Need lisavad kirjed `InterceptedRequest` globaalsesse puhvrisse `globalTrafficBuffer` (vt [Liiklusinspektor](./TRAFFIC_INSPECTOR.md) §4).

### 2.4 Sihtkohtade register (`src/mitm/targets/`)

Igal agendil on deklaratiivne sihtkohafail:

```ts
// src/mitm/targets/copilot.ts
export const COPILOT_TARGET: MitmTarget = {
  id: "copilot",
  name: "GitHub Copilot",
  hosts: ["api.githubcopilot.com", "copilot-proxy.githubusercontent.com"],
  port: 443,
  endpointPatterns: ["/chat/completions", "/v1/chat/completions"],
  defaultModels: [{ id: "gpt-4o", name: "GPT-4o", alias: "gpt-4o" }],
  handler: () => import("../handlers/copilot"),
  riskNoticeKey: "providers.riskNotice.oauth",
};
```

Register (`targets/index.ts`) ekspordib `ALL_TARGETS` ja loob käivitamisel faili `DATA_DIR/mitm/targets.json`.

### 2.5 Otseühendus ja möödaviiguloend (`src/mitm/passthrough.ts`)

**Möödaviiguloend** (kontrollitakse esimesena ja sellel on sihtkoha vaste ees prioriteet):

- Vaikemustrid: panganduse hostid, `.gov.`, OAuth-/SSO-pakkujad (Okta, Auth0) jne
- Kasutaja mustrid: talletatakse andmebaasitabelis `agent_bridge_bypass`
- Möödaviidavad hostid saavad läbipaistva TCP-tunneli — TLS-i **ei dekrüptita kunagi**

**Vaikimisi otseühendus** (sihtkoha vastet pole ja host pole möödaviiguloendis):

- Saab samuti TCP-tunneli — ühendusi ei katkestata kunagi
- Takistab AgentBridge'il üldist süsteemi HTTPS-liiklust häirimast

Marsruutimise prioriteet:

```
möödaviiguloend → sihtkoha vaste → otseühendus
```

### 2.6 Ülesvoolu CA-sert (`src/mitm/upstreamTrust.ts`)

Kohandatud CA-ga ettevõtte võrgukeskkondade jaoks:

```bash
AGENTBRIDGE_UPSTREAM_CA_CERT=/path/to/corporate-ca.pem
```

Kui see on määratud, konfigureerib see `undici` globaalse dispetšeri täiendava CA-serdiga, võimaldades AgentBridge'il jõuda ülesvoolu teenusepakkujateni ettevõtte TLS-i lõpetavate prokside kaudu.

### 2.7 Saladuste maskeerimine (`src/mitm/maskSecrets.ts`)

Sõltumatut puhasruumi skannerit rakendatakse päringu kehadele ja mandaadipäistele
**enne**, kui need jõuavad liiklusinspektori puhvrisse või mis tahes logisse. See teeb ühe lineaarse läbimise:

- Eesliitega `sk-` / `ak-` / `pk-` sõned (OpenAI/Anthropicu laadis)
- RFC 6750 `Authorization: Bearer <token>` mandaadid, eelistades terviklikku sõnet
- Üldised pikad läbipaistmatud sõned (≥40 märki), sealhulgas punktidega ja täitemärkidega vormid

`sanitizeHeaders()` teisendab allesjäetud nimed väiketähtedeks, ühendab massiivväärtused deterministlikult, eemaldab
ühise hüppelt-hüppele-/raamistuspäiste keeluloendi (sealhulgas proksi autentimise), redigeerib täielikult päised `cookie` ja
`set-cookie` ning delegeerib mandaadiväärtused skannerile.

---

## §3 Seadistamine

### 3.1 MITM-serveri käivitamine/peatamine

Kasutage AgentBridge'i serverikaarti aadressil `/dashboard/tools/agent-bridge`:

| Toiming                | Kirjeldus                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------- |
| Käivita server         | Käivitab `src/mitm/server.cjs` pordil 443                                           |
| Peata server           | Sulgeb alamprotsessi korrektselt                                                    |
| Taaskäivita server     | Peatab ja käivitab uuesti (rakendab sihtmärkide muudatused)                         |
| Usalda sertifikaati    | Installib `DATA_DIR/mitm/ca.crt` operatsioonisüsteemi usaldusväärsete hoidlasse     |
| Laadi sertifikaat alla | Laadib faili `ca.crt` käsitsi installimiseks                                        |
| Loo sertifikaat uuesti | Loob uue CA-võtmepaari (kõik olemasolevad agendipõhised sertifikaadid tühistatakse) |

### 3.2 Sertifikaadi usaldamine

AgentBridge'i CA-sertifikaat peab olema operatsioonisüsteemis usaldatud, enne kui IDE-d MITM-ühenduse aktsepteerivad.

**Linux (NSS — Chrome/Firefox):**

```bash
certutil -A -d sql:$HOME/.pki/nssdb -n "OmniRoute AgentBridge" -t CT,, -i ~/.omniroute/mitm/ca.crt
```

**macOS (Keychain):**

```bash
sudo security add-trusted-cert -d -r trustRoot \
  -k /Library/Keychains/System.keychain ~/.omniroute/mitm/ca.crt
```

**Windows (certmgr):**

```powershell
certutil -addstore -f Root $env:USERPROFILE\.omniroute\mitm\ca.crt
```

Või kasutage töölaual nuppu „Usalda sertifikaati“ (see käivitab teie operatsioonisüsteemile sobiva käsu ja küsib vajaduse korral sudo parooli).

#### Electronil põhinevad IDE-d eiravad operatsioonisüsteemi usaldushoidlat (`NODE_EXTRA_CA_CERTS`)

Mõned IDE-d — eelkõige **Antigravity IDE** ja teised Electronil / VS Code'il põhinevad rakendused — sisaldavad oma Node.js-i käituskeskkonda, mis **ei kasuta operatsioonisüsteemi usaldushoidlat** väljaminevate `fetch`-/HTTPS-päringute jaoks. CA usaldamisest operatsioonisüsteemi/NSS-i tasemel piisab IDE loomuliku **taustsüsteemi** jaoks (nt Go keeleserver, mis kasutab operatsioonisüsteemi CA-paketti), kuid **Electroni kasutajaliideses** TLS siiski ebaõnnestub — rakendus näib olevat _välja logitud_ või kuvab _„ühenduse tõrke“_, kuigi MITM-logis on näha, et taustsüsteemi alglaadimispäringud tagastavad `200`. Vajalikud on kaks sammu ja mõlemad on olulised:

1. Suunake käituskeskkond selgesõnaliselt CA-le:
   ```bash
   export NODE_EXTRA_CA_CERTS=/path/to/omniroute-agentbridge-ca.crt
   ```
2. **Käivitage IDE sellest kestast.** Selle käivitamisel töölauaikoonist / Dockist / menüüst Start ei pärita kesta eksporditud muutujaid ning `~/.config/environment.d/*.conf` rakendub alles pärast uut graafilist sisselogimist. Sulgege IDE esmalt täielikult — Electroni üksikeksemplari lukk tähendab, et teine käivitus toob lihtsalt olemasoleva protsessi fookusesse ja uut keskkonda eiratakse.

Ülaltoodud operatsioonisüsteemi usalduse ja NSS-i samm on endiselt vajalik (mõne autentimisvoo kasutatav Chromiumi võrgupinu loeb kasutajapõhist NSS-i hoidlat ning sisaldab oma staatilisi pinne domeeni `*.googleapis.com` jaoks, mille kohalikult usaldatud CA alistab). `NODE_EXTRA_CA_CERTS` katab lisaks Node'i `fetch`-tee.

### 3.3 DNS-i marsruutimine

Iga pealtkuulatava agendi API host(id) peavad lahenema aadressiks `127.0.0.1`. AgentBridge haldab `/etc/hosts` kirjeid automaatselt, kui lülitate seadistusviisardis agendi DNS-i sisse või välja.

Näited GitHub Copiloti `/etc/hosts` kirjetest:

```
127.0.0.1 api.githubcopilot.com
127.0.0.1 copilot-proxy.githubusercontent.com
```

### 3.4 Mudelite vastendamine

Kasutage iga agendi kaardil olevat mudelite vastendustabelit, et määratleda lähte- → sihtmudeli vastendused:

| Lähte­mudel (agendi omamudel) | Sihtmudel (OmniRoute) |
| ----------------------------- | --------------------- |
| `gpt-4o`                      | `claude-sonnet-4.7`   |
| `*` (metamärk)                | `claude-haiku-4.7`    |

Metamärk `*` vastendab kõik tundmatud mudelid määratud sihtmudelile. Vastendused talletatakse tabelis `agent_bridge_mappings`.

> **Nõuanne — tuvastage agendi tegelikud mudeli-ID-d.** IDE võib saata mudelinimesid, mis erinevad
> kasutajaliidese siltidest ja muutuvad põhiversioonide vahel. Näiteks saadab **Antigravity 2**
> võrgu kaudu `gemini-3.1-pro-low`, `gemini-pro-agent` ja `gemini-3.1-flash-lite`, mitte vanemates
> dokumentides näidatud `gemini-2.5-pro`. Saatke üks vestluspäring ilma sobiva vastenduseta: MITM
> logib täpse sissetuleva väärtuse `model:` ja edastab päringu muutmata kujul. Vastendage see
> literaalväärtus ning järgmine päring pealtkuulatakse ja marsruuditakse teie sihtmudelile.

### 3.5 Riskiteade

AgentBridge püüab kinni identimisteabe (OAuthi tõendid, API-võtmed), mida IDE kasutab autentimiseks väliste teenusepakkujate juures. Need **maskeeritakse enne logimist** (vt §2.7), kuid on OmniRoute'i MITM-kihile nähtavad. Iga agendi esmakordsel aktiveerimisel kuvatakse suletav riskiteate modaalaken.

### 3.6 Hooldus ja diagnostika

Töölaud sisaldab kaarti **Hooldus ja diagnostika** (`AgentBridgeMaintenanceCard`, asukohas `src/app/(dashboard)/dashboard/tools/agent-bridge/components/`), mis teeb kasutajaliideses kättesaadavaks operatiivsed MITM-marsruudid, millel varem kasutajaliides puudus. Selle alapealkiri on: _„Testige hõivekonveierit, tühistage süsteemi allesjäänud olek ja teisaldage seadistus masinate vahel.“_ Kaardi kliendi abifunktsioonid asuvad failis `src/lib/inspector/agentBridgeMaintenanceApi.ts`.

| Nupp                   | Marsruut                               | Mida see teeb                                                                                                                                                                                                  |
| ---------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Diagnostika**        | `GET /api/tools/agent-bridge/diagnose` | Käivitab hõivekonveieri enesetesti ja kuvab iga kontrolli kohta aruande (✓/✗ + parandamissoovitus).                                                                                                            |
| **Paranda**            | `POST /api/tools/agent-bridge/repair`  | Võtab tagasi krahhi või SIGKILL-i järel jäänud hüljatud MITM-i süsteemioleku (DNS-i võltsimiskirjed, juur-CA, süsteemipuhverserver). Idempotentne — puhta oleku korral teatab „Midagi pole parandada”.         |
| **Eemalda CA**         | `DELETE /api/tools/agent-bridge/cert`  | Eemaldab MITM-i juur-CA usalduse ja kustutab selle OS-i usaldushoidlast (selgesõnaline, idempotentne). Kuvatakse ainult siis, kui CA on parajasti usaldatud; nõuab tekstisisest kinnitust „Kas eemaldada CA?”. |
| **Ekspordi seadistus** | `GET /api/tools/agent-bridge/config`   | Laadib alla porditava seadistuse JSON-i (vt §3.7).                                                                                                                                                             |
| **Impordi seadistus**  | `POST /api/tools/agent-bridge/config`  | Laadib üles varem eksporditud seadistuse JSON-i (vt §3.7).                                                                                                                                                     |

**Diagnostikakontrollid** (`summarizeDiagnostics()` failis `src/mitm/inspector/diagnostics.ts`). Marsruut käivitab iga kontrolli jaoks kõrvalmõjudega proovi ja edastab tõeväärtused puhtale kokkuvõttefunktsioonile; tagastatakse üks `healthy` hinnang koos iga tõrke parandamissoovitusega:

| Kontrolli nimi     | Mida see kontrollib                                     | Soovitus tõrke korral                                                                                                                                                  |
| ------------------ | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server-running`   | MITM-serveri protsess on aktiivne                       | „MITM-server ei tööta. Käivitage see vahekaardilt AgentBridge.”                                                                                                        |
| `server-reachable` | MITM-server võtab oma pordil ühendusi vastu (TCP-proov) | „MITM-server ei võta oma pordil ühendusi vastu. Kontrollige, et port oleks vaba ja et teil oleksid selle sidumiseks vajalikud õigused.”                                |
| `cert-exists`      | MITM-sertifikaat on kettale genereeritud                | „MITM-sertifikaati pole veel genereeritud. Genereerige see vahekaardilt AgentBridge.”                                                                                  |
| `cert-trusted`     | MITM-i juur-CA asub OS-i usaldushoidlas                 | „OS-i usaldushoidla ei usalda MITM-i juur-CA-d, mistõttu TLS-i pealtkuulamine nurjub. Usaldage sertifikaati vahekaardil AgentBridge.”                                  |
| `dns-configured`   | Siht-hostinimed on failis `/etc/hosts` võltsitud        | „Siht-hostinimed pole failis /etc/hosts võltsitud, mistõttu liiklus ei jõua kunagi puhverserverisse. Lubage DNS nende agentide jaoks, kelle liiklust soovite hõivata.” |

**Hüljatud oleku bänner:** kui leht tuvastab krahhi järel jäänud oleku (DNS-i võltsimine / CA / süsteemipuhverserver), kuvab kaart merevaigukollase bänneri — _„Eelmine seanss jättis süsteemioleku alles (DNS-i võltsimine, CA või süsteemipuhverserver). Käivitage selle puhastamiseks parandamine.”_ — ja tõstab esile nupu **Paranda**. `Repair` on rakenduskihi vaste ProxyBridge'i lipule `--cleanup` (see delegeerib funktsioonile `repairMitm()` failis `src/mitm/manager.ts`).

> MITM-i juur-CA jäetakse peatamiste ja käivitamiste vahel installituks, et vältida
> korduvaid sudo viipasid (sama käitumine nagu mitmproxy/Charlesi puhul), seega on selle eemaldamine
> selgesõnaline toiming **Eemalda CA**, mitte midagi, mis toimuks peatamisel automaatselt.

### 3.7 Porditava seadistuse import/eksport

AgentBridge saab serialiseerida **operaatori häälestatava** oleku versioonitud JSON-objektiks, et seadistust saaks eri masinates paljundada. Serialiseerija on `src/lib/inspector/configPortability.ts` (`exportConfig()` / `importConfig()`) ja seda valideerib `AgentBridgeConfigSchema`.

Eksport sisaldab täpselt kolme osa (sisseehitatud vaikeväärtusi tahtlikult **EI** ekspordita, et importimine neid kunagi ei dubleeriks ega nendega vastuollu läheks):

| Väli             | Allikas                                                         | Märkused                                                                     |
| ---------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `bypassPatterns` | kasutaja määratud möödaviigumustrid (`agent_bridge_bypass`)     | panga/riigiasutuste/okta vaikemustrid jäetakse välja                         |
| `customHosts`    | Traffic Inspectori kohandatud hostid (`inspector_custom_hosts`) | igaüks: `{ host, kind: "llm"\|"app"\|"custom", label? }`                     |
| `agentMappings`  | agendipõhised mudelivastendused (`agent_bridge_mappings`)       | `{ [agentId]: [{ source, target }] }` iga agendi kohta, millel on vastendusi |

```jsonc
// GET /api/tools/agent-bridge/config
{
  "version": 1,
  "bypassPatterns": ["*.internal.example.com"],
  "customHosts": [{ "host": "api.example.com", "kind": "llm", "label": null }],
  "agentMappings": {
    "copilot": [{ "source": "gpt-4o", "target": "claude-sonnet-4.7" }],
  },
}
```

**Importimise käitumine** (`POST /api/tools/agent-bridge/config`): möödaviigumustrid ja agendipõhised vastendused **asendatakse täielikult**; kohandatud hostid lisatakse **idempotentselt** (`INSERT OR IGNORE`). Vastus teatab, mitu iga tüübi kirjet rakendati:

```jsonc
{ "ok": true, "bypassPatterns": 1, "customHosts": 1, "agents": 1 }
```

Mida konfiguratsioon **EI** sisalda: serveri töötamise olek, sertide asukohad, agendipõhine DNS-i olek, ülesvoolu CA asukoht ja TPROXY sätted — need on hosti/käituskeskkonna olek, mitte porditavad eelistused.

---

## §4 Agendipõhine ülevaade

| #   | Agent              | Olek         | Pealtkuulatavad hostid                                             | Autentimise tüüp |
| --- | ------------------ | ------------ | ------------------------------------------------------------------ | ---------------- |
| 1   | **Antigravity**    | ✅ Toetatud  | `daily-cloudcode-pa.googleapis.com`, `cloudcode-pa.googleapis.com` | Firebase OAuth   |
| 2   | **Kiro (AWS)**     | ✅ Toetatud  | `prod.kiro.aws`, `dev.kiro.aws`                                    | AWS SigV4        |
| 3   | **GitHub Copilot** | ✅ Toetatud  | `api.githubcopilot.com`, `copilot-proxy.githubusercontent.com`     | GitHub OAuth     |
| 4   | **OpenAI Codex**   | ✅ Toetatud  | `api.openai.com` (Codexi teed), `chatgpt.com`                      | OpenAI võti      |
| 5   | **Cursor IDE**     | ✅ Toetatud  | `api2.cursor.sh`, `api.cursor.sh`                                  | Cursor OAuth     |
| 6   | **Zed Industries** | ✅ Toetatud  | `api.zed.dev`, `llm.zed.dev`                                       | Zed OAuth        |
| 7   | **Claude Code**    | ✅ Toetatud  | `api.anthropic.com` (valikuline)                                   | Anthropicu võti  |
| 8   | **Open Code**      | ✅ Toetatud  | `openrouter.ai`, `api.openai.com` (zeni teed)                      | API-võti         |
| 9   | **Trae**           | 🔍 Uurimisel | Määramata — vt §8                                                  | Määramata        |

### Seadistusviisardi sammud (agendi kohta)

Igal agendikaardil on kolmeastmeline seadistusviisard:

1. **Kontrolli eeltingimusi** — kas server töötab? Kas sertifikaati usaldatakse? Kas IDE on installitud (automaatselt tuvastatud)?
2. **Luba DNS** — lisab kirjed faili `/etc/hosts` (nõuab sudot). Näitab täpselt, millised read lisatakse.
3. **Vastenda mudelid** — valikuline mudelite vastendustabel. Metamärgid on lubatud.

### Agendi tuvastamine

Agentide 1–8 puhul üritab AgentBridge IDE installi automaatselt tuvastada:

```ts
export async function detectAgent(agentId: AgentId): Promise<DetectionResult>;
// Tagastab: { installed: boolean, version?: string, path?: string }
```

Tuvastamiseks kasutatakse operatsioonisüsteemipõhiseid teid ja binaarfailide kontrolle (nt Copiloti puhul `code --list-extensions | grep github.copilot`, Antigravity puhul `~/.config/antigravity/`).

---

## §5 Turvalisus

### Rakendatud ranged reeglid

| Reegel                                | Rakendamine                                                                                      |
| ------------------------------------- | ------------------------------------------------------------------------------------------------ |
| **#12** `sanitizeErrorMessage`        | Kõik töötlejate vead puhastatakse enne vastusesse või puhvrikirjesse lisamist                    |
| **#13** Keskkonna edastamine shellile | Faili `/etc/hosts` muutmisel kasutatakse suvandit `env` — teid ei interpoleerita stringides      |
| **#15 + #17** `isLocalOnlyPath()`     | `/api/tools/agent-bridge/` on LOCAL_ONLY + SPAWN_CAPABLE — loopback jõustatakse enne autentimist |

### Tundlike hostide möödaviiguloend

Möödaviiguloend tagab, et finantsasutuste, OAuthi/SSO pakkujate ja muude tundlike hostide liiklust **ei dekrüptita kunagi**. Nende TLS-liiklus läbib süsteemi läbipaistva TCP-tunnelina — OmniRoute ei näe kunagi avateksti.

Vaikimisi möödaviigumustrid hõlmavad järgmist:

- `*.bank.*`, `*.gov.*` (finantsasutused/valitsusasutused)
- `*.okta.com`, `*.auth0.com`, `*.microsoft.com` (SSO/identiteet)
- `*.apple.com`, `*.icloud.com` (Apple'i süsteemiteenused)

Kasutaja lisatud möödaviigumustrid salvestatakse tabelisse `agent_bridge_bypass` ja need on kõigest muust ülimuslikud.

### Saladuste maskeerimine

Rakendatakse funktsiooni `maskSecrets()` failist `src/mitm/maskSecrets.ts`:

- Igale päringu kehale enne `TrafficBuffer.push()` väljakutsumist
- Igale päisele enne logimist või leviedastust

Mustrid: eesliitega `sk-`/`ak-`/`pk-` pääsmikud, `Bearer`-pääsmikud ja üldised vähemalt 40 märgi pikkused pääsmikud.

### Ülesvoolu CA-sertifikaat

Kui `AGENTBRIDGE_UPSTREAM_CA_CERT` on määratud, loetakse fail käivitamisel sisse. Kui tee on olemas, kuid fail pole loetav, logib AgentBridge selge vea ja keeldub käivitumast (see hoiab ettevõttekeskkondades ära varjatud TLS-tõrked).

### Teadaolevad piirangud

- **Port 443 nõuab eriõigusi**: Linuxis vajab AgentBridge Node'i binaarfailile käsku `setcap 'cap_net_bind_service=+ep'` või käitamist `authbind` kaudu. Seadistusviisard kuvab operatsioonisüsteemipõhised juhised.
- **IDE tuleb taaskäivitada**: pärast DNS-i ümbersuunamist tuleb IDE taaskäivitada, et uus hostinime lahendus jõustuks.
- **Püsikodeeritud OAuthi pääsmikud**: mõned agendid (Kiro, Antigravity) talletavad OAuthi värskenduspääsmikke kohapeal. Need on AgentBridge'i jaoks läbipaistvad — see näeb igas päringus Bearer-pääsmikku, mis enne logimist maskeeritakse.
- **Electroni kasutajaliidesed vajavad muutujat `NODE_EXTRA_CA_CERTS`**: IDE-d, mille kasutajaliides töötab komplekti kuuluvas Node'i/Electroni käituskeskkonnas, eiravad operatsioonisüsteemi/NSS-i usaldushoidlat ja need tuleb käivitada shellist, kus `NODE_EXTRA_CA_CERTS` on määratud (vt §3.2). Puuduva muutuja sümptom: IDE tagasüsteem autentib (MITM näitab vastuseid `200`), kuid kasutajaliides jääb väljalogituks.
- **Sama IDE mitu installi on üksteisest sõltumatud**: süsteemiinstall (nt `/usr/share/antigravity/antigravity`) ja kasutaja kohalik „Full”-install (nt `~/AntigravityIDE_Full/antigravity-ide`) on eraldi protsessid oma käituskeskkondadega — igaüks tuleb uuesti käivitada koos sisestatud CA-ga. Enne taaskäivitamist tuvastage binaarfaili tee järgi, milline neist töötab.
- **Identiteedi määrab agendi süsteemiviip, mitte marsruuditud mudel**: kui vastendate agendi mudeli ümber teisele pakkujale, väidab vastus endiselt, et tal on agendi algne identiteet (nt Antigravity vastab „Minu mootoriks on Gemini”), sest IDE sisestab selle süsteemiviipa. Kontrollige tegelikku tagasüsteemi tabelitest `call_logs` / `proxy_logs` (`provider`, `model`, `target_format`), mitte mudelilt küsides, kes ta on.

---

## §6 Tõrkeotsing

### Pordi 443 konflikt

Kui mõni teine protsess juba kuulab porti 443 (veebiserver, VPN jne):

```bash
lsof -i :443          # leia protsess
sudo fuser -k 443/tcp  # sundlõpeta (kasuta ettevaatlikult)
```

Teise võimalusena seadista AgentBridge'i sätetes mitteprivilegeeritud port ning lisa `iptables` / `pf` ümbersuunamisreeglid.

### Sertifikaati ei usaldata

Kui IDE kuvab pärast AgentBridge'i käivitamist TLS-i tõrkeid:

1. Kontrolli, kas sertifikaat paigaldati: `security find-certificate -c "OmniRoute AgentBridge"` (macOS) või `certutil -L -d sql:$HOME/.pki/nssdb` (Linux/NSS)
2. Mõned rakendused haldavad oma usaldushoidlat (Firefox, Chrome Linuxis). Käivita „Trust Cert” uuesti ja kontrolli NSS-i/Firefoxi-spetsiifilist sertifikaadihoidlat.
3. Pärast usalduse määramist taaskäivita IDE — pooleliolevad TLS-seansid kasutavad vana usaldusolekut.

### IDE-st on välja logitud / „ühenduse viga” hoolimata usaldatud CA-st

Sümptom: pärast DNS-i ümbersuunamist ja CA usaldamist avaneb Electronil põhinev IDE (nt Antigravity)
**väljalogituna** või kuvab autentimis-/ühendusvea, kuigi MITM-i logi näitab, et
alglaadimiskutsed (`loadCodeAssist`, `fetchAvailableModels`, …) tagastavad `200`.

Põhjus: IDE-ga **kaasasolev Node'i/Electroni käituskeskkond eirab operatsioonisüsteemi usaldushoidlat**. Natiivne
taustateenus (Go keeleserver) usaldab operatsioonisüsteemi CA-d ja autendib, kuid Electroni kasutajaliides
seda ei tee — seetõttu peab kasutajaliides ennast võrguühenduseta olevaks.

Lahendus (mõlemad toimingud): ekspordi `NODE_EXTRA_CA_CERTS=<ca.crt>` **ja käivita IDE uuesti sellest
kestast**, mitte töölauaikoonilt. Sulge IDE esmalt täielikult — Electroni üksikeksemplari lukk tähendab,
et teine käivitus lihtsalt aktiveerib olemasoleva protsessi ning uut keskkonda eiratakse. Vaata §3.2.
See vastab avalikule ülesvoolu veateatele, mille kohaselt töötab eraldiseisev agent MITM-i kaudu, kuid IDE
variant nurjub sama seadistuse korral.

### DNS pole levinud

Kontrolli, kas `/etc/hosts` värskendati:

```bash
grep "omniroute\|127.0.0.1.*github\|127.0.0.1.*cursor" /etc/hosts
```

Tühjenda DNS-i vahemälu:

```bash
# macOS
sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder
# Linux (systemd-resolved)
sudo systemctl restart systemd-resolved
# Windows
ipconfig /flushdns
```

### IDE-d ei tuvastatud

Automaatne tuvastamine kasutab tavapäraseid paigaldusteid. Kui tuvastamine nurjub, kuid IDE on paigaldatud:

- Kontrolli, kas IDE kahendfail asub ebastandardses asukohas
- Seadistusviisard töötab endiselt — tuvastamise nurjumine tähendab vaid seda, et märk ei kuva paigaldusteed

### Töötleja tõrked (ülesvoolust toomine nurjub)

Kui AgentBridge püüab päringud kinni, kuid kõik päringud nurjuvad:

1. Kontrolli aadressil `/dashboard/providers`, et ühendatud oleks vähemalt üks teenusepakkuja
2. Kontrolli OmniRoute'i serveri logisid: `APP_LOG_LEVEL=debug` failis `.env`
3. Kontrolli, et `OMNIROUTE_BASE_URL` osutaks õigele ruuteri lõpp-punktile (vaikeväärtus: `http://127.0.0.1:20128`)

---

## §7 API viitedokumentatsioon

Kõik marsruudid on `LOCAL_ONLY` (ainult tagasisideahela kaudu, jõustatakse enne autentimist) ja `SPAWN_CAPABLE`. Vaata `src/server/authz/routeGuard.ts`.

Baastee: `/api/tools/agent-bridge/`

| Meetod              | Tee                                            | Kirjeldus                                                                                                                        |
| ------------------- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| GET                 | `/api/tools/agent-bridge/state`                | Serveri globaalne olek + iga agendi tuvastus/olek                                                                                |
| GET                 | `/api/tools/agent-bridge/agents`               | Registreeritud agentide loend (id, nimi, hostid, kasutatavus, olek)                                                              |
| GET                 | `/api/tools/agent-bridge/agents/{id}`          | Ühe agendi olek (sihtkonfiguratsioon + tuvastus + salvestatud olek)                                                              |
| PATCH               | `/api/tools/agent-bridge/agents/{id}`          | Agendi `setup_completed` väärtuse uuendamine                                                                                     |
| GET                 | `/api/tools/agent-bridge/agents/{id}/detect`   | Agendi tuvastuskontrolli käivitamine (`installed`, `version?`, `path?`)                                                          |
| POST                | `/api/tools/agent-bridge/agents/{id}/dns`      | Agendi DNS-i lubamine/keelamine (`{enabled: boolean}`)                                                                           |
| GET                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Agendi mudelivastendused                                                                                                         |
| PUT                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Mudelivastenduste asendamine                                                                                                     |
| POST                | `/api/tools/agent-bridge/server`               | Serveri käivitamine/peatamine/taaskäivitamine (`action: "start"\|"stop"\|"restart"\|"trust-cert"\|"regenerate-cert"`)            |
| GET                 | `/api/tools/agent-bridge/cert`                 | Serdi olek (`exists`, `trusted`, `path`)                                                                                         |
| POST                | `/api/tools/agent-bridge/cert`                 | MITM-i juur-CA usaldamine (installimine)                                                                                         |
| DELETE              | `/api/tools/agent-bridge/cert`                 | MITM-i juur-CA usalduse eemaldamine — idempotentne (vt §3.6)                                                                     |
| POST                | `/api/tools/agent-bridge/cert/regenerate`      | Iseallkirjastatud MITM-serdi uuesti genereerimine                                                                                |
| GET                 | `/api/tools/agent-bridge/cert/download`        | PEM-serdi voogedastus allalaadimiseks                                                                                            |
| GET                 | `/api/tools/agent-bridge/bypass`               | Möödaviigumustrite loend (`default` + `user`)                                                                                    |
| POST                | `/api/tools/agent-bridge/bypass`               | Kasutaja määratud möödaviigumustrite täielik asendamine                                                                          |
| DELETE              | `/api/tools/agent-bridge/bypass?pattern=...`   | Ühe kasutaja määratud möödaviigumustri eemaldamine                                                                               |
| GET                 | `/api/tools/agent-bridge/diagnose`             | Hõivekonveieri enesetest (vt §3.6)                                                                                               |
| POST                | `/api/tools/agent-bridge/repair`               | Orvuks jäänud MITM-i süsteemioleku tagasivõtmine (vt §3.6)                                                                       |
| GET                 | `/api/tools/agent-bridge/config`               | Porditava konfiguratsiooni eksportimine JSON-ina (vt §3.7)                                                                       |
| POST                | `/api/tools/agent-bridge/config`               | Porditava konfiguratsiooni importimine JSON-ina (vt §3.7)                                                                        |
| GET                 | `/api/tools/agent-bridge/upstream-ca`          | Konfigureeritud ülesvoolu CA tee hankimine                                                                                       |
| POST                | `/api/tools/agent-bridge/upstream-ca`          | Ülesvoolu CA tee valideerimine + püsiv salvestamine                                                                              |
| POST                | `/api/tools/agent-bridge/upstream-ca/test`     | Ülesvoolu CA tee ainult valideerimine (proovikäivitus) — ei salvestata püsivalt                                                  |
| GET / POST / DELETE | `/api/tools/agent-bridge/tproxy`               | TPROXY läbipaistva dekrüpteerimisega hõiverežiim — vt `docs/security/MITM-TPROXY-DECRYPT.md` (git; ei kompileerita `/docs` alla) |

Täielikud OpenAPI skeemid: `docs/openapi.yaml` → silt `AgentBridge`.

---

## §8 Teekaart

### Trae uurimine

Trae on suhteliselt uus tehisintellektil põhinev programmeerimisassistent. Enne töötleja rakendamist:

1. Tuvastage binaarfail/laiendus VS Code'i / JetBrainsi poodides või eraldiseisva rakendusena
2. Jäädvustage liiklus mitmproxy abil, et tuvastada API hostid ja lõpp-punktide struktuur
3. Tehke kindlaks autentimismehhanism
4. Hinnake kasutustingimuste ja API tuvastatavuse põhjal, kas jätkata või mitte

Kuni uurimine on lõpule viidud, kuvatakse Trae kaardil juhtpaneelil märk „Uurimisel“ koos lingiga „Teata teostatavusest“. Töötleja toes failis `src/mitm/handlers/trae.ts` väljastab struktureeritud vea `Pole veel rakendatud`.

### Ootel olevad agendid (MITM on nõutav — kohandatud baas-URL-i tugi puudub)

Järgmised tööriistad ei toeta oma praegustes versioonides kohandatud baas-URL-e, mistõttu on MITM ainus liikluse pealtkuulamise võimalus. Teostatavuse hindamine on ootel:

- **Windsurf** (Codeium/Cognition)
- **Amp** (Sourcegraph)
- **Amazon Q / Kiro CLI** (AWS Bedrock — Kiro IDE-st eraldiseisev)
- **Cowork** (Anthropicu töölauarakendus)

Märkus. GitHub Copilot CLI ≥v1.0.19 toetab muutujat `COPILOT_PROVIDER_BASE_URL` — selle tööriista puhul kasutage MITM-i asemel otsekonfiguratsiooni.
