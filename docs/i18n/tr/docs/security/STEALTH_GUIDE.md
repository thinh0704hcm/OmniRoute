# Stealth Guide (Türkçe)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/security/STEALTH_GUIDE.md) · 🇩🇰 [da](../../../da/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇮🇩 [id](../../../id/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇮🇹 [it](../../../it/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Doğruluk kaynağı:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Son güncelleme:** 2026-09-02 — v3.8.51
> **Hedef kitle:** Sağlayıcıya özgü gizlilik entegrasyonlarının bakımını yapan mühendisler.

OmniRoute, uç noktaları resmî olmayan istemcilerin aktif olarak parmak izini çıkaran sağlayıcılarla (TLS JA3/JA4, başlık sıralaması, JSON gövde yapısı, bütünlük belirteçleri) entegre olur. Bu sayfa, OmniRoute'un sunduğu gizlilik yüzeylerini ve bunların nerede uygulandığını açıklar.

## Yasal ve Etik Bildirim

Gizlilik özellikleri, OmniRoute'un kullanıcıların sahip olduğu resmî hesaplar (Claude Code CLI, Codex, Antigravity, Cursor vb.) ile OmniRoute'un birleşik API'si arasında bir uyumluluk katmanı olarak çalışabilmesi için mevcuttur. Bunlar dolandırıcılık tespitinden kaçınmak, kimlik bilgilerini paylaşmak veya sağlayıcının Hizmet Şartları'nı ihlal etmek için **değildir**. Bakımı yapanlar, operatörlerin hesap oluştururken kabul ettikleri üst sağlayıcı Hizmet Şartları'na uymasını bekler.

---

## TLS Parmak İzi Katmanı

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Kalıcı `wreq-js` oturumları, hesap kapsamı ve çözümlenmiş proxy başına gerektiğinde oluşturulur. Süreç genelindeki `TlsClient`, Cloudflare arkasındaki üst hizmetler için **macOS üzerinde Chrome 124** kimliğine bürünen en fazla 128 oturumu havuzda tutar. Yerel çalışma zamanı kullanılamadığında `TlsClient.fetch()` güvenli biçimde başarısız olur; çağıran taraf bu sarmalayıcının dışında açıkça bir geri dönüş seçeneği belirleyebilir.

- Oturum profili: `browser: "chrome_124", os: "macos"`
- Proxy çözümlemesi (öncelik): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (küçük harfli biçimleri de dâhil)
- Zaman aşımı: `TLS_CLIENT_TIMEOUT_MS` (`FETCH_TIMEOUT_MS` değerinden devralınır, varsayılan 600000)
- `wreq-js` Response, fetch ile uyumludur (`headers`, `text()`, `json()`, `clone()`, `body`).
- İlk bayt gözcüsü (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()`,
  üst hizmet başlıkları ulaşır ulaşmaz çözümlenir; dolayısıyla tek başına `TLS_CLIENT_TIMEOUT_MS`,
  hiçbir zaman ilk baytı vermeyen bir gövdeyi sınırlayamaz. `guardTlsFirstByte()`, gövdenin ilk
  `read()` çağrısını `TLS_FIRST_BYTE_WATCHDOG_MS` (varsayılan `10000`, `0` devre dışı bırakır) ile
  yarıştırır; sağlıklı bir gövde bundan etkilenmezken takılmış bir gövde, wreq okuyucusunu iptal eder
  ve `proxyFetch` işlevinin mevcut TLS geri dönüş mantığının doğrudan/proxy dağıtıcısına geçmesini
  sağlar (ör. gövdeli bir POST gibi yeniden oynatılması güvenli olmayan bir istek, sessizce yeniden
  denenmek yerine yine hata fırlatır).

### Web çerezi sağlayıcı aktarımı — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts`, aşağıdaki beş özelleştirilmiş web çerezi aktarımı için paylaşılan
bağdaştırıcıdır. Her ince sağlayıcı sarmalayıcısı bir tarayıcı/işletim sistemi profili seçer.
Bağdaştırıcı, `open-sse/utils/tlsClient.ts` içindeki tek wreq çalışma zamanı yükleyicisini ve aktarım
havuzunu kullanır; bunlar profil + işletim sistemi + çözümlenmiş proxy anahtarına göre tutulurken her
istek `cookieMode: "ephemeral"` kullanır. Bu nedenle hesaplar ve istekler aktarım düzeyindeki
bağlantıları paylaşır, ancak hiçbir zaman bir wreq oturumunu veya çerez deposunu paylaşmaz.

| Sağlayıcı  | Profil        | Taklit edilen işletim sistemi | Akış EOF politikası                  |
| ---------- | ------------- | ----------------------------- | ------------------------------------ |
| Claude     | `chrome_146`  | Linux                         | `[DONE]` dâhil                       |
| Perplexity | `firefox_148` | macOS                         | `event: end_of_stream` dâhil         |
| Grok       | `chrome_146`  | Linux                         | `[DONE]` hariç                       |
| Notion     | `chrome_146`  | Windows                       | `[DONE]` dâhil                       |
| LMArena    | `chrome_146`  | Windows                       | belirteç yok; yerel EOF'de kapatılır |

- Akış, yerel yanıt `ReadableStream` nesnesini doğrudan tüketir; geçici dosya veya yardımcı süreç
  oluşturulmaz.
- Bir akış kullanıma sunulmadan önce başlangıçtaki en fazla 256 bayt incelenir. SSE sağlayıcıları,
  SSE olmayan hataları arabelleğe alır; Grok/LMArena, Cloudflare doğrulamalarını `403` durumuna ve
  HTML ara sayfalarını `502` durumuna eşler.
- Yerel istek zaman aşımı, mutlak bir JS kesin son tarihiyle sarmalanmaya devam eder. Bir takılma,
  yalnızca etkilenen profil/işletim sistemi/proxy aktarımını geçersiz kılıp kapatır; sonraki istek
  bunu yeniden oluşturur.
- Proxy çözümleme önceliği, her çağrı için `proxyUrl` → istek kapsamındaki hesap/gösterge paneli
  bağlamı → `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (küçük harfli biçimleri dâhil) şeklindedir.
  Çözümleme hataları, doğrudan bağlantı sızdırmak yerine güvenli biçimde başarısız olur. LMArena
  bilinçli olarak `arena.ai` üzerinden çözümleme yapar.
- `byteResponse`, UTF-8 bozulması olmadan içerik türü belirtilmiş bir `data:` URL'si döndürür.
- Hatalar; `TlsClientUnavailableError` (paket/eklenti kullanılamıyor), `TlsClientHangError`
  (kesin son tarih aşıldı) ve sınırlandırılmış 128 profil/işletim sistemi/proxy yuvasının tümü etkin
  veya kapanmakta olduğunda `WreqTransportCapacityError` (paylaşılan oturum kapasitesi hata kodu)
  şeklindedir.

Yukarıdaki genel `TlsClient` oturumu, tarayıcı destekli kalıcı çerez durumu için özelleştirilmiş
olmaya devam eder. Her iki yol da önbelleğe alınmış tek bir wreq modül yükleyicisini ve süreç yaşam
döngüsü kancasını yeniden kullanır; çerez ömürleri kasıtlı olarak farklı olduğundan havuzları ayrı
kalır.

Profiller sabitlenmiş paket tarafından desteklenir, ancak gerçek WAF kabulü yerel sözleşme
testlerinden bağımsız olarak değişebilir. Bir üst tarayıcıyla eşdeğerlik iddia etmeden önce parmak
izi değişikliklerini açıkça yetkilendirilmiş canlı bir hesapla doğrulayın.

---

## Claude Code Gizlilik Paketi

`cliCompatMode` açık olduğunda OmniRoute, giden Claude isteklerini `claude-cli` trafiğinden ayırt edilemeyecek şekilde yeniden biçimlendirir. Üç modül birlikte çalışır:

### `claudeCodeFingerprint.ts`

Faturalandırma başlığına gömülen 3 karakterli `cc_version` parmak izini hesaplar:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (sabit kodlanmıştır; resmî istemciyle eşleşir)
- Girdiler: İlk kullanıcı mesajı metninin 4, 7 ve 20. indekslerindeki karakterler + sürüm dizesi
- Çıktı: 3 karakterli onaltılık önek

### `claudeCodeCCH.ts` (İstemci İçerik Karması)

Resmî Claude Code CLI'ın Bun/Zig aracılığıyla hesapladığı sunucu taraflı bütünlük denetimidir. OmniRoute bunu `xxhash-wasm` ile yeniden uygular:

1. Gövdeyi `cch=00000;` yer tutucusuyla serileştirin
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. Sıfırlarla doldurulmuş, 5 karakterli küçük harfli onaltılık değer
4. `cch=00000;` ifadesini hesaplanan belirteçle değiştirin

Sabitler:

- Başlangıç değeri: `0x6e52736ac806831e`
- Desen: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Üst sistem filtrelerinin bunları grep ile aramasını engellemek için "hassas" istemci adlarının ilk karakterinden sonra bir Unicode **sıfır genişlikli birleştirici** (`U+200D`) ekler. Varsayılan sözcük listesi:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Şunlara uygulanır: `system` blokları, tüm `messages[].content` alanları ve `tools[].description` / `tools[].function.description`. Operatör tarafından `setSensitiveWords()` aracılığıyla geçersiz kılınabilir.

### `claudeCodeCompatible.ts` — `anthropic-compatible-cc-*` sağlayıcıları

Yalnızca "gerçek Claude Code" trafiğini kabul eden üçüncü taraf Anthropic aktarıcıları için:

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- Varsayılan olarak `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"`
- Bağlantı başına "Redaksiyonlu düşünme betasını etkinleştir" seçeneği, bir CC Compatible üst sistemi özellikle redaksiyonlu düşünme akışları gerektirdiğinde `redact-thinking-2026-02-12` ekler
- Bağlantı başına "Özetlenmiş düşünme gösterimini etkinleştir" seçeneği, `providerSpecificData.requestDefaults.summarizeThinking` değerini saklar ve henüz bir gösterim modu ayarlanmamış CC Compatible düşünme isteklerine `display: "summarized"` ekler
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (Opus/Sonnet 4.x ailesi)
- Varsayılan yol: `/v1/messages?beta=true`

Aynı paketteki kardeş modüller:

- `claudeCodeConstraints.ts` — sıcaklık + önbellek denetimi kuralları
- `claudeCodeToolRemapper.ts` — araç adı yeniden eşleme
- `claudeCodeExtraRemap.ts` — ek yük normalleştirme

---

## Antigravity Gizliliği

Antigravity istekleri, çağıranın metnini bayt bayt korur. OmniRoute, istemlere sıfır genişlikli karakterler eklemez veya bir IDE istemcisini taklit etmek için araçları yeniden adlandırmaz/eklemez.

### `antigravityHeaderScrub.ts`

İletmeden önce Stainless SDK işaretleyicilerini (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`) kaldırır.

### ⚠️ Risk: `ANTIGRAVITY_CREDITS=always` (hesap yasaklama açısından yüksek riskli nokta)

`ANTIGRAVITY_CREDITS=always` (`open-sse/executors/antigravity.ts` tarafından kullanılır), Google'ın ücretsiz katman kotasının istekleri sınırlamasına izin vermek yerine **her** isteği Antigravity AI Credit Overage (ücretli Google kredileri) üzerinden yönlendirir. Bu bir özellik olarak belgelenmiştir ancak **gördüğümüz en yaygın Hizmet Şartları ihlali bildiriminin açık ara birinci nedenidir** — birden fazla Google Ultra hesabı, `=always` ile birkaç saat çalıştırıldıktan sonra `403 / "service disabled for ToS violation" / insufficient_quota` hatasıyla yasaklanmıştır.

Üst sistem uygulaması **Google tarafındadır** ve OmniRoute'un önleyebileceği bir şey değildir. Ortam değişkeninin adı ve mevcut belgeler, bunun güvenle açılabilecek bir ayar olduğu izlenimini verir; ancak öyle değildir.

**Bunun kötüye kullanım tespitini yalnızca ücretsiz katman kullanımından daha agresif biçimde tetiklemesinin nedenleri:**

- Tek bir Google hesabındaki sürekli otomatik harcama, ücretsiz katmanda kotaya ulaşıp durmaktan farklı şekilde işaretlenir.
- Kredi aşımı kullanımında hız üst sınırı yoktur; bu nedenle yanlış yapılandırılmış bir istemci dakikalar içinde birkaç yüz USD tüketebilir ve API anahtarı satışı veya bot trafiği gibi görünebilir.
- Aynı harici IP'den paralel olarak kredi aşımı kullanan birden fazla OmniRoute kullanıcısı sinyali güçlendirir.

**Önerilen yaklaşım:**

1. Operatör, ücretli kredi ve hesap yaptırımı riskini açıkça kabul etmediği sürece varsayılan `ANTIGRAVITY_CREDITS=off` ayarını koruyun. `retry`, önce normal isteği gönderir ve uygun bir kota 429 hatasından sonra en fazla bir kez kredi ekler; `always` ise ilk istekte kredi ekler.
2. Tek bir Antigravity hesabını kapasitesinin sonuna kadar kullanmak yerine **Auto-Combo aracılığıyla yükü sağlayıcılar arasında dağıtın** (`model: "auto"` veya `kr/glm/etc`-combo).
3. Antigravity sağlayıcısının düzenleme sayfasında **bağlantı başına RPM sınırları belirleyin** (Dashboard → Providers → Antigravity → connection → rate limit). Sürekli kullanım için 30–60 RPM savunulabilir bir üst sınırdır.
4. **Kararlı ve operatör tarafından kontrol edilen üst sistem ağ bağlantısı kullanın** ve tek bir hesabı birbiriyle ilgisiz kullanıcılar veya iş yükleri arasında paylaşmaktan kaçının.
5. **Yasaklanmanız durumunda**: Google'ın gönderdiği tam `quota_exceeded` / `service disabled` yanıt gövdesiyle birlikte `support.google.com` → "Restore Workspace/Account access" üzerinden itiraz edin. Hesabın geri açılması garanti edilmez.

Ortam referansı, her kredi modunun hesap ve harcama üzerindeki etkilerini belgeler.

Temas noktaları:

- `open-sse/executors/antigravity.ts` — `process.env.ANTIGRAVITY_CREDITS` değerini okur
- `src/lib/oauth/providers/antigravity.ts` — kimlik bilgisi altyapısı
- İlk olay bildirimi: Tartışma [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## CLI Parmak İzi Kayıt Defteri — `open-sse/config/cliFingerprints.ts`

Resmî CLI'ların mitmproxy izlerinden yakalanan **kesin** üstbilgi sıralamasını ve JSON gövde alanı sıralamasını sabitleyen, sağlayıcı bazında tablo. Şu anda kayıtlı olanlar: `codex`, `claude`; ayrıca `antigravity` ve `github` için `providerHeaderProfiles.ts` içinde çalışma zamanında türetilen profiller.

```ts
interface CliFingerprint {
  headerOrder: string[]; // büyük/küçük harfe duyarlı
  bodyFieldOrder: string[]; // üst düzey JSON anahtarları
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Ortam değişkeni aracılığıyla sağlayıcı bazında açılıp kapatılabilir (aşağıya bakın). Devre dışı bırakıldığında üstbilgiler/gövde anahtarları, Node/JSON'un sağladığı sırada görünür; bu da parmak izi çıkarmayı kolaylaştırır.

---

## MITM Proxy (Antigravity, Linux/macOS/Windows)

İkili dosyaları `OPENAI_BASE_URL` aracılığıyla yeniden yönlendirilemeyen CLI'lar için OmniRoute, TLS sonlandırması yapan yerel bir proxy çalıştırır. Uç noktalar `src/app/api/cli-tools/antigravity-mitm/` altında bulunur.

| Yöntem | Uç nokta                                | Amaç                                                   |
| ------ | --------------------------------------- | ------------------------------------------------------ |
| GET    | `/api/cli-tools/antigravity-mitm`       | Durum — çalışıyor, pid, dnsConfigured, certExists      |
| POST   | `/api/cli-tools/antigravity-mitm`       | MITM'i başlatır (`apiKey` + `sudoPassword` gerektirir) |
| DELETE | `/api/cli-tools/antigravity-mitm`       | MITM'i durdurur                                        |
| GET    | `/api/cli-tools/antigravity-mitm/alias` | Model takma adlarını listeler                          |
| PUT    | `/api/cli-tools/antigravity-mitm/alias` | Bir araç için model takma adlarını kaydeder            |

Hedef olarak yakalanan ana makine: **`daily-cloudcode-pa.googleapis.com`** (Antigravity'nin yukarı akış sunucusu).

### Başlatma sırası (`src/mitm/manager.ts::startMitm`)

1. `selfsigned` aracılığıyla kendinden imzalı sertifika oluşturun (RSA-2048, SHA-256, 1y) — `cert/generate.ts`
2. Sertifikayı sistem güven deposuna yükleyin — `cert/install.ts`
3. `127.0.0.1 daily-cloudcode-pa.googleapis.com` hosts girdisini ekleyin — `dns/dnsConfig.ts`
4. `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (varsayılan `443`) ile `src/mitm/server.cjs` sürecini başlatın
5. PID'yi `<DATA_DIR>/mitm/.mitm.pid` konumunda kalıcı olarak saklayın

### Linux dinamik güven deposu algılama — `cert/install.ts`

`getLinuxCertConfig()`, bir öncelik listesini dolaşır ve mevcut olan ilk dizini seçer:

| Dağıtım ailesi           | Dizin                                       | Güncelleme komutu        |
| ------------------------ | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu          | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS   | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                 | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Sertifika dosya adı: `omniroute-mitm.crt`. Parmak izi eşleştirmesi `getCertFingerprint()` aracılığıyla yapılır (DER'in SHA-1 özeti).

Ayrıca `updateNssDatabases()`, `certutil` kullanılabildiğinde kullanıcı başına NSS veritabanlarına yükleme yapar: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb` ve tüm Firefox profilleri (snap dâhil). Kullanılan takma ad: **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** yükseltilmiş PowerShell → `certutil -addstore Root`

### Kimlik doğrulama

Tüm MITM uç noktaları yönetim kimlik doğrulaması (`requireCliToolsAuth`) gerektirir. Sudo parolası modül kapsamında önbelleğe alınır (asla `globalThis` içinde değil) ve `stopMitm()` çağrıldığında temizlenir.

---

## User-Agent Geçersiz Kılmaları — ortam değişkenleri (`.env.example` bölüm 12)

| Değişken                 | Varsayılan                                                      |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.149.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

`open-sse/executors/base.ts::buildHeaders()` tarafından dinamik arama yoluyla kullanılır. **Sağlayıcılar yeni CLI sürümleri yayımladığında bunları yükseltin** — güncelliğini yitirmiş UA dizeleri eski istemciler olarak reddedilmeye başlar.

## CLI Uyumluluk Modu Anahtarları (`.env.example` bölüm 13)

| Değişken                   | Etki                               |
| -------------------------- | ---------------------------------- |
| `CLI_COMPAT_CODEX=1`       | Codex parmak izi                   |
| `CLI_COMPAT_CLAUDE=1`      | claude-cli parmak izi              |
| `CLI_COMPAT_GITHUB=1`      | GitHub Copilot Chat parmak izi     |
| `CLI_COMPAT_ANTIGRAVITY=1` | Antigravity parmak izi             |
| `CLI_COMPAT_KIRO=1`        | Kiro                               |
| `CLI_COMPAT_CURSOR=1`      | Cursor                             |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                        |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                           |
| `CLI_COMPAT_CLINE=1`       | Cline                              |
| `CLI_COMPAT_ALL=1`         | Yukarıdakilerin tümünü etkinleştir |

Sağlayıcı IP'si **her zaman korunur** — anahtar yalnızca isteğin ağ üzerindeki görünümünü yeniden şekillendirir, çıkış IP'sini değiştirmez.

---

## Gelen Üstbilgilerin Temizlenmesi

OmniRoute, Cursor'dan gelen bir isteğin Claude üst hizmetine `User-Agent: Cursor/X.Y.Z` bilgisini sızdırmaması için iletmeden önce gelen istemci üstbilgilerini temizler. Zod şemaları ve birim testleriyle eş zamanlı tutulan engelleme listesi için `src/shared/constants/upstreamHeaders.ts` dosyasına bakın.

---

## Sağlayıcı Değişiklik Yaptığında Parmak İzlerini Güncelleme

1. Resmî CLI trafiğini `mitmproxy` ile yakalayın (TLS müdahalesi + döküm)
2. JA3/JA4 değerlerini ve üstbilgilerin gerçek sırasını çıkarın
3. İlgili `CLI_FINGERPRINTS[...]` girdisini güncelleyin
4. `.env.example` içindeki eşleşen `*_USER_AGENT` varsayılanını yükseltin
5. TLS el sıkışmasının kendisi değiştiyse ilgili sağlayıcı sarmalayıcısını veya wreq-js `browser:` seçeneğini güncelleyin
6. Sağlayıcıya özgü TLS testlerini ve canlı sağlayıcıya karşı manuel bir kanarya testini çalıştırın
7. Bir yama sürümü olarak yayımlayın; `CHANGELOG.md` içinde belgelendirin

---

## Testler

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — paylaşılan TLS sarmalayıcı davranışı
- `tests/unit/anthropic-cache-fingerprint.test.ts` — parmak izi belirlenimciliği
- `tests/unit/chatgpt-web-source-retirement.test.ts` — ortak ChatGPT Web gizlilik kaynağının bulunmaması sürerken Codex Web'in mevcut kalması

---

## Ayrıca Bakınız

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — gizli bir yol `403` aldığında ne olur
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — tüm ortam değişkenleri referansı
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — MITM iş akışının operatör görünümü
