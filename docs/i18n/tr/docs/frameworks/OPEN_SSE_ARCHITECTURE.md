# open-sse Architecture (Türkçe)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Özet**: `open-sse/`, OmniRoute'taki her LLM isteğine güç veren temel akış motorudur. İstek işlem hattını, yürütücüleri, servisleri, MCP sunucusunu ve çeviri katmanını uygulayan yaklaşık 1.400 dosya içerir. Bu kılavuz, parçaların nasıl bir araya geldiğini açıklar.

**Kaynak:** `open-sse/` (çalışma alanı paketi, yaklaşık 1.440 test dışı `.ts` dosyası)

---

## Neden Ayrı Bir Çalışma Alanı Paketi?

`open-sse/`, çeşitli nedenlerle OmniRoute monorepo'sunda **bağımsız bir çalışma alanıdır**:

1. **Yeniden kullanılabilirlik** — `open-sse`, npm üzerinde `@omniroute/open-sse` olarak yayımlanır; böylece diğer projeler onu bağımsız olarak kullanabilir
2. **Net sınırlar** — akış motoru, OmniRoute'a özgü UI/DB katmanından ayrıştırılmıştır
3. **Performans** — motorun Next.js bağımlılığı yoktur; bu da CLI/sunucusuz bağlamlarda daha hızlı soğuk başlatmalar sağlar
4. **Sürümleme** — `open-sse` kendi yayın takvimine göre sürüm çıkarabilir

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Üst Düzey Yapı

```
open-sse/
├── index.ts              # Genel kullanıma açık giriş noktası
├── types.d.ts            # Genel kullanıma açık tür dışa aktarımları
├── package.json          # @omniroute/open-sse
├── config/               # Sağlayıcı yapılandırmaları, sabitler, kayıtlar
├── executors/            # Sağlayıcı başına HTTP yürütücüleri (67 + base.ts/index.ts)
├── handlers/             # İstek işleyicileri (chatCore, responses vb.)
├── lib/                  # Dahili yardımcı araçlar
├── mcp-server/           # Model Context Protocol sunucusu
├── services/             # Yaklaşık 298 servis modülü
├── transformer/          # Responses API biçim dönüştürücüsü
├── translator/           # Biçim çevirisi (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Paylaşılan yardımcı araçlar (günlük kaydı, hata, akış vb.)
```

### Modül Sayıları

| Dizin | Dosyalar | Amaç |
| `executors/` | 167 | Sağlayıcı başına HTTP yürütücüleri (DefaultExecutor fabrikası aracılığıyla birleştirilir) |
| `handlers/` | 157 | İstek giriş noktaları (chatCore, responses, embeddings) |
| `services/` | ~536 | Yönlendirme, önbelleğe alma, hız sınırlama, yenileme vb. |
| `translator/` | 56 | Biçim dönüştürme (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP araçları ve aktarımları |
| `utils/` | ~108 | Birden fazla alanı ilgilendiren yardımcı araçlar (günlük kaydı, hata, akış) |
| `config/` | ~339 | Sağlayıcı yapılandırmaları, sabitler, kayıtlar |

---

## İstek İşlem Hattı

Her LLM isteği **5 aşamalı bir işlem hattından** geçer:

```
                              ┌──────────────┐
   HTTP isteği                │  1. YÖNLENDİR│   combo çözümleme, model seçimi
   (Next.js rotası)           └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. ÇEVİR    │   biçim dönüştürme (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. YÜRÜT    │   sağlayıcı yürütücüsü, HTTP, yeniden deneme, devre kesici
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. AKIŞ     │   SSE dönüşümü, geri basınç
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. KAYDET   │   kullanım takibi, çağrı günlüğü, hata sınıflandırma
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP yanıtı (SSE veya JSON)
```

### Aşama 1: Yönlendirme (services/combo.ts)

**Giriş noktası**: `services/combo.ts` içindeki `handleComboChat()`

İsteği somut bir `(provider, model, account, credentials)` demetine çözümler:

- Combo'yu kimliğine göre ara (veya `auto/*` modelleri için sanal bir combo oluştur)
- Yönlendirme stratejisini uygula (öncelik, ağırlıklı, round-robin vb.)
- Sağlıksız sağlayıcıları filtrele (devre kesici)
- Sıradaki kullanılabilir hedefi seç

`auto/*` modelleri için bu aşama ayrıca:

- **16 faktörlü puanlama** algoritmasını çalıştırır (`services/autoCombo/`)
- Sağlık, maliyet, gecikme vb. ölçütlere göre bir `provider+model` çifti seçer

### Aşama 2: Çeviri (translator/)

Kaynak biçim (ör. OpenAI) hedef biçimden (ör. Claude) farklıysa istek **çevrilir**:

- Sistem istemi → sistem mesajı
- Araç tanımları → sağlayıcıya özgü araç biçimi
- Akıl yürütme/düşünme parametreleri → sağlayıcıya özgü eşdeğerler
- Mesaj rolü normalizasyonu (OpenAI dışındakiler için `developer` → `system`)

`translator/index.ts` şunları dışa açar:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Aşama 3: Yürütme (executors/)

**Giriş noktası**: `getExecutor(providerId).execute(request, options)`

Tüm sağlayıcılar, `getExecutor()` fabrika yedeği aracılığıyla `DefaultExecutor` (`executors/default.ts`) kullanır. Yürütücü:

- Üst servis URL'sini oluşturur (`buildUrl()`)
- Sağlayıcıya özgü başlıkları ekler (`buildHeaders()`)
- İstek gövdesini dönüştürür (`transformRequest()`)
- HTTP isteğini yeniden deneme + üstel geri çekilme ile gönderir
- Gerektiğinde kimlik doğrulama bilgilerini yeniler (OAuth sağlayıcıları)

Tüm yürütücüler, aşağıdakileri sağlayan `BaseExecutor`'ı (`executors/base.ts`, 1170 LOC) genişletir:

- Ortak yeniden deneme mantığı
- Proxy entegrasyonu
- Devre kesici entegrasyonu
- Kullanım kaydı kancaları

### Aşama 4: Akış (utils/stream.ts)

Akış yanıtlarında yürütücü bir **ReadableStream** döndürür. İşleyici:

- Bir SSE dönüşümü üzerinden aktarır (`createSSETransformStreamWithLogger`)
- Kopmuş bağlantıları algılamak için periyodik sinyaller uygular
- İstemci bağlantısının kesilmesini sorunsuz biçimde işler (`pipeWithDisconnect`)
- Akış kullanmayan istemciler için SSE → JSON dönüşümü yapar

Akışsız yanıtlarda yürütücü, değişiklik yapılmadan aktarılan ayrıştırılmış bir JSON nesnesi döndürür.

### Aşama 5: Kaydetme (services/usage.ts)

Yanıttan sonra (başarılı veya başarısız), kullanım kaydedilir:

- Yanıttaki `prompt_tokens`, `completion_tokens`, `cached_tokens`
- Fiyatlandırma verilerinden hesaplanan `cost_usd`
- Başarısız olması durumunda `latency_ms`, `status`, `error_class`
- `usage_history` tablosunda kalıcı olarak saklanır

Çağrı günlüğü yapıtları (etkinleştirilmişse) `${DATA_DIR}/call_logs/` dizinine yazılır.

---

## Temel Dosyalara Derinlemesine Bakış

### chatCore.ts (5977 satır)

**Ana istek işleyicisi**. Boyutuna rağmen net bir yapıya sahiptir:

```ts
// chatCore.ts dosyasının sözde yapısı
export async function handleChat(request: NextRequest) {
  // 1. Kimlik doğrulama + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Gövde doğrulaması
  const body = await parseRequestBody(request);

  // 3. Biçim algılama + dönüştürme
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Kombinasyon yönlendirmesi
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Sonraki hedefle devam et
    }
  }

  // 5. Acil durum yedeği
  return await emergencyFallback(body);
}
```

Tek ve devasa bir fonksiyon olmasına rağmen, 5 aşamalı işlem hattıyla eşleşen **yorumlarla belirtilmiş bölümler** hâlinde düzenlenmiştir.

### combo.ts (4456 kod satırı)

Bir kombinasyonu sıralı hedeflere çözümleyen **yönlendirme motoru**.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("hedef başarısız oldu, sonraki deneniyor", { target, err });
    }
  }
  throw new ComboExhaustedError("Tüm hedefler başarısız oldu");
}
```

**19 yönlendirme stratejisini** destekler (bkz. `src/shared/constants/routingStrategies.ts`):

| Strateji            | Davranış                                                                                            |
| ------------------- | --------------------------------------------------------------------------------------------------- |
| `priority`          | İlk hedefe öncelik veren sıralı liste                                                               |
| `weighted`          | Hedef başına ağırlığa göre olasılıksal seçim                                                        |
| `round-robin`       | Hedefler arasında sırayla döngü                                                                     |
| `context-relay`     | Bağlamı hedefler arasında aktarır                                                                   |
| `fill-first`        | Sonraki hedefe geçmeden önce kotayı doldurur                                                        |
| `p2c`               | İki seçenekten güçlü olanı                                                                          |
| `random`            | Eşit olasılıklı rastgele seçim                                                                      |
| `least-used`        | Son zamanlarda en az kullanılanı seçer                                                              |
| `cost-optimized`    | Önce en ucuz ve sağlıklı hedef                                                                      |
| `reset-aware`       | Sağlayıcı sıfırlama pencerelerini dikkate alır                                                      |
| `reset-window`      | Sıfırlama penceresine dayalı yönlendirme                                                            |
| `headroom`          | Önce en fazla kalan kota kapasitesine sahip olan                                                    |
| `strict-random`     | Gerçek anlamda eşit olasılıklı (kalite ağırlığı yok)                                                |
| `auto`              | 16 faktörlü puanlamayı kullanır (`autoCombo/`)                                                      |
| `lkgp`              | Önce bilinen son iyi sağlayıcı                                                                      |
| `context-optimized` | Uzun bağlamlı istekler için en iyisi                                                                |
| `fusion`            | Paralel olarak bir panele dağıtır, ardından bir değerlendirici aracılığıyla sentezler (`fusion.ts`) |

### base.ts (1170 kod satırı)

107 yürütücünün tamamının genişlettiği **soyut yürütücü**. Şunları içerir:

- `buildUrl()` — varsayılan URL oluşturma (alt sınıflar özel davranış için geçersiz kılar)
- `buildHeaders()` — varsayılan üstbilgiler (kimlik doğrulama, içerik türü)
- `transformRequest()` — varsayılan olarak değişiklik yapmadan geçirir
- `execute()` — yeniden deneme/artan bekleme/devre kesici özellikli ana HTTP döngüsü

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // OpenAI/Anthropic uyumlu tüm sağlayıcıları işler
  // Sağlayıcılar yapılandırmaları (URL, kimlik doğrulama, üstbilgiler) kaydeder ancak yürütücü mantığını paylaşır
}
```

Sağlayıcıya özgü davranışlar (kimlik doğrulama üstbilgileri, temel URL, sürüm üstbilgileri) ayrı yürütücü sınıfları üzerinden değil, sağlayıcı kayıt defteri aracılığıyla yapılandırılır.

````

---

## Servisler (117 modül)

Servisler, işleyicilerin bir araya getirdiği **odaklanmış, tek amaçlı modüllerdir**. Başlıca kategoriler:

### Yönlendirme ve Combo

- `combo.ts` — combo ile yönlendirilen istekler için giriş noktası
- `services/autoCombo/` — 16 faktörlü puanlama, 8 otomatik yönlendirme stratejisi
- `wildcardRouter.ts` — joker karakterli rotaları (`gpt-*`) eşleştirir
- `modelFamilyFallback.ts` — T5 aile içi geri dönüşü

### Hız Sınırlama ve Kota

- `rateLimitManager.ts` — anahtar+sağlayıcı başına token kovası
- `usage.ts` — kullanım kaydı
- `quotaCache.ts` — bellek içi kota anlık görüntüleri

### Hesap ve Token

- `tokenRefresh.ts` — 401 durumunda OAuth yenileme
- `accountFallback.ts` — alternatif hesaba geçiş
- `sessionManager.ts` — çok turlu oturum durumu

### Akıllı İşlemler

- `intentClassifier.ts` — istek amacını sınıflandırır
- `taskAwareRouter.ts` — görev türüne göre yönlendirir
- `thinkingBudget.ts` — düşünme token'larını tahsis eder
- `contextManager.ts` — yönlendirme bağlamı ekler

### Dayanıklılık

- `resilience.ts` — yeniden deneme, geri çekilme ve devre kesici orkestrasyonu
- `emergencyFallback.ts` — son çare geri dönüşü
- `modelDeprecation.ts` — ardıl modellere otomatik yönlendirme

### Durum

- `signatureCache.ts` — istek imzasına göre yinelenenleri kaldırır
- `volumeDetector.ts` — yük azaltma
- `contextHandoff.ts` — oturum serileştirme

### Sıkıştırma

- `compression/` (alt dizin) — eksiksiz sıkıştırma işlem hattı
- Motorları, kural paketlerini ve adaptörleri kapsayan 39 dosya

### Beceriler

- ([SKILLS.md](./SKILLS.md) dosyasında ele alınmıştır)

### Bellek

- ([MEMORY.md](./MEMORY.md) dosyasında ele alınmıştır)

---

## Yürütücüler (75+ dosya)

Her sağlayıcı için bir dosya bulunur. Bunların tümü `BaseExecutor` sınıfını genişletir ve farklı olan davranışları geçersiz kılar.

### Yaygın Kalıplar

Sağlayıcılar, yapılandırılmış yürütücüyü döndüren `getExecutor(providerId)` aracılığıyla çözümlenir. OpenAI/Anthropic uyumlu sağlayıcılar `DefaultExecutor` (`executors/default.ts`) kullanır. Sağlayıcıya özgü davranışlar (temel URL, kimlik doğrulama başlıkları, API sürümü) `open-sse/config/providers/` içinde yapılandırılırken istek gövdesi dönüşümleri `open-sse/translator/` içinde gerçekleştirilir.

**Özel URL**, sağlayıcı yapılandırması aracılığıyla ayarlanır:

```ts
// open-sse/config/providers/ içindeki sağlayıcı yapılandırması
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Özel kimlik doğrulama**, sağlayıcı kayıt sisteminin kimlik doğrulama yapılandırması (API anahtarı, OAuth, başlık profilleri) üzerinden gerçekleştirilir.

**Özel istek gövdesi** dönüşümleri (ör. Anthropic'in `system` öğesini `messages` öğesinden ayırması), `open-sse/translator/` içinde sağlayıcı bazında kaydedilir.

````

### Yürütücü Fabrikası

`executors/index.ts`, `getExecutor(providerId)` fonksiyonunu dışa aktarır:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Çözümleme, `ExecutorRegistry` (`executors/registry.ts`) üzerinden gerçekleştirilir: her özelleştirilmiş yürütücü, `executors/index.ts` içindeki yerleşik tabloda bildirilir ve modül yüklenirken `registerExecutor(alias, instance)` aracılığıyla kaydedilir; `getExecutor()`, kayıt sistemine başvurur ve özelleştirilmiş girdisi bulunmayan herhangi bir sağlayıcı için belleğe alınmış bir `DefaultExecutor` örneğine geri döner. Tam alias → yürütücü eşlemesi, `tests/unit/executor-map-golden.test.ts` altın testiyle tanımlanır.

---

## Çeviriciler

Yeni Responses API'nin yanı sıra **3 format** arasında çeviri yapar: OpenAI, Anthropic, Gemini.

### Çevirinin Gerçekleştiği Durumlar

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Yaygın çeviriler:

- `OpenAI → Anthropic`: ayrı `system` alanı, `x-api-key` üst bilgisi
- `OpenAI → Gemini`: `messages` yerine `contents`, `systemInstruction`
- `OpenAI → Responses API`: `input` dizisi, `previous_response_id` durumu

### Ele Alınan Uç Durumlar

- `developer` rolü → OpenAI dışındaki sağlayıcılar için `system`
- `system` rolü → GLM/ERNIE için ilk kullanıcı mesajıyla birleştirilir
- `json_schema` → Gemini'nin `responseMimeType` + `responseSchema` alanları
- `tools` → sağlayıcıya özgü araç formatı
- Düşünme parametreleri (o1, Claude) → sağlayıcıya özgü karşılıklar

---

## MCP Sunucusu

`open-sse/mcp-server/`, **Model Context Protocol** sunucusunu uygular:

- **110 araç** (sağlayıcı yönetimi, kombinasyonlar, bellek, önbellek, sıkıştırma, proxy, beceriler, oyunlaştırma, eklentiler, Notion, Obsidian, yerel külliyat)
- **3 aktarım yöntemi**: stdio, SSE, Streamable HTTP
- Ayrıntılı yetkilendirme için **33 kapsam**

### Araç Kaydı

Araçlar, `open-sse/mcp-server/tools/` içinde bağımsız dosyalar olarak kaydedilir; her biri bir ad, şema, işleyici ve kapsam dışa aktarır:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Sistem durumu anlık görüntüsünü al",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Aktarım Yöntemleri

```ts
// stdio (CLI kullanımı)
startMcpStdio(server);

// SSE (HTTP tabanlı akış)
startMcpSse(server, port);

// Streamable HTTP (modern MCP)
startMcpStreamable(server, port);
```

### Yetkilendirme

Her araç çağrısı kapsam denetimlerinden geçer (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Yetersiz kapsam");
}
```

---

## Dönüştürücüler

`open-sse/transformer/`, **Chat Completions** ve **Responses API** formatları arasında dönüşüm yapar.

### Neden Ayrı Bir Dönüştürücü Var?

Responses API, OpenAI'ın **durum bilgisi tutan konuşmalar** (`previous_response_id`) içeren yeni formatıdır. Bir istemci Responses isteği gönderdiğinde OmniRoute:

1. Responses formatını dahili olarak Chat Completions formatına dönüştürür
2. İsteği sağlayıcıya gönderir (Chat Completions destekleyen herhangi bir sağlayıcı)
3. Yanıtı tekrar Responses formatına dönüştürür
4. Dönüştürülen yanıtı istemciye akış olarak iletir

Dönüştürücü (`transformer/responsesTransformer.ts`) şunu sağlar:

```ts
createResponsesApiTransformStream(): TransformStream
```

Bu, şunları işler:

- `response.output_item.added` olayları
- `response.output_text.delta` olayları
- `response.completed` olayı
- Araç çağrısı eşleme (`function_call` ↔ `tool_calls`)

---

## Yapılandırma

`open-sse/config/`, yapılandırma katmanını barındırır:

| Dosya                         | Amaç                                                           |
| ----------------------------- | -------------------------------------------------------------- |
| `providerRegistry.ts`         | 352 sağlayıcılı katalog üzerindeki sohbet modeli kayıt defteri |
| `providerModels.ts`           | Model takma adları, format eşleme                              |
| `constants.ts`                | Zaman aşımları, sınırlar, durum kodları                        |
| `defaultThinkingSignature.ts` | Varsayılan Claude düşünme imzası                               |
| `modelStrip.ts` (hizmetlerde) | Sağlayıcı bazında alan kaldırma                                |

### Sağlayıcı Kayıt Defteri Şeması

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Modül yüklenirken gerçekleştirilen Zod doğrulaması, tüm sağlayıcı yapılandırmalarının geçerli olmasını sağlar.

---

## Performans Kısıtlamaları

Yönlendirme motorunun katı performans bütçeleri vardır:

| İşlem                                              | Hedef | Ölçüm                            |
| -------------------------------------------------- | ----- | -------------------------------- |
| Kombinasyon çözümleme                              | <10ms | 50 hedef için                    |
| Hız sınırı kontrolü                                | <1ms  | Bellek içi belirteç kovası       |
| Model ailesi geri dönüşü                           | <5ms  | Önbelleğe alınmış aile tanımları |
| İstek yönlendirme dağıtımı                         | <2ms  | Kritik yol                       |
| **Yönlendirme kritik yolunda engelleyici G/Ç yok** | —     | Tümü asenkron                    |

---

## Kaçınılması Gereken Kalıplar

❌ **`combo.ts` içinde senkron veritabanı çağrıları** — önceden hesaplayın ve önbelleğe alın
❌ **İşleyicilerde yeniden deneme mantığı** — dayanıklılık hizmetindeki `retry()` işlevini kullanın
❌ **Sağlayıcı yapılandırmasına doğrudan erişim** — `providerRegistry` alıcılarını kullanın
❌ **Sabit kodlanmış geri dönüş zincirleri** — `modelFamilyFallback.ts` içinde tanımlayın
❌ **Eşzamanlı istekler arasında durum değişiklikleri** — yalnızca istek kapsamlı bağlam kullanın

---

## Yeni Bir Bileşen Ekleme

### Yeni Bir Hizmet Ekleme

1. Odaklanmış bir sorumlulukla `open-sse/services/[serviceName].ts` oluşturun
2. Ana işleyici işlevini ve varsa sabitleri dışa aktarın
3. `tests/unit/services/[serviceName].test.mjs` içine birim testleri ekleyin
4. Yönlendirmeyle ilgiliyse `handlers/chatCore.ts` içindeki istek işlem hattına entegre edin
5. Hizmet hedef seçimini etkiliyorsa `combo.ts` içindeki yönlendirme mantığını güncelleyin
6. Bu dosyada belgelendirin

### Yeni Bir Yürütücü Ekleme

1. `BaseExecutor` sınıfını genişleten `open-sse/executors/[provider].ts` dosyasını oluşturun
2. `config/providerRegistry.ts` içinde kaydedin
3. `executors/index.ts` fabrikasına ekleyin
4. Yürütücü için birim testleri ekleyin
5. `docs/architecture/ARCHITECTURE.md` içinde belgelendirin

### Yeni Bir MCP Aracı Ekleme

1. `open-sse/mcp-server/tools/[category]Tools.ts` dosyasını oluşturun veya güncelleyin
2. Girdiler için Zod şemasını tanımlayın
3. Aracı `mcp-server/index.ts` içinde kaydedin
4. `mcp-server/auth/` içindeki kapsam matrisine ekleyin
5. Birim testleri ekleyin

---

## Ayrıca Bakınız

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — üst düzey mimari
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — mühendislik başvuru kaynağı
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — dizin bazında açıklamalar
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 faktörlü puanlama
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP sunucusu
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A sunucusu
- Kaynak: `open-sse/` (400+ dosya, ~143K LOC)
