# Agent Protocols Guide (Deutsch)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md)

---

> **Quelle:** `src/lib/{a2a,acp,cloudAgent}/`, `src/app/api/{a2a,acp,cloud}/`, `src/app/api/v1/agents/`
> **Zuletzt aktualisiert:** 2026-06-28 — v3.8.40

OmniRoute stellt drei verschiedene agentenbezogene Schnittstellen bereit. Auf den ersten Blick sehen sie ähnlich aus, lösen jedoch unterschiedliche Probleme. Verwenden Sie diese Seite, um die richtige auszuwählen.

## Kurzfassung

| Schnittstelle                 | Optimal für                                                                                                                                                       | Transport                         | Standard                        |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------------------------------- |
| **A2A — Agent-to-Agent**      | Agentenübergreifende Zusammenarbeit mit Peer-Agenten, die das A2A-Protokoll unterstützen                                                                          | JSON-RPC 2.0 über HTTP            | A2A v0.3 (offene Spezifikation) |
| **ACP — CLI-Agentenregister** | Erkennen, Registrieren und Starten von CLI-Coding-Agenten, die auf dem Rechner des Benutzers installiert sind (Cursor, Cline, Codex CLI, Claude Code, Aider usw.) | HTTP REST                         | OmniRoute-spezifisch            |
| **Cloud-Agenten**             | Übermitteln lang laufender Coding-Aufgaben an externe Cloud-Dienste (Codex Cloud, Devin, Jules, Cursor Cloud)                                                     | HTTP REST + DB-gestützte Aufgaben | OmniRoute-spezifisch            |

Die drei Schnittstellen sind unabhängig voneinander — wählen Sie eine beliebige Kombination aus.

## Entscheidungsbaum

```
Benötigen Sie einen Cloud-Dienst, der Arbeiten außerhalb dieses Rechners ausführt (Codex Cloud / Cursor Cloud / Devin / Jules)?
├─ JA → Cloud-Agenten (POST /api/v1/agents/tasks)
└─ NEIN → Weiter
    │
    Haben Sie einen Peer-Agenten, der A2A unterstützt und zusammenarbeiten möchte?
    ├─ JA → A2A (POST /a2a)
    └─ NEIN → Weiter
        │
        Müssen Sie lokal installierte CLI-Coding-Agenten auflisten oder konfigurieren?
        ├─ JA → ACP (GET /api/acp/agents)
        └─ NEIN → Verwenden Sie einfach /v1/chat/completions
```

## 1. A2A — Agent-to-Agent

**Spezifikation:** [A2A v0.3](https://a2a-protocol.org)
**OmniRoute-Endpunkt:** `POST /a2a` (JSON-RPC 2.0)
**Agentenkarte:** `GET /.well-known/agent.json`

### Einsatzgebiete

- Aufbau eines Multi-Agenten-Systems, in dem OmniRoute einer der Peer-Agenten ist
- Bereitstellung der Routing-Intelligenz von OmniRoute (intelligentes Routing, Kontingentverwaltung usw.) für Agenten in Frameworks wie Google ADK oder generischen Agentennetzwerken
- Einbindung von OmniRoute hinter einer standardisierten Schnittstelle für Erkennung und Aufruf

### Methoden

- `message/send` — eine Nachricht übermitteln und eine synchrone Antwort empfangen
- `message/stream` — eine Nachricht übermitteln und per SSE gestreamte Fortschrittsereignisse empfangen
- `tasks/get` — eine Aufgabe anhand ihrer ID abrufen
- `tasks/cancel` — eine laufende Aufgabe abbrechen

### Integrierte Fähigkeiten (6)

- `smart-routing` — einen Prompt über die optimale Kombination routen
- `quota-management` — den Kontingentstatus für jeden Anbieter melden
- `provider-discovery` — installierte Anbieter mit ihren Funktionen auflisten
- `cost-analysis` — die Kosten einer Anfrage oder Unterhaltung schätzen
- `health-report` — den Status von Schutzschaltern, Abklingzeiten und Sperren für jeden Anbieter aggregieren
- `list-capabilities` — die verfügbaren Fähigkeiten und Metadaten des Agenten auflisten

### Vertiefung

Weitere Informationen zu Transportdetails, zur Struktur der Agentenkarte, zur TTL-Konfiguration von Aufgaben und zur Vorlage für das Hinzufügen neuer Fähigkeiten finden Sie unter [A2A-SERVER.md](./A2A-SERVER.md).

## 2. ACP — CLI-Agenten-Registry

**OmniRoute-Endpunkt:** `GET /api/acp/agents`
**Quelle:** `src/lib/acp/{index,manager,registry}.ts`

### Was es ist

ACP ist OmniRoutes **lokales Inventar für CLI-Agenten**. Es erkennt, welche Coding-CLIs auf dem Host installiert sind (Cursor, Cline, Claude Code, Codex CLI, Continue usw.), ermittelt deren Versionen und zeigt sie im Dashboard an, damit Benutzer jede CLI so konfigurieren können, dass sie auf OmniRoute verweist.

Dies ist KEIN externes Protokoll — es handelt sich um eine interne Registry, die die Benutzeroberfläche „CLI Tools“ und die Verfolgung von CLI-Fingerprints unterstützt (siehe [CLI-TOOLS.md](../reference/CLI-TOOLS.md)).

### Funktionsweise

- Prüft den Host auf installierte CLI-Binärdateien (verwendet je nach Betriebssystem `which` / `where`)
- Liest die Version jeder CLI aus (ruft `<bin> --version` auf)
- Akzeptiert optional benutzerdefinierte Agenten (Binärdateipfad + Versionsprüfung + Startargumente)
- Speichert benutzerdefinierte Agenten in den Einstellungen
- Gibt die zusammengeführte Liste an das Dashboard zurück

### REST-API

| Endpunkt          | Methode | Beschreibung                                                                    | Authentifizierung |
| ----------------- | ------- | ------------------------------------------------------------------------------- | ----------------- |
| `/api/acp/agents` | GET     | Erkannte + benutzerdefinierte Agenten auflisten (installiert/Gesamtanzahl)      | API-Schlüssel     |
| `/api/acp/agents` | POST    | Benutzerdefinierten Agenten hinzufügen/aktualisieren/entfernen (Aktion im Body) | API-Schlüssel     |

Body-Format für POST (`customAgentBodySchema` in `src/app/api/acp/agents/route.ts`):

```json
{
  "action": "add|update|remove",
  "id": "cursor",
  "name": "Cursor",
  "binary": "/usr/local/bin/cursor",
  "versionCommand": "--version",
  "providerAlias": "cursor",
  "spawnArgs": ["--api-base", "http://localhost:20128"],
  "protocol": "stdio"
}
```

### Anwendungsfälle

- Die Dashboard-Seite „CLI Tools“ listet auf, was installiert ist, und hilft dabei, die einzelnen Tools auf OmniRoute zu verweisen
- Mit benutzerdefinierten Agenten können erfahrene Benutzer interne/proprietäre CLIs registrieren, die OmniRoute standardmäßig nicht kennt
- Das Erkennungsergebnis dient als Grundlage für die `cli-tools`-Fingerprint-Matrix

### Wann ACP NICHT verwendet werden sollte

- ACP führt keine Aufgaben _aus_. Es erkennt und konfiguriert lediglich CLIs. Um eine CLI tatsächlich aufzurufen, starten Sie sie selbst mit den von OmniRoute bereitgestellten Umgebungsvariablen (`OPENAI_BASE_URL`, `OPENAI_API_KEY` usw.).

## 3. Cloud-Agenten

**OmniRoute-Endpunkte:** `/api/v1/agents/tasks/*` (Lebenszyklus) + `/api/cloud/*` (Infrastruktur)
**Quelle:** `src/lib/cloudAgent/`

### Was es ist

Eine einheitliche Schnittstelle für Cloud-Coding-Agenten von Drittanbietern. Sie übermitteln einen Prompt + eine Repository-URL, OmniRoute leitet die Anfrage an den richtigen Cloud-Agenten weiter, fragt den Status ab und gibt die Ergebnisse zurück.

### Unterstützte Agenten (4, alle in `src/lib/cloudAgent/agents/` bestätigt)

- `codex-cloud` — OpenAI Codex Cloud
- `devin` — Cognition Devin
- `jules` — Google Jules

### Lebenszyklus

```
POST /api/v1/agents/tasks
  → BaseAgent.createTask() je Agentenklasse
  → externer Dienst beginnt mit der Arbeit
  → Aufgabenzeile wird in der DB erstellt (cloud_agent_tasks)
  ↓
GET /api/v1/agents/tasks/[id]
  → verzögerte Statussynchronisierung mit dem Anbieter
  → gibt aktuellen Status + Plan + Aktivitätsprotokoll zurück
  ↓
POST /api/v1/agents/tasks/[id]   (Aktion: "approve" | "message" | "cancel")
  → leitet die Anfrage an den Anbieter weiter (oder markiert sie lokal als abgebrochen)
  ↓
DELETE /api/v1/agents/tasks/[id]
  → lokaler Abbruch
```

### Authentifizierung

⚠️ **Alle `/api/v1/agents/tasks/*`-Endpunkte erfordern eine Management-Authentifizierung** (Commit `588a0333`). Aufrufer, die ausschließlich Bearer-Authentifizierung verwenden, erhalten seit v3.8.0 den Statuscode 401.

### Ausführliche Informationen

Siehe [CLOUD_AGENT.md](./CLOUD_AGENT.md) für den `CloudAgentBase`-Vertrag, agentenspezifische Details, Schemadetails und Endpunkte für die Anmeldedaten-Infrastruktur.

## Vergleich: A2A vs. Cloud Agents

Beide unterstützen „lang laufende Aufgaben“, jedoch auf unterschiedlichen Ebenen:

| Aspekt                 | A2A                                                                                        | Cloud Agents                                          |
| ---------------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------- |
| Standard               | Offenes A2A v0.3                                                                           | OmniRoute-spezifisch                                  |
| Ausführungsort         | Innerhalb von OmniRoute (verwendet konfigurierte Kombinationen)                            | Extern (Codex-/Devin-/Jules-Server)                   |
| Aufgabendauer          | Standard-TTL: 5 Min. (in `TaskManager` konfigurierbar)                                     | Minuten bis Stunden                                   |
| Repository-Verständnis | Nein (übergibt nur Prompts)                                                                | Ja (Repository-URL + Branch)                          |
| Anwendungsfall         | Agentenübergreifende Zusammenarbeit, intelligentes Routing als Dienst                      | „Feature X in Repository Y implementieren“ delegieren |
| Authentifizierung      | Optionaler `OMNIROUTE_API_KEY` für `/a2a`; Verwaltungszugriff für `/api/a2a/*`-REST-Helfer | Immer Verwaltungszugriff                              |

## Integrationsbeispiele

### A2A-Funktionen von OmniRoute ermitteln

```bash
curl http://localhost:20128/.well-known/agent.json
```

Gibt die Agent Card mit allen 6 Fähigkeiten, Transporten und der Version zurück.

### OmniRoute als A2A-Agent aufrufen

```bash
curl -X POST http://localhost:20128/a2a \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "message/send",
    "params": {
      "messages": [{"role": "user", "content": "Route this prompt"}],
      "skill": "smart-routing"
    },
    "id": 1
  }'
```

### Installierte CLI-Agenten über ACP auflisten

```bash
curl http://localhost:20128/api/acp/agents \
  -H "Authorization: Bearer <api-key>"
```

### Einen benutzerdefinierten CLI-Agenten hinzufügen

```bash
curl -X POST http://localhost:20128/api/acp/agents \
  -H "Authorization: Bearer <api-key>" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "add",
    "id": "my-custom-cli",
    "name": "My Custom CLI",
    "binary": "/opt/mycli/bin/mycli",
    "versionCommand": "--version",
    "providerAlias": "openai"
  }'
```

### Eine Cloud-Agent-Aufgabe übermitteln

```bash
curl -X POST http://localhost:20128/api/v1/agents/tasks \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{
    "providerId": "devin",
    "prompt": "Implement feature X in repo Y",
    "source": {
      "repoName": "user/repo",
      "repoUrl": "https://github.com/user/repo",
      "branch": "main"
    }
  }'
```

### Status einer Cloud-Aufgabe abfragen

```bash
curl http://localhost:20128/api/v1/agents/tasks/<task-id> \
  -H "Cookie: auth_token=..."
```

## Wann was verwendet werden sollte

- **Chatbot-/Copilot-Frontend** → `/v1/chat/completions` (OpenAI-kompatibel – kein Agentenprotokoll)
- **Zusammenarbeit mehrerer Agenten** → A2A
- **Lokale CLIs im Dashboard auflisten** → ACP
- **Lang laufende Programmieraufgaben an Cloud-Dienste delegieren** → Cloud Agents

## Interne Architektur

```
                ┌─────────────────────┐
                │   OmniRoute Core    │
                └─────────────────────┘
                  ↑       ↑        ↑
        ┌─────────┘       │        └─────────┐
        │                 │                  │
    ┌───────┐        ┌─────────┐       ┌────────────┐
    │  A2A  │        │   ACP   │       │  Cloud-    │
    │ (/a2a)│        │ (/acp)  │       │  Agenten   │
    └───────┘        └─────────┘       │ (/v1/agents│
        │                 │            │  /tasks)   │
        ↓                 ↓            └────────────┘
   Externe Peer-     Lokale CLI-             │
   Agenten, die      Binärdateien auf         ↓
   A2A v0.3          dem Host           Codex Cloud, Cursor,
   unterstützen                         Devin, Jules
```

## Siehe auch

- [A2A-SERVER.md](./A2A-SERVER.md) — ausführliche Informationen zu A2A
- [CLOUD_AGENT.md](./CLOUD_AGENT.md) — ausführliche Informationen zu Cloud-Agenten
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — externe CLI-Integrationen (verwenden ACP)
- [SKILLS.md](./SKILLS.md) — Skills-Framework (unterscheidet sich von A2A-Skills — lokale Ausführungs-Sandbox)
- [API_REFERENCE.md](../reference/API_REFERENCE.md#agents-protocol) — Endpunktreferenz
- Quelle: `src/lib/{a2a,acp,cloudAgent}/`
