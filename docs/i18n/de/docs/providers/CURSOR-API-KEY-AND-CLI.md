# Cursor API provider and the Cursor CLI passthrough (Deutsch)

🌐 **Languages:** 🇺🇸 [English](../../../../providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇹 [am](../../../am/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇦 [ar](../../../ar/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇿 [az](../../../az/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇬 [bg](../../../bg/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇩 [bn](../../../bn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇦 [bs](../../../bs/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇿 [cs](../../../cs/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇰 [da](../../../da/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇷 [el](../../../el/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇸 [es](../../../es/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇪 [et](../../../et/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇷 [fa](../../../fa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇮 [fi](../../../fi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇷 [fr](../../../fr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇪 [ga](../../../ga/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [gu](../../../gu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ha](../../../ha/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇱 [he](../../../he/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [hi](../../../hi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇷 [hr](../../../hr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇺 [hu](../../../hu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇲 [hy](../../../hy/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇩 [id](../../../id/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ig](../../../ig/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇹 [it](../../../it/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇯🇵 [ja](../../../ja/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇪 [ka](../../../ka/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇭 [km](../../../km/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [kn](../../../kn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇷 [ko](../../../ko/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇹 [lt](../../../lt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇻 [lv](../../../lv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ml](../../../ml/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [mr](../../../mr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇾 [ms](../../../ms/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇹 [mt](../../../mt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇲 [my](../../../my/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇵 [ne](../../../ne/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇱 [nl](../../../nl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇴 [no](../../../no/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [or](../../../or/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [pa](../../../pa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇭 [phi](../../../phi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇱 [pl](../../../pl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇹 [pt](../../../pt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇴 [ro](../../../ro/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇺 [ru](../../../ru/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇰 [si](../../../si/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇰 [sk](../../../sk/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇮 [sl](../../../sl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇸 [sr](../../../sr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇪 [sv](../../../sv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇪 [sw](../../../sw/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ta](../../../ta/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [te](../../../te/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇭 [th](../../../th/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇷 [tr](../../../tr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇰 [ur](../../../ur/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇿 [uz](../../../uz/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇻🇳 [vi](../../../vi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [yo](../../../yo/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/providers/CURSOR-API-KEY-AND-CLI.md)

---

Zwei Möglichkeiten, Cursor ohne IDE-Sitzung hinter OmniRoute zu verwenden:

1. **`cursor-api`-Provider** (Karte „Cursor API“, Alias `cua`): ein API-Schlüssel-
   Provider, der einen Cursor-Benutzer-API-Schlüssel enthält (`crsr_…`, generiert unter
   `https://cursor.com/dashboard/api`). Jeder OmniRoute-Client kann dann
   über `/v1/chat/completions` als `cursor-api/<model>` oder
   `cua/<model>` auf Cursor-Modelle zugreifen, einschließlich der üblichen Kontingent-,
   Fallback- und Protokollierungsebenen. Der IDE-Provider
   (`cursor`, OAuth-/IDE-Sitzung) bleibt unverändert.
2. **Cursor-CLI-Passthrough**: Richten Sie die Cursor-CLI (`agent`) auf OmniRoute,
   sodass jeder RPC-Aufruf der CLI mit einem OmniRoute-API-Schlüssel authentifiziert,
   mit den Anmeldedaten einer `cursor-api`-Verbindung an Cursor weitergeleitet und auf
   der Protokollseite aufgezeichnet wird.

## Warum der Schlüssel ausgetauscht wird

`api2.cursor.sh` lehnt einen unverarbeiteten `crsr_…`-Schlüssel als Bearer-Token ab (401). Die Cursor-
CLI sendet den Schlüssel zunächst per POST an `/auth/exchange_user_api_key` und erhält ein Sitzungs-
JWT, das nach einer Stunde abläuft; das zurückgegebene `refreshToken` enthält denselben
`exp`-Wert, weshalb eine Aktualisierung einen erneuten Schlüsselaustausch erfordert.
`open-sse/services/cursorApiKeyAuth.ts` führt diesen Austausch durch, speichert pro Schlüssel ein Sitzungs-
Token im Cache, tauscht es fünf Minuten vor Ablauf erneut aus und verwirft das zwischengespeicherte
Token, wenn Cursor mit 401 antwortet. `CursorExecutor` ruft dies unmittelbar vor dem Öffnen
des Upstream-Streams für `cursor-api`-Verbindungen auf.

## Der `cursor-api`-Provider

Registry: `open-sse/config/providers/registry/cursor/index.ts`
(`cursor_apiProvider`, `authType: "apikey"`, dieselben Werte für `format`, `baseUrl` und
`models` wie bei `cursor`). Katalogkarte:
`src/shared/constants/providers/apikey/specialty-media.ts`. Executor-Zuordnung:
`open-sse/executors/index.ts` (`"cursor-api"` / `cua` →
`new CursorExecutor("cursor-api")`).

Dashboard: Provider → Cursor API → API-Schlüssel hinzufügen.

REST:

```bash
curl -sS -X POST http://localhost:20128/api/providers \
  -H "Content-Type: application/json" \
  -d '{"provider":"cursor-api","name":"cursor-api-key","apiKey":"crsr_…","priority":1}'
```

Anschließend:

```bash
curl -sS http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer <omniroute-api-key>" \
  -H "Content-Type: application/json" \
  -d '{"model":"cursor-api/auto","messages":[{"role":"user","content":"say PONG"}]}'
```

Hinweise:

- Die Modellauflistung für `cursor-api` stammt aus der statischen Cursor-Registry (derselben
  Liste, auf die der IDE-Provider als Fallback zurückgreift); auf dem OmniRoute-Host muss
  kein `cursor-agent` installiert sein.
- `POST /api/providers/{id}/refresh-cursor` ist ausschließlich für den `cursor`-IDE-Provider
  vorgesehen; `cursor-api`-Verbindungen verfügen über keine zu erneuernde IDE-Sitzung.

## Native Modell-IDs und Aufwand

Für `cursor` / `cu` und `cursor-api` / `cua` lässt der gemeinsam verwendete Claude-Aufwandsnormalisierer
die angeforderte Modell-ID unverändert. Cursor kann ein Suffix wie
`-low` als Teil einer tatsächlichen Modell-ID veröffentlichen, statt als OmniRoute-Aufwandsalias.
Der Cursor-Executor behält eine exakte Übereinstimmung mit dem Live-Katalog bei; wenn keine
Übereinstimmung vorliegt, übernimmt sein bestehender Modell-Resolver den Fallback vom Suffix
zum Parameter.

Dies ändert nicht die Aufwandsnormalisierung für direkte Claude-, Claude-kompatible
oder Vertex-Routen. Die Verfügbarkeit hängt weiterhin vom Katalog und der Berechtigung
des ausgewählten Cursor-Kontos ab.

## Cursor-CLI-Passthrough

Route: `src/app/api/cursor-cli/[...path]/route.ts` →
`open-sse/handlers/cursorCliProxy.ts`. Das Präfix `/api/cursor-cli/` ist in
`src/shared/constants/publicApiRoutes.ts` registriert, da der Handler seine
eigene Authentifizierung durchsetzt:

| Pfad                                                                                                                          | Von der CLI erwartete Authentifizierung | Verhalten von OmniRoute                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `POST /auth/exchange_user_api_key`                                                                                            | `Bearer <OmniRoute-API-Schlüssel>`      | Validiert den Schlüssel, stellt ein 1 Stunde gültiges HS256-JWT aus (signiert mit `JWT_SECRET`) und gibt es zurück                                                                                   |
| jeder andere Pfad (`/aiserver.v1.*`, `/agent.v1.AgentService/RunSSE`, `/aiserver.v1.BidiService/BidiAppend`, `/v1/traces`, …) | `Bearer <dieses JWT>`                   | Überprüft Aussteller, Zielgruppe und Ablaufzeit, wählt eine aktive `cursor-api`-Verbindung aus, ersetzt den Authorization-Header durch das ausgetauschte Cursor-Token und streamt die Antwort zurück |

Die CLI dekodiert `exp` aus jedem empfangenen Token. Wird ihr ein opakes
Token übergeben, führt sie daher vor fast jeder Anfrage einen erneuten Austausch
durch; das ausgestellte JWT verhindert dies. Bei einer 401-Antwort von
OmniRoute führt die CLI den Austausch erneut durch.

### Einrichtung

1. Erstellen Sie einen OmniRoute-API-Schlüssel (Dashboard → API-Schlüssel) und
   eine `cursor-api`-Verbindung.
2. Konfigurieren Sie die CLI so, dass sie HTTP/1.1 für den Agenten-Stream
   verwendet. In `~/.cursor/cli-config.json`:

   ```json
   { "network": { "useHttp1ForAgent": true } }
   ```

   Ohne diese Einstellung öffnet die CLI den Agentendurchlauf über HTTP/2 zu
   einem separat konfigurierten Agentenhost, und nur die RPCs der
   Steuerungsebene werden über den Endpunkt geleitet.

3. Führen Sie die CLI gegen OmniRoute aus:

   ```bash
   export CURSOR_API_ENDPOINT=http://localhost:20128/api/cursor-cli
   export CURSOR_API_KEY=<omniroute-api-key>
   agent -p --trust "Reply with exactly OK"
   ```

Jeder Hop erscheint in den Protokollen mit dem Anbieter `cursor-api`, dem
Anfragetyp `cursor-cli` und dem Pfad `/api/cursor-cli/<rpc>` und wird dem
OmniRoute-API-Schlüssel sowie der Verbindung zugeordnet, die ihn verarbeitet
hat.

### Fehlerszenarien

| Situation                                                  | Antwort an die CLI                                                 |
| ---------------------------------------------------------- | ------------------------------------------------------------------ |
| Unbekannter OmniRoute-Schlüssel und `REQUIRE_API_KEY=true` | 401 `unauthenticated` beim Austausch                               |
| `REQUIRE_API_KEY=false`                                    | anonyme Sitzung (entspricht dem Verhalten von `/v1/*`)             |
| Abgelaufenes / fremdes / manipuliertes Sitzungs-JWT        | 401, die CLI führt den Austausch erneut durch                      |
| OmniRoute-API-Schlüssel nach dem Austausch widerrufen      | 401 beim nächsten RPC                                              |
| Keine aktive `cursor-api`-Verbindung                       | 503 `unavailable`                                                  |
| Cursor lehnt den Schlüssel der Verbindung ab               | 401 `unauthenticated`, zwischengespeicherte Sitzung wird verworfen |
| Upstream nicht erreichbar                                  | 502 `unavailable` (bereinigte Meldung)                             |
| `JWT_SECRET` nicht gesetzt                                 | 503 beim Austausch                                                 |
