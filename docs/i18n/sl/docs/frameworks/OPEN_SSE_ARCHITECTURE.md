# open-sse Architecture (Slovenščina)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Na kratko**: `open-sse/` je osrednji pretočni mehanizem, ki poganja vsako zahtevo LLM v OmniRoute. Vsebuje približno 1.400 datotek, ki implementirajo cevovod zahtev, izvajalnike, storitve, strežnik MCP in prevajalsko plast. Ta vodnik pojasnjuje, kako so ti deli povezani.

**Vir:** `open-sse/` (paket delovnega prostora, približno 1.440 netestnih datotek `.ts`)

---

## Zakaj ločen paket delovnega prostora?

`open-sse/` je **samostojen delovni prostor** v monorepozitoriju OmniRoute iz več razlogov:

1. **Ponovna uporabnost** — `open-sse` je objavljen kot `@omniroute/open-sse` na npm, zato ga lahko drugi projekti uporabljajo neodvisno
2. **Jasne meje** — pretočni mehanizem je ločen od plasti uporabniškega vmesnika/podatkovne zbirke, specifične za OmniRoute
3. **Zmogljivost** — mehanizem nima odvisnosti od Next.js, kar omogoča hitrejše hladne zagone v okoljih CLI/brezstrežniških okoljih
4. **Različice** — `open-sse` lahko izdaja različice po lastnem urniku

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Struktura na najvišji ravni

```
open-sse/
├── index.ts              # Javna vstopna točka
├── types.d.ts            # Javni izvozi tipov
├── package.json          # @omniroute/open-sse
├── config/               # Konfiguracije ponudnikov, konstante, registri
├── executors/            # Izvajalniki HTTP za posamezne ponudnike (67 + base.ts/index.ts)
├── handlers/             # Obdelovalniki zahtev (chatCore, responses itd.)
├── lib/                  # Notranji pripomočki
├── mcp-server/           # Strežnik Model Context Protocol
├── services/             # Približno 298 storitvenih modulov
├── transformer/          # Pretvornik formata Responses API
├── translator/           # Prevajanje formatov (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Skupni pripomočki (beleženje, napake, pretoki itd.)
```

### Število modulov

| Imenik | Datoteke | Namen |
| `executors/` | 167 | Izvajalniki HTTP za posamezne ponudnike (poenoteni prek tovarne DefaultExecutor) |
| `handlers/` | 157 | Vstopne točke zahtev (chatCore, responses, embeddings) |
| `services/` | ~536 | Usmerjanje, predpomnjenje, omejevanje hitrosti, osveževanje itd. |
| `translator/` | 56 | Pretvorba formatov (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Orodja in prenosi MCP |
| `utils/` | ~108 | Prečni pripomočki (beleženje, napake, pretoki) |
| `config/` | ~339 | Konfiguracije ponudnikov, konstante, registri |

---

## Cevovod zahtev

Vsaka zahteva LLM potuje skozi **5-stopenjski cevovod**:

```
                              ┌──────────────┐
   Zahteva HTTP                │  1. USMERI   │   razreševanje kombinacije, izbira modela
   (pot Next.js)               └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. PREVEDI  │   pretvorba formata (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. IZVEDI   │   izvajalnik ponudnika, HTTP, ponovni poskus, odklopnik
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. PRETAKAJ │   pretvorba SSE, povratni pritisk
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. ZABELEŽI │   sledenje uporabi, dnevnik klicev, razvrščanje napak
                              └──────┬───────┘
                                     │
                                     ▼
                              Odgovor HTTP (SSE ali JSON)
```

### 1. stopnja: Usmerjanje (services/combo.ts)

**Vstopna točka**: `handleComboChat()` v `services/combo.ts`

Razreši zahtevo v konkreten nabor `(provider, model, account, credentials)`:

- Poišče kombinacijo po ID-ju (ali ustvari navidezno kombinacijo za modele `auto/*`)
- Uporabi strategijo usmerjanja (prednostna, utežena, krožna itd.)
- Izloči ponudnike, ki ne delujejo pravilno (odklopnik)
- Izbere naslednji izvedljivi cilj

Za modele `auto/*` ta stopnja tudi:

- Izvede algoritem **ocenjevanja s 16 dejavniki** (`services/autoCombo/`)
- Izbere par `provider+model` glede na stanje, stroške, zakasnitev itd.

### 2. stopnja: Prevajanje (translator/)

Če se izvorni format (npr. OpenAI) razlikuje od ciljnega formata (npr. Claude), se zahteva **prevede**:

- Sistemski poziv → sistemsko sporočilo
- Definicije orodij → format orodij, specifičen za ponudnika
- Parametri sklepanja/razmišljanja → ustrezniki, specifični za ponudnika
- Normalizacija vlog sporočil (`developer` → `system` za ponudnike, ki niso OpenAI)

`translator/index.ts` izpostavlja:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### 3. stopnja: Izvajanje (executors/)

**Vstopna točka**: `getExecutor(providerId).execute(request, options)`

Vsi ponudniki uporabljajo `DefaultExecutor` (`executors/default.ts`) prek nadomestne tovarne `getExecutor()`. Izvajalnik:

- Sestavi URL nadrejene storitve (`buildUrl()`)
- Doda glave, specifične za ponudnika (`buildHeaders()`)
- Pretvori telo zahteve (`transformRequest()`)
- Pošlje zahtevo HTTP s ponovnimi poskusi in eksponentnim podaljševanjem čakanja
- Po potrebi osveži avtentikacijo (ponudniki OAuth)

Vsi izvajalniki razširjajo `BaseExecutor` (`executors/base.ts`, 1170 LOC), ki zagotavlja:

- Skupno logiko ponovnih poskusov
- Integracijo posredniškega strežnika
- Integracijo odklopnika
- Kavlje za beleženje uporabe

### 4. stopnja: Pretakanje (utils/stream.ts)

Pri pretočnih odgovorih izvajalnik vrne **ReadableStream**. Obdelovalnik:

- Pretok preusmeri skozi pretvorbo SSE (`createSSETransformStreamWithLogger`)
- Uporabi periodične signale za zaznavanje prekinjenih povezav
- Elegantno obravnava prekinitev povezave odjemalca (`pipeWithDisconnect`)
- Pretvori SSE → JSON za odjemalce brez pretakanja

Pri nepretočnih odgovorih izvajalnik vrne razčlenjen objekt JSON, ki je posredovan naprej brez sprememb.

### 5. stopnja: Beleženje (services/usage.ts)

Po odgovoru (uspešnem ali neuspešnem) se zabeleži uporaba:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` iz odgovora
- `cost_usd`, izračunan na podlagi podatkov o cenah
- `latency_ms`, `status`, `error_class` v primeru neuspeha
- Shranjeno v tabelo `usage_history`

Artefakti dnevnika klicev (če so omogočeni) se zapišejo v `${DATA_DIR}/call_logs/`.

---

## Podroben pregled ključnih datotek

### chatCore.ts (5977 vrstic)

**Glavni obdelovalnik zahtev**. Kljub velikosti ima jasno strukturo:

```ts
// Psevdostruktura datoteke chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Preverjanje pristnosti + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Preverjanje veljavnosti telesa
  const body = await parseRequestBody(request);

  // 3. Zaznavanje oblike + pretvorba
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Usmerjanje kombinacije
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Nadaljuj z naslednjim ciljem
    }
  }

  // 5. Rezervna možnost v sili
  return await emergencyFallback(body);
}
```

Čeprav gre za eno samo ogromno funkcijo, je organizirana v **razdelke s komentarji**, ki ustrezajo petstopenjskemu cevovodu.

### combo.ts (4456 vrstic kode)

**Mehanizem za usmerjanje**, ki kombinacijo razreši v urejen seznam ciljev.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Podpira **19 strategij usmerjanja** (glejte `src/shared/constants/routingStrategies.ts`):

| Strategija          | Delovanje                                                                                        |
| ------------------- | ------------------------------------------------------------------------------------------------ |
| `priority`          | Urejen seznam s prvim ciljem na prvem mestu                                                      |
| `weighted`          | Verjetnostna izbira glede na utež posameznega cilja                                              |
| `round-robin`       | Kroženje skozi cilje po vrstnem redu                                                             |
| `context-relay`     | Predajanje konteksta med cilji                                                                   |
| `fill-first`        | Zapolnitev kvote pred prehodom na naslednji cilj                                                 |
| `p2c`               | Izbira med dvema možnostma                                                                       |
| `random`            | Enakomerna naključna izbira                                                                      |
| `least-used`        | Izbira cilja z najmanj nedavnimi uporabami                                                       |
| `cost-optimized`    | Najprej najcenejši razpoložljivi cilj                                                            |
| `reset-aware`       | Upošteva časovna okna ponastavitve ponudnikov                                                    |
| `reset-window`      | Usmerjanje na podlagi časovnega okna ponastavitve                                                |
| `headroom`          | Najprej cilj z največ preostale razpoložljive kvote                                              |
| `strict-random`     | Resnično enakomerna izbira (brez uteževanja glede na kakovost)                                   |
| `auto`              | Uporabi ocenjevanje s 16 dejavniki (`autoCombo/`)                                                |
| `lkgp`              | Najprej zadnji znani delujoči ponudnik                                                           |
| `context-optimized` | Najprimernejši za zahteve z dolgim kontekstom                                                    |
| `fusion`            | Vzporedno razpošlje zahtevo skupini, nato pa odgovore sintetizira prek ocenjevalca (`fusion.ts`) |

### base.ts (1170 vrstic kode)

**Abstraktni izvajalnik**, ki ga razširja vseh 107 izvajalnikov. Vsebuje:

- `buildUrl()` — privzeta sestava URL-ja (podrazredi jo preglasijo za prilagojeno vedenje)
- `buildHeaders()` — privzete glave (preverjanje pristnosti, vrsta vsebine)
- `transformRequest()` — privzeto posredovanje brez sprememb
- `execute()` — glavna zanka HTTP s ponovnimi poskusi, eksponentnim zamikom in prekinjevalnikom

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Obravnava vse ponudnike, združljive z OpenAI/Anthropic
  // Ponudniki registrirajo konfiguracije (URL, preverjanje pristnosti, glave), vendar si delijo logiko izvajalnika
}
```

Vedenje, specifično za ponudnika (glave za preverjanje pristnosti, osnovni URL, glave različice), je konfigurirano prek registra ponudnikov in ne prek ločenih razredov izvajalnikov.

````

---

## Storitve (117 modulov)

Storitve so **osredotočeni, namenski moduli**, ki jih obdelovalniki združujejo. Glavne kategorije:

### Usmerjanje in kombiniranje

- `combo.ts` — vstopna točka za kombinirano usmerjene zahteve
- `services/autoCombo/` — 16-faktorsko točkovanje, 8 strategij samodejnega usmerjanja
- `wildcardRouter.ts` — ujema poti z nadomestnimi znaki (`gpt-*`)
- `modelFamilyFallback.ts` — nadomestna izbira znotraj družine T5

### Omejevanje hitrosti in kvote

- `rateLimitManager.ts` — žetonovni vedro za vsako kombinacijo ključa in ponudnika
- `usage.ts` — beleženje uporabe
- `quotaCache.ts` — posnetki kvot v pomnilniku

### Računi in žetoni

- `tokenRefresh.ts` — osvežitev OAuth ob napaki 401
- `accountFallback.ts` — preklop na nadomestni račun
- `sessionManager.ts` — stanje večkrožne seje

### Inteligenca

- `intentClassifier.ts` — razvrščanje namena zahteve
- `taskAwareRouter.ts` — usmerjanje glede na vrsto opravila
- `thinkingBudget.ts` — dodeljevanje žetonov za razmišljanje
- `contextManager.ts` — vstavljanje konteksta usmerjanja

### Odpornost

- `resilience.ts` — usklajevanje ponovnih poskusov, zakasnitev in odklopnika
- `emergencyFallback.ts` — nadomestna možnost v skrajni sili
- `modelDeprecation.ts` — samodejno usmerjanje na nasledniške modele

### Stanje

- `signatureCache.ts` — odstranjevanje dvojnikov glede na podpis zahteve
- `volumeDetector.ts` — razbremenjevanje obremenitve
- `contextHandoff.ts` — serializacija seje

### Stiskanje

- `compression/` (podimenik) — celoten cevovod stiskanja
- 39 datotek, ki zajemajo mehanizme, pakete pravil in prilagojevalnike

### Veščine

- (opisano v [SKILLS.md](./SKILLS.md))

### Pomnilnik

- (opisano v [MEMORY.md](./MEMORY.md))

---

## Izvajalniki (več kot 75 datotek)

Ena datoteka na ponudnika. Vsi razširjajo `BaseExecutor` in preglasijo tisto, kar se razlikuje.

### Pogosti vzorci

Ponudniki se razrešijo prek `getExecutor(providerId)`, ki vrne konfigurirani izvajalnik. Ponudniki, združljivi z OpenAI/Anthropic, uporabljajo `DefaultExecutor` (`executors/default.ts`). Vedenje, specifično za ponudnika (osnovni URL, glave za preverjanje pristnosti, različica API-ja), je konfigurirano v `open-sse/config/providers/`, medtem ko se pretvorbe telesa zahteve izvajajo v `open-sse/translator/`.

**URL po meri** se nastavi prek konfiguracije ponudnika:

```ts
// Konfiguracija ponudnika v open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Preverjanje pristnosti po meri** se obravnava prek konfiguracije preverjanja pristnosti v registru ponudnikov (ključ API, OAuth, profili glav).

**Pretvorbe telesa zahteve po meri** (npr. Anthropicovo ločevanje `system` od `messages`) se registrirajo za posameznega ponudnika v `open-sse/translator/`.

````

### Tovarna izvajalnikov

`executors/index.ts` izvaža `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Razreševanje poteka prek `ExecutorRegistry` (`executors/registry.ts`): vsak specializirani izvajalnik je deklariran v vgrajeni tabeli datoteke `executors/index.ts` in registriran prek `registerExecutor(alias, instance)` ob nalaganju modula; `getExecutor()` preveri register in za vsakega ponudnika brez specializiranega vnosa uporabi predpomnjeni `DefaultExecutor`. Celotna preslikava vzdevek → izvajalnik je opredeljena z referenčnim testom `tests/unit/executor-map-golden.test.ts`.

---

## Prevajalniki

Prevajajte med **3 formati**: OpenAI, Anthropic, Gemini ter novim Responses API.

### Kdaj se izvede prevajanje

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Pogoste pretvorbe:

- `OpenAI → Anthropic`: ločeno polje `system`, glava `x-api-key`
- `OpenAI → Gemini`: `contents` namesto `messages`, `systemInstruction`
- `OpenAI → Responses API`: polje `input`, stanje `previous_response_id`

### Obravnavani robni primeri

- vloga `developer` → `system` za ponudnike, ki niso OpenAI
- vloga `system` → združena v prvo uporabniško sporočilo za GLM/ERNIE
- `json_schema` → Gemini-jeva `responseMimeType` + `responseSchema`
- `tools` → oblika orodij, specifična za ponudnika
- Parametri razmišljanja (o1, Claude) → ustreznice, specifične za ponudnika

---

## Strežnik MCP

`open-sse/mcp-server/` implementira strežnik **Model Context Protocol**:

- **110 orodij** (upravljanje ponudnikov, kombinacije, pomnilnik, predpomnilnik, stiskanje, posredniški strežnik, veščine, igrifikacija, vtičniki, Notion, Obsidian, lokalni korpus)
- **3 prenosi**: stdio, SSE, Streamable HTTP
- **33 obsegov** za natančno določanje pooblastil

### Registracija orodij

Orodja so registrirana kot samostojne datoteke v `open-sse/mcp-server/tools/`, pri čemer vsaka izvaža ime, shemo, obravnavalnik in obseg:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Prenosi

```ts
// stdio (uporaba prek CLI-ja)
startMcpStdio(server);

// SSE (pretočno pošiljanje na osnovi HTTP-ja)
startMcpSse(server, port);

// Streamable HTTP (sodobni MCP)
startMcpStreamable(server, port);
```

### Pooblastila

Vsak klic orodja gre skozi preverjanje obsegov (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Pretvorniki

`open-sse/transformer/` pretvarja med formatoma **Chat Completions** in **Responses API**.

### Zakaj ločen pretvornik?

Responses API je novi format OpenAI s **pogovori s stanjem** (`previous_response_id`). Ko odjemalec pošlje zahtevo Responses, OmniRoute:

1. Interno pretvori Responses → Chat Completions
2. Pošlje ponudniku (kateremu koli ponudniku, ki podpira Chat Completions)
3. Pretvori odgovor nazaj v format Responses
4. Pretočno pošlje pretvorjeni odgovor odjemalcu

Pretvornik (`transformer/responsesTransformer.ts`) zagotavlja:

```ts
createResponsesApiTransformStream(): TransformStream
```

Ta obravnava:

- dogodke `response.output_item.added`
- dogodke `response.output_text.delta`
- dogodek `response.completed`
- Preslikavo klicev orodij (`function_call` ↔ `tool_calls`)

---

## Konfiguracija

`open-sse/config/` vsebuje konfiguracijsko plast:

| Datoteka                      | Namen                                                 |
| ----------------------------- | ----------------------------------------------------- |
| `providerRegistry.ts`         | Register modelov za klepet iz kataloga 352 ponudnikov |
| `providerModels.ts`           | Vzdevki modelov, preslikava formatov                  |
| `constants.ts`                | Časovne omejitve, omejitve, kode stanja               |
| `defaultThinkingSignature.ts` | Privzeti podpis razmišljanja Claude                   |
| `modelStrip.ts` (v storitvah) | Odstranjevanje polj glede na ponudnika                |

### Shema registra ponudnikov

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Preverjanje z Zodom ob nalaganju modula zagotavlja veljavnost vseh konfiguracij ponudnikov.

---

## Omejitve zmogljivosti

Usmerjevalni mehanizem ima strogo določene zmogljivostne omejitve:

| Operacija                                             | Cilj  | Meritev                        |
| ----------------------------------------------------- | ----- | ------------------------------ |
| Razreševanje kombinacij                               | <10ms | Za 50 ciljev                   |
| Preverjanje omejitve hitrosti                         | <1ms  | Žetonovni vedro v pomnilniku   |
| Nadomestni model iz družine modelov                   | <5ms  | Predpomnjene definicije družin |
| Posredovanje zahteve v usmerjanje                     | <2ms  | Kritična pot                   |
| **Brez blokirajočega V/I v kritični poti usmerjanja** | —     | Vse je asinhrono               |

---

## Vzorci, ki se jim je treba izogibati

❌ **Sinhroni klici podatkovne zbirke v `combo.ts`** — vnaprej izračunajte in predpomnite
❌ **Logika ponovnih poskusov v obdelovalnikih** — uporabite `retry()` iz storitve za odpornost
❌ **Neposreden dostop do konfiguracije ponudnika** — uporabite metode za pridobivanje iz `providerRegistry`
❌ **Trdo kodirane verige nadomestnih možnosti** — definirajte jih v `modelFamilyFallback.ts`
❌ **Spreminjanje stanja med sočasnimi zahtevami** — uporabljajte samo kontekst z obsegom zahteve

---

## Dodajanje nove komponente

### Dodajanje nove storitve

1. Ustvarite `open-sse/services/[serviceName].ts` z jasno opredeljeno odgovornostjo
2. Izvozite glavno funkcijo obdelovalnika in vse konstante
3. Dodajte teste enot v `tests/unit/services/[serviceName].test.mjs`
4. Integrirajte jo v cevovod zahtev v `handlers/chatCore.ts` (če je povezana z usmerjanjem)
5. Posodobite logiko usmerjanja v `combo.ts`, če storitev vpliva na izbiro cilja
6. Dokumentirajte jo v tej datoteki

### Dodajanje novega izvajalnika

1. Ustvarite `open-sse/executors/[provider].ts`, ki razširja `BaseExecutor`
2. Registrirajte ga v `config/providerRegistry.ts`
3. Dodajte ga v tovarniško funkcijo v `executors/index.ts`
4. Dodajte teste enot za izvajalnik
5. Dokumentirajte ga v `docs/architecture/ARCHITECTURE.md`

### Dodajanje novega orodja MCP

1. Ustvarite ali posodobite `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definirajte shemo Zod za vhode
3. Registrirajte orodje v `mcp-server/index.ts`
4. Dodajte ga v matriko obsegov v `mcp-server/auth/`
5. Dodajte teste enot

---

## Glejte tudi

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — arhitektura na visoki ravni
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — tehnična referenca
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — pregled po imenikih
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16-faktorsko ocenjevanje
- [MCP-SERVER.md](./MCP-SERVER.md) — strežnik MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — strežnik A2A
- Izvorna koda: `open-sse/` (več kot 400 datotek, ~143K vrstic kode)
