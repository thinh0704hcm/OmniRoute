# Cost & Spend Tracking (Slovenščina)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/COST_TRACKING.md) · 🇪🇹 [am](../../../am/docs/guides/COST_TRACKING.md) · 🇸🇦 [ar](../../../ar/docs/guides/COST_TRACKING.md) · 🇦🇿 [az](../../../az/docs/guides/COST_TRACKING.md) · 🇧🇬 [bg](../../../bg/docs/guides/COST_TRACKING.md) · 🇧🇩 [bn](../../../bn/docs/guides/COST_TRACKING.md) · 🇧🇦 [bs](../../../bs/docs/guides/COST_TRACKING.md) · 🇨🇿 [cs](../../../cs/docs/guides/COST_TRACKING.md) · 🇩🇰 [da](../../../da/docs/guides/COST_TRACKING.md) · 🇩🇪 [de](../../../de/docs/guides/COST_TRACKING.md) · 🇬🇷 [el](../../../el/docs/guides/COST_TRACKING.md) · 🇪🇸 [es](../../../es/docs/guides/COST_TRACKING.md) · 🇪🇪 [et](../../../et/docs/guides/COST_TRACKING.md) · 🇮🇷 [fa](../../../fa/docs/guides/COST_TRACKING.md) · 🇫🇮 [fi](../../../fi/docs/guides/COST_TRACKING.md) · 🇫🇷 [fr](../../../fr/docs/guides/COST_TRACKING.md) · 🇮🇪 [ga](../../../ga/docs/guides/COST_TRACKING.md) · 🇮🇳 [gu](../../../gu/docs/guides/COST_TRACKING.md) · 🇳🇬 [ha](../../../ha/docs/guides/COST_TRACKING.md) · 🇮🇱 [he](../../../he/docs/guides/COST_TRACKING.md) · 🇮🇳 [hi](../../../hi/docs/guides/COST_TRACKING.md) · 🇭🇷 [hr](../../../hr/docs/guides/COST_TRACKING.md) · 🇭🇺 [hu](../../../hu/docs/guides/COST_TRACKING.md) · 🇦🇲 [hy](../../../hy/docs/guides/COST_TRACKING.md) · 🇮🇩 [id](../../../id/docs/guides/COST_TRACKING.md) · 🇳🇬 [ig](../../../ig/docs/guides/COST_TRACKING.md) · 🇮🇹 [it](../../../it/docs/guides/COST_TRACKING.md) · 🇯🇵 [ja](../../../ja/docs/guides/COST_TRACKING.md) · 🇬🇪 [ka](../../../ka/docs/guides/COST_TRACKING.md) · 🇰🇭 [km](../../../km/docs/guides/COST_TRACKING.md) · 🇮🇳 [kn](../../../kn/docs/guides/COST_TRACKING.md) · 🇰🇷 [ko](../../../ko/docs/guides/COST_TRACKING.md) · 🇱🇹 [lt](../../../lt/docs/guides/COST_TRACKING.md) · 🇱🇻 [lv](../../../lv/docs/guides/COST_TRACKING.md) · 🇮🇳 [ml](../../../ml/docs/guides/COST_TRACKING.md) · 🇮🇳 [mr](../../../mr/docs/guides/COST_TRACKING.md) · 🇲🇾 [ms](../../../ms/docs/guides/COST_TRACKING.md) · 🇲🇹 [mt](../../../mt/docs/guides/COST_TRACKING.md) · 🇲🇲 [my](../../../my/docs/guides/COST_TRACKING.md) · 🇳🇵 [ne](../../../ne/docs/guides/COST_TRACKING.md) · 🇳🇱 [nl](../../../nl/docs/guides/COST_TRACKING.md) · 🇳🇴 [no](../../../no/docs/guides/COST_TRACKING.md) · 🇮🇳 [or](../../../or/docs/guides/COST_TRACKING.md) · 🇮🇳 [pa](../../../pa/docs/guides/COST_TRACKING.md) · 🇵🇭 [phi](../../../phi/docs/guides/COST_TRACKING.md) · 🇵🇱 [pl](../../../pl/docs/guides/COST_TRACKING.md) · 🇵🇹 [pt](../../../pt/docs/guides/COST_TRACKING.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/COST_TRACKING.md) · 🇷🇴 [ro](../../../ro/docs/guides/COST_TRACKING.md) · 🇷🇺 [ru](../../../ru/docs/guides/COST_TRACKING.md) · 🇱🇰 [si](../../../si/docs/guides/COST_TRACKING.md) · 🇸🇰 [sk](../../../sk/docs/guides/COST_TRACKING.md) · 🇷🇸 [sr](../../../sr/docs/guides/COST_TRACKING.md) · 🇸🇪 [sv](../../../sv/docs/guides/COST_TRACKING.md) · 🇰🇪 [sw](../../../sw/docs/guides/COST_TRACKING.md) · 🇮🇳 [ta](../../../ta/docs/guides/COST_TRACKING.md) · 🇮🇳 [te](../../../te/docs/guides/COST_TRACKING.md) · 🇹🇭 [th](../../../th/docs/guides/COST_TRACKING.md) · 🇹🇷 [tr](../../../tr/docs/guides/COST_TRACKING.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/COST_TRACKING.md) · 🇵🇰 [ur](../../../ur/docs/guides/COST_TRACKING.md) · 🇺🇿 [uz](../../../uz/docs/guides/COST_TRACKING.md) · 🇻🇳 [vi](../../../vi/docs/guides/COST_TRACKING.md) · 🇳🇬 [yo](../../../yo/docs/guides/COST_TRACKING.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/COST_TRACKING.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/COST_TRACKING.md)

---

Kako OmniRoute ocenjuje, beleži in poroča o stroških vsake zahteve — in zakaj je
številka na nadzorni plošči **sledilnik prihrankov** in ne račun.

Glejte tudi: [Uporabniški priročnik](./USER_GUIDE.md) · [Galerija funkcij](./FEATURES.md)

---

## Kaj je (in kaj ni)

OmniRoute vsaki zaključitvi pripiše strošek v USD na zahtevo tako, da število žetonov
pomnoži s cenami modela. Te številke so osnova za nadzorno ploščo **Stroški**,
CLI `omniroute cost` / `omniroute usage`, izvoze CSV/JSON in proračune posameznih ključev API.

> **»Strošek« na nadzorni plošči je sledilnik prihrankov in ne račun.** OmniRoute vam
> nikoli ničesar ne zaračuna — vaše zahteve usmerja ponudnikom, ki ste jih že povezali
> (vaše lastne naročnine, brezplačne ravni in ključi API). »Skupni strošek 290 USD«, ki je
> v celoti nastal pri brezplačnih modelih, pomeni približno **290 USD, ki jih _niste_ plačali**
> plačljivemu API-ju. Številka je _ocena_ tega, koliko bi isti promet stal po standardnih
> cenah iz cenika, zato lahko vidite, kje je osredotočena vaša uporaba in koliko prihranite
> z usmerjanjem k cenejšim/brezplačnim ponudnikom.

Ta razlaga je neposredno navedena v projektnem dokumentu [README](../../README.md)
(»'strošek' na nadzorni plošči je sledilnik prihrankov in ne račun«).

Ker je številka le ocena:

- Odvisna je od cenika, ki ga ima OmniRoute za vsak model. Model brez vnosa cene
  prispeva strošek `0` (v raziskovalcu je prikazan kot vrstica »Starejši / brezplačen«).
- Promet v brezplačni ravni in prek naročnin še vedno ustvarja _ocenjeni_ strošek — to je
  znesek, ki ga prihranite, in ne dolgovani znesek.

---

## Kako se stroški ocenjujejo

### Vir cen

Stroški izhajajo iz cenika, ki se razreši po naslednjem prednostnem vrstnem redu
([`src/lib/pricingSync.ts`](../../src/lib/pricingSync.ts)):

1. **Uporabniške preglasitve** — cene, ki jih nastavite na nadzorni plošči / prek `PATCH /api/pricing`.
2. **Sinhronizirane zunanje cene** — pridobljene iz LiteLLM-ove javne datoteke
   `model_prices_and_context_window.json`, ko je sinhronizacija omogočena (shranjene v
   ločenem imenskem prostoru `pricing_synced`, zato nikoli ne prepišejo vaših preglasitev).
3. **Vnaprej določene privzete vrednosti** — vključene v OmniRoute.

Sinhronizacija zunanjih cen je **izbirna** in privzeto onemogočena. Ustrezne okoljske
spremenljivke (glejte [`.env.example`](../../.env.example)):

| Okoljska spremenljivka  | Privzeto  | Namen                                                               |
| ----------------------- | --------- | ------------------------------------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`   | Omogoči sinhronizacijo cen LiteLLM v ozadju ob zagonu.              |
| `PRICING_SYNC_INTERVAL` | `86400`   | Interval sinhronizacije v **sekundah** (privzeto enkrat dnevno).    |
| `PRICING_SYNC_SOURCES`  | `litellm` | Seznam virov, ločen z vejicami (trenutno je podprt samo `litellm`). |

### Formula za izračun stroškov

Strošek se izračuna za vsako zahtevo na podlagi števila žetonov in cen na milijon žetonov
v [`src/lib/usage/costCalculator.ts`](../../src/lib/usage/costCalculator.ts)
(`computeCostFromPricing` / `calculateCost`):

- **Vhodni žetoni** (brez branj iz predpomnilnika in žetonov za ustvarjanje predpomnilnika) × cena `input`.
- **Žetoni, prebrani iz predpomnilnika** × cena `cached` (če ni določena, se uporabi cena za vhodne žetone).
- **Žetoni za ustvarjanje predpomnilnika** × cena `cache_creation` (če ni določena, se uporabi cena za vhodne žetone).
- **Izhodni žetoni** × cena `output`.
- **Žetoni sklepanja** × cena `reasoning` (če ni določena, se uporabi cena za izhodne žetone).

Vse cene se razlagajo kot USD na 1.000.000 žetonov. Storitvena raven Codex »fast«/»priority«
ali »flex« uporabi množitelj stroškov (`getCodexFastCostMultiplier`) — raven flex se na
primer obračuna s 50-odstotnim popustom na žetone, kar je na nadzorni plošči prikazano kot
**prihranek flex**.

Imena modelov se najprej normalizirajo (predpone poti ponudnikov, kot sta `openai/` ali
`accounts/fireworks/models/`, se odstranijo), tako da se tudi pretekle vrstice še vedno
ujemajo s ceno.

### Kako se poraba beleži

- Strošek posamezne zahteve se izračuna po odgovoru in zabeleži brez čakanja na dokončanje,
  zato odjemalcu nikoli ne povzroči dodatne zakasnitve. Poraba skupne kvote je načrtovana
  za naslednji cikel zanke dogodkov prek
  [`src/lib/quota/spendRecorder.ts`](../../src/lib/quota/spendRecorder.ts).
- Poraba ključa API se medpomni in paketno zapisuje z
  [`SpendBatchWriter`](../../src/lib/spend/batchWriter.ts) (privzeti interval zapisovanja
  je 60 s, medpomnilnik pa vsebuje 1.000 vnosov). Nastavljivo prek:

  | Okoljska spremenljivka              | Privzeto | Namen                                              |
  | ----------------------------------- | -------- | -------------------------------------------------- |
  | `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` | `60000`  | Interval zapisovanja v milisekundah.               |
  | `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`   | `1000`   | Največje število medpomnjenih vnosov pred zapisom. |

Podatki o stroških na nadzorni plošči se **ne** berejo iz shranjenega dolarskega zneska
za posamezno vrstico — ob vsakem zagonu analitične končne točke se sproti znova izračunajo
iz števila žetonov in trenutnega cenika. To pomeni, da popravek napačne cene (in ponovna
sinhronizacija) retroaktivno posodobi pretekle ocene stroškov.

---

## Nadzorna plošča: stran Stroški

Stran **Stroški** je na naslovu `/dashboard/costs`
(`src/app/(dashboard)/dashboard/costs/`).
Njen glavni pogled je zavihek **Pregled stroškov**
(`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`),
ki vse podatke naloži prek `GET /api/usage/analytics`.

Kaj prikazuje:

- **Ploščice s porabo** — ocenjeni stroški za _danes (1d)_, _7d_, _30d_ in izbrano
  časovno obdobje. Izbirnik obdobja: `7d`, `30d`, `90d`, `all`.
- **Ključne metrike** — zahteve v časovnem obdobju, aktivni ponudniki, aktivni modeli in
  povprečni strošek na zahtevo.
- **Raziskovalec stroškov** — tabela z možnostjo razvrščanja in filtriranja, združena po
  **ponudniku**, **modelu**, **ključu API**, **računu** ali **ravni storitve**, s stroški,
  zahtevami, žetoni, povprečnim stroškom na zahtevo in odstotnim deležem celote.
- **Poraba žetonov** — skupno število ter število vhodnih in izhodnih žetonov ter razmerje
  med vhodnimi in izhodnimi žetoni.
- **Učinkovitost usmerjanja** — število preklopov na nadomestno možnost, stopnja takih
  preklopov in pokritost zahtevanega modela.
- **Mesečna napoved** — napoved stroškov do konca meseca na podlagi nedavnega dnevnega
  povprečja.
- **Primerjava obdobij** — odstotna sprememba med prvo in drugo polovico časovnega obdobja.
- **Grafikoni** — dnevni trend stroškov, delež ponudnikov (tortni grafikon), vodilni
  ponudniki, vodilni modeli, stroški po ključu API, stroški po računu, tedenski vzorec
  uporabe in toplotni zemljevid dejavnosti.
- **Izvoz** — prenos trenutnega časovnega obdobja v obliki **CSV** ali **JSON** (gumba se
  prikažeta, ko obstajajo podatki o stroških, večji od nič).

Ko ni obračunanega prometa, se v vrsticah namesto `$0` prikaže oznaka »Starejše / brezplačno«,
kar odraža model sledenja prihrankom.

### Povezane podstrani Stroškov

Območje Stroški vključuje tudi naslednje strani (vse pod `/dashboard/costs/`):

- **Cene** (`/dashboard/costs/pricing`) — ogled in preglasitev cen za posamezne modele
  (prikaže skupni zavihek Cene).
- **Proračun** (`/dashboard/costs/budget`) — nastavitev omejitev porabe po obsegu
  (prikaže skupni zavihek Proračun).
- **Deljenje kvote** (`/dashboard/costs/quota-share`) — skladi skupne kvote in pogledi
  stopnje porabe.

---

## Končne točke API

Vse zahtevajo overjanje za upravljanje (povratna zanka/JWT prek `requireManagementAuth`),
razen kjer je navedeno drugače.

### Analitika uporabe in stroškov

| Metoda | Končna točka             | Namen                                                                                                                                                                         |
| ------ | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/analytics`   | Celovita analitika stroškov/uporabe: povzetek, dnevni trend, po ponudniku/modelu/ključu API/računu/ravni. Poizvedba: `range`, `startDate`, `endDate`, `apiKeyIds`, `presets`. |
| `GET`  | `/api/usage/utilization` | Izkoriščenost kvote po ponudnikih skozi čas. Poizvedba: `range` (`1h`/`24h`/`7d`/`30d`), `provider`.                                                                          |
| `GET`  | `/api/usage/history`     | Neobdelane vrstice zgodovine uporabe.                                                                                                                                         |
| `GET`  | `/api/usage/call-logs`   | Dnevniki posameznih zahtev (model, žetoni, strošek, zakasnitev, stanje).                                                                                                      |
| `GET`  | `/api/usage/quota`       | Stanje kvote ponudnika.                                                                                                                                                       |
| `GET`  | `/api/usage/proxy-logs`  | Dnevniki zahtev posredniškega strežnika.                                                                                                                                      |

### Proračuni

| Metoda | Končna točka             | Namen                                                                                                 |
| ------ | ------------------------ | ----------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/budget`      | Povzetek stroškov in preverjanje proračuna za en ključ API (zahtevan parameter poizvedbe `apiKeyId`). |
| `POST` | `/api/usage/budget`      | Nastavitev dnevnih/tedenskih/mesečnih omejitev v USD in opozorilnega praga za ključ API.              |
| `GET`  | `/api/usage/budget/bulk` | Množični povzetki proračunov za več ključev API.                                                      |

> API za proračun je omejen na posamezen **ključ API** (`apiKeyId`). Omejitve, ki jih vrne
> `GET /api/usage/budget`, vključujejo `dailyLimitUsd`, `weeklyLimitUsd`, `monthlyLimitUsd`,
> `warningThreshold` in tekoče skupne vrednosti (`totalCostToday`, `totalCostMonth`, …).

### Cene

| Metoda   | Končna točka            | Namen                                                                                                                |
| -------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `GET`    | `/api/pricing`          | Trenutne združene cene (uporabniške + sinhronizirane + privzete). `?includeSources=1` prikaže vir posameznega vnosa. |
| `PATCH`  | `/api/pricing`          | Preglasitev cen za `{ provider: { model: { input, output, cached, … } } }`.                                          |
| `DELETE` | `/api/pricing`          | Ponastavitev cen na privzete vrednosti (izbirno omejeno z `?provider=&model=`).                                      |
| `GET`    | `/api/pricing/defaults` | Prikaz privzetih nadomestnih tarif na 1 milijon enot.                                                                |
| `GET`    | `/api/pricing/models`   | Cene, indeksirane po modelu.                                                                                         |
| `POST`   | `/api/pricing/sync`     | Sprožitev ročne sinhronizacije iz zunanjih virov (LiteLLM).                                                          |
| `GET`    | `/api/pricing/sync`     | Trenutno stanje sinhronizacije.                                                                                      |
| `DELETE` | `/api/pricing/sync`     | Izbris vseh sinhroniziranih podatkov o cenah.                                                                        |

### Druge končne točke, povezane s stroški

| Metoda | Končna točka                  | Namen                                                                                          |
| ------ | ----------------------------- | ---------------------------------------------------------------------------------------------- |
| `GET`  | `/api/free-tier/summary`      | Skupno število žetonov brezplačnih modelov, poraba v tem mesecu in preostala brezplačna kvota. |
| `GET`  | `/api/quota/pools/[id]/usage` | Poraba skupine s skupno kvoto.                                                                 |

---

## CLI

Vmesnik CLI orodja OmniRoute ponuja ukaze za stroške, uporabo in cene (registrirane v
[`bin/cli/commands/registry.mjs`](../../bin/cli/commands/registry.mjs)).

### `omniroute cost`

Poročilo o stroških, združeno iz podatkov `/api/usage/analytics`.

```bash
omniroute cost                          # zadnjih 30 dni, združeno po ponudniku
omniroute cost --period 7d              # zadnjih 7 dni
omniroute cost --group-by model         # združi po provider | model | combo | api-key | day
omniroute cost --since 2026-06-01 --until 2026-06-13
omniroute cost --api-key <key> --limit 50
```

Stolpci: skupina, zahteve, vhodni/izhodni žetoni, strošek (USD) in % skupnega zneska. Vrstica s skupnim seštevkom
se izpiše na koncu (izpis je onemogočen z `--quiet` ali `--output json`).

### `omniroute usage`

```bash
omniroute usage analytics --period 30d [--provider <id>]   # povzetek stroškov po ponudnikih
omniroute usage logs [--limit 100] [--follow] [--api-key <k>] [--search <q>]
omniroute usage quota [--provider <id>] [--check]
omniroute usage utilization [--api-key <k>]
omniroute usage history [--limit 100]
omniroute usage proxy-logs [--limit 100]

# Proračuni
omniroute usage budget list
omniroute usage budget get [scope]
omniroute usage budget set <amount> [--scope global] [--period monthly]
omniroute usage budget reset [scope]
```

### `omniroute pricing`

```bash
omniroute pricing list [--provider <p>] [--model <m>] [--limit 200]
omniroute pricing get <model>
omniroute pricing sync [--provider <p>] [--force]   # POST /api/pricing/sync
omniroute pricing diff [--model <m>]
omniroute pricing defaults show
omniroute pricing defaults set [--input <p>] [--output <p>] [--cache-read <p>] [--cache-write <p>]
```

> `pricing defaults show` bere podatke iz `GET /api/pricing/defaults`. Če želite namesto tega urediti cene
> posameznih modelov, uporabite stran nadzorne plošče **Cene** ali `PATCH /api/pricing`.

---

## Odpravljanje težav

- **Vsi stroški so prikazani kot $0 / »Legacy / Free«.** Uporabljeni modeli nimajo vnosa s ceno.
  Omogočite zunanjo sinhronizacijo (`PRICING_SYNC_ENABLED=true`) in zaženite `omniroute pricing sync` ali
  ročno nastavite cene na strani Cene oziroma prek `PATCH /api/pricing`.
- **Cena zgodovinskega modela je napačna.** Popravite ceno (s preglasitvijo ali ponovno sinhronizacijo) — strošek se
  ob vsakem branju analitičnih podatkov znova izračuna iz števila žetonov, zato se ocene posodobijo tudi za nazaj.
- **Poraba zaostaja za dejanskim stanjem.** Poraba za posamezen ključ se obdeluje paketno; znižajte
  `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS`, če potrebujete bolj ažurne podatke.

---

Za umestitev teh funkcij v širši kontekst nadzorne plošče si oglejte [Uporabniški priročnik](./USER_GUIDE.md) in
[Galerijo funkcij](./FEATURES.md).
