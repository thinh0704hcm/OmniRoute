# Cursor API provider and the Cursor CLI passthrough (Bahasa Indonesia)

🌐 **Languages:** 🇺🇸 [English](../../../../providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇹 [am](../../../am/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇦 [ar](../../../ar/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇿 [az](../../../az/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇬 [bg](../../../bg/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇩 [bn](../../../bn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇦 [bs](../../../bs/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇿 [cs](../../../cs/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇰 [da](../../../da/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇩🇪 [de](../../../de/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇷 [el](../../../el/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇸 [es](../../../es/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇪🇪 [et](../../../et/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇷 [fa](../../../fa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇮 [fi](../../../fi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇫🇷 [fr](../../../fr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇪 [ga](../../../ga/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [gu](../../../gu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ha](../../../ha/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇱 [he](../../../he/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [hi](../../../hi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇷 [hr](../../../hr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇭🇺 [hu](../../../hu/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇦🇲 [hy](../../../hy/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [ig](../../../ig/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇹 [it](../../../it/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇯🇵 [ja](../../../ja/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇬🇪 [ka](../../../ka/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇭 [km](../../../km/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [kn](../../../kn/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇷 [ko](../../../ko/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇹 [lt](../../../lt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇻 [lv](../../../lv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ml](../../../ml/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [mr](../../../mr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇾 [ms](../../../ms/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇹 [mt](../../../mt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇲🇲 [my](../../../my/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇵 [ne](../../../ne/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇱 [nl](../../../nl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇴 [no](../../../no/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [or](../../../or/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [pa](../../../pa/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇭 [phi](../../../phi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇱 [pl](../../../pl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇹 [pt](../../../pt/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇴 [ro](../../../ro/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇺 [ru](../../../ru/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇱🇰 [si](../../../si/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇰 [sk](../../../sk/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇮 [sl](../../../sl/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇷🇸 [sr](../../../sr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇸🇪 [sv](../../../sv/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇰🇪 [sw](../../../sw/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [ta](../../../ta/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇮🇳 [te](../../../te/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇭 [th](../../../th/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇷 [tr](../../../tr/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇵🇰 [ur](../../../ur/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇺🇿 [uz](../../../uz/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇻🇳 [vi](../../../vi/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇳🇬 [yo](../../../yo/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/providers/CURSOR-API-KEY-AND-CLI.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/providers/CURSOR-API-KEY-AND-CLI.md)

---

Dua cara untuk menempatkan Cursor di belakang OmniRoute tanpa sesi IDE:

1. **Penyedia `cursor-api`** (kartu "Cursor API", alias `cua`): penyedia berbasis kunci API
   yang menyimpan kunci API pengguna Cursor (`crsr_…`, dibuat di
   `https://cursor.com/dashboard/api`). Setiap klien OmniRoute kemudian dapat mengakses
   model Cursor melalui `/v1/chat/completions` sebagai `cursor-api/<model>` atau
   `cua/<model>`, dengan lapisan kuota, fallback, dan pencatatan seperti biasa. Penyedia IDE
   (`cursor`, sesi OAuth/IDE) tidak berubah.
2. **Passthrough Cursor CLI**: arahkan Cursor CLI (`agent`) ke OmniRoute sehingga
   setiap RPC yang dibuat CLI diautentikasi dengan kunci API OmniRoute, diteruskan
   ke Cursor menggunakan kredensial koneksi `cursor-api`, dan dicatat di
   halaman Logs.

## Alasan kunci dipertukarkan

`api2.cursor.sh` menolak kunci mentah `crsr_…` sebagai token Bearer (401). Cursor
CLI terlebih dahulu mengirimkan kunci melalui POST ke `/auth/exchange_user_api_key` dan menerima JWT
sesi yang kedaluwarsa setelah satu jam; `refreshToken` yang dikembalikan memiliki
`exp` yang sama, sehingga penyegaran berarti mempertukarkan kembali kunci tersebut.
`open-sse/services/cursorApiKeyAuth.ts` melakukan pertukaran tersebut, menyimpan satu token
sesi dalam cache untuk setiap kunci, melakukan pertukaran ulang lima menit sebelum kedaluwarsa, dan menghapus
token yang disimpan dalam cache ketika Cursor merespons dengan 401. `CursorExecutor` memanggilnya tepat sebelum membuka
stream upstream untuk koneksi `cursor-api`.

## Penyedia `cursor-api`

Registri: `open-sse/config/providers/registry/cursor/index.ts`
(`cursor_apiProvider`, `authType: "apikey"`, dengan `format`, `baseUrl`, dan
`models` yang sama seperti `cursor`). Kartu katalog:
`src/shared/constants/providers/apikey/specialty-media.ts`. Peta executor:
`open-sse/executors/index.ts` (`"cursor-api"` / `cua` →
`new CursorExecutor("cursor-api")`).

Dasbor: Providers → Cursor API → Add API key.

REST:

```bash
curl -sS -X POST http://localhost:20128/api/providers \
  -H "Content-Type: application/json" \
  -d '{"provider":"cursor-api","name":"cursor-api-key","apiKey":"crsr_…","priority":1}'
```

Kemudian:

```bash
curl -sS http://localhost:20128/v1/chat/completions \
  -H "Authorization: Bearer <omniroute-api-key>" \
  -H "Content-Type: application/json" \
  -d '{"model":"cursor-api/auto","messages":[{"role":"user","content":"say PONG"}]}'
```

Catatan:

- Daftar model untuk `cursor-api` berasal dari registri statis Cursor (daftar
  yang sama yang digunakan sebagai fallback oleh penyedia IDE); instalasi `cursor-agent` tidak
  diperlukan pada host OmniRoute.
- `POST /api/providers/{id}/refresh-cursor` hanya untuk penyedia IDE `cursor`;
  koneksi `cursor-api` tidak memiliki sesi IDE yang perlu diperbarui.

## ID model native dan effort

Untuk `cursor` / `cu` dan `cursor-api` / `cua`, normalizer effort Claude bersama
mempertahankan ID model yang diminta tanpa perubahan. Cursor dapat mengiklankan sufiks seperti
`-low` sebagai bagian dari ID model sebenarnya, bukan sebagai alias effort OmniRoute.
Executor Cursor mempertahankan kecocokan persis dengan katalog aktif; jika tidak ada
kecocokan, resolver model yang sudah ada menangani fallback dari sufiks ke parameter.

Hal ini tidak mengubah normalisasi effort untuk rute Claude langsung, yang kompatibel
dengan Claude, atau Vertex. Ketersediaan tetap bergantung pada katalog dan hak akses
akun Cursor yang dipilih.

## Passthrough Cursor CLI

Rute: `src/app/api/cursor-cli/[...path]/route.ts` →
`open-sse/handlers/cursorCliProxy.ts`. Prefiks `/api/cursor-cli/`
didaftarkan di `src/shared/constants/publicApiRoutes.ts` karena handler
menerapkan autentikasinya sendiri:

| Path                                                                                                                            | Autentikasi yang diharapkan dari CLI | Yang dilakukan OmniRoute                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `POST /auth/exchange_user_api_key`                                                                                              | `Bearer <OmniRoute API key>`         | Memvalidasi kunci, menerbitkan JWT HS256 berdurasi 1 jam (ditandatangani dengan `JWT_SECRET`), lalu mengembalikannya                                                                       |
| setiap path lainnya (`/aiserver.v1.*`, `/agent.v1.AgentService/RunSSE`, `/aiserver.v1.BidiService/BidiAppend`, `/v1/traces`, …) | `Bearer <that JWT>`                  | Memverifikasi penerbit/audiens/kedaluwarsa, memilih koneksi `cursor-api` yang aktif, mengganti header Authorization dengan token Cursor hasil pertukaran, lalu mengalirkan respons kembali |

CLI mendekode `exp` dari token apa pun yang diterimanya, sehingga memberinya token
buram membuatnya melakukan pertukaran ulang sebelum hampir setiap permintaan; JWT
yang diterbitkan menghindari hal tersebut. Respons 401 dari OmniRoute membuat CLI
melakukan pertukaran ulang.

### Penyiapan

1. Buat kunci API OmniRoute (Dasbor → Kunci API) dan koneksi `cursor-api`.
2. Atur CLI agar menggunakan HTTP/1.1 untuk stream agen. Di
   `~/.cursor/cli-config.json`:

   ```json
   { "network": { "useHttp1ForAgent": true } }
   ```

   Tanpa pengaturan ini, CLI membuka giliran agen melalui HTTP/2 ke host agen
   yang dikonfigurasi secara terpisah dan hanya RPC bidang kontrol yang melewati
   endpoint.

3. Jalankan CLI menggunakan OmniRoute:

   ```bash
   export CURSOR_API_ENDPOINT=http://localhost:20128/api/cursor-cli
   export CURSOR_API_KEY=<omniroute-api-key>
   agent -p --trust "Reply with exactly OK"
   ```

Setiap hop muncul di Log dengan penyedia `cursor-api`, jenis permintaan `cursor-cli`,
path `/api/cursor-cli/<rpc>`, yang dikaitkan dengan kunci API OmniRoute dan
koneksi yang melayaninya.

### Mode kegagalan

| Situasi                                                  | Respons kepada CLI                                |
| -------------------------------------------------------- | ------------------------------------------------- |
| Kunci OmniRoute tidak dikenal dan `REQUIRE_API_KEY=true` | 401 `unauthenticated` saat pertukaran             |
| `REQUIRE_API_KEY=false`                                  | sesi anonim (mencerminkan perilaku `/v1/*`)       |
| JWT sesi kedaluwarsa / asing / dimanipulasi              | 401, CLI melakukan pertukaran ulang               |
| Kunci API OmniRoute dicabut setelah pertukaran           | 401 pada RPC berikutnya                           |
| Tidak ada koneksi `cursor-api` yang aktif                | 503 `unavailable`                                 |
| Cursor menolak kunci milik koneksi                       | 401 `unauthenticated`, sesi yang di-cache dihapus |
| Upstream tidak dapat dijangkau                           | 502 `unavailable` (pesan yang telah disanitasi)   |
| `JWT_SECRET` tidak ditetapkan                            | 503 saat pertukaran                               |
