# Usage, Quota & Spend Tracking (Bahasa Indonesia)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/USAGE_QUOTA_GUIDE.md) · 🇪🇹 [am](../../../am/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇦🇿 [az](../../../az/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇩🇰 [da](../../../da/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇩🇪 [de](../../../de/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇬🇷 [el](../../../el/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇪🇸 [es](../../../es/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇪🇪 [et](../../../et/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇱 [he](../../../he/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇹 [it](../../../it/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇭 [km](../../../km/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇲 [my](../../../my/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇴 [no](../../../no/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [or](../../../or/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇰 [si](../../../si/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [te](../../../te/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇭 [th](../../../th/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/USAGE_QUOTA_GUIDE.md)

---

> **TL;DR**: OmniRoute melacak penggunaan token dari setiap permintaan, menghitung biaya, menerapkan kuota per kunci API, dan menampilkan analitik di dasbor. Panduan ini menjelaskan cara kerja semuanya.

**Sumber:**

- `open-sse/services/usage.ts` (~70KB) — pelacakan penggunaan utama
- `src/lib/usageAnalytics.ts` (~10KB) — agregasi untuk dasbor
- `src/lib/db/quotaSnapshots.ts` — data historis kuota
- `src/lib/db/usage*.ts` — beberapa modul basis data terkait penggunaan

---

## Gambaran Umum

Setiap permintaan yang mengalir melalui OmniRoute menghasilkan **catatan penggunaan** yang mencakup:

- **Identitas**: kunci API, penyedia, model, dan combo yang digunakan
- **Token**: token prompt, token penyelesaian, token yang di-cache, total
- **Biaya**: jumlah dalam USD (dihitung dari data harga)
- **Waktu**: latensi, stempel waktu mulai/selesai
- **Status**: berhasil, error, dibatasi lajunya, dan sebagainya

Catatan ini diagregasikan menjadi **analitik**, disimpan sebagai **snapshot kuota**, dan digunakan untuk menerapkan **batas anggaran per kunci**.

```
Permintaan ──▶ chatCore ──▶ usage.record() ──▶ SQLite
                                  │
                          ┌───────┼───────┐
                          ▼       ▼       ▼
                     analitik   kuota   penagihan
                     (dasbor) (penerapan) (ekspor)
```

---

## Data yang Dicatat

Layanan `usage.ts` mencatat sebuah **peristiwa penggunaan** untuk setiap permintaan:

| Bidang             | Tipe    | Sumber                                                     |
| ------------------ | ------- | ---------------------------------------------------------- |
| `id`               | string  | UUID yang dibuat saat pencatatan                           |
| `apiKeyId`         | string  | Kunci API yang memulai permintaan                          |
| `provider`         | string  | ID penyedia (openai, anthropic, dan sebagainya)            |
| `model`            | string  | ID model (gpt-5, claude-opus-4-6, dan sebagainya)          |
| `comboId`          | string? | ID combo jika dirutekan melalui sebuah combo               |
| `promptTokens`     | number  | Dari respons upstream                                      |
| `completionTokens` | number  | Dari respons upstream                                      |
| `cachedTokens`     | number  | Token cache hit (caching prompt Anthropic, dan sebagainya) |
| `totalTokens`      | number  | prompt + penyelesaian                                      |
| `costUsd`          | number  | Dihitung dari data harga                                   |
| `latencyMs`        | number  | Durasi permintaan menyeluruh                               |
| `status`           | enum    | `success`, `error`, `rate_limited`, `timeout`, `cancelled` |
| `errorClass`       | string? | Kelas error jika status != success                         |
| `timestamp`        | string  | ISO 8601 UTC                                               |
| `metadata`         | object  | Data khusus yang disuntikkan oleh plugin                   |

### Asal Token

Token diekstrak dari respons penyedia upstream di **penangan respons**:

```ts
// Dari open-sse/handlers/chatCore.ts
const response = await providerExecutor.execute(provider, request);
const usage = response.usage || {
  prompt_tokens: 0,
  completion_tokens: 0,
  cached_tokens: 0,
};
```

Untuk penyedia yang tidak mengembalikan penggunaan (beberapa penyedia berbasis cookie web), OmniRoute **memperkirakan** token menggunakan heuristik `~4 karakter per token` (lihat `open-sse/services/autoCombo/pipelineRouter.ts`).

### Token yang Di-cache

OmniRoute melacak `cached_tokens` secara terpisah dari `prompt_tokens` karena:

- Caching prompt Anthropic mengenakan tarif lebih rendah untuk token yang di-cache (10% dari tarif normal)
- Beberapa penyedia mengembalikan `cache_read_input_tokens` yang harus diberi harga berbeda
- Analitik dapat menampilkan **rasio cache hit** = `cached_tokens / prompt_tokens`

---

## Perhitungan Biaya

Biaya dihitung dari **data harga** yang disinkronkan dari LiteLLM (`src/lib/pricingSync.ts`):

| Model             | Input $/1M | Output $/1M | Cache $/1M |
| ----------------- | ---------- | ----------- | ---------- |
| gpt-5             | $2.50      | $10.00      | —          |
| claude-opus-4-6   | $15.00     | $75.00      | $1.50      |
| claude-sonnet-4-5 | $3.00      | $15.00      | $0.30      |
| gemini-2.5-pro    | $1.25      | $10.00      | —          |

Rumus biaya (`src/lib/usage/costCalculator.ts`):

```ts
cost =
  (prompt_tokens - cached_tokens) * input_price +
  cached_tokens * cached_price +
  completion_tokens * output_price;
```

> **Mengapa token yang di-cache dikurangi dari prompt?** Bagian yang di-cache memiliki harga tersendiri; mengenakan harga input pada seluruh prompt akan menyebabkan penghitungan berlebih.

### Sinkronisasi Harga

Data harga disinkronkan secara otomatis dari LiteLLM melalui endpoint `/api/pricing/sync` (dipicu oleh tugas cron bawaan, bukan variabel lingkungan yang tersedia bagi pengguna):

```bash
# Pemicu manual
curl -X POST http://localhost:20128/api/pricing/sync
```

Untuk model yang tidak memiliki data harga, OmniRoute akan **memperkirakan biaya** menggunakan tarif rata-rata internal (bersumber dari data harga LiteLLM).

---

## Agregasi Rentang Tanggal

Modul `usageAnalytics.ts` menghitung widget dasbor dari data penggunaan mentah. Modul ini mendukung 7 rentang waktu:

| Rentang  | Periode                             | Kegunaan                       |
| -------- | ----------------------------------- | ------------------------------ |
| `1d`     | 24 jam terakhir                     | Deteksi lonjakan biaya per jam |
| `7d`     | 7 hari terakhir                     | Tinjauan mingguan              |
| `30d`    | 30 hari terakhir                    | Penagihan bulanan              |
| `90d`    | 90 hari terakhir                    | Analisis triwulanan            |
| `ytd`    | Sejak 1 Jan tahun berjalan          | Pelacakan anggaran tahunan     |
| `all`    | Sepanjang waktu                     | Statistik sepanjang masa       |
| `custom` | Awal/akhir yang ditentukan pengguna | Audit, kueri ad hoc            |

### Widget Dasbor yang Dihitung

Untuk setiap rentang tanggal, lapisan analitik menghitung:

| Widget                   | Deskripsi                                                              |
| ------------------------ | ---------------------------------------------------------------------- |
| **Kartu ringkasan**      | Total permintaan, total biaya, total token, tingkat keberhasilan       |
| **Grafik tren harian**   | Biaya + token per hari, ditumpuk berdasarkan model                     |
| **Peta panas aktivitas** | Kisi jam dalam sehari × hari dalam seminggu, warna = jumlah permintaan |
| **Rincian model**        | Diagram lingkaran biaya berdasarkan model                              |
| **Rincian penyedia**     | Diagram batang permintaan berdasarkan penyedia                         |
| **Kunci API teratas**    | Tabel 10 kunci teratas berdasarkan biaya                               |
| **Analisis kesalahan**   | Tingkat kesalahan dari waktu ke waktu, kelas kesalahan teratas         |

### Akses Terprogram

````ts
import { computeAnalytics } from "@/lib/usageAnalytics";

const analytics = await computeAnalytics(
  history,              // catatan riwayat penggunaan
  "7d",                 // rentang waktu: "1d" | "7d" | "30d" | "90d" | "ytd" | "all" | "custom"
  connectionMap,        // peta koneksi penyedia (connectionId → nama akun)
  {
    startDate: "2025-01-01",  // opsional: untuk rentang "custom"
    endDate: "2025-06-01",   // opsional: untuk rentang "custom"
  }
);

console.log(analytics.summary.totalCost);   // 12.34 (sen)
console.log(analytics.byModel[0]);           // { model, biaya, permintaan, token prompt, token penyelesaian }

---

## Penerapan Kuota

Kuota per kunci API diterapkan di dua tempat:

1. **Batas lunak** (`quotaWarnAt`): peringatan di dasbor ketika penggunaan melampaui ambang batas
2. **Batas keras** (`quotaLimit`): permintaan ditolak dengan HTTP 429 ketika batas terlampaui

### Konfigurasi

```ts
// Per kunci API
await updateApiKey(keyId, {
  quotaWarnAt: 5_00,    // $5.00 — tampilkan peringatan
  quotaLimit: 10_00,    // $10.00 — penghentian mutlak
  quotaWindow: "month", // "day" | "week" | "month" | "all"
});
````

### Alur Penerapan

```
Permintaan ──▶ quotaCheck()
                 │
                 ├── Dalam batas?  ──▶ izinkan
                 │
                 └── Melebihi batas?  ──▶ 429 Too Many Requests
                                          dengan header Retry-After
```

### Snapshot Kuota

Tabel `quotaSnapshots` menyimpan **status kuota historis** untuk analisis tren:

| Bidang | Deskripsi |
| ----------- | ------------------------------------------ | ------ | ------- |
| `apiKeyId` | Kunci yang dilacak |
| `window` | "day" | "week" | "month" |
| `used` | Biaya yang digunakan dalam jendela ini (sen) |
| `limit` | Batas (sen) |
| `resetAt` | Waktu jendela diatur ulang |
| `createdAt` | Waktu snapshot diambil |

Snapshot diambil **pada setiap permintaan** yang menggunakan biaya > 0, dan digunakan untuk:

- Merender bilah progres kuota di dasbor
- Menampilkan grafik tren kuota 30 hari
- Memicu peringatan ketika penggunaan mendekati batas

---

## REST API

### Daftar Catatan Penggunaan

```bash
GET /api/usage?range=7d&limit=100
GET /api/usage?apiKeyId=key-123&range=30d
GET /api/usage?provider=openai&range=1d
```

Respons:

```json
{
  "records": [
    {
      "id": "uuid",
      "apiKeyId": "key-123",
      "provider": "openai",
      "model": "gpt-5",
      "promptTokens": 1234,
      "completionTokens": 567,
      "totalTokens": 1801,
      "costUsd": 0.005,
      "latencyMs": 1234,
      "status": "success",
      "timestamp": "2026-06-08T12:00:00Z"
    }
  ],
  "total": 1234,
  "nextCursor": "..."
}
```

### Dapatkan Ringkasan Analitik

```bash
GET /api/usage/analytics?range=7d&groupBy=model
```

Respons:

```json
{
  "summary": {
    "totalCost": 12.34,
    "totalRequests": 5678,
    "totalTokens": 12345678,
    "successRate": 0.987,
    "avgLatencyMs": 1234
  },
  "models": [
    { "model": "gpt-5", "cost": 8.5, "requests": 1234, "tokens": 4567890 },
    {
      "model": "claude-opus-4-6",
      "cost": 3.84,
      "requests": 234,
      "tokens": 234567
    }
  ],
  "daily": [
    { "date": "2026-06-01", "cost": 1.5, "requests": 800 },
    { "date": "2026-06-02", "cost": 2.0, "requests": 1000 }
  ]
}
```

### Kueri Analitik Penggunaan

Data penggunaan diakses melalui dasbor atau alat MCP, bukan melalui endpoint ekspor REST langsung. Analitik yang tersedia:

- **`/api/usage/analytics`** — metrik penggunaan teragregasi (dikelompokkan berdasarkan model, penyedia, kunci)
- **`/api/usage/quota`** — status kuota saat ini per kunci API
- **`/api/usage/history`** — log riwayat permintaan

---

## Alat MCP

Dua alat MCP mengekspos data penggunaan kepada agen (lihat `open-sse/mcp-server/tools/`):

| Alat                    | Deskripsi                                                   |
| ----------------------- | ----------------------------------------------------------- |
| `omniroute_cost_report` | Menghasilkan laporan biaya per kunci untuk periode tertentu |
| `omniroute_check_quota` | Mengembalikan status kuota saat ini untuk sebuah kunci API  |

Contoh pemanggilan oleh agen:

```json
{
  "tool": "omniroute_cost_report",
  "args": { "period": "week" }
}
```

---

## Retensi dan Pembersihan

Data penggunaan bertambah sekitar ~1-10KB per permintaan. Dalam skala besar, jumlah ini dapat menjadi signifikan.

### Pengaturan Retensi

Retensi riwayat penggunaan dikonfigurasi melalui Pengaturan Database di UI atau melalui `/api/settings/database`.

Secara default, riwayat penggunaan dipertahankan selama **90 hari**.

### Pembersihan

Catatan lama dibersihkan oleh `src/lib/db/cleanup.ts`:

- Dipicu oleh proses cron latar belakang
- Menghapus catatan dari `usage_history` yang lebih lama dari pengaturan retensi `usageHistory` yang dikonfigurasi

### Estimasi Penyimpanan

| Tingkat permintaan      | Penyimpanan 30 hari | Penyimpanan 90 hari |
| ----------------------- | ------------------- | ------------------- |
| 100 permintaan/hari     | ~3MB                | ~9MB                |
| 1.000 permintaan/hari   | ~30MB               | ~90MB               |
| 10.000 permintaan/hari  | ~300MB              | ~900MB              |
| 100.000 permintaan/hari | ~3GB                | ~9GB                |

Untuk lalu lintas yang sangat tinggi, pertimbangkan:

- Mengurangi periode retensi melalui Pengaturan Database
- Menggunakan `aggregated_metrics` alih-alih catatan mentah (hanya untuk analitik)

---

## Tips Pengoptimalan Biaya

### 1. Gunakan Model yang Tepat

```bash
# Jawaban cepat — gunakan yang murah + cepat
curl -d '{"model":"auto/fast","messages":[...]}'

# Tugas kompleks — gunakan yang berkualitas
curl -d '{"model":"auto/smart","messages":[...]}'
```

### 2. Aktifkan Caching

Caching prompt Anthropic menghemat **90% pada konteks yang berulang**:

```ts
// Caching berlangsung otomatis — cukup sertakan prompt sistem besar yang sama
const response = await openai.chat({
  model: "claude-sonnet-4-5",
  system: longSystemPrompt, // Akan di-cache secara otomatis
  messages: [{ role: "user", content: "..." }],
});
```

### 3. Gunakan Kompresi

Kompresi RTK + Caveman menghemat **15-95% pada sesi yang banyak menggunakan alat**:

```ts
const config = {
  compression: {
    engine: "rtk",
    intensity: "aggressive",
  },
};
```

### 4. Tetapkan Kuota Per Kunci

Selalu tetapkan `quotaLimit` untuk mencegah biaya tak terkendali:

```ts
await updateApiKey(keyId, { quotaLimit: 10_00 }); // Batas $10/bulan
```

### 5. Audit Konsumen Teratas

Gunakan dasbor atau **`/api/usage/analytics`** untuk mengelompokkan berdasarkan kunci API dan mengurutkan berdasarkan biaya:

```bash
GET /api/usage/analytics?groupBy=apiKey
```

---

## Pemecahan Masalah

### "Biaya lebih tinggi dari yang diperkirakan"

1. Periksa **`/api/usage/analytics?groupBy=model`** — temukan model yang mahal
2. Periksa **`/api/usage/analytics?groupBy=apiKey`** — temukan konsumen terbesar
3. Pastikan data harga sudah diperbarui: `POST /api/pricing/sync`

### "Catatan hilang"

- Periksa pengaturan retensi DB di Dasbor → Database → Pembersihan — catatan lama dihapus oleh tugas pembersihan berkala (`src/lib/db/cleanup.ts`)
- Periksa kesalahan di `src/lib/db/usage*.ts` — kegagalan penulisan DB dicatat dalam log, tetapi tidak ditampilkan
- Pastikan permintaan benar-benar mencapai `chatCore` — periksa perutean kombo

### "Kuota tidak diterapkan"

- Periksa pengaturan `quotaLimit` milik kunci
- Pastikan `quotaWindow` ditetapkan dengan benar
- Cari catatan `quotaSnapshots` — catatan tersebut seharusnya dibuat pada setiap permintaan

---

## Lihat Juga

- [DATABASE_GUIDE.md](../ops/DATABASE_GUIDE.md) — Skema untuk tabel penggunaan
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md#18-pricing-sync) — variabel lingkungan sinkronisasi harga
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — Cara `auto/fast`, `auto/cheap` mengurangi biaya
- [API_REFERENCE.md](../reference/API_REFERENCE.md) — Referensi lengkap `/api/usage/*`
- Sumber: `open-sse/services/usage.ts`, `src/lib/usageAnalytics.ts`, `src/lib/db/usage*.ts`
