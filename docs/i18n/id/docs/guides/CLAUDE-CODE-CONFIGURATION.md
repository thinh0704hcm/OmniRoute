# Claude Code CLI — Configuration with OmniRoute (Bahasa Indonesia)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

Arahkan CLI **Claude Code** (`claude`) ke OmniRoute — lokal atau VPS jarak jauh —
dengan profil per model, serupa dengan penyiapan Codex.

---

## Mulai cepat

```bash
# Jalankan Claude Code dengan OmniRoute lokal (mendeteksi konteks aktif secara otomatis)
omniroute launch

# Dengan OmniRoute jarak jauh (setelah `omniroute connect <host>`, ini berlangsung otomatis)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Buat profil per model, lalu jalankan salah satunya
omniroute setup-claude            # menulis ~/.claude/profiles/<name>/settings.json
omniroute launch --profile glm52  # Claude Code menggunakan glm/glm-5.2 melalui OmniRoute
```

---

## Cara Claude Code terhubung ke gateway

Claude Code berkomunikasi dengan **Anthropic Messages API** dan diarahkan ke endpoint
khusus melalui variabel lingkungan (tidak memiliki flag `--base-url`):

| Variabel                                     | Tujuan                                                                                          |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `ANTHROPIC_BASE_URL`                         | URL root gateway (Claude Code menambahkan `/v1/messages`). **Tanpa akhiran `/v1`.**             |
| `ANTHROPIC_AUTH_TOKEN`                       | Dikirim sebagai `Authorization: Bearer …` — gunakan token akses / kunci API OmniRoute Anda      |
| `ANTHROPIC_API_KEY`                          | Alternatif: dikirim sebagai `x-api-key`. Jika keduanya diatur, `ANTHROPIC_AUTH_TOKEN` digunakan |
| `ANTHROPIC_MODEL`                            | Memaksa model tertentu (menggantikan pilihan default `/model`)                                  |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | `1` → pemilih bawaan `/model` mencantumkan model `claude*`/`anthropic*` dari `/v1/models`       |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | Membatasi token keluaran per respons (misalnya `65536`)                                         |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | Ambang batas token untuk pemadatan otomatis                                                     |

> Variabel lingkungan dibaca **sekali saat dimulai** — mulai ulang Claude Code setelah mengubahnya.

`omniroute launch` mengatur semuanya untuk Anda: perintah ini menentukan URL dasar + token
dari konteks aktif (sehingga `omniroute connect <vps>` lalu `omniroute launch`
langsung berfungsi), memeriksa kesehatan server, dan menjalankan `claude` melalui exec.

---

## Alias penemuan — tampilkan model non-Claude di pemilih `/model`

Penemuan model gateway Claude Code hanya mencantumkan id yang diawali dengan `claude`
atau `anthropic`, sehingga dengan `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`, pemilih
bawaan `/model` biasanya **hanya** menampilkan model Claude/Anthropic milik OmniRoute —
`kimi/kimi-k2.6` atau `glm/glm-5.2` tidak pernah muncul, meskipun peruteannya berfungsi dengan baik.

OmniRoute dapat mencerminkan model apa pun yang diaktifkan (dan kombinasi) di bawah id `claude/…`
agar lolos dari filter tersebut dan muncul di pemilih:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<combo "custo-otimizado"> →  claude/combo/custo-otimizado
```

Saat Anda memilih salah satunya di Claude Code, OmniRoute menghapus pembungkus `claude/`
dan mengembalikannya ke id asli sebelum melakukan perutean — id `claude/<real-claude-model>`
asli (penyedia OAuth Claude yang sebenarnya) selalu dibiarkan tanpa perubahan.

**Fitur ini dinonaktifkan secara default** dan dikendalikan oleh pengaturan tiga tingkat (yang paling spesifik
diprioritaskan), sehingga OmniRoute biasa tidak pernah menggandakan katalognya untuk klien yang tidak menggunakan
Claude Code:

| Tingkat  | Lokasi                                                                        |
| -------- | ----------------------------------------------------------------------------- |
| Model    | Halaman detail penyedia → pengalih "Expose in Claude Code" per model          |
| Penyedia | Halaman detail penyedia → pengalih tingkat penyedia (mencakup semua modelnya) |
| Global   | Settings → Feature Flags → `EXPOSE_CC_DISCOVERY_ALIASES` (default nonaktif)   |

Variabel lingkungan `EXPOSE_CC_DISCOVERY_ALIASES` memaksa tingkat global agar aktif
dan lebih diprioritaskan daripada penggantian melalui dasbor (layar Feature Flags menampilkan
catatan "active via environment variable" saat ini terjadi). Pengalih per penyedia dan
per model menyempurnakan pengaturan tersebut — misalnya, global nonaktif + penyedia Kimi aktif akan menampilkan
hanya model Kimi.

> ⚠️ **Ketidakcocokan jendela pada model non-Claude.** Claude Code mengasumsikan jendela konteks
> 200K untuk setiap id yang tidak dikenali (Claude Code tidak dapat membaca jendela sebenarnya dari
> `/v1/models`). Untuk model dengan jendela yang lebih besar (misalnya 256K milik Kimi K2), atur
> `CLAUDE_CODE_AUTO_COMPACT_WINDOW` ke nilai di bawah jendela sebenarnya milik model agar
> pemadatan otomatis tidak dijalankan terlalu dini. Profil yang dibuat di atas telah
> melakukan pengaturan ini untuk setiap model.

---

## Blok onboarding di dasbor

Kartu alat Claude (**Dasbor → CLI Code**) menampilkan fragmen `settings.json` yang tepat
untuk instans ini, di sebelah tombol info alias penemuan, lengkap dengan tombol salin:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<OmniRoute Anda>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<kunci API OmniRoute Anda>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

URL dasar adalah URL yang dihasilkan oleh kartu tersebut (termasuk penggantian khusus yang Anda masukkan), dan sudah
dinormalisasi — tanpa sufiks `/v1` dan tanpa garis miring penutup. **Kunci tidak pernah ditampilkan**: blok tersebut menyediakan
placeholder, sehingga tangkapan layar atau cuplikan yang ditempel tidak dapat membocorkannya. Tempelkan kunci Anda untuk menggantinya.

Tambahkan `CLAUDE_CODE_AUTO_COMPACT_WINDOW` di bawah blok `env` yang sama untuk setiap model yang jendela
konteks sebenarnya bukan 200K — Claude Code mengasumsikan 200K untuk setiap id yang tidak dikenali, sehingga
pemadatan otomatis akan dijalankan pada titik yang salah (lihat peringatan di bagian sebelumnya).
Penyusun cuplikan juga menerima nilai tersebut, sehingga pemanggil yang mengetahui ukuran jendela model target
dapat menghasilkannya secara langsung.

Sumber: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (penyusun
murni, diuji dengan pengujian unit) yang ditampilkan oleh `ClaudeGatewayOnboardingBlock`.

---

## Profil (`CLAUDE_CONFIG_DIR`)

Claude Code **tidak memiliki berkas profil bawaan** (tidak seperti `~/.codex/<name>.config.toml` milik Codex).
Mekanisme idiomatisnya adalah `CLAUDE_CONFIG_DIR` — direktori konfigurasi terpisah untuk setiap
profil, masing-masing memiliki `settings.json`, kredensial, riwayat, dan cache sendiri.

`omniroute setup-claude` mengambil katalog `/v1/models` aktif dan menulis satu
profil per model ke `~/.claude/profiles/<name>/settings.json`, dengan menggunakan kembali
**nama yang sama seperti `setup-codex`** (`glm52`, `kimi-k27`, `deepseek-pro`, …):

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **Token autentikasi tidak pernah ditulis ke profil.** Jalankan dengan
> `omniroute launch --profile <name>` (perintah ini menyuntikkan `ANTHROPIC_AUTH_TOKEN` dari
> konteks aktif), atau ekspor sendiri `ANTHROPIC_AUTH_TOKEN` dan jalankan
> `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude`.

**Sinkronisasi otomatis setelah penemuan model (opsional).** OmniRoute dapat membuat ulang berkas
`~/.claude/profiles/<name>/settings.json` yang sama secara otomatis setiap kali sinkronisasi model penyedia
mengubah katalog aktif — sehingga model baru atau yang berganti nama memperoleh profil tanpa perlu menjalankan ulang
perintah tersebut. Fitur ini **dinonaktifkan secara default**: aktifkan melalui **dasbor CLI Code** ("Sinkronisasi otomatis profil
CLI" → Claude Code), atau atur `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` (fitur ini juga mematuhi
`CLI_ALLOW_CONFIG_WRITES`, yang aktif secara default). Saat diaktifkan, fitur ini hanya menulis berkas profil; tidak pernah
mengubah konfigurasi Claude aktif/default, autentikasi, atau `~/.claude/settings.json` Anda.

### Membuat + menggunakan profil

```bash
# OmniRoute lokal
omniroute setup-claude

# VPS jarak jauh (menyematkan URL VPS ke setiap profil)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Hanya beberapa penyedia
omniroute setup-claude --only glm,kimi

# Pratinjau tanpa menulis
omniroute setup-claude --dry-run

# Jalankan profil
omniroute launch --profile kimi-k27
```

---

## Tingkat model (opsional)

Claude Code merutekan permintaan ke tingkat kapabilitas. Petakan setiap tingkat ke model OmniRoute melalui env /
pengaturan jika Anda ingin menggunakan penyedia yang berbeda untuk setiap tingkat:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

Jika tidak, satu `ANTHROPIC_MODEL` (yang ditetapkan oleh profil) akan digunakan untuk semuanya.

---

## Mode jarak jauh

Setelah Anda menjalankan `omniroute connect <host>` (lihat
[Mode Jarak Jauh](./REMOTE-MODE.md)), `omniroute launch` dan `omniroute setup-claude`
secara otomatis menargetkan server jarak jauh tersebut dan menggunakan token akses cakupannya — tidak
diperlukan flag tambahan. Timpa untuk setiap pemanggilan dengan `--remote` / `--api-key`.

---

## Pemecahan masalah

**Claude Code mengabaikan gateway** — pastikan `ANTHROPIC_BASE_URL` **tidak
mengandung `/v1`** dan mulai ulang `claude` (env hanya dibaca sekali saat proses dimulai). `omniroute launch`
menangani hal ini untuk Anda.

**Pemilih `/model` kosong / model gateway tidak tersedia** — memerlukan Claude Code
v2.1.219+ dan `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`. Hanya ID model `claude*` /
`anthropic*` yang muncul di pemilih; paksa model lain dengan
`ANTHROPIC_MODEL=<id>` (inilah yang dilakukan oleh profil).

**`400 Ambiguous model 'claude-…'`** — Claude Code selalu mengirimkan ID model **tanpa prefiks**
(misalnya `claude-opus-4-8`), sehingga ketika penyedia Claude Code (`cc/…`) dan
Claude (`claude/…`) keduanya terhubung, ID tanpa prefiks cocok dengan dua rute dan
OmniRoute menolak untuk menebak. Perbaiki dengan salah satu cara berikut: tetapkan ID berprefiks dengan
`ANTHROPIC_MODEL=cc/claude-opus-4-8`, atau aktifkan **Prioritaskan Claude Code untuk
model Claude tanpa prefiks** — tombol pengalih di halaman penyedia Claude, atau
`OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` (secara default nonaktif;
lihat [Lingkungan](../reference/ENVIRONMENT.md)) — yang merutekan ID `claude-*`
tanpa prefiks ke Claude Code. Prefiks penyedia eksplisit selalu diprioritaskan.

**Kesalahan autentikasi** — profil tidak menyimpan token. Gunakan `omniroute launch --profile`
(menyuntikkannya) atau ekspor `ANTHROPIC_AUTH_TOKEN`.

**Profil tidak terisolasi** — setiap profil merupakan `CLAUDE_CONFIG_DIR` yang berbeda;
pastikan `echo $CLAUDE_CONFIG_DIR` di dalam sesi mengarah ke
`~/.claude/profiles/<name>`.
