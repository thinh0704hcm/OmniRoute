# Stealth Guide (Bahasa Indonesia)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/security/STEALTH_GUIDE.md) · 🇩🇰 [da](../../../da/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇮🇹 [it](../../../it/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Sumber acuan utama:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Terakhir diperbarui:** 2026-09-02 — v3.8.51
> **Audiens:** Engineer yang memelihara integrasi tersamar khusus penyedia.

OmniRoute terintegrasi dengan penyedia yang sistem edge-nya secara aktif mengidentifikasi sidik jari klien tidak resmi (TLS JA3/JA4, urutan header, struktur body JSON, token integritas). Halaman ini mendokumentasikan permukaan penyamaran yang disediakan OmniRoute dan lokasi implementasinya.

## Pemberitahuan Hukum dan Etika

Fitur penyamaran tersedia agar OmniRoute dapat bertindak sebagai lapisan kompatibilitas antara akun resmi milik pengguna (Claude Code CLI, Codex, Antigravity, Cursor, dll.) dan API terpadu OmniRoute. Fitur ini **bukan** untuk menghindari deteksi penipuan, membagikan kredensial, atau melanggar Ketentuan Layanan penyedia. Pengelola mengharapkan operator mematuhi ToS upstream yang mereka setujui saat membuat akun.

---

## Lapisan Sidik Jari TLS

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Sesi `wreq-js` persisten dibuat secara lazy untuk setiap cakupan akun dan proksi yang telah di-resolve. `TlsClient` pada cakupan proses menampung paling banyak 128 sesi yang menyamar sebagai **Chrome 124 di macOS** untuk upstream di belakang Cloudflare. `TlsClient.fetch()` menerapkan mekanisme fail-closed ketika runtime native tidak tersedia; pemanggil dapat secara eksplisit memilih fallback di luar wrapper ini.

- Profil sesi: `browser: "chrome_124", os: "macos"`
- Resolusi proksi (prioritas): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (juga huruf kecil)
- Batas waktu: `TLS_CLIENT_TIMEOUT_MS` (diwarisi dari `FETCH_TIMEOUT_MS`, default 600000)
- Response `wreq-js` kompatibel dengan fetch (`headers`, `text()`, `json()`, `clone()`, `body`).
- Watchdog byte pertama (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()`
  diselesaikan segera setelah header upstream tiba, sehingga `TLS_CLIENT_TIMEOUT_MS` saja tidak dapat membatasi
  body yang tidak pernah menghasilkan byte pertama. `guardTlsFirstByte()` mengadu `read()` pertama pada body
  dengan `TLS_FIRST_BYTE_WATCHDOG_MS` (default `10000`, `0` menonaktifkannya); body yang sehat tidak
  terpengaruh, sedangkan body yang macet membatalkan reader wreq dan memungkinkan logika
  fallback TLS milik `proxyFetch` yang sudah ada beralih ke dispatcher langsung/proksi (permintaan yang tidak aman
  untuk diputar ulang, misalnya POST dengan body, tetap menghasilkan error alih-alih dicoba kembali secara diam-diam).

### Transportasi penyedia berbasis cookie web — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts` adalah adaptor bersama untuk lima transportasi
cookie web khusus di bawah ini. Setiap wrapper penyedia yang tipis memilih profil browser/OS. Adaptor tersebut
menggunakan loader runtime wreq tunggal dan pool transportasi di `open-sse/utils/tlsClient.ts`, dengan kunci berdasarkan
profil + OS + proksi yang telah di-resolve, sementara setiap permintaan menggunakan `cookieMode: "ephemeral"`. Dengan demikian, akun dan
permintaan berbagi koneksi pada tingkat transportasi, tetapi tidak pernah berbagi sesi wreq atau cookie jar.

| Penyedia   | Profil        | OS yang Diemulasi | Kebijakan EOF Stream                  |
| ---------- | ------------- | ----------------- | ------------------------------------- |
| Claude     | `chrome_146`  | Linux             | sertakan `[DONE]`                     |
| Perplexity | `firefox_148` | macOS             | sertakan `event: end_of_stream`       |
| Grok       | `chrome_146`  | Linux             | kecualikan `[DONE]`                   |
| Notion     | `chrome_146`  | Windows           | sertakan `[DONE]`                     |
| LMArena    | `chrome_146`  | Windows           | tanpa sentinel; tutup saat EOF native |

- Streaming menggunakan `ReadableStream` response native secara langsung; tidak ada file sementara atau sidecar yang
  dibuat.
- Hingga 256 byte awal diperiksa sebelum stream diekspos. Penyedia SSE melakukan buffering terhadap error non-SSE;
  Grok/LMArena memetakan challenge Cloudflare ke `403` dan interstisial HTML ke `502`.
- Batas waktu permintaan native tetap dibungkus oleh tenggat mutlak JS. Kondisi hang menginvalidasi
  dan menutup hanya transportasi profil/OS/proksi yang terdampak sebelum permintaan berikutnya membuatnya kembali.
- Prioritas resolusi proksi adalah `proxyUrl` per pemanggilan → konteks akun/dasbor dalam cakupan permintaan →
  `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (termasuk varian huruf kecil). Error resolusi menerapkan
  fail-closed alih-alih membocorkan koneksi langsung. LMArena sengaja melakukan resolusi terhadap `arena.ai`.
- `byteResponse` mengembalikan URL `data:` dengan tipe konten tanpa kerusakan UTF-8.
- Error yang digunakan adalah `TlsClientUnavailableError` (paket/addon tidak tersedia), `TlsClientHangError`
  (tenggat terlampaui), dan `WreqTransportCapacityError` (kode error kapasitas sesi bersama)
  ketika seluruh 128 slot profil/OS/proksi terbatas sedang aktif atau dalam proses ditutup.

Sesi `TlsClient` generik di atas tetap dikhususkan untuk status cookie persisten yang didukung browser.
Kedua jalur menggunakan kembali satu loader modul wreq yang di-cache dan hook siklus hidup proses; pool keduanya tetap
terpisah karena masa berlaku cookie-nya memang sengaja dibuat berbeda.

Profil-profil tersebut didukung oleh paket yang versinya dikunci, tetapi penerimaan WAF aktual dapat berubah secara independen
dari pengujian kontrak lokal. Validasi perubahan sidik jari terhadap akun live yang diotorisasi secara eksplisit
sebelum mengeklaim kesetaraan dengan browser upstream.

---

## Paket Tersamar Claude Code

Saat `cliCompatMode` aktif, OmniRoute membentuk ulang permintaan Claude keluar agar tidak dapat dibedakan dari lalu lintas `claude-cli`. Tiga modul bekerja sama:

### `claudeCodeFingerprint.ts`

Menghitung sidik jari `cc_version` sepanjang 3 karakter yang disematkan dalam header penagihan:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (ditulis langsung; cocok dengan klien resmi)
- Input: karakter pada indeks 4, 7, dan 20 dari teks pesan pengguna pertama + string versi
- Output: prefiks heksadesimal sepanjang 3 karakter

### `claudeCodeCCH.ts` (Hash Konten Klien)

Pemeriksaan integritas sisi server yang dihitung oleh Claude Code CLI resmi melalui Bun/Zig. OmniRoute mengimplementasikannya ulang dengan `xxhash-wasm`:

1. Serialisasikan isi dengan placeholder `cch=00000;`
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. Heksadesimal huruf kecil sepanjang 5 karakter yang diawali nol
4. Ganti `cch=00000;` dengan token yang dihitung

Konstanta:

- Seed: `0x6e52736ac806831e`
- Pola: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Menyisipkan **zero-width joiner** Unicode (`U+200D`) setelah karakter pertama dari nama klien yang "sensitif" agar filter upstream tidak dapat menemukannya dengan grep. Daftar kata bawaan:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Diterapkan pada: blok `system`, semua `messages[].content`, dan `tools[].description` / `tools[].function.description`. Dapat diganti oleh operator melalui `setSensitiveWords()`.

### `claudeCodeCompatible.ts` — penyedia `anthropic-compatible-cc-*`

Untuk relay Anthropic pihak ketiga yang hanya menerima lalu lintas "Claude Code asli":

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"` secara bawaan
- Tombol per koneksi "Enable redact-thinking beta" menambahkan `redact-thinking-2026-02-12` saat upstream CC Compatible secara khusus memerlukan aliran proses berpikir yang disunting
- Tombol per koneksi "Enable summarized thinking display" menyimpan `providerSpecificData.requestDefaults.summarizeThinking` dan menambahkan `display: "summarized"` ke permintaan proses berpikir CC Compatible yang belum menetapkan mode tampilan
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (keluarga Opus/Sonnet 4.x)
- Jalur bawaan: `/v1/messages?beta=true`

Modul terkait dalam paket yang sama:

- `claudeCodeConstraints.ts` — aturan temperature + cache-control
- `claudeCodeToolRemapper.ts` — pemetaan ulang nama alat
- `claudeCodeExtraRemap.ts` — normalisasi payload tambahan

---

## Penyamaran Antigravity

Permintaan Antigravity mempertahankan teks pemanggil byte demi byte. OmniRoute tidak menyisipkan karakter zero-width ke dalam prompt atau mengganti nama/menyuntikkan alat untuk meniru klien IDE.

### `antigravityHeaderScrub.ts`

Menghapus penanda SDK Stainless (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`) sebelum meneruskannya.

### ⚠️ Risiko: `ANTIGRAVITY_CREDITS=always` (titik rawan pemblokiran akun)

`ANTIGRAVITY_CREDITS=always` (digunakan oleh `open-sse/executors/antigravity.ts`) mengarahkan **setiap** permintaan melalui Antigravity AI Credit Overages (kredit Google berbayar), alih-alih membiarkan kuota tingkat gratis Google membatasi penggunaan. Ini didokumentasikan sebagai fitur, tetapi merupakan **laporan pelanggaran ToS paling umum yang kami terima** — beberapa akun Google Ultra telah diblokir dengan `403 / "service disabled for ToS violation" / insufficient_quota` setelah berjalan selama beberapa jam dengan `=always`.

Penegakan upstream dilakukan di **pihak Google**, bukan sesuatu yang dapat dicegah oleh OmniRoute. Nama variabel lingkungan dan dokumentasi yang ada membuatnya terdengar seperti opsi yang aman untuk diaktifkan; kenyataannya tidak demikian.

**Mengapa hal ini memicu deteksi penyalahgunaan lebih agresif daripada penggunaan tingkat gratis saja:**

- Pengeluaran otomatis yang berkelanjutan pada satu akun Google ditandai secara berbeda dibandingkan penggunaan tingkat gratis yang mencapai kuota lalu berhenti.
- Kelebihan penggunaan kredit tidak memiliki batas laju, sehingga klien yang salah konfigurasi dapat menghabiskan beberapa ratus USD dalam hitungan menit dan terlihat seperti penjualan kembali API key atau lalu lintas bot.
- Beberapa pengguna OmniRoute yang menggunakan kredit kelebihan secara paralel dari IP eksternal yang sama memperkuat sinyal tersebut.

**Sikap yang direkomendasikan:**

1. Pertahankan nilai bawaan `ANTIGRAVITY_CREDITS=off` kecuali operator secara eksplisit menerima risiko kredit berbayar dan penindakan akun. `retry` mengirim permintaan normal terlebih dahulu dan menyuntikkan kredit paling banyak satu kali setelah respons 429 kuota yang memenuhi syarat; `always` menyuntikkan kredit pada permintaan pertama.
2. **Sebarkan beban ke beberapa penyedia melalui Auto-Combo** (`model: "auto"` atau combo `kr/glm/etc`) alih-alih memenuhi kapasitas satu akun Antigravity.
3. **Tetapkan batas RPM per koneksi** di halaman edit penyedia Antigravity (Dashboard → Providers → Antigravity → connection → rate limit). 30–60 RPM merupakan batas atas yang dapat dipertanggungjawabkan untuk penggunaan berkelanjutan.
4. **Gunakan jaringan upstream yang stabil dan dikendalikan operator** serta hindari berbagi satu akun dengan pengguna atau beban kerja yang tidak terkait.
5. **Jika diblokir**: ajukan banding melalui `support.google.com` → "Restore Workspace/Account access" dengan isi respons persis `quota_exceeded` / `service disabled` yang dikirim Google. Pemulihan tidak dijamin.

Referensi lingkungan mendokumentasikan dampak terhadap akun dan pengeluaran dari setiap mode kredit.

Titik terkait:

- `open-sse/executors/antigravity.ts` — membaca `process.env.ANTIGRAVITY_CREDITS`
- `src/lib/oauth/providers/antigravity.ts` — penyaluran kredensial
- Laporan insiden asli: Diskusi [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## Registri Fingerprint CLI — `open-sse/config/cliFingerprints.ts`

Tabel per penyedia yang menetapkan urutan header dan urutan field body JSON secara **persis**, sebagaimana ditangkap dari trace mitmproxy CLI resmi. Saat ini terdaftar: `codex`, `claude`, serta profil yang diturunkan saat runtime dalam `providerHeaderProfiles.ts` untuk `antigravity` dan `github`.

```ts
interface CliFingerprint {
  headerOrder: string[]; // peka huruf besar-kecil
  bodyFieldOrder: string[]; // key JSON tingkat teratas
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Aktifkan atau nonaktifkan per penyedia melalui env (lihat di bawah). Saat dinonaktifkan, header/key body muncul dalam urutan apa pun yang diberikan oleh Node/JSON — mudah diidentifikasi melalui fingerprint.

---

## Proksi MITM (Antigravity, Linux/macOS/Windows)

Untuk CLI yang binernya tidak dapat dialihkan melalui `OPENAI_BASE_URL`, OmniRoute menjalankan proksi lokal yang mengakhiri TLS. Endpoint berada di bawah `src/app/api/cli-tools/antigravity-mitm/`.

| Metode | Endpoint                                | Tujuan                                            |
| ------ | --------------------------------------- | ------------------------------------------------- |
| GET    | `/api/cli-tools/antigravity-mitm`       | Status — berjalan, pid, dnsConfigured, certExists |
| POST   | `/api/cli-tools/antigravity-mitm`       | Mulai MITM (memerlukan `apiKey` + `sudoPassword`) |
| DELETE | `/api/cli-tools/antigravity-mitm`       | Hentikan MITM                                     |
| GET    | `/api/cli-tools/antigravity-mitm/alias` | Cantumkan alias model                             |
| PUT    | `/api/cli-tools/antigravity-mitm/alias` | Simpan alias model untuk suatu alat               |

Host target yang dicegat: **`daily-cloudcode-pa.googleapis.com`** (upstream Antigravity).

### Urutan proses mulai (`src/mitm/manager.ts::startMitm`)

1. Buat sertifikat yang ditandatangani sendiri melalui `selfsigned` (RSA-2048, SHA-256, 1 tahun) — `cert/generate.ts`
2. Instal sertifikat ke trust store sistem — `cert/install.ts`
3. Tambahkan entri hosts `127.0.0.1 daily-cloudcode-pa.googleapis.com` — `dns/dnsConfig.ts`
4. Jalankan `src/mitm/server.cjs` dengan `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (default `443`)
5. Simpan PID ke `<DATA_DIR>/mitm/.mitm.pid`

### Deteksi trust store dinamis Linux — `cert/install.ts`

`getLinuxCertConfig()` menelusuri daftar prioritas dan memilih direktori pertama yang tersedia:

| Keluarga distro          | Direktori                                   | Perintah pembaruan       |
| ------------------------ | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu          | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS   | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                 | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Nama file sertifikat: `omniroute-mitm.crt`. Pencocokan fingerprint melalui `getCertFingerprint()` (SHA-1 dari DER).

Selain itu, `updateNssDatabases()` menginstal ke DB NSS per pengguna saat `certutil` tersedia: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb`, semua profil Firefox (termasuk snap), dengan nama panggilan **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** PowerShell dengan hak akses tinggi → `certutil -addstore Root`

### Autentikasi

Semua endpoint MITM memerlukan autentikasi manajemen (`requireCliToolsAuth`). Kata sandi sudo disimpan dalam cache pada cakupan modul (tidak pernah di `globalThis`) dan dihapus saat `stopMitm()`.

## Penggantian User-Agent — env vars (`.env.example` bagian 12)

| Variabel                 | Default                                                         |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.149.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

Digunakan oleh `open-sse/executors/base.ts::buildHeaders()` melalui pencarian dinamis. **Naikkan versi ini saat penyedia merilis versi CLI baru** — string UA yang usang mulai ditolak karena dianggap sebagai klien kedaluwarsa.

## Sakelar Mode Kompatibilitas CLI (`.env.example` bagian 13)

| Variabel                   | Efek                           |
| -------------------------- | ------------------------------ |
| `CLI_COMPAT_CODEX=1`       | Sidik jari Codex               |
| `CLI_COMPAT_CLAUDE=1`      | Sidik jari claude-cli          |
| `CLI_COMPAT_GITHUB=1`      | Sidik jari GitHub Copilot Chat |
| `CLI_COMPAT_ANTIGRAVITY=1` | Sidik jari Antigravity         |
| `CLI_COMPAT_KIRO=1`        | Kiro                           |
| `CLI_COMPAT_CURSOR=1`      | Cursor                         |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                    |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                       |
| `CLI_COMPAT_CLINE=1`       | Cline                          |
| `CLI_COMPAT_ALL=1`         | Aktifkan semua opsi di atas    |

IP penyedia **selalu dipertahankan** — sakelar ini hanya membentuk ulang citra wire permintaan, tidak mengubah egress IP.

---

## Sanitasi Header Masuk

OmniRoute membersihkan header klien yang masuk sebelum meneruskannya, sehingga permintaan yang berasal dari Cursor tidak membocorkan `User-Agent: Cursor/X.Y.Z` ke upstream Claude. Lihat `src/shared/constants/upstreamHeaders.ts` untuk daftar penolakan yang dijaga tetap selaras dengan skema Zod dan pengujian unit.

---

## Memperbarui Sidik Jari Saat Penyedia Melakukan Rotasi

1. Tangkap lalu lintas CLI resmi dengan `mitmproxy` (intersepsi TLS + dump)
2. Ekstrak JA3/JA4 dan urutan header literal
3. Perbarui entri `CLI_FINGERPRINTS[...]` yang relevan
4. Naikkan default `*_USER_AGENT` yang sesuai di `.env.example`
5. Jika handshake TLS itu sendiri berubah, perbarui wrapper penyedia yang relevan atau opsi `browser:` wreq-js
6. Jalankan pengujian TLS khusus penyedia dan canary manual terhadap penyedia live
7. Rilis sebagai patch; dokumentasikan di `CHANGELOG.md`

---

## Pengujian

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — perilaku wrapper TLS bersama
- `tests/unit/anthropic-cache-fingerprint.test.ts` — determinisme sidik jari
- `tests/unit/chatgpt-web-source-retirement.test.ts` — sumber stealth ChatGPT Web umum tetap tidak tersedia, sementara Codex Web tetap tersedia

---

## Lihat Juga

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — apa yang terjadi ketika jalur tersembunyi menerima `403`
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — referensi lengkap variabel lingkungan
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — tampilan operator untuk alur kerja MITM
