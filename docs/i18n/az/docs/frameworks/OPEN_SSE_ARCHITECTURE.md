# open-sse Architecture (Azərbaycan dili)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Qısa xülasə**: `open-sse/` OmniRoute daxilində hər bir LLM sorğusunu işlədən əsas axın mühərrikidir. Burada sorğu konveyerini, icraçıları, xidmətləri, MCP serverini və tərcümə qatını həyata keçirən təxminən 1,400 fayl var. Bu təlimat hissələrin bir-biri ilə necə əlaqəli olduğunu izah edir.

**Mənbə:** `open-sse/` (iş sahəsi paketi, test olmayan təxminən 1,440 `.ts` faylı)

---

## Niyə ayrıca iş sahəsi paketi?

`open-sse/` bir neçə səbəbə görə OmniRoute monoreposunda **müstəqil iş sahəsidir**:

1. **Təkrar istifadə imkanı** — `open-sse`, npm-də `@omniroute/open-sse` kimi dərc edilir, buna görə digər layihələr ondan müstəqil şəkildə istifadə edə bilər
2. **Aydın sərhədlər** — axın mühərriki OmniRoute-a xas UI/DB qatından ayrılıb
3. **Performans** — mühərrikin Next.js asılılıqları yoxdur, bu da CLI/serverless kontekstlərində daha sürətli soyuq başlanğıca imkan verir
4. **Versiyalaşdırma** — `open-sse` öz buraxılış qrafikinə uyğun olaraq buraxıla bilər

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Yuxarı səviyyəli struktur

```
open-sse/
├── index.ts              # İctimai giriş nöqtəsi
├── types.d.ts            # İctimai tip ixracları
├── package.json          # @omniroute/open-sse
├── config/               # Provayder konfiqurasiyaları, sabitlər, reyestrlər
├── executors/            # Hər provayder üçün HTTP icraçıları (67 + base.ts/index.ts)
├── handlers/             # Sorğu emalçıları (chatCore, responses və s.)
├── lib/                  # Daxili köməkçi vasitələr
├── mcp-server/           # Model Context Protocol serveri
├── services/             # Təxminən 298 xidmət modulu
├── transformer/          # Responses API format transformatoru
├── translator/           # Format tərcüməsi (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Ortaq köməkçi vasitələr (jurnallaşdırma, xəta, axın və s.)
```

### Modul sayları

| Kataloq | Fayllar | Məqsəd |
| `executors/` | 167 | Hər provayder üçün HTTP icraçıları (`DefaultExecutor` fabriki vasitəsilə vahidləşdirilir) |
| `handlers/` | 157 | Sorğu giriş nöqtələri (`chatCore`, `responses`, daxiletmələr) |
| `services/` | ~536 | Marşrutlaşdırma, keşləmə, sürət məhdudlaşdırması, yeniləmə və s. |
| `translator/` | 56 | Format çevrilməsi (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP alətləri və ötürmə mexanizmləri |
| `utils/` | ~108 | Ümumi köməkçi vasitələr (jurnallaşdırma, xəta, axın) |
| `config/` | ~339 | Provayder konfiqurasiyaları, sabitlər, reyestrlər |

---

## Sorğu konveyeri

Hər LLM sorğusu **5 mərhələli konveyerdən** keçir:

```
                              ┌──────────────────┐
   HTTP sorğusu               │  1. MARŞRUTLAŞDIR│   kombinasiyanın həlli, model seçimi
   (Next.js marşrutu)          └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  2. TƏRCÜMƏ ET   │   format çevrilməsi (OpenAI ↔ Claude ↔ Gemini)
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  3. İCRA ET      │   provayder icraçısı, HTTP, təkrar cəhd, qoruyucu
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  4. AXINLA ÖTÜR  │   SSE transformasiyası, əks təzyiq
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  5. QEYDƏ AL     │   istifadə izlənməsi, çağırış jurnalı, xəta təsnifatı
                              └────────┬─────────┘
                                       │
                                       ▼
                              HTTP cavabı (SSE və ya JSON)
```

### Mərhələ 1: Marşrutlaşdırma (services/combo.ts)

**Giriş nöqtəsi**: `services/combo.ts` daxilindəki `handleComboChat()`

Sorğunu konkret `(provider, model, account, credentials)` kortejinədək həll edir:

- Kombinasiyanı ID üzrə axtarır (və ya `auto/*` modelləri üçün virtual kombinasiya yaradır)
- Marşrutlaşdırma strategiyasını tətbiq edir (prioritet, çəkili, növbəli və s.)
- Sağlam olmayan provayderləri filtrdən keçirir (dövrə qoruyucusu)
- Növbəti uyğun hədəfi seçir

`auto/*` modelləri üçün bu mərhələ həmçinin:

- **16 amilli qiymətləndirmə** alqoritmini işə salır (`services/autoCombo/`)
- Sağlamlıq, xərc, gecikmə və digər amillərə əsasən `provider+model` cütünü seçir

### Mərhələ 2: Tərcümə (translator/)

Mənbə formatı (məsələn, OpenAI) hədəf formatından (məsələn, Claude) fərqlənirsə, sorğu **tərcümə edilir**:

- Sistem promptu → sistem mesajı
- Alət tərifləri → provayderə xas alət formatı
- Mühakimə/düşünmə parametrləri → provayderə xas ekvivalentlər
- Mesaj rollarının normallaşdırılması (OpenAI olmayan sistemlər üçün `developer` → `system`)

`translator/index.ts` aşağıdakıları təqdim edir:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Mərhələ 3: İcra (executors/)

**Giriş nöqtəsi**: `getExecutor(providerId).execute(request, options)`

Bütün provayderlər `getExecutor()` fabrikinin ehtiyat variantı vasitəsilə `DefaultExecutor`-dan (`executors/default.ts`) istifadə edir. İcraçı:

- Yuxarı axın URL-ni yaradır (`buildUrl()`)
- Provayderə xas başlıqları əlavə edir (`buildHeaders()`)
- Sorğu gövdəsini çevirir (`transformRequest()`)
- HTTP sorğusunu təkrar cəhd və eksponensial gecikmə ilə göndərir
- Lazım olduqda autentifikasiya məlumatlarını yeniləyir (OAuth provayderləri)

Bütün icraçılar aşağıdakıları təmin edən `BaseExecutor`-u (`executors/base.ts`, 1170 kod sətri) genişləndirir:

- Ümumi təkrar cəhd məntiqi
- Proksi inteqrasiyası
- Dövrə qoruyucusu inteqrasiyası
- İstifadənin qeydə alınması üçün qarmaqlar

### Mərhələ 4: Axın (utils/stream.ts)

Axınla ötürülən cavablar üçün icraçı **ReadableStream** qaytarır. Emalçı:

- SSE transformasiyasından keçirir (`createSSETransformStreamWithLogger`)
- Ölü bağlantıları aşkar etmək üçün heartbeat pingləri tətbiq edir
- Müştəri bağlantısının kəsilməsini düzgün şəkildə idarə edir (`pipeWithDisconnect`)
- Axınla ötürülməyən müştərilər üçün SSE → JSON çevrilməsini həyata keçirir

Axınla ötürülməyən cavablar üçün icraçı dəyişdirilmədən ötürülən, təhlil edilmiş JSON obyekti qaytarır.

### Mərhələ 5: Qeydəalma (services/usage.ts)

Cavabdan sonra (uğurlu və ya uğursuz olmasından asılı olmayaraq) istifadə qeydə alınır:

- Cavabdan `prompt_tokens`, `completion_tokens`, `cached_tokens`
- Qiymət məlumatları əsasında hesablanan `cost_usd`
- Uğursuz olduqda `latency_ms`, `status`, `error_class`
- `usage_history` cədvəlində saxlanılır

Çağırış jurnalı artefaktları (aktivləşdirilibsə) `${DATA_DIR}/call_logs/` qovluğuna yazılır.

---

## Əsas Faylların Dərin Təhlili

### chatCore.ts (5977 sətir)

**Əsas sorğu emalçısıdır**. Böyük həcmli olmasına baxmayaraq, aydın struktura malikdir:

```ts
// chatCore.ts faylının psevdostrukturu
export async function handleChat(request: NextRequest) {
  // 1. Autentifikasiya + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Sorğu gövdəsinin yoxlanılması
  const body = await parseRequestBody(request);

  // 3. Formatın aşkarlanması + çevrilməsi
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Combo marşrutlaşdırması
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Növbəti hədəflə davam et
    }
  }

  // 5. Fövqəladə ehtiyat mexanizmi
  return await emergencyFallback(body);
}
```

Nəhəng bir funksiya olmasına baxmayaraq, 5 mərhələli konveyerə uyğun gələn **şərhlərlə ayrılmış bölmələr** şəklində təşkil edilib.

### combo.ts (4456 sətir)

Combo-nu sıralanmış hədəflərə çevirən **marşrutlaşdırma mühərrikidir**.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

**19 marşrutlaşdırma strategiyasını** dəstəkləyir (baxın: `src/shared/constants/routingStrategies.ts`):

| Strategiya          | Davranış                                                                                 |
| ------------------- | ---------------------------------------------------------------------------------------- |
| `priority`          | İlk hədəfə üstünlük verən sıralanmış siyahı                                              |
| `weighted`          | Hər hədəfin çəkisinə əsaslanan ehtimallı seçim                                           |
| `round-robin`       | Hədəflər arasında ardıcıllıqla dövr edir                                                 |
| `context-relay`     | Konteksti hədəflər arasında ötürür                                                       |
| `fill-first`        | Növbəti hədəfə keçməzdən əvvəl kvotanı doldurur                                          |
| `p2c`               | İki seçimdən daha yaxşısını seçir                                                        |
| `random`            | Bərabər paylanmış təsadüfi seçim                                                         |
| `least-used`        | Son dövrdə ən az istifadə ediləni seçir                                                  |
| `cost-optimized`    | Əvvəlcə ən ucuz və işlək hədəfi seçir                                                    |
| `reset-aware`       | Provayderin sıfırlama intervallarını nəzərə alır                                         |
| `reset-window`      | Sıfırlama intervalına əsaslanan marşrutlaşdırma                                          |
| `headroom`          | Əvvəlcə ən çox qalan kvota ehtiyatına malik olanı seçir                                  |
| `strict-random`     | Həqiqətən bərabər paylanmış seçim (keyfiyyət çəkisi olmadan)                             |
| `auto`              | 16 amilli qiymətləndirmədən istifadə edir (`autoCombo/`)                                 |
| `lkgp`              | Əvvəlcə son məlum işlək provayderi seçir                                                 |
| `context-optimized` | Uzun kontekstli sorğular üçün ən uyğun seçim                                             |
| `fusion`            | Sorğunu paralel olaraq panelə göndərir, sonra hakim vasitəsilə sintez edir (`fusion.ts`) |

### base.ts (1170 sətir)

Bütün 107 icraçının genişləndirdiyi **abstrakt icraçıdır**. Buraya aşağıdakılar daxildir:

- `buildUrl()` — standart URL qurulması (alt siniflər xüsusi hallar üçün bunu əvəz edir)
- `buildHeaders()` — standart başlıqlar (autentifikasiya, məzmun növü)
- `transformRequest()` — standart olaraq dəyişiklik etmədən ötürür
- `execute()` — təkrar cəhd, eksponensial gözləmə və dövrə kəsici mexanizmləri ilə əsas HTTP dövrü

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Bütün OpenAI/Anthropic-uyğun provayderləri idarə edir
  // Provayderlər konfiqurasiyaları (URL, autentifikasiya, başlıqlar) qeydiyyatdan keçirir, lakin icraçı məntiqini paylaşırlar
}
```

Provayderə xas davranışlar (autentifikasiya başlıqları, əsas URL, versiya başlıqları) ayrıca icraçı sinifləri vasitəsilə deyil, provayder reyestri vasitəsilə konfiqurasiya edilir.

````

---

## Xidmətlər (117 modul)

Xidmətlər handler-lərin birləşdirdiyi **fokuslanmış, tək məqsədli modullardır**. Əsas kateqoriyalar:

### Marşrutlaşdırma və Combo

- `combo.ts` — combo ilə marşrutlaşdırılan sorğular üçün giriş nöqtəsi
- `services/autoCombo/` — 16 amilli qiymətləndirmə, 8 avtomatik marşrutlaşdırma strategiyası
- `wildcardRouter.ts` — wildcard marşrutlarını (`gpt-*`) uyğunlaşdırır
- `modelFamilyFallback.ts` — T5 ailədaxili ehtiyat keçidi

### Sürət Məhdudiyyəti və Kvota

- `rateLimitManager.ts` — hər açar+provayder üçün token vedrəsi
- `usage.ts` — istifadənin qeydə alınması
- `quotaCache.ts` — yaddaşdaxili kvota anlıq görüntüləri

### Hesab və Token

- `tokenRefresh.ts` — 401 zamanı OAuth yeniləməsi
- `accountFallback.ts` — alternativ hesaba keçid
- `sessionManager.ts` — çoxaddımlı sessiya vəziyyəti

### İntellekt

- `intentClassifier.ts` — sorğunun niyyətini təsnif edir
- `taskAwareRouter.ts` — tapşırıq növünə görə marşrutlaşdırır
- `thinkingBudget.ts` — düşünmə tokenlərini ayırır
- `contextManager.ts` — marşrutlaşdırma kontekstini daxil edir

### Dayanıqlılıq

- `resilience.ts` — təkrar cəhd, gecikdirmə və kəsici orkestrasiya
- `emergencyFallback.ts` — son çarə ehtiyat keçidi
- `modelDeprecation.ts` — varis modellərə avtomatik marşrutlaşdırma

### Vəziyyət

- `signatureCache.ts` — sorğu imzasına görə dublikatların aradan qaldırılması
- `volumeDetector.ts` — yükün azaldılması
- `contextHandoff.ts` — sessiyanın seriallaşdırılması

### Sıxışdırma

- `compression/` (alt kataloq) — tam sıxışdırma konveyeri
- Mühərrikləri, qayda paketlərini və adapterləri əhatə edən 39 fayl

### Bacarıqlar

- ([SKILLS.md](./SKILLS.md) sənədində əhatə olunur)

### Yaddaş

- ([MEMORY.md](./MEMORY.md) sənədində əhatə olunur)

---

## İcraçılar (75+ fayl)

Hər provayder üçün bir fayl. Onların hamısı `BaseExecutor` sinfini genişləndirir və fərqlənən hissələri yenidən təyin edir.

### Ümumi Nümunələr

Provayderlər konfiqurasiya edilmiş icraçını qaytaran `getExecutor(providerId)` vasitəsilə müəyyən edilir. OpenAI/Anthropic ilə uyğun provayderlər `DefaultExecutor` (`executors/default.ts`) istifadə edir. Provayderə xas davranış (baza URL-i, autentifikasiya başlıqları, API versiyası) `open-sse/config/providers/` daxilində konfiqurasiya edilir, sorğu gövdəsinin çevrilmələri isə `open-sse/translator/` daxilində idarə olunur.

**Fərdi URL** provayder konfiqurasiyası vasitəsilə təyin edilir:

```ts
// open-sse/config/providers/ daxilində provayder konfiqurasiyası
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Fərdi autentifikasiya** provayder reyestrinin autentifikasiya konfiqurasiyası (API açarı, OAuth, başlıq profilləri) vasitəsilə idarə olunur.

**Fərdi sorğu gövdəsi** çevrilmələri (məsələn, Anthropic-in `system` sahəsini `messages` sahəsindən ayırması) `open-sse/translator/` daxilində hər provayder üçün ayrıca qeydiyyata alınır.

````

### İcraçı Fabriki

`executors/index.ts` faylı `getExecutor(providerId)` ixrac edir:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Müəyyənləşdirmə `ExecutorRegistry` (`executors/registry.ts`) vasitəsilə həyata keçirilir: hər bir ixtisaslaşmış icraçı `executors/index.ts` faylının daxili cədvəlində elan edilir və modul yüklənərkən `registerExecutor(alias, instance)` vasitəsilə qeydiyyata alınır; `getExecutor()` reyestrə müraciət edir və ixtisaslaşmış qeydi olmayan istənilən provayder üçün yaddaşa alınmış `DefaultExecutor` variantına keçir. Tam alias → icraçı uyğunluğu `tests/unit/executor-map-golden.test.ts` qızıl testi ilə xarakterizə olunur.

---

## Tərcüməçilər

**3 format** arasında tərcümə edir: OpenAI, Anthropic, Gemini və həmçinin yeni Responses API.

### Tərcümə nə zaman baş verir

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Ümumi tərcümələr:

- `OpenAI → Anthropic`: ayrıca `system` sahəsi, `x-api-key` başlığı
- `OpenAI → Gemini`: `messages` əvəzinə `contents`, `systemInstruction`
- `OpenAI → Responses API`: `input` massivi, `previous_response_id` vəziyyəti

### Emal edilən kənar hallar

- `developer` rolu → OpenAI-dan fərqli sistemlər üçün `system`
- `system` rolu → GLM/ERNIE üçün ilk istifadəçi mesajına birləşdirilir
- `json_schema` → Gemini-nin `responseMimeType` + `responseSchema` formatı
- `tools` → provayderə xas alət formatı
- Düşünmə parametrləri (o1, Claude) → provayderə xas ekvivalentlər

---

## MCP Serveri

`open-sse/mcp-server/` **Model Context Protocol** serverini reallaşdırır:

- **110 alət** (provayderlərin idarə edilməsi, kombinasiyalar, yaddaş, keş, sıxılma, proksi, bacarıqlar, oyunlaşdırma, plaginlər, Notion, Obsidian, lokal korpus)
- **3 nəqliyyat üsulu**: stdio, SSE, Streamable HTTP
- Dəqiq səviyyəli avtorizasiya üçün **33 icazə sahəsi**

### Alətlərin qeydiyyatı

Alətlər `open-sse/mcp-server/tools/` daxilində müstəqil fayllar kimi qeydiyyatdan keçirilir; hər biri ad, sxem, emal funksiyası və icazə sahəsini ixrac edir:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Nəqliyyat üsulları

```ts
// stdio (CLI istifadəsi)
startMcpStdio(server);

// SSE (HTTP əsaslı axın)
startMcpSse(server, port);

// Streamable HTTP (müasir MCP)
startMcpStreamable(server, port);
```

### Avtorizasiya

Hər alət çağırışı icazə sahəsi yoxlamalarından keçir (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Transformatorlar

`open-sse/transformer/` **Chat Completions** və **Responses API** formatları arasında çevirmə aparır.

### Niyə ayrıca transformator?

Responses API, OpenAI-ın **vəziyyət saxlayan söhbətlərə** (`previous_response_id`) malik yeni formatıdır. Müştəri Responses sorğusu göndərdikdə OmniRoute:

1. Daxildə Responses → Chat Completions çevrilməsini həyata keçirir
2. Sorğunu provayderə (Chat Completions dəstəkləyən istənilən provayderə) göndərir
3. Cavabı yenidən Responses formatına çevirir
4. Çevrilmiş cavabı axın şəklində müştəriyə göndərir

Transformator (`transformer/responsesTransformer.ts`) aşağıdakıları təmin edir:

```ts
createResponsesApiTransformStream(): TransformStream
```

Bu, aşağıdakıları emal edir:

- `response.output_item.added` hadisələri
- `response.output_text.delta` hadisələri
- `response.completed` hadisəsi
- Alət çağırışlarının uyğunlaşdırılması (`function_call` ↔ `tool_calls`)

---

## Konfiqurasiya

`open-sse/config/` konfiqurasiya qatını ehtiva edir:

| Fayl                          | Məqsəd                                                |
| ----------------------------- | ----------------------------------------------------- |
| `providerRegistry.ts`         | 352 provayderlik kataloq üzərində çat modeli reyestri |
| `providerModels.ts`           | Model aliasları, format uyğunlaşdırılması             |
| `constants.ts`                | Taymautlar, limitlər, status kodları                  |
| `defaultThinkingSignature.ts` | Standart Claude düşünmə imzası                        |
| `modelStrip.ts` (xidmətlərdə) | Provayder üzrə sahələrin çıxarılması                  |

### Provayder reyestrinin sxemi

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Modul yüklənərkən aparılan Zod yoxlaması bütün provayder konfiqurasiyalarının etibarlı olmasını təmin edir.

---

## Performans Məhdudiyyətləri

Marşrutlaşdırma mühərrikinin ciddi performans limitləri var:

| Əməliyyat                                                            | Hədəf | Ölçmə                      |
| -------------------------------------------------------------------- | ----- | -------------------------- |
| Kombinasiya həlli                                                    | <10ms | 50 hədəf üçün              |
| Sorğu tezliyi limitinin yoxlanılması                                 | <1ms  | Yaddaşdaxili token vedrəsi |
| Model ailəsi üzrə ehtiyat varianta keçid                             | <5ms  | Keşlənmiş ailə tərifləri   |
| Sorğunun marşrut üzrə yönləndirilməsi                                | <2ms  | Kritik icra yolu           |
| **Marşrutlaşdırmanın kritik icra yolunda bloklayan I/O olmamalıdır** | —     | Hamısı asinxron            |

---

## Anti-Patternlər

❌ **`combo.ts` daxilində sinxron DB çağırışları** — əvvəlcədən hesablayın və keşləyin
❌ **İşləyicilərdə təkrar cəhd məntiqi** — dayanıqlılıq xidmətindəki `retry()` funksiyasından istifadə edin
❌ **Provayder konfiqurasiyasına birbaşa giriş** — `providerRegistry` getter-lərindən istifadə edin
❌ **Sərt kodlaşdırılmış ehtiyat keçid zəncirləri** — `modelFamilyFallback.ts` daxilində müəyyən edin
❌ **Paralel sorğular arasında vəziyyət dəyişiklikləri** — yalnız sorğu miqyaslı kontekstdən istifadə edin

---

## Yeni Komponentin Əlavə Edilməsi

### Yeni Xidmətin Əlavə Edilməsi

1. Məhdud və konkret məsuliyyət daşıyan `open-sse/services/[serviceName].ts` yaradın
2. Əsas işləyici funksiyanı və bütün sabitləri ixrac edin
3. `tests/unit/services/[serviceName].test.mjs` daxilində vahid testlər əlavə edin
4. Marşrutlaşdırma ilə əlaqəlidirsə, `handlers/chatCore.ts` daxilində sorğu emalı ardıcıllığına inteqrasiya edin
5. Xidmət hədəf seçiminə təsir edirsə, `combo.ts` daxilində marşrutlaşdırma məntiqini yeniləyin
6. Bu faylda sənədləşdirin

### Yeni İcraedicinin Əlavə Edilməsi

1. `BaseExecutor` sinfini genişləndirən `open-sse/executors/[provider].ts` yaradın
2. `config/providerRegistry.ts` daxilində qeydiyyatdan keçirin
3. `executors/index.ts` fabrik funksiyasına əlavə edin
4. İcraedici üçün vahid testlər əlavə edin
5. `docs/architecture/ARCHITECTURE.md` daxilində sənədləşdirin

### Yeni MCP Alətinin Əlavə Edilməsi

1. `open-sse/mcp-server/tools/[category]Tools.ts` faylını yaradın və ya yeniləyin
2. Girişlər üçün Zod sxemini müəyyən edin
3. Aləti `mcp-server/index.ts` daxilində qeydiyyatdan keçirin
4. `mcp-server/auth/` daxilindəki əhatə matrisi bölməsinə əlavə edin
5. Vahid testlər əlavə edin

---

## Həmçinin Baxın

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — yüksək səviyyəli arxitektura
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — mühəndislik üzrə istinad sənədi
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — qovluqlar üzrə struktur
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 amilli qiymətləndirmə
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP serveri
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A serveri
- Mənbə: `open-sse/` (400-dən çox fayl, ~143K kod sətri)
