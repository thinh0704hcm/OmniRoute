# Embedded Services (Gaeilge)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Leagan:** v3.8.44
> **Nuashonrú deireanach:** 2026-09-09
> **Lucht léitheoireachta:** Innealtóirí a chuireann seirbhísí leabaithe leis, a chothabhálann iad, nó a dhéanann dífhabhtú orthu (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Is uirlisí próisis taobhcharr iad seirbhísí leabaithe a shuiteáiltear go háitiúil agus a shuiteálann agus a mhaoirsíonn OmniRoute, agus
a nochtann sé mar spriocanna ródúcháin den chéad scoth. Murab ionann agus soláthraithe seachtracha (a mbaintear amach iad thar an idirlíon
trí eochracha API), ritheann seirbhísí leabaithe ar an ríomhaire céanna le OmniRoute agus déanann siad cumarsáid thar an gcomhéadan aisfhillte.

---

## Clár na nÁbhar

1. [Forléargas](#1-overview)
2. [Ailtireacht — 4 shraith](#2-architecture--4-layers)
3. [Meaisín staide saolré](#3-lifecycle-state-machine)
4. [Tagairt API](#4-api-reference)
5. [Slándáil](#5-security)
6. [Seirbhís leabaithe nua a chur leis](#6-adding-a-new-embedded-service)
7. [Fabhtcheartú](#7-troubleshooting)
8. [Ceisteanna coitianta](#8-faq)

---

## 1. Forbhreathnú

### Cén fáth seirbhísí leabaithe?

Tá sé sheirbhís leabaithe:

| Seirbhís        | pacáiste npm                                | Port réamhshocraithe | Cuspóir                                                                                                                                                                                                                  |
| --------------- | ------------------------------------------- | :------------------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **9Router**     | `9router`                                   |        20130         | Ródaire AI is féidir le OmniRoute a úsáid mar fhofhoshnáthraí. Nochtar samhlacha mar `9router/{sub}/{model}`                                                                                                             |
| **CLIProxyAPI** | Dénártha eisiúna GitHub (`cliproxy`)        |         8317         | Cuibheoir seachfhreastalaí logánta le haghaidh sreafaí fíordheimhnithe Anthropic CLI. Soláthraíonn sé ródú cúltaca nuair a théann comharthaí OAuth in éag                                                                |
| **Mux**         | `mux` (`mux server` gan chomhéadan grafach) |         8322         | Deamhan logánta ceolfhoirneoireachta gníomhairí (coder/mux). Ní dhéantar ach a shaolré a bhainistiú — ní sprioc ródaithe é (gan seachfhreastalaíocht LLM).                                                               |
| **Bifrost**     | `@maximhq/bifrost`                          |         8080         | Inneall sealaíochta geata AI Go. Nuair atá sé ag rith, roghnaítear go huathoibríoch é ag an mbealach sealaíochta (`/v1/relay/`)                                                                                          |
| **Dario**       | `@askalf/dario`                             |         3456         | Seachfhreastalaí síntiúis Claude — rogha mhalartach/chúltaca ar CLIProxyAPI do thrácht i bhformáid Claude Code; déantar `DARIO_ADMIN_TOKEN` den eochair insteallta, rud a gheataíonn a phlána rialaithe OAuth `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`                      |         8323         | Uathoibriú WhatsApp Web (Chromium gan chomhéadan grafach trí Puppeteer). Ní dhéantar ach a shaolré a bhainistiú — ní sprioc ródaithe é.                                                                                  |

Leanann na sé cinn ar fad an tsamhail mhaoirseachta chéanna:

- Suiteálann OmniRoute iad faoi `DATA_DIR/services/{name}/` (scoite ó `package.json` OmniRoute féin)
- Seolann agus déanann OmniRoute monatóireacht orthu mar phróisis mhac
- Insteallann OmniRoute eochair API ghearrshaolach isteach i dtimpeallacht an phróisis mhic agus rothlaíonn sé í gan aga neamhfhónaimh (nuair is infheidhme)
- Tá gach bealach bainistíochta (`/api/services/*`) **LOCAL_ONLY** — níl rochtain orthu ach ón gcomhéadan lúb-ais (riail dhocht #17)

### Príomhchinntí (ón bplean dearaidh)

| Cinneadh                                          | Luach                                                                                         |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| Rochtain ón deais ar chomhéadan dúchasach 9Router | Seachfhreastalaí droim ar ais ag `/dashboard/providers/services/9router/embed/*`              |
| Meicníocht suiteála                               | `npm install {package}` trí `execFile` (gan idirshuíomh blaosc)                               |
| Mód úsáide                                        | Soláthraí cláraithe mar `9router/{sub}/{model}` san inneall ródaithe                          |
| Bainistiú eochracha API                           | Gineann OmniRoute iad, criptíonn sé iad ar fos (AES-256-GCM), agus insteallann sé trí env iad |
| Suíomh na deaise                                  | `/dashboard/providers/services` (trí chluaisín)                                               |
| Uath-thosú                                        | Scorán do gach seirbhís, MÚCHTA de réir réamhshocraithe                                       |

---

## 2. Ailtireacht — 4 shraith

```
┌────────────────────────────────────────────────────────────────────┐
│  Sraith 1 — Comhéadan úsáideora                                    │
│  /dashboard/providers/services  (cluaisíní: CLIProxyAPI | 9Router | Mux)│
│  Logaí beo (SSE), Tosaigh/Stop/Aththosaigh/Nuashonraigh, Socruithe, Suiteáil│
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Blaosc + ródú cluaisíní de réir ?tab=│
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (fetch Next.js)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Sraith 2 — API (LOCAL_ONLY — aisfhillte amháin)                   │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (seachfhreastalaí HTTP + WebSocket → 9Router réamhtheachtach)   │
│                                                                    │
│  Geata: cuimsíonn LOCAL_ONLY_API_PREFIXES "/api/services/" agus   │
│         "/dashboard/providers/services/*/embed/"                   │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ glaonna laistigh den phróiseas
┌──────────────────────▼─────────────────────────────────────────────┐
│  Sraith 3 — ServiceSupervisor (src/lib/services/)                  │
│                                                                    │
│  ServiceSupervisor.ts   Maoirseoir ginearálta (child_process.spawn)│
│    ├── suiteáil:   execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── tosú:       spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── port:       20130 do 9Router (inchumraithe)                 │
│    ├── logaí:      maolán fáinne stdio 5 MB → teagmhais SSE       │
│    ├── sláinte:    HTTP GET /health gach 2–5 s, athshlánú leisciúil│
│    └── saolré:     SIGTERM 15 s → SIGKILL                          │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Tosaíonn gach SERVICES[] ag tosú an phróisis  │
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       GET tréimhsiúil /v1/models → tábla service_models│
│  ringBuffer.ts      Maolán loga ciorclach (5 MB in aghaidh na seirbhíse)│
│  healthCheck.ts     Tóireadóir sláinte HTTP a dhéanann vótaíocht   │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (cuibheoirí suiteálaí)                        │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP comhoiriúnach le OpenAI (aisfhillte)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Sraith 4 — Soláthraí / Ródú                                      │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Aimsíonn sé an port agus an eochair API arís do gach iarratas (gan taisceadh).│
│    Baineann sé an réimír "9router/" d'aitheantas na samhla roimh sheachfhreastal.│
│    Filleann sé 503 service_not_running mura bhfuil an maoirseoir i "running".│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Iontráil le haghaidh "9router": isEmbeddedService: true         │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Stóráiltear samhlacha mar "9router/{sub}/{model}" (le réimír).  │
│    Sioncronaíonn modelSync.ts iad gach 5 nóiméad.                  │
│                                                                    │
│  Ní dhéantar bainistiú ach ar shaolré Mux (Sraitheanna 1-3) — is   │
│  deamhan ceolfhoirneoireachta gníomhairí é, ní seachfhreastalaí LLM,│
│  mar sin níl iontráil seiceadóra/soláthraí i Sraith 4 aige agus ní │
│  sprioc ródaithe é riamh.                                         │
└────────────────────────────────────────────────────────────────────┘
```

### Príomhchomhaid foinse

| Comhad                                      | Ról                                                      |
| ------------------------------------------- | -------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Croí-aicme: saolré, glas, sláinte, maolán fáinneach      |
| `src/lib/services/bootstrap.ts`             | Clárú ar leibhéal an phróisis agus uath-thosú            |
| `src/lib/services/registry.ts`              | Léarscáil aonáin `tool → supervisor`                     |
| `src/lib/services/apiKey.ts`                | Giniúint eochracha, criptiú AES-256-GCM ar fos           |
| `src/lib/services/modelSync.ts`             | Sioncronú tréimhsiúil samhlacha (5 nóiméad) + ar éileamh |
| `src/lib/services/ringBuffer.ts`            | Maolán loga ciorclach 5 MB le síntiús SSE                |
| `src/lib/services/healthCheck.ts`           | Tóraíocht sláinte HTTP (eatramh inchumraithe)            |
| `src/lib/services/installers/ninerouter.ts` | Suiteáil/nuashonrú/díshuiteáil npm do 9Router            |
| `src/lib/services/installers/cliproxy.ts`   | Suiteáil/nuashonrú/díshuiteáil npm do CLIProxyAPI        |
| `src/lib/services/installers/mux.ts`        | Suiteáil/nuashonrú/díshuiteáil npm do Mux                |
| `src/lib/services/installers/openwa.ts`     | Suiteáil/nuashonrú/díshuiteáil npm do open-wa            |
| `src/app/api/services/9router/_lib.ts`      | Feidhm chúnta `getOrInitSupervisor()`                    |
| `src/app/api/services/[name]/logs/route.ts` | Críochphointe comhroinnte do logaí SSE                   |
| `open-sse/executors/ninerouter.ts`          | Seiceadóir soláthraí (Sraith 4)                          |

---

## 3. Meaisín staide saolré

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
            taiscéalaí sláinte ceart│         tuairt / SIGTERM   │
                               ┌────▼─────┐  (scor laistigh de 5s)│
                               │ running  │──── tuairt ──────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Stóráiltear na staideanna i dtábla DB `version_manager` (colún `status`) agus déantar
iad a scáthánú i staid chuimhne inmheánach `ServiceSupervisor`. Is í an staid chuimhne
inmheánach an fhoinse údarásach do phróiseas atá ar siúl; is í staid DB an rogha
mharthanach chúltaca tráth tosaithe.

### Trasdulta staide

| Ó               | Teagmhas                                        | Go dtí                 |
| --------------- | ----------------------------------------------- | ---------------------- |
| `not_installed` | éiríonn le `install()`                          | `stopped`              |
| `stopped`       | glaoitear `start()`                             | `starting`             |
| `starting`      | filleann an taiscéalaí sláinte 200              | `running`              |
| `starting`      | scoireann an próiseas sula mbíonn sé sláintiúil | `error`                |
| `running`       | glaoitear `stop()`                              | `stopping` → `stopped` |
| `running`       | scoireann an próiseas gan choinne (< 5 s)       | `error` (tuairt thapa) |
| `running`       | scoireann an próiseas gan choinne (> 5 s)       | `error`                |
| `error`         | glaoitear `start()`                             | `starting`             |
| aon cheann      | `stop()` agus é `stopping`                      | gan oibríocht          |

### Glas oibríochta

Déanann `ServiceSupervisor` oibríochtaí saolré a shraithiú trí ghlas oibríochta
aisioncronach (`withLock()`). Má dhéantar glaonna comhthráthacha ar `start()` ar an
maoirseoir céanna, ní chruthaítear ach próiseas amháin; fanann an dara glaoiteoir agus
filleann sé an stádas atá ann cheana. Cuireann sé seo cosc ar riochtaí rása nuair,
mar shampla, a ghníomhaíonn uath-thosú agus cnaipe sa chomhéadan úsáideora ag an am céanna.

---

## 4. Tagairt API

Tá gach bealach faoi `/api/services/` **LOCAL_ONLY** (aisfhillte amháin, riail dhocht #17).
Faigheann iarratais nach iarratais aisfhillte iad `403 LOCAL_ONLY` beag beann ar an gcomhartha fíordheimhnithe.

### 4.1 Críochphointí 9Router (11 bhealach)

#### `POST /api/services/9router/install`

Suiteáil 9Router ó npm. Cruthaítear `DATA_DIR/services/9router/` lena
`package.json` agus `node_modules/` féin. Ní thagann sé salach ar spleáchais OmniRoute féin.

**Corp an iarratais** (iad uile roghnach):

```json
{ "version": "latest" }
```

| Réimse    | Cineál   | Réamhshocrú | Cur síos                               |
| --------- | -------- | ----------- | -------------------------------------- |
| `version` | `string` | `"latest"`  | Clib leagain npm nó semver le suiteáil |

**Freagraí:**

| Stádas | Cur síos                                                                 |
| ------ | ------------------------------------------------------------------------ |
| `200`  | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                   |
| `400`  | Corp iarratais neamhbhailí (teip bhailíochtaithe Zod)                    |
| `409`  | Suiteáil ar siúl cheana féin (glas á choinneáil)                         |
| `500`  | Theip ar shuiteáil npm — féach `message` le haghaidh earráide sothuigthe |

**Nótaí:** Úsáidtear `execFile('npm', [...])` — gan bhlaosc, gan idirshuíomh (riail dhocht #13).
Cuirtear earráidí EACCES i láthair mar theachtaireachtaí sothuigthe.

---

#### `POST /api/services/9router/start`

Tosaigh 9Router. Cláraítear maoirseoir mura bhfuil ceann cláraithe cheana féin, agus ansin glaoitear ar
`supervisor.start()`. Idé-phoitéinseach nuair atá sé ag rith cheana féin.

**Corp an iarratais:** faic

**Freagraí:**

| Stádas | Cur síos                                                |
| ------ | ------------------------------------------------------- |
| `200`  | Oibiacht `ServiceStatus` (féach an scéimre thíos)       |
| `409`  | Níl 9Router suiteáilte (`status: "not_installed"`)      |
| `503`  | Theip ar an tosú (earráid phróisis — féach `lastError`) |

**Scéimre ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Stop 9Router go rianúil. Seoltar SIGTERM, fantar 15 s, agus ansin seoltar SIGKILL má tá sé fós beo.
Idé-phoitéinseach nuair atá sé stoptha cheana féin.

**Corp an iarratais:** faic

**Freagraí:**

| Stádas | Cur síos                           |
| ------ | ---------------------------------- |
| `200`  | `ServiceStatus` (state: "stopped") |
| `503`  | Theip ar an stopadh gan choinne    |

---

#### `POST /api/services/9router/restart`

Comhionann le `stop()` agus ansin `start()` faoi ghlas na hoibríochta.

**Corp an iarratais:** faic

**Freagraí:** mar an gcéanna le `start` (seoltar an `ServiceStatus` deiridh ar ais).

---

#### `POST /api/services/9router/update`

Nuashonraítear 9Router go leagan npm níos nuaí. Má tá an tseirbhís ag rith, stoptar í
ar dtús, ritear suiteáil npm (agus an leagan níos nuaí á shuiteáil san áit chéanna), agus ansin
atosaítear an tseirbhís.

**Corp an iarratais** (iad uile roghnach):

```json
{ "version": "latest" }
```

**Freagraí:**

| Stádas | Cur síos                                                        |
| ------ | --------------------------------------------------------------- |
| `200`  | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`  | Corp neamhbhailí                                                |
| `500`  | Theip ar nuashonrú npm                                          |

---

#### `POST /api/services/9router/rotate-key`

Gintear eochair API nua do 9Router, criptítear í ar fos, agus atosaítear an tseirbhís
(má tá sí ag rith) ionas go bhfaighidh sí an eochair nua óna timpeallacht. Déantar an tseaneochair
neamhbhailí láithreach.

**Corp an iarratais:** faic

**Freagraí:**

| Stádas | Cur síos                                   |
| ------ | ------------------------------------------ |
| `200`  | `{ keyRotated: true, restarted: boolean }` |
| `500`  | Theip ar rothlú na heochrach               |

**Slándáil:** Ní sheoltar an eochair nua ar ais sa fhreagra riamh (gan aon sceitheadh dintiúr).
Stóráiltear í criptithe (AES-256-GCM) sa tábla `version_manager`.

---

#### `GET /api/services/9router/status`

Seoltar stádas comhcheangailte beo + DB ar ais, lena n-áirítear meiteashonraí leagain agus réamhamharc ar an eochair API.

**Freagraí:**

| Stádas | Cur síos                  |
| ------ | ------------------------- |
| `200`  | Féach an scéimre thíos    |
| `500`  | Theip ar léamh an stádais |

**Scéimre an fhreagra:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Scoránaigh an bhratach uath-thosaithe. Nuair atá `enabled: true`, tosaíonn an tseirbhís go huathoibríoch
an chéad uair eile a thosaíonn OmniRoute (má tá an tseirbhís suiteáilte).

**Corp an iarratais:**

```json
{ "enabled": true }
```

**Freagraí:**

| Stádas | Cur síos              |
| ------ | --------------------- |
| `200`  | `{ autoStart: true }` |
| `400`  | Corp neamhbhailí      |

---

#### `GET /api/services/9router/logs`

Sruth SSE de logaí beo ó mhaolán fáinne stdout/stderr 9Router.

**Paraiméadair iarratais:**

| Paraiméadar | Cineál    | Réamhshocrú | Cur síos                                                                     |
| ----------- | --------- | ----------- | ---------------------------------------------------------------------------- |
| `tail`      | `integer` | 200         | Líon na línte stairiúla atá le seoladh ar dtús (uasmhéid 1000)               |
| `filter`    | `string`  | faic        | Scagaire fotheaghráin nach bhfuil cásíogair (gan regex — sábháilte ar ReDoS) |

**Teagmhais SSE:**

| Teagmhas    | Sonraí      | Cur síos                    |
| ----------- | ----------- | --------------------------- |
| `snapshot`  | `LogLine[]` | Eireaball stairiúil tosaigh |
| `log`       | `LogLine`   | Líne loga bheo              |
| `heartbeat` | `{}`        | Coinneáil beo gach 15 s     |

**Scéimre LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Freagraí:**

| Stádas | Cur síos                                                 |
| ------ | -------------------------------------------------------- |
| `200`  | `text/event-stream`                                      |
| `400`  | Paraiméadar `filter` rófhada (> 200 carachtar)           |
| `404`  | Níor aimsíodh an tseirbhís (níl an maoirseoir cláraithe) |

---

### 4.2 Críochphointí CLIProxyAPI (10 mbealach)

Tá an struchtúr críochphointe céanna ag CLIProxyAPI agus atá ag 9Router, seachas
`rotate-key`, agus cuirtear `accounts`, `provider-expose` agus `auto-restart-adopted`
leis. Faigheann sé anois eochair API thiomnaithe don phlána sonraí a instealltar
nuair a sheoltar é (`needsApiKey: true` in `bootstrap.ts`, a úsáidtear chun
samhlacha a shioncronú); tá níos lú réimsí in `status`.

| Modh   | Conair                              | Cur síos                             |
| ------ | ----------------------------------- | ------------------------------------ |
| `POST` | `/api/services/cliproxy/install`    | Suiteáil CLIProxyAPI ó npm           |
| `POST` | `/api/services/cliproxy/start`      | Tosaigh CLIProxyAPI                  |
| `POST` | `/api/services/cliproxy/stop`       | Stop CLIProxyAPI                     |
| `POST` | `/api/services/cliproxy/restart`    | Atosaigh CLIProxyAPI                 |
| `POST` | `/api/services/cliproxy/update`     | Nuashonraigh go leagan níos nuaí     |
| `GET`  | `/api/services/cliproxy/status`     | Stádas beo + DB (gan `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | Scoránaigh an t-uath-thosú           |

Oibríonn an críochphointe comhroinnte `GET /api/services/{name}/logs` (féach §4.1)
do na ceithre sheirbhís ar fad agus an deighleog dhinimiciúil `[name]` á húsáid.

---

### 4.3 Críochphointí Mux (8 mbealach)

Tá an struchtúr críochphointe céanna ag Mux agus atá ag CLIProxyAPI — níl bealach
`rotate-key` sa dromchla API (gineann `getOrCreateApiKey("mux")` an comhartha
iompróra ar an mbealach céanna le comhartha 9Router agus instealltar é tríd an
athróg timpeallachta `MUX_SERVER_AUTH_TOKEN`, ach níl críochphointe tiomnaithe
rothlaithe ann fós). Ní bhainistítear ach saolré Mux: murab ionann agus 9Router,
níl aon seiceadóir Sraith 4 aige agus ní chláraítear riamh é mar sholáthraí
ródaithe.

| Modh   | Conair                         | Cur síos                             |
| ------ | ------------------------------ | ------------------------------------ |
| `POST` | `/api/services/mux/install`    | Suiteáil Mux ó npm (`npm i mux`)     |
| `POST` | `/api/services/mux/start`      | Tosaigh Mux (`mux server`)           |
| `POST` | `/api/services/mux/stop`       | Stop Mux                             |
| `POST` | `/api/services/mux/restart`    | Atosaigh Mux                         |
| `POST` | `/api/services/mux/update`     | Nuashonraigh go leagan npm níos nuaí |
| `GET`  | `/api/services/mux/status`     | Stádas beo + DB                      |
| `POST` | `/api/services/mux/auto-start` | Scoránaigh an t-uath-thosú           |

---

### 4.4 Críochphointí Bifrost (8 mbealach)

Is inneall sealaíochta tairsí IS in Go é Bifrost (`@maximhq/bifrost`). Úsáideann
sé an struchtúr críochphointe céanna le CLIProxyAPI (gan `rotate-key` — bainistíonn
Bifrost a eochracha soláthraí féin in `config.json` faoina `-app-dir`).

| Modh   | Conair                             | Cur síos                                                                    |
| ------ | ---------------------------------- | --------------------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | Suiteáil Bifrost ó npm (`@maximhq/bifrost`)                                 |
| `POST` | `/api/services/bifrost/start`      | Tosaigh Bifrost ar phort 8080 (réamhshocrú)                                 |
| `POST` | `/api/services/bifrost/stop`       | Stop Bifrost                                                                |
| `POST` | `/api/services/bifrost/restart`    | Atosaigh Bifrost                                                            |
| `POST` | `/api/services/bifrost/update`     | Nuashonraigh go leagan níos nuaí                                            |
| `GET`  | `/api/services/bifrost/status`     | Stádas beo + DB                                                             |
| `POST` | `/api/services/bifrost/auto-start` | Scoránaigh an t-uath-thosú                                                  |
| `GET`  | `/api/services/bifrost/logs`       | Eireaball loga SSE (tríd an mbealach dinimiciúil comhroinnte `[name]/logs`) |

**Sreangú ródaithe:** Nuair nach bhfuil `BIFROST_BASE_URL` socraithe agus an
t-ásc Bifrost atá faoi mhaoirseacht ar siúl, úsáideann
`getBifrostRoutingConfig()` (in `routingBackend.ts`) `http://127.0.0.1:{port}`
go huathoibríoch mar bhun-URL an tseachadáin. Bíonn tosaíocht i gcónaí ag athróg
timpeallachta shainráite `BIFROST_BASE_URL`.

---

### 4.5 Críochphointí Dario (12 bhealach)

An struchtúr saolré céanna leis na seirbhísí eile (`install`, `start`, `stop`,
`restart`, `update`, `status`, `auto-start`, `auto-restart-adopted`), chomh maith
le plána rialaithe OAuth faoi `admin/` a bhfuil comhartha riachtanach dó:
`admin/accounts`, `admin/import-from-omniroute`, `admin/login-start`,
`admin/login-complete` (iad uile faoi chosaint `DARIO_ADMIN_TOKEN`).

### 4.6 Críochphointí open-wa (7 mbealach)

Tiomáineann open-wa (`@open-wa/wa-automate`) ásc Chromium gan chomhéadan grafach
(trí Puppeteer) chun WhatsApp Web a uathoibriú. Úsáideann sé an struchtúr
críochphointe céanna le Mux (níl bealach `rotate-key` ann fós). Ní bhainistítear
ach a shaolré — ní sprioc ródaithe é, agus níl aon seiceadóir Sraith 4 ná iontráil
soláthraí aige.

| Modh   | Conair                            | Cur síos                                                           |
| ------ | --------------------------------- | ------------------------------------------------------------------ |
| `POST` | `/api/services/openwa/install`    | Suiteáil open-wa ó npm (`@open-wa/wa-automate`)                    |
| `POST` | `/api/services/openwa/start`      | Tosaigh open-wa ar phort 8323 (réamhshocrú)                        |
| `POST` | `/api/services/openwa/stop`       | Stop open-wa                                                       |
| `POST` | `/api/services/openwa/restart`    | Atosaigh open-wa                                                   |
| `POST` | `/api/services/openwa/update`     | Nuashonraigh go leagan níos nuaí                                   |
| `GET`  | `/api/services/openwa/status`     | Stádas beo + DB                                                    |
| `POST` | `/api/services/openwa/auto-start` | Scoránaigh an t-uath-thosú                                         |
| `GET`  | `/api/services/openwa/logs`       | Deireadh loga SSE (trí róda dinimiciúil comhroinnte `[name]/logs`) |

**Eochair API:** insteallta mar `WA_KEY` — mapálann sárú cineálach athróige
timpeallachta open-wa, leis an réimír `WA_*`, í chuig an rogha CLI `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, fíoraithe i gcoinne an phacáiste suiteáilte
4.76.0). Cuirtear an réimír `ow_` léi nuair a ghineann
`generateServiceApiKey()` í. Léann open-wa an eochair ar ais ó cheanntásc HTTP
`key`/`api_key` (ní ó `Authorization: Bearer`); tá `/api-docs*` díolmhaithe go
sainráite ón tseiceáil (`setupAuthenticationLayer` in
`dist/cli/server.js`), mar sin ní theastaíonn ceanntásc fíordheimhnithe ón
tóireadóir sláinte.

**Péireáil:** tá open-wa neamhoifigiúil agus níl sé cleamhnaithe le WhatsApp —
tá baol ann go gcuirfidh WhatsApp cosc ar an uimhir nasctha mar gheall ar a
bhrath uathoibrithe féin. Ar an gcéad tosú, priontáiltear an cód QR péireála
chuig stdout agus cuirtear ar fáil é tríd an bpainéal Logaí/sruth SSE atá ann
cheana — níl aon chríochphointe tiomnaithe d’íomhá QR sa chomhtháthú seo fós.

---

### 4.7 Seachfhreastalaí droim ar ais (leabú dheais 9Router)

Leabaíonn an deais comhéadan gréasáin 9Router laistigh de iframe trí
sheachfhreastalaí inmheánach droim ar ais ag:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Déanann an seachfhreastalaí seo na nithe seo a leanas:

- Cuireann sé an t-iarratas ar aghaidh chuig `http://127.0.0.1:{port}/{path}` (aischúrsáil amháin)
- Baineann sé na ceanntásca isteach `cookie` agus `authorization` (gan seisiún OmniRoute a sceitheadh)
- Insteallann sé `Authorization: Bearer {apiKey}` le haghaidh fhíordheimhniú 9Router
- Baineann sé `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` den fhreagra
- Athscríobhann sé freagraí HTML chun `<base href>` a instealladh agus conairí absalóideacha a normalú (`/foo` → `/dashboard/.../embed/foo`)

Láimhseálann freastalaí compánach ar phort tiomnaithe uasghráduithe WebSocket don
deais leabaithe (féach `src/lib/services/embedWsProxy.ts`).

**Slándáil:** aicmítear na róid seachfhreastalaí leabaithe faoi
`LOCAL_ONLY_API_PREFIXES` agus ní féidir iad a rochtain ach ón aischúrsáil. Ní
féidir le hionsaitheoir a fhaigheann JWT trí thollán Cloudflare/Ngrok
seachfhreastalú isteach i seirbhísí leabaithe.

---

## 5. Slándáil

### Forfheidhmiú LOCAL_ONLY (riail dhocht #17)

Aicmítear gach bealach faoi `/api/services/` agus `/dashboard/providers/services/*/embed/`
mar LOCAL_ONLY in `src/server/authz/routeGuard.ts`. Ritheann an tseiceáil loopback
gan choinníoll roimh aon bhrainse fíordheimhnithe:

```
tagann an t-iarratas
  → isLocalOnlyPath(path)?
      → ní loopback é → 403 LOCAL_ONLY (i gcónaí, roimh an tseiceáil fíordheimhnithe)
      → loopback é    → lean ar aghaidh chuig an ngnáthfhíordheimhniú
```

Cuireann sé seo cosc ar JWT sceite (m.sh., trí thollán) `npm install` nó
sceitheadh próisis a thionscnamh. Féach `docs/security/ROUTE_GUARD_TIERS.md` don
mhaitrís iomlán sraitheanna.

### Instealladh eochrach API

Teastaíonn eochair API/comhartha iompróra ó 9Router agus Mux dá gcríochphointí HTTP féin.
Déanann OmniRoute an méid seo a leanas:

1. Gineann sé eochair trí `crypto.randomBytes(32).toString("base64url")` le
   réimír a bhaineann go sonrach leis an tseirbhís (`nr_` do 9Router, `mx_` do Mux).
2. Criptíonn sé í agus í ar fos ag úsáid AES-256-GCM (an sifear céanna a úsáidtear do dhintiúir soláthraithe).
3. Díchriptíonn agus insteallann sé í mar athróg timpeallachta tráth sceite —
   `NINEROUTER_API_KEY` do 9Router, `MUX_SERVER_AUTH_TOKEN` do Mux (ní mar bhratach CLI
   riamh, ionas nach mbíonn an comhartha le feiceáil riamh i liostaí `ps`/próiseas).
4. Ní sheolann sé an eochair ghnáth-théacs ar ais riamh in aon fhreagairt HTTP.

Faigheann CLIProxyAPI eochair thiomnaithe don phlána sonraí a instealltar tráth sceite
(`needsApiKey: true` — úsáidtear í chun samhlacha a shioncronú leis an oiriúntóir).

### Cosaint SSRF

Tá an seachfhreastalaí droim ar ais HTTP (`/dashboard/.../embed/[...path]`) códaithe go docht ionas nach gcuireann sé
ar aghaidh ach chuig `http://127.0.0.1:{port}`. Ní leanann sé atreoruithe chuig
cinn scríbe nach loopback iad riamh. Úsáidtear leabharlann `ssrf-req-filter` chun aon URL réamhtheachtach a
réitíonn lasmuigh den raon loopback a dhiúltú.

### Sábháilteacht bhlaoisce (riail dhocht #13)

Glaoitear `npm install` trí `execFile('npm', ['install', pkg, '--prefix', dir])` —
gan aon teimpléid liteartha, blaosc ná idirshuíomh conairí seachtracha i dteaghrán na
hordaithe. Cuirtear luachanna am rite (poirt, eochracha API) ar aghaidh trí oibiacht `env` an phróisis linbh.

### Sláintíocht earráidí (riail dhocht #12)

Téann gach freagairt earráide ó `/api/services/*` trí `buildErrorBody()` nó
`sanitizeErrorMessage()`. Ní sheoltar `err.stack` ná `err.message` amh ar ais
focal ar fhocal chuig an nglaoiteoir riamh.

---

## 6. Seirbhís leabaithe nua a chur leis

Lean na 8 gcéim seo. Léigh na feidhmiúcháin atá ann cheana in `src/lib/services/installers/`
agus `src/app/api/services/` mar an tagairt chanónach.

### Céim 1 — Cruthaigh an suiteálaí

Cruthaigh `src/lib/services/installers/{name}.ts` bunaithe ar `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // roghnaigh port saor

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Úsáid `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` ó `installers/utils.ts`
— ná húsáid `execSync` ná idirshuíomh blaoisce riamh.

### Céim 2 — Cláraigh sa bootstrap

Cuir `ServiceEntry` leis an eagar `SERVICES` in `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false mura bhfuil eochair API ag teastáil
}
```

Leathnaigh `buildSpawnArgsFactory()` chun `cfg.tool === "myservice"` a láimhseáil.

#### Conradh breiseáin soláthraí inphlugáilte (Céim 1, #7333)

Tugann `src/lib/services/providerPlugins/` conradh `ServiceProviderPlugin` isteach a
phacáistíonn réimsí `ServiceEntry` ó `bootstrap.ts` de chuid inneall cúil agus réimsí
teimpléid léirithe ó `serviceBackends.ts` in aon oibiacht amháin, in ionad cruth an innill chúil
chéanna a chur in iúl ar leithligh in dhá chomhad neamhghaolmhara. Agus é seo á scríobh, **níl ach `9router`
aistrithe** — díorthaíonn `bootstrap.ts` a iontráil `SERVICES[]` ó
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
agus caitear earráid tosaithe má bhíonn an breiseán ar iarraidh riamh. Fanann `cliproxy`, `mux`, agus `bifrost`
ar na liteartha inlíne `SERVICES[]` a bhí ann cheana, gan athrú.

Cuireadh cúntóir breisitheach
`createServiceBackendManifestEntry(pluginId, template)` le `open-sse/config/providerPluginManifest.ts` freisin, a thógann
`ProviderPluginManifestEntry` dea-chumtha ó iontráil `SERVICE_BACKEND_MANIFEST_TEMPLATE` — níl sé
**nasctha** le haon chonair iarratais bheo fós (ná le `generateProviderPluginManifestFromRegistry()`
ná le `/v1/providers/[provider]/models`); fanann sin mar obair leantach a luaithe a bheidh an conradh
cruthaithe do dhara hinneall cúil.

Cuireadh siar chuig PRanna leantacha é, arna rianú faoi shaincheist #7333: `cliproxyapi` a aistriú tríd an
gclárlann chéanna, `mux`/`bifrost` a ghinearálú isteach san aontas `ServiceBackendPluginId`,
an láimhseáil speisialta ar ródú seiceadóirí (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) a ionchorprú sa chonradh breiseáin, agus
`createServiceBackendManifestEntry()` a nascadh le conair bheo cód léirithe/samhlacha.

### Céim 3 — Cuir imirce agus síol bunachair sonraí leis

Cinntigh go bhfuil ró don tseirbhís in `version_manager` trí imirce in
`src/lib/db/migrations/`. Ba cheart an méid seo a bheith sa ró:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Céim 4 — Cruthaigh na 7 gcríochphointe API

Faoi `src/app/api/services/{name}/`:

```
_lib.ts            cúntóir getOrInitSupervisor()
install/route.ts   POST — glaonn sé installer.install()
start/route.ts     POST — glaonn sé supervisor.start()
stop/route.ts      POST — glaonn sé supervisor.stop()
restart/route.ts   POST — glaonn sé supervisor.restart()
update/route.ts    POST — glaonn sé installer.update()
status/route.ts    GET  — cumascann sé stádas beo + stádas DB
auto-start/route.ts POST — scoránaíonn sé an bhratach auto_start
```

Tá an bealach comhroinnte `GET /api/services/[name]/logs` sreangaithe cheana féin — níl aon athruithe
ag teastáil ann.

Tarmlig gach freagra earráide trí `createErrorResponse()` / `buildErrorBody()`.

### Céim 5 — Cuir le LOCAL_ONLY_API_PREFIXES

In `src/server/authz/routeGuard.ts`, deimhnigh go bhfuil `/api/services/` liostaithe cheana féin.
Má thugann tú réimír nua isteach (m.sh., `/api/tools/`), cuir le
`LOCAL_ONLY_API_PREFIXES` í agus, má sheolann sí próisis, le `SPAWN_CAPABLE_PREFIXES` freisin.
Cuir tástáil leis in `tests/unit/authz/routeGuard.test.ts`.

### Céim 6 — Cuir cluaisín an chomhéadain úsáideora leis

Cruthaigh `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Athúsáid comhpháirteanna comhroinnte:

- `ServiceStatusCard` — staid bheo + suaitheantas sláinte
- `ServiceLifecycleButtons` — Tosaigh / Stop / Atosaigh / Nuashonraigh
- `ServiceLogsPanel` — deireadh loga SSE (nascann sé le `/api/services/{name}/logs`)
- `ApiKeyCard` — nochtadh + rothlú eochrach (má tá `needsApiKey: true`)

Cláraigh an cluaisín in `ServicesPageShell.tsx`.

### Céim 7 — Cuir iontráil an tsoláthraí leis (más sprioc ródaithe í an tseirbhís)

Má nochtann an tseirbhís leabaithe críochphointe `/v1/chat/completions` atá comhoiriúnach le OpenAI:

1. Cuir iontráil soláthraí le `src/shared/constants/providers.ts` agus `isEmbeddedService: true` inti.
2. Cruthaigh `open-sse/executors/{name}.ts` a shíneann `BaseExecutor`. Aimsigh an port agus
   an eochair API as an nua do gach iarratas (ná taisc sa chruthaitheoir iad riamh). Tabhair freagra
   `503 service_not_running` ar ais nuair nach é `"running"` staid an mhaoirseora.
3. Cláraigh samhlacha in `open-sse/config/providerRegistry.ts` le réimír na seirbhíse
   (m.sh., `myservice/sub/model`). Coinneoidh `modelSync.ts` cothrom le dáta iad.

### Céim 8 — Déan doiciméadú agus tástáil

1. Nuashonraigh `docs/frameworks/EMBEDDED-SERVICES.md` (an comhad seo) — cuir an tseirbhís leis an
   tábla in §1 agus aon chríochphointí nua le §4.
2. Cuir tástálacha aonaid leis in `tests/unit/services/` (saolré, suiteálaí, cruth API).
3. Cuir tástáil chomhtháthaithe leis in `tests/integration/services/` (taobh thiar de `RUN_SERVICES_INT=1`).
4. Nuashonraigh `docs/openapi.yaml` leis na críochphointí nua.

---

## 7. Fabhtcheartú

### Ní thosaíonn an tseirbhís

**Comharthaí:** Tugann an cnaipe tosaithe 503 ar ais, agus fanann an staid mar `"error"` nó `"starting"`.

**Seicliosta:**

1. Seiceáil `GET /api/services/{name}/logs` (nó painéal na Logaí sa deais). Cuardaigh
   línte amhail `Error: ENOENT`, `address already in use`, nó `Cannot find module`.
2. Deimhnigh go bhfuil `npm` in PATH: `which npm` ón gcuntas úsáideora céanna a ritheann OmniRoute.
3. Deimhnigh go bhfuil an tseirbhís suiteáilte: seiceáil `GET /api/services/{name}/status` le haghaidh
   `installedVersion`. Más `null` atá ann, rith an tsuiteáil ar dtús.
4. Seiceáil go bhfuil `DATA_DIR/services/{name}/node_modules/` ann agus nach bhfuil sé folamh.
5. Seiceáil an réimse `lastError` sa fhreagairt stádais le haghaidh chúis slánaithe an scortha.

---

### Tá an fuarthosú mall (> 10 s chun `running` a bhaint amach)

**Comharthaí:** Fanann an staid mar `"starting"` ar feadh i bhfad sula n-athraíonn sí go `"running"` nó `"error"`.

**Míniú:** Áirítear le fuarthosú 9Router crainn mhóra spleáchas a iompórtáil (DNS,
tollán, modúil MITM). Is é 2 s an t-eatramh réamhshocraithe sláinte, le 3 iarracht sula
bhfógraíonn an maoirseoir teorainn ama (ach leanann sé den vótaíocht).

**Réiteach:** Is féidir an teorainn ama `healthIntervalMs` agus `waitForHealthy`
(`healthIntervalMs * 3`) a chumrú in `bootstrap.ts`. I gcás seirbhísí a bhfuil agaí
tosaithe níos faide acu, méadaigh `healthIntervalMs` go 5000 agus `stopTimeoutMs` go 30 000.

---

### Imbhualadh poirt (`EADDRINUSE`)

**Comharthaí:** Taispeánann na logaí `address already in use :::20130`.

**Cúiseanna:**

- Tá próiseas eile ag úsáid phort 20130 cheana féin.
- Níor stopadh próiseas 9Router roimhe seo go hiomlán (PID zombaí).

**Réiteach:**

1. Athraigh an port réamhshocraithe tríd an athróg timpeallachta `NINEROUTER_PORT` in `.env`.
2. Aimsigh agus maraigh an próiseas atá ag teacht salach air: `lsof -ti :20130 | xargs kill -9`.
3. Is féidir an port a chumrú do gach seirbhís in `bootstrap.ts` tríd an réimse `port`.

**Nóta:** Úsáideann 9Router port 20130 de réir réamhshocraithe go sonrach chun imbhualadh le
port réamhshocraithe OmniRoute, 20128, a sheachaint.

---

### Cead diúltaithe (EACCES) le linn suiteála

**Comharthaí:** Tugann an tsuiteáil 500 ar ais, agus taispeánann na logaí `EACCES` nó `permission denied`.

**Cúiseanna:**

- Níl `DATA_DIR` ná a mháthairchomhadlann inscríofa ag próiseas OmniRoute.
- Tá sé á rith laistigh de Docker gan phribhléidí fréimhe agus gan rochtain scríofa ar an imleabhar mapáilte.

**Réiteach:**

1. Seiceáil `DATA_DIR` (réamhshocrú: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Cinntigh gur le húsáideoir phróiseas OmniRoute an chomhadlann: `chown -R $USER ~/.omniroute/`
3. In Docker, cinntigh go bhfuil na ceadanna cearta don úsáideoir coimeádáin ag gléasadh an imleabhair.

---

### Teipeann ar an nuashonrú (teorainn ama `npm install` nó earráid líonra)

**Comharthaí:** Tugann an nuashonrú 500 ar ais le `InstallError`, agus taispeánann na logaí teorainn ama líonra.

**Seicliosta:**

1. Deimhnigh gur féidir clárlann npm a rochtain: `npm ping`.
2. Seiceáil seachfhreastalaí corparáideach: `npm config get proxy`, `npm config get https-proxy`.
3. Bain triail as an tsuiteáil de láimh: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Má tá tú taobh thiar de bhearna aeir, réamh-íoslódáil an tarball agus úsáid `npm install /path/to/tarball.tgz`.

---

### Taispeánann an tseirbhís staid `"error"` díreach tar éis tosaithe (tuairt thapa)

**Comharthaí:** Athraíonn an staid ó `"starting"` go `"error"` laistigh de 5 shoicind.
Taispeánann `lastError` `"Fast crash (exited with code 1)"`.

**Seicliosta:**

1. Léigh eireaball iomlán an loga: `GET /api/services/{name}/logs?tail=500`.
2. Cúis choitianta: athróga timpeallachta a bhfuil an tseirbhís ag súil leo a bheith ar iarraidh.
3. I gcás 9Router: deimhnigh go bhfuil `NINEROUTER_DISABLE_MITM=true` agus
   `NINEROUTER_DISABLE_TUNNEL=true` sa timpeallacht a chuirtear ar aghaidh tráth sceite (féach
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Ceisteanna Coitianta

**C: An féidir liom críochphointí na seirbhísí leabaithe a nochtadh do chliaint nach cliaint loopback iad?**

Ní féidir. Tá an tsraith LOCAL_ONLY ann d’aon ghnó (riail dhocht #17). Ní ceadmhach bealaí ar féidir leo
`npm install` a agairt nó próisis `node` a thosú a bheith inrochtana ó thrácht nach trácht loopback é,
mar dá sceithfí JWT trí thollán (Cloudflare, Ngrok, Tailscale), d’fhéadfaí próisis threallacha
a thosú murach sin. Níl aon eisceacht roghnach ann do
`/api/services/` — murab ionann agus `/api/mcp/`, tá sé eisiata ó liosta seachanta an scóip bhainistíochta.
Féach `docs/security/ROUTE_GUARD_TIERS.md`.

---

**C: An mbeidh 9Router agus CLIProxyAPI ar fáil in imscaradh táirgthe/scamaill?**

Beidh. Leanann an dá sheirbhís an tsamhail chéanna atá áitiúil ar dtús agus a leanann OmniRoute féin. Ritheann siad
ar an meaisín céanna agus déanann siad cumarsáid thar loopback. Ciallaíonn "táirgeadh" anseo an VPS
nó an freastalaí áitiúil ar a bhfuil OmniRoute imscartha, seachas soláthraí cianda néalríomhaireachta.

---

**C: Conas a dhéanaim dífhabhtú ar an maoirseoir?**

1. Lean sruth loga SSE: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Seiceáil na logaí struchtúrtha in aschur pino OmniRoute agus iad scagtha de réir
   an ainmspáis `service:supervisor`.
3. Scrúdaigh an ró sa bhunachar sonraí: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Úsáid `GET /api/services/9router/status` chun an staid bheo reatha, PID, sláinte,
   agus `lastError` a fheiceáil in aon ghlao amháin.

---

**C: Taispeánann an maoirseoir `health: "degraded"` nó `health: "unknown"` ach is é `"running"` an staid. An fadhb é sin?**

Ciallaíonn `"degraded"` gur thug an tóireadóir sláinte freagra nach freagra 200 é. Ciallaíonn `"unknown"` nach bhfuil aon
tóireadóir críochnaithe fós (rás leis an gcéad vótaíocht). Bíonn an dá cheann sealadach le linn tosaithe.
Má fhanann an tsláinte ag `"degraded"` ar feadh níos mó ná `healthIntervalMs * 3` ms tar éis
`"running"`, tá an tseirbhís leabaithe ag rith ach níl a API HTTP ag freagairt. Seiceáil
an bhfuil an port ceart sa fhreagra stádais agus an bhfuil an tseirbhís ag éisteacht
ar an bport sin i ndáiríre.

---

**C: An féidir liom eochair API 9Router a athrú gan atosú iomlán?**

Ní féidir. Cuirtear an eochair API ar aghaidh chuig 9Router trí athróg timpeallachta tráth tosaithe an phróisis.
Ní féidir athróga timpeallachta a athrú i bpróiseas atá ag rith. Stopann agus atosaíonn `POST .../rotate-key`
an tseirbhís go huathoibríoch chun an eochair nua a chur i bhfeidhm. Tagann rothlú na heochrach
i bhfeidhm laistigh de `stopTimeoutMs` na seirbhíse (15 s de réir réamhshocraithe) móide a tréimhse
tosaithe.

---

**C: Cad é teorainn an mhaoláin fháinnigh agus cad a tharlaíonn nuair a líontar é?**

Tá maolán fáinneach tiomnaithe 5 MB ag gach seirbhís. Nuair a bhíonn an maolán lán, díbrítear na línte loga
is sine chun spás a dhéanamh do chinn nua. Tugann an teagmhas SSE `snapshot`
na línte is déanaí laistigh den teorainn `tail` ar ais. Ní bhuanchoimeádtar logaí ar diosca mura bhfuil
`logsBufferPath` socraithe sa ró sa bhunachar sonraí.

---

## Féach freisin

- `docs/security/ROUTE_GUARD_TIERS.md` — sonraí faoin tsraith LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 mapáil mhodúl na Seirbhísí Leabaithe
- `docs/architecture/ARCHITECTURE.md` — comhthéacs ar leibhéal an chórais
- `docs/openapi.yaml` — sainmhínithe críochphointí atá inléite ag meaisín
- `CLAUDE.md` §"Seirbhís Leabaithe Nua a Chur Leis" — seicliosta mear-thagartha
