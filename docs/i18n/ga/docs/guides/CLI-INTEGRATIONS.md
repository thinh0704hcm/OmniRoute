# CLI Integrations (Gaeilge)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLI-INTEGRATIONS.md) · 🇪🇹 [am](../../../am/docs/guides/CLI-INTEGRATIONS.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLI-INTEGRATIONS.md) · 🇦🇿 [az](../../../az/docs/guides/CLI-INTEGRATIONS.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLI-INTEGRATIONS.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLI-INTEGRATIONS.md) · 🇧🇦 [bs](../../../bs/docs/guides/CLI-INTEGRATIONS.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLI-INTEGRATIONS.md) · 🇩🇰 [da](../../../da/docs/guides/CLI-INTEGRATIONS.md) · 🇩🇪 [de](../../../de/docs/guides/CLI-INTEGRATIONS.md) · 🇬🇷 [el](../../../el/docs/guides/CLI-INTEGRATIONS.md) · 🇪🇸 [es](../../../es/docs/guides/CLI-INTEGRATIONS.md) · 🇪🇪 [et](../../../et/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLI-INTEGRATIONS.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLI-INTEGRATIONS.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLI-INTEGRATIONS.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇱 [he](../../../he/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLI-INTEGRATIONS.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLI-INTEGRATIONS.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLI-INTEGRATIONS.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇩 [id](../../../id/docs/guides/CLI-INTEGRATIONS.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇹 [it](../../../it/docs/guides/CLI-INTEGRATIONS.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLI-INTEGRATIONS.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLI-INTEGRATIONS.md) · 🇰🇭 [km](../../../km/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLI-INTEGRATIONS.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLI-INTEGRATIONS.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLI-INTEGRATIONS.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLI-INTEGRATIONS.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLI-INTEGRATIONS.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLI-INTEGRATIONS.md) · 🇲🇲 [my](../../../my/docs/guides/CLI-INTEGRATIONS.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLI-INTEGRATIONS.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLI-INTEGRATIONS.md) · 🇳🇴 [no](../../../no/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [or](../../../or/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLI-INTEGRATIONS.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLI-INTEGRATIONS.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLI-INTEGRATIONS.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLI-INTEGRATIONS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLI-INTEGRATIONS.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLI-INTEGRATIONS.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLI-INTEGRATIONS.md) · 🇱🇰 [si](../../../si/docs/guides/CLI-INTEGRATIONS.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLI-INTEGRATIONS.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLI-INTEGRATIONS.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLI-INTEGRATIONS.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLI-INTEGRATIONS.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLI-INTEGRATIONS.md) · 🇮🇳 [te](../../../te/docs/guides/CLI-INTEGRATIONS.md) · 🇹🇭 [th](../../../th/docs/guides/CLI-INTEGRATIONS.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLI-INTEGRATIONS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLI-INTEGRATIONS.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLI-INTEGRATIONS.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLI-INTEGRATIONS.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLI-INTEGRATIONS.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLI-INTEGRATIONS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLI-INTEGRATIONS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLI-INTEGRATIONS.md)

---

Cuireann OmniRoute teaghlach d’orduithe `setup-*` ar fáil a chumraíonn CLI
códaithe (Codex, Claude Code, OpenCode, Cline, …) chun OmniRoute a úsáid mar a
inneall — ionas go labhraíonn an uirlis le críochphointe **amháin** agus go
seolann OmniRoute an t-iarratas chuig an soláthraí ceart le cúltiú uathoibríoch.
Léann gach ordú catalóg **bheo** na samhlacha ó OmniRoute atá ar siúl
(go háitiúil nó go cianda) agus scríobhann sé comhad cumraíochta na huirlise
féin ar **do** ríomhaire. Déantar tagairt don eochair API trí athróg timpeallachta
pé áit a dtacaíonn an uirlis leis sin. Luaitear thíos na horduithe a chaomhnaíonn
comhad timpeallachta áitiúil don uirlis.

Tá lainseálaí cineálach ann freisin — `omniroute run <target>` — a thosaíonn
`claude`, `codex`, `aider`, `goose`, `opencode`, `qwen` nó `gemini` agus an
timpeallacht cheart insteallta aige, gan aon chumraíocht a scríobh ar chor ar bith.
Tagann na spriocanna agus a n-ailiasanna ón bhforógra canónach
`bin/cli/cli-manifest.mjs`
(`claude-code|cc|anthropic`, `codex-cli|openai-codex|openai`, `goose-cli`,
`open-code`, `qwen-code`, `gemini-cli`), agus cuireann `omniroute completion` na
spriocfhocail chéanna, a dhíorthaítear ón bhforógra, ar fáil. Tá na seanlainseálaithe
ar leith do gach uirlis — `omniroute launch` (Claude Code) agus
`omniroute launch-codex` (Codex) — ar fáil i gcónaí.

Tá ionduchtú soláthraithe ar fáil ón gcomhthéacs áitiúil/cianda céanna. Coinníonn
na horduithe API-lárnacha thíos fíordheimhniú bainistíochta ar leithligh ó
dhintiúir soláthraithe agus ní phriontálann siad dintiúr riamh in aschur
struchtúrtha:

```bash
omniroute providers add glm --credential-env GLM_API_KEY --name work
omniroute providers import ./providers.json --dry-run --json
omniroute providers auth openai
omniroute providers edit <connection-id> --default-model glm/glm-5.2
omniroute providers remove <connection-id> --yes
```

I gcás scripteanna, b’fhearr `--credential-stdin` nó `--credential-env` a úsáid;
coinnítear `--credential` le haghaidh úsáid áitiúil rialaithe. Éilíonn
`providers remove` an rogha `--yes` ar theirminéal neamh-idirghníomhach, agus
cloíonn gach ceann de na cúig ordú leis an gcomhthéacs gníomhach nó leis na
roghanna domhanda `--base-url`/`--api-key`.

Le haghaidh bhunshocrú aonuaire de láimh don dá chomhtháthú is saibhre, féach na
miontreoracha do gach uirlis:

- [Cumraíocht Claude Code](./CLAUDE-CODE-CONFIGURATION.md)
- [Cumraíocht Codex CLI](./CODEX-CLI-CONFIGURATION.md)
- [Mód Cianda](./REMOTE-MODE.md) — rialaigh OmniRoute cianda (VPS / Tailnet) ó do ríomhaire glúine
- [VS Code Copilot Chat](./VSCODE-COPILOT.md) — an eisínteacht OmniCopilot; is féidir léi na
  horduithe `setup-*` seo a rith duit ón taobh istigh den eagarthóir freisin

---

## Máistirthábla

Cloíonn gach ordú leis an **gcomhthéacs gníomhach** (a shocraítear le
`omniroute connect`, féach [Mód Cianda](./REMOTE-MODE.md)) nó le bratacha
sainráite `--remote <url> --api-key <key>`.
Seo a leanas is brí le "áitiúil i gcomparáid le cianda": gan bhratacha ar bith,
díríonn sé ar `http://localhost:20128`; le `--remote` (nó comhthéacs cianda
gníomhach), faigheann sé an chatalóg ón bhfreastalaí sin agus scríobhann sé an
chumraíocht go háitiúil.

| Ordú                       | Uirlis                             | An méid a scríobhann sé                                                                                                                                                                            | Príomhbhratacha                                                                                                                            | Logánta nó cianda |
| -------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ----------------- |
| `omniroute setup-codex`    | OpenAI Codex CLI                   | `~/.codex/<name>.config.toml` — próifíl amháin in aghaidh gach samhla téacs comhoiriúnaí (`codex --profile <name>`)                                                                                | `--remote` `--api-key` `--only` `--dry-run` `--port` `--codex-home`                                                                        | An dá cheann      |
| `omniroute setup-claude`   | Claude Code                        | `~/.claude/profiles/<name>/settings.json` — próifíl amháin in aghaidh gach samhla chomhoiriúnaithe (`CLAUDE_CONFIG_DIR`)                                                                           | `--remote` `--api-key` `--only` `--dry-run` `--port` `--claude-home`                                                                       | An dá cheann      |
| `omniroute setup-opencode` | OpenCode (comhoiriúnach le openai) | `~/.config/opencode/opencode.json` — soláthraí `omniroute` le gach samhail sa chatalóg (`opencode -m omniroute/<model>`)                                                                           | `--remote` `--api-key` `--only` `--model` `--dry-run` `--port`                                                                             | An dá cheann      |
| `omniroute setup-cline`    | Cline                              | `~/.cline/data/{globalState,secrets}.json` (mód CLI) + priontálann sé socruithe eisínteachta VS Code                                                                                               | `--remote` `--api-key` `--model` `--yes` `--dry-run` `--port` `--cline-dir`                                                                | An dá cheann      |
| `omniroute setup-kilo`     | Kilo Code                          | `~/.local/share/kilo/auth.json` (CLI) + cumascann sé `kilocode.*` isteach i `settings.json` VS Code má tá sé ann                                                                                   | `--remote` `--api-key` `--model` `--yes` `--dry-run` `--port` `--auth-path` `--vscode-settings`                                            | An dá cheann      |
| `omniroute setup-continue` | Continue / `cn` CLI                | `~/.continue/config.yaml` — samhlacha `provider: openai`, eochair trí `${{ secrets.OMNIROUTE_API_KEY }}`                                                                                           | `--remote` `--api-key` `--only` `--dry-run` `--port` `--config-path`                                                                       | An dá cheann      |
| `omniroute setup-cursor`   | Cursor                             | Faic — priontálann sé na céimeanna laistigh den aip (is SQLite doiléir é cumraíocht Cursor)                                                                                                        | `--remote` `--api-key` `--only` `--port`                                                                                                   | An dá cheann      |
| `omniroute setup-roo`      | Roo Code                           | `~/.omniroute/roo-settings.json` (doiciméad iompórtála) + socraíonn sé `roo-cline.autoImportSettingsPath` má tá `settings.json` VS Code ann                                                        | `--remote` `--api-key` `--model` `--yes` `--dry-run` `--port` `--import-path` `--vscode-settings`                                          | An dá cheann      |
| `omniroute setup-crush`    | Crush                              | `~/.config/crush/crush.json` — soláthraí `openai-compat`, eochair trí `$OMNIROUTE_API_KEY`                                                                                                         | `--remote` `--api-key` `--only` `--dry-run` `--port` `--config-path`                                                                       | An dá cheann      |
| `omniroute setup-goose`    | Goose                              | `~/.config/goose/config.yaml` (`GOOSE_PROVIDER`/`OPENAI_HOST`/`GOOSE_MODEL`) + priontálann sé oideas timpeallachta                                                                                 | `--remote` `--api-key` `--model` `--yes` `--dry-run` `--port` `--config-path`                                                              | An dá cheann      |
| `omniroute setup-aider`    | Aider                              | `~/.aider.conf.yml` (`openai-api-base` + `model: openai/<id>`) + priontálann sé oideas timpeallachta                                                                                               | `--remote` `--api-key` `--model` `--yes` `--dry-run` `--port` `--config-path`                                                              | An dá cheann      |
| `omniroute setup-qwen`     | Qwen Code                          | `~/.qwen/settings.json` — eagar V4 `modelProviders.openai` + `OMNIROUTE_API_KEY` in `~/.qwen/.env`                                                                                                 | `--remote` `--api-key` `--model` `--yes` `--dry-run` `--port` `--config-path` `--env-path`                                                 | An dá cheann      |
| `omniroute setup-5dive`    | 5dive (flít gníomhairí)            | Faic faoi `$HOME` — scríobhann sé **próifíl fíordheimhnithe** 5dive (`/var/lib/5dive/auth-profiles/<name>/`) trí `5dive agent auth set`; don úsáideoir fréimhe amháin, ritear ar óstach an fhlít é | `--remote` `--api-key` `--model` `--auth-profile` `--agent` `--byo-provider` `--fivedive-bin` `--no-sudo` `--yes` `--dry-run` `--port`     | An dá cheann      |
| `omniroute run <target>`   | Seoladh ama rite (ginearálta)      | Faic — sceitheann sé `claude`/`codex`/`aider`/`goose`/`opencode`/`qwen`/`gemini` leis an timpeallacht agus na hargóintí cearta; úsáideann Qwen agus Gemini baile sealadach leithlisithe            | `--remote` `--base-url` `--context` `--provider` `--model` `--api-key` `--api-key-env` `--dry-run` `--json` `--port` `--profile` `--token` | An dá cheann      |
| `omniroute launch`         | Claude Code                        | Faic — sceitheann sé `claude` agus `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` insteallta ann                                                                                                      | `--remote` `--api-key` `--token` `--profile` `--port`                                                                                      | An dá cheann      |
| `omniroute launch-codex`   | OpenAI Codex CLI                   | Faic — sceitheann sé `codex` agus an soláthraí `omniroute` insteallta trí bhratacha `-c`                                                                                                           | `--remote` `--api-key` `--profile` (`-p`) `--port`                                                                                         | An dá cheann      |

Nótaí faoi bhratacha (fíoraithe i bhfoinse an ordaithe):

- `--remote <url>` — faigh an chatalóg ó OmniRoute cianda (sáraíonn sé `--port`
  agus an comhthéacs gníomhach). Soláthraíonn `--api-key <key>` an dintiúr don
  fhreastalaí sin (úsáidtear an athróg timpeallachta `OMNIROUTE_API_KEY` mar réamhshocrú, nó comhartha an chomhthéacs ghníomhaigh).
- `--only <patterns>` — fotheaghráin scartha le camóga; ná coinnigh ach aitheantais samhlacha a mheaitseálann
  (m.sh. `--only glm,kimi`). Ar fáil ar `setup-codex`, `setup-claude`,
  `setup-opencode`, `setup-continue`, `setup-cursor`, `setup-crush`.
- `--dry-run` — priontáil go díreach an méid a scríobhfaí gan teagmháil a dhéanamh leis an
  gcóras comhad. Ar fáil ar gach ordú `setup-*` **seachas** `setup-cursor`
  (nach scríobhann comhad riamh).
- `--model <id>` — riachtanach (nó roghnaithe go hidirghníomhach) do na huirlisí nach bhfuil
  uathaimsiú samhlacha acu: Cline, Kilo, Roo, Goose, Qwen, Aider, 5dive. Glacann na huirlisí sin
  le `--yes` freisin le haghaidh rite neamh-idirghníomhacha (agus bíonn `--model` riachtanach ansin).
  Glacann `setup-opencode` le `--model` chun an tsamhail réamhshocraithe barrleibhéil a shocrú.
- Leanann `--model <id>` ar `omniroute run` sreangú an lastliosta de réir sprice
  (`bin/cli/cli-manifest.mjs`): faigheann **aider** `--model openai/<id>` agus
  faigheann **opencode** `--model omniroute/<id>` (ní chuirtear an réimír leis ach amháin nuair nach bhfuil sé
  ar an aitheantas cheana féin); faigheann **qwen** agus **gemini** an t-aitheantas focal ar fhocal;
  faigheann **claude** é trí `ANTHROPIC_MODEL`, **goose** trí `GOOSE_MODEL`, agus
  **codex** trí argóintí `-c model_providers.omniroute.*`. **Is é Qwen an t-aon sprioc rite
  a éilíonn `--model` go docht** — scoireann `omniroute run qwen` gan é
  le `2` agus earráid shainráite.
- `--port <port>` — port áitiúil OmniRoute (`20128` mar réamhshocrú, déantar neamhaird de nuair atá `--remote`
  socraithe). I láthair ar gach `setup-*` agus ar an dá thosaitheoir.
- Cóid scoir `omniroute run`: cuirtear cód scoir CLI an phróisis mhic ar aghaidh
  focal ar fhocal; `2` = argóintí neamhbhailí (sprioc gan tacaíocht, easpa
  `--model` riachtanach, cosaint coimeádáin); `127` = níl dénártha na sprice in `PATH`;
  `130`/`143`/`129` nuair a chríochnaíonn `SIGINT`/`SIGTERM`/`SIGHUP` an tosú;
  `1` = teip eile ar thosú ag am rite.
- Glacann an dá thosaitheoir (`launch`, `launch-codex`) le `--profile <name>` chun
  próifíl arna scríobh ag `setup-claude` / `setup-codex` a roghnú, chomh maith le hargóintí a chuirtear ar aghaidh
  chuig an dénártha bunúsach `claude` / `codex`.

Tá an roghnóir idirghníomhach comhroinnte ag na hoidis socraithe freisin:

```bash
# Roghnaigh ón gcatalóg samhlacha áitiúil nó chianda ghníomhach agus cumraigh an sprioc.
omniroute configure claude
omniroute configure opencode --provider glm
omniroute configure qwen --model qwen/qwen3.8-max-preview --yes
```

Faoi láthair, tarmligeann `configure` chuig na hoidis thástáilte le haghaidh `codex`, `claude`,
`opencode`, `qwen`, `aider`, `goose`, `cline`, `continue`, `kilo`, agus `5dive`.
Fanann iontrálacha catalóige a bhaineann le IDE amháin,
MITM, agus treoracha amháin mar shreafaí sainráite `setup-*`/láimhe agus
ní chuirtear i láthair iad mar spriocanna intosaithe.

> Is é `setup-opencode` an comhtháthú OpenCode **éadrom atá comhoiriúnach le openai**.
> Tá comhtháthú breiseáin níos saibhre ann freisin — `omniroute setup opencode` — a
> shuiteálann `@omniroute/opencode-plugin`. Is orduithe éagsúla iad; déanann an tábla
> thuas cur síos ar `setup-opencode`.
>
> Tagann an breiseán in dhá phacáiste, ceann amháin do gach mórleagan de OpenCode, toisc go
> mbíonn pointí iontrála éagsúla ag teastáil ón dá lódálaí:
> `@omniroute/opencode-plugin` le haghaidh OpenCode v1 agus
> `@omniroute/opencode-plugin-v2` le haghaidh OpenCode v2. Tá an pacáiste v2 nua
> (`0.1.0`) agus leanann sé conradh óstaigh atá fós ag athrú, mar sin léann sé an
> cruth a chuireann OpenCode isteach i ndréacht na catalóige seachas glacadh leis roimh ré. Suiteáil
> é trí iontráil `plugins` a chur le `opencode.json`; suiteálann `omniroute setup opencode`
> an pacáiste v1 fós. Tá na roghanna agus ord cuardaigh na ndintiúr i
> README an phacáiste.

---

## Úsáid áitiúil

Agus OmniRoute ag rith ar `localhost:20128`, níl le déanamh ach an t-ordú socraithe a rith do d’uirlis. Faightear an chatalóg ón bhfreastalaí áitiúil.

```bash
# Codex: scríobh próifíl do gach samhail mheaitseáilte isteach in ~/.codex/
omniroute setup-codex
codex --profile glm52            # úsáid próifíl ghinte

# Claude Code: scríobh próifílí de réir samhla, ansin seol ceann acu
omniroute setup-claude
omniroute launch --profile glm52

# OpenCode: scríobh an soláthraí atá comhoiriúnach le openai agus samhlacha uile na catalóige ann
omniroute setup-opencode
export OMNIROUTE_API_KEY=sk-...  # tagraítear dó trí {env:OMNIROUTE_API_KEY}, ní chuirtear ar diosca riamh é
opencode -m omniroute/glm/glm-5.2 "..."

# Teastaíonn samhail shainráite ó uirlisí nach bhfuil uathfhionnachtain acu:
omniroute setup-aider --model glm/glm-5.2
omniroute setup-qwen --model qwen/qwen3.8-max-preview

# Réamhamharc gan aon rud a scríobh:
omniroute setup-continue --dry-run
```

Seol gan aon chumraíocht a scríobh ar chor ar bith (instealladh timpeallachta amháin):

```bash
omniroute launch                 # Claude Code → OmniRoute áitiúil
omniroute launch-codex           # Codex CLI → OmniRoute áitiúil
omniroute launch-codex --profile glm52
omniroute run claude --model openai/gpt-5.4
omniroute run codex --model openai/gpt-5.4 --dry-run --json
omniroute run aider --model glm/glm-5.2 -- --message "reply OK"
omniroute run goose --model glm/glm-5.2
omniroute run opencode --model glm/glm-5.2 -- run "reply OK"
omniroute run qwen --model glm/glm-5.2 -- -p "reply OK"
omniroute run gemini --model glm/glm-5.2 -- --skip-trust -p "reply OK"

# Conair ordaithe shainráite: cuir ar aghaidh cibé rud a thagann tar éis --
omniroute run claude -- --print-system-prompt "review this diff"
```

---

## Úsáid chianda

Dírigh aon ordú socraithe ar OmniRoute cianda le `--remote` + `--api-key`. Faightear an chatalóg ón gcianchóras; scríobhtar an chumraíocht ar do ríomhaire áitiúil.

```bash
# OpenCode i gcoinne VPS chianda, ná coinnigh ach samhlacha glm/kimi
omniroute setup-opencode --remote http://192.168.0.15:20128 --api-key oma_live_xxx \
  --only glm,kimi
opencode -m omniroute/glm/glm-5.2 "..."   # easpórtáil OMNIROUTE_API_KEY ar dtús

# Próifílí Codex ó chatalóg chianda
omniroute setup-codex --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Seol CLI go díreach i gcoinne an chianchórais
omniroute launch       --remote http://192.168.0.15:20128 --api-key oma_live_xxx
omniroute launch-codex --remote http://192.168.0.15:20128 --api-key oma_live_xxx
```

In ionad `--remote`/`--api-key` a thabhairt gach uair, logáil isteach uair amháin agus lig don **chomhthéacs gníomhach** iad a sholáthar go huathoibríoch:

```bash
omniroute connect 192.168.0.15        # gineann comhartha le raon feidhme agus stórálann sé an comhthéacs
omniroute setup-codex                 # ← úsáideann sé an chatalóg chianda anois
omniroute setup-opencode              # ← mar an gcéanna
omniroute launch                      # ← Claude Code i gcoinne an chianchórais
```

Féach [Mód Cianda](./REMOTE-MODE.md) le haghaidh comhthéacsanna, raonta feidhme agus bainistíocht comharthaí.

---

## Flíteanna gníomhairí 5dive

Ritheann [5dive](https://5dive.ai) flít de ghníomhairí códúcháin fadreatha, agus gach ceann acu ina aonad systemd faoina úsáideoir Unix féin. Ní CLI códúcháin é féin, mar sin níl aon rud ann a d’fhéadfadh `omniroute run` a sheoladh — is sprioc **chumraíochta amháin** é `5dive`.

```bash
omniroute configure 5dive --model failover-demo --yes
omniroute setup-5dive --model failover-demo --auth-profile omniroute --agent worker1
```

Scríobhann an dá fhoirm próifíl **fíordheimhnithe** amháin do 5dive, agus déanann gach suíochán `claude` atá ceangailte leis an bpróifíl sin cumarsáid le OmniRoute ansin. Baineann trí rud go sonrach leis an sprioc seo:

- **Ritheann sé ar óstríomhaire an fhlít, mar root.** Feidhmíonn briathra 5dive ar aonaid áitiúla systemd agus ar chomhadlann staide atá faoi úinéireacht root; níl aon mhód cianda ann. Athsheolann an t-oideas é féin trí `sudo` nuair nach bhfuil sé ag rith mar root cheana féin (múchann `--no-sudo` é sin agus priontálann sé an t-ordú ina ionad).
- **Ní mór don chríochphointe a bheith ina `https://` mura seoladh aislúibe é.** Seoltar eochair API an ghníomhaire leis an URL sin ar gach iarratas, agus diúltaíonn 5dive do chríochphointe neamhchriptithe lasmuigh den óstríomhaire. Ní eisceacht é seoladh príobháideach LAN.
- **Tá tosaíocht ag socrú seasta samhla gach suíocháin féin ar an bpróifíl.** Iompraíonn an phróifíl `ANTHROPIC_DEFAULT_{OPUS,SONNET,HAIKU}_MODEL`, ach teipeann ar shuíochán atá fós socraithe go seasta ar aitheantas samhla caighdeánach ar a chéad seal leis an teachtaireacht _"There's an issue with the selected model"_. Tabhair `--agent <name>` (is féidir é a athdhéanamh) chun na suíocháin a shocrú go seasta freisin; priontálann an t-oideas an t-ordú nuair nach ndéanann tú amhlaidh.

Tugtar an eochair API do 5dive ar **stdin** (`--api-key=-`), mar sin ní bhíonn sí le feiceáil riamh in aschur `ps`.

Tríd an bpróifíl a dhíriú ar **theaglaim** OmniRoute seachas ar shamhail aonair, faigheann an flít cúltaca soláthraí: nuair a thit an príomhchríochphointe go hiomlán i lár seala sa rith a taifeadadh ar [#11578](https://github.com/diegosouzapw/OmniRoute/issues/11578), chríochnaigh an gníomhaire na céimeanna a bhí fágtha aige ar an rogha chúltaca agus níor nocht sé an briseadh seirbhíse riamh.

---

## Coinbhinsiúin an bhun-URL (cé na huirlisí a éilíonn `/v1`)

Nochtann OmniRoute dromchla OpenAI ag `/v1`, dromchla Anthropic ag an bhfréamh,
agus dromchla dúchasach Gemini ag `/v1beta`. Tá gach comhtháthú ceangailte leis an bhfoirm a
mbíonn a uirlis ag súil léi (fíoraithe i bhfoinse an ordaithe):

| Comhtháthú                                                                 | Bun-URL a scríobhtar | `/v1`?                                                |
| -------------------------------------------------------------------------- | -------------------- | ----------------------------------------------------- |
| `setup-cline` (`openAiBaseUrl`)                                            | fréamh               | Ní hea — cuireann Cline `/v1/chat/completions` leis   |
| `setup-goose` (`OPENAI_HOST`)                                              | fréamh               | Ní hea — cuireann Goose an chonair leis               |
| `setup-aider` (`OPENAI_API_BASE`)                                          | fréamh               | Ní hea — cuireann LiteLLM `/v1/chat/completions` leis |
| `setup-kilo`, `setup-roo`, `setup-continue`, `setup-crush`, `setup-cursor` | le `/v1`             | Is ea                                                 |
| `setup-claude` (`ANTHROPIC_BASE_URL`), `launch`                            | fréamh               | Ní hea — cuireann Claude Code `/v1/messages` leis     |
| `setup-codex`, `launch-codex` (`model_providers.omniroute.base_url`)       | le `/v1`             | Is ea                                                 |
| `setup-qwen` (`modelProviders.openai[].baseUrl`)                           | le `/v1`             | Is ea                                                 |
| `run gemini` (`GOOGLE_GEMINI_BASE_URL`)                                    | fréamh               | Ní hea — cuireann an SDK `/v1beta/models/…` leis      |
| `setup-5dive` (`ANTHROPIC_BASE_URL` sa phróifíl fíordheimhnithe)           | fréamh               | Ní hea — cuireann Claude Code `/v1/messages` leis     |

---

## Spleáchais dhúchasacha a choinneáil le linn nuashonraithe: `--include=optional`

Nuair a dhéanann tú nuashonrú le `omniroute update` (tar éis deimhniú, nó le `--apply`),
ritheann OmniRoute an tsuiteáil agus `--include=optional` ionsuite inti:

```bash
npm install -g omniroute@latest --include=optional
```

**Ní** bratach í seo a chuireann tú ar aghaidh chuig `omniroute update` — cuireann an
nuashonraitheoir i bhfeidhm i gcónaí í. Cinntíonn sí go maireann na `optionalDependencies`
(`better-sqlite3`, `keytar`, `tls-client`, cruach SLM LLMLingua) an nuashonrú fiú má tá
`omit=optional` socraithe i do chumraíocht npm, rud a d’fhágfadh, murach sin, go mbainfí an
tiománaí dúchasach SQLite agus an ceangal le fáinne eochracha an chórais oibriúcháin go ciúin. Chun réamhamharc a fháil ar an ordú beacht gan é a chur i bhfeidhm:

```bash
omniroute update --dry-run
# [RITH TIRIM] Rithfí: npm install -g omniroute@latest --include=optional
```

Bratacha eile `omniroute update` (fíoraithe san fhoinse): `--check` (scoir le 1 má tá sé
as dáta), `--apply` (suiteáil gan leid), `--changelog`, `--no-backup`,
`--yes`.

---

## Google Gemini CLI trí `omniroute run gemini`

Conradh fíoraithe i gcoinne `@google/gemini-cli` 0.50.0: cloíonn an CLI le
`GOOGLE_GEMINI_BASE_URL` agus eisíonn sé `POST /v1beta/models/<model>:generateContent`
(agus `:streamGenerateContent?alt=sse`) chuige — dromchla dúchasach
Gemini OmniRoute (`/v1beta`) go díreach. Ceanglaíonn `omniroute run gemini` é sin go huathoibríoch:

- `GOOGLE_GEMINI_BASE_URL` → bun-URL gníomhach OmniRoute (fréamh, gan `/v1`);
- `GEMINI_API_KEY` → dintiúr OmniRoute a réitíodh (rogha/timpeallacht/comhthéacs);
- **`GEMINI_CLI_HOME` sealadach leithlisithe** a roghnaíonn a `.gemini/settings.json`
  fíordheimhniú `gemini-api-key`, ionas nach sáróidh seisiún stóráilte Google OAuth (Code Assist)
  an lainseáil atá dírithe ar OmniRoute choíche — baintear é tar éis scoir;
- **sláinteachas timpeallachta**: glantar `GOOGLE_API_KEY`,
  `GOOGLE_GENAI_USE_VERTEXAI` agus `GOOGLE_GENAI_USE_GCA` de thimpeallacht an phróisis linbh
  (a dhéanfadh fíordheimhniú a atreorú chuig Vertex/Code Assist), agus socraítear
  `GEMINI_DEFAULT_AUTH_TYPE=gemini-api-key` mar chúltaca breise — cuirtear an
  chóireáil chéanna ar na spriocanna `run` eile dá n-athróga contrártha féin;
- instealladh `--model <id>` ó `--provider`/`--model`.

```bash
omniroute run gemini --model glm/glm-5.2 -- --skip-trust -p "hello"
```

Tá cosaint iontaoibhe spáis oibre Gemini fós i bhfeidhm sa mhód gan chomhéadan — cuir
`--skip-trust` ar aghaidh (nó cuir muinín san eolaire go hidirghníomhach) tú féin; ní
sheachnaíonn an lainseálaí í d’aon ghnó. Tá an lainseálaí seo éagsúil leis an **gclárú
ACP** (`src/lib/acp/registry.ts`, `gemini --acp`), arb é fós an
comhtháthú prótacail gníomhaire do `/dashboard/acp-agents`.

---

## Fíor-scuabadh tástála deataigh (roghnach)

Ritear aischéimniú cinntitheach an phlean seolta in CI (`tests/unit/cli/run-command.test.ts`,
`tests/unit/cli/run-execution.test.ts`). Chun na FÍOR-dhénártha a bhailíochtú i gcoinne FÍORfhreastalaí
OmniRoute, tá creat tástála roghnach ar fáil ag
`tests/integration/upstream-cli-smoke.int.test.ts`. Ní ritear go huathoibríoch riamh é
(déantar gach fotheagmháil tástála a scipeáil mura bhfuil `RUN_CLI_SMOKE=1` ann), tugann sé ainm na hathróige timpeallachta
don dintiúr (ní an luach riamh), folaíonn sé teaghráin atá cosúil le heochracha ó aon aschur taifeadta, scipeálann sé
spriocanna nach bhfuil a ndénártha suiteáilte, agus aicmíonn sé teipeanna mar
fhíordheimhniú / réamhtheachta / cumraíocht seachas mar luach Boole lom:

```bash
RUN_CLI_SMOKE=1 \
OMNIROUTE_SMOKE_BASE_URL="http://localhost:20128" \
OMNIROUTE_SMOKE_MODEL="<provider/model>" \
OMNIROUTE_SMOKE_API_KEY_ENV="OMNIROUTE_API_KEY" \
node --import tsx/esm --test tests/integration/upstream-cli-smoke.int.test.ts
```

Roghnach: cuireann `OMNIROUTE_SMOKE_TARGETS="codex,opencode,qwen"` srian leis an scuabadh;
sáraíonn `OMNIROUTE_SMOKE_TIMEOUT_MS` an teorainn ama 120s in aghaidh na sprice.

---

## Féach freisin

- [Cumraíocht Claude Code](./CLAUDE-CODE-CONFIGURATION.md) — an treoir níos doimhne do Claude Code
- [Cumraíocht Codex CLI](./CODEX-CLI-CONFIGURATION.md) — an bunsocrú aonuaire `[model_providers.omniroute]`
- [Mód Cianda](./REMOTE-MODE.md) — comhthéacsanna, comharthaí rochtana scóipithe, agus cianfhreastalaí a rialú
- [Tagairt Uirlisí CLI](../reference/CLI-TOOLS.md) — catalóg iomlán na n-uirlisí tacaithe + leathanaigh an deais
- [Treoir Socraithe](./SETUP_GUIDE.md) — modhanna suiteála agus ionduchtú don chéad rith
