# open-sse Architecture (Українська)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Коротко**: `open-sse/` — це основний рушій потокового передавання, який обробляє кожен запит до LLM в OmniRoute. Він містить близько 1 400 файлів, що реалізують конвеєр запитів, виконавці, сервіси, MCP-сервер і шар перетворення форматів. У цьому посібнику пояснюється, як ці компоненти взаємодіють.

**Джерело:** `open-sse/` (пакет робочого простору, близько 1 440 нетестових файлів `.ts`)

---

## Навіщо окремий пакет робочого простору?

`open-sse/` — це **автономний робочий простір** у монорепозиторії OmniRoute з кількох причин:

1. **Повторне використання** — `open-sse` публікується в npm як `@omniroute/open-sse`, тому інші проєкти можуть використовувати його незалежно
2. **Чіткі межі** — рушій потокового передавання відокремлений від специфічного для OmniRoute шару UI/БД
3. **Продуктивність** — рушій не має залежностей від Next.js, що забезпечує швидший холодний запуск у середовищах CLI/serverless
4. **Керування версіями** — `open-sse` може випускати релізи за власним графіком

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Структура верхнього рівня

```
open-sse/
├── index.ts              # Публічна точка входу
├── types.d.ts            # Експорти публічних типів
├── package.json          # @omniroute/open-sse
├── config/               # Конфігурації провайдерів, константи, реєстри
├── executors/            # HTTP-виконавці для окремих провайдерів (67 + base.ts/index.ts)
├── handlers/             # Обробники запитів (chatCore, responses тощо)
├── lib/                  # Внутрішні утиліти
├── mcp-server/           # Сервер Model Context Protocol
├── services/             # Близько 298 сервісних модулів
├── transformer/          # Перетворювач формату Responses API
├── translator/           # Перетворення форматів (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Спільні утиліти (журналювання, помилки, потоки тощо)
```

### Кількість модулів

| Каталог | Файли | Призначення |
| `executors/` | 167 | HTTP-виконавці для окремих провайдерів (уніфіковані через фабрику DefaultExecutor) |
| `handlers/` | 157 | Точки входу запитів (chatCore, responses, embeddings) |
| `services/` | ~536 | Маршрутизація, кешування, обмеження частоти запитів, оновлення тощо |
| `translator/` | 56 | Перетворення форматів (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Інструменти й транспорти MCP |
| `utils/` | ~108 | Наскрізні утиліти (журналювання, помилки, потоки) |
| `config/` | ~339 | Конфігурації провайдерів, константи, реєстри |

---

## Конвеєр запитів

Кожен запит до LLM проходить через **5-етапний конвеєр**:

```
                              ┌──────────────┐
   HTTP-запит                  │ 1. МАРШРУТ   │   визначення комбінації, вибір моделі
   (маршрут Next.js)           └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 2. ПЕРЕКЛАД  │   перетворення формату (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 3. ВИКОНАННЯ │   виконавець провайдера, HTTP, повторні спроби, запобіжник
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 4. ПОТІК     │   перетворення SSE, зворотний тиск
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │ 5. ЗАПИС     │   відстеження використання, журнал викликів, класифікація помилок
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP-відповідь (SSE або JSON)
```

### Етап 1: Маршрутизація (services/combo.ts)

**Точка входу**: `handleComboChat()` у `services/combo.ts`

Зіставляє запит із конкретним кортежем `(provider, model, account, credentials)`:

- Знаходить комбінацію за ID (або створює віртуальну комбінацію для моделей `auto/*`)
- Застосовує стратегію маршрутизації (пріоритетну, зважену, циклічну тощо)
- Відфільтровує непрацездатних провайдерів (автоматичний запобіжник)
- Вибирає наступну придатну ціль

Для моделей `auto/*` цей етап також:

- Запускає алгоритм **16-факторного оцінювання** (`services/autoCombo/`)
- Вибирає пару `provider+model` на основі працездатності, вартості, затримки тощо

### Етап 2: Перетворення формату (translator/)

Якщо вихідний формат (наприклад, OpenAI) відрізняється від цільового (наприклад, Claude), запит **перетворюється**:

- Системний промпт → системне повідомлення
- Визначення інструментів → специфічний для провайдера формат інструментів
- Параметри міркування/мислення → специфічні для провайдера еквіваленти
- Нормалізація ролей повідомлень (`developer` → `system` для форматів, відмінних від OpenAI)

`translator/index.ts` експортує:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Етап 3: Виконання (executors/)

**Точка входу**: `getExecutor(providerId).execute(request, options)`

Усі провайдери використовують `DefaultExecutor` (`executors/default.ts`) через резервний механізм фабрики `getExecutor()`. Виконавець:

- Створює URL вищого рівня (`buildUrl()`)
- Додає специфічні для провайдера заголовки (`buildHeaders()`)
- Перетворює тіло запиту (`transformRequest()`)
- Надсилає HTTP-запит із повторними спробами та експоненційною затримкою
- За потреби обробляє оновлення автентифікації (для провайдерів OAuth)

Усі виконавці розширюють `BaseExecutor` (`executors/base.ts`, 1170 рядків коду), який надає:

- Спільну логіку повторних спроб
- Інтеграцію з проксі
- Інтеграцію з автоматичним запобіжником
- Хуки для запису використання

### Етап 4: Потокове передавання (utils/stream.ts)

Для потокових відповідей виконавець повертає **ReadableStream**. Обробник:

- Передає дані через SSE-перетворення (`createSSETransformStreamWithLogger`)
- Застосовує контрольні сигнали heartbeat для виявлення розірваних з’єднань
- Коректно обробляє відключення клієнта (`pipeWithDisconnect`)
- Перетворює SSE → JSON для клієнтів без підтримки потокового передавання

Для непотокових відповідей виконавець повертає розібраний JSON-об’єкт, який передається без змін.

### Етап 5: Запис (services/usage.ts)

Після відповіді (успішної чи невдалої) використання записується:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` із відповіді
- `cost_usd`, обчислена на основі даних про ціни
- `latency_ms`, `status`, `error_class` у разі помилки
- Зберігається в таблиці `usage_history`

Артефакти журналу викликів (якщо ввімкнено) записуються до `${DATA_DIR}/call_logs/`.

---

## Детальний огляд ключових файлів

### chatCore.ts (5977 рядків)

**Основний обробник запитів**. Попри свій розмір, він має чітку структуру:

```ts
// Псевдоструктура chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Автентифікація + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Валідація тіла
  const body = await parseRequestBody(request);

  // 3. Визначення формату + перетворення
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Маршрутизація комбінації
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Перейти до наступної цілі
    }
  }

  // 5. Аварійний резервний варіант
  return await emergencyFallback(body);
}
```

Попри те, що це одна гігантська функція, її організовано в **прокоментовані секції**, які відповідають 5-етапному конвеєру.

### combo.ts (4456 рядків коду)

**Рушій маршрутизації**, який перетворює комбінацію на впорядкований список цілей.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Підтримує **19 стратегій маршрутизації** (див. `src/shared/constants/routingStrategies.ts`):

| Стратегія           | Поведінка                                                                          |
| ------------------- | ---------------------------------------------------------------------------------- |
| `priority`          | Упорядкований список із першою пріоритетною ціллю                                  |
| `weighted`          | Імовірнісний вибір за вагою кожної цілі                                            |
| `round-robin`       | Циклічний перебір цілей за порядком                                                |
| `context-relay`     | Передавання контексту між цілями                                                   |
| `fill-first`        | Заповнення квоти перед переходом до наступної цілі                                 |
| `p2c`               | Вибір із двох варіантів                                                            |
| `random`            | Рівномірний випадковий вибір                                                       |
| `least-used`        | Вибір цілі з найменшою кількістю нещодавніх використань                            |
| `cost-optimized`    | Спочатку найдешевша справна ціль                                                   |
| `reset-aware`       | Урахування вікон скидання постачальника                                            |
| `reset-window`      | Маршрутизація на основі вікна скидання                                             |
| `headroom`          | Спочатку ціль із найбільшим залишковим запасом квоти                               |
| `strict-random`     | Справді рівномірний вибір (без зважування за якістю)                               |
| `auto`              | Використання 16-факторного оцінювання (`autoCombo/`)                               |
| `lkgp`              | Спочатку останній відомий справний постачальник                                    |
| `context-optimized` | Найкращий варіант для запитів із довгим контекстом                                 |
| `fusion`            | Паралельне розподілення між панеллю з подальшим синтезом через суддю (`fusion.ts`) |

### base.ts (1170 рядків коду)

**Абстрактний виконавець**, який розширюють усі 107 виконавців. Він містить:

- `buildUrl()` — побудова URL за замовчуванням (підкласи перевизначають її для особливих випадків)
- `buildHeaders()` — заголовки за замовчуванням (автентифікація, тип вмісту)
- `transformRequest()` — за замовчуванням передає дані без змін
- `execute()` — основний цикл HTTP із повторними спробами, відкладенням і запобіжником

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Обробляє всіх постачальників, сумісних з OpenAI/Anthropic
  // Постачальники реєструють конфігурації (URL, автентифікацію, заголовки), але використовують спільну логіку виконавця
}
```

Специфічну для постачальника поведінку (заголовки автентифікації, базовий URL, заголовки версій) налаштовують через реєстр постачальників, а не через окремі класи виконавців.

````

---

## Сервіси (117 модулів)

Сервіси — це **спеціалізовані модулі з єдиним призначенням**, які компонують обробники. Основні категорії:

### Маршрутизація та Combo

- `combo.ts` — точка входу для запитів із combo-маршрутизацією
- `services/autoCombo/` — оцінювання за 16 факторами, 8 стратегій автоматичної маршрутизації
- `wildcardRouter.ts` — зіставляє маршрути з символами підстановки (`gpt-*`)
- `modelFamilyFallback.ts` — резервне перемикання в межах сімейства T5

### Обмеження частоти запитів і квоти

- `rateLimitManager.ts` — кошик токенів для кожної комбінації ключа й провайдера
- `usage.ts` — реєстрація використання
- `quotaCache.ts` — знімки квот у пам’яті

### Обліковий запис і токен

- `tokenRefresh.ts` — оновлення OAuth у разі 401
- `accountFallback.ts` — перемикання на альтернативний обліковий запис
- `sessionManager.ts` — стан багатокрокового сеансу

### Інтелектуальні функції

- `intentClassifier.ts` — класифікація наміру запиту
- `taskAwareRouter.ts` — маршрутизація за типом завдання
- `thinkingBudget.ts` — виділення токенів для міркування
- `contextManager.ts` — додавання контексту маршрутизації

### Відмовостійкість

- `resilience.ts` — оркестрація повторних спроб, затримок і запобіжника
- `emergencyFallback.ts` — резервний варіант на крайній випадок
- `modelDeprecation.ts` — автоматична маршрутизація до моделей-наступників

### Стан

- `signatureCache.ts` — дедуплікація за сигнатурою запиту
- `volumeDetector.ts` — скидання навантаження
- `contextHandoff.ts` — серіалізація сеансу

### Стиснення

- `compression/` (підкаталог) — повний конвеєр стиснення
- 39 файлів, що охоплюють рушії, набори правил та адаптери

### Навички

- (описано в [SKILLS.md](./SKILLS.md))

### Пам’ять

- (описано в [MEMORY.md](./MEMORY.md))

---

## Виконавці (75+ файлів)

По одному файлу на кожного провайдера. Усі вони розширюють `BaseExecutor` і перевизначають те, що відрізняється.

### Загальні шаблони

Провайдери визначаються через `getExecutor(providerId)`, що повертає налаштований виконавець. Провайдери, сумісні з OpenAI/Anthropic, використовують `DefaultExecutor` (`executors/default.ts`). Специфічна для провайдера поведінка (базова URL-адреса, заголовки автентифікації, версія API) налаштовується в `open-sse/config/providers/`, тоді як перетворення тіла запиту виконуються в `open-sse/translator/`.

**Власна URL-адреса** задається через конфігурацію провайдера:

```ts
// Конфігурація провайдера в open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Власна автентифікація** обробляється через конфігурацію автентифікації в реєстрі провайдерів (ключ API, OAuth, профілі заголовків).

**Власні перетворення тіла запиту** (наприклад, відокремлення Anthropic поля `system` від `messages`) реєструються окремо для кожного провайдера в `open-sse/translator/`.

````

### Фабрика виконавців

`executors/index.ts` експортує `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Визначення виконавця відбувається через `ExecutorRegistry` (`executors/registry.ts`): кожен спеціалізований виконавець оголошується у вбудованій таблиці `executors/index.ts` і реєструється за допомогою `registerExecutor(alias, instance)` під час завантаження модуля; `getExecutor()` звертається до реєстру та використовує мемоізований `DefaultExecutor` для будь-якого провайдера без спеціалізованого запису. Повне зіставлення псевдонім → виконавець описується еталонним тестом `tests/unit/executor-map-golden.test.ts`.

---

## Перекладачі

Перетворюють дані між **3 форматами**: OpenAI, Anthropic, Gemini, а також новим Responses API.

### Коли виконується перетворення

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Поширені перетворення:

- `OpenAI → Anthropic`: окреме поле `system`, заголовок `x-api-key`
- `OpenAI → Gemini`: `contents` замість `messages`, `systemInstruction`
- `OpenAI → Responses API`: масив `input`, стан `previous_response_id`

### Оброблювані крайові випадки

- Роль `developer` → `system` для форматів, відмінних від OpenAI
- Роль `system` → об’єднується з першим повідомленням користувача для GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` у Gemini
- `tools` → формат інструментів, специфічний для постачальника
- Параметри міркування (o1, Claude) → еквіваленти, специфічні для постачальника

---

## Сервер MCP

`open-sse/mcp-server/` реалізує сервер **Model Context Protocol**:

- **110 інструментів** (керування постачальниками, комбінації, пам’ять, кеш, стиснення, проксі, навички, гейміфікація, плагіни, Notion, Obsidian, локальний корпус)
- **3 транспорти**: stdio, SSE, Streamable HTTP
- **33 області доступу** для детальної авторизації

### Реєстрація інструментів

Інструменти реєструються як окремі файли в `open-sse/mcp-server/tools/`, кожен із яких експортує назву, схему, обробник і область доступу:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Транспорти

```ts
// stdio (використання через CLI)
startMcpStdio(server);

// SSE (потокове передавання на основі HTTP)
startMcpSse(server, port);

// Streamable HTTP (сучасний MCP)
startMcpStreamable(server, port);
```

### Авторизація

Кожен виклик інструмента проходить перевірку областей доступу (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Трансформери

`open-sse/transformer/` виконує перетворення між форматами **Chat Completions** і **Responses API**.

### Навіщо потрібен окремий трансформер?

Responses API — це новий формат OpenAI з **діалогами зі збереженням стану** (`previous_response_id`). Коли клієнт надсилає запит Responses, OmniRoute:

1. Внутрішньо перетворює Responses → Chat Completions
2. Надсилає запит постачальнику (будь-якому постачальнику, що підтримує Chat Completions)
3. Перетворює відповідь назад у формат Responses
4. Потоково передає перетворену відповідь клієнту

Трансформер (`transformer/responsesTransformer.ts`) надає:

```ts
createResponsesApiTransformStream(): TransformStream
```

Він обробляє:

- Події `response.output_item.added`
- Події `response.output_text.delta`
- Подію `response.completed`
- Зіставлення викликів інструментів (`function_call` ↔ `tool_calls`)

---

## Конфігурація

`open-sse/config/` містить рівень конфігурації:

| Файл                          | Призначення                                                |
| ----------------------------- | ---------------------------------------------------------- |
| `providerRegistry.ts`         | Реєстр чат-моделей на основі каталогу з 352 постачальників |
| `providerModels.ts`           | Псевдоніми моделей, зіставлення форматів                   |
| `constants.ts`                | Тайм-аути, обмеження, коди стану                           |
| `defaultThinkingSignature.ts` | Стандартний підпис міркування Claude                       |
| `modelStrip.ts` (у сервісах)  | Видалення полів окремо для кожного постачальника           |

### Схема реєстру постачальників

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Перевірка Zod під час завантаження модуля гарантує дійсність усіх конфігурацій постачальників.

---

## Обмеження продуктивності

Механізм маршрутизації має суворі бюджети продуктивності:

| Операція                                                            | Цільове значення | Вимірювання                  |
| ------------------------------------------------------------------- | ---------------- | ---------------------------- |
| Визначення комбінації                                               | <10ms            | Для 50 цільових об’єктів     |
| Перевірка обмеження частоти запитів                                 | <1ms             | Кошик токенів у пам’яті      |
| Резервний вибір сімейства моделей                                   | <5ms             | Кешовані визначення сімейств |
| Диспетчеризація маршрутизації запиту                                | <2ms             | Критичний шлях               |
| **Без блокувального вводу-виводу в критичному шляху маршрутизації** | —                | Усе асинхронно               |

---

## Антипатерни

❌ **Синхронні виклики БД у `combo.ts`** — попередньо обчислюйте та кешуйте
❌ **Логіка повторних спроб в обробниках** — використовуйте `retry()` із сервісу відмовостійкості
❌ **Прямий доступ до конфігурації провайдера** — використовуйте гетери `providerRegistry`
❌ **Жорстко закодовані ланцюжки резервного вибору** — визначайте їх у `modelFamilyFallback.ts`
❌ **Зміни стану між паралельними запитами** — використовуйте лише контекст у межах запиту

---

## Додавання нового компонента

### Додавання нового сервісу

1. Створіть `open-sse/services/[serviceName].ts` із чітко визначеною відповідальністю
2. Експортуйте основну функцію-обробник і всі константи
3. Додайте модульні тести в `tests/unit/services/[serviceName].test.mjs`
4. Інтегруйте в конвеєр обробки запитів у `handlers/chatCore.ts` (якщо пов’язано з маршрутизацією)
5. Оновіть логіку маршрутизації в `combo.ts`, якщо сервіс впливає на вибір цільового об’єкта
6. Задокументуйте в цьому файлі

### Додавання нового виконавця

1. Створіть `open-sse/executors/[provider].ts`, розширивши `BaseExecutor`
2. Зареєструйте в `config/providerRegistry.ts`
3. Додайте до фабрики в `executors/index.ts`
4. Додайте модульні тести для виконавця
5. Задокументуйте в `docs/architecture/ARCHITECTURE.md`

### Додавання нового інструмента MCP

1. Створіть або оновіть `open-sse/mcp-server/tools/[category]Tools.ts`
2. Визначте схему Zod для вхідних даних
3. Зареєструйте інструмент у `mcp-server/index.ts`
4. Додайте до матриці областей доступу в `mcp-server/auth/`
5. Додайте модульні тести

---

## Дивіться також

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — високорівнева архітектура
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — інженерний довідник
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — опис кожного каталогу
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — оцінювання за 16 факторами
- [MCP-SERVER.md](./MCP-SERVER.md) — сервер MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — сервер A2A
- Джерело: `open-sse/` (понад 400 файлів, ~143K рядків коду)
