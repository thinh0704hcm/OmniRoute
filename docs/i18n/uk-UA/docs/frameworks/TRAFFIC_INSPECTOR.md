# Traffic Inspector (Українська)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇹 [am](../../../am/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇿 [az](../../../az/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇰 [da](../../../da/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇪 [de](../../../de/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇷 [el](../../../el/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇸 [es](../../../es/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇪 [et](../../../et/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇱 [he](../../../he/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇩 [id](../../../id/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇹 [it](../../../it/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇭 [km](../../../km/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇲 [my](../../../my/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇴 [no](../../../no/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [or](../../../or/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇰 [si](../../../si/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [te](../../../te/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇭 [th](../../../th/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/TRAFFIC_INSPECTOR.md)

---

Traffic Inspector — це вбудований в OmniRoute засіб налагодження HTTPS-трафіку, подібний до Charles Proxy / mitmweb / HTTP Toolkit, який **враховує особливості LLM** та **агентів**. Він доступний за адресою `/dashboard/tools/traffic-inspector` і отримує трафік у реальному часі щонайбільше з 5 одночасних джерел захоплення.

**Розташування в панелі керування:** `/dashboard/tools/traffic-inspector`
**Група на бічній панелі:** Інструменти (після AgentBridge)
**Див. також:** [`AGENTBRIDGE.md`](./AGENTBRIDGE.md) — AgentBridge є режимом захоплення 1.

---

## §1 Огляд

### У чому унікальність Traffic Inspector

| Функція                                                                            | mitmweb  | Charles | Fiddler  | **OmniRoute Traffic Inspector** |
| ---------------------------------------------------------------------------------- | :------: | :-----: | :------: | :-----------------------------: |
| Вебінтерфейс                                                                       |    ✓     |    ✗    |    ✗     |                ✓                |
| Відкритий вихідний код                                                             |    ✓     |    ✗    | частково |                ✓                |
| **Врахування агентів** (визначає, чи запит надійшов від Antigravity/Copilot тощо)  |    ✗     |    ✗    |    ✗     |                ✓                |
| **Врахування LLM** (аналізує формат OpenAI/Anthropic/Gemini, токени та модель)     |    ✗     |    ✗    |    ✗     |                ✓                |
| **Видиме зіставлення моделей** (gemini-3-flash → claude-sonnet-4.7)                |    ✗     |    ✗    |    ✗     |                ✓                |
| **Розподіл затримки між проксі та upstream-сервером**                              | частково |    ✗    |    ✗     |                ✓                |
| **Інтеграція з маршрутизацією, резервуванням і розрахунком вартості OmniRoute**    |    ✗     |    ✗    |    ✗     |                ✓                |
| **Загальносистемне налагодження проксі** (для будь-якого застосунку на комп’ютері) |    ✓     |    ✓    |    ✓     |                ✓                |
| **Захоплення власних хостів** (перенаправлення DNS для кожного хоста)              |    ✓     |    ✓    |    ✓     |                ✓                |
| **Режим змінної середовища HTTP_PROXY**                                            |    ✓     |    ✓    |    ✓     |                ✓                |
| **Перегляд розмови** (багатораундові повідомлення, tool_use/tool_result)           |    ✗     |    ✗    |    ✗     |                ✓                |
| **Об’єднання потоку SSE** (відновлення з дельта-подій)                             |    ✗     |    ✗    |    ✗     |                ✓                |
| **Запис сеансів** (іменовані, з можливістю експорту у .har/.jsonl)                 |    ✗     |    ✓    |    ✓     |                ✓                |

### Архітектура одним абзацом

`TrafficBuffer` (`src/mitm/inspector/buffer.ts`) — це спільний кільцевий буфер у пам’яті (за замовчуванням 1000 записів, налаштовується через `INSPECTOR_BUFFER_SIZE`). Усі джерела захоплення записують до нього через `push()`. Клас буфера класифікує кожен запис за допомогою `kindDetector.ts` (визначає, чи є він запитом до LLM), обчислює `contextKey` (відбиток системного промпту на основі SHA-256) і транслює дані всім WebSocket-підписникам через `globalTrafficBuffer.subscribe()`. Панель керування підключається через `GET /api/tools/traffic-inspector/ws` і після підключення отримує знімок стану, а потім події `new`/`update`/`clear`.

---

## §2 Режими перехоплення

Traffic Inspector підтримує **5 одночасних джерел перехоплення**. Кожне з них можна вмикати та вимикати незалежно. Поле `source` у кожному `InterceptedRequest` (`src/mitm/inspector/types.ts`) має одне зі значень: `"agent-bridge"`, `"custom-host"`, `"http-proxy"`, `"system-proxy"` або `"tproxy"`.

### Режим 1 — AgentBridge (за замовчуванням, завжди ввімкнений)

**Джерело:** обробники AgentBridge (`src/mitm/handlers/base.ts`)
**Механізм:** кожен виклик `intercept()` у `MitmHandlerBase` викликає `hookBufferStart()` перед переспрямуванням і `hookBufferUpdate()` після завершення. Додаткове налаштування не потрібне — працює відразу після запуску AgentBridge.
**Охоплення:** 9 IDE-агентів, налаштованих в AgentBridge
**Примітка:** поле `source` в `InterceptedRequest` = `"agent-bridge"`

### Режим 2 — Власні хости (переспрямування DNS)

**Джерело:** визначений користувачем список хостів (таблиця `inspector_custom_hosts`)
**Механізм:** додавання хоста через інтерфейс додає `127.0.0.1 <host>` до `/etc/hosts` (потрібен sudo). Наявний MITM-сервер AgentBridge (порт 443) динамічно генерує SNI-сертифікат для нового хоста.
**Охоплення:** будь-яка програма, що використовує доданий хост — змінювати конфігурацію програми не потрібно
**Примітка:** `source` = `"custom-host"`

Приклади використання:

- Моніторинг `api.openai.com` зі скриптів Python
- Налагодження `my-internal-llm.company.com`
- Перехоплення трафіку з мобільних пристроїв у тій самій мережі (через ARP-спуфінг — для досвідчених користувачів)

### Режим 3 — Прослуховувач HTTP_PROXY (порт 8080)

**Джерело:** програми, що використовують змінні середовища `HTTP_PROXY`/`HTTPS_PROXY`
**Механізм:** додатковий прослуховувач на порту 8080 (`src/mitm/inspector/httpProxyServer.ts`), який працює як стандартний явний HTTP/HTTPS-проксі. Приймає тунелі `CONNECT` (HTTPS) і прямі HTTP-запити.
**Охоплення:** будь-яка програма, що враховує змінну середовища `HTTP_PROXY` — без зміни DNS і без sudo
**Примітка:** `source` = `"http-proxy"`

```bash
# Швидке перехоплення для однієї команди:
HTTPS_PROXY=http://127.0.0.1:8080 curl https://api.openai.com/v1/models

# Постійне перехоплення протягом сеансу оболонки:
export HTTP_PROXY=http://127.0.0.1:8080
export HTTPS_PROXY=http://127.0.0.1:8080
```

**Обмеження TLS:** тунелі HTTPS `CONNECT` за замовчуванням перехоплюються лише як метадані (хост, порт, часові показники) — тіло TLS не розшифровується. Увімкніть перемикач «Розшифровувати HTTPS у режимі проксі» (необов’язково; сертифікат AgentBridge має бути довіреним), щоб повністю перевіряти тіло запиту.

**Конфлікт портів:** якщо порт 8080 використовується, AgentBridge повертає помилку 409 зі структурованими даними. Змініть порт за допомогою змінної середовища `INSPECTOR_HTTP_PROXY_PORT`.

### Режим 4 — Загальносистемний проксі (розширений, необов’язковий)

**Джерело:** налаштування проксі на рівні ОС (застосовуються до всіх програм на комп’ютері)
**Механізм:** використовує API операційної системи для переспрямування всього HTTP/HTTPS-трафіку через прослуховувач HTTP_PROXY:

- **macOS:** `networksetup -setwebproxy / -setsecurewebproxy`
- **Linux:** `gsettings set org.gnome.system.proxy` + `/etc/environment`
- **Windows:** `netsh winhttp set proxy 127.0.0.1:8080`
  **Охоплення:** кожна програма на комп’ютері, що враховує системні налаштування проксі
  **Примітка:** `source` = `"system-proxy"`

**Механізми безпеки:**

- Таймер автоматичного вимкнення (за замовчуванням 30 хв, налаштовується через `INSPECTOR_SYSTEM_PROXY_GUARD_MINUTES`)
- Попередній стан системного проксі зберігається в БД і відновлюється під час скасування
- Якщо користувач переходить з активної сторінки, панель керування показує запит «Відновлення системного проксі»
- Інтерфейс показує значок `⚠ Розширений` і явний прапорець підтвердження

### Режим 5 — Прозоре розшифрування TPROXY (Linux, root, необов’язкове)

**Джерело:** TPROXY ядра + маршрутизація на основі політик (`src/mitm/tproxy/`)
**Механізм:** позначає нові локальні вихідні TCP-з’єднання з цільовим портом (за замовчуванням `443`) у `mangle OUTPUT`; правило `ip rule` переспрямовує позначені пакети на локальну доставку, а ціль `TPROXY` у `mangle PREROUTING` передає їх прозорому прослуховувачу (**IP_TRANSPARENT**) (порт за замовчуванням `8443`). Прослуховувач завершує TLS за допомогою кінцевого сертифіката, який динамічний центр сертифікації випускає **на вимогу окремо для кожного імені хоста SNI**, перехоплює розшифрований обмін і пересилає повторно зашифрований запит до початкового призначення.
**Охоплення:** **довільні** хости призначення на цільовому порту — без підміни `/etc/hosts`, змінної середовища `HTTP_PROXY` або зміни загальносистемного проксі. Конфігурацію перехоплюваного процесу змінювати не потрібно, але він має довіряти динамічному центру сертифікації.
**Примітка:** `source` = `"tproxy"`

**Вимоги:** лише Linux (**IP_TRANSPARENT** доступний тільки в Linux), можливість **CAP_NET_ADMIN** (root) і нативний додаток N-API, який потрібно зібрати за допомогою інструментарію C (`npm run build:native:tproxy`). Якщо потрібні компоненти недоступні, перемикач на панелі керування вимкнено з підказкою «Для розшифрування TPROXY потрібні Linux + root + нативний додаток». Правила брандмауера застосовуються та скасовуються транзакційно (аварійне завершення роботи ніколи не залишає правило `mangle`) і очищуються під час перезавантаження. Механізм запобігання зацикленню на основі SO_MARK не допускає повторного перехоплення власного повторно зашифрованого пересилання проксі.

Це значна підсистема з окремим спеціалізованим посібником для операторів — повний рецепт налаштування брандмауера, відомості про динамічний центр сертифікації для кожного SNI та інсталятор сховища довіри, локальний маршрут, механізм запобігання зацикленню й схему конфігурації див. у `docs/security/MITM-TPROXY-DECRYPT.md` (git; не компілюється до `/docs`). Перемикач керується через `GET / POST / DELETE /api/tools/agent-bridge/tproxy` (примітка: маршрут розташований під префіксом AgentBridge, а не Traffic Inspector).

### Порівняння режимів перехоплення

| Режим                   | Налаштування                         |             Sudo?             | Охоплення                                      | Примітки                                                                                                                             |
| ----------------------- | ------------------------------------ | :---------------------------: | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 1. AgentBridge          | Автоматичне                          | Один раз (сертифікат + hosts) | 9 агентів IDE                                  | Увімкнено за замовчуванням                                                                                                           |
| 2. Власні хости         | Введення для кожного хоста           |       Так (файл hosts)        | Будь-який застосунок, що використовує цей хост | Зберігається в БД                                                                                                                    |
| 3. HTTP_PROXY           | `export HTTPS_PROXY=...`             |              Ні               | Застосунки, що враховують змінні середовища    | Порт 8080, без розшифрування TLS за замовчуванням                                                                                    |
| 4. Загальносистемний    | Перемикач + підтвердження            |              Так              | Усі застосунки на комп’ютері                   | Автоматичне вимкнення через 30 хв                                                                                                    |
| 5. Розшифрування TPROXY | Перемикач (Linux + нативний додаток) | Так (root + встановлення CA)  | Будь-який хост на цільовому порту              | Розшифровує довільні хости; вимкнено за замовчуванням — див. `docs/security/MITM-TPROXY-DECRYPT.md` (git; не компілюється в `/docs`) |

---

## §3 Інтерфейс

### 3.1 Компонування

```
┌─ Інспектор трафіку ────────────────────────────────────────────────────┐
│ ┌─ Панель джерел перехоплення ─────────────────────────────────────┐   │
│ │ [✓ AgentBridge] [✓ Власні хости (3)] [○ HTTP_PROXY] [○ Система] │   │
│ └─────────────────────────────────────────────────────────────────────┘  │
│ ┌─ Панель фільтрів і керування ────────────────────────────────────┐   │
│ │ Профіль: (●) Лише LLM  (○) Власний  (○) Усі                      │   │
│ │ [⎉ Пауза] [🗑 Очистити] [⬇ .har] [● ЗАПИС сеансу] ● наживо 482/1k│   │
│ └─────────────────────────────────────────────────────────────────────┘  │
├══◀▶══════════════════════════════╬══════════════════════════════════════╤╡
│ СПИСОК ЗАПИТІВ (змінний розмір)  ║ ПАНЕЛЬ ВІДОМОСТЕЙ                   ▲ │
│ ────────────────────────────── │ ║ [Розмова][Заголовки][Запит]         │ │
│ ▎ 14:32 POST 200 12k AG openai ║ [Відповідь][Час][LLM][Статистика]   │ │
│ ▎ 14:31 POST 200 8k  CP openai ║                                     ▼ │
│ ▎ 14:31 POST 503 ⚠   KR ...   ║                                       │
│ ▎ 14:30 GET  200 3k  🌐 власний ║                                      │
└══════════════════════════════════╝══════════════════════════════════════╝
```

### 3.2 Список запитів (ліва панель)

- **Віртуалізований** (`useVirtualList` + `ResizeObserver`): опрацьовує 1000 елементів без зависання
- **Автопрокручування** з перемикачем для призупинення під час перегляду
- **Кольорове кодування статусу**: зелений (2xx), жовтий (3xx), червоний (4xx/5xx), сірий (виконується)
- **Емодзі агента**: 🔵 Antigravity, 🟢 Copilot, 🟠 Kiro, 🟣 Codex, 🔷 Cursor, 🟤 Zed, 🟡 Claude Code, ⚫ Open Code, 🌐 власний хост
- **Кольорова смуга контексту**: ліва межа завтовшки 1px, забарвлена за `contextKey` (SHA-256 системної підказки), — візуально групує пов’язані розмови
- **Ліниве завантаження тіла**: у вкладках із відомостями матеріалізується лише тіло вибраного запиту (щоб уникнути рендерингу 1000 тіл по 1MB)

### 3.3 Панель відомостей — 7 вкладок

| Вкладка           | Вміст                                                                                                 | Примітки                                                                                            |
| ----------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| **Розмова**       | Бульбашки багатораундового чату (system/user/assistant + tool_use/tool_result)                        | Нормалізовано з формату будь-якого провайдера; показується лише для `detectedKind === "llm"`        |
| **Заголовки**     | Таблиці заголовків запиту та відповіді                                                                | Чутливі заголовки (Authorization, Cookie, api-key) типово замасковані; перемикач «Показати секрети» |
| **Запит**         | Необроблене тіло, деревоподібне подання JSON, мітка поля моделі                                       | Форматований JSON або необроблений текст                                                            |
| **Відповідь**     | Необроблене тіло або список подій SSE; перемикач «Необроблене ↔ Об’єднане»                            | Засіб об’єднання SSE відновлює остаточне повідомлення з дельта-подій                                |
| **Час**           | Каскадна діаграма: накладні витрати проксі та затримка висхідного сервера                             | Загальний час, TTFB і розмір                                                                        |
| **Відомості LLM** | Провайдер, модель, кількість повідомлень, вхідні/вихідні токени, оцінка вартості, зіставлена ціль     | Показується лише для запитів LLM                                                                    |
| **Статистика**    | Recharts: часова шкала затримки, стовпчикова діаграма токенів, точкова діаграма викликів інструментів | Показується лише після завантаження записаного сеансу                                               |

### 3.4 Елементи керування панелі інструментів

| Елемент керування | Дія                                                                                |
| ----------------- | ---------------------------------------------------------------------------------- |
| ⎉ Пауза           | Припиняє рендеринг нових запитів; накопичується мітка «X нових»                    |
| 🗑 Очистити        | Очищає список в інтерфейсі (буфер сервера не змінюється)                           |
| ⬇ Експорт .har    | Завантажує поточний відфільтрований список як файл HAR                             |
| ● Записати сеанс  | Починає іменований сеанс запису                                                    |
| Вибір профілю     | Лише LLM / Власні хости / Усі                                                      |
| Фільтр хостів     | Збіг підрядка в полі `host`                                                        |
| Фільтр агентів    | Розкривний список: усі / за окремим агентом                                        |
| Фільтр статусу    | Усі / 2xx / 3xx / 4xx / 5xx / помилка                                              |
| Фільтр джерел     | Усі / agent-bridge / custom-host / http-proxy / system-proxy / tproxy              |
| Фільтр **Наживо** | Показує лише запити, що виконуються (відкриті), — перемикач `liveOnly` (див. §4.6) |

### 3.5 Панелі зі змінним розміром

- Список і панель відомостей розділені маркером перетягування
- Ширина списку: мінімум 280px, максимум 720px, зберігається в `localStorage` (`inspector.listWidth`)
- Можна згорнути до панелі завширшки 48px (лише піктограми); клацніть рядок на панелі, щоб розгорнути

---

## §4 Функції з урахуванням LLM

### 4.1 Визначення типу (`src/mitm/inspector/kindDetector.ts`)

Класифікує кожен запит як `"llm"`, `"app"` або `"unknown"` за допомогою 4 сигналів:

1. **Реєстр хостів** — приблизно 18 відомих імен хостів API LLM (OpenAI, Anthropic, Gemini, Groq, Mistral, Together, Fireworks, Cohere, Perplexity, Hugging Face, OpenRouter, xAI, Moonshot тощо)
2. **Шаблони шляхів** — `/v1/chat/completions`, `/v1/messages`, `/generateContent`, `/v1/responses` тощо
3. **Структура тіла** — виявляє поля `messages[]` (OpenAI/Claude), `contents[]` (Gemini), `prompt`, `input`
4. **Підказки user-agent** — `codex`, `claude`, `gemini`, `antigravity`, `kiro`, `copilot`, `cursor` у рядку UA

Власні хости, додані через режим 2, успадковують свій `kind` із введених у формі даних (типове значення — `"custom"`).

### 4.2 Об’єднувач SSE (`src/mitm/inspector/sseMerger.ts`)

**Незалежна реалізація, створена з чистого аркуша.** Розбір подій відповідає
[алгоритму подій, надісланих сервером, WHATWG](https://html.spec.whatwg.org/multipage/server-sent-events.html#parsing-an-event-stream),
а реконструкція відповідає загальнодоступним схемам потокового передавання
[OpenAI](https://platform.openai.com/docs/api-reference/chat/create),
[Anthropic](https://platform.claude.com/docs/en/build-with-claude/streaming) і
[Gemini](https://ai.google.dev/api/generate-content#method:-models.streamgeneratecontent).

Відтворює остаточне повідомлення асистента з необроблених дельта-подій SSE:

- **Anthropic**: накопичує `content_block_delta` за індексом; обробляє `text_delta`, `input_json_delta` (виклики інструментів), `thinking_delta`
- **OpenAI**: накопичує варіанти Chat Completions/виклики інструментів і вихідні елементи Responses API
  за індексом
- **Gemini**: накопичує `candidates[i].content.parts`
- **Невідомий формат**: повертає необроблені події без змін

На вкладці Response відображається перемикач: **«Необроблені події ↔ Об’єднаний результат»**.

### 4.3 Нормалізатор розмов (`src/mitm/inspector/conversationNormalizer.ts`)

**Незалежна реалізація, створена з чистого аркуша.** Нормалізацію визначають локальні контракти
«чорної скриньки» та загальнодоступні схеми повідомлень OpenAI, Anthropic і Gemini; вихідний код
сторонніх реалізацій не використовується.

Перетворює формати повідомлень OpenAI, Anthropic і Gemini на єдиний `NormalizedConversation` перед відтворенням:

```ts
interface NormalizedConversation {
  request: NormalizedTurn[]; // повідомлення / вміст / запит із тіла запиту
  response: NormalizedTurn[]; // відповідь асистента (об’єднана через sseMerger)
  contextKey: string | null; // відбиток системного запиту SHA-256
}
```

Типи блоків: `text`, `tool_use`, `tool_result`. Вкладка Conversation використовує цю структуру незалежно від постачальника.

### 4.4 Кольорове позначення ключа контексту (`src/mitm/inspector/contextKey.ts`)

- Обчислює `SHA-256` системного запиту (перше повідомлення `role:system`, поле `system` або `systemInstruction` Gemini)
- Повертає 12-символьний шістнадцятковий префікс (`"a3f9c2..."`)
- Інтерфейс зіставляє ключ із детермінованим кольором HSL для смуги лівої межі
- **Фільтр «той самий контекст»**: натискання мітки `ctx #a3f` додає фільтр, який показує лише запити з тим самим відбитком

Це дає змогу легко візуально розрізняти різні «персони» або завдання, що виконуються в межах одного сеансу агента.

### 4.5 Видобування метаданих LLM

Для запитів LLM вкладка LLM Details видобуває:

```ts
interface LlmMetadata {
  provider: string | null; // "openai" | "anthropic" | "gemini" | ...
  apiKind: string | null; // "chat.completions" | "messages" | "embeddings" | ...
  model: string | null; // із тіла запиту або відповіді
  messages: number; // кількість реплік
  tokensIn: number | null; // usage.prompt_tokens / usage.input_tokens
  tokensOut: number | null; // usage.completion_tokens / usage.output_tokens
  streamed: boolean; // true, якщо це відповідь SSE
  mappedTo: string | null; // заголовок x-omniroute-mapped
  costEstimateUsd: number | null; // орієнтовна вартість на основі тарифів OmniRoute
}
```

### 4.6 Фільтр активних запитів у реальному часі

Поле `status` запиту має тип `number | "in-flight" | "error"` — запис
додається зі значенням `"in-flight"` у момент початку запиту та **оновлюється на місці**,
коли надходить відповідь (або виникає помилка). Перемикач **«Наживо»** на панелі інструментів
(`liveOnly`, ключ i18n `trafficInspector.liveOnly`) обмежує список записами,
у яких `status === "in-flight"`, даючи змогу спостерігати за відкритими з’єднаннями в реальному часі.

Фільтр є чистим предикатом на стороні клієнта у
`src/lib/inspector/matchesTrafficFilter.ts`:

```ts
if (f.liveOnly && req.status !== "in-flight") return false;
```

Стан перемикача зберігається в `useTrafficFilters` (хуки панелі інспектора) і
поєднується з іншими фільтрами (профіль, хост, агент, джерело, статус, контекст).

### 4.7 Прив’язування до процесу (Linux)

У Linux кожен перехоплений запит можна пов’язати з **локальним процесом-джерелом**.
До `InterceptedRequest` додаються два необов’язкові поля:

```ts
pid?: number;          // ідентифікатор процесу-джерела (лише Linux)
processName?: string;  // назва процесу-джерела (лише Linux)
```

`src/mitm/inspector/processAttribution.ts` зіставляє тимчасовий порт _клієнта_
з PID і назвою таким чином:

1. Читає `/proc/net/tcp` і `/proc/net/tcp6`, щоб знайти inode сокета для
   порту (`parseProcNetTcpForInode` — чистий парсер, який можна тестувати на фікстурах).
2. Переглядає `/proc/<pid>/fd/` у пошуках символічного посилання на `socket:[<inode>]`.
3. Читає назву процесу з `/proc/<pid>/comm`.

Кеш із TTL 1 секунда обмежує витрати на сканування procfs під навантаженням. Прив’язування
виконується **за можливості** — за будь-якої помилки повертається `null`, і захоплення ніколи
не блокується. У macOS/Windows функція повертає `null` (заглушка; підтримку
`lsof`/`GetExtendedTcpTable` буде додано згодом).

---

## §5 Сеанси

### 5.1 Запис сеансу

1. Натисніть **"● Записати сеанс"** на панелі інструментів → введіть назву (необов’язково)
2. Відображення даних у реальному часі продовжується у звичайному режимі; червоний пульсуючий індикатор показує `◉ ЗАПИС · <назва> · 00:42 · 23 запити`
3. Натисніть **"⏹ Зупинити"** → знімок сеансу буде збережено до `inspector_sessions` + `inspector_session_requests`

### 5.2 Перегляд записаного сеансу

Розкривний список **Сеанси** на панелі інструментів містить збережені сеанси. Після вибору сеансу:

- Завантажується знімок сеансу (зафіксований стан)
- На банері відображається: `Перегляд записаного сеансу "<назва>" — [Повернутися до даних у реальному часі]`
- Стає доступною вкладка «Статистика» з агрегованими даними Recharts

### 5.3 Формати експорту

Кожен сеанс можна експортувати в таких форматах:

| Формат                     | Використання                                                                       |
| -------------------------- | ---------------------------------------------------------------------------------- |
| **HAR** (HTTP Archive 1.2) | Сумісний із Chrome DevTools, Charles, Fiddler — імпортуйте для офлайн-аналізу      |
| **JSONL**                  | Один `InterceptedRequest` у кожному рядку — сумісний із форматом `llm-interceptor` |

Експортуйте через `GET /api/tools/traffic-inspector/sessions/{id}/export.har` або за допомогою кнопки ⬇ у розкривному списку «Сеанси».

---

## §6 Безпека

Traffic Inspector показує **весь перехоплений HTTPS-трафік**, включно із заголовками авторизації та тілами запитів. Застосовуються такі засоби контролю:

| Засіб контролю                      | Докладні відомості                                                                                                                                                                                  |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **LOCAL_ONLY**                      | Усі маршрути й кінцева точка WebSocket доступні лише через loopback-інтерфейс (це перевіряється в `routeGuard.ts` перед автентифікацією)                                                            |
| **Маскування секретів**             | Лінійний сканер `maskSecret()` редагує облікові дані Bearer за RFC 6750, ключі з префіксами постачальників і довгі непрозорі токени перед викликом `TrafficBuffer.push()`                           |
| **Обмеження розміру тіла**          | Тіла розміром понад `INSPECTOR_MAX_BODY_KB` (типове значення — 1024 КБ) обрізаються з повідомленням `"(обрізано задля продуктивності)"`                                                             |
| **Санітизація заголовків**          | Назви переводяться в нижній регістр; заголовки фреймінгу/hop-by-hop і проксі-автентифікації відкидаються; файли cookie повністю редагуються; значення облікових даних передаються до `maskSecret()` |
| **CSP**                             | Сувора Content Security Policy на сторінках Traffic Inspector для запобігання XSS через ін’єкцію тіл відповідей                                                                                     |
| **Без збереження за замовчуванням** | `TrafficBuffer` зберігається в пам’яті й втрачається після перезапуску сервера. Сеанси зберігаються лише тоді, коли їх записують явно                                                               |

### Застосовані жорсткі правила

| Правило                           | Застосування                                                                                      |
| --------------------------------- | ------------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`    | Усі відповіді Traffic Inspector про помилки HTTP проходять санітизацію                            |
| **#15 + #17** `isLocalOnlyPath()` | `/api/tools/traffic-inspector/` має режими LOCAL_ONLY + SPAWN_CAPABLE (команди системного проксі) |

### Відомі обмеження

- **Режим загальносистемного проксі** впливає на всі програми на комп’ютері, включно з клієнтами VPN і SSO. Завжди використовуйте його з таймером автоматичного вимкнення. Не використовуйте на спільних комп’ютерах.
- **HTTPS через тунель CONNECT**: режим 3 (HTTP_PROXY) перехоплює лише метадані тунелю для адрес призначення HTTPS, якщо не ввімкнено перехоплення TLS. Це передбачена поведінка — прозоре перехоплення без довіри до сертифіката AgentBridge порушило б перевірку TLS для таких програм.
- **Жорстко задані рядки в деяких компонентах**: деякі компоненти інтерфейсу користувача (F7/F8) містять невелику кількість жорстко заданих рядків, які ще не охоплені ключами i18n. Їх задокументовано як відоме обмеження у звіті про прогалини i18n; їх буде перенесено під час наступного етапу. Зазначені рядки є декоративними мітками інтерфейсу користувача, які не потребують перекладу для функціонального використання.

---

## §7 Усунення несправностей

### Відключення WebSocket

Якщо в потоці даних у реальному часі відображається «Відключено»:

1. Перевірте, чи сервер усе ще працює: `GET /api/tools/traffic-inspector/capture-modes`
2. Перезавантажте сторінку — WebSocket повторно підключиться й отримає новий знімок
3. Якщо сервер було перезапущено, буфер у пам’яті було очищено — старі записи втрачено, якщо сеанс не було записано

### Конфлікт порту 8080

Якщо режим HTTP_PROXY не запускається:

```bash
lsof -i :8080    # знайти процес
```

Змініть порт:

```bash
# .env
INSPECTOR_HTTP_PROXY_PORT=8888
```

### Системний проксі не повернуто до попереднього стану

Якщо OmniRoute аварійно завершує роботу, коли активний загальносистемний режим проксі:

**macOS:**

```bash
networksetup -setwebproxystate Wi-Fi off
networksetup -setsecurewebproxystate Wi-Fi off
```

**Linux (GNOME):**

```bash
gsettings set org.gnome.system.proxy mode 'none'
```

**Windows:**

```cmd
netsh winhttp reset proxy
```

Під час наступного завантаження панель керування також запропонує «Повернути системний проксі до попереднього стану», якщо виявить, що стан у БД указує на активний проксі.

### Буфер заповнений

Коли буфер досягає розміру `INSPECTOR_BUFFER_SIZE` (типово 1000), нові записи витісняють найстаріші. Якщо важливі запити втрачаються:

- Збільште `INSPECTOR_BUFFER_SIZE` (наприклад, до 5000) — це збільшить використання пам’яті, але дасть змогу зберігати більше записів
- Запишіть сеанс, щоб зберегти відповідний проміжок у БД

---

## §8 Довідник API

Усі маршрути мають режими `LOCAL_ONLY` (лише для loopback-інтерфейсу) і `SPAWN_CAPABLE` (команди системного проксі). Див. `src/server/authz/routeGuard.ts`.

Базовий шлях: `/api/tools/traffic-inspector/`

### Керування запитами

| Метод  | Шлях                        | Опис                                                                                   |
| ------ | --------------------------- | -------------------------------------------------------------------------------------- |
| GET    | `/requests`                 | Перелік запитів (із фільтрами: `?profile=llm&host=&agent=&status=&source=&sessionId=`) |
| GET    | `/requests/{id}`            | Відомості про окремий запит                                                            |
| DELETE | `/requests`                 | Очистити буфер у пам’яті                                                               |
| POST   | `/requests/{id}/replay`     | Повторно виконати той самий запит через маршрутизатор OmniRoute                        |
| PUT    | `/requests/{id}/annotation` | Зберегти або оновити примітку до запиту                                                |

### WebSocket

| Метод | Шлях  | Опис                                                                                                            |
| ----- | ----- | --------------------------------------------------------------------------------------------------------------- |
| GET   | `/ws` | Потік WebSocket у реальному часі. Надсилає `snapshot` під час підключення, а потім події `new`/`update`/`clear` |

### Експорт

| Метод | Шлях          | Опис                                                    |
| ----- | ------------- | ------------------------------------------------------- |
| GET   | `/export.har` | Експортувати поточний відфільтрований список як HAR 1.2 |

### Власні хости

| Метод  | Шлях            | Опис                                           |
| ------ | --------------- | ---------------------------------------------- |
| GET    | `/hosts`        | Перелік власних хостів                         |
| POST   | `/hosts`        | Додати хост (автоматично редагує `/etc/hosts`) |
| DELETE | `/hosts/{host}` | Видалити хост                                  |
| PATCH  | `/hosts/{host}` | Перемкнути `enabled`                           |

### Режими перехоплення

| Метод | Шлях                           | Опис                                                                                                     |
| ----- | ------------------------------ | -------------------------------------------------------------------------------------------------------- |
| GET   | `/capture-modes`               | Стан режимів AgentBridge / власних хостів / HTTP_PROXY / системного проксі та перемикача `tls-intercept` |
| POST  | `/capture-modes/http-proxy`    | Запустити/зупинити слухач HTTP_PROXY (`{action: "start"\|"stop"}`)                                       |
| POST  | `/capture-modes/system-proxy`  | Застосувати/скасувати загальносистемний проксі (`{action: "apply"\|"revert"}`)                           |
| POST  | `/capture-modes/tls-intercept` | Перемкнути розшифрування тіла HTTPS у режимі проксі (`{enabled: boolean}`)                               |

> **Розшифрування TPROXY** (режим перехоплення 5) керується через **окремий** маршрут із префіксом
> AgentBridge — `GET / POST / DELETE /api/tools/agent-bridge/tproxy` — а не
> через `/api/tools/traffic-inspector/`. Див.
> `docs/security/MITM-TPROXY-DECRYPT.md` (git; не компілюється в `/docs`).

### Сеанси

| Метод  | Шлях                        | Опис                                                                     |
| ------ | --------------------------- | ------------------------------------------------------------------------ |
| POST   | `/sessions`                 | Розпочати запис (`{name?: string}`)                                      |
| PATCH  | `/sessions/{id}`            | Зупинити або перейменувати (`{action: "stop"\|"rename", name?: string}`) |
| GET    | `/sessions`                 | Перелік усіх збережених сеансів                                          |
| GET    | `/sessions/{id}`            | Знімок сеансу (усі запити)                                               |
| DELETE | `/sessions/{id}`            | Видалити сеанс                                                           |
| GET    | `/sessions/{id}/export.har` | Експортувати сеанс як HAR 1.2                                            |

### Внутрішнє приймання даних (резервний варіант D4)

| Метод | Шлях               | Опис                                                                                                              |
| ----- | ------------------ | ----------------------------------------------------------------------------------------------------------------- |
| POST  | `/internal/ingest` | Приймає перехоплений запит із наскрізного шляху `server.cjs`; вимагає заголовок `INSPECTOR_INTERNAL_INGEST_TOKEN` |

Повні схеми OpenAPI: `docs/openapi.yaml` → тег `Traffic Inspector`.
