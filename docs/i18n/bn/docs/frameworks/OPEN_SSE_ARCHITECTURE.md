# open-sse Architecture (বাংলা)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **সংক্ষেপে**: `open-sse/` হলো মূল স্ট্রিমিং ইঞ্জিন, যা OmniRoute-এর প্রতিটি LLM অনুরোধ পরিচালনা করে। এতে অনুরোধ পাইপলাইন, এক্সিকিউটর, সার্ভিস, MCP সার্ভার এবং অনুবাদ স্তর বাস্তবায়নকারী প্রায় ১,৪০০টি ফাইল রয়েছে। এই নির্দেশিকায় অংশগুলো কীভাবে একসঙ্গে কাজ করে তা ব্যাখ্যা করা হয়েছে।

**উৎস:** `open-sse/` (ওয়ার্কস্পেস প্যাকেজ, প্রায় ১,৪৪০টি নন-টেস্ট `.ts` ফাইল)

---

## আলাদা ওয়ার্কস্পেস প্যাকেজ কেন?

OmniRoute মনোরেপোতে `open-sse/` একটি **স্বতন্ত্র ওয়ার্কস্পেস**, যার কয়েকটি কারণ রয়েছে:

1. **পুনর্ব্যবহারযোগ্যতা** — `open-sse`, npm-এ `@omniroute/open-sse` হিসেবে প্রকাশিত হয়, তাই অন্যান্য প্রজেক্ট এটি স্বাধীনভাবে ব্যবহার করতে পারে
2. **সুস্পষ্ট সীমারেখা** — স্ট্রিমিং ইঞ্জিনটি OmniRoute-নির্দিষ্ট UI/DB স্তর থেকে পৃথক
3. **কর্মক্ষমতা** — ইঞ্জিনটির কোনো Next.js নির্ভরতা নেই, ফলে CLI/serverless প্রেক্ষাপটে দ্রুততর কোল্ড স্টার্ট সম্ভব হয়
4. **সংস্করণ ব্যবস্থাপনা** — `open-sse` নিজস্ব সময়সূচি অনুযায়ী রিলিজ করতে পারে

```json
// package.json
"workspaces": ["open-sse"]
```

---

## শীর্ষ-স্তরের কাঠামো

```
open-sse/
├── index.ts              # পাবলিক এন্ট্রি পয়েন্ট
├── types.d.ts            # পাবলিক টাইপ এক্সপোর্ট
├── package.json          # @omniroute/open-sse
├── config/               # প্রোভাইডার কনফিগ, ধ্রুবক, রেজিস্ট্রি
├── executors/            # প্রতি-প্রোভাইডার HTTP এক্সিকিউটর (৬৭ + base.ts/index.ts)
├── handlers/             # অনুরোধ হ্যান্ডলার (chatCore, responses ইত্যাদি)
├── lib/                  # অভ্যন্তরীণ ইউটিলিটি
├── mcp-server/           # Model Context Protocol সার্ভার
├── services/             # প্রায় ২৯৮টি সার্ভিস মডিউল
├── transformer/          # Responses API ফরম্যাট ট্রান্সফরমার
├── translator/           # ফরম্যাট অনুবাদ (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # শেয়ার্ড ইউটিলিটি (লগিং, ত্রুটি, স্ট্রিম ইত্যাদি)
```

### মডিউলের সংখ্যা

| ডিরেক্টরি | ফাইল | উদ্দেশ্য |
| `executors/` | 167 | প্রতি-প্রোভাইডার HTTP এক্সিকিউটর (`DefaultExecutor` ফ্যাক্টরির মাধ্যমে একীভূত) |
| `handlers/` | 157 | অনুরোধের এন্ট্রি পয়েন্ট (`chatCore`, `responses`, embeddings) |
| `services/` | ~536 | রাউটিং, ক্যাশিং, রেট লিমিটিং, রিফ্রেশ ইত্যাদি |
| `translator/` | 56 | ফরম্যাট রূপান্তর (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP টুল এবং ট্রান্সপোর্ট |
| `utils/` | ~108 | বিভিন্ন অংশে ব্যবহৃত ইউটিলিটি (লগিং, ত্রুটি, স্ট্রিম) |
| `config/` | ~339 | প্রোভাইডার কনফিগ, ধ্রুবক, রেজিস্ট্রি |

---

## অনুরোধ পাইপলাইন

প্রতিটি LLM অনুরোধ একটি **৫-ধাপের পাইপলাইনের** মধ্য দিয়ে প্রবাহিত হয়:

```
                              ┌──────────────┐
   HTTP অনুরোধ                │  ১. রাউট     │   কম্বো নির্ধারণ, মডেল নির্বাচন
   (Next.js রুট)              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  ২. অনুবাদ   │   ফরম্যাট রূপান্তর (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  ৩. সম্পাদন  │   প্রোভাইডার এক্সিকিউটর, HTTP, পুনঃচেষ্টা, ব্রেকার
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  ৪. স্ট্রিম   │   SSE রূপান্তর, ব্যাকপ্রেশার
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  ৫. রেকর্ড   │   ব্যবহার ট্র্যাকিং, কল লগ, ত্রুটি শ্রেণিবিন্যাস
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP প্রতিক্রিয়া (SSE অথবা JSON)
```

### ধাপ ১: রাউট (services/combo.ts)

**এন্ট্রি পয়েন্ট**: `services/combo.ts`-এর `handleComboChat()`

অনুরোধটিকে একটি নির্দিষ্ট `(provider, model, account, credentials)` টাপলে রূপ দেয়:

- ID দিয়ে কম্বো খুঁজে বের করে (অথবা `auto/*` মডেলের জন্য একটি ভার্চুয়াল কম্বো তৈরি করে)
- রাউটিং কৌশল প্রয়োগ করে (অগ্রাধিকার, ওয়েটেড, রাউন্ড-রবিন ইত্যাদি)
- অস্বাস্থ্যকর প্রোভাইডারগুলো বাদ দেয় (সার্কিট ব্রেকার)
- পরবর্তী কার্যকর টার্গেট নির্বাচন করে

`auto/*` মডেলের জন্য এই ধাপটি আরও যা করে:

- **১৬-ফ্যাক্টর স্কোরিং** অ্যালগরিদম (`services/autoCombo/`) চালায়
- স্বাস্থ্য, খরচ, ল্যাটেন্সি ইত্যাদির ভিত্তিতে একটি `provider+model` জোড়া নির্বাচন করে

### ধাপ ২: অনুবাদ (translator/)

উৎস ফরম্যাট (যেমন OpenAI) এবং টার্গেট ফরম্যাট (যেমন Claude) ভিন্ন হলে, অনুরোধটি **অনুবাদ** করা হয়:

- সিস্টেম প্রম্পট → সিস্টেম মেসেজ
- টুলের সংজ্ঞা → প্রোভাইডার-নির্দিষ্ট টুল ফরম্যাট
- রিজনিং/থিংকিং প্যারামিটার → প্রোভাইডার-নির্দিষ্ট সমতুল্য রূপ
- মেসেজ ভূমিকা স্বাভাবিকীকরণ (OpenAI ছাড়া অন্যগুলোর জন্য `developer` → `system`)

`translator/index.ts` যা প্রকাশ করে:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### ধাপ ৩: সম্পাদন (executors/)

**এন্ট্রি পয়েন্ট**: `getExecutor(providerId).execute(request, options)`

সব প্রোভাইডার `getExecutor()` ফ্যাক্টরি ফলব্যাকের মাধ্যমে `DefaultExecutor` (`executors/default.ts`) ব্যবহার করে। এক্সিকিউটর যা করে:

- আপস্ট্রিম URL তৈরি করে (`buildUrl()`)
- প্রোভাইডার-নির্দিষ্ট হেডার যোগ করে (`buildHeaders()`)
- অনুরোধের বডি রূপান্তর করে (`transformRequest()`)
- পুনঃচেষ্টা ও এক্সপোনেনশিয়াল ব্যাকঅফসহ HTTP অনুরোধ পাঠায়
- প্রয়োজন হলে অথেন্টিকেশন রিফ্রেশ পরিচালনা করে (OAuth প্রোভাইডার)

সব এক্সিকিউটর `BaseExecutor` (`executors/base.ts`, 1170 LOC) সম্প্রসারণ করে, যা প্রদান করে:

- সাধারণ পুনঃচেষ্টা লজিক
- প্রক্সি ইন্টিগ্রেশন
- সার্কিট ব্রেকার ইন্টিগ্রেশন
- ব্যবহার রেকর্ডিং হুক

### ধাপ ৪: স্ট্রিম (utils/stream.ts)

স্ট্রিমিং প্রতিক্রিয়ার ক্ষেত্রে, এক্সিকিউটর একটি **ReadableStream** ফেরত দেয়। হ্যান্ডলার যা করে:

- একটি SSE ট্রান্সফর্মের (`createSSETransformStreamWithLogger`) মধ্য দিয়ে পাইপ করে
- বিচ্ছিন্ন সংযোগ শনাক্ত করতে হার্টবিট পিং প্রয়োগ করে
- ক্লায়েন্টের সংযোগ বিচ্ছিন্ন হওয়া সাবলীলভাবে পরিচালনা করে (`pipeWithDisconnect`)
- নন-স্ট্রিমিং ক্লায়েন্টের জন্য SSE → JSON রূপান্তর করে

নন-স্ট্রিমিং প্রতিক্রিয়ার ক্ষেত্রে, এক্সিকিউটর একটি পার্স করা JSON অবজেক্ট ফেরত দেয়, যা কোনো পরিবর্তন ছাড়াই পাঠিয়ে দেওয়া হয়।

### ধাপ ৫: রেকর্ড (services/usage.ts)

প্রতিক্রিয়ার পরে (সফল বা ব্যর্থ), ব্যবহারসংক্রান্ত তথ্য রেকর্ড করা হয়:

- প্রতিক্রিয়া থেকে `prompt_tokens`, `completion_tokens`, `cached_tokens`
- মূল্য নির্ধারণের ডেটা থেকে গণনা করা `cost_usd`
- ব্যর্থ হলে `latency_ms`, `status`, `error_class`
- `usage_history` টেবিলে সংরক্ষিত

কল লগের আর্টিফ্যাক্টগুলো (সক্রিয় থাকলে) `${DATA_DIR}/call_logs/`-এ লেখা হয়।

---

## প্রধান ফাইলগুলোর গভীর বিশ্লেষণ

### chatCore.ts (5977 লাইন)

**প্রধান রিকোয়েস্ট হ্যান্ডলার**। আকারে বড় হলেও এর কাঠামো সুস্পষ্ট:

```ts
// chatCore.ts-এর ছদ্ম-কাঠামো
export async function handleChat(request: NextRequest) {
  // 1. প্রমাণীকরণ + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. বডি যাচাইকরণ
  const body = await parseRequestBody(request);

  // 3. ফরম্যাট শনাক্তকরণ + রূপান্তর
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. কম্বো রাউটিং
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // পরবর্তী টার্গেটে এগিয়ে যান
    }
  }

  // 5. জরুরি ফলব্যাক
  return await emergencyFallback(body);
}
```

একটি বিশাল ফাংশন হওয়া সত্ত্বেও, এটি **মন্তব্যযুক্ত সেকশনগুলোতে** বিন্যস্ত, যেগুলো ৫-ধাপের পাইপলাইনের সঙ্গে সামঞ্জস্যপূর্ণ।

### combo.ts (4456 LOC)

এটি হলো **রাউটিং ইঞ্জিন**, যা একটি কম্বোকে ক্রমানুসারে সাজানো টার্গেটগুলোর তালিকায় রূপান্তর করে।

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

**১৯টি রাউটিং কৌশল** সমর্থন করে (`src/shared/constants/routingStrategies.ts` দেখুন):

| কৌশল                | আচরণ                                                                               |
| ------------------- | ---------------------------------------------------------------------------------- |
| `priority`          | প্রথম টার্গেটকে অগ্রাধিকার দেওয়া ক্রমানুসার তালিকা                                |
| `weighted`          | প্রতি-টার্গেট ওজন অনুযায়ী সম্ভাবনাভিত্তিক নির্বাচন                                |
| `round-robin`       | ক্রমানুসারে টার্গেটগুলোর মধ্যে চক্রাকারে নির্বাচন                                  |
| `context-relay`     | টার্গেটগুলোর মধ্যে কনটেক্সট হস্তান্তর                                              |
| `fill-first`        | পরবর্তীটিতে যাওয়ার আগে কোটা পূরণ                                                  |
| `p2c`               | দুটি বিকল্পের মধ্যে সেরা নির্বাচন                                                  |
| `random`            | সমান সম্ভাবনায় এলোমেলো নির্বাচন                                                   |
| `least-used`        | সম্প্রতি সবচেয়ে কম ব্যবহৃতটি নির্বাচন                                             |
| `cost-optimized`    | সবচেয়ে সস্তা সচল টার্গেটকে প্রথমে নির্বাচন                                        |
| `reset-aware`       | প্রোভাইডারের রিসেট উইন্ডো সম্পর্কে সচেতন                                           |
| `reset-window`      | রিসেট উইন্ডোভিত্তিক রাউটিং                                                         |
| `headroom`          | অবশিষ্ট কোটার সর্বাধিক হেডরুমযুক্তটিকে প্রথমে নির্বাচন                             |
| `strict-random`     | সম্পূর্ণ সমান সম্ভাবনাভিত্তিক নির্বাচন (কোনো গুণমানভিত্তিক ওজন ছাড়া)              |
| `auto`              | ১৬-ফ্যাক্টর স্কোরিং ব্যবহার (`autoCombo/`)                                         |
| `lkgp`              | সর্বশেষ পরিচিত কার্যকর প্রোভাইডারকে প্রথমে নির্বাচন                                |
| `context-optimized` | দীর্ঘ-কনটেক্সট রিকোয়েস্টের জন্য সর্বোত্তম                                         |
| `fusion`            | সমান্তরালে একটি প্যানেলে পাঠিয়ে, পরে একজন বিচারকের মাধ্যমে সংশ্লেষণ (`fusion.ts`) |

### base.ts (1170 LOC)

এটি হলো **অ্যাবস্ট্র্যাক্ট এক্সিকিউটর**, যেটিকে সব ১০৭টি এক্সিকিউটর এক্সটেন্ড করে। এতে রয়েছে:

- `buildUrl()` — ডিফল্ট URL নির্মাণ (কাস্টম আচরণের জন্য সাবক্লাসগুলো ওভাররাইড করে)
- `buildHeaders()` — ডিফল্ট হেডার (প্রমাণীকরণ, কনটেন্ট-টাইপ)
- `transformRequest()` — ডিফল্টভাবে অপরিবর্তিতভাবে পাঠায়
- `execute()` — রিট্রাই/ব্যাকঅফ/ব্রেকারসহ প্রধান HTTP লুপ

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // সব OpenAI/Anthropic-সামঞ্জস্যপূর্ণ প্রোভাইডার পরিচালনা করে
  // প্রোভাইডারগুলো কনফিগারেশন (URL, প্রমাণীকরণ, হেডার) নিবন্ধন করে, তবে এক্সিকিউটর লজিক অভিন্নভাবে ব্যবহার করে
}
```

প্রোভাইডার-নির্দিষ্ট আচরণ (প্রমাণীকরণ হেডার, বেস URL, সংস্করণ হেডার) আলাদা এক্সিকিউটর ক্লাসের মাধ্যমে নয়, বরং প্রোভাইডার রেজিস্ট্রির মাধ্যমে কনফিগার করা হয়।

````

---

## সার্ভিসসমূহ (117টি মডিউল)

সার্ভিসগুলো হলো **নির্দিষ্ট কাজে কেন্দ্রীভূত, একক-উদ্দেশ্যের মডিউল**, যেগুলো হ্যান্ডলারগুলো সমন্বয় করে। প্রধান বিভাগগুলো:

### রাউটিং ও কম্বো

- `combo.ts` — কম্বো-রাউটেড অনুরোধের এন্ট্রি পয়েন্ট
- `services/autoCombo/` — 16-ফ্যাক্টর স্কোরিং, 8টি স্বয়ংক্রিয় রাউটিং কৌশল
- `wildcardRouter.ts` — ওয়াইল্ডকার্ড রুট (`gpt-*`) মেলায়
- `modelFamilyFallback.ts` — T5 ইন্ট্রা-ফ্যামিলি ফলব্যাক

### রেট লিমিটিং ও কোটা

- `rateLimitManager.ts` — প্রতিটি key+provider-এর জন্য টোকেন বাকেট
- `usage.ts` — ব্যবহার রেকর্ডিং
- `quotaCache.ts` — ইন-মেমরি কোটা স্ন্যাপশট

### অ্যাকাউন্ট ও টোকেন

- `tokenRefresh.ts` — 401 হলে OAuth রিফ্রেশ
- `accountFallback.ts` — বিকল্প অ্যাকাউন্টে স্যুইচ করে
- `sessionManager.ts` — মাল্টি-টার্ন সেশন স্টেট

### ইন্টেলিজেন্স

- `intentClassifier.ts` — অনুরোধের উদ্দেশ্য শ্রেণিবদ্ধ করে
- `taskAwareRouter.ts` — টাস্কের ধরন অনুযায়ী রাউট করে
- `thinkingBudget.ts` — থিংকিং টোকেন বরাদ্দ করে
- `contextManager.ts` — রাউটিং কনটেক্সট ইনজেক্ট করে

### স্থিতিস্থাপকতা

- `resilience.ts` — রিট্রাই, ব্যাকঅফ ও ব্রেকার অর্কেস্ট্রেশন
- `emergencyFallback.ts` — সর্বশেষ অবলম্বনের ফলব্যাক
- `modelDeprecation.ts` — উত্তরসূরি মডেলে স্বয়ংক্রিয়ভাবে রাউট করে

### স্টেট

- `signatureCache.ts` — অনুরোধের সিগনেচার অনুযায়ী ডিডুপ্লিকেশন
- `volumeDetector.ts` — লোড শেডিং
- `contextHandoff.ts` — সেশন সিরিয়ালাইজেশন

### কম্প্রেশন

- `compression/` (সাবডিরেক্টরি) — সম্পূর্ণ কম্প্রেশন পাইপলাইন
- ইঞ্জিন, রুল প্যাক ও অ্যাডাপ্টার নিয়ে 39টি ফাইল

### স্কিল

- ([SKILLS.md](./SKILLS.md)-এ অন্তর্ভুক্ত)

### মেমোরি

- ([MEMORY.md](./MEMORY.md)-এ অন্তর্ভুক্ত)

---

## এক্সিকিউটরসমূহ (75টির বেশি ফাইল)

প্রতিটি প্রোভাইডারের জন্য একটি ফাইল। এগুলোর সবই `BaseExecutor` এক্সটেন্ড করে এবং যেসব অংশ ভিন্ন, সেগুলো ওভাররাইড করে।

### প্রচলিত প্যাটার্ন

প্রোভাইডারগুলো `getExecutor(providerId)`-এর মাধ্যমে রিজলভ করা হয়, যা কনফিগার করা এক্সিকিউটর ফেরত দেয়। OpenAI/Anthropic-সামঞ্জস্যপূর্ণ প্রোভাইডারগুলো `DefaultExecutor` (`executors/default.ts`) ব্যবহার করে। প্রোভাইডার-নির্দিষ্ট আচরণ (বেস URL, অথ হেডার, API সংস্করণ) `open-sse/config/providers/`-এ কনফিগার করা হয়, অন্যদিকে রিকোয়েস্ট বডির রূপান্তরগুলো `open-sse/translator/`-এ পরিচালিত হয়।

**কাস্টম URL** প্রোভাইডার কনফিগারেশনের মাধ্যমে সেট করা হয়:

```ts
// open-sse/config/providers/-এ প্রোভাইডার কনফিগ
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**কাস্টম অথ** প্রোভাইডার রেজিস্ট্রির অথ কনফিগারেশনের মাধ্যমে পরিচালিত হয় (API কী, OAuth, হেডার প্রোফাইল)।

**কাস্টম রিকোয়েস্ট বডি** রূপান্তরগুলো (যেমন Anthropic-এর `system`-কে `messages` থেকে আলাদা করা) প্রতিটি প্রোভাইডারের জন্য `open-sse/translator/`-এ নিবন্ধিত হয়।

````

### এক্সিকিউটর ফ্যাক্টরি

`executors/index.ts`, `getExecutor(providerId)` এক্সপোর্ট করে:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

রিজল্যুশন `ExecutorRegistry` (`executors/registry.ts`)-এর মধ্য দিয়ে সম্পন্ন হয়: প্রতিটি বিশেষায়িত এক্সিকিউটর `executors/index.ts`-এর বিল্ট-ইন টেবিলে ঘোষণা করা হয় এবং মডিউল লোডের সময় `registerExecutor(alias, instance)`-এর মাধ্যমে নিবন্ধিত হয়; `getExecutor()` রেজিস্ট্রি পরীক্ষা করে এবং বিশেষায়িত এন্ট্রি নেই এমন যেকোনো প্রোভাইডারের জন্য মেমোইজড `DefaultExecutor`-এ ফলব্যাক করে। সম্পূর্ণ alias → executor ম্যাপিংটি গোল্ডেন টেস্ট `tests/unit/executor-map-golden.test.ts` দ্বারা নির্ধারিত।

---

## অনুবাদক

**3টি ফরম্যাটের** মধ্যে অনুবাদ করুন: OpenAI, Anthropic, Gemini, এবং নতুন Responses API।

### কখন অনুবাদ হয়

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

সাধারণ অনুবাদসমূহ:

- `OpenAI → Anthropic`: পৃথক `system` ফিল্ড, `x-api-key` হেডার
- `OpenAI → Gemini`: `messages`-এর পরিবর্তে `contents`, `systemInstruction`
- `OpenAI → Responses API`: `input` অ্যারে, `previous_response_id` স্টেট

### পরিচালিত এজ কেসসমূহ

- `developer` রোল → OpenAI নয় এমন ক্ষেত্রে `system`
- `system` রোল → GLM/ERNIE-এর জন্য প্রথম ব্যবহারকারী মেসেজের সঙ্গে একীভূত
- `json_schema` → Gemini-এর `responseMimeType` + `responseSchema`
- `tools` → প্রোভাইডার-নির্দিষ্ট টুল ফরম্যাট
- চিন্তন প্যারামিটার (o1, Claude) → প্রোভাইডার-নির্দিষ্ট সমতুল্য

---

## MCP সার্ভার

`open-sse/mcp-server/` **Model Context Protocol** সার্ভার বাস্তবায়ন করে:

- **110টি টুল** (প্রোভাইডার ব্যবস্থাপনা, কম্বো, মেমরি, ক্যাশ, কম্প্রেশন, প্রক্সি, স্কিল, গেমিফিকেশন, প্লাগইন, Notion, Obsidian, লোকাল কর্পাস)
- **3টি ট্রান্সপোর্ট**: stdio, SSE, Streamable HTTP
- সূক্ষ্মভাবে নিয়ন্ত্রিত অনুমোদনের জন্য **33টি স্কোপ**

### টুল নিবন্ধন

টুলগুলো `open-sse/mcp-server/tools/`-এ স্বতন্ত্র ফাইল হিসেবে নিবন্ধিত হয়, যার প্রতিটি একটি নাম, স্কিমা, হ্যান্ডলার এবং স্কোপ এক্সপোর্ট করে:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### ট্রান্সপোর্টসমূহ

```ts
// stdio (CLI ব্যবহার)
startMcpStdio(server);

// SSE (HTTP-ভিত্তিক স্ট্রিমিং)
startMcpSse(server, port);

// Streamable HTTP (আধুনিক MCP)
startMcpStreamable(server, port);
```

### অনুমোদন

প্রতিটি টুল কল স্কোপ যাচাইয়ের মধ্য দিয়ে যায় (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## ট্রান্সফর্মারসমূহ

`open-sse/transformer/` **Chat Completions** এবং **Responses API** ফরম্যাটের মধ্যে রূপান্তর করে।

### পৃথক ট্রান্সফর্মার কেন?

Responses API হলো **স্টেটফুল কথোপকথন** (`previous_response_id`) সমর্থিত OpenAI-এর নতুন ফরম্যাট। যখন কোনো ক্লায়েন্ট Responses অনুরোধ পাঠায়, OmniRoute:

1. অভ্যন্তরীণভাবে Responses → Chat Completions-এ রূপান্তর করে
2. প্রোভাইডারের কাছে পাঠায় (Chat Completions সমর্থন করে এমন যেকোনো প্রোভাইডার)
3. রেসপন্সটিকে আবার Responses ফরম্যাটে রূপান্তর করে
4. রূপান্তরিত রেসপন্সটি ক্লায়েন্টের কাছে স্ট্রিম করে

ট্রান্সফর্মারটি (`transformer/responsesTransformer.ts`) প্রদান করে:

```ts
createResponsesApiTransformStream(): TransformStream
```

এটি নিম্নলিখিত বিষয়গুলো পরিচালনা করে:

- `response.output_item.added` ইভেন্ট
- `response.output_text.delta` ইভেন্ট
- `response.completed` ইভেন্ট
- টুল কল ম্যাপিং (`function_call` ↔ `tool_calls`)

---

## কনফিগারেশন

`open-sse/config/` কনফিগারেশন স্তর ধারণ করে:

| ফাইল                          | উদ্দেশ্য                                             |
| ----------------------------- | ---------------------------------------------------- |
| `providerRegistry.ts`         | 352-প্রোভাইডারের ক্যাটালগের ওপর Chat-মডেল রেজিস্ট্রি |
| `providerModels.ts`           | মডেল উপনাম, ফরম্যাট ম্যাপিং                          |
| `constants.ts`                | টাইমআউট, সীমা, স্ট্যাটাস কোড                         |
| `defaultThinkingSignature.ts` | ডিফল্ট Claude চিন্তন স্বাক্ষর                        |
| `modelStrip.ts` (সার্ভিসে)    | প্রোভাইডারভিত্তিক ফিল্ড অপসারণ                       |

### প্রোভাইডার রেজিস্ট্রি স্কিমা

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

মডিউল লোডের সময় Zod যাচাইকরণ নিশ্চিত করে যে সব প্রোভাইডার কনফিগ বৈধ।

---

## পারফরম্যান্স সীমাবদ্ধতা

রাউটিং ইঞ্জিনের কঠোর পারফরম্যান্স বাজেট রয়েছে:

| অপারেশন                                | লক্ষ্য | পরিমাপ                      |
| -------------------------------------- | ------ | --------------------------- |
| কম্বো রেজোলিউশন                        | <10ms  | 50টি টার্গেটের জন্য         |
| রেট লিমিট যাচাই                        | <1ms   | ইন-মেমরি টোকেন বাকেট        |
| মডেল ফ্যামিলি ফলব্যাক                  | <5ms   | ক্যাশ করা ফ্যামিলি ডেফিনিশন |
| রিকোয়েস্ট রাউটিং ডিসপ্যাচ             | <2ms   | হট পাথ                      |
| **রাউটিং হট পাথে কোনো ব্লকিং I/O নয়** | —      | সবই অ্যাসিঙ্ক               |

---

## অ্যান্টি-প্যাটার্ন

❌ **`combo.ts`-এ সিঙ্ক্রোনাস DB কল** — আগে থেকে কম্পিউট করে ক্যাশ করুন
❌ **হ্যান্ডলারে রিট্রাই লজিক** — রেজিলিয়েন্স সার্ভিসের `retry()` ব্যবহার করুন
❌ **প্রোভাইডার কনফিগে সরাসরি অ্যাক্সেস** — `providerRegistry` গেটার ব্যবহার করুন
❌ **হার্ডকোড করা ফলব্যাক চেইন** — `modelFamilyFallback.ts`-এ সংজ্ঞায়িত করুন
❌ **সমসাময়িক রিকোয়েস্টজুড়ে স্টেট মিউটেশন** — শুধু রিকোয়েস্ট-স্কোপড কনটেক্সট ব্যবহার করুন

---

## নতুন কম্পোনেন্ট যোগ করা

### নতুন সার্ভিস যোগ করা

1. নির্দিষ্ট দায়িত্বসহ `open-sse/services/[serviceName].ts` তৈরি করুন
2. মূল হ্যান্ডলার ফাংশন এবং যেকোনো কনস্ট্যান্ট এক্সপোর্ট করুন
3. `tests/unit/services/[serviceName].test.mjs`-এ ইউনিট টেস্ট যোগ করুন
4. রাউটিং-সম্পর্কিত হলে `handlers/chatCore.ts`-এর রিকোয়েস্ট পাইপলাইনে ইন্টিগ্রেট করুন
5. সার্ভিসটি টার্গেট নির্বাচনকে প্রভাবিত করলে `combo.ts`-এর রাউটিং লজিক আপডেট করুন
6. এই ফাইলে ডকুমেন্ট করুন

### নতুন এক্সিকিউটর যোগ করা

1. `BaseExecutor` এক্সটেন্ড করে `open-sse/executors/[provider].ts` তৈরি করুন
2. `config/providerRegistry.ts`-এ রেজিস্টার করুন
3. `executors/index.ts` ফ্যাক্টরিতে যোগ করুন
4. এক্সিকিউটরের জন্য ইউনিট টেস্ট যোগ করুন
5. `docs/architecture/ARCHITECTURE.md`-এ ডকুমেন্ট করুন

### নতুন MCP টুল যোগ করা

1. `open-sse/mcp-server/tools/[category]Tools.ts` তৈরি বা আপডেট করুন
2. ইনপুটের জন্য Zod স্কিমা সংজ্ঞায়িত করুন
3. `mcp-server/index.ts`-এ টুলটি রেজিস্টার করুন
4. `mcp-server/auth/`-এর স্কোপ ম্যাট্রিক্সে যোগ করুন
5. ইউনিট টেস্ট যোগ করুন

---

## আরও দেখুন

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — উচ্চ-স্তরের আর্কিটেকচার
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — ইঞ্জিনিয়ারিং রেফারেন্স
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — ডিরেক্টরি-বাই-ডিরেক্টরি
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16-ফ্যাক্টর স্কোরিং
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP সার্ভার
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A সার্ভার
- সোর্স: `open-sse/` (400+ ফাইল, ~143K LOC)
