# Claude Code CLI — Configuration with OmniRoute (বাংলা)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇦 [bs](../../../bs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

**Claude Code** CLI (`claude`)-কে OmniRoute-এর দিকে নির্দেশ করুন—স্থানীয় বা রিমোট VPS—
প্রতি-মডেল প্রোফাইলসহ, Codex সেটআপের অনুরূপভাবে।

---

## দ্রুত শুরু

```bash
# স্থানীয় OmniRoute-এর সঙ্গে Claude Code চালু করুন (সক্রিয় context স্বয়ংক্রিয়ভাবে শনাক্ত করে)
omniroute launch

# রিমোট OmniRoute-এর সঙ্গে (`omniroute connect <host>`-এর পরে এটি স্বয়ংক্রিয়)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# প্রতি-মডেল প্রোফাইল তৈরি করুন, তারপর একটি চালু করুন
omniroute setup-claude            # ~/.claude/profiles/<name>/settings.json-এ লেখে
omniroute launch --profile glm52  # OmniRoute-এর মাধ্যমে glm/glm-5.2 ব্যবহারকারী Claude Code
```

---

## Claude Code কীভাবে একটি gateway-এর সঙ্গে সংযুক্ত হয়

Claude Code **Anthropic Messages API**-এর সঙ্গে যোগাযোগ করে এবং environment variable-এর
মাধ্যমে একটি custom endpoint-এর দিকে নির্দেশিত হয় (এতে কোনো `--base-url` flag নেই):

| Variable                                     | উদ্দেশ্য                                                                                           |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `ANTHROPIC_BASE_URL`                         | Gateway-এর root URL (Claude Code শেষে `/v1/messages` যোগ করে)। **কোনো `/v1` suffix নয়।**          |
| `ANTHROPIC_AUTH_TOKEN`                       | `Authorization: Bearer …` হিসেবে পাঠানো হয়—আপনার OmniRoute access token / API key ব্যবহার করুন    |
| `ANTHROPIC_API_KEY`                          | বিকল্প: `x-api-key` হিসেবে পাঠানো হয়। উভয়টি সেট করা থাকলে `ANTHROPIC_AUTH_TOKEN` অগ্রাধিকার পায় |
| `ANTHROPIC_MODEL`                            | একটি নির্দিষ্ট model বাধ্যতামূলক করে (`/model` picker-এর default-কে override করে)                  |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | `1` → native `/model` picker `/v1/models` থেকে `claude*`/`anthropic*` model দেখায়                 |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | প্রতি response-এ output token সীমাবদ্ধ করে (যেমন `65536`)                                          |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | auto-compaction-এর জন্য token threshold                                                            |

> Env var-গুলো **startup-এর সময় একবারই পড়া হয়**—এগুলো পরিবর্তন করার পরে Claude Code restart করুন।

`omniroute launch` আপনার জন্য এগুলোর সবই সেট করে: এটি সক্রিয় context থেকে base URL + token
নির্ধারণ করে (তাই `omniroute connect <vps>` এবং তারপর `omniroute launch`
সরাসরি কাজ করে), server-এর health check করে এবং `claude` exec করে।

---

## Discovery alias—`/model` picker-এ non-Claude model প্রদর্শন করুন

Claude Code-এর gateway model discovery কেবল `claude` বা `anthropic` দিয়ে শুরু হওয়া id
দেখায়, তাই `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` থাকলে native
`/model` picker সাধারণত OmniRoute-এর **শুধু** Claude/Anthropic model-গুলো দেখায়—
`kimi/kimi-k2.6` বা `glm/glm-5.2` কখনোই দেখা যায় না, যদিও সেগুলো ঠিকভাবেই route হয়।

OmniRoute যেকোনো enabled model (এবং combo)-কে একটি `claude/…` id-এর অধীনে mirror করতে
পারে, যাতে এটি ওই filter অতিক্রম করে এবং picker-এ দেখা যায়:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<combo "custo-otimizado"> →  claude/combo/custo-otimizado
```

Claude Code-এ এগুলোর একটি বেছে নিলে, routing-এর আগে OmniRoute `claude/` wrapper
সরিয়ে প্রকৃত id ফিরিয়ে আনে—একটি আসল `claude/<real-claude-model>` id
(প্রকৃত Claude OAuth provider) সর্বদা অপরিবর্তিত রাখা হয়।

**এটি default হিসেবে বন্ধ থাকে** এবং একটি তিন-স্তরের gate দ্বারা নিয়ন্ত্রিত হয় (সবচেয়ে
নির্দিষ্টটি অগ্রাধিকার পায়), ফলে সাধারণ OmniRoute কখনোই Claude Code ব্যবহার না করা
client-দের জন্য তার catalog দ্বিগুণ করে না:

| স্তর     | কোথায়                                                                         |
| -------- | ------------------------------------------------------------------------------ |
| Model    | Provider detail page → প্রতি-model "Expose in Claude Code" toggle              |
| Provider | Provider detail page → provider-level toggle (এর সব model-কে অন্তর্ভুক্ত করে)  |
| Global   | Settings → Feature Flags → `EXPOSE_CC_DISCOVERY_ALIASES` (default হিসেবে বন্ধ) |

`EXPOSE_CC_DISCOVERY_ALIASES` environment variable global level-কে বাধ্যতামূলকভাবে চালু
করে এবং dashboard override-এর ওপর অগ্রাধিকার পায় (এটি কার্যকর হলে Feature Flags screen-এ
একটি "environment variable-এর মাধ্যমে সক্রিয়" note দেখা যায়)। সেখান থেকে প্রতি-provider
এবং প্রতি-model toggle আরও সূক্ষ্ম নিয়ন্ত্রণ দেয়—যেমন global বন্ধ + Kimi provider চালু
থাকলে কেবল Kimi-এর model-গুলো প্রকাশিত হয়।

> ⚠️ **Non-Claude model-এ window-এর অমিল।** Claude Code তার অপরিচিত যেকোনো id-এর জন্য
> 200K context window ধরে নেয় (এটি `/v1/models` থেকে প্রকৃত window পড়তে পারে না)।
> বড় window-যুক্ত model-এর ক্ষেত্রে (যেমন Kimi K2-এর 256K), model-এর প্রকৃত window-এর
> চেয়ে কম একটি মানে `CLAUDE_CODE_AUTO_COMPACT_WINDOW` সেট করুন, যাতে
> auto-compaction সময়ের আগেই সক্রিয় না হয়। উপরে তৈরি হওয়া profile-গুলো ইতিমধ্যেই
> প্রতি model-এর জন্য এটি করে।

---

## ড্যাশবোর্ডে অনবোর্ডিং ব্লক

Claude টুল কার্ডটি (**Dashboard → CLI Code**) এই ইনস্ট্যান্সের জন্য নির্দিষ্ট `settings.json` ফ্র্যাগমেন্টটি
ডিসকভারি-অ্যালিয়াস তথ্য বোতামের পাশে একটি কপি বোতামসহ রেন্ডার করে:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<আপনার OmniRoute>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<আপনার OmniRoute API কী>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

বেস URL হলো কার্ডটি যে URL নির্ধারণ করেছে (আপনার টাইপ করা কাস্টম ওভাররাইডসহ), এবং এটি ইতিমধ্যেই
স্বাভাবিকীকৃত — কোনো `/v1` সাফিক্স বা শেষে স্ল্যাশ নেই। **কী কখনো রেন্ডার করা হয় না**: ব্লকটিতে একটি
প্লেসহোল্ডার থাকে, তাই কোনো স্ক্রিনশট বা পেস্ট করা স্নিপেটের মাধ্যমে কী ফাঁস হতে পারে না। এর জায়গায় আপনার কী পেস্ট করুন।

যেসব মডেলের প্রকৃত কনটেক্সট উইন্ডো 200K নয়, সেগুলোর জন্য একই `env` ব্লকের অধীনে
`CLAUDE_CODE_AUTO_COMPACT_WINDOW` যোগ করুন — Claude Code তার অপরিচিত প্রতিটি id-এর জন্য 200K ধরে নেয়, তাই
তা না করলে অটো-কমপ্যাকশন ভুল সময়ে সক্রিয় হয় (পূর্ববর্তী অংশের সতর্কতাটি দেখুন)।
স্নিপেট বিল্ডারও এই মান গ্রহণ করে, তাই টার্গেট মডেলের উইন্ডো জানা থাকলে
কলার এটি সরাসরি নির্গত করতে পারে।

উৎস: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (`ClaudeGatewayOnboardingBlock` দ্বারা রেন্ডার করা
বিশুদ্ধ বিল্ডার, যা ইউনিট-টেস্ট করা হয়েছে)।

---

## প্রোফাইলসমূহ (`CLAUDE_CONFIG_DIR`)

Claude Code-এ **কোনো নেটিভ প্রোফাইল ফাইল নেই** (Codex-এর `~/.codex/<name>.config.toml`-এর বিপরীতে)।
প্রচলিত পদ্ধতি হলো `CLAUDE_CONFIG_DIR` — প্রতিটি প্রোফাইলের জন্য একটি পৃথক কনফিগ ডিরেক্টরি,
যার প্রত্যেকটির নিজস্ব `settings.json`, ক্রেডেনশিয়াল, হিস্ট্রি ও ক্যাশ থাকে।

`omniroute setup-claude` লাইভ `/v1/models` ক্যাটালগ নিয়ে আসে এবং
`~/.claude/profiles/<name>/settings.json`-এ প্রতিটি মডেলের জন্য একটি করে প্রোফাইল লেখে,
**`setup-codex`-এর একই নামগুলো** পুনরায় ব্যবহার করে (`glm52`, `kimi-k27`, `deepseek-pro`, …):

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **অথ টোকেন কখনো প্রোফাইলে লেখা হয় না।** `omniroute launch --profile <name>` দিয়ে চালু করুন
> (এটি সক্রিয় কনটেক্সট থেকে `ANTHROPIC_AUTH_TOKEN` ইনজেক্ট করে), অথবা নিজে `ANTHROPIC_AUTH_TOKEN` এক্সপোর্ট করে
> `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude` চালান।

**মডেল ডিসকভারির পর অটো-সিঙ্ক (অপ্ট-ইন)।** যখনই কোনো প্রোভাইডার মডেল
সিঙ্ক লাইভ ক্যাটালগ পরিবর্তন করে, OmniRoute স্বয়ংক্রিয়ভাবে একই
`~/.claude/profiles/<name>/settings.json` ফাইলগুলো পুনরায় তৈরি করতে পারে — ফলে কমান্ডটি আবার না চালিয়েই
নতুন/নাম-পরিবর্তিত মডেলগুলোর প্রোফাইল পাওয়া যায়। এটি **ডিফল্টভাবে বন্ধ থাকে**: **CLI Code dashboard** থেকে এটি টগল করুন ("CLI profile
auto-sync" → Claude Code), অথবা `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` সেট করুন (এটি
ডিফল্টভাবে চালু থাকা `CLI_ALLOW_CONFIG_WRITES`-কেও সম্মান করে)। সক্রিয় করা হলে এটি শুধু প্রোফাইল ফাইল লেখে; এটি কখনোই
আপনার সক্রিয়/ডিফল্ট Claude কনফিগ, অথ বা `~/.claude/settings.json` পরিবর্তন করে না।

### প্রোফাইল তৈরি + ব্যবহার

```bash
# লোকাল OmniRoute
omniroute setup-claude

# রিমোট VPS (প্রতিটি প্রোফাইলে VPS URL স্থায়ীভাবে অন্তর্ভুক্ত করে)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# শুধু কিছু প্রোভাইডার
omniroute setup-claude --only glm,kimi

# না লিখে প্রিভিউ
omniroute setup-claude --dry-run

# একটি প্রোফাইল চালু করুন
omniroute launch --profile kimi-k27
```

---

## মডেল স্তরসমূহ (ঐচ্ছিক)

Claude Code সক্ষমতার স্তর অনুযায়ী রুট করে। প্রতিটি স্তরের জন্য ভিন্ন প্রদানকারী ব্যবহার করতে চাইলে env /
settings-এর মাধ্যমে সেটিকে একটি OmniRoute মডেলের সঙ্গে ম্যাপ করুন:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

অন্যথায় সবকিছুর জন্য একটি একক `ANTHROPIC_MODEL` (যা প্রোফাইলগুলো সেট করে) ব্যবহার করা হয়।

---

## রিমোট মোড

একবার `omniroute connect <host>` চালানোর পর (দেখুন
[রিমোট মোড](./REMOTE-MODE.md)), `omniroute launch` এবং `omniroute setup-claude`
স্বয়ংক্রিয়ভাবে সেই রিমোট সার্ভারকে লক্ষ্য করে এবং সেটির নির্দিষ্ট-পরিসরের অ্যাক্সেস টোকেন ব্যবহার করে — কোনো
অতিরিক্ত ফ্ল্যাগের প্রয়োজন নেই। প্রতিটি পৃথক আহ্বানের জন্য `--remote` / `--api-key` দিয়ে ওভাররাইড করুন।

---

## সমস্যা সমাধান

**Claude Code গেটওয়ে উপেক্ষা করে** — নিশ্চিত করুন `ANTHROPIC_BASE_URL`-এ **কোনো
`/v1` নেই** এবং `claude` পুনরায় চালু করুন (স্টার্টআপের সময় env একবারই পড়া হয়)। `omniroute launch`
আপনার হয়ে এটি পরিচালনা করে।

**`/model` পিকার খালি / গেটওয়ে মডেল অনুপস্থিত** — Claude Code
v2.1.219+ এবং `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` প্রয়োজন। পিকারে কেবল `claude*` /
`anthropic*` মডেল ID দেখা যায়; অন্য যেকোনো মডেল `ANTHROPIC_MODEL=<id>` দিয়ে
জোরপূর্বক ব্যবহার করুন (প্রোফাইলগুলো এভাবেই কাজ করে)।

**`400 Ambiguous model 'claude-…'`** — Claude Code সর্বদা **প্রিফিক্সবিহীন**
মডেল ID পাঠায় (যেমন `claude-opus-4-8`), তাই Claude Code (`cc/…`) এবং
Claude (`claude/…`) উভয় প্রদানকারী সংযুক্ত থাকলে প্রিফিক্সবিহীন ID দুটি রুটের সঙ্গে মিলে যায় এবং
OmniRoute অনুমান করতে অস্বীকার করে। যেকোনো একভাবে এটি ঠিক করুন: `ANTHROPIC_MODEL=cc/claude-opus-4-8`
দিয়ে একটি প্রিফিক্সযুক্ত ID পিন করুন, অথবা **প্রিফিক্সবিহীন Claude মডেলের জন্য Claude Code-কে
অগ্রাধিকার দিন** সক্ষম করুন — Claude প্রদানকারী পৃষ্ঠার টগলটি, অথবা
`OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` (ডিফল্টরূপে বন্ধ;
দেখুন [পরিবেশ](../reference/ENVIRONMENT.md)) — যা প্রিফিক্সবিহীন `claude-*`
ID-গুলোকে পরিবর্তে Claude Code-এ রুট করে। স্পষ্ট প্রদানকারী প্রিফিক্স সবসময় অগ্রাধিকার পায়।

**প্রমাণীকরণ ত্রুটি** — প্রোফাইলে কোনো টোকেন থাকে না। `omniroute launch --profile`
ব্যবহার করুন (এটি টোকেন ইনজেক্ট করে) অথবা `ANTHROPIC_AUTH_TOKEN` এক্সপোর্ট করুন।

**প্রোফাইলগুলো বিচ্ছিন্ন থাকে না** — প্রতিটি প্রোফাইল একটি স্বতন্ত্র `CLAUDE_CONFIG_DIR`;
সেশনের ভেতরে `echo $CLAUDE_CONFIG_DIR` চালিয়ে যাচাই করুন যে এটি
`~/.claude/profiles/<name>` নির্দেশ করছে।
