# Embedded Services (Ελληνικά)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Έκδοση:** v3.8.44
> **Τελευταία ενημέρωση:** 2026-09-09
> **Απευθύνεται σε:** Μηχανικούς που προσθέτουν, συντηρούν ή αποσφαλματώνουν ενσωματωμένες υπηρεσίες (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Οι ενσωματωμένες υπηρεσίες είναι τοπικά εγκατεστημένα βοηθητικά εργαλεία διεργασιών, τα οποία το OmniRoute εγκαθιστά, εποπτεύει και
εκθέτει ως πλήρως υποστηριζόμενους προορισμούς δρομολόγησης. Σε αντίθεση με τους εξωτερικούς παρόχους (στους οποίους η πρόσβαση γίνεται μέσω διαδικτύου
με κλειδιά API), οι ενσωματωμένες υπηρεσίες εκτελούνται στον ίδιο υπολογιστή με το OmniRoute και επικοινωνούν μέσω της διεπαφής loopback.

---

## Πίνακας περιεχομένων

1. [Επισκόπηση](#1-overview)
2. [Αρχιτεκτονική — 4 επίπεδα](#2-architecture--4-layers)
3. [Μηχανή καταστάσεων κύκλου ζωής](#3-lifecycle-state-machine)
4. [Αναφορά API](#4-api-reference)
5. [Ασφάλεια](#5-security)
6. [Προσθήκη νέας ενσωματωμένης υπηρεσίας](#6-adding-a-new-embedded-service)
7. [Αντιμετώπιση προβλημάτων](#7-troubleshooting)
8. [Συχνές ερωτήσεις](#8-faq)

---

## 1. Επισκόπηση

### Γιατί ενσωματωμένες υπηρεσίες;

Έχουν ενσωματωθεί έξι υπηρεσίες:

| Υπηρεσία        | Πακέτο npm                                    | Προεπιλεγμένη θύρα | Σκοπός                                                                                                                                                                                                                               |
| --------------- | --------------------------------------------- | :----------------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **9Router**     | `9router`                                     |       20130        | Δρομολογητής AI που το OmniRoute μπορεί να χρησιμοποιήσει ως δευτερεύοντα πάροχο. Τα μοντέλα εκτίθενται ως `9router/{sub}/{model}`                                                                                                   |
| **CLIProxyAPI** | Δυαδικό αρχείο έκδοσης GitHub (`cliproxy`)    |        8317        | Τοπικός προσαρμογέας διακομιστή μεσολάβησης για ροές ελέγχου ταυτότητας του Anthropic CLI. Παρέχει εφεδρική δρομολόγηση όταν λήγουν τα διακριτικά OAuth                                                                              |
| **Mux**         | `mux` (`mux server` χωρίς γραφικό περιβάλλον) |        8322        | Τοπικός δαίμονας ενορχήστρωσης πρακτόρων (coder/mux). Γίνεται μόνο διαχείριση του κύκλου ζωής — δεν αποτελεί προορισμό δρομολόγησης (δεν μεσολαβεί αιτήματα LLM).                                                                    |
| **Bifrost**     | `@maximhq/bifrost`                            |        8080        | Backend αναμετάδοσης πύλης AI σε Go. Όταν εκτελείται, επιλέγεται αυτόματα από τη διαδρομή αναμετάδοσης (`/v1/relay/`)                                                                                                                |
| **Dario**       | `@askalf/dario`                               |        3456        | Διακομιστής μεσολάβησης συνδρομής Claude — εναλλακτική/εφεδρική λύση του CLIProxyAPI για κίνηση με μορφή Claude Code· το εισαγόμενο κλειδί γίνεται `DARIO_ADMIN_TOKEN` και ελέγχει την πρόσβαση στο επίπεδο ελέγχου OAuth `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`                        |        8323        | Αυτοματοποίηση WhatsApp Web (Chromium χωρίς γραφικό περιβάλλον μέσω Puppeteer). Γίνεται μόνο διαχείριση του κύκλου ζωής — δεν αποτελεί προορισμό δρομολόγησης.                                                                       |

Και οι έξι ακολουθούν το ίδιο μοντέλο εποπτείας:

- Το OmniRoute τις εγκαθιστά στον κατάλογο `DATA_DIR/services/{name}/` (απομονωμένες από το `package.json` του OmniRoute)
- Το OmniRoute τις εκκινεί ως θυγατρικές διεργασίες και τις παρακολουθεί
- Το OmniRoute εισάγει ένα προσωρινό κλειδί API στο περιβάλλον της θυγατρικής διεργασίας και το ανανεώνει χωρίς διακοπή λειτουργίας (όπου εφαρμόζεται)
- Όλες οι διαδρομές διαχείρισης (`/api/services/*`) είναι **LOCAL_ONLY** — προσβάσιμες μόνο μέσω loopback (αυστηρός κανόνας #17)

### Βασικές αποφάσεις (από το σχέδιο σχεδιασμού)

| Απόφαση                                                | Τιμή                                                                                                                       |
| ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| Πρόσβαση του πίνακα ελέγχου στο εγγενές UI του 9Router | Αντίστροφος διακομιστής μεσολάβησης στη διαδρομή `/dashboard/providers/services/9router/embed/*`                           |
| Μηχανισμός εγκατάστασης                                | `npm install {package}` μέσω `execFile` (χωρίς παρεμβολή κελύφους)                                                         |
| Τρόπος χρήσης                                          | Ο πάροχος καταχωρίζεται ως `9router/{sub}/{model}` στη μηχανή δρομολόγησης                                                 |
| Διαχείριση κλειδιού API                                | Το OmniRoute το δημιουργεί, το κρυπτογραφεί κατά την αποθήκευση (AES-256-GCM) και το εισάγει μέσω μεταβλητής περιβάλλοντος |
| Θέση στον πίνακα ελέγχου                               | `/dashboard/providers/services` (τρεις καρτέλες)                                                                           |
| Αυτόματη εκκίνηση                                      | Διακόπτης ανά υπηρεσία, προεπιλεγμένα ΑΝΕΝΕΡΓΟΣ                                                                            |

---

## 2. Αρχιτεκτονική — 4 επίπεδα

```
┌────────────────────────────────────────────────────────────────────┐
│  Επίπεδο 1 — UI                                                    │
│  /dashboard/providers/services  (καρτέλες: CLIProxyAPI | 9Router | Mux)│
│  Ζωντανά αρχεία καταγραφής (SSE), Έναρξη/Διακοπή/Επανεκκίνηση/Ενημέρωση, Ρυθμίσεις, Εγκατάσταση│
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Κέλυφος + δρομολόγηση καρτελών μέσω ?tab=│
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Επίπεδο 2 — API (LOCAL_ONLY — μόνο loopback)                      │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (αντίστροφος διακομιστής μεσολάβησης HTTP + WebSocket → upstream 9Router)│
│                                                                    │
│  Πύλη: το LOCAL_ONLY_API_PREFIXES περιλαμβάνει τα "/api/services/" και│
│        "/dashboard/providers/services/*/embed/"                    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ κλήσεις εντός διεργασίας
┌──────────────────────▼─────────────────────────────────────────────┐
│  Επίπεδο 3 — ServiceSupervisor (src/lib/services/)                 │
│                                                                    │
│  ServiceSupervisor.ts   Γενικός επόπτης (child_process.spawn)      │
│    ├── εγκατάσταση: execFile('npm', ['install', pkg, '--prefix'])  │
│    ├── έναρξη:      spawn(node, [entrypoint], {env, cwd})          │
│    ├── api_key:     crypto.randomBytes(32) → env NINEROUTER_API_KEY│
│    ├── θύρα:        20130 για το 9Router (διαμορφώσιμη)            │
│    ├── καταγραφές:  κυκλική μνήμη stdio 5 MB → συμβάντα SSE       │
│    ├── υγεία:       HTTP GET /health κάθε 2–5 s, ανάκτηση κατ’ απαίτηση│
│    └── κύκλος ζωής: SIGTERM 15 s → SIGKILL                         │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Εκκινεί όλα τα SERVICES[] κατά την έναρξη της διεργασίας│
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       Περιοδικό GET /v1/models → πίνακας service_models│
│  ringBuffer.ts      Κυκλική μνήμη καταγραφής (5 MB ανά υπηρεσία)   │
│  healthCheck.ts     Περιοδικός έλεγχος υγείας μέσω HTTP            │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (προσαρμογείς εγκατάστασης)                   │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP συμβατό με OpenAI (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Επίπεδο 4 — Πάροχος / Δρομολόγηση                                │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Αναζητά ξανά τη θύρα και το κλειδί API ανά αίτημα (χωρίς προσωρινή αποθήκευση).│
│    Αφαιρεί το πρόθεμα "9router/" από το αναγνωριστικό μοντέλου πριν από τη μεσολάβηση.│
│    Επιστρέφει 503 service_not_running αν ο επόπτης δεν βρίσκεται σε κατάσταση "running".│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Καταχώριση για το "9router": isEmbeddedService: true            │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Τα μοντέλα αποθηκεύονται ως "9router/{sub}/{model}" (με πρόθεμα).│
│    Συγχρονίζονται κάθε 5 λεπτά από το modelSync.ts.                │
│                                                                    │
│  Το Mux διαθέτει ΜΟΝΟ διαχείριση κύκλου ζωής (Επίπεδα 1-3) — είναι │
│  daemon ενορχήστρωσης agent και όχι διακομιστής μεσολάβησης LLM,   │
│  επομένως δεν έχει εκτελεστή/καταχώριση παρόχου στο Επίπεδο 4 και  │
│  δεν αποτελεί ποτέ στόχο δρομολόγησης.                             │
└────────────────────────────────────────────────────────────────────┘
```

### Βασικά αρχεία πηγαίου κώδικα

| Αρχείο                                      | Ρόλος                                                           |
| ------------------------------------------- | --------------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Βασική κλάση: κύκλος ζωής, κλείδωμα, υγεία, δακτυλιοειδής μνήμη |
| `src/lib/services/bootstrap.ts`             | Καταχώριση σε επίπεδο διεργασίας και αυτόματη εκκίνηση          |
| `src/lib/services/registry.ts`              | Μοναδικός χάρτης `tool → supervisor`                            |
| `src/lib/services/apiKey.ts`                | Δημιουργία κλειδιών, κρυπτογράφηση AES-256-GCM σε αδράνεια      |
| `src/lib/services/modelSync.ts`             | Περιοδικός συγχρονισμός μοντέλων (5 λεπτά) + κατ’ απαίτηση      |
| `src/lib/services/ringBuffer.ts`            | Κυκλική ενδιάμεση μνήμη καταγραφών 5 MB με συνδρομή SSE         |
| `src/lib/services/healthCheck.ts`           | Έλεγχος υγείας HTTP (διαμορφώσιμο χρονικό διάστημα)             |
| `src/lib/services/installers/ninerouter.ts` | Εγκατάσταση/ενημέρωση/απεγκατάσταση μέσω npm για το 9Router     |
| `src/lib/services/installers/cliproxy.ts`   | Εγκατάσταση/ενημέρωση/απεγκατάσταση μέσω npm για το CLIProxyAPI |
| `src/lib/services/installers/mux.ts`        | Εγκατάσταση/ενημέρωση/απεγκατάσταση μέσω npm για το Mux         |
| `src/lib/services/installers/openwa.ts`     | Εγκατάσταση/ενημέρωση/απεγκατάσταση μέσω npm για το open-wa     |
| `src/app/api/services/9router/_lib.ts`      | Βοηθητική συνάρτηση `getOrInitSupervisor()`                     |
| `src/app/api/services/[name]/logs/route.ts` | Κοινόχρηστο τελικό σημείο καταγραφών SSE                        |
| `open-sse/executors/ninerouter.ts`          | Εκτελεστής παρόχου (Επίπεδο 4)                                  |

---

## 3. Μηχανή καταστάσεων κύκλου ζωής

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
             επιτυχής έλεγχος υγείας │         κατάρρευση / SIGTERM
                               ┌────▼─────┐  (έξοδος εντός 5 s) │
                               │ running  │── κατάρρευση ───────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Οι καταστάσεις αποθηκεύονται στον πίνακα `version_manager` της βάσης δεδομένων
(στήλη `status`) και αντικατοπτρίζονται στην κατάσταση μνήμης του
`ServiceSupervisor`. Η κατάσταση στη μνήμη είναι η έγκυρη πηγή για μια διεργασία
που εκτελείται· η κατάσταση της βάσης δεδομένων αποτελεί τη μόνιμη εφεδρική
πηγή κατά την εκκίνηση.

### Μεταβάσεις καταστάσεων

| Από             | Συμβάν                                       | Προς                        |
| --------------- | -------------------------------------------- | --------------------------- |
| `not_installed` | το `install()` ολοκληρώνεται επιτυχώς        | `stopped`                   |
| `stopped`       | καλείται το `start()`                        | `starting`                  |
| `starting`      | ο έλεγχος υγείας επιστρέφει 200              | `running`                   |
| `starting`      | η διεργασία τερματίζεται πριν καταστεί υγιής | `error`                     |
| `running`       | καλείται το `stop()`                         | `stopping` → `stopped`      |
| `running`       | η διεργασία τερματίζεται απροσδόκητα (< 5 s) | `error` (ταχεία κατάρρευση) |
| `running`       | η διεργασία τερματίζεται απροσδόκητα (> 5 s) | `error`                     |
| `error`         | καλείται το `start()`                        | `starting`                  |
| οποιαδήποτε     | `stop()` ενώ βρίσκεται σε `stopping`         | καμία ενέργεια              |

### Κλείδωμα λειτουργίας

Το `ServiceSupervisor` σειριοποιεί τις λειτουργίες κύκλου ζωής μέσω ενός ασύγχρονου
κλειδώματος λειτουργίας (`withLock()`). Οι ταυτόχρονες κλήσεις `start()` στον ίδιο
επόπτη έχουν ως αποτέλεσμα ακριβώς μία δημιουργία διεργασίας· ο δεύτερος καλών
περιμένει και λαμβάνει την υπάρχουσα κατάσταση. Αυτό αποτρέπει συνθήκες
ανταγωνισμού όταν, για παράδειγμα, η αυτόματη εκκίνηση και ένα κουμπί του
περιβάλλοντος χρήστη ενεργοποιούνται ταυτόχρονα.

---

## 4. Αναφορά API

Όλες οι διαδρομές κάτω από το `/api/services/` είναι **LOCAL_ONLY** (μόνο loopback, αυστηρός κανόνας #17).
Τα αιτήματα που δεν προέρχονται από loopback λαμβάνουν `403 LOCAL_ONLY` ανεξάρτητα από το διακριτικό ελέγχου ταυτότητας.

### 4.1 Τελικά σημεία του 9Router (11 διαδρομές)

#### `POST /api/services/9router/install`

Εγκαθιστά το 9Router από το npm. Δημιουργεί το `DATA_DIR/services/9router/` με δικά του
`package.json` και `node_modules/`. Δεν έρχεται σε διένεξη με τις εξαρτήσεις του OmniRoute.

**Σώμα αιτήματος** (όλα προαιρετικά):

```json
{ "version": "latest" }
```

| Πεδίο     | Τύπος    | Προεπιλογή | Περιγραφή                                     |
| --------- | -------- | ---------- | --------------------------------------------- |
| `version` | `string` | `"latest"` | Ετικέτα έκδοσης npm ή semver προς εγκατάσταση |

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή                                                                          |
| --------- | ---------------------------------------------------------------------------------- |
| `200`     | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                             |
| `400`     | Μη έγκυρο σώμα αιτήματος (αποτυχία επικύρωσης Zod)                                 |
| `409`     | Η εγκατάσταση βρίσκεται ήδη σε εξέλιξη (το κλείδωμα διατηρείται)                   |
| `500`     | Η εγκατάσταση μέσω npm απέτυχε — δείτε το `message` για κατανοητό μήνυμα σφάλματος |

**Σημειώσεις:** Χρησιμοποιεί `execFile('npm', [...])` — χωρίς shell και χωρίς παρεμβολή (αυστηρός κανόνας #13).
Τα σφάλματα EACCES εμφανίζονται ως κατανοητά μηνύματα.

---

#### `POST /api/services/9router/start`

Εκκινεί το 9Router. Καταχωρίζει έναν επόπτη, αν δεν έχει ήδη καταχωριστεί, και έπειτα καλεί
τη `supervisor.start()`. Είναι ιδιοδύναμο όταν εκτελείται ήδη.

**Σώμα αιτήματος:** κανένα

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή                                                      |
| --------- | -------------------------------------------------------------- |
| `200`     | Αντικείμενο `ServiceStatus` (δείτε το σχήμα παρακάτω)          |
| `409`     | Το 9Router δεν είναι εγκατεστημένο (`status: "not_installed"`) |
| `503`     | Η εκκίνηση απέτυχε (σφάλμα διεργασίας — δείτε το `lastError`)  |

**Σχήμα ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Τερματίζει ομαλά το 9Router. Στέλνει SIGTERM, περιμένει 15 s και έπειτα στέλνει SIGKILL, αν εξακολουθεί να εκτελείται.
Είναι ιδιοδύναμο όταν έχει ήδη τερματιστεί.

**Σώμα αιτήματος:** κανένα

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή                          |
| --------- | ---------------------------------- |
| `200`     | `ServiceStatus` (state: "stopped") |
| `503`     | Ο τερματισμός απέτυχε απροσδόκητα  |

---

#### `POST /api/services/9router/restart`

Ισοδυναμεί με `stop()` και έπειτα `start()` υπό το κλείδωμα λειτουργίας.

**Σώμα αιτήματος:** κανένα

**Αποκρίσεις:** ίδιες με το `start` (επιστρέφει το τελικό `ServiceStatus`).

---

#### `POST /api/services/9router/update`

Ενημερώνει το 9Router σε νεότερη έκδοση npm. Αν η υπηρεσία εκτελείται, τερματίζεται
πρώτα, εκτελείται η εγκατάσταση μέσω npm (εγκαθιστώντας τη νεότερη έκδοση επί τόπου) και έπειτα
η υπηρεσία επανεκκινείται.

**Σώμα αιτήματος** (όλα προαιρετικά):

```json
{ "version": "latest" }
```

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή                                                       |
| --------- | --------------------------------------------------------------- |
| `200`     | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`     | Μη έγκυρο σώμα                                                  |
| `500`     | Η ενημέρωση μέσω npm απέτυχε                                    |

---

#### `POST /api/services/9router/rotate-key`

Δημιουργεί ένα νέο κλειδί API για το 9Router, το κρυπτογραφεί κατά την αποθήκευση και επανεκκινεί την υπηρεσία
(αν εκτελείται), ώστε να λάβει το νέο κλειδί από το περιβάλλον της. Το παλιό κλειδί
ακυρώνεται αμέσως.

**Σώμα αιτήματος:** κανένα

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή                                  |
| --------- | ------------------------------------------ |
| `200`     | `{ keyRotated: true, restarted: boolean }` |
| `500`     | Η εναλλαγή κλειδιού απέτυχε                |

**Ασφάλεια:** Το νέο κλειδί δεν επιστρέφεται ποτέ στην απόκριση (καμία διαρροή διαπιστευτηρίων).
Αποθηκεύεται κρυπτογραφημένο (AES-256-GCM) στον πίνακα `version_manager`.

---

#### `GET /api/services/9router/status`

Επιστρέφει συνδυασμένη κατάσταση σε πραγματικό χρόνο και από τη βάση δεδομένων, συμπεριλαμβανομένων των μεταδεδομένων έκδοσης και μιας προεπισκόπησης του κλειδιού API.

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή                     |
| --------- | ----------------------------- |
| `200`     | Δείτε το σχήμα παρακάτω       |
| `500`     | Η ανάγνωση κατάστασης απέτυχε |

**Σχήμα απόκρισης:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Εναλλάσσει τη σημαία αυτόματης εκκίνησης. Όταν `enabled: true`, η υπηρεσία εκκινείται αυτόματα
την επόμενη φορά που εκκινείται το OmniRoute (αν η υπηρεσία είναι εγκατεστημένη).

**Σώμα αιτήματος:**

```json
{ "enabled": true }
```

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή             |
| --------- | --------------------- |
| `200`     | `{ autoStart: true }` |
| `400`     | Μη έγκυρο σώμα        |

---

#### `GET /api/services/9router/logs`

Ροή SSE ζωντανών αρχείων καταγραφής από την κυκλική ενδιάμεση μνήμη stdout/stderr του 9Router.

**Παράμετροι ερωτήματος:**

| Παράμετρος | Τύπος     | Προεπιλογή | Περιγραφή                                                                                   |
| ---------- | --------- | ---------- | ------------------------------------------------------------------------------------------- |
| `tail`     | `integer` | 200        | Πόσες ιστορικές γραμμές θα σταλούν πρώτα (μέγιστο 1000)                                     |
| `filter`   | `string`  | καμία      | Φίλτρο υποσυμβολοσειράς χωρίς διάκριση πεζών-κεφαλαίων (χωρίς regex — ασφαλές έναντι ReDoS) |

**Συμβάντα SSE:**

| Συμβάν      | Δεδομένα    | Περιγραφή                    |
| ----------- | ----------- | ---------------------------- |
| `snapshot`  | `LogLine[]` | Αρχικό ιστορικό τμήμα        |
| `log`       | `LogLine`   | Ζωντανή γραμμή καταγραφής    |
| `heartbeat` | `{}`        | Διατήρηση σύνδεσης κάθε 15 s |

**Σχήμα LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Αποκρίσεις:**

| Κατάσταση | Περιγραφή                                                  |
| --------- | ---------------------------------------------------------- |
| `200`     | `text/event-stream`                                        |
| `400`     | Η παράμετρος `filter` είναι πολύ μεγάλη (> 200 χαρακτήρες) |
| `404`     | Η υπηρεσία δεν βρέθηκε (ο επόπτης δεν έχει καταχωριστεί)   |

---

### 4.2 Τερματικά σημεία του CLIProxyAPI (10 διαδρομές)

Το CLIProxyAPI έχει την ίδια δομή τερματικών σημείων με το 9Router, χωρίς το
`rotate-key`, και επιπλέον τα `accounts`, `provider-expose` και
`auto-restart-adopted`. Πλέον λαμβάνει ένα αποκλειστικό κλειδί API επιπέδου δεδομένων,
το οποίο εισάγεται κατά την εκκίνηση (`needsApiKey: true` στο
`bootstrap.ts`, χρησιμοποιείται για τον συγχρονισμό μοντέλων)· το `status`
περιλαμβάνει λιγότερα πεδία.

| Μέθοδος | Διαδρομή                            | Περιγραφή                                                 |
| ------- | ----------------------------------- | --------------------------------------------------------- |
| `POST`  | `/api/services/cliproxy/install`    | Εγκατάσταση του CLIProxyAPI από το npm                    |
| `POST`  | `/api/services/cliproxy/start`      | Εκκίνηση του CLIProxyAPI                                  |
| `POST`  | `/api/services/cliproxy/stop`       | Διακοπή του CLIProxyAPI                                   |
| `POST`  | `/api/services/cliproxy/restart`    | Επανεκκίνηση του CLIProxyAPI                              |
| `POST`  | `/api/services/cliproxy/update`     | Ενημέρωση σε νεότερη έκδοση                               |
| `GET`   | `/api/services/cliproxy/status`     | Κατάσταση σε πραγματικό χρόνο + ΒΔ (χωρίς `apiKeyMasked`) |
| `POST`  | `/api/services/cliproxy/auto-start` | Εναλλαγή αυτόματης εκκίνησης                              |

Το κοινόχρηστο τερματικό σημείο `GET /api/services/{name}/logs` (βλ. §4.1) λειτουργεί
και για τις τέσσερις υπηρεσίες, χρησιμοποιώντας το δυναμικό τμήμα `[name]`.

---

### 4.3 Τερματικά σημεία του Mux (8 διαδρομές)

Το Mux έχει την ίδια δομή τερματικών σημείων με το CLIProxyAPI — δεν υπάρχει διαδρομή
`rotate-key` στην επιφάνεια του API (το διακριτικό bearer δημιουργείται με τον ίδιο
τρόπο όπως στο 9Router, μέσω του `getOrCreateApiKey("mux")`, και εισάγεται μέσω της
μεταβλητής περιβάλλοντος `MUX_SERVER_AUTH_TOKEN`, αλλά δεν υπάρχει ακόμη αποκλειστικό
τερματικό σημείο εναλλαγής). Το Mux υπόκειται μόνο σε διαχείριση κύκλου ζωής: σε
αντίθεση με το 9Router, δεν διαθέτει εκτελεστή Επιπέδου 4 και δεν καταχωρίζεται ποτέ
ως πάροχος δρομολόγησης.

| Μέθοδος | Διαδρομή                       | Περιγραφή                                    |
| ------- | ------------------------------ | -------------------------------------------- |
| `POST`  | `/api/services/mux/install`    | Εγκατάσταση του Mux από το npm (`npm i mux`) |
| `POST`  | `/api/services/mux/start`      | Εκκίνηση του Mux (`mux server`)              |
| `POST`  | `/api/services/mux/stop`       | Διακοπή του Mux                              |
| `POST`  | `/api/services/mux/restart`    | Επανεκκίνηση του Mux                         |
| `POST`  | `/api/services/mux/update`     | Ενημέρωση σε νεότερη έκδοση npm              |
| `GET`   | `/api/services/mux/status`     | Κατάσταση σε πραγματικό χρόνο + ΒΔ           |
| `POST`  | `/api/services/mux/auto-start` | Εναλλαγή αυτόματης εκκίνησης                 |

---

### 4.4 Τερματικά σημεία του Bifrost (8 διαδρομές)

Το Bifrost είναι ένα backend αναμετάδοσης πύλης AI σε Go (`@maximhq/bifrost`).
Χρησιμοποιεί την ίδια δομή τερματικών σημείων με το CLIProxyAPI (χωρίς `rotate-key` —
το Bifrost διαχειρίζεται τα δικά του κλειδιά παρόχων στο `config.json`, κάτω από το
`-app-dir`).

| Μέθοδος | Διαδρομή                           | Περιγραφή                                                                                                 |
| ------- | ---------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `POST`  | `/api/services/bifrost/install`    | Εγκατάσταση του Bifrost από το npm (`@maximhq/bifrost`)                                                   |
| `POST`  | `/api/services/bifrost/start`      | Εκκίνηση του Bifrost στη θύρα 8080 (προεπιλογή)                                                           |
| `POST`  | `/api/services/bifrost/stop`       | Διακοπή του Bifrost                                                                                       |
| `POST`  | `/api/services/bifrost/restart`    | Επανεκκίνηση του Bifrost                                                                                  |
| `POST`  | `/api/services/bifrost/update`     | Ενημέρωση σε νεότερη έκδοση                                                                               |
| `GET`   | `/api/services/bifrost/status`     | Κατάσταση σε πραγματικό χρόνο + ΒΔ                                                                        |
| `POST`  | `/api/services/bifrost/auto-start` | Εναλλαγή αυτόματης εκκίνησης                                                                              |
| `GET`   | `/api/services/bifrost/logs`       | Παρακολούθηση ουράς αρχείου καταγραφής μέσω SSE (μέσω της κοινόχρηστης δυναμικής διαδρομής `[name]/logs`) |

**Σύνδεση δρομολόγησης:** Όταν το `BIFROST_BASE_URL` δεν έχει οριστεί και η
εποπτευόμενη διεργασία του Bifrost εκτελείται, το `getBifrostRoutingConfig()` (στο
`routingBackend.ts`) χρησιμοποιεί αυτόματα το `http://127.0.0.1:{port}` ως βασικό URL
αναμετάδοσης. Η ρητά ορισμένη μεταβλητή περιβάλλοντος `BIFROST_BASE_URL` έχει πάντα
προτεραιότητα.

---

### 4.5 Τερματικά σημεία του Dario (12 διαδρομές)

Ίδια δομή κύκλου ζωής με τις άλλες υπηρεσίες (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`), συν ένα επίπεδο ελέγχου
OAuth με προστασία μέσω διακριτικού, κάτω από το `admin/`: `admin/accounts`,
`admin/import-from-omniroute`, `admin/login-start`, `admin/login-complete` (όλα
προστατεύονται από το `DARIO_ADMIN_TOKEN`).

### 4.6 Τερματικά σημεία του open-wa (7 διαδρομές)

Το open-wa (`@open-wa/wa-automate`) ελέγχει μια ακέφαλη διεργασία Chromium (μέσω
Puppeteer) για την αυτοματοποίηση του WhatsApp Web. Χρησιμοποιεί την ίδια δομή
τερματικών σημείων με το Mux (δεν υπάρχει ακόμη διαδρομή `rotate-key`). Υπόκειται
μόνο σε διαχείριση κύκλου ζωής — δεν αποτελεί προορισμό δρομολόγησης και δεν διαθέτει
εκτελεστή Επιπέδου 4 ούτε καταχώριση παρόχου.

| Μέθοδος | Διαδρομή                          | Περιγραφή                                                                                  |
| ------- | --------------------------------- | ------------------------------------------------------------------------------------------ |
| `POST`  | `/api/services/openwa/install`    | Εγκατάσταση του open-wa από το npm (`@open-wa/wa-automate`)                                |
| `POST`  | `/api/services/openwa/start`      | Εκκίνηση του open-wa στη θύρα 8323 (προεπιλογή)                                            |
| `POST`  | `/api/services/openwa/stop`       | Διακοπή του open-wa                                                                        |
| `POST`  | `/api/services/openwa/restart`    | Επανεκκίνηση του open-wa                                                                   |
| `POST`  | `/api/services/openwa/update`     | Ενημέρωση σε νεότερη έκδοση                                                                |
| `GET`   | `/api/services/openwa/status`     | Κατάσταση σε πραγματικό χρόνο + κατάσταση στη βάση δεδομένων                               |
| `POST`  | `/api/services/openwa/auto-start` | Ενεργοποίηση/απενεργοποίηση αυτόματης εκκίνησης                                            |
| `GET`   | `/api/services/openwa/logs`       | Ροή SSE των πρόσφατων καταγραφών (μέσω της κοινόχρηστης δυναμικής διαδρομής `[name]/logs`) |

**Κλειδί API:** εισάγεται ως `WA_KEY` — η γενική παράκαμψη μέσω μεταβλητών περιβάλλοντος του open-wa με πρόθεμα `WA_*`
το αντιστοιχίζει στην επιλογή CLI `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, επαληθευμένο με βάση το εγκατεστημένο πακέτο 4.76.0).
Προστίθεται το πρόθεμα `ow_` όταν δημιουργείται από τη `generateServiceApiKey()`. Το open-wa
διαβάζει ξανά το κλειδί από μια κεφαλίδα HTTP `key`/`api_key` (όχι από
`Authorization: Bearer`)· το `/api-docs*` εξαιρείται ρητά από τον έλεγχο
(`setupAuthenticationLayer` στο `dist/cli/server.js`), επομένως ο έλεγχος εύρυθμης λειτουργίας
δεν χρειάζεται κεφαλίδα ελέγχου ταυτότητας.

**Σύζευξη:** το open-wa είναι ανεπίσημο και δεν συνδέεται με το WhatsApp — ο
συνδεδεμένος αριθμός διατρέχει κίνδυνο αποκλεισμού από τον μηχανισμό ανίχνευσης αυτοματισμών του ίδιου του WhatsApp.
Κατά την πρώτη εκκίνηση, ο κωδικός QR σύζευξης εκτυπώνεται στο stdout και προβάλλεται μέσω
του υπάρχοντος πίνακα καταγραφών/της ροής SSE — δεν υπάρχει ακόμη ειδικό endpoint εικόνας QR
σε αυτήν την ενσωμάτωση.

---

### 4.7 Αντίστροφος διακομιστής μεσολάβησης (ενσωμάτωση πίνακα ελέγχου 9Router)

Ο πίνακας ελέγχου ενσωματώνει το περιβάλλον web του 9Router μέσα σε ένα iframe μέσω ενός εσωτερικού αντίστροφου
διακομιστή μεσολάβησης στη διεύθυνση:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Αυτός ο διακομιστής μεσολάβησης:

- Προωθεί το αίτημα στο `http://127.0.0.1:{port}/{path}` (μόνο μέσω loopback)
- Αφαιρεί τις εισερχόμενες κεφαλίδες `cookie` και `authorization` (καμία διαρροή της συνεδρίας OmniRoute)
- Εισάγει `Authorization: Bearer {apiKey}` για τον έλεγχο ταυτότητας του 9Router
- Αφαιρεί τα `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` από την απόκριση
- Επανεγγράφει τις αποκρίσεις HTML ώστε να εισάγει το `<base href>` και να κανονικοποιεί τις απόλυτες διαδρομές (`/foo` → `/dashboard/.../embed/foo`)

Οι αναβαθμίσεις WebSocket για τον ενσωματωμένο πίνακα ελέγχου διεκπεραιώνονται από έναν συνοδευτικό διακομιστή σε
μια αποκλειστική θύρα (βλ. `src/lib/services/embedWsProxy.ts`).

**Ασφάλεια:** Οι διαδρομές του διακομιστή μεσολάβησης ενσωμάτωσης ταξινομούνται στο `LOCAL_ONLY_API_PREFIXES`
και είναι προσβάσιμες μόνο μέσω loopback. Ένας εισβολέας που αποκτά ένα JWT μέσω μιας
σήραγγας Cloudflare/Ngrok δεν μπορεί να χρησιμοποιήσει τον διακομιστή μεσολάβησης για πρόσβαση στις ενσωματωμένες υπηρεσίες.

---

## 5. Ασφάλεια

### Επιβολή LOCAL_ONLY (αυστηρός κανόνας #17)

Όλες οι διαδρομές κάτω από `/api/services/` και `/dashboard/providers/services/*/embed/`
ταξινομούνται ως LOCAL_ONLY στο `src/server/authz/routeGuard.ts`. Ο έλεγχος loopback
εκτελείται άνευ όρων πριν από οποιονδήποτε κλάδο ελέγχου ταυτότητας:

```
το αίτημα φτάνει
  → isLocalOnlyPath(path)?
      → εκτός loopback → 403 LOCAL_ONLY (πάντα, πριν από τον έλεγχο ταυτότητας)
      → loopback       → συνέχεια στον κανονικό έλεγχο ταυτότητας
```

Αυτό αποτρέπει τη χρήση ενός JWT που έχει διαρρεύσει (π.χ. μέσω tunnel) για την
ενεργοποίηση του `npm install` ή τη δημιουργία διεργασιών. Ανατρέξτε στο
`docs/security/ROUTE_GUARD_TIERS.md` για τον πλήρη πίνακα επιπέδων.

### Εισαγωγή κλειδιού API

Τα 9Router και Mux απαιτούν κλειδί API/bearer token για τα δικά τους HTTP endpoints.
Το OmniRoute:

1. Δημιουργεί ένα κλειδί μέσω `crypto.randomBytes(32).toString("base64url")` με ένα
   πρόθεμα ειδικό για την υπηρεσία (`nr_` για το 9Router, `mx_` για το Mux).
2. Το κρυπτογραφεί κατά την αποθήκευση χρησιμοποιώντας AES-256-GCM (τον ίδιο αλγόριθμο κρυπτογράφησης που χρησιμοποιείται για τα διαπιστευτήρια παρόχων).
3. Το αποκρυπτογραφεί και το εισάγει ως μεταβλητή περιβάλλοντος κατά τη δημιουργία της διεργασίας —
   `NINEROUTER_API_KEY` για το 9Router, `MUX_SERVER_AUTH_TOKEN` για το Mux (ποτέ ως σημαία CLI,
   ώστε το token να μην εμφανίζεται ποτέ στο `ps`/στις λίστες διεργασιών).
4. Δεν επιστρέφει ποτέ το κλειδί σε μορφή απλού κειμένου σε καμία απόκριση HTTP.

Το CLIProxyAPI λαμβάνει ένα αποκλειστικό κλειδί επιπέδου δεδομένων, το οποίο εισάγεται κατά τη δημιουργία
της διεργασίας (`needsApiKey: true` — χρησιμοποιείται για συγχρονισμό μοντέλων με τον προσαρμογέα).

### Προστασία από SSRF

Ο αντίστροφος διακομιστής μεσολάβησης HTTP (`/dashboard/.../embed/[...path]`) είναι ρητά ρυθμισμένος να προωθεί
μόνο προς το `http://127.0.0.1:{port}`. Δεν ακολουθεί ποτέ ανακατευθύνσεις προς προορισμούς
εκτός loopback. Η βιβλιοθήκη `ssrf-req-filter` χρησιμοποιείται για την απόρριψη οποιουδήποτε upstream URL που
επιλύεται εκτός του εύρους loopback.

### Ασφάλεια κελύφους (αυστηρός κανόνας #13)

Το `npm install` καλείται μέσω `execFile('npm', ['install', pkg, '--prefix', dir])` —
χωρίς template literals, χωρίς κέλυφος και χωρίς παρεμβολή εξωτερικών διαδρομών στη συμβολοσειρά
εντολής. Οι τιμές χρόνου εκτέλεσης (θύρες, κλειδιά API) μεταβιβάζονται μέσω του αντικειμένου `env` της θυγατρικής διεργασίας.

### Εξυγίανση σφαλμάτων (αυστηρός κανόνας #12)

Όλες οι αποκρίσεις σφάλματος από το `/api/services/*` περνούν μέσω του `buildErrorBody()` ή του
`sanitizeErrorMessage()`. Τα ανεπεξέργαστα `err.stack` και `err.message` δεν επιστρέφονται ποτέ
αυτούσια στον καλούντα.

---

## 6. Προσθήκη νέας ενσωματωμένης υπηρεσίας

Ακολουθήστε αυτά τα 8 βήματα. Διαβάστε τις υπάρχουσες υλοποιήσεις στα `src/lib/services/installers/`
και `src/app/api/services/` ως την κανονιστική αναφορά.

### Βήμα 1 — Δημιουργία του προγράμματος εγκατάστασης

Δημιουργήστε το `src/lib/services/installers/{name}.ts` με πρότυπο το `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // επιλέξτε μια ελεύθερη θύρα

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Χρησιμοποιήστε το `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` από το `installers/utils.ts`
— ποτέ το `execSync` ή παρεμβολή μέσω κελύφους.

### Βήμα 2 — Καταχώριση στην αρχικοποίηση

Προσθέστε μια καταχώριση `ServiceEntry` στον πίνακα `SERVICES` στο `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false εάν δεν απαιτείται κλειδί API
}
```

Επεκτείνετε το `buildSpawnArgsFactory()` ώστε να χειρίζεται το `cfg.tool === "myservice"`.

#### Επεκτάσιμη σύμβαση πρόσθετου παρόχου (Φάση 1, #7333)

Το `src/lib/services/providerPlugins/` εισάγει μια σύμβαση `ServiceProviderPlugin` που
συσκευάζει τα πεδία `ServiceEntry` του `bootstrap.ts` ενός backend και τα πεδία προτύπου manifest του
`serviceBackends.ts` σε ένα αντικείμενο, αντί η μορφή του ίδιου backend να
εκφράζεται ξεχωριστά σε δύο άσχετα αρχεία. Τη στιγμή της σύνταξης αυτού του κειμένου, **μόνο το `9router` έχει
μετεγκατασταθεί** — το `bootstrap.ts` παράγει την καταχώρισή του στο `SERVICES[]` από το
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
προκαλώντας σφάλμα εκκίνησης εάν το πρόσθετο λείψει ποτέ. Τα `cliproxy`, `mux` και `bifrost`
παραμένουν αμετάβλητα στα προϋπάρχοντα inline literals του `SERVICES[]`.

Το `open-sse/config/providerPluginManifest.ts` απέκτησε επίσης μια πρόσθετη βοηθητική συνάρτηση
`createServiceBackendManifestEntry(pluginId, template)`, η οποία δημιουργεί μια ορθά διαμορφωμένη
`ProviderPluginManifestEntry` από μια καταχώριση `SERVICE_BACKEND_MANIFEST_TEMPLATE` — **δεν**
έχει ακόμη συνδεθεί με καμία ενεργή διαδρομή αιτημάτων (ούτε με το `generateProviderPluginManifestFromRegistry()`
ούτε με το `/v1/providers/[provider]/models`)· αυτό παραμένει εργασία για επόμενο στάδιο, αφού η σύμβαση
επαληθευτεί για ένα δεύτερο backend.

Αναβλήθηκαν για επόμενα PR και παρακολουθούνται στο issue #7333: η μετεγκατάσταση του `cliproxyapi` μέσω του
ίδιου μητρώου, η γενίκευση των `mux`/`bifrost` στην ένωση `ServiceBackendPluginId`,
η ενσωμάτωση των ειδικών περιπτώσεων δρομολόγησης executor (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) στη σύμβαση πρόσθετου και η σύνδεση του
`createServiceBackendManifestEntry()` με μια ενεργή διαδρομή κώδικα manifest/μοντέλων.

### Βήμα 3 — Προσθήκη migration και αρχικών δεδομένων βάσης δεδομένων

Βεβαιωθείτε ότι η υπηρεσία διαθέτει μια γραμμή στο `version_manager` μέσω ενός migration στο
`src/lib/db/migrations/`. Η γραμμή θα πρέπει να περιέχει:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Βήμα 4 — Δημιουργία των 7 API endpoints

Κάτω από το `src/app/api/services/{name}/`:

```
_lib.ts            βοηθητική συνάρτηση getOrInitSupervisor()
install/route.ts   POST — καλεί το installer.install()
start/route.ts     POST — καλεί το supervisor.start()
stop/route.ts      POST — καλεί το supervisor.stop()
restart/route.ts   POST — καλεί το supervisor.restart()
update/route.ts    POST — καλεί το installer.update()
status/route.ts    GET  — συγχωνεύει την ενεργή κατάσταση με την κατάσταση της βάσης δεδομένων
auto-start/route.ts POST — εναλλάσσει τη σημαία auto_start
```

Η κοινόχρηστη διαδρομή `GET /api/services/[name]/logs` είναι ήδη συνδεδεμένη — δεν απαιτούνται αλλαγές εκεί.

Αναθέστε όλες τις αποκρίσεις σφαλμάτων μέσω των `createErrorResponse()` / `buildErrorBody()`.

### Βήμα 5 — Προσθήκη στα LOCAL_ONLY_API_PREFIXES

Στο `src/server/authz/routeGuard.ts`, επαληθεύστε ότι το `/api/services/` περιλαμβάνεται ήδη.
Αν εισαγάγετε ένα νέο πρόθεμα (π.χ. `/api/tools/`), προσθέστε το τόσο στο
`LOCAL_ONLY_API_PREFIXES` όσο και, αν εκκινεί διεργασίες, στο `SPAWN_CAPABLE_PREFIXES`.
Προσθέστε μια δοκιμή στο `tests/unit/authz/routeGuard.test.ts`.

### Βήμα 6 — Προσθήκη της καρτέλας διεπαφής χρήστη

Δημιουργήστε το `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Επαναχρησιμοποιήστε τα κοινόχρηστα στοιχεία:

- `ServiceStatusCard` — κατάσταση σε πραγματικό χρόνο + ένδειξη εύρυθμης λειτουργίας
- `ServiceLifecycleButtons` — Έναρξη / Διακοπή / Επανεκκίνηση / Ενημέρωση
- `ServiceLogsPanel` — ουρά καταγραφών SSE (συνδέεται στο `/api/services/{name}/logs`)
- `ApiKeyCard` — αποκάλυψη + ανανέωση κλειδιού (αν `needsApiKey: true`)

Καταχωρίστε την καρτέλα στο `ServicesPageShell.tsx`.

### Βήμα 7 — Προσθήκη της καταχώρισης παρόχου (αν η υπηρεσία αποτελεί στόχο δρομολόγησης)

Αν η ενσωματωμένη υπηρεσία εκθέτει ένα συμβατό με το OpenAI endpoint `/v1/chat/completions`:

1. Προσθέστε μια καταχώριση παρόχου στο `src/shared/constants/providers.ts` με `isEmbeddedService: true`.
2. Δημιουργήστε το `open-sse/executors/{name}.ts` που επεκτείνει το `BaseExecutor`. Αναζητήστε εκ νέου τη θύρα και
   το κλειδί API ανά αίτημα (μην τα αποθηκεύετε ποτέ στην κρυφή μνήμη του constructor). Επιστρέψτε μια απόκριση
   `503 service_not_running` όταν η κατάσταση του επόπτη δεν είναι `"running"`.
3. Καταχωρίστε τα μοντέλα στο `open-sse/config/providerRegistry.ts` με το πρόθεμα της υπηρεσίας
   (π.χ. `myservice/sub/model`). Το `modelSync.ts` θα τα διατηρεί ενημερωμένα.

### Βήμα 8 — Τεκμηρίωση και δοκιμή

1. Ενημερώστε το `docs/frameworks/EMBEDDED-SERVICES.md` (αυτό το αρχείο) — προσθέστε την υπηρεσία στον
   πίνακα της §1 και τυχόν νέα endpoint στη §4.
2. Προσθέστε δοκιμές μονάδας στο `tests/unit/services/` (κύκλος ζωής, πρόγραμμα εγκατάστασης, μορφή API).
3. Προσθέστε δοκιμή ενσωμάτωσης στο `tests/integration/services/` (πίσω από το `RUN_SERVICES_INT=1`).
4. Ενημερώστε το `docs/openapi.yaml` με τα νέα endpoint.

---

## 7. Αντιμετώπιση προβλημάτων

### Η υπηρεσία δεν ξεκινά

**Συμπτώματα:** Το κουμπί εκκίνησης επιστρέφει 503, η κατάσταση παραμένει `"error"` ή `"starting"`.

**Λίστα ελέγχου:**

1. Ελέγξτε το `GET /api/services/{name}/logs` (ή τον πίνακα Logs στον πίνακα ελέγχου). Αναζητήστε
   γραμμές όπως `Error: ENOENT`, `address already in use` ή `Cannot find module`.
2. Επαληθεύστε ότι το `npm` βρίσκεται στο PATH: εκτελέστε `which npm` από τον ίδιο λογαριασμό χρήστη που εκτελεί το OmniRoute.
3. Επαληθεύστε ότι η υπηρεσία είναι εγκατεστημένη: ελέγξτε το `GET /api/services/{name}/status` για το
   `installedVersion`. Αν είναι `null`, εκτελέστε πρώτα την εγκατάσταση.
4. Ελέγξτε ότι το `DATA_DIR/services/{name}/node_modules/` υπάρχει και δεν είναι κενό.
5. Ελέγξτε το πεδίο `lastError` στην απόκριση κατάστασης για τον φιλτραρισμένο λόγο τερματισμού.

---

### Η ψυχρή εκκίνηση είναι αργή (> 10 s μέχρι να φτάσει στην κατάσταση `running`)

**Συμπτώματα:** Η κατάσταση παραμένει `"starting"` για μεγάλο χρονικό διάστημα προτού μεταβεί σε `"running"` ή `"error"`.

**Επεξήγηση:** Η ψυχρή εκκίνηση του 9Router περιλαμβάνει την εισαγωγή μεγάλων δέντρων εξαρτήσεων (μονάδες DNS,
σήραγγας και MITM). Το προεπιλεγμένο διάστημα ελέγχου εύρυθμης λειτουργίας είναι 2 s, με 3 προσπάθειες προτού ο
επόπτης δηλώσει λήξη χρονικού ορίου (αλλά συνεχίζει τον περιοδικό έλεγχο).

**Διόρθωση:** Το `healthIntervalMs` και το χρονικό όριο του `waitForHealthy`
(`healthIntervalMs * 3`) μπορούν να ρυθμιστούν στο `bootstrap.ts`. Για υπηρεσίες με μεγαλύτερους
χρόνους εκκίνησης, αυξήστε το `healthIntervalMs` σε 5000 και το `stopTimeoutMs` σε 30 000.

---

### Σύγκρουση θύρας (`EADDRINUSE`)

**Συμπτώματα:** Τα αρχεία καταγραφής εμφανίζουν `address already in use :::20130`.

**Αιτίες:**

- Μια άλλη διεργασία χρησιμοποιεί ήδη τη θύρα 20130.
- Μια προηγούμενη διεργασία του 9Router δεν τερματίστηκε πλήρως (ανενεργό PID).

**Διόρθωση:**

1. Αλλάξτε την προεπιλεγμένη θύρα μέσω της μεταβλητής περιβάλλοντος `NINEROUTER_PORT` στο `.env`.
2. Εντοπίστε και τερματίστε τη διεργασία που προκαλεί τη σύγκρουση: `lsof -ti :20130 | xargs kill -9`.
3. Η θύρα μπορεί να ρυθμιστεί ανά υπηρεσία στο `bootstrap.ts` μέσω του πεδίου `port`.

**Σημείωση:** Το 9Router χρησιμοποιεί από προεπιλογή τη θύρα 20130 ειδικά για να αποφεύγει τη σύγκρουση με
την προεπιλεγμένη θύρα 20128 του OmniRoute.

---

### Απόρριψη άδειας (EACCES) κατά την εγκατάσταση

**Συμπτώματα:** Η εγκατάσταση επιστρέφει 500 και τα αρχεία καταγραφής εμφανίζουν `EACCES` ή `permission denied`.

**Αιτίες:**

- Το `DATA_DIR` ή ο γονικός του κατάλογος δεν είναι εγγράψιμος από τη διεργασία OmniRoute.
- Εκτέλεση εντός Docker χωρίς δικαιώματα root και χωρίς πρόσβαση εγγραφής στον αντιστοιχισμένο τόμο.

**Διόρθωση:**

1. Ελέγξτε το `DATA_DIR` (προεπιλογή: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Βεβαιωθείτε ότι ο χρήστης της διεργασίας OmniRoute είναι κάτοχος του καταλόγου: `chown -R $USER ~/.omniroute/`
3. Στο Docker, βεβαιωθείτε ότι η προσάρτηση τόμου έχει τα σωστά δικαιώματα για τον χρήστη του κοντέινερ.

---

### Η ενημέρωση αποτυγχάνει (λήξη χρονικού ορίου του `npm install` ή σφάλμα δικτύου)

**Συμπτώματα:** Η ενημέρωση επιστρέφει 500 με `InstallError` και τα αρχεία καταγραφής εμφανίζουν λήξη χρονικού ορίου δικτύου.

**Λίστα ελέγχου:**

1. Επιβεβαιώστε ότι το μητρώο npm είναι προσβάσιμο: `npm ping`.
2. Ελέγξτε για εταιρικό διακομιστή μεσολάβησης: `npm config get proxy`, `npm config get https-proxy`.
3. Δοκιμάστε την εγκατάσταση χειροκίνητα: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Αν βρίσκεστε πίσω από απομονωμένο δίκτυο, πραγματοποιήστε εκ των προτέρων λήψη του tarball και χρησιμοποιήστε `npm install /path/to/tarball.tgz`.

---

### Η υπηρεσία εμφανίζει κατάσταση `"error"` αμέσως μετά την εκκίνηση (γρήγορη κατάρρευση)

**Συμπτώματα:** Η κατάσταση μεταβαίνει από `"starting"` σε `"error"` σε λιγότερο από 5 δευτερόλεπτα.
Το `lastError` εμφανίζει `"Fast crash (exited with code 1)"`.

**Λίστα ελέγχου:**

1. Διαβάστε το πλήρες τελευταίο τμήμα του αρχείου καταγραφής: `GET /api/services/{name}/logs?tail=500`.
2. Συνήθης αιτία: απουσία μεταβλητών περιβάλλοντος που αναμένονται από την υπηρεσία.
3. Για το 9Router: επαληθεύστε ότι τα `NINEROUTER_DISABLE_MITM=true` και
   `NINEROUTER_DISABLE_TUNNEL=true` περιλαμβάνονται στο περιβάλλον που μεταβιβάζεται κατά τη δημιουργία της διεργασίας (δείτε
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Συχνές ερωτήσεις

**Ε: Μπορώ να εκθέσω τα τελικά σημεία των ενσωματωμένων υπηρεσιών σε πελάτες εκτός loopback;**

Όχι. Το επίπεδο LOCAL_ONLY είναι σκόπιμο (αυστηρός κανόνας #17). Οι διαδρομές που μπορούν να εκτελέσουν
`npm install` ή να εκκινήσουν διεργασίες `node` δεν πρέπει να είναι προσβάσιμες από κίνηση
εκτός loopback, επειδή ένα JWT που έχει διαρρεύσει μέσω tunnel (Cloudflare, Ngrok, Tailscale) θα
επέτρεπε διαφορετικά την αυθαίρετη εκκίνηση διεργασιών. Δεν υπάρχει δυνατότητα εξαίρεσης για
το `/api/services/` — σε αντίθεση με το `/api/mcp/`, εξαιρείται από τη λίστα παράκαμψης
του manage-scope. Ανατρέξτε στο `docs/security/ROUTE_GUARD_TIERS.md`.

---

**Ε: Θα είναι διαθέσιμα τα 9Router και CLIProxyAPI σε αναπτύξεις παραγωγής/cloud;**

Ναι. Και οι δύο υπηρεσίες ακολουθούν το ίδιο μοντέλο με προτεραιότητα στην τοπική εκτέλεση όπως το ίδιο το OmniRoute. Εκτελούνται
στο ίδιο μηχάνημα και επικοινωνούν μέσω loopback. Ο όρος «παραγωγή» εδώ σημαίνει το VPS
ή τον τοπικό διακομιστή όπου έχει αναπτυχθεί το OmniRoute, όχι έναν απομακρυσμένο πάροχο cloud.

---

**Ε: Πώς μπορώ να εντοπίσω σφάλματα στον supervisor;**

1. Παρακολουθήστε τη ροή καταγραφής SSE: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Ελέγξτε τις δομημένες καταγραφές στην έξοδο pino του OmniRoute, φιλτραρισμένες βάσει του
   namespace `service:supervisor`.
3. Εξετάστε τη γραμμή της βάσης δεδομένων: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Χρησιμοποιήστε το `GET /api/services/9router/status` για να δείτε την τρέχουσα ενεργή κατάσταση, το PID, την εύρυθμη λειτουργία
   και το `lastError` σε μία κλήση.

---

**Ε: Ο supervisor εμφανίζει `health: "degraded"` ή `health: "unknown"`, αλλά η κατάσταση είναι `"running"`. Είναι πρόβλημα;**

Το `"degraded"` σημαίνει ότι ο έλεγχος εύρυθμης λειτουργίας επέστρεψε απόκριση διαφορετική από 200. Το `"unknown"` σημαίνει ότι δεν
έχει ολοκληρωθεί ακόμη κανένας έλεγχος (ανταγωνισμός χρονισμού με την πρώτη επανάληψη ελέγχου). Και οι δύο καταστάσεις είναι παροδικές κατά την εκκίνηση.
Αν η εύρυθμη λειτουργία παραμένει `"degraded"` για περισσότερα από `healthIntervalMs * 3` ms μετά την κατάσταση
`"running"`, η ενσωματωμένη υπηρεσία εκτελείται, αλλά το HTTP API της δεν αποκρίνεται. Ελέγξτε
αν η θύρα στην απόκριση κατάστασης είναι σωστή και αν η υπηρεσία όντως
ακούει σε αυτήν τη θύρα.

---

**Ε: Μπορώ να αλλάξω το κλειδί API του 9Router χωρίς πλήρη επανεκκίνηση;**

Όχι. Το κλειδί API μεταβιβάζεται στο 9Router μέσω μιας μεταβλητής περιβάλλοντος κατά την εκκίνηση της διεργασίας.
Οι μεταβλητές περιβάλλοντος δεν μπορούν να αλλάξουν σε μια εκτελούμενη διεργασία. Το `POST .../rotate-key`
διακόπτει και επανεκκινεί αυτόματα την υπηρεσία για να εφαρμόσει το νέο κλειδί. Η εναλλαγή κλειδιού
τίθεται σε ισχύ εντός του `stopTimeoutMs` της υπηρεσίας (προεπιλογή 15 s), συν τον χρόνο
εκκίνησής της.

---

**Ε: Ποιο είναι το όριο του κυκλικού buffer και τι συμβαίνει όταν γεμίσει;**

Κάθε υπηρεσία διαθέτει έναν αποκλειστικό κυκλικό buffer 5 MB. Όταν ο buffer γεμίσει, οι παλαιότερες
γραμμές καταγραφής αφαιρούνται για να δημιουργηθεί χώρος για νέες. Το συμβάν SSE `snapshot` επιστρέφει
τις πιο πρόσφατες γραμμές εντός του ορίου `tail`. Οι καταγραφές δεν αποθηκεύονται μόνιμα στον δίσκο, εκτός αν
το `logsBufferPath` έχει οριστεί στη γραμμή της βάσης δεδομένων.

---

## Δείτε επίσης

- `docs/security/ROUTE_GUARD_TIERS.md` — λεπτομέρειες για το επίπεδο LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 αντιστοίχιση της μονάδας ενσωματωμένων υπηρεσιών
- `docs/architecture/ARCHITECTURE.md` — περιβάλλον σε επίπεδο συστήματος
- `docs/openapi.yaml` — ορισμοί τελικών σημείων σε μηχανικώς αναγνώσιμη μορφή
- `CLAUDE.md` §"Adding a New Embedded Service" — λίστα ελέγχου γρήγορης αναφοράς
