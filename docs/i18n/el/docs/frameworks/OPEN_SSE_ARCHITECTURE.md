# open-sse Architecture (Ελληνικά)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Συνοπτικά**: Το `open-sse/` είναι η βασική μηχανή ροής που υποστηρίζει κάθε αίτημα LLM στο OmniRoute. Περιέχει ~1.400 αρχεία που υλοποιούν τη διοχέτευση αιτημάτων, τους εκτελεστές, τις υπηρεσίες, τον διακομιστή MCP και το επίπεδο μετάφρασης. Αυτός ο οδηγός εξηγεί πώς συνδέονται μεταξύ τους τα επιμέρους τμήματα.

**Πηγή:** `open-sse/` (πακέτο χώρου εργασίας, ~1.440 αρχεία `.ts` εκτός δοκιμών)

---

## Γιατί ξεχωριστό πακέτο χώρου εργασίας;

Το `open-sse/` είναι ένας **αυτόνομος χώρος εργασίας** στο monorepo του OmniRoute για διάφορους λόγους:

1. **Επαναχρησιμοποίηση** — το `open-sse` δημοσιεύεται ως `@omniroute/open-sse` στο npm, ώστε άλλα έργα να μπορούν να το χρησιμοποιούν ανεξάρτητα
2. **Σαφή όρια** — η μηχανή ροής είναι αποσυνδεδεμένη από το επίπεδο UI/DB που αφορά ειδικά το OmniRoute
3. **Απόδοση** — η μηχανή δεν έχει εξαρτήσεις από το Next.js, επιτρέποντας ταχύτερες ψυχρές εκκινήσεις σε περιβάλλοντα CLI/serverless
4. **Διαχείριση εκδόσεων** — το `open-sse` μπορεί να κυκλοφορεί εκδόσεις με τον δικό του ρυθμό

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Δομή ανώτατου επιπέδου

```
open-sse/
├── index.ts              # Δημόσιο σημείο εισόδου
├── types.d.ts            # Δημόσιες εξαγωγές τύπων
├── package.json          # @omniroute/open-sse
├── config/               # Ρυθμίσεις παρόχων, σταθερές, μητρώα
├── executors/            # Εκτελεστές HTTP ανά πάροχο (67 + base.ts/index.ts)
├── handlers/             # Χειριστές αιτημάτων (chatCore, responses κ.λπ.)
├── lib/                  # Εσωτερικά βοηθητικά εργαλεία
├── mcp-server/           # Διακομιστής Model Context Protocol
├── services/             # ~298 αρθρώματα υπηρεσιών
├── transformer/          # Μετασχηματιστής μορφής του Responses API
├── translator/           # Μετάφραση μορφής (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Κοινόχρηστα βοηθητικά εργαλεία (καταγραφή, σφάλματα, ροή κ.λπ.)
```

### Πλήθος αρθρωμάτων

| Κατάλογος | Αρχεία | Σκοπός |
| `executors/` | 167 | Εκτελεστές HTTP ανά πάροχο (ενοποιημένοι μέσω του εργοστασίου DefaultExecutor) |
| `handlers/` | 157 | Σημεία εισόδου αιτημάτων (chatCore, responses, embeddings) |
| `services/` | ~536 | Δρομολόγηση, προσωρινή αποθήκευση, περιορισμός ρυθμού, ανανέωση κ.λπ. |
| `translator/` | 56 | Μετατροπή μορφής (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Εργαλεία και μέσα μεταφοράς MCP |
| `utils/` | ~108 | Οριζόντια βοηθητικά εργαλεία (καταγραφή, σφάλματα, ροή) |
| `config/` | ~339 | Ρυθμίσεις παρόχων, σταθερές, μητρώα |

---

## Η διοχέτευση αιτημάτων

Κάθε αίτημα LLM διέρχεται από μια **διοχέτευση 5 σταδίων**:

```
                              ┌──────────────────┐
   Αίτημα HTTP                 │  1. ΔΡΟΜΟΛΟΓΗΣΗ  │   επίλυση combo, επιλογή μοντέλου
   (διαδρομή Next.js)          └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  2. ΜΕΤΑΦΡΑΣΗ    │   μετατροπή μορφής (OpenAI ↔ Claude ↔ Gemini)
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  3. ΕΚΤΕΛΕΣΗ     │   εκτελεστής παρόχου, HTTP, επανάληψη, διακόπτης
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  4. ΡΟΗ          │   μετασχηματισμός SSE, αντίθλιψη
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  5. ΚΑΤΑΓΡΑΦΗ    │   παρακολούθηση χρήσης, αρχείο κλήσεων, ταξινόμηση σφαλμάτων
                              └────────┬─────────┘
                                       │
                                       ▼
                              Απόκριση HTTP (SSE ή JSON)
```

### Στάδιο 1: Δρομολόγηση (services/combo.ts)

**Σημείο εισόδου**: `handleComboChat()` στο `services/combo.ts`

Αντιστοιχίζει το αίτημα σε μια συγκεκριμένη πλειάδα `(provider, model, account, credentials)`:

- Αναζητά το combo βάσει ID (ή δημιουργεί ένα εικονικό combo για μοντέλα `auto/*`)
- Εφαρμόζει τη στρατηγική δρομολόγησης (προτεραιότητα, σταθμισμένη, round-robin κ.λπ.)
- Φιλτράρει τους μη υγιείς παρόχους (διακόπτης κυκλώματος)
- Επιλέγει τον επόμενο βιώσιμο προορισμό

Για μοντέλα `auto/*`, αυτό το στάδιο επίσης:

- Εκτελεί τον αλγόριθμο **βαθμολόγησης 16 παραγόντων** (`services/autoCombo/`)
- Επιλέγει ένα ζεύγος `provider+model` βάσει εύρυθμης λειτουργίας, κόστους, καθυστέρησης κ.λπ.

### Στάδιο 2: Μετάφραση (translator/)

Εάν η μορφή προέλευσης (π.χ. OpenAI) διαφέρει από τη μορφή προορισμού (π.χ. Claude), το αίτημα **μεταφράζεται**:

- Προτροπή συστήματος → μήνυμα συστήματος
- Ορισμοί εργαλείων → μορφή εργαλείων ειδική για τον πάροχο
- Παράμετροι συλλογισμού/σκέψης → αντίστοιχες παράμετροι ειδικές για τον πάροχο
- Κανονικοποίηση ρόλων μηνυμάτων (`developer` → `system` για παρόχους εκτός OpenAI)

Το `translator/index.ts` εκθέτει:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Στάδιο 3: Εκτέλεση (executors/)

**Σημείο εισόδου**: `getExecutor(providerId).execute(request, options)`

Όλοι οι πάροχοι χρησιμοποιούν το `DefaultExecutor` (`executors/default.ts`) μέσω της εφεδρικής επιλογής του εργοστασίου `getExecutor()`. Ο εκτελεστής:

- Δημιουργεί το URL ανώτερου επιπέδου (`buildUrl()`)
- Προσθέτει κεφαλίδες ειδικές για τον πάροχο (`buildHeaders()`)
- Μετασχηματίζει το σώμα του αιτήματος (`transformRequest()`)
- Στέλνει το αίτημα HTTP με επανάληψη + εκθετική οπισθοχώρηση
- Διαχειρίζεται την ανανέωση ελέγχου ταυτότητας, εάν απαιτείται (πάροχοι OAuth)

Όλοι οι εκτελεστές επεκτείνουν το `BaseExecutor` (`executors/base.ts`, 1170 γραμμές κώδικα), το οποίο παρέχει:

- Κοινή λογική επανάληψης
- Ενσωμάτωση διακομιστή μεσολάβησης
- Ενσωμάτωση διακόπτη κυκλώματος
- Άγκιστρα καταγραφής χρήσης

### Στάδιο 4: Ροή (utils/stream.ts)

Για αποκρίσεις ροής, ο εκτελεστής επιστρέφει ένα **ReadableStream**. Ο χειριστής:

- Διοχετεύει μέσω ενός μετασχηματισμού SSE (`createSSETransformStreamWithLogger`)
- Εφαρμόζει παλμούς heartbeat για τον εντοπισμό νεκρών συνδέσεων
- Διαχειρίζεται ομαλά την αποσύνδεση του πελάτη (`pipeWithDisconnect`)
- Μετασχηματίζει SSE → JSON για πελάτες χωρίς υποστήριξη ροής

Για αποκρίσεις χωρίς ροή, ο εκτελεστής επιστρέφει ένα αναλυμένο αντικείμενο JSON, το οποίο προωθείται χωρίς αλλαγές.

### Στάδιο 5: Καταγραφή (services/usage.ts)

Μετά την απόκριση (επιτυχή ή αποτυχημένη), καταγράφεται η χρήση:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` από την απόκριση
- `cost_usd` που υπολογίζεται από τα δεδομένα τιμολόγησης
- `latency_ms`, `status`, `error_class` σε περίπτωση αποτυχίας
- Αποθηκεύεται στον πίνακα `usage_history`

Τα τεχνουργήματα καταγραφής κλήσεων (εάν είναι ενεργοποιημένα) εγγράφονται στο `${DATA_DIR}/call_logs/`.

---

## Εμβάθυνση στα βασικά αρχεία

### chatCore.ts (5977 γραμμές)

Ο **κύριος χειριστής αιτημάτων**. Παρά το μέγεθός του, έχει σαφή δομή:

```ts
// Ψευδοδομή του chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Αυθεντικοποίηση + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Επικύρωση σώματος
  const body = await parseRequestBody(request);

  // 3. Ανίχνευση μορφής + μετάφραση
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Δρομολόγηση συνδυασμού
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Συνέχεια στον επόμενο προορισμό
    }
  }

  // 5. Εφεδρική λύση έκτακτης ανάγκης
  return await emergencyFallback(body);
}
```

Παρότι αποτελεί μία τεράστια συνάρτηση, είναι οργανωμένη σε **ενότητες με σχόλια** που αντιστοιχούν στα 5 στάδια της διοχέτευσης.

### combo.ts (4456 γραμμές κώδικα)

Η **μηχανή δρομολόγησης** που αναλύει έναν συνδυασμό σε μια ταξινομημένη σειρά προορισμών.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Υποστηρίζει **19 στρατηγικές δρομολόγησης** (βλ. `src/shared/constants/routingStrategies.ts`):

| Στρατηγική          | Συμπεριφορά                                                                        |
| ------------------- | ---------------------------------------------------------------------------------- |
| `priority`          | Ταξινομημένη λίστα με προτεραιότητα στον πρώτο προορισμό                           |
| `weighted`          | Πιθανοτική επιλογή βάσει του βάρους κάθε προορισμού                                |
| `round-robin`       | Κυκλική διέλευση από τους προορισμούς με τη σειρά                                  |
| `context-relay`     | Μεταβίβαση του συγκειμένου μεταξύ προορισμών                                       |
| `fill-first`        | Κάλυψη του ορίου πριν από τη μετάβαση στον επόμενο                                 |
| `p2c`               | Επιλογή μεταξύ δύο προορισμών                                                      |
| `random`            | Ομοιόμορφα τυχαία επιλογή                                                          |
| `least-used`        | Επιλογή εκείνου με τις λιγότερες πρόσφατες χρήσεις                                 |
| `cost-optimized`    | Πρώτα ο οικονομικότερος διαθέσιμος προορισμός                                      |
| `reset-aware`       | Λαμβάνει υπόψη τα χρονικά παράθυρα επαναφοράς του παρόχου                          |
| `reset-window`      | Δρομολόγηση βάσει χρονικού παραθύρου επαναφοράς                                    |
| `headroom`          | Πρώτα ο προορισμός με το μεγαλύτερο υπολειπόμενο περιθώριο ορίου                   |
| `strict-random`     | Πραγματικά ομοιόμορφη επιλογή (χωρίς στάθμιση ποιότητας)                           |
| `auto`              | Χρήση βαθμολόγησης 16 παραγόντων (`autoCombo/`)                                    |
| `lkgp`              | Πρώτα ο τελευταίος γνωστός λειτουργικός πάροχος                                    |
| `context-optimized` | Βέλτιστη επιλογή για αιτήματα μεγάλου συγκειμένου                                  |
| `fusion`            | Παράλληλη διανομή σε μια ομάδα και, στη συνέχεια, σύνθεση μέσω κριτή (`fusion.ts`) |

### base.ts (1170 γραμμές κώδικα)

Ο **αφηρημένος εκτελεστής** τον οποίο επεκτείνουν και οι 107 εκτελεστές. Περιλαμβάνει:

- `buildUrl()` — προεπιλεγμένη κατασκευή URL (οι υποκλάσεις την παρακάμπτουν για προσαρμοσμένη συμπεριφορά)
- `buildHeaders()` — προεπιλεγμένες κεφαλίδες (αυθεντικοποίηση, τύπος περιεχομένου)
- `transformRequest()` — προεπιλεγμένη αμετάβλητη προώθηση
- `execute()` — ο κύριος βρόχος HTTP με επαναλήψεις/εκθετική καθυστέρηση/διακόπτη κυκλώματος

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Χειρίζεται όλους τους παρόχους που είναι συμβατοί με OpenAI/Anthropic
  // Οι πάροχοι καταχωρίζουν διαμορφώσεις (URL, αυθεντικοποίηση, κεφαλίδες), αλλά χρησιμοποιούν από κοινού τη λογική του εκτελεστή
}
```

Η συμπεριφορά που αφορά συγκεκριμένους παρόχους (κεφαλίδες αυθεντικοποίησης, βασικό URL, κεφαλίδες έκδοσης) διαμορφώνεται μέσω του μητρώου παρόχων και όχι μέσω ξεχωριστών κλάσεων εκτελεστών.

````

---

## Υπηρεσίες (117 αρθρώματα)

Οι υπηρεσίες είναι **εστιασμένα αρθρώματα συγκεκριμένου σκοπού** που συνδυάζονται από τους χειριστές. Οι κύριες κατηγορίες:

### Δρομολόγηση & Συνδυασμοί

- `combo.ts` — σημείο εισόδου για αιτήματα συνδυαστικής δρομολόγησης
- `services/autoCombo/` — βαθμολόγηση 16 παραγόντων, 8 στρατηγικές αυτόματης δρομολόγησης
- `wildcardRouter.ts` — αντιστοιχίζει διαδρομές με χαρακτήρες μπαλαντέρ (`gpt-*`)
- `modelFamilyFallback.ts` — εναλλακτική μετάβαση εντός της οικογένειας T5

### Περιορισμός Ρυθμού & Ποσόστωση

- `rateLimitManager.ts` — κάδος διακριτικών ανά κλειδί+πάροχο
- `usage.ts` — καταγραφή χρήσης
- `quotaCache.ts` — στιγμιότυπα ποσόστωσης στη μνήμη

### Λογαριασμός & Διακριτικό

- `tokenRefresh.ts` — ανανέωση OAuth σε απόκριση 401
- `accountFallback.ts` — μετάβαση σε εναλλακτικό λογαριασμό
- `sessionManager.ts` — κατάσταση συνεδρίας πολλαπλών γύρων

### Ευφυΐα

- `intentClassifier.ts` — ταξινόμηση της πρόθεσης του αιτήματος
- `taskAwareRouter.ts` — δρομολόγηση ανά τύπο εργασίας
- `thinkingBudget.ts` — κατανομή διακριτικών συλλογισμού
- `contextManager.ts` — εισαγωγή περιβάλλοντος δρομολόγησης

### Ανθεκτικότητα

- `resilience.ts` — ενορχήστρωση επαναλήψεων, εκθετικής καθυστέρησης και διακόπτη κυκλώματος
- `emergencyFallback.ts` — εναλλακτική λύση έσχατης ανάγκης
- `modelDeprecation.ts` — αυτόματη δρομολόγηση σε διάδοχα μοντέλα

### Κατάσταση

- `signatureCache.ts` — αποδιπλοποίηση βάσει υπογραφής αιτήματος
- `volumeDetector.ts` — απόρριψη φορτίου
- `contextHandoff.ts` — σειριοποίηση συνεδρίας

### Συμπίεση

- `compression/` (υποκατάλογος) — πλήρης διοχέτευση συμπίεσης
- 39 αρχεία που καλύπτουν μηχανές, πακέτα κανόνων και προσαρμογείς

### Δεξιότητες

- (καλύπτονται στο [SKILLS.md](./SKILLS.md))

### Μνήμη

- (καλύπτεται στο [MEMORY.md](./MEMORY.md))

---

## Εκτελεστές (75+ αρχεία)

Ένα αρχείο ανά πάροχο. Όλοι επεκτείνουν το `BaseExecutor` και παρακάμπτουν ό,τι διαφέρει.

### Συνήθη Μοτίβα

Οι πάροχοι επιλύονται μέσω του `getExecutor(providerId)`, το οποίο επιστρέφει τον διαμορφωμένο εκτελεστή. Οι πάροχοι που είναι συμβατοί με OpenAI/Anthropic χρησιμοποιούν το `DefaultExecutor` (`executors/default.ts`). Η συμπεριφορά που αφορά ειδικά κάθε πάροχο (βασικό URL, κεφαλίδες αυθεντικοποίησης, έκδοση API) διαμορφώνεται στο `open-sse/config/providers/`, ενώ οι μετασχηματισμοί του σώματος αιτήματος διεκπεραιώνονται στο `open-sse/translator/`.

Το **προσαρμοσμένο URL** ορίζεται μέσω της διαμόρφωσης του παρόχου:

```ts
// Διαμόρφωση παρόχου στο open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

Η **προσαρμοσμένη αυθεντικοποίηση** διεκπεραιώνεται μέσω της διαμόρφωσης αυθεντικοποίησης του μητρώου παρόχων (κλειδί API, OAuth, προφίλ κεφαλίδων).

Οι μετασχηματισμοί του **προσαρμοσμένου σώματος αιτήματος** (π.χ. ο διαχωρισμός του `system` από το `messages` από την Anthropic) καταχωρίζονται ανά πάροχο στο `open-sse/translator/`.

````

### Το Εργοστάσιο Εκτελεστών

Το `executors/index.ts` εξάγει το `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Η επίλυση πραγματοποιείται μέσω του `ExecutorRegistry` (`executors/registry.ts`): κάθε εξειδικευμένος εκτελεστής δηλώνεται στον ενσωματωμένο πίνακα του `executors/index.ts` και καταχωρίζεται μέσω του `registerExecutor(alias, instance)` κατά τη φόρτωση του αρθρώματος· το `getExecutor()` συμβουλεύεται το μητρώο και, για κάθε πάροχο χωρίς εξειδικευμένη καταχώριση, χρησιμοποιεί ως εναλλακτική έναν απομνημονευμένο `DefaultExecutor`. Η πλήρης αντιστοίχιση ψευδωνύμου → εκτελεστή χαρακτηρίζεται από τη χρυσή δοκιμή `tests/unit/executor-map-golden.test.ts`.

---

## Μεταφραστές

Μετατροπή μεταξύ **3 μορφών**: OpenAI, Anthropic, Gemini, καθώς και του νέου Responses API.

### Πότε πραγματοποιείται η μετατροπή

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Συνήθεις μετατροπές:

- `OpenAI → Anthropic`: ξεχωριστό πεδίο `system`, κεφαλίδα `x-api-key`
- `OpenAI → Gemini`: `contents` αντί για `messages`, `systemInstruction`
- `OpenAI → Responses API`: πίνακας `input`, κατάσταση `previous_response_id`

### Οριακές περιπτώσεις που υποστηρίζονται

- Ρόλος `developer` → `system` για παρόχους εκτός OpenAI
- Ρόλος `system` → συγχώνευση στο πρώτο μήνυμα χρήστη για GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` του Gemini
- `tools` → μορφή εργαλείων ειδική για κάθε πάροχο
- Παράμετροι συλλογισμού (o1, Claude) → ισοδύναμες παράμετροι ειδικές για κάθε πάροχο

---

## Διακομιστής MCP

Το `open-sse/mcp-server/` υλοποιεί τον διακομιστή **Model Context Protocol**:

- **110 εργαλεία** (διαχείριση παρόχων, συνδυασμοί, μνήμη, κρυφή μνήμη, συμπίεση, διακομιστής μεσολάβησης, δεξιότητες, παιχνιδοποίηση, πρόσθετα, Notion, Obsidian, τοπικό σώμα κειμένων)
- **3 τρόποι μεταφοράς**: stdio, SSE, Streamable HTTP
- **33 πεδία δικαιωμάτων** για λεπτομερή εξουσιοδότηση

### Καταχώριση εργαλείων

Τα εργαλεία καταχωρίζονται ως αυτόνομα αρχεία στο `open-sse/mcp-server/tools/`, καθένα από τα οποία εξάγει ένα όνομα, ένα σχήμα, έναν χειριστή και ένα πεδίο δικαιωμάτων:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Τρόποι μεταφοράς

```ts
// stdio (χρήση μέσω CLI)
startMcpStdio(server);

// SSE (ροή μέσω HTTP)
startMcpSse(server, port);

// Streamable HTTP (σύγχρονο MCP)
startMcpStreamable(server, port);
```

### Εξουσιοδότηση

Κάθε κλήση εργαλείου υποβάλλεται σε ελέγχους πεδίων δικαιωμάτων (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Μετασχηματιστές

Το `open-sse/transformer/` πραγματοποιεί μετατροπές μεταξύ των μορφών **Chat Completions** και **Responses API**.

### Γιατί χρειάζεται ξεχωριστός μετασχηματιστής;

Το Responses API είναι η νέα μορφή της OpenAI με **συνομιλίες με κατάσταση** (`previous_response_id`). Όταν ένας πελάτης στέλνει ένα αίτημα Responses, το OmniRoute:

1. Μετατρέπει εσωτερικά το Responses → Chat Completions
2. Το στέλνει στον πάροχο (οποιονδήποτε πάροχο που υποστηρίζει Chat Completions)
3. Μετατρέπει την απόκριση ξανά στη μορφή Responses
4. Μεταδίδει τη μετατρεπόμενη απόκριση στον πελάτη μέσω ροής

Ο μετασχηματιστής (`transformer/responsesTransformer.ts`) παρέχει:

```ts
createResponsesApiTransformStream(): TransformStream
```

Αυτός χειρίζεται:

- Συμβάντα `response.output_item.added`
- Συμβάντα `response.output_text.delta`
- Συμβάν `response.completed`
- Αντιστοίχιση κλήσεων εργαλείων (`function_call` ↔ `tool_calls`)

---

## Διαμόρφωση

Το `open-sse/config/` περιέχει το επίπεδο διαμόρφωσης:

| Αρχείο                           | Σκοπός                                                  |
| -------------------------------- | ------------------------------------------------------- |
| `providerRegistry.ts`            | Μητρώο μοντέλων συνομιλίας για τον κατάλογο 352 παρόχων |
| `providerModels.ts`              | Ψευδώνυμα μοντέλων, αντιστοίχιση μορφών                 |
| `constants.ts`                   | Χρονικά όρια, περιορισμοί, κωδικοί κατάστασης           |
| `defaultThinkingSignature.ts`    | Προεπιλεγμένη υπογραφή συλλογισμού του Claude           |
| `modelStrip.ts` (στις υπηρεσίες) | Αφαίρεση πεδίων ανά πάροχο                              |

### Σχήμα μητρώου παρόχων

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Η επικύρωση Zod κατά τη φόρτωση του module διασφαλίζει ότι όλες οι διαμορφώσεις παρόχων είναι έγκυρες.

---

## Περιορισμοί Απόδοσης

Η μηχανή δρομολόγησης έχει αυστηρά όρια απόδοσης:

| Λειτουργία                                                    | Στόχος | Μέτρηση                           |
| ------------------------------------------------------------- | ------ | --------------------------------- |
| Επίλυση συνδυασμού                                            | <10ms  | Για 50 προορισμούς                |
| Έλεγχος ορίου ρυθμού                                          | <1ms   | Κάδος διακριτικών στη μνήμη       |
| Εφεδρική επιλογή οικογένειας μοντέλων                         | <5ms   | Αποθηκευμένοι ορισμοί οικογενειών |
| Αποστολή αιτήματος δρομολόγησης                               | <2ms   | Κρίσιμη διαδρομή                  |
| **Χωρίς αποκλειστικό I/O στην κρίσιμη διαδρομή δρομολόγησης** | —      | Όλα ασύγχρονα                     |

---

## Αντιμοτίβα

❌ **Σύγχρονες κλήσεις DB στο `combo.ts`** — προϋπολογίστε και αποθηκεύστε στην κρυφή μνήμη
❌ **Λογική επανάληψης στους χειριστές** — χρησιμοποιήστε το `retry()` από την υπηρεσία ανθεκτικότητας
❌ **Άμεση πρόσβαση στη διαμόρφωση παρόχου** — χρησιμοποιήστε τις μεθόδους ανάκτησης του `providerRegistry`
❌ **Αλυσίδες εφεδρικών επιλογών ενσωματωμένες στον κώδικα** — ορίστε τις στο `modelFamilyFallback.ts`
❌ **Μεταβολές κατάστασης μεταξύ ταυτόχρονων αιτημάτων** — χρησιμοποιήστε μόνο περιβάλλον με εμβέλεια αιτήματος

---

## Προσθήκη Νέου Στοιχείου

### Προσθήκη Νέας Υπηρεσίας

1. Δημιουργήστε το `open-sse/services/[serviceName].ts` με σαφώς εστιασμένη αρμοδιότητα
2. Εξαγάγετε την κύρια συνάρτηση χειρισμού και τυχόν σταθερές
3. Προσθέστε δοκιμές μονάδας στο `tests/unit/services/[serviceName].test.mjs`
4. Ενσωματώστε την στη διοχέτευση αιτημάτων στο `handlers/chatCore.ts` (εάν σχετίζεται με τη δρομολόγηση)
5. Ενημερώστε τη λογική δρομολόγησης στο `combo.ts` εάν η υπηρεσία επηρεάζει την επιλογή προορισμού
6. Τεκμηριώστε τη σε αυτό το αρχείο

### Προσθήκη Νέου Εκτελεστή

1. Δημιουργήστε το `open-sse/executors/[provider].ts` επεκτείνοντας το `BaseExecutor`
2. Καταχωρίστε τον στο `config/providerRegistry.ts`
3. Προσθέστε τον στο εργοστάσιο του `executors/index.ts`
4. Προσθέστε δοκιμές μονάδας για τον εκτελεστή
5. Τεκμηριώστε τον στο `docs/architecture/ARCHITECTURE.md`

### Προσθήκη Νέου Εργαλείου MCP

1. Δημιουργήστε ή ενημερώστε το `open-sse/mcp-server/tools/[category]Tools.ts`
2. Ορίστε σχήμα Zod για τις εισόδους
3. Καταχωρίστε το εργαλείο στο `mcp-server/index.ts`
4. Προσθέστε το στον πίνακα εμβελειών στο `mcp-server/auth/`
5. Προσθέστε δοκιμές μονάδας

---

## Δείτε Επίσης

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — αρχιτεκτονική υψηλού επιπέδου
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — αναφορά μηχανικής
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — ανάλυση ανά κατάλογο
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — βαθμολόγηση 16 παραγόντων
- [MCP-SERVER.md](./MCP-SERVER.md) — διακομιστής MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — διακομιστής A2A
- Πηγαίος κώδικας: `open-sse/` (400+ αρχεία, ~143K γραμμές κώδικα)
