# Cost & Spend Tracking (Ελληνικά)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/COST_TRACKING.md) · 🇪🇹 [am](../../../am/docs/guides/COST_TRACKING.md) · 🇸🇦 [ar](../../../ar/docs/guides/COST_TRACKING.md) · 🇦🇿 [az](../../../az/docs/guides/COST_TRACKING.md) · 🇧🇬 [bg](../../../bg/docs/guides/COST_TRACKING.md) · 🇧🇩 [bn](../../../bn/docs/guides/COST_TRACKING.md) · 🇧🇦 [bs](../../../bs/docs/guides/COST_TRACKING.md) · 🇨🇿 [cs](../../../cs/docs/guides/COST_TRACKING.md) · 🇩🇰 [da](../../../da/docs/guides/COST_TRACKING.md) · 🇩🇪 [de](../../../de/docs/guides/COST_TRACKING.md) · 🇪🇸 [es](../../../es/docs/guides/COST_TRACKING.md) · 🇪🇪 [et](../../../et/docs/guides/COST_TRACKING.md) · 🇮🇷 [fa](../../../fa/docs/guides/COST_TRACKING.md) · 🇫🇮 [fi](../../../fi/docs/guides/COST_TRACKING.md) · 🇫🇷 [fr](../../../fr/docs/guides/COST_TRACKING.md) · 🇮🇪 [ga](../../../ga/docs/guides/COST_TRACKING.md) · 🇮🇳 [gu](../../../gu/docs/guides/COST_TRACKING.md) · 🇳🇬 [ha](../../../ha/docs/guides/COST_TRACKING.md) · 🇮🇱 [he](../../../he/docs/guides/COST_TRACKING.md) · 🇮🇳 [hi](../../../hi/docs/guides/COST_TRACKING.md) · 🇭🇷 [hr](../../../hr/docs/guides/COST_TRACKING.md) · 🇭🇺 [hu](../../../hu/docs/guides/COST_TRACKING.md) · 🇦🇲 [hy](../../../hy/docs/guides/COST_TRACKING.md) · 🇮🇩 [id](../../../id/docs/guides/COST_TRACKING.md) · 🇳🇬 [ig](../../../ig/docs/guides/COST_TRACKING.md) · 🇮🇹 [it](../../../it/docs/guides/COST_TRACKING.md) · 🇯🇵 [ja](../../../ja/docs/guides/COST_TRACKING.md) · 🇬🇪 [ka](../../../ka/docs/guides/COST_TRACKING.md) · 🇰🇭 [km](../../../km/docs/guides/COST_TRACKING.md) · 🇮🇳 [kn](../../../kn/docs/guides/COST_TRACKING.md) · 🇰🇷 [ko](../../../ko/docs/guides/COST_TRACKING.md) · 🇱🇹 [lt](../../../lt/docs/guides/COST_TRACKING.md) · 🇱🇻 [lv](../../../lv/docs/guides/COST_TRACKING.md) · 🇮🇳 [ml](../../../ml/docs/guides/COST_TRACKING.md) · 🇮🇳 [mr](../../../mr/docs/guides/COST_TRACKING.md) · 🇲🇾 [ms](../../../ms/docs/guides/COST_TRACKING.md) · 🇲🇹 [mt](../../../mt/docs/guides/COST_TRACKING.md) · 🇲🇲 [my](../../../my/docs/guides/COST_TRACKING.md) · 🇳🇵 [ne](../../../ne/docs/guides/COST_TRACKING.md) · 🇳🇱 [nl](../../../nl/docs/guides/COST_TRACKING.md) · 🇳🇴 [no](../../../no/docs/guides/COST_TRACKING.md) · 🇮🇳 [or](../../../or/docs/guides/COST_TRACKING.md) · 🇮🇳 [pa](../../../pa/docs/guides/COST_TRACKING.md) · 🇵🇭 [phi](../../../phi/docs/guides/COST_TRACKING.md) · 🇵🇱 [pl](../../../pl/docs/guides/COST_TRACKING.md) · 🇵🇹 [pt](../../../pt/docs/guides/COST_TRACKING.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/COST_TRACKING.md) · 🇷🇴 [ro](../../../ro/docs/guides/COST_TRACKING.md) · 🇷🇺 [ru](../../../ru/docs/guides/COST_TRACKING.md) · 🇱🇰 [si](../../../si/docs/guides/COST_TRACKING.md) · 🇸🇰 [sk](../../../sk/docs/guides/COST_TRACKING.md) · 🇸🇮 [sl](../../../sl/docs/guides/COST_TRACKING.md) · 🇷🇸 [sr](../../../sr/docs/guides/COST_TRACKING.md) · 🇸🇪 [sv](../../../sv/docs/guides/COST_TRACKING.md) · 🇰🇪 [sw](../../../sw/docs/guides/COST_TRACKING.md) · 🇮🇳 [ta](../../../ta/docs/guides/COST_TRACKING.md) · 🇮🇳 [te](../../../te/docs/guides/COST_TRACKING.md) · 🇹🇭 [th](../../../th/docs/guides/COST_TRACKING.md) · 🇹🇷 [tr](../../../tr/docs/guides/COST_TRACKING.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/COST_TRACKING.md) · 🇵🇰 [ur](../../../ur/docs/guides/COST_TRACKING.md) · 🇺🇿 [uz](../../../uz/docs/guides/COST_TRACKING.md) · 🇻🇳 [vi](../../../vi/docs/guides/COST_TRACKING.md) · 🇳🇬 [yo](../../../yo/docs/guides/COST_TRACKING.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/COST_TRACKING.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/COST_TRACKING.md)

---

Πώς το OmniRoute εκτιμά, καταγράφει και αναφέρει το κόστος κάθε αιτήματος — και γιατί ο
αριθμός στον πίνακα ελέγχου είναι ένας **μετρητής εξοικονόμησης**, όχι λογαριασμός.

Δείτε επίσης: [Οδηγός Χρήστη](./USER_GUIDE.md) · [Συλλογή Δυνατοτήτων](./FEATURES.md)

---

## Τι είναι (και τι δεν είναι)

Το OmniRoute αποδίδει ένα κόστος σε USD ανά αίτημα σε κάθε ολοκλήρωση, πολλαπλασιάζοντας
τον αριθμό των token με τις τιμές χρέωσης ενός μοντέλου. Αυτοί οι αριθμοί τροφοδοτούν τον
πίνακα ελέγχου **Κόστη**, το CLI `omniroute cost` / `omniroute usage`, τις εξαγωγές
CSV/JSON και τους προϋπολογισμούς ανά κλειδί API.

> **Το «κόστος» στον πίνακα ελέγχου είναι ένας μετρητής εξοικονόμησης, όχι λογαριασμός.**
> Το OmniRoute δεν σας χρεώνει ποτέ — δρομολογεί τα αιτήματά σας σε παρόχους που έχετε
> ήδη συνδέσει (δικές σας συνδρομές, δωρεάν επίπεδα και κλειδιά API). Ένα «συνολικό
> κόστος $290» που έχει συσσωρευτεί εξ ολοκλήρου σε δωρεάν μοντέλα σημαίνει περίπου
> **$290 που _δεν_ πληρώσατε** σε ένα επί πληρωμή API. Το ποσό είναι μια _εκτίμηση_
> του κόστους που θα είχε η ίδια κίνηση με τις τυπικές τιμές καταλόγου, ώστε να μπορείτε
> να βλέπετε πού συγκεντρώνεται η χρήση σας και πόσα εξοικονομείτε μέσω της δρομολόγησης
> σε φθηνότερους/δωρεάν παρόχους.

Αυτή η οπτική διατυπώνεται απευθείας στο [README](../../README.md) του έργου («το
'κόστος' στον πίνακα ελέγχου είναι ένας μετρητής εξοικονόμησης, όχι λογαριασμός»).

Επειδή ο αριθμός είναι εκτίμηση:

- Εξαρτάται από τον πίνακα τιμολόγησης που διαθέτει το OmniRoute για κάθε μοντέλο. Ένα
  μοντέλο χωρίς καταχώριση τιμολόγησης συνεισφέρει κόστος `0` (εμφανίζεται ως γραμμή
  «Παλαιού τύπου / Δωρεάν» στην εξερεύνηση).
- Η κίνηση από δωρεάν επίπεδα και συνδρομές εξακολουθεί να συσσωρεύει ένα _εκτιμώμενο_
  κόστος — αυτό είναι το ποσό που εξοικονομείτε, όχι ένα οφειλόμενο ποσό.

---

## Πώς εκτιμώνται τα κόστη

### Η πηγή τιμολόγησης

Τα κόστη προέρχονται από έναν πίνακα τιμολόγησης που επιλέγεται με την ακόλουθη σειρά
προτεραιότητας ([`src/lib/pricingSync.ts`](../../src/lib/pricingSync.ts)):

1. **Παρακάμψεις χρήστη** — τιμές που ορίζετε στον πίνακα ελέγχου / μέσω του `PATCH /api/pricing`.
2. **Συγχρονισμένη εξωτερική τιμολόγηση** — ανακτάται από το δημόσιο
   `model_prices_and_context_window.json` του LiteLLM όταν είναι ενεργοποιημένος ο
   συγχρονισμός (αποθηκεύεται σε ξεχωριστό χώρο ονομάτων `pricing_synced`, ώστε να μην
   αντικαθιστά ποτέ τις παρακάμψεις σας).
3. **Ενσωματωμένες προεπιλογές** — παρέχονται μαζί με το OmniRoute.

Ο συγχρονισμός εξωτερικής τιμολόγησης είναι **προαιρετικός** και απενεργοποιημένος από
προεπιλογή. Σχετικές μεταβλητές περιβάλλοντος
(δείτε το [`.env.example`](../../.env.example)):

| Μεταβλητή περιβάλλοντος | Προεπιλογή | Σκοπός                                                                              |
| ----------------------- | ---------- | ----------------------------------------------------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`    | Ενεργοποίηση του συγχρονισμού τιμολόγησης LiteLLM στο παρασκήνιο κατά την εκκίνηση. |
| `PRICING_SYNC_INTERVAL` | `86400`    | Διάστημα συγχρονισμού σε **δευτερόλεπτα** (από προεπιλογή καθημερινά).              |
| `PRICING_SYNC_SOURCES`  | `litellm`  | Λίστα πηγών διαχωρισμένων με κόμματα (σήμερα υποστηρίζεται μόνο το `litellm`).      |

### Ο τύπος υπολογισμού του κόστους

Το κόστος υπολογίζεται ανά αίτημα από τον αριθμό των token και τις τιμές ανά εκατομμύριο
token στο [`src/lib/usage/costCalculator.ts`](../../src/lib/usage/costCalculator.ts)
(`computeCostFromPricing` / `calculateCost`):

- **Token εισόδου** (μείον τις αναγνώσεις cache και τα token δημιουργίας cache) × τιμή `input`.
- **Token ανάγνωσης cache** × τιμή `cached` (χρησιμοποιείται ως εναλλακτική η τιμή εισόδου).
- **Token δημιουργίας cache** × τιμή `cache_creation` (χρησιμοποιείται ως εναλλακτική η τιμή εισόδου).
- **Token εξόδου** × τιμή `output`.
- **Token συλλογισμού** × τιμή `reasoning` (χρησιμοποιείται ως εναλλακτική η τιμή εξόδου).

Όλες οι τιμές ερμηνεύονται ως USD ανά 1.000.000 token. Ένα επίπεδο υπηρεσίας Codex
«fast»/«priority» ή «flex» εφαρμόζει έναν πολλαπλασιαστή κόστους
(`getCodexFastCostMultiplier`) — π.χ. το flex χρεώνεται με έκπτωση 50% στα token, η
οποία εμφανίζεται ως **εξοικονόμηση flex** στον πίνακα ελέγχου.

Τα ονόματα μοντέλων κανονικοποιούνται πρώτα (αφαιρούνται προθέματα διαδρομών παρόχων,
όπως `openai/` ή `accounts/fireworks/models/`), ώστε οι ιστορικές γραμμές να συνεχίζουν
να αντιστοιχίζονται σε κάποια τιμή.

### Πώς καταγράφεται η δαπάνη

- Το κόστος ανά αίτημα υπολογίζεται μετά την απόκριση και καταγράφεται ασύγχρονα, χωρίς
  αναμονή, ώστε να μην προσθέτει ποτέ καθυστέρηση για τον πελάτη. Η κατανάλωση
  κοινόχρηστου ορίου προγραμματίζεται στον επόμενο κύκλο του βρόχου συμβάντων μέσω του
  [`src/lib/quota/spendRecorder.ts`](../../src/lib/quota/spendRecorder.ts).
- Η δαπάνη ανά κλειδί API αποθηκεύεται προσωρινά σε buffer και εγγράφεται ομαδικά από
  το [`SpendBatchWriter`](../../src/lib/spend/batchWriter.ts) (προεπιλεγμένο διάστημα
  εγγραφής 60s, buffer 1.000 καταχωρίσεων). Ρυθμίζεται μέσω των εξής:

  | Μεταβλητή περιβάλλοντος             | Προεπιλογή | Σκοπός                                                         |
  | ----------------------------------- | ---------- | -------------------------------------------------------------- |
  | `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` | `60000`    | Διάστημα εγγραφής σε χιλιοστά του δευτερολέπτου.               |
  | `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`   | `1000`     | Μέγιστος αριθμός καταχωρίσεων στο buffer πριν από την εγγραφή. |

Τα ποσά κόστους στον πίνακα ελέγχου **δεν** διαβάζονται από κάποιο αποθηκευμένο ποσό σε
δολάρια ανά γραμμή — επανυπολογίζονται δυναμικά από τον αριθμό των token και τον
τρέχοντα πίνακα τιμολόγησης κάθε φορά που εκτελείται το τελικό σημείο αναλυτικών
στοιχείων. Αυτό σημαίνει ότι η διόρθωση μιας λανθασμένης τιμής (και ο εκ νέου
συγχρονισμός) ενημερώνει αναδρομικά τις εκτιμήσεις ιστορικού κόστους.

---

## Πίνακας ελέγχου: η σελίδα Κόστους

Η σελίδα **Κόστος** βρίσκεται στη διαδρομή `/dashboard/costs`
(`src/app/(dashboard)/dashboard/costs/`).
Η κύρια προβολή της είναι η καρτέλα **Επισκόπηση κόστους**
(`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`),
η οποία φορτώνει όλα τα δεδομένα από το `GET /api/usage/analytics`.

Τι εμφανίζει:

- **Πλακίδια δαπανών** — εκτιμώμενη δαπάνη για _Σήμερα (1d)_, _7d_, _30d_ και το επιλεγμένο
  χρονικό διάστημα. Επιλογέας εύρους: `7d`, `30d`, `90d`, `all`.
- **Κύριες μετρήσεις** — αιτήματα εντός του χρονικού διαστήματος, ενεργοί πάροχοι, ενεργά μοντέλα, μέσο
  κόστος ανά αίτημα.
- **Εξερεύνηση κόστους** — ένας πίνακας με δυνατότητα ταξινόμησης/φιλτραρίσματος, ομαδοποιημένος κατά **πάροχο**, **μοντέλο**,
  **κλειδί API**, **λογαριασμό** ή **επίπεδο υπηρεσίας**, με κόστος, αιτήματα, tokens, μέσο
  κόστος/αίτημα και ποσοστό επί του συνόλου.
- **Χρήση tokens** — συνολικά / εισόδου / εξόδου tokens και αναλογία εισόδου:εξόδου.
- **Αποδοτικότητα δρομολόγησης** — πλήθος εναλλακτικών δρομολογήσεων, ποσοστό εναλλακτικών δρομολογήσεων και κάλυψη ζητούμενου μοντέλου.
- **Μηνιαία πρόβλεψη** — προβλέπει τη δαπάνη έως το τέλος του μήνα βάσει του πρόσφατου ημερήσιου μέσου όρου.
- **Σύγκριση περιόδων** — ποσοστιαία μεταβολή μεταξύ του πρώτου και του δεύτερου μισού του χρονικού διαστήματος.
- **Γραφήματα** — ημερήσια τάση κόστους, μερίδιο παρόχων (πίτα), κορυφαίοι πάροχοι, κορυφαία μοντέλα, κόστος
  ανά κλειδί API, κόστος ανά λογαριασμό, εβδομαδιαίο μοτίβο χρήσης και θερμικός χάρτης δραστηριότητας.
- **Εξαγωγή** — λήψη του τρέχοντος χρονικού διαστήματος ως **CSV** ή **JSON** (τα κουμπιά εμφανίζονται
  μόλις υπάρχουν δεδομένα με κόστος μεγαλύτερο του μηδενός).

Όταν δεν υπάρχει κίνηση με τιμολόγηση, οι γραμμές εμφανίζουν την ετικέτα "Παλαιού τύπου / Δωρεάν" αντί για `$0`,
αντικατοπτρίζοντας το μοντέλο παρακολούθησης εξοικονόμησης.

### Σχετικές υποσελίδες Κόστους

Η περιοχή Κόστους περιλαμβάνει επίσης (όλες κάτω από `/dashboard/costs/`):

- **Τιμολόγηση** (`/dashboard/costs/pricing`) — προβολή και παράκαμψη τιμών ανά μοντέλο (αποδίδει
  την κοινόχρηστη καρτέλα Τιμολόγησης).
- **Προϋπολογισμός** (`/dashboard/costs/budget`) — ορισμός ορίων δαπανών ανά πεδίο εφαρμογής (αποδίδει την κοινόχρηστη
  καρτέλα Προϋπολογισμού).
- **Κοινή χρήση ποσόστωσης** (`/dashboard/costs/quota-share`) — κοινόχρηστες δεξαμενές ποσόστωσης και προβολές ρυθμού
  κατανάλωσης.

---

## Τερματικά σημεία API

Όλα απαιτούν έλεγχο ταυτότητας διαχείρισης (loopback/JWT, μέσω `requireManagementAuth`), εκτός εάν
αναφέρεται διαφορετικά.

### Αναλυτικά στοιχεία χρήσης και κόστους

| Μέθοδος | Τερματικό σημείο         | Σκοπός                                                                                                                                                                                            |
| ------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`   | `/api/usage/analytics`   | Πλήρη αναλυτικά στοιχεία κόστους/χρήσης: σύνοψη, ημερήσια τάση, ανά πάροχο/μοντέλο/κλειδί API/λογαριασμό/επίπεδο. Παράμετροι ερωτήματος: `range`, `startDate`, `endDate`, `apiKeyIds`, `presets`. |
| `GET`   | `/api/usage/utilization` | Χρήση ποσόστωσης ανά πάροχο με την πάροδο του χρόνου. Παράμετροι ερωτήματος: `range` (`1h`/`24h`/`7d`/`30d`), `provider`.                                                                         |
| `GET`   | `/api/usage/history`     | Ακατέργαστες εγγραφές ιστορικού χρήσης.                                                                                                                                                           |
| `GET`   | `/api/usage/call-logs`   | Αρχεία καταγραφής κλήσεων ανά αίτημα (μοντέλο, tokens, κόστος, καθυστέρηση, κατάσταση).                                                                                                           |
| `GET`   | `/api/usage/quota`       | Κατάσταση ποσόστωσης παρόχου.                                                                                                                                                                     |
| `GET`   | `/api/usage/proxy-logs`  | Αρχεία καταγραφής αιτημάτων διακομιστή μεσολάβησης.                                                                                                                                               |

### Προϋπολογισμοί

| Μέθοδος | Τερματικό σημείο         | Σκοπός                                                                                                      |
| ------- | ------------------------ | ----------------------------------------------------------------------------------------------------------- |
| `GET`   | `/api/usage/budget`      | Σύνοψη κόστους + έλεγχος προϋπολογισμού για ένα κλειδί API (απαιτείται η παράμετρος ερωτήματος `apiKeyId`). |
| `POST`  | `/api/usage/budget`      | Ορισμός ημερήσιων/εβδομαδιαίων/μηνιαίων ορίων USD + ορίου προειδοποίησης για ένα κλειδί API.                |
| `GET`   | `/api/usage/budget/bulk` | Μαζικές συνόψεις προϋπολογισμού για πολλά κλειδιά API.                                                      |

> Το API προϋπολογισμού έχει πεδίο εφαρμογής ανά **κλειδί API** (`apiKeyId`). Τα όρια που επιστρέφονται από το
> `GET /api/usage/budget` περιλαμβάνουν τα `dailyLimitUsd`, `weeklyLimitUsd`, `monthlyLimitUsd`,
> ένα `warningThreshold` και τα τρέχοντα σύνολα (`totalCostToday`, `totalCostMonth`, …).

### Τιμολόγηση

| Μέθοδος  | Τερματικό σημείο        | Σκοπός                                                                                                                               |
| -------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `GET`    | `/api/pricing`          | Τρέχουσα συγχωνευμένη τιμολόγηση (χρήστη + συγχρονισμένη + προεπιλεγμένη). `?includeSources=1` για προβολή της πηγής ανά καταχώριση. |
| `PATCH`  | `/api/pricing`          | Παράκαμψη τιμολόγησης για `{ provider: { model: { input, output, cached, … } } }`.                                                   |
| `DELETE` | `/api/pricing`          | Επαναφορά της τιμολόγησης στις προεπιλογές (προαιρετικά περιορισμένη μέσω `?provider=&model=`).                                      |
| `GET`    | `/api/pricing/defaults` | Εμφάνιση προεπιλεγμένων εφεδρικών τιμών ανά 1M.                                                                                      |
| `GET`    | `/api/pricing/models`   | Τιμολόγηση με κλειδί το μοντέλο.                                                                                                     |
| `POST`   | `/api/pricing/sync`     | Ενεργοποίηση χειροκίνητου συγχρονισμού από εξωτερικές πηγές (LiteLLM).                                                               |
| `GET`    | `/api/pricing/sync`     | Τρέχουσα κατάσταση συγχρονισμού.                                                                                                     |
| `DELETE` | `/api/pricing/sync`     | Εκκαθάριση όλων των συγχρονισμένων δεδομένων τιμολόγησης.                                                                            |

### Άλλα τερματικά σημεία σχετικά με το κόστος

| Μέθοδος | Τελικό σημείο                 | Σκοπός                                                                                 |
| ------- | ----------------------------- | -------------------------------------------------------------------------------------- |
| `GET`   | `/api/free-tier/summary`      | Σύνολα διακριτικών δωρεάν μοντέλων, χρήση αυτού του μήνα και υπολειπόμενο δωρεάν όριο. |
| `GET`   | `/api/quota/pools/[id]/usage` | Χρήση για μια ομάδα κοινόχρηστου ορίου.                                                |

---

## CLI

Το CLI του OmniRoute παρέχει εντολές κόστους, χρήσης και τιμολόγησης (καταχωρισμένες στο
[`bin/cli/commands/registry.mjs`](../../bin/cli/commands/registry.mjs)).

### `omniroute cost`

Μια αναφορά κόστους συγκεντρωμένη από το `/api/usage/analytics`.

```bash
omniroute cost                          # τελευταίες 30 ημέρες, ομαδοποίηση ανά πάροχο
omniroute cost --period 7d              # τελευταίες 7 ημέρες
omniroute cost --group-by model         # ομαδοποίηση ανά provider | model | combo | api-key | day
omniroute cost --since 2026-06-01 --until 2026-06-13
omniroute cost --api-key <key> --limit 50
```

Στήλες: ομάδα, αιτήματα, διακριτικά εισόδου/εξόδου, κόστος (USD) και % επί του συνόλου. Στο τέλος
εμφανίζεται μια γραμμή γενικού συνόλου (παραλείπεται με `--quiet` ή `--output json`).

### `omniroute usage`

```bash
omniroute usage analytics --period 30d [--provider <id>]   # σύνοψη κόστους ανά πάροχο
omniroute usage logs [--limit 100] [--follow] [--api-key <k>] [--search <q>]
omniroute usage quota [--provider <id>] [--check]
omniroute usage utilization [--api-key <k>]
omniroute usage history [--limit 100]
omniroute usage proxy-logs [--limit 100]

# Προϋπολογισμοί
omniroute usage budget list
omniroute usage budget get [scope]
omniroute usage budget set <amount> [--scope global] [--period monthly]
omniroute usage budget reset [scope]
```

### `omniroute pricing`

```bash
omniroute pricing list [--provider <p>] [--model <m>] [--limit 200]
omniroute pricing get <model>
omniroute pricing sync [--provider <p>] [--force]   # POST /api/pricing/sync
omniroute pricing diff [--model <m>]
omniroute pricing defaults show
omniroute pricing defaults set [--input <p>] [--output <p>] [--cache-read <p>] [--cache-write <p>]
```

> Το `pricing defaults show` διαβάζει το `GET /api/pricing/defaults`. Για να επεξεργαστείτε τις τιμές
> μεμονωμένων μοντέλων, χρησιμοποιήστε τη σελίδα **Pricing** του πίνακα ελέγχου ή το `PATCH /api/pricing`.

---

## Αντιμετώπιση προβλημάτων

- **Όλα τα κόστη εμφανίζονται ως $0 / "Legacy / Free".** Τα μοντέλα που χρησιμοποιούνται δεν έχουν καταχώριση τιμολόγησης.
  Ενεργοποιήστε τον εξωτερικό συγχρονισμό (`PRICING_SYNC_ENABLED=true`) και εκτελέστε `omniroute pricing sync` ή
  ορίστε τις τιμές χειροκίνητα μέσω της σελίδας Pricing / `PATCH /api/pricing`.
- **Ένα παλαιότερο μοντέλο έχει λανθασμένη τιμή.** Διορθώστε την τιμή (παράκαμψη ή εκ νέου συγχρονισμός) — το κόστος
  επανυπολογίζεται από τον αριθμό των διακριτικών σε κάθε ανάγνωση αναλυτικών στοιχείων, επομένως οι εκτιμήσεις ενημερώνονται αναδρομικά.
- **Η δαπάνη καθυστερεί σε σχέση με τον πραγματικό χρόνο.** Η δαπάνη ανά κλειδί υποβάλλεται σε επεξεργασία κατά παρτίδες· μειώστε το
  `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` εάν χρειάζεστε πιο πρόσφατα δεδομένα.

---

Για να δείτε πού εντάσσεται αυτό στον ευρύτερο πίνακα ελέγχου, ανατρέξτε στον [Οδηγό χρήστη](./USER_GUIDE.md) και
στη [Συλλογή δυνατοτήτων](./FEATURES.md).
