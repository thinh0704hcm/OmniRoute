# open-sse Architecture (Filipino)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **TL;DR**: Ang `open-sse/` ang pangunahing streaming engine na nagpapatakbo sa bawat kahilingan sa LLM sa OmniRoute. Naglalaman ito ng ~1,400 file na nagpapatupad sa pipeline ng kahilingan, mga executor, serbisyo, MCP server, at translation layer. Ipinapaliwanag ng gabay na ito kung paano nagtutugma ang mga bahagi.

**Pinagmulan:** `open-sse/` (workspace package, ~1,440 non-test na `.ts` file)

---

## Bakit Hiwalay na Workspace Package?

Ang `open-sse/` ay isang **standalone workspace** sa OmniRoute monorepo dahil sa ilang kadahilanan:

1. **Muling paggamit** — inilalathala ang `open-sse` bilang `@omniroute/open-sse` sa npm, kaya maaari itong gamitin nang hiwalay ng ibang mga proyekto
2. **Malinaw na mga hangganan** — nakahiwalay ang streaming engine mula sa UI/DB layer na partikular sa OmniRoute
3. **Pagganap** — walang dependency sa Next.js ang engine, kaya mas mabilis ang mga cold start sa mga kontekstong CLI/serverless
4. **Pagbe-version** — maaaring maglabas ng release ang `open-sse` ayon sa sarili nitong iskedyul

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Pangunahing Istruktura

```
open-sse/
├── index.ts              # Pampublikong entry point
├── types.d.ts            # Mga pampublikong type export
├── package.json          # @omniroute/open-sse
├── config/               # Mga config ng provider, constant, at registry
├── executors/            # Mga HTTP executor para sa bawat provider (67 + base.ts/index.ts)
├── handlers/             # Mga handler ng kahilingan (chatCore, responses, atbp.)
├── lib/                  # Mga panloob na utility
├── mcp-server/           # Model Context Protocol server
├── services/             # ~298 module ng serbisyo
├── transformer/          # Transformer ng format ng Responses API
├── translator/           # Pagsasalin ng format (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Mga nakabahaging utility (logging, error, stream, atbp.)
```

### Bilang ng mga Module

| Direktoryo | Mga File | Layunin |
| `executors/` | 167 | Mga HTTP executor para sa bawat provider (pinag-isa sa pamamagitan ng DefaultExecutor factory) |
| `handlers/` | 157 | Mga entry point ng kahilingan (chatCore, responses, embeddings) |
| `services/` | ~536 | Routing, caching, rate limiting, refresh, atbp. |
| `translator/` | 56 | Pag-convert ng format (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Mga MCP tool at transport |
| `utils/` | ~108 | Mga utility na ginagamit sa iba't ibang bahagi (logging, error, stream) |
| `config/` | ~339 | Mga config ng provider, constant, at registry |

---

## Ang Pipeline ng Kahilingan

Dumadaan ang bawat kahilingan sa LLM sa isang **5-yugtong pipeline**:

```
                              ┌──────────────┐
   Kahilingang HTTP            │  1. ROUTE    │   paglutas ng combo, pagpili ng model
   (Next.js route)             └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TRANSLATE│   pag-convert ng format (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. EXECUTE  │   executor ng provider, HTTP, retry, breaker
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAM   │   SSE transformation, backpressure
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. RECORD   │   pagsubaybay sa paggamit, call log, pag-uuri ng error
                              └──────┬───────┘
                                     │
                                     ▼
                              Tugon na HTTP (SSE o JSON)
```

### Yugto 1: Route (services/combo.ts)

**Entry point**: `handleComboChat()` sa `services/combo.ts`

Nireresolba ang kahilingan tungo sa isang kongkretong `(provider, model, account, credentials)` tuple:

- Hanapin ang combo gamit ang ID (o bumuo ng virtual combo para sa mga `auto/*` model)
- Ilapat ang routing strategy (priority, weighted, round-robin, atbp.)
- Salain at alisin ang mga hindi maayos na provider (circuit breaker)
- Piliin ang susunod na maaaring gamiting target

Para sa mga `auto/*` model, ginagawa rin ng yugtong ito ang sumusunod:

- Pinapatakbo ang **16-factor scoring** algorithm (`services/autoCombo/`)
- Pumipili ng pares na `provider+model` batay sa health, gastos, latency, atbp.

### Yugto 2: Translate (translator/)

Kung naiiba ang pinagmulang format (hal., OpenAI) sa target na format (hal., Claude), **isinasalin** ang kahilingan:

- System prompt → system message
- Mga depinisyon ng tool → format ng tool na partikular sa provider
- Mga parameter ng reasoning/thinking → mga katumbas na partikular sa provider
- Normalisasyon ng message role (`developer` → `system` para sa hindi OpenAI)

Inilalantad ng `translator/index.ts` ang:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Yugto 3: Execute (executors/)

**Entry point**: `getExecutor(providerId).execute(request, options)`

Ginagamit ng lahat ng provider ang `DefaultExecutor` (`executors/default.ts`) sa pamamagitan ng fallback ng `getExecutor()` factory. Ginagawa ng executor ang sumusunod:

- Binubuo ang upstream URL (`buildUrl()`)
- Nagdaragdag ng mga header na partikular sa provider (`buildHeaders()`)
- Tina-transform ang body ng kahilingan (`transformRequest()`)
- Ipinapadala ang kahilingang HTTP na may retry + exponential backoff
- Pinangangasiwaan ang auth refresh kung kinakailangan (mga OAuth provider)

Lahat ng executor ay nag-e-extend sa `BaseExecutor` (`executors/base.ts`, 1170 LOC), na nagbibigay ng:

- Karaniwang retry logic
- Proxy integration
- Circuit breaker integration
- Mga hook sa pagtatala ng paggamit

### Yugto 4: Stream (utils/stream.ts)

Para sa mga streaming response, nagbabalik ang executor ng **ReadableStream**. Ginagawa ng handler ang sumusunod:

- Ipinapadaan sa isang SSE transform (`createSSETransformStreamWithLogger`)
- Naglalapat ng mga heartbeat ping upang matukoy ang mga naputol na koneksyon
- Maayos na pinangangasiwaan ang pag-disconnect ng client (`pipeWithDisconnect`)
- Tina-transform ang SSE → JSON para sa mga non-streaming client

Para sa mga non-streaming response, nagbabalik ang executor ng na-parse na JSON object na ipinapasa nang walang pagbabago.

### Yugto 5: Record (services/usage.ts)

Pagkatapos ng tugon (tagumpay man o kabiguan), itinatala ang paggamit:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` mula sa tugon
- `cost_usd` na kinuwenta mula sa datos ng pagpepresyo
- `latency_ms`, `status`, `error_class` kung nabigo
- Iniimbak sa talahanayang `usage_history`

Ang mga artifact ng log ng tawag (kung naka-enable) ay isinusulat sa `${DATA_DIR}/call_logs/`.

---

## Masusing Pagsusuri sa Mahahalagang File

### chatCore.ts (5977 linya)

Ang **pangunahing tagapangasiwa ng request**. Sa kabila ng laki nito, mayroon itong malinaw na istruktura:

```ts
// Pseudo-istruktura ng chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Auth + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Pagpapatunay ng body
  const body = await parseRequestBody(request);

  // 3. Pagtukoy + pagsasalin ng format
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Combo routing
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Magpatuloy sa susunod na target
    }
  }

  // 5. Pang-emergency na fallback
  return await emergencyFallback(body);
}
```

Sa kabila ng pagiging isang napakalaking function, nakaayos ito sa mga **seksyon na may komento** na tumutugma sa 5-yugtong pipeline.

### combo.ts (4456 LOC)

Ang **routing engine** na nagre-resolve ng combo tungo sa mga target na nakaayos ayon sa pagkakasunod-sunod.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Sinusuportahan ang **19 na routing strategy** (tingnan ang `src/shared/constants/routingStrategies.ts`):

| Strategy            | Pag-uugali                                                                                              |
| ------------------- | ------------------------------------------------------------------------------------------------------- |
| `priority`          | Listahang inuuna ang unang target                                                                       |
| `weighted`          | Probabilistiko batay sa timbang ng bawat target                                                         |
| `round-robin`       | Umikot sa mga target ayon sa pagkakasunod-sunod                                                         |
| `context-relay`     | Ipasa ang context sa iba't ibang target                                                                 |
| `fill-first`        | Punuin ang quota bago lumipat sa susunod                                                                |
| `p2c`               | Kapangyarihan ng dalawang pagpipilian                                                                   |
| `random`            | Pantay-pantay na random                                                                                 |
| `least-used`        | Piliin ang may pinakakaunting kamakailang paggamit                                                      |
| `cost-optimized`    | Unahin ang pinakamurang maayos na target                                                                |
| `reset-aware`       | Isinasaalang-alang ang mga reset window ng provider                                                     |
| `reset-window`      | Routing batay sa reset window                                                                           |
| `headroom`          | Unahin ang may pinakamaraming natitirang quota headroom                                                 |
| `strict-random`     | Talagang pantay-pantay (walang pagtitimbang ayon sa kalidad)                                            |
| `auto`              | Gumamit ng 16-factor scoring (`autoCombo/`)                                                             |
| `lkgp`              | Unahin ang huling provider na napatunayang maayos                                                       |
| `context-optimized` | Pinakamainam para sa mga request na may mahabang context                                                |
| `fusion`            | Ipadala sa isang panel nang sabay-sabay, pagkatapos ay pagsamahin sa pamamagitan ng judge (`fusion.ts`) |

### base.ts (1170 LOC)

Ang **abstract executor** na pinapalawak ng lahat ng 107 executor. Naglalaman ito ng:

- `buildUrl()` — default na pagbuo ng URL (ino-override ng mga subclass para sa custom na gawi)
- `buildHeaders()` — mga default na header (auth, content-type)
- `transformRequest()` — pass-through bilang default
- `execute()` — ang pangunahing HTTP loop na may retry/backoff/breaker

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Pinangangasiwaan ang lahat ng provider na compatible sa OpenAI/Anthropic
  // Nagrerehistro ang mga provider ng mga configuration (URL, auth, headers) ngunit iisa ang ginagamit na executor logic
}
```

Ang gawi na partikular sa provider (mga auth header, base URL, mga version header) ay kino-configure sa pamamagitan ng provider registry, hindi sa magkakahiwalay na executor class.

````

---

## Mga Serbisyo (117 module)

Ang mga serbisyo ay mga **nakatuon at may iisang layuning module** na pinagsasama-sama ng mga handler. Ang malalaking kategorya:

### Pagruruta at Combo

- `combo.ts` — entry point para sa mga kahilingang niruruta sa pamamagitan ng combo
- `services/autoCombo/` — 16-factor na pagmamarka, 8 awtomatikong estratehiya sa pagruruta
- `wildcardRouter.ts` — tumutugma sa mga wildcard route (`gpt-*`)
- `modelFamilyFallback.ts` — T5 fallback sa loob ng parehong pamilya

### Paglilimita sa Rate at Quota

- `rateLimitManager.ts` — token bucket kada key+provider
- `usage.ts` — pagtatala ng paggamit
- `quotaCache.ts` — mga in-memory na snapshot ng quota

### Account at Token

- `tokenRefresh.ts` — pag-refresh ng OAuth kapag may 401
- `accountFallback.ts` — lumipat sa alternatibong account
- `sessionManager.ts` — estado ng multi-turn na session

### Intelihensiya

- `intentClassifier.ts` — uriin ang layunin ng kahilingan
- `taskAwareRouter.ts` — iruta ayon sa uri ng gawain
- `thinkingBudget.ts` — maglaan ng mga token para sa pag-iisip
- `contextManager.ts` — mag-inject ng konteksto sa pagruruta

### Katatagan

- `resilience.ts` — orkestrasyon ng retry, backoff, at breaker
- `emergencyFallback.ts` — fallback bilang huling opsyon
- `modelDeprecation.ts` — awtomatikong iruta sa mga kapalit na modelo

### Estado

- `signatureCache.ts` — pag-aalis ng mga duplikado ayon sa signature ng kahilingan
- `volumeDetector.ts` — pagbabawas ng load
- `contextHandoff.ts` — serialization ng session

### Compression

- `compression/` (subdirectory) — kumpletong pipeline ng compression
- 39 na file na sumasaklaw sa mga engine, rule pack, at adapter

### Mga Skill

- (tinatalakay sa [SKILLS.md](./SKILLS.md))

### Memorya

- (tinatalakay sa [MEMORY.md](./MEMORY.md))

---

## Mga Executor (75+ file)

Isang file kada provider. Lahat ng ito ay nag-e-extend sa `BaseExecutor` at nag-o-override sa mga bahaging naiiba.

### Mga Karaniwang Pattern

Nire-resolve ang mga provider sa pamamagitan ng `getExecutor(providerId)`, na nagbabalik ng naka-configure na executor. Ginagamit ng mga provider na compatible sa OpenAI/Anthropic ang `DefaultExecutor` (`executors/default.ts`). Kino-configure sa `open-sse/config/providers/` ang gawi na partikular sa provider (base URL, mga auth header, bersyon ng API), habang pinangangasiwaan sa `open-sse/translator/` ang mga transformation sa request body.

Itinatakda ang **Custom URL** sa pamamagitan ng configuration ng provider:

```ts
// Configuration ng provider sa open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

Pinangangasiwaan ang **custom auth** sa pamamagitan ng auth configuration ng registry ng provider (API key, OAuth, mga header profile).

Ang mga transformation sa **custom request body** (halimbawa, paghihiwalay ng Anthropic sa `system` mula sa `messages`) ay nirerehistro kada provider sa `open-sse/translator/`.

````

### Ang Executor Factory

Ini-export ng `executors/index.ts` ang `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Dumadaan ang resolution sa `ExecutorRegistry` (`executors/registry.ts`): idinedeklara ang bawat espesyal na executor sa built-in na talahanayan ng `executors/index.ts` at nirerehistro sa pamamagitan ng `registerExecutor(alias, instance)` kapag nilo-load ang module; kinokonsulta ng `getExecutor()` ang registry at bumabalik sa isang memoized na `DefaultExecutor` para sa anumang provider na walang espesyal na entry. Inilalarawan ng golden test na `tests/unit/executor-map-golden.test.ts` ang kumpletong mapping ng alias → executor.

---

## Mga Tagapagsalin

Magsalin sa pagitan ng **3 format**: OpenAI, Anthropic, Gemini, pati na rin ang bagong Responses API.

### Kailan Isinasagawa ang Pagsasalin

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Mga karaniwang pagsasalin:

- `OpenAI → Anthropic`: hiwalay na `system` field, `x-api-key` header
- `OpenAI → Gemini`: `contents` sa halip na `messages`, `systemInstruction`
- `OpenAI → Responses API`: `input` array, `previous_response_id` state

### Mga Edge Case na Sinusuportahan

- `developer` role → `system` para sa hindi OpenAI
- `system` role → isinasama sa unang mensahe ng user para sa GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` ng Gemini
- `tools` → format ng tool na partikular sa provider
- Mga parameter ng pag-iisip (o1, Claude) → mga katumbas na partikular sa provider

---

## MCP Server

Ipinapatupad ng `open-sse/mcp-server/` ang server ng **Model Context Protocol**:

- **110 tool** (pamamahala ng provider, mga combo, memory, cache, compression, proxy, mga skill, gamification, mga plugin, Notion, Obsidian, lokal na corpus)
- **3 transport**: stdio, SSE, Streamable HTTP
- **33 scope** para sa detalyadong awtorisasyon

### Pagpaparehistro ng Tool

Inirerehistro ang mga tool bilang mga hiwalay na file sa `open-sse/mcp-server/tools/`, at bawat isa ay nag-e-export ng pangalan, schema, handler, at scope:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Mga Transport

```ts
// stdio (paggamit sa CLI)
startMcpStdio(server);

// SSE (streaming na nakabatay sa HTTP)
startMcpSse(server, port);

// Streamable HTTP (modernong MCP)
startMcpStreamable(server, port);
```

### Awtorisasyon

Dumaraan ang bawat pagtawag sa tool sa mga pagsusuri ng scope (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Mga Transformer

Kino-convert ng `open-sse/transformer/` ang mga format sa pagitan ng **Chat Completions** at **Responses API**.

### Bakit May Hiwalay na Transformer?

Ang Responses API ang bagong format ng OpenAI na may **mga stateful na pag-uusap** (`previous_response_id`). Kapag nagpapadala ang isang client ng kahilingan sa Responses, ginagawa ng OmniRoute ang sumusunod:

1. Kino-convert ang Responses → Chat Completions sa loob ng system
2. Ipinapadala sa provider (anumang provider na sumusuporta sa Chat Completions)
3. Kino-convert pabalik sa format ng Responses ang tugon
4. Ini-stream ang na-convert na tugon sa client

Ibinibigay ng transformer (`transformer/responsesTransformer.ts`) ang:

```ts
createResponsesApiTransformStream(): TransformStream
```

Pinangangasiwaan nito ang:

- Mga event na `response.output_item.added`
- Mga event na `response.output_text.delta`
- Event na `response.completed`
- Pagmamapa ng pagtawag sa tool (`function_call` ↔ `tool_calls`)

---

## Configuration

Nasa `open-sse/config/` ang layer ng configuration:

| File                          | Layunin                                            |
| ----------------------------- | -------------------------------------------------- |
| `providerRegistry.ts`         | Registry ng chat model sa katalogo ng 352 provider |
| `providerModels.ts`           | Mga alias ng model, pagmamapa ng format            |
| `constants.ts`                | Mga timeout, limitasyon, at status code            |
| `defaultThinkingSignature.ts` | Default na signature ng pag-iisip ng Claude        |
| `modelStrip.ts` (sa services) | Pag-aalis ng field para sa bawat provider          |

### Schema ng Provider Registry

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Tinitiyak ng Zod validation sa pag-load ng module na valid ang lahat ng configuration ng provider.

---

## Mga Limitasyon sa Performance

May mahihigpit na badyet sa performance ang routing engine:

| Operasyon                                   | Target | Pagsukat                               |
| ------------------------------------------- | ------ | -------------------------------------- |
| Pagresolba ng combo                         | <10ms  | Para sa 50 target                      |
| Pagsusuri ng rate limit                     | <1ms   | In-memory na token bucket              |
| Fallback ng model family                    | <5ms   | Mga naka-cache na depinisyon ng family |
| Pag-dispatch ng request routing             | <2ms   | Hot path                               |
| **Walang blocking I/O sa routing hot path** | —      | Lahat ay async                         |

---

## Mga Anti-Pattern

❌ **Mga synchronous na DB call sa `combo.ts`** — paunang kalkulahin at i-cache
❌ **Retry logic sa mga handler** — gamitin ang `retry()` mula sa resilience service
❌ **Direktang pag-access sa provider config** — gamitin ang mga getter ng `providerRegistry`
❌ **Mga hardcoded na fallback chain** — tukuyin sa `modelFamilyFallback.ts`
❌ **Mga pagbabago sa state sa magkakasabay na request** — gumamit lamang ng request-scoped na context

---

## Pagdaragdag ng Bagong Component

### Pagdaragdag ng Bagong Service

1. Gumawa ng `open-sse/services/[serviceName].ts` na may nakatuong responsibilidad
2. I-export ang pangunahing handler function at anumang constant
3. Magdagdag ng mga unit test sa `tests/unit/services/[serviceName].test.mjs`
4. Isama sa request pipeline sa `handlers/chatCore.ts` (kung nauugnay sa routing)
5. I-update ang routing logic sa `combo.ts` kung naaapektuhan ng service ang pagpili ng target
6. Idokumento sa file na ito

### Pagdaragdag ng Bagong Executor

1. Gumawa ng `open-sse/executors/[provider].ts` na nag-e-extend sa `BaseExecutor`
2. Irehistro sa `config/providerRegistry.ts`
3. Idagdag sa factory ng `executors/index.ts`
4. Magdagdag ng mga unit test para sa executor
5. Idokumento sa `docs/architecture/ARCHITECTURE.md`

### Pagdaragdag ng Bagong MCP Tool

1. Gumawa o mag-update ng `open-sse/mcp-server/tools/[category]Tools.ts`
2. Tukuyin ang Zod schema para sa mga input
3. Irehistro ang tool sa `mcp-server/index.ts`
4. Idagdag sa scope matrix sa `mcp-server/auth/`
5. Magdagdag ng mga unit test

---

## Tingnan Din

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — pangkalahatang arkitektura
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — sanggunian sa engineering
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — bawat directory
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16-factor na pagmamarka
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP server
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A server
- Source: `open-sse/` (400+ file, ~143K LOC)
