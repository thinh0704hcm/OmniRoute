# Stealth Guide (Română)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/security/STEALTH_GUIDE.md) · 🇩🇰 [da](../../../da/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇮🇩 [id](../../../id/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇮🇹 [it](../../../it/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Sursa adevărului:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Ultima actualizare:** 2026-09-02 — v3.8.51
> **Public-țintă:** Ingineri care întrețin integrări invizibile specifice furnizorilor.

OmniRoute se integrează cu furnizori ale căror infrastructuri de margine identifică activ clienții neoficiali prin amprentare (TLS JA3/JA4, ordinea antetelor, structura corpului JSON, tokenuri de integritate). Această pagină documentează suprafețele de disimulare expuse de OmniRoute și locurile în care sunt implementate.

## Notificare juridică și etică

Funcționalitățile de disimulare există pentru ca OmniRoute să poată acționa ca strat de compatibilitate între conturile oficiale deținute de utilizatori (Claude Code CLI, Codex, Antigravity, Cursor etc.) și API-ul unificat al OmniRoute. Acestea **nu** sunt destinate evitării detectării fraudelor, partajării credențialelor sau încălcării Condițiilor de utilizare ale furnizorilor. Responsabilii de întreținere se așteaptă ca operatorii să respecte condițiile de utilizare ale furnizorilor din amonte pe care le-au acceptat la crearea conturilor.

---

## Stratul de amprentare TLS

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Sesiunile persistente `wreq-js` sunt create în mod leneș pentru fiecare domeniu de cont și proxy rezolvat. La nivel de proces, `TlsClient` menține în grup cel mult 128 de sesiuni care imită **Chrome 124 pe macOS** pentru serviciile din amonte protejate de Cloudflare. `TlsClient.fetch()` eșuează în mod închis atunci când mediul de execuție nativ nu este disponibil; un apelant poate selecta explicit o soluție de rezervă în afara acestui wrapper.

- Profil de sesiune: `browser: "chrome_124", os: "macos"`
- Rezolvarea proxy-ului (prioritate): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (inclusiv variantele cu litere mici)
- Timp de expirare: `TLS_CLIENT_TIMEOUT_MS` (moștenit din `FETCH_TIMEOUT_MS`, implicit 600000)
- Răspunsul `wreq-js` este compatibil cu fetch (`headers`, `text()`, `json()`, `clone()`, `body`).
- Mecanismul de supraveghere pentru primul octet (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()`
  se finalizează imediat ce sosesc antetele din amonte, astfel încât `TLS_CLIENT_TIMEOUT_MS` singur nu poate limita durata
  unui corp care nu furnizează niciodată primul octet. `guardTlsFirstByte()` pune în competiție primul `read()` al corpului
  cu `TLS_FIRST_BYTE_WATCHDOG_MS` (implicit `10000`, valoarea `0` îl dezactivează); un corp valid nu este
  afectat, în timp ce un corp blocat determină anularea cititorului wreq și permite logicii existente de
  revenire TLS din `proxyFetch` să continue cu dispecerul direct/proxy (o cerere care nu poate fi reluată în siguranță, de ex.
  un POST cu corp, va genera în continuare o eroare în loc să fie reîncercată în mod transparent).

### Transport pentru furnizori bazat pe cookie-uri web — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts` este adaptorul comun pentru cele cinci mecanisme de transport
specializate, bazate pe cookie-uri web, prezentate mai jos. Fiecare wrapper minimal specific furnizorului selectează un profil de browser/sistem de operare. Adaptorul
utilizează unicul încărcător al mediului de execuție wreq și grupul de transporturi din `open-sse/utils/tlsClient.ts`, indexate după
profil + sistem de operare + proxy rezolvat, în timp ce fiecare cerere utilizează `cookieMode: "ephemeral"`. Prin urmare, conturile și
cererile partajează conexiunile la nivel de transport, dar niciodată o sesiune wreq sau un depozit de cookie-uri.

| Furnizor   | Profil        | Sistem de operare emulat | Politica EOF a fluxului                |
| ---------- | ------------- | ------------------------ | -------------------------------------- |
| Claude     | `chrome_146`  | Linux                    | include `[DONE]`                       |
| Perplexity | `firefox_148` | macOS                    | include `event: end_of_stream`         |
| Grok       | `chrome_146`  | Linux                    | exclude `[DONE]`                       |
| Notion     | `chrome_146`  | Windows                  | include `[DONE]`                       |
| LMArena    | `chrome_146`  | Windows                  | fără santinelă; închidere la EOF nativ |

- Transmiterea în flux consumă direct obiectul `ReadableStream` al răspunsului nativ; nu este creat
  niciun fișier temporar sau proces auxiliar.
- Sunt inspectați până la 256 de octeți inițiali înainte de expunerea unui flux. Furnizorii SSE memorează temporar erorile
  non-SSE; Grok/LMArena mapează provocările Cloudflare la `403`, iar paginile HTML intermediare la `502`.
- Timpul de expirare al cererii native rămâne încadrat de un termen-limită absolut strict în JS. O blocare invalidează
  și închide doar transportul afectat pentru combinația profil/sistem de operare/proxy, înainte ca următoarea cerere să îl recreeze.
- Prioritatea rezolvării proxy-ului este `proxyUrl` per apel → contextul contului/panoului de control aferent cererii →
  `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (inclusiv variantele cu litere mici). Erorile de rezolvare eșuează
  în mod închis, în loc să permită scurgerea unei conexiuni directe. LMArena efectuează în mod deliberat rezolvarea pentru `arena.ai`.
- `byteResponse` returnează un URL `data:` cu tipul de conținut specificat, fără coruperea UTF-8.
- Erorile sunt `TlsClientUnavailableError` (pachet/extensie indisponibilă), `TlsClientHangError`
  (termen-limită depășit) și `WreqTransportCapacityError` (codul comun de eroare privind capacitatea sesiunilor)
  atunci când toate cele 128 de sloturi limitate pentru profil/sistem de operare/proxy sunt active sau în curs de închidere.

Sesiunea generică `TlsClient` de mai sus rămâne specializată pentru starea persistentă a cookie-urilor
gestionată de browser. Ambele căi reutilizează un singur încărcător cache-uit al modulului wreq și un hook al ciclului de viață al procesului; grupurile lor rămân
separate deoarece duratele de viață ale cookie-urilor sunt intenționat diferite.

Profilurile sunt acceptate de pachetul fixat, însă acceptarea de către WAF-urile reale se poate modifica independent
de testele contractuale locale. Validați modificările amprentelor folosind un cont real autorizat în mod explicit,
înainte de a declara paritatea cu un browser din amonte.

---

## Pachetul Stealth Claude Code

Când `cliCompatMode` este activat, OmniRoute remodelează cererile Claude trimise astfel încât acestea să nu poată fi deosebite de traficul `claude-cli`. Trei module colaborează:

### `claudeCodeFingerprint.ts`

Calculează amprenta `cc_version` de 3 caractere încorporată în antetul de facturare:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (codificat direct; corespunde clientului oficial)
- Intrări: caracterele de la indicii 4, 7 și 20 din textul primului mesaj al utilizatorului + șirul versiunii
- Ieșire: prefix hexazecimal de 3 caractere

### `claudeCodeCCH.ts` (Hashul conținutului clientului)

Verificare a integrității pe server, calculată de CLI-ul oficial Claude Code prin Bun/Zig. OmniRoute o reimplementează cu `xxhash-wasm`:

1. Serializează corpul cu substituentul `cch=00000;`
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. Valoare hexazecimală cu litere mici, de 5 caractere, completată cu zerouri
4. Înlocuiește `cch=00000;` cu tokenul calculat

Constante:

- Seed: `0x6e52736ac806831e`
- Șablon: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Inserează un caracter Unicode **zero-width joiner** (`U+200D`) după primul caracter al numelor de clienți „sensibile”, astfel încât filtrele din amonte să nu le poată găsi prin grep. Lista implicită de cuvinte:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Se aplică pentru: blocurile `system`, întregul conținut `messages[].content` și `tools[].description` / `tools[].function.description`. Poate fi suprascris de operator prin `setSensitiveWords()`.

### `claudeCodeCompatible.ts` — furnizori `anthropic-compatible-cc-*`

Pentru relee Anthropic terțe care acceptă numai trafic „Claude Code autentic”:

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"` în mod implicit
- Comutatorul per conexiune „Activează versiunea beta redact-thinking” adaugă `redact-thinking-2026-02-12` când un serviciu CC Compatible din amonte necesită în mod explicit fluxuri de raționament redactate
- Comutatorul per conexiune „Activează afișarea raționamentului rezumat” stochează `providerSpecificData.requestDefaults.summarizeThinking` și adaugă `display: "summarized"` la cererile de raționament CC Compatible care nu au setat deja un mod de afișare
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (familia Opus/Sonnet 4.x)
- Cale implicită: `/v1/messages?beta=true`

Module asociate din același pachet:

- `claudeCodeConstraints.ts` — reguli pentru temperatură + controlul memoriei cache
- `claudeCodeToolRemapper.ts` — remaparea numelor instrumentelor
- `claudeCodeExtraRemap.ts` — normalizarea suplimentară a sarcinii utile

---

## Stealth Antigravity

Cererile Antigravity păstrează textul apelantului octet cu octet. OmniRoute nu inserează caractere cu lățime zero în prompturi și nu redenumește/inserează instrumente pentru a imita un client IDE.

### `antigravityHeaderScrub.ts`

Elimină marcajele SDK-ului Stainless (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`) înainte de redirecționare.

### ⚠️ Risc: `ANTIGRAVITY_CREDITS=always` (punct critic pentru suspendarea contului)

`ANTIGRAVITY_CREDITS=always` (utilizat de `open-sse/executors/antigravity.ts`) direcționează **fiecare** cerere prin depășirile de credite Antigravity AI (credite Google plătite), în loc să permită cotei gratuite Google să limiteze activitatea. Această funcționalitate este documentată, dar reprezintă **cea mai frecventă raportare de încălcare a ToS pe care o întâlnim** — mai multe conturi Google Ultra au fost suspendate cu `403 / "service disabled for ToS violation" / insufficient_quota` după ce au rulat câteva ore cu `=always`.

Aplicarea restricțiilor în amonte are loc **de partea Google** și nu este ceva ce OmniRoute poate preveni. Numele variabilei de mediu și documentația existentă lasă impresia că este o opțiune care poate fi activată în siguranță; nu este.

**De ce această configurație atrage detectarea abuzurilor mai agresiv decât utilizarea exclusivă a nivelului gratuit:**

- Cheltuielile automate susținute pe un singur cont Google sunt semnalate diferit față de situația în care nivelul gratuit atinge cota și se oprește.
- Depășirile pe bază de credite nu au o limită superioară de rată, astfel încât un client configurat greșit poate consuma câteva sute de USD în câteva minute și poate părea implicat în revânzarea cheilor API sau în trafic automatizat.
- Mai mulți utilizatori OmniRoute care consumă în paralel credite suplimentare de la aceeași adresă IP externă amplifică semnalul.

**Abordare recomandată:**

1. Păstrați valoarea implicită `ANTIGRAVITY_CREDITS=off`, cu excepția cazului în care operatorul acceptă explicit riscul privind creditele plătite și măsurile aplicate asupra contului. `retry` trimite mai întâi cererea normală și injectează credite cel mult o dată după un răspuns 429 eligibil privind cota; `always` injectează credite în prima cerere.
2. **Distribuiți încărcarea între furnizori prin Auto-Combo** (`model: "auto"` sau combinația `kr/glm/etc`), în loc să saturați un singur cont Antigravity.
3. **Setați limite RPM per conexiune** în pagina de editare a furnizorului Antigravity (Dashboard → Providers → Antigravity → connection → rate limit). 30–60 RPM reprezintă o limită superioară justificabilă pentru utilizarea susținută.
4. **Utilizați o rețea stabilă din amonte, controlată de operator** și evitați partajarea unui singur cont între utilizatori sau sarcini de lucru fără legătură.
5. **În cazul suspendării**: depuneți o contestație prin `support.google.com` → „Restore Workspace/Account access”, incluzând corpul exact al răspunsului `quota_exceeded` / `service disabled` trimis de Google. Restabilirea nu este garantată.

Referința variabilelor de mediu documentează implicațiile fiecărui mod de credite asupra contului și cheltuielilor.

Puncte relevante:

- `open-sse/executors/antigravity.ts` — citește `process.env.ANTIGRAVITY_CREDITS`
- `src/lib/oauth/providers/antigravity.ts` — gestionarea acreditărilor
- Raportul inițial al incidentului: Discuția [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## Registrul de amprente CLI — `open-sse/config/cliFingerprints.ts`

Tabel per furnizor care fixează ordinea **exactă** a antetelor și ordinea câmpurilor din corpul JSON, capturate din urmele mitmproxy ale CLI-urilor oficiale. În prezent sunt înregistrate: `codex`, `claude`, plus profilurile derivate în timpul rulării din `providerHeaderProfiles.ts` pentru `antigravity` și `github`.

```ts
interface CliFingerprint {
  headerOrder: string[]; // sensibilă la litere mari și mici
  bodyFieldOrder: string[]; // chei JSON de nivel superior
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Activați sau dezactivați pentru fiecare furnizor prin intermediul variabilelor de mediu (consultați mai jos). Când funcționalitatea este dezactivată, antetele/cheile corpului apar în ordinea furnizată de Node/JSON — ușor de identificat prin amprentare.

---

## Proxy MITM (Antigravity, Linux/macOS/Windows)

Pentru CLI-urile ale căror binare nu pot fi redirecționate prin `OPENAI_BASE_URL`, OmniRoute rulează un proxy local care termină conexiunile TLS. Endpointurile se află în `src/app/api/cli-tools/antigravity-mitm/`.

| Metodă | Endpoint                                | Scop                                               |
| ------ | --------------------------------------- | -------------------------------------------------- |
| GET    | `/api/cli-tools/antigravity-mitm`       | Stare — rulează, pid, dnsConfigured, certExists    |
| POST   | `/api/cli-tools/antigravity-mitm`       | Pornește MITM (necesită `apiKey` + `sudoPassword`) |
| DELETE | `/api/cli-tools/antigravity-mitm`       | Oprește MITM                                       |
| GET    | `/api/cli-tools/antigravity-mitm/alias` | Listează aliasurile modelelor                      |
| PUT    | `/api/cli-tools/antigravity-mitm/alias` | Salvează aliasurile modelelor pentru un instrument |

Gazda țintă interceptată: **`daily-cloudcode-pa.googleapis.com`** (serverul upstream al Antigravity).

### Secvența de pornire (`src/mitm/manager.ts::startMitm`)

1. Generează un certificat autosemnat prin `selfsigned` (RSA-2048, SHA-256, 1 an) — `cert/generate.ts`
2. Instalează certificatul în depozitul de încredere al sistemului — `cert/install.ts`
3. Adaugă în fișierul hosts intrarea `127.0.0.1 daily-cloudcode-pa.googleapis.com` — `dns/dnsConfig.ts`
4. Pornește `src/mitm/server.cjs` cu `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (implicit `443`)
5. Salvează persistent PID-ul în `<DATA_DIR>/mitm/.mitm.pid`

### Detectarea dinamică a depozitului de încredere pe Linux — `cert/install.ts`

`getLinuxCertConfig()` parcurge o listă de priorități și selectează primul director existent:

| Familie de distribuții   | Director                                    | Comandă de actualizare   |
| ------------------------ | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu          | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS   | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                 | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Numele fișierului certificatului: `omniroute-mitm.crt`. Verificarea amprentei se face prin `getCertFingerprint()` (SHA-1 al DER).

În plus, `updateNssDatabases()` instalează certificatul în bazele de date NSS ale fiecărui utilizator atunci când `certutil` este disponibil: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb`, toate profilurile Firefox (inclusiv snap), sub pseudonimul **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** PowerShell cu privilegii ridicate → `certutil -addstore Root`

### Autentificare

Toate endpointurile MITM necesită autentificare de administrare (`requireCliToolsAuth`). Parola sudo este stocată temporar în domeniul modulului (niciodată în `globalThis`) și este ștearsă la apelarea `stopMitm()`.

---

## Suprascrieri User-Agent — variabile de mediu (secțiunea 12 din `.env.example`)

| Variabilă                | Valoare implicită                                               |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.149.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

Utilizate de `open-sse/executors/base.ts::buildHeaders()` prin căutare dinamică. **Actualizați-le atunci când furnizorii lansează versiuni CLI noi** — șirurile UA învechite încep să fie respinse ca aparținând unor clienți depășiți.

## Comutatoare pentru modul de compatibilitate CLI (secțiunea 13 din `.env.example`)

| Variabilă                  | Efect                                |
| -------------------------- | ------------------------------------ |
| `CLI_COMPAT_CODEX=1`       | Amprentă Codex                       |
| `CLI_COMPAT_CLAUDE=1`      | Amprentă claude-cli                  |
| `CLI_COMPAT_GITHUB=1`      | Amprentă GitHub Copilot Chat         |
| `CLI_COMPAT_ANTIGRAVITY=1` | Amprentă Antigravity                 |
| `CLI_COMPAT_KIRO=1`        | Kiro                                 |
| `CLI_COMPAT_CURSOR=1`      | Cursor                               |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                          |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                             |
| `CLI_COMPAT_CLINE=1`       | Cline                                |
| `CLI_COMPAT_ALL=1`         | Activează toate opțiunile de mai sus |

Adresa IP a furnizorului este păstrată **întotdeauna** — comutatorul doar remodelează reprezentarea cererii transmise, fără a schimba adresa IP de ieșire.

---

## Igienizarea antetelor de intrare

OmniRoute elimină antetele clientului primit înainte de redirecționare, astfel încât o cerere provenită de la Cursor să nu divulge `User-Agent: Cursor/X.Y.Z` către un serviciu Claude din amonte. Consultați `src/shared/constants/upstreamHeaders.ts` pentru lista de blocare, menținută sincronizată cu schemele Zod și testele unitare.

---

## Actualizarea amprentelor când un furnizor le rotește

1. Capturați traficul CLI oficial cu `mitmproxy` (interceptare TLS + captură)
2. Extrageți JA3/JA4 și ordinea literală a antetelor
3. Actualizați intrarea relevantă `CLI_FINGERPRINTS[...]`
4. Actualizați valoarea implicită corespunzătoare `*_USER_AGENT` din `.env.example`
5. Dacă s-a modificat chiar handshake-ul TLS, actualizați wrapperul furnizorului relevant sau opțiunea `browser:` din wreq-js
6. Rulați testele TLS specifice furnizorului și un test canar manual cu furnizorul activ
7. Livrați modificările într-o versiune de corecție; documentați-le în `CHANGELOG.md`

---

## Teste

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — comportamentul wrapperului TLS partajat
- `tests/unit/anthropic-cache-fingerprint.test.ts` — caracterul determinist al amprentei
- `tests/unit/chatgpt-web-source-retirement.test.ts` — sursa stealth comună ChatGPT Web rămâne absentă, în timp ce Codex Web rămâne prezent

---

## Vezi și

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — ce se întâmplă când o cale discretă primește un răspuns `403`
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — referința completă pentru variabilele de mediu
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — perspectiva operatorului asupra fluxului de lucru MITM
