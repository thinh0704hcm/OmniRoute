# Cost & Spend Tracking (Română)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/COST_TRACKING.md) · 🇪🇹 [am](../../../am/docs/guides/COST_TRACKING.md) · 🇸🇦 [ar](../../../ar/docs/guides/COST_TRACKING.md) · 🇦🇿 [az](../../../az/docs/guides/COST_TRACKING.md) · 🇧🇬 [bg](../../../bg/docs/guides/COST_TRACKING.md) · 🇧🇩 [bn](../../../bn/docs/guides/COST_TRACKING.md) · 🇧🇦 [bs](../../../bs/docs/guides/COST_TRACKING.md) · 🇨🇿 [cs](../../../cs/docs/guides/COST_TRACKING.md) · 🇩🇰 [da](../../../da/docs/guides/COST_TRACKING.md) · 🇩🇪 [de](../../../de/docs/guides/COST_TRACKING.md) · 🇬🇷 [el](../../../el/docs/guides/COST_TRACKING.md) · 🇪🇸 [es](../../../es/docs/guides/COST_TRACKING.md) · 🇪🇪 [et](../../../et/docs/guides/COST_TRACKING.md) · 🇮🇷 [fa](../../../fa/docs/guides/COST_TRACKING.md) · 🇫🇮 [fi](../../../fi/docs/guides/COST_TRACKING.md) · 🇫🇷 [fr](../../../fr/docs/guides/COST_TRACKING.md) · 🇮🇪 [ga](../../../ga/docs/guides/COST_TRACKING.md) · 🇮🇳 [gu](../../../gu/docs/guides/COST_TRACKING.md) · 🇳🇬 [ha](../../../ha/docs/guides/COST_TRACKING.md) · 🇮🇱 [he](../../../he/docs/guides/COST_TRACKING.md) · 🇮🇳 [hi](../../../hi/docs/guides/COST_TRACKING.md) · 🇭🇷 [hr](../../../hr/docs/guides/COST_TRACKING.md) · 🇭🇺 [hu](../../../hu/docs/guides/COST_TRACKING.md) · 🇦🇲 [hy](../../../hy/docs/guides/COST_TRACKING.md) · 🇮🇩 [id](../../../id/docs/guides/COST_TRACKING.md) · 🇳🇬 [ig](../../../ig/docs/guides/COST_TRACKING.md) · 🇮🇹 [it](../../../it/docs/guides/COST_TRACKING.md) · 🇯🇵 [ja](../../../ja/docs/guides/COST_TRACKING.md) · 🇬🇪 [ka](../../../ka/docs/guides/COST_TRACKING.md) · 🇰🇭 [km](../../../km/docs/guides/COST_TRACKING.md) · 🇮🇳 [kn](../../../kn/docs/guides/COST_TRACKING.md) · 🇰🇷 [ko](../../../ko/docs/guides/COST_TRACKING.md) · 🇱🇹 [lt](../../../lt/docs/guides/COST_TRACKING.md) · 🇱🇻 [lv](../../../lv/docs/guides/COST_TRACKING.md) · 🇮🇳 [ml](../../../ml/docs/guides/COST_TRACKING.md) · 🇮🇳 [mr](../../../mr/docs/guides/COST_TRACKING.md) · 🇲🇾 [ms](../../../ms/docs/guides/COST_TRACKING.md) · 🇲🇹 [mt](../../../mt/docs/guides/COST_TRACKING.md) · 🇲🇲 [my](../../../my/docs/guides/COST_TRACKING.md) · 🇳🇵 [ne](../../../ne/docs/guides/COST_TRACKING.md) · 🇳🇱 [nl](../../../nl/docs/guides/COST_TRACKING.md) · 🇳🇴 [no](../../../no/docs/guides/COST_TRACKING.md) · 🇮🇳 [or](../../../or/docs/guides/COST_TRACKING.md) · 🇮🇳 [pa](../../../pa/docs/guides/COST_TRACKING.md) · 🇵🇭 [phi](../../../phi/docs/guides/COST_TRACKING.md) · 🇵🇱 [pl](../../../pl/docs/guides/COST_TRACKING.md) · 🇵🇹 [pt](../../../pt/docs/guides/COST_TRACKING.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/COST_TRACKING.md) · 🇷🇺 [ru](../../../ru/docs/guides/COST_TRACKING.md) · 🇱🇰 [si](../../../si/docs/guides/COST_TRACKING.md) · 🇸🇰 [sk](../../../sk/docs/guides/COST_TRACKING.md) · 🇸🇮 [sl](../../../sl/docs/guides/COST_TRACKING.md) · 🇷🇸 [sr](../../../sr/docs/guides/COST_TRACKING.md) · 🇸🇪 [sv](../../../sv/docs/guides/COST_TRACKING.md) · 🇰🇪 [sw](../../../sw/docs/guides/COST_TRACKING.md) · 🇮🇳 [ta](../../../ta/docs/guides/COST_TRACKING.md) · 🇮🇳 [te](../../../te/docs/guides/COST_TRACKING.md) · 🇹🇭 [th](../../../th/docs/guides/COST_TRACKING.md) · 🇹🇷 [tr](../../../tr/docs/guides/COST_TRACKING.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/COST_TRACKING.md) · 🇵🇰 [ur](../../../ur/docs/guides/COST_TRACKING.md) · 🇺🇿 [uz](../../../uz/docs/guides/COST_TRACKING.md) · 🇻🇳 [vi](../../../vi/docs/guides/COST_TRACKING.md) · 🇳🇬 [yo](../../../yo/docs/guides/COST_TRACKING.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/COST_TRACKING.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/COST_TRACKING.md)

---

Cum estimează, înregistrează și raportează OmniRoute costul fiecărei solicitări — și de ce
valoarea din panoul de control este un **instrument de urmărire a economiilor**, nu o factură.

Consultați și: [Ghidul utilizatorului](./USER_GUIDE.md) · [Galeria de funcționalități](./FEATURES.md)

---

## Ce este (și ce nu este)

OmniRoute atribuie fiecărei completări un cost în USD per solicitare, înmulțind numărul de
tokenuri cu tarifele modelului. Aceste valori alimentează panoul de control **Costuri**,
CLI-ul `omniroute cost` / `omniroute usage`, exporturile CSV/JSON și bugetele per cheie API.

> **„Costul” din panoul de control este un instrument de urmărire a economiilor, nu o factură.** OmniRoute nu vă taxează
> niciodată — vă direcționează solicitările către furnizorii pe care i-ați conectat deja
> (propriile abonamente, niveluri gratuite și chei API). Un „cost total de 290 USD” acumulat exclusiv prin modele
> gratuite înseamnă că, aproximativ, **nu ați plătit 290 USD** unui API cu plată. Valoarea este o _estimare_
> a costului pe care l-ar fi avut același trafic la prețurile standard de listă, astfel încât să puteți vedea unde
> este concentrată utilizarea și cât economisiți prin direcționarea către furnizori mai ieftini/gratuiți.

Această interpretare este menționată direct în fișierul [README](../../README.md) al proiectului
(„«costul» din panoul de control este un instrument de urmărire a economiilor, nu o factură”).

Deoarece valoarea este o estimare:

- Aceasta depinde de tabelul de prețuri pe care OmniRoute îl are pentru fiecare model. Un model fără o intrare
  de preț contribuie cu un cost de `0` (apare ca un rând „Moștenit / Gratuit” în explorator).
- Traficul din nivelurile gratuite și abonamente acumulează în continuare un cost _estimat_ — acesta este
  cuantumul pe care îl economisiți, nu o sumă datorată.

---

## Cum sunt estimate costurile

### Sursa prețurilor

Costurile provin dintr-un tabel de prețuri determinat în următoarea ordine de prioritate
([`src/lib/pricingSync.ts`](../../src/lib/pricingSync.ts)):

1. **Suprascrieri ale utilizatorului** — prețurile pe care le setați în panoul de control / prin `PATCH /api/pricing`.
2. **Prețuri externe sincronizate** — preluate din fișierul public
   `model_prices_and_context_window.json` al LiteLLM atunci când sincronizarea este activată (stocate într-un spațiu de nume
   separat, `pricing_synced`, astfel încât să nu suprascrie niciodată setările dumneavoastră).
3. **Valori implicite predefinite** — incluse în OmniRoute.

Sincronizarea prețurilor externe este **opțională** și dezactivată în mod implicit. Variabile de mediu relevante
(consultați [`.env.example`](../../.env.example)):

| Variabilă de mediu      | Valoare implicită | Scop                                                                             |
| ----------------------- | ----------------- | -------------------------------------------------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`           | Activează sincronizarea în fundal a prețurilor LiteLLM la pornire.               |
| `PRICING_SYNC_INTERVAL` | `86400`           | Intervalul de sincronizare în **secunde** (implicit, zilnic).                    |
| `PRICING_SYNC_SOURCES`  | `litellm`         | Lista de surse separată prin virgule (în prezent, doar `litellm` este acceptat). |

### Formula de calcul al costului

Costul este calculat pentru fiecare solicitare pe baza numărului de tokenuri și a tarifelor per milion de tokenuri din
[`src/lib/usage/costCalculator.ts`](../../src/lib/usage/costCalculator.ts)
(`computeCostFromPricing` / `calculateCost`):

- **Tokenuri de intrare** (minus citirile din cache și tokenurile de creare a cache-ului) × tariful `input`.
- **Tokenuri citite din cache** × tariful `cached` (se utilizează tariful de intrare dacă acesta lipsește).
- **Tokenuri de creare a cache-ului** × tariful `cache_creation` (se utilizează tariful de intrare dacă acesta lipsește).
- **Tokenuri de ieșire** × tariful `output`.
- **Tokenuri de raționament** × tariful `reasoning` (se utilizează tariful de ieșire dacă acesta lipsește).

Toate tarifele sunt interpretate ca USD per 1.000.000 de tokenuri. Un nivel de serviciu Codex „fast”/„priority” sau
„flex” aplică un multiplicator de cost (`getCodexFastCostMultiplier`) — de exemplu, flex
este facturat cu o reducere de 50% pentru tokenuri, afișată drept **economii flex** în panoul de control.

Numele modelelor sunt normalizate mai întâi (prefixele căilor furnizorilor, precum `openai/` sau
`accounts/fireworks/models/`, sunt eliminate), astfel încât rândurile istorice să corespundă în continuare unui preț.

### Cum sunt înregistrate cheltuielile

- Costul per solicitare este calculat după răspuns și înregistrat asincron, fără a se aștepta finalizarea operației, astfel încât
  să nu adauge niciodată latență pentru client. Consumul cotei partajate este programat în următoarea iterație a
  buclei de evenimente prin [`src/lib/quota/spendRecorder.ts`](../../src/lib/quota/spendRecorder.ts).
- Cheltuielile aferente cheilor API sunt stocate temporar și scrise în loturi de
  [`SpendBatchWriter`](../../src/lib/spend/batchWriter.ts) (interval implicit de scriere de 60 s,
  buffer de 1.000 de intrări). Configurabil prin:

  | Variabilă de mediu                  | Valoare implicită | Scop                                                 |
  | ----------------------------------- | ----------------- | ---------------------------------------------------- |
  | `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` | `60000`           | Intervalul de scriere în milisecunde.                |
  | `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`   | `1000`            | Numărul maxim de intrări stocate înainte de scriere. |

Valorile costurilor din panoul de control **nu** sunt citite dintr-o sumă în dolari stocată pentru fiecare rând — acestea
sunt recalculate dinamic pe baza numărului de tokenuri și a tabelului curent de prețuri de fiecare dată când
este executat endpointul de analiză. Aceasta înseamnă că remedierea unui preț incorect (și resincronizarea)
actualizează retroactiv estimările costurilor istorice.

---

## Tablou de bord: pagina Costuri

Pagina **Costuri** se află la `/dashboard/costs`
(`src/app/(dashboard)/dashboard/costs/`).
Vizualizarea sa principală este fila **Prezentare generală a costurilor**
(`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`),
care încarcă toate datele de la `GET /api/usage/analytics`.

Ce afișează:

- **Casete de cheltuieli** — cheltuielile estimate pentru _Astăzi (1d)_, _7d_, _30d_ și
  intervalul selectat. Selector de interval: `7d`, `30d`, `90d`, `all`.
- **Indicatori principali** — solicitările din interval, furnizorii activi, modelele active,
  costul mediu per solicitare.
- **Explorator de costuri** — un tabel care poate fi sortat și filtrat, grupat după
  **furnizor**, **model**, **cheie API**, **cont** sau **nivel de serviciu**, cu costul,
  solicitările, tokenurile, costul mediu per solicitare și ponderea procentuală din total.
- **Utilizarea tokenurilor** — totalul tokenurilor / tokenurile de intrare / tokenurile de
  ieșire și raportul intrare:ieșire.
- **Eficiența rutării** — numărul de reveniri la alternative, rata de revenire la alternative
  și acoperirea modelelor solicitate.
- **Prognoză lunară** — estimează cheltuielile de la sfârșitul lunii pe baza mediei zilnice
  recente.
- **Comparație între perioade** — modificarea procentuală dintre prima și a doua jumătate a
  intervalului.
- **Grafice** — tendința zilnică a costurilor, ponderea furnizorilor (diagramă circulară),
  principalii furnizori, principalele modele, costul per cheie API, costul per cont, tiparul
  săptămânal de utilizare și o hartă termică a activității.
- **Export** — descărcarea intervalului curent în format **CSV** sau **JSON** (butoanele apar
  după ce există date cu un cost diferit de zero).

Atunci când nu există trafic tarifat, rândurile afișează eticheta „Moștenit / Gratuit” în
loc de `$0`, reflectând modelul instrumentului de monitorizare a economiilor.

### Subpagini asociate secțiunii Costuri

Zona Costuri găzduiește, de asemenea (toate sub `/dashboard/costs/`):

- **Tarifare** (`/dashboard/costs/pricing`) — vizualizarea și înlocuirea prețurilor per model
  (afișează fila comună Tarifare).
- **Buget** (`/dashboard/costs/budget`) — stabilirea limitelor de cheltuieli per domeniu
  (afișează fila comună Buget).
- **Partajarea cotei** (`/dashboard/costs/quota-share`) — grupuri cu cote partajate și
  vizualizări ale ratei de consum.

---

## Endpointuri API

Toate acestea necesită autentificare de administrare (loopback/JWT, prin
`requireManagementAuth`), dacă nu se specifică altfel.

### Analize privind utilizarea și costurile

| Metodă | Endpoint                 | Scop                                                                                                                                                                                     |
| ------ | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/analytics`   | Analize complete privind costurile/utilizarea: rezumat, tendință zilnică, după furnizor/model/cheie API/cont/nivel. Interogare: `range`, `startDate`, `endDate`, `apiKeyIds`, `presets`. |
| `GET`  | `/api/usage/utilization` | Utilizarea cotei per furnizor în timp. Interogare: `range` (`1h`/`24h`/`7d`/`30d`), `provider`.                                                                                          |
| `GET`  | `/api/usage/history`     | Rânduri cu istoricul brut al utilizării.                                                                                                                                                 |
| `GET`  | `/api/usage/call-logs`   | Jurnale de apel per solicitare (model, tokenuri, cost, latență, stare).                                                                                                                  |
| `GET`  | `/api/usage/quota`       | Starea cotei furnizorului.                                                                                                                                                               |
| `GET`  | `/api/usage/proxy-logs`  | Jurnale ale solicitărilor proxy.                                                                                                                                                         |

### Bugete

| Metodă | Endpoint                 | Scop                                                                                                                    |
| ------ | ------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/budget`      | Rezumatul costurilor + verificarea bugetului pentru o cheie API (parametrul de interogare `apiKeyId` este obligatoriu). |
| `POST` | `/api/usage/budget`      | Stabilirea limitelor zilnice/săptămânale/lunare în USD + pragul de avertizare pentru o cheie API.                       |
| `GET`  | `/api/usage/budget/bulk` | Rezumate de buget în bloc pentru mai multe chei API.                                                                    |

> API-ul pentru bugete este delimitat per **cheie API** (`apiKeyId`). Limitele returnate de
> `GET /api/usage/budget` includ `dailyLimitUsd`, `weeklyLimitUsd`, `monthlyLimitUsd`,
> un `warningThreshold` și totalurile curente (`totalCostToday`, `totalCostMonth`, …).

### Tarifare

| Metodă   | Endpoint                | Scop                                                                                                                                   |
| -------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`    | `/api/pricing`          | Tarifarea combinată curentă (utilizator + sincronizată + valori implicite). `?includeSources=1` pentru a vedea sursa fiecărei intrări. |
| `PATCH`  | `/api/pricing`          | Înlocuirea tarifării pentru `{ provider: { model: { input, output, cached, … } } }`.                                                   |
| `DELETE` | `/api/pricing`          | Resetarea tarifării la valorile implicite (opțional, delimitată prin `?provider=&model=`).                                             |
| `GET`    | `/api/pricing/defaults` | Afișarea tarifelor de rezervă implicite per 1M.                                                                                        |
| `GET`    | `/api/pricing/models`   | Tarifare indexată după model.                                                                                                          |
| `POST`   | `/api/pricing/sync`     | Declanșarea unei sincronizări manuale din surse externe (LiteLLM).                                                                     |
| `GET`    | `/api/pricing/sync`     | Starea curentă a sincronizării.                                                                                                        |
| `DELETE` | `/api/pricing/sync`     | Ștergerea tuturor datelor de tarifare sincronizate.                                                                                    |

### Alte endpointuri relevante pentru costuri

| Metodă | Endpoint                      | Scop                                                                                                        |
| ------ | ----------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/free-tier/summary`      | Totalul tokenurilor pentru modelele gratuite, numărul utilizat în această lună și alocarea gratuită rămasă. |
| `GET`  | `/api/quota/pools/[id]/usage` | Utilizarea pentru un grup cu cotă partajată.                                                                |

---

## CLI

CLI-ul OmniRoute expune comenzi pentru costuri, utilizare și prețuri (înregistrate în
[`bin/cli/commands/registry.mjs`](../../bin/cli/commands/registry.mjs)).

### `omniroute cost`

Un raport de costuri agregat din `/api/usage/analytics`.

```bash
omniroute cost                          # ultimele 30 de zile, grupate după furnizor
omniroute cost --period 7d              # ultimele 7 zile
omniroute cost --group-by model         # grupare după provider | model | combo | api-key | day
omniroute cost --since 2026-06-01 --until 2026-06-13
omniroute cost --api-key <key> --limit 50
```

Coloane: grup, solicitări, tokenuri de intrare/ieșire, cost (USD) și % din total. La final
este afișată o linie cu totalul general (suprimată cu `--quiet` sau `--output json`).

### `omniroute usage`

```bash
omniroute usage analytics --period 30d [--provider <id>]   # rezumatul costurilor pentru fiecare furnizor
omniroute usage logs [--limit 100] [--follow] [--api-key <k>] [--search <q>]
omniroute usage quota [--provider <id>] [--check]
omniroute usage utilization [--api-key <k>]
omniroute usage history [--limit 100]
omniroute usage proxy-logs [--limit 100]

# Bugete
omniroute usage budget list
omniroute usage budget get [scope]
omniroute usage budget set <amount> [--scope global] [--period monthly]
omniroute usage budget reset [scope]
```

### `omniroute pricing`

```bash
omniroute pricing list [--provider <p>] [--model <m>] [--limit 200]
omniroute pricing get <model>
omniroute pricing sync [--provider <p>] [--force]   # POST /api/pricing/sync
omniroute pricing diff [--model <m>]
omniroute pricing defaults show
omniroute pricing defaults set [--input <p>] [--output <p>] [--cache-read <p>] [--cache-write <p>]
```

> `pricing defaults show` citește `GET /api/pricing/defaults`. Pentru a edita în schimb
> prețurile modelelor individuale, utilizați pagina **Pricing** din panoul de control sau `PATCH /api/pricing`.

---

## Depanare

- **Toate costurile sunt afișate ca $0 / „Legacy / Free”.** Modelele utilizate nu au nicio intrare de preț.
  Activați sincronizarea externă (`PRICING_SYNC_ENABLED=true`) și rulați `omniroute pricing sync` sau
  setați manual prețurile prin pagina Pricing / `PATCH /api/pricing`.
- **Un model istoric are un preț incorect.** Corectați prețul (prin suprascriere sau resincronizare) — costul este
  recalculat pe baza numărului de tokenuri la fiecare citire a datelor analitice, astfel încât estimările sunt actualizate retroactiv.
- **Cheltuielile sunt afișate cu întârziere față de timpul real.** Cheltuielile per cheie sunt procesate în loturi; reduceți
  `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` dacă aveți nevoie de valori mai recente.

---

Pentru a vedea cum se integrează aceasta în panoul de control general, consultați [Ghidul utilizatorului](./USER_GUIDE.md) și
[Galeria de funcționalități](./FEATURES.md).
