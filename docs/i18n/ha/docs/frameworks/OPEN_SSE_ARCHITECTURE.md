# open-sse Architecture (Hausa)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **A taƙaice**: `open-sse/` shi ne babban injin watsa bayanai da ke sarrafa kowace buƙatar LLM a OmniRoute. Yana ɗauke da fayiloli kusan 1,400 waɗanda ke aiwatar da bututun buƙata, masu aiwatarwa, ayyuka, sabar MCP, da matakin fassara. Wannan jagorar tana bayyana yadda sassan suke aiki tare.

**Tushe:** `open-sse/` (kunshin workspace, kusan fayilolin `.ts` 1,440 waɗanda ba na gwaji ba)

---

## Me Ya Sa Ake da Kunshin Workspace Na Daban?

`open-sse/` **workspace mai zaman kansa** ne a cikin monorepo na OmniRoute saboda dalilai da dama:

1. **Sake amfani** — ana wallafa `open-sse` a matsayin `@omniroute/open-sse` a npm, don haka sauran ayyuka za su iya amfani da shi da kansu
2. **Iyakoki masu tsabta** — an ware injin watsa bayanai daga matakin UI/DB na musamman ga OmniRoute
3. **Aiki mai sauri** — injin ba shi da dogaro da Next.js, wanda ke ba da damar farawa cikin sauri a mahallin CLI/serverless
4. **Sarrafa sigogi** — `open-sse` zai iya fitar da sabbin sigogi bisa jadawalinsa

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Tsarin Babban Mataki

```
open-sse/
├── index.ts              # Wurin shiga na jama'a
├── types.d.ts            # Fitar da nau'ikan jama'a
├── package.json          # @omniroute/open-sse
├── config/               # Saitunan masu samarwa, ƙayyadaddun ƙimomi, rajistoci
├── executors/            # Masu aiwatar da HTTP na kowane mai samarwa (67 + base.ts/index.ts)
├── handlers/             # Masu kula da buƙatu (chatCore, responses, da sauransu)
├── lib/                  # Kayan aikin ciki
├── mcp-server/           # Sabar Model Context Protocol
├── services/             # Kusan kayayyakin aiki 298
├── transformer/          # Mai sauya tsarin Responses API
├── translator/           # Fassarar tsari (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Kayan aikin da ake rabawa (logging, error, stream, da sauransu)
```

### Adadin Modules

| Kundin adireshi | Fayiloli | Manufa |
| `executors/` | 167 | Masu aiwatar da HTTP na kowane mai samarwa (an haɗa su ta hanyar masana'antar DefaultExecutor) |
| `handlers/` | 157 | Wuraren shigar buƙata (chatCore, responses, embeddings) |
| `services/` | ~536 | Gudanar da hanya, caching, iyakance adadin buƙatu, sabuntawa, da sauransu |
| `translator/` | 56 | Sauya tsari (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Kayan aiki da hanyoyin sufuri na MCP |
| `utils/` | ~108 | Kayan aikin da suka shafi ɓangarori da dama (logging, error, stream) |
| `config/` | ~339 | Saitunan masu samarwa, ƙayyadaddun ƙimomi, rajistoci |

---

## Bututun Buƙata

Kowace buƙatar LLM tana bi ta cikin **bututu mai matakai 5**:

```
                              ┌──────────────┐
   Buƙatar HTTP                │  1. HANYA    │   warware combo, zaɓen model
   (hanyar Next.js)            └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. FASSARA  │   sauya tsari (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. AIWATAR  │   mai aiwatar da mai samarwa, HTTP, sake gwadawa, breaker
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. WATSA    │   sauya SSE, backpressure
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. RIKODA   │   bin sawun amfani, rajistar kira, rarraba kurakurai
                              └──────┬───────┘
                                     │
                                     ▼
                              Amsar HTTP (SSE ko JSON)
```

### Mataki na 1: Hanya (services/combo.ts)

**Wurin shiga**: `handleComboChat()` a cikin `services/combo.ts`

Yana warware buƙatar zuwa takamaiman tuple na `(provider, model, account, credentials)`:

- Nemo combo ta ID (ko gina combo na kama-da-wane don models na `auto/*`)
- Aiwatar da dabarar zaɓen hanya (fifiko, weighted, round-robin, da sauransu)
- Tace masu samarwa marasa lafiya (circuit breaker)
- Zaɓi manufa mai aiki ta gaba

Ga models na `auto/*`, wannan matakin kuma yana:

- Gudanar da tsarin **ƙididdiga mai dalilai 16** (`services/autoCombo/`)
- Zaɓar tagwayen `provider+model` bisa lafiya, farashi, latency, da sauransu

### Mataki na 2: Fassara (translator/)

Idan tsarin tushe (misali, OpenAI) ya bambanta da tsarin manufa (misali, Claude), ana **fassara** buƙatar:

- System prompt → system message
- Ma'anar kayan aiki → tsarin kayan aiki na musamman ga mai samarwa
- Sigogin reasoning/thinking → makamantansu na musamman ga mai samarwa
- Daidaita matsayin saƙo (`developer` → `system` ga waɗanda ba OpenAI ba)

`translator/index.ts` yana samar da:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Mataki na 3: Aiwatar (executors/)

**Wurin shiga**: `getExecutor(providerId).execute(request, options)`

Dukkan masu samarwa suna amfani da `DefaultExecutor` (`executors/default.ts`) ta hanyar madadin masana'antar `getExecutor()`. Mai aiwatarwar yana:

- Gina URL na upstream (`buildUrl()`)
- Ƙara headers na musamman ga mai samarwa (`buildHeaders()`)
- Sauya jikin buƙatar (`transformRequest()`)
- Aika buƙatar HTTP tare da sake gwadawa + exponential backoff
- Gudanar da sabunta auth idan ana buƙata (masu samarwa na OAuth)

Dukkan masu aiwatarwa suna faɗaɗa `BaseExecutor` (`executors/base.ts`, layukan lamba 1170), wanda ke samar da:

- Dabarar sake gwadawa ta gama-gari
- Haɗin proxy
- Haɗin circuit breaker
- Hooks na rikodin amfani

### Mataki na 4: Watsawa (utils/stream.ts)

Don amsoshin da ake watsawa, mai aiwatarwar yana dawo da **ReadableStream**. Mai sarrafa buƙatar yana:

- Tura bayanai ta hanyar sauyin SSE (`createSSETransformStreamWithLogger`)
- Aiwatar da saƙonnin heartbeat don gano haɗin da ya mutu
- Gudanar da yankewar haɗin abokin ciniki cikin tsari (`pipeWithDisconnect`)
- Sauya SSE → JSON ga abokan ciniki marasa amfani da watsawa

Don amsoshin da ba a watsa su ba, mai aiwatarwar yana dawo da parsed JSON object wanda ake miƙawa ba tare da canji ba.

### Mataki na 5: Rikodi (services/usage.ts)

Bayan amsa (nasara ko gazawa), ana adana bayanan amfani:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` daga amsar
- `cost_usd` da aka ƙididdige daga bayanan farashi
- `latency_ms`, `status`, `error_class` idan an gaza
- Ana adanawa a teburin `usage_history`

Ana rubuta fayilolin rajistar kira (idan an kunna su) zuwa `${DATA_DIR}/call_logs/`.

---

## Zurfafa Cikin Muhimman Fayiloli

### chatCore.ts (layuka 5977)

**Babban mai sarrafa buƙata**. Duk da girmansa, yana da tsari mai sauƙin fahimta:

```ts
// Kwaikwayon tsarin chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Tabbatar da izini + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Tabbatar da ingancin jikin buƙata
  const body = await parseRequestBody(request);

  // 3. Gano tsari + fassara
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Zaɓin hanyar combo
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Ci gaba zuwa manufa ta gaba
    }
  }

  // 5. Madadin gaggawa
  return await emergencyFallback(body);
}
```

Duk da kasancewarsa babban function guda ɗaya, an tsara shi zuwa **sassa masu tsokaci** waɗanda suka dace da tsarin matakai 5.

### combo.ts (4456 LOC)

**Injin zaɓin hanya** wanda ke warware combo zuwa jerin manufofi da aka tsara.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Yana tallafa wa **dabarun zaɓin hanya guda 19** (duba `src/shared/constants/routingStrategies.ts`):

| Dabara              | Halayya                                                                                |
| ------------------- | -------------------------------------------------------------------------------------- |
| `priority`          | Jerin da aka tsara bisa fara da manufa ta farko                                        |
| `weighted`          | Zaɓi bisa yiwuwar da nauyin kowace manufa ya ƙayyade                                   |
| `round-robin`       | Zagayawa cikin manufofi bi-da-bi                                                       |
| `context-relay`     | Miƙa mahallin bayanai tsakanin manufofi                                                |
| `fill-first`        | Cika ƙayyadadden kaso kafin matsawa zuwa na gaba                                       |
| `p2c`               | Ƙarfin zaɓuɓɓuka biyu                                                                  |
| `random`            | Zaɓin bazuwar da ya yi daidai                                                          |
| `least-used`        | Zaɓi wanda aka fi ƙarancin amfani da shi kwanan nan                                    |
| `cost-optimized`    | Fara da manufa mafi arha da ke aiki lafiya                                             |
| `reset-aware`       | La'akari da lokutan sake saita mai samarwa                                             |
| `reset-window`      | Zaɓin hanya bisa lokacin sake saiti                                                    |
| `headroom`          | Fara da wanda ya fi samun ragowar sararin ƙayyadadden kaso                             |
| `strict-random`     | Bazuwar gaske (ba tare da auna inganci ba)                                             |
| `auto`              | Yi amfani da ƙididdigar abubuwa 16 (`autoCombo/`)                                      |
| `lkgp`              | Fara da mai samarwa na ƙarshe da aka san yana aiki                                     |
| `context-optimized` | Mafi dacewa da buƙatun mahalli masu tsawo                                              |
| `fusion`            | Aika zuwa rukuni a lokaci guda, sannan a haɗa sakamakon ta hanyar alƙali (`fusion.ts`) |

### base.ts (1170 LOC)

**Mai aiwatarwa na abstract** wanda duk masu aiwatarwa guda 107 ke faɗaɗawa. Ya ƙunshi:

- `buildUrl()` — gina URL na asali (subclasses suna maye gurbinsa don keɓancewa)
- `buildHeaders()` — headers na asali (auth, content-type)
- `transformRequest()` — wucewa kai tsaye a matsayin tsoho
- `execute()` — babban zagayen HTTP tare da sake gwadawa/jinkiri/breaker

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Yana sarrafa duk masu samarwa masu dacewa da OpenAI/Anthropic
  // Masu samarwa suna rajistar saituna (URL, auth, headers) amma suna amfani da dabarar aiwatarwa guda
}
```

Ana saita halayen da suka keɓanta ga mai samarwa (auth headers, base URL, version headers) ta hanyar rajistar masu samarwa, ba ta azuzuwan masu aiwatarwa daban-daban ba.

````

---

## Sabis (modula 117)

Sabis su ne **modula masu mayar da hankali kan manufa guda** waɗanda handlers ke haɗawa. Manyan rukunonin su ne:

### Routing & Combo

- `combo.ts` — mashigar buƙatun da ake routing ta combo
- `services/autoCombo/` — ƙididdigar abubuwa 16, dabarun auto routing guda 8
- `wildcardRouter.ts` — yana daidaita hanyoyin wildcard (`gpt-*`)
- `modelFamilyFallback.ts` — T5 fallback a cikin iyali ɗaya

### Iyakance Sauri & Quota

- `rateLimitManager.ts` — token bucket ga kowane key+provider
- `usage.ts` — rikodin amfani
- `quotaCache.ts` — hotunan quota na cikin ƙwaƙwalwa

### Account & Token

- `tokenRefresh.ts` — sabunta OAuth idan an samu 401
- `accountFallback.ts` — sauyawa zuwa wani account na madadin
- `sessionManager.ts` — yanayin session mai zagaye da yawa

### Basira

- `intentClassifier.ts` — rarraba manufar buƙata
- `taskAwareRouter.ts` — yin routing bisa nau'in aiki
- `thinkingBudget.ts` — ware tokens na tunani
- `contextManager.ts` — saka context na routing

### Juriya

- `resilience.ts` — tsara retry, backoff, da breaker
- `emergencyFallback.ts` — fallback na mafita ta ƙarshe
- `modelDeprecation.ts` — yin auto-route zuwa samfuran da suka gaje su

### Yanayi

- `signatureCache.ts` — cire maimaitawa bisa signature na buƙata
- `volumeDetector.ts` — rage nauyi
- `contextHandoff.ts` — serialization na session

### Matse Bayanai

- `compression/` (ƙaramin kundin adireshi) — cikakken tsarin compression
- Fayiloli 39 da suka ƙunshi engines, rule packs, da adapters

### Ƙwarewa

- (an yi bayani a cikin [SKILLS.md](./SKILLS.md))

### Ƙwaƙwalwa

- (an yi bayani a cikin [MEMORY.md](./MEMORY.md))

---

## Executors (fayiloli 75+)

Fayil guda ɗaya ga kowane provider. Dukkansu suna faɗaɗa `BaseExecutor` kuma suna override abin da ya bambanta.

### Tsarukan da Aka Fi Amfani da Su

Ana tantance providers ta hanyar `getExecutor(providerId)`, wanda ke mayar da executor da aka saita. Providers masu dacewa da OpenAI/Anthropic suna amfani da `DefaultExecutor` (`executors/default.ts`). Ana saita halayen da suka keɓanta ga provider (base URL, auth headers, API version) a cikin `open-sse/config/providers/`, yayin da ake sarrafa sauye-sauyen request body a cikin `open-sse/translator/`.

Ana saita **Custom URL** ta hanyar saitin provider:

```ts
// Saitin provider a cikin open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

Ana sarrafa **Custom auth** ta hanyar saitin auth na registry ɗin provider (API key, OAuth, header profiles).

Ana rajistar sauye-sauyen **Custom request body** (misali, yadda Anthropic ke ware `system` daga `messages`) ga kowane provider a cikin `open-sse/translator/`.

````

### Factory na Executor

`executors/index.ts` yana fitar da `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Tantancewa yana bi ta `ExecutorRegistry` (`executors/registry.ts`): ana bayyana kowane executor na musamman a cikin ginannen jadawalin `executors/index.ts`, sannan a yi masa rajista ta hanyar `registerExecutor(alias, instance)` lokacin load ɗin module; `getExecutor()` yana duba registry kuma yana komawa zuwa `DefaultExecutor` da aka memoize ga duk provider da ba shi da keɓaɓɓen entry. Ana bayyana cikakken taswirar alias → executor ta golden test `tests/unit/executor-map-golden.test.ts`.

---

## Masu Fassara

Fassara tsakanin **tsare-tsare 3**: OpenAI, Anthropic, Gemini, tare da sabon Responses API.

### Lokacin da Fassara ke Faruwa

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Fassarorin da aka fi amfani da su:

- `OpenAI → Anthropic`: keɓantaccen filin `system`, kanun `x-api-key`
- `OpenAI → Gemini`: `contents` maimakon `messages`, `systemInstruction`
- `OpenAI → Responses API`: jerin `input`, yanayin `previous_response_id`

### Abubuwan Musamman da Aka Sarrafa

- Matsayin `developer` → `system` ga waɗanda ba OpenAI ba
- Matsayin `system` → ana haɗa shi cikin saƙon mai amfani na farko don GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` na Gemini
- `tools` → tsarin kayan aiki na musamman ga mai samarwa
- Sigogin tunani (o1, Claude) → makamantansu na musamman ga mai samarwa

---

## Sabar MCP

`open-sse/mcp-server/` yana aiwatar da sabar **Model Context Protocol**:

- **kayan aiki 110** (sarrafa masu samarwa, haɗe-haɗe, ma’adanar tunawa, cache, matsewa, proxy, ƙwarewa, mayar da abubuwa wasa, plugins, Notion, Obsidian, ma’ajiyar bayanai ta cikin gida)
- **hanyoyin sufuri 3**: stdio, SSE, Streamable HTTP
- **scopes 33** don cikakken sarrafa izini

### Rijistar Kayan Aiki

Ana rijistar kayan aiki a matsayin fayiloli masu zaman kansu a cikin `open-sse/mcp-server/tools/`, inda kowannensu ke fitar da suna, schema, handler, da scope:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Samo taƙaitaccen bayanin lafiyar tsarin",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Hanyoyin Sufuri

```ts
// stdio (amfani da CLI)
startMcpStdio(server);

// SSE (yaɗawa bisa HTTP)
startMcpSse(server, port);

// Streamable HTTP (MCP na zamani)
startMcpStreamable(server, port);
```

### Ba da Izini

Kowane kiran kayan aiki yana bi ta binciken scope (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Izinin scope bai isa ba");
}
```

---

## Masu Sauya Tsari

`open-sse/transformer/` yana sauyawa tsakanin tsare-tsaren **Chat Completions** da **Responses API**.

### Me Ya Sa Ake Buƙatar Wani Mai Sauya Tsari Na Daban?

Responses API shi ne sabon tsarin OpenAI mai **tattaunawa da ake riƙe yanayinta** (`previous_response_id`). Lokacin da abokin hulɗa ya aika buƙatar Responses, OmniRoute:

1. Yana sauya Responses → Chat Completions a ciki
2. Yana aikawa ga mai samarwa (duk wani mai samarwa da ke goyon bayan Chat Completions)
3. Yana mayar da amsar zuwa tsarin Responses
4. Yana yaɗa amsar da aka sauya zuwa ga abokin hulɗa

Mai sauya tsarin (`transformer/responsesTransformer.ts`) yana samar da:

```ts
createResponsesApiTransformStream(): TransformStream
```

Wannan yana sarrafa:

- Abubuwan da suka faru na `response.output_item.added`
- Abubuwan da suka faru na `response.output_text.delta`
- Abin da ya faru na `response.completed`
- Daidaita kiran kayan aiki (`function_call` ↔ `tool_calls`)

---

## Saituna

`open-sse/config/` yana ɗauke da matakin saituna:

| Fayil                         | Manufa                                              |
| ----------------------------- | --------------------------------------------------- |
| `providerRegistry.ts`         | Rijistar samfurin hira bisa kundin masu samarwa 352 |
| `providerModels.ts`           | Laƙaban samfura, daidaita tsari                     |
| `constants.ts`                | Iyakokin lokaci, iyakoki, lambobin matsayi          |
| `defaultThinkingSignature.ts` | Sa hannun tunani na Claude na asali                 |
| `modelStrip.ts` (a services)  | Cire filaye na musamman ga kowane mai samarwa       |

### Schema na Rijistar Masu Samarwa

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Tabbatarwar Zod a lokacin loda module tana tabbatar da cewa duk saitunan masu samarwa suna da inganci.

---

## Ƙa'idodin Aiki

Injin bayar da hanya yana da tsauraran iyakokin aiki:

| Aiki                                                    | Manufa | Ma'auni                          |
| ------------------------------------------------------- | ------ | -------------------------------- |
| Warware combo                                           | <10ms  | Ga maƙasudai 50                  |
| Duba iyakar ƙima                                        | <1ms   | Bokitin token na cikin ƙwaƙwalwa |
| Komawa ga dangin model                                  | <5ms   | Ma'anonin dangi da aka cache     |
| Tura buƙata zuwa hanya                                  | <2ms   | Hanya mai yawan amfani           |
| **Babu blocking I/O a hanyar routing mai yawan amfani** | —      | Duk async                        |

---

## Hanyoyin da Ba Su Dace Ba

❌ **Kiran DB na synchronous a cikin `combo.ts`** — yi lissafi tun da wuri kuma a cache
❌ **Dabarar sake gwadawa a cikin handlers** — yi amfani da `retry()` daga resilience service
❌ **Samun damar config na provider kai tsaye** — yi amfani da getters na `providerRegistry`
❌ **Jerin fallback da aka rubuta kai tsaye** — ayyana a cikin `modelFamilyFallback.ts`
❌ **Sauya state a tsakanin buƙatu masu gudana lokaci guda** — yi amfani da context na kowace buƙata kawai

---

## Ƙara Sabon Bangare

### Ƙara Sabon Service

1. Ƙirƙiri `open-sse/services/[serviceName].ts` mai takamaiman nauyi
2. Fitar da babban handler function da duk wasu constants
3. Ƙara unit tests a cikin `tests/unit/services/[serviceName].test.mjs`
4. Haɗa shi cikin request pipeline a `handlers/chatCore.ts` (idan yana da alaƙa da routing)
5. Sabunta routing logic a cikin `combo.ts` idan service ɗin yana shafar zaɓin target
6. Yi bayani a cikin wannan fayil

### Ƙara Sabon Executor

1. Ƙirƙiri `open-sse/executors/[provider].ts` wanda ke faɗaɗa `BaseExecutor`
2. Yi rajista a cikin `config/providerRegistry.ts`
3. Ƙara shi zuwa factory na `executors/index.ts`
4. Ƙara unit tests don executor
5. Yi bayani a cikin `docs/architecture/ARCHITECTURE.md`

### Ƙara Sabon MCP Tool

1. Ƙirƙiri ko sabunta `open-sse/mcp-server/tools/[category]Tools.ts`
2. Ayyana Zod schema don inputs
3. Yi rajistar tool a cikin `mcp-server/index.ts`
4. Ƙara shi zuwa scope matrix a cikin `mcp-server/auth/`
5. Ƙara unit tests

---

## Duba Kuma

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — tsarin gine-gine na matakin sama
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — madogarar injiniyanci
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — bayani kan kowane directory
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — ƙididdiga mai abubuwa 16
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP server
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A server
- Tushe: `open-sse/` (fayiloli 400+, ~143K LOC)
