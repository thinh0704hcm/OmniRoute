# open-sse Architecture (Slovenčina)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **TL;DR**: `open-sse/` je základný streamovací engine, ktorý zabezpečuje každú požiadavku na LLM v OmniRoute. Obsahuje približne 1 400 súborov implementujúcich spracovanie požiadaviek, exekútory, služby, server MCP a prekladovú vrstvu. Táto príručka vysvetľuje, ako jednotlivé časti spolupracujú.

**Zdroj:** `open-sse/` (balík pracovného priestoru, približne 1 440 netestovacích súborov `.ts`)

---

## Prečo samostatný balík pracovného priestoru?

`open-sse/` je **samostatný pracovný priestor** v monorepozitári OmniRoute z niekoľkých dôvodov:

1. **Opätovná použiteľnosť** — `open-sse` je publikovaný na npm ako `@omniroute/open-sse`, takže ho môžu nezávisle používať aj iné projekty
2. **Jasné hranice** — streamovací engine je oddelený od vrstvy používateľského rozhrania/databázy špecifickej pre OmniRoute
3. **Výkon** — engine nemá žiadne závislosti od Next.js, čo umožňuje rýchlejšie studené štarty v prostrediach CLI/serverless
4. **Verzovanie** — `open-sse` môže vydávať nové verzie podľa vlastného harmonogramu

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Štruktúra najvyššej úrovne

```
open-sse/
├── index.ts              # Verejný vstupný bod
├── types.d.ts            # Verejné exporty typov
├── package.json          # @omniroute/open-sse
├── config/               # Konfigurácie poskytovateľov, konštanty, registre
├── executors/            # HTTP exekútory jednotlivých poskytovateľov (67 + base.ts/index.ts)
├── handlers/             # Obslužné rutiny požiadaviek (chatCore, responses atď.)
├── lib/                  # Interné pomocné nástroje
├── mcp-server/           # Server Model Context Protocol
├── services/             # Približne 298 modulov služieb
├── transformer/          # Transformátor formátu Responses API
├── translator/           # Preklad formátov (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Zdieľané pomocné nástroje (protokolovanie, chyby, streamy atď.)
```

### Počty modulov

| Adresár | Súbory | Účel |
| `executors/` | 167 | HTTP exekútory jednotlivých poskytovateľov (zjednotené pomocou továrne DefaultExecutor) |
| `handlers/` | 157 | Vstupné body požiadaviek (chatCore, responses, embeddings) |
| `services/` | ~536 | Smerovanie, ukladanie do vyrovnávacej pamäte, obmedzovanie frekvencie, obnovovanie atď. |
| `translator/` | 56 | Konverzia formátov (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Nástroje a transporty MCP |
| `utils/` | ~108 | Prierezové pomocné nástroje (protokolovanie, chyby, streamy) |
| `config/` | ~339 | Konfigurácie poskytovateľov, konštanty, registre |

---

## Spracovanie požiadavky

Každá požiadavka na LLM prechádza **5-fázovým procesom**:

```
                              ┌──────────────┐
   HTTP požiadavka             │  1. SMEROVAŤ│   rozlíšenie kombinácie, výber modelu
   (trasa Next.js)             └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. PRELOŽIŤ │   konverzia formátu (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. VYKONAŤ  │   exekútor poskytovateľa, HTTP, opakovanie, istič
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAMOVAŤ│  transformácia SSE, spätný tlak
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. ZAZNAMENAŤ│  sledovanie využitia, záznam volania, klasifikácia chýb
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP odpoveď (SSE alebo JSON)
```

### Fáza 1: Smerovanie (services/combo.ts)

**Vstupný bod**: `handleComboChat()` v `services/combo.ts`

Prevedie požiadavku na konkrétnu n-ticu `(provider, model, account, credentials)`:

- Vyhľadá kombináciu podľa ID (alebo vytvorí virtuálnu kombináciu pre modely `auto/*`)
- Použije stratégiu smerovania (prioritná, vážená, cyklická atď.)
- Odfiltruje poskytovateľov, ktorí nie sú v poriadku (istič)
- Vyberie ďalší použiteľný cieľ

Pre modely `auto/*` táto fáza tiež:

- Spustí **16-faktorový bodovací** algoritmus (`services/autoCombo/`)
- Vyberie dvojicu `provider+model` na základe stavu, ceny, latencie atď.

### Fáza 2: Preklad (translator/)

Ak sa zdrojový formát (napr. OpenAI) líši od cieľového formátu (napr. Claude), požiadavka sa **preloží**:

- Systémová výzva → systémová správa
- Definície nástrojov → formát nástrojov špecifický pre poskytovateľa
- Parametre uvažovania/premýšľania → ekvivalenty špecifické pre poskytovateľa
- Normalizácia rolí správ (`developer` → `system` pre systémy iné ako OpenAI)

Súbor `translator/index.ts` sprístupňuje:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Fáza 3: Vykonanie (executors/)

**Vstupný bod**: `getExecutor(providerId).execute(request, options)`

Všetci poskytovatelia používajú `DefaultExecutor` (`executors/default.ts`) prostredníctvom záložného mechanizmu továrne `getExecutor()`. Exekútor:

- Zostaví nadradenú URL adresu (`buildUrl()`)
- Pridá hlavičky špecifické pre poskytovateľa (`buildHeaders()`)
- Transformuje telo požiadavky (`transformRequest()`)
- Odošle HTTP požiadavku s opakovaním a exponenciálnym predlžovaním intervalov
- V prípade potreby spracuje obnovenie autentifikácie (poskytovatelia OAuth)

Všetky exekútory rozširujú `BaseExecutor` (`executors/base.ts`, 1170 riadkov kódu), ktorý poskytuje:

- Spoločnú logiku opakovania
- Integráciu proxy
- Integráciu ističa
- Hooky na zaznamenávanie využitia

### Fáza 4: Streamovanie (utils/stream.ts)

Pri streamovaných odpovediach exekútor vráti **ReadableStream**. Obslužná rutina:

- Presmeruje dáta cez transformáciu SSE (`createSSETransformStreamWithLogger`)
- Používa kontrolné signály heartbeat na detekciu neaktívnych pripojení
- Korektne spracuje odpojenie klienta (`pipeWithDisconnect`)
- Transformuje SSE → JSON pre klientov bez podpory streamovania

Pri nestreamovaných odpovediach exekútor vráti spracovaný objekt JSON, ktorý sa ďalej odovzdá bez zmien.

### Fáza 5: Zaznamenanie (services/usage.ts)

Po odpovedi (úspešnej alebo neúspešnej) sa zaznamenajú údaje o použití:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` z odpovede
- `cost_usd` vypočítané z cenových údajov
- `latency_ms`, `status`, `error_class` v prípade zlyhania
- Uložené v tabuľke `usage_history`

Artefakty protokolov volaní (ak sú povolené) sa zapisujú do `${DATA_DIR}/call_logs/`.

---

## Hĺbkový pohľad na kľúčové súbory

### chatCore.ts (5977 riadkov)

**Hlavná obsluha požiadaviek**. Napriek svojej veľkosti má jasnú štruktúru:

```ts
// Pseudoštruktúra súboru chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Autentifikácia + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Validácia tela
  const body = await parseRequestBody(request);

  // 3. Detekcia formátu + preklad
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Smerovanie kombinácie
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Pokračovať ďalším cieľom
    }
  }

  // 5. Núdzový záložný mechanizmus
  return await emergencyFallback(body);
}
```

Napriek tomu, že ide o jednu obrovskú funkciu, je usporiadaná do **komentovaných sekcií**, ktoré zodpovedajú 5-fázovému spracovateľskému reťazcu.

### combo.ts (4456 riadkov kódu)

**Smerovací mechanizmus**, ktorý pre kombináciu určuje usporiadané ciele.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Podporuje **19 stratégií smerovania** (pozrite si `src/shared/constants/routingStrategies.ts`):

| Stratégia           | Správanie                                                                                               |
| ------------------- | ------------------------------------------------------------------------------------------------------- |
| `priority`          | Usporiadaný zoznam začínajúci prvým cieľom                                                              |
| `weighted`          | Pravdepodobnostný výber podľa váhy jednotlivých cieľov                                                  |
| `round-robin`       | Cyklické prechádzanie cieľov v danom poradí                                                             |
| `context-relay`     | Odovzdávanie kontextu medzi cieľmi                                                                      |
| `fill-first`        | Vyčerpanie kvóty pred prechodom na ďalší cieľ                                                           |
| `p2c`               | Výber z dvoch možností                                                                                  |
| `random`            | Rovnomerný náhodný výber                                                                                |
| `least-used`        | Výber cieľa s najnižším počtom nedávnych použití                                                        |
| `cost-optimized`    | Najprv najlacnejší funkčný cieľ                                                                         |
| `reset-aware`       | Zohľadňuje intervaly obnovenia poskytovateľa                                                            |
| `reset-window`      | Smerovanie založené na intervale obnovenia                                                              |
| `headroom`          | Najprv cieľ s najväčšou zostávajúcou rezervou kvóty                                                     |
| `strict-random`     | Skutočne rovnomerný výber (bez váženia podľa kvality)                                                   |
| `auto`              | Používa 16-faktorové bodovanie (`autoCombo/`)                                                           |
| `lkgp`              | Najprv posledný známy funkčný poskytovateľ                                                              |
| `context-optimized` | Najvhodnejší pre požiadavky s dlhým kontextom                                                           |
| `fusion`            | Paralelne rozošle požiadavku panelu a následne výsledky syntetizuje pomocou posudzovateľa (`fusion.ts`) |

### base.ts (1170 riadkov kódu)

**Abstraktný vykonávací modul**, ktorý rozširuje všetkých 107 vykonávacích modulov. Obsahuje:

- `buildUrl()` — predvolené zostavenie URL (podtriedy ho pre vlastné potreby prepisujú)
- `buildHeaders()` — predvolené hlavičky (autentifikácia, typ obsahu)
- `transformRequest()` — predvolene odovzdáva údaje bez zmien
- `execute()` — hlavný cyklus HTTP s opakovanými pokusmi, exponenciálnym oneskorením a ističom

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Obsluhuje všetkých poskytovateľov kompatibilných s OpenAI/Anthropic
  // Poskytovatelia registrujú konfigurácie (URL, autentifikáciu, hlavičky), ale zdieľajú logiku vykonávacieho modulu
}
```

Správanie špecifické pre poskytovateľa (autentifikačné hlavičky, základná URL, hlavičky verzie) sa konfiguruje prostredníctvom registra poskytovateľov, nie pomocou samostatných tried vykonávacích modulov.

````

---

## Služby (117 modulov)

Služby sú **účelovo zamerané moduly s jedinou zodpovednosťou**, ktoré handlery skladajú dohromady. Hlavné kategórie:

### Smerovanie a Combo

- `combo.ts` — vstupný bod pre požiadavky smerované cez combo
- `services/autoCombo/` — bodovanie podľa 16 faktorov, 8 stratégií automatického smerovania
- `wildcardRouter.ts` — vyhľadáva zhodu s trasami so zástupnými znakmi (`gpt-*`)
- `modelFamilyFallback.ts` — T5 fallback v rámci rodiny

### Obmedzovanie rýchlosti a kvóty

- `rateLimitManager.ts` — token bucket pre každý kľúč a poskytovateľa
- `usage.ts` — zaznamenávanie využitia
- `quotaCache.ts` — snímky kvót v pamäti

### Účty a tokeny

- `tokenRefresh.ts` — obnovenie OAuth pri 401
- `accountFallback.ts` — prepnutie na alternatívny účet
- `sessionManager.ts` — stav viacťahovej relácie

### Inteligencia

- `intentClassifier.ts` — klasifikácia zámeru požiadavky
- `taskAwareRouter.ts` — smerovanie podľa typu úlohy
- `thinkingBudget.ts` — prideľovanie tokenov na uvažovanie
- `contextManager.ts` — vkladanie kontextu smerovania

### Odolnosť

- `resilience.ts` — orchestrácia opakovaných pokusov, oneskorenia a ističa
- `emergencyFallback.ts` — fallback poslednej možnosti
- `modelDeprecation.ts` — automatické smerovanie na nástupnícke modely

### Stav

- `signatureCache.ts` — deduplikácia podľa signatúry požiadavky
- `volumeDetector.ts` — odľahčovanie záťaže
- `contextHandoff.ts` — serializácia relácie

### Kompresia

- `compression/` (podadresár) — kompletný kompresný pipeline
- 39 súborov zahŕňajúcich enginy, balíky pravidiel a adaptéry

### Zručnosti

- (opísané v [SKILLS.md](./SKILLS.md))

### Pamäť

- (opísané v [MEMORY.md](./MEMORY.md))

---

## Exekútory (75+ súborov)

Jeden súbor pre každého poskytovateľa. Všetky rozširujú `BaseExecutor` a prepisujú to, čo sa líši.

### Bežné vzory

Poskytovatelia sa rozlišujú prostredníctvom `getExecutor(providerId)`, ktorý vráti nakonfigurovaný exekútor. Poskytovatelia kompatibilní s OpenAI/Anthropic používajú `DefaultExecutor` (`executors/default.ts`). Správanie špecifické pre poskytovateľa (základná URL, autorizačné hlavičky, verzia API) sa konfiguruje v `open-sse/config/providers/`, zatiaľ čo transformácie tela požiadavky sa spracúvajú v `open-sse/translator/`.

**Vlastná URL** sa nastavuje prostredníctvom konfigurácie poskytovateľa:

```ts
// Konfigurácia poskytovateľa v open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Vlastná autentifikácia** sa spracúva prostredníctvom konfigurácie autentifikácie v registri poskytovateľov (kľúč API, OAuth, profily hlavičiek).

Transformácie **vlastného tela požiadavky** (napr. oddelenie `system` od `messages` pri Anthropic) sa registrujú pre jednotlivých poskytovateľov v `open-sse/translator/`.

````

### Továreň exekútorov

`executors/index.ts` exportuje `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Rozlíšenie prebieha cez `ExecutorRegistry` (`executors/registry.ts`): každý špecializovaný exekútor je deklarovaný vo vstavanej tabuľke v `executors/index.ts` a pri načítaní modulu zaregistrovaný prostredníctvom `registerExecutor(alias, instance)`; `getExecutor()` vyhľadá v registri a pri každom poskytovateľovi bez špecializovanej položky použije ako fallback memoizovaný `DefaultExecutor`. Kompletné mapovanie alias → exekútor charakterizuje referenčný test `tests/unit/executor-map-golden.test.ts`.

---

## Prekladače

Preklad medzi **3 formátmi**: OpenAI, Anthropic, Gemini a navyše nové Responses API.

### Kedy dochádza k prekladu

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Bežné preklady:

- `OpenAI → Anthropic`: samostatné pole `system`, hlavička `x-api-key`
- `OpenAI → Gemini`: `contents` namiesto `messages`, `systemInstruction`
- `OpenAI → Responses API`: pole `input`, stav `previous_response_id`

### Ošetrené okrajové prípady

- rola `developer` → `system` pre iné systémy ako OpenAI
- rola `system` → zlúčená do prvej správy používateľa pre GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` systému Gemini
- `tools` → formát nástrojov špecifický pre poskytovateľa
- Parametre uvažovania (o1, Claude) → ekvivalenty špecifické pre poskytovateľa

---

## Server MCP

`open-sse/mcp-server/` implementuje server protokolu **Model Context Protocol**:

- **110 nástrojov** (správa poskytovateľov, kombinácie, pamäť, vyrovnávacia pamäť, kompresia, proxy, zručnosti, gamifikácia, pluginy, Notion, Obsidian, lokálny korpus)
- **3 transporty**: stdio, SSE, Streamable HTTP
- **33 rozsahov** na jemne odstupňovanú autorizáciu

### Registrácia nástrojov

Nástroje sú registrované ako samostatné súbory v `open-sse/mcp-server/tools/`, pričom každý exportuje názov, schému, obslužnú funkciu a rozsah:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Získať snímku stavu systému",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transporty

```ts
// stdio (použitie v CLI)
startMcpStdio(server);

// SSE (streamovanie založené na HTTP)
startMcpSse(server, port);

// Streamovateľné HTTP (moderné MCP)
startMcpStreamable(server, port);
```

### Autorizácia

Každé volanie nástroja prechádza kontrolami rozsahu (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Nedostatočný rozsah");
}
```

---

## Transformátory

`open-sse/transformer/` konvertuje medzi formátmi **Chat Completions** a **Responses API**.

### Prečo samostatný transformátor?

Responses API je nový formát OpenAI so **stavovými konverzáciami** (`previous_response_id`). Keď klient odošle požiadavku Responses, OmniRoute:

1. Interne konvertuje Responses → Chat Completions
2. Odošle ju poskytovateľovi (ľubovoľnému poskytovateľovi, ktorý podporuje Chat Completions)
3. Konvertuje odpoveď späť do formátu Responses
4. Streamuje konvertovanú odpoveď klientovi

Transformátor (`transformer/responsesTransformer.ts`) poskytuje:

```ts
createResponsesApiTransformStream(): TransformStream
```

Ten spracováva:

- udalosti `response.output_item.added`
- udalosti `response.output_text.delta`
- udalosť `response.completed`
- Mapovanie volaní nástrojov (`function_call` ↔ `tool_calls`)

---

## Konfigurácia

`open-sse/config/` obsahuje konfiguračnú vrstvu:

| Súbor                         | Účel                                                          |
| ----------------------------- | ------------------------------------------------------------- |
| `providerRegistry.ts`         | Register chatovacích modelov nad katalógom 352 poskytovateľov |
| `providerModels.ts`           | Aliasy modelov, mapovanie formátov                            |
| `constants.ts`                | Časové limity, obmedzenia, stavové kódy                       |
| `defaultThinkingSignature.ts` | Predvolený podpis uvažovania Claude                           |
| `modelStrip.ts` (v službách)  | Odstraňovanie polí pre jednotlivých poskytovateľov            |

### Schéma registra poskytovateľov

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Validácia pomocou Zod pri načítaní modulu zaisťuje platnosť všetkých konfigurácií poskytovateľov.

---

## Výkonnostné obmedzenia

Smerovací engine má prísne výkonnostné limity:

| Operácia                                               | Cieľ  | Meranie                                |
| ------------------------------------------------------ | ----- | -------------------------------------- |
| Vyhodnotenie kombinácie                                | <10ms | Pre 50 cieľov                          |
| Kontrola obmedzenia frekvencie                         | <1ms  | Token bucket v pamäti                  |
| Záložný výber rodiny modelov                           | <5ms  | Definície rodín vo vyrovnávacej pamäti |
| Odoslanie požiadavky na smerovanie                     | <2ms  | Kritická cesta                         |
| **Žiadne blokujúce I/O na kritickej ceste smerovania** | —     | Všetko asynchrónne                     |

---

## Antivzory

❌ **Synchrónne volania databázy v `combo.ts`** — vykonajte predbežný výpočet a použite vyrovnávaciu pamäť
❌ **Logika opakovania v obslužných rutinách** — použite `retry()` zo služby odolnosti
❌ **Priamy prístup ku konfigurácii poskytovateľa** — použite gettery `providerRegistry`
❌ **Natvrdo zakódované reťazce záložných možností** — definujte ich v `modelFamilyFallback.ts`
❌ **Mutácie stavu medzi súbežnými požiadavkami** — používajte iba kontext viazaný na požiadavku

---

## Pridanie nového komponentu

### Pridanie novej služby

1. Vytvorte `open-sse/services/[serviceName].ts` s jasne vymedzenou zodpovednosťou
2. Exportujte hlavnú obslužnú funkciu a všetky konštanty
3. Pridajte jednotkové testy do `tests/unit/services/[serviceName].test.mjs`
4. Integrujte službu do procesu spracovania požiadaviek v `handlers/chatCore.ts` (ak súvisí so smerovaním)
5. Ak služba ovplyvňuje výber cieľa, aktualizujte logiku smerovania v `combo.ts`
6. Zdokumentujte ju v tomto súbore

### Pridanie nového vykonávateľa

1. Vytvorte `open-sse/executors/[provider].ts`, ktorý rozširuje `BaseExecutor`
2. Zaregistrujte ho v `config/providerRegistry.ts`
3. Pridajte ho do továrne v `executors/index.ts`
4. Pridajte jednotkové testy vykonávateľa
5. Zdokumentujte ho v `docs/architecture/ARCHITECTURE.md`

### Pridanie nového nástroja MCP

1. Vytvorte alebo aktualizujte `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definujte schému Zod pre vstupy
3. Zaregistrujte nástroj v `mcp-server/index.ts`
4. Pridajte ho do matice rozsahov v `mcp-server/auth/`
5. Pridajte jednotkové testy

---

## Pozrite tiež

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — architektúra na vysokej úrovni
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — technická referencia
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — prehľad jednotlivých adresárov
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — hodnotenie podľa 16 faktorov
- [MCP-SERVER.md](./MCP-SERVER.md) — server MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — server A2A
- Zdroj: `open-sse/` (viac ako 400 súborov, ~143K riadkov kódu)
