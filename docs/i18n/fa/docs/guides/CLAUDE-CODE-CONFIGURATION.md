# Claude Code CLI — Configuration with OmniRoute (فارسی)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

رابط خط فرمان **Claude Code** (`claude`) را به OmniRoute — بهصورت محلی یا روی یک VPS راه دور —
با پروفایلهای جداگانه برای هر مدل متصل کنید؛ مشابه پیکربندی Codex.

---

## شروع سریع

```bash
# اجرای Claude Code با یک OmniRoute محلی (context فعال را بهصورت خودکار تشخیص میدهد)
omniroute launch

# اتصال به یک OmniRoute راه دور (پس از `omniroute connect <host>`، این کار خودکار است)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# ایجاد پروفایلهای جداگانه برای هر مدل، سپس اجرای یکی از آنها
omniroute setup-claude            # فایل ~/.claude/profiles/<name>/settings.json را مینویسد
omniroute launch --profile glm52  # استفاده Claude Code از glm/glm-5.2 از طریق OmniRoute
```

---

## نحوه اتصال Claude Code به یک درگاه

Claude Code با **Anthropic Messages API** ارتباط برقرار میکند و از طریق متغیرهای
محیطی به یک endpoint سفارشی هدایت میشود (این ابزار گزینه `--base-url` ندارد):

| متغیر                                        | کاربرد                                                                                                   |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `ANTHROPIC_BASE_URL`                         | URL ریشه درگاه (Claude Code مسیر `/v1/messages` را به آن اضافه میکند). **بدون پسوند `/v1`.**             |
| `ANTHROPIC_AUTH_TOKEN`                       | بهشکل `Authorization: Bearer …` ارسال میشود — از توکن دسترسی یا کلید API مربوط به OmniRoute استفاده کنید |
| `ANTHROPIC_API_KEY`                          | جایگزین: بهشکل `x-api-key` ارسال میشود. اگر هر دو تنظیم شده باشند، `ANTHROPIC_AUTH_TOKEN` اولویت دارد    |
| `ANTHROPIC_MODEL`                            | اجبار به استفاده از یک مدل مشخص (پیشفرض انتخابگر `/model` را نادیده میگیرد)                              |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | `1` ← انتخابگر بومی `/model` مدلهای `claude*`/`anthropic*` را از `/v1/models` فهرست میکند                |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | محدودکردن تعداد توکنهای خروجی در هر پاسخ (برای مثال `65536`)                                             |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | آستانه توکن برای فشردهسازی خودکار                                                                        |

> متغیرهای محیطی فقط **یکبار هنگام راهاندازی** خوانده میشوند — پس از تغییر آنها، Claude Code را دوباره راهاندازی کنید.

`omniroute launch` همه این موارد را برای شما تنظیم میکند: URL پایه و توکن را
از context فعال بهدست میآورد (بنابراین پس از `omniroute connect <vps>`، اجرای
`omniroute launch` بدون تنظیمات اضافه کار میکند)، سلامت سرور را بررسی میکند و
سپس `claude` را اجرا میکند.

---

## نامهای مستعار اکتشاف — نمایش مدلهای غیر Claude در انتخابگر `/model`

قابلیت اکتشاف مدل درگاه در Claude Code فقط شناسههایی را فهرست میکند که با
`claude` یا `anthropic` آغاز شوند؛ بنابراین با
`CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`، انتخابگر بومی `/model` معمولاً
**فقط** مدلهای Claude/Anthropic در OmniRoute را نمایش میدهد — مدلهایی مانند
`kimi/kimi-k2.6` یا `glm/glm-5.2` هرگز ظاهر نمیشوند، حتی با اینکه مسیریابی آنها
بهدرستی انجام میشود.

OmniRoute میتواند هر مدل فعال (و combo) را با یک شناسه `claude/…` بازتاب دهد تا
از این فیلتر عبور کرده و در انتخابگر ظاهر شود:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<combo "custo-otimizado"> →  claude/combo/custo-otimizado
```

هنگامی که یکی از این موارد را در Claude Code انتخاب میکنید، OmniRoute پیش از
مسیریابی، پوشش `claude/` را حذف کرده و شناسه واقعی را بازیابی میکند — یک شناسه
اصلی `claude/<real-claude-model>` (ارائهدهنده واقعی Claude OAuth) همیشه بدون
تغییر باقی میماند.

**این قابلیت بهصورت پیشفرض غیرفعال است** و توسط یک سازوکار سهسطحی کنترل میشود
(خاصترین سطح اولویت دارد). بنابراین یک OmniRoute معمولی برای کلاینتهایی که از
Claude Code استفاده نمیکنند، کاتالوگ خود را دوبرابر نمیکند:

| سطح        | محل                                                                                 |
| ---------- | ----------------------------------------------------------------------------------- |
| مدل        | صفحه جزئیات ارائهدهنده ← کلید جداگانه «نمایش در Claude Code» برای هر مدل            |
| ارائهدهنده | صفحه جزئیات ارائهدهنده ← کلید سطح ارائهدهنده (شامل همه مدلهای آن میشود)             |
| سراسری     | تنظیمات ← Feature Flags ← `EXPOSE_CC_DISCOVERY_ALIASES` (بهصورت پیشفرض غیرفعال است) |

متغیر محیطی `EXPOSE_CC_DISCOVERY_ALIASES` سطح سراسری را اجباراً فعال میکند و
بر مقدار تنظیمشده در داشبورد اولویت دارد (در این حالت، صفحه Feature Flags
یادداشت «فعال از طریق متغیر محیطی» را نمایش میدهد). کلیدهای هر ارائهدهنده و
هر مدل، تنظیم را از آن نقطه دقیقتر میکنند — برای مثال، اگر سطح سراسری خاموش
و ارائهدهنده Kimi روشن باشد، فقط مدلهای Kimi نمایش داده میشوند.

> ⚠️ **ناهماهنگی پنجره در مدلهای غیر Claude.** Claude Code برای هر شناسهای که
> نمیشناسد، یک پنجره context با اندازه 200K در نظر میگیرد (نمیتواند اندازه
> واقعی پنجره را از `/v1/models` بخواند). برای مدلی با پنجره بزرگتر (برای مثال
> پنجره 256K در Kimi K2)، مقدار `CLAUDE_CODE_AUTO_COMPACT_WINDOW` را پایینتر از
> اندازه واقعی پنجره مدل تنظیم کنید تا فشردهسازی خودکار زودتر از موعد اجرا نشود.
> پروفایلهای تولیدشده در بالا از قبل این مقدار را بهصورت جداگانه برای هر مدل
> تنظیم میکنند.

---

## بلوک راهاندازی اولیه در داشبورد

کارت ابزار Claude (**Dashboard → CLI Code**) قطعهٔ دقیق `settings.json` را
برای این نمونه، در کنار دکمهٔ اطلاعات نام مستعار کشف و همراه با یک دکمهٔ کپی نمایش میدهد:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<OmniRoute شما>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<کلید API مربوط به OmniRoute شما>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

URL پایه همان مقداری است که کارت آن را تعیین کرده است (از جمله مقدار سفارشیای که وارد کردهاید)
و از قبل نرمالسازی شده است — بدون پسوند `/v1` و بدون اسلش پایانی. **کلید هرگز نمایش داده نمیشود**:
این بلوک با یک جاینگهدار ارائه میشود، بنابراین تصویر صفحه یا قطعهٔ جایگذاریشده نمیتواند کلیدی را
افشا کند. کلید خود را جایگزین آن کنید.

برای هر مدلی که پنجرهٔ زمینهٔ واقعی آن 200K نیست، `CLAUDE_CODE_AUTO_COMPACT_WINDOW` را زیر همان
بلوک `env` اضافه کنید — Claude Code برای هر شناسهای که نمیشناسد مقدار 200K را فرض میکند؛ بنابراین
در غیر این صورت، فشردهسازی خودکار در نقطهٔ اشتباهی فعال میشود (هشدار بخش قبلی را ببینید).
سازندهٔ قطعه نیز این مقدار را میپذیرد؛ بنابراین فراخوانی که اندازهٔ پنجرهٔ مدل هدف را میداند،
میتواند آن را مستقیماً تولید کند.

منبع: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (سازندهٔ
خالص، دارای آزمون واحد) که توسط `ClaudeGatewayOnboardingBlock` نمایش داده میشود.

---

## پروفایلها (`CLAUDE_CONFIG_DIR`)

Claude Code **فایل پروفایل بومی ندارد** (برخلاف `~/.codex/<name>.config.toml` در Codex).
سازوکار متداول، `CLAUDE_CONFIG_DIR` است — یک دایرکتوری پیکربندی جداگانه برای هر
پروفایل که هرکدام `settings.json`، اعتبارنامهها، تاریخچه و کش مخصوص خود را دارند.

`omniroute setup-claude` کاتالوگ زندهٔ `/v1/models` را دریافت میکند و برای هر مدل یک
پروفایل در `~/.claude/profiles/<name>/settings.json` مینویسد و از
**همان نامهای `setup-codex`** (`glm52`، `kimi-k27`، `deepseek-pro`، …) استفاده میکند:

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **توکن احراز هویت هرگز در پروفایل نوشته نمیشود.** با
> `omniroute launch --profile <name>` اجرا کنید (این فرمان `ANTHROPIC_AUTH_TOKEN` را از
> زمینهٔ فعال تزریق میکند)، یا خودتان `ANTHROPIC_AUTH_TOKEN` را export کرده و
> `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude` را اجرا کنید.

**همگامسازی خودکار پس از کشف مدل (اختیاری).** OmniRoute میتواند همین فایلهای
`~/.claude/profiles/<name>/settings.json` را هر زمان که همگامسازی مدلهای یک ارائهدهنده
کاتالوگ زنده را تغییر میدهد، بهطور خودکار بازتولید کند — بنابراین مدلهای جدید یا تغییرنامیافته
بدون اجرای مجدد فرمان، پروفایل دریافت میکنند. این قابلیت **بهطور پیشفرض غیرفعال است**: آن را از
**داشبورد CLI Code** («CLI profile auto-sync» → Claude Code) فعال کنید، یا
`OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` را تنظیم کنید (همچنین از `CLI_ALLOW_CONFIG_WRITES`
پیروی میکند که بهطور پیشفرض فعال است). در صورت فعال بودن، فقط فایلهای پروفایل را مینویسد؛
هرگز پیکربندی فعال/پیشفرض Claude، احراز هویت یا `~/.claude/settings.json` را تغییر نمیدهد.

### تولید و استفاده از پروفایلها

```bash
# OmniRoute محلی
omniroute setup-claude

# VPS راهدور (URL مربوط به VPS را در همهٔ پروفایلها درج میکند)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# فقط برخی از ارائهدهندگان
omniroute setup-claude --only glm,kimi

# پیشنمایش بدون نوشتن
omniroute setup-claude --dry-run

# اجرای یک پروفایل
omniroute launch --profile kimi-k27
```

---

## سطحهای مدل (اختیاری)

Claude Code درخواستها را به سطحهای مختلف قابلیت هدایت میکند. اگر میخواهید برای هر سطح از ارائهدهندهای متفاوت استفاده کنید، از طریق متغیرهای محیطی / تنظیمات، هر سطح را به یک مدل OmniRoute نگاشت کنید:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

در غیر این صورت، یک `ANTHROPIC_MODEL` واحد (که پروفایلها تنظیم میکنند) برای همهچیز استفاده میشود.

---

## حالت راهدور

پس از اجرای `omniroute connect <host>` (به
[حالت راهدور](./REMOTE-MODE.md) مراجعه کنید)، `omniroute launch` و `omniroute setup-claude`
بهطور خودکار آن سرور راهدور را هدف قرار میدهند و از توکن دسترسی محدودشدهٔ آن استفاده میکنند — هیچ
فلگ اضافهای لازم نیست. برای بازنویسی در هر بار اجرا، از `--remote` / `--api-key` استفاده کنید.

---

## عیبیابی

**Claude Code دروازه را نادیده میگیرد** — تأیید کنید که `ANTHROPIC_BASE_URL` شامل
`/v1` **نیست** و `claude` را دوباره راهاندازی کنید (متغیرهای محیطی فقط یکبار هنگام راهاندازی خوانده میشوند). `omniroute launch`
این کار را برای شما انجام میدهد.

**انتخابگر `/model` خالی است / مدلهای دروازه وجود ندارند** — به Claude Code
v2.1.219+ و `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` نیاز دارد. فقط شناسههای مدل `claude*` /
`anthropic*` در انتخابگر نمایش داده میشوند؛ هر مدل دیگری را با
`ANTHROPIC_MODEL=<id>` اجباری کنید (پروفایلها همین کار را انجام میدهند).

**`400 Ambiguous model 'claude-…'`** — Claude Code همیشه شناسههای مدل را **بدون پیشوند**
ارسال میکند (برای مثال `claude-opus-4-8`)، بنابراین وقتی هر دو ارائهدهندهٔ Claude Code (`cc/…`) و
Claude (`claude/…`) متصل باشند، شناسهٔ بدون پیشوند با دو مسیر مطابقت دارد و
OmniRoute از حدسزدن خودداری میکند. این مشکل را به یکی از دو روش برطرف کنید: یک شناسهٔ پیشونددار را با
`ANTHROPIC_MODEL=cc/claude-opus-4-8` ثابت کنید، یا گزینهٔ **ترجیح Claude Code برای
مدلهای Claude بدون پیشوند** را فعال کنید — این گزینه در صفحهٔ ارائهدهندهٔ Claude قرار دارد؛ یا
`OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` را تنظیم کنید (بهطور پیشفرض غیرفعال است؛
به [محیط](../reference/ENVIRONMENT.md) مراجعه کنید) — که شناسههای بدون پیشوند `claude-*`
را بهجای آن به Claude Code هدایت میکند. پیشوندهای صریح ارائهدهنده همیشه اولویت دارند.

**خطاهای احراز هویت** — پروفایل هیچ توکنی نگه نمیدارد. از `omniroute launch --profile`
استفاده کنید (توکن را تزریق میکند) یا `ANTHROPIC_AUTH_TOKEN` را صادر کنید.

**پروفایلها ایزوله نیستند** — هر پروفایل یک `CLAUDE_CONFIG_DIR` مجزا دارد؛
بررسی کنید که خروجی `echo $CLAUDE_CONFIG_DIR` درون نشست به
`~/.claude/profiles/<name>` اشاره میکند.
