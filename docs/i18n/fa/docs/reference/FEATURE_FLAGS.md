# Feature Flags (فارسی)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇷🇺 [ru](../../../ru/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> کلیدهای runtime که رفتار OmniRoute را **بدون استقرار مجدد** تغییر میدهند.
> تمام فلگهای فهرستشده در اینجا در
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> تعریف شدهاند — منبع واحد حقیقت. داشبورد و REST API هر دو از
> آن فایل میخوانند، بنابراین جدول زیر بهگونهای تولید شده است که دقیقاً بهصورت 1:1 با آن مطابقت داشته باشد.

---

## فلگهای قابلیت چیستند

فلگ قابلیت یک کلید نامگذاریشده (از نوع boolean یا enum) است که مقدار آن را میتوان در
runtime تغییر داد و در پایگاه داده ذخیره کرد، بدون اینکه نیاز به استقرار مجدد فرایند باشد. هر
فلگ با یک `FeatureFlagDefinition` توصیف میشود که شامل `key`، `label`،
`description`، `category`، `defaultValue`، `type` و راهنمای `requiresRestart` است.

### ترتیب تعیین مقدار

**مقدار مؤثر** یک فلگ توسط
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) و با ترتیب
اولویت زیر تعیین میشود (بالاترین اولویت برنده است):

1. **بازنویسی DB** — مقداری که در جدول `key_value` و در فضای نام
   `feature_flags` ذخیره شده است (از طریق داشبورد یا REST API تنظیم میشود).
2. **متغیر محیطی** — `process.env[<KEY>]`، در صورتی که تنظیم شده و خالی نباشد.
3. **مقدار پیشفرض تعریف** — مقدار `defaultValue` از `featureFlagDefinitions.ts`.

یک فلگ boolean زمانی **فعال** در نظر گرفته میشود که مقدار مؤثر آن `"true"`،
`"1"` یا `"yes"` باشد (`isFeatureFlagEnabled()` را ببینید).

> [!NOTE]
> بیشتر فلگها همچنین یک متغیر محیطی متناظر با **همان نام** دارند
> که در [`ENVIRONMENT.md`](./ENVIRONMENT.md) مستند شده است. بازنویسی DB فلگ
> نسبت به آن متغیر محیطی اولویت دارد. فلگی با
> `requiresRestart: true` بلافاصله ذخیره میشود، اما فقط هنگام راهاندازی
> فرایند دوباره خوانده میشود — تغییر آن، بنر **«راهاندازی مجدد سرور»** را در داشبورد نمایش میدهد.

---

## فهرست فلگها

۷۴ فلگ در ۶ دسته. **پیشفرض** همان مقدار پیشفرض تعریفشده است — مقداری که
وقتی نه بازنویسی DB و نه متغیر محیطی وجود داشته باشد، استفاده میشود.

### امنیت (۱۰)

| کلید                                    | نوع  | پیشفرض   | توضیحات                                                                                                                                                                                                                                                                                       |
| --------------------------------------- | ---- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | بولی | `false`  | برای همه درخواستهای ورودی، کلید API الزامی باشد.                                                                                                                                                                                                                                              |
| `INPUT_SANITIZER_ENABLED`               | بولی | `true`   | پاکسازی ورودی برای همه درخواستها فعال شود.                                                                                                                                                                                                                                                    |
| `INJECTION_GUARD_MODE`                  | enum | `off`    | حالت محافظت در برابر تزریق پرامپت. مقادیر: `off`، `warn`، `block`، `redact`.                                                                                                                                                                                                                  |
| `PII_REDACTION_ENABLED`                 | بولی | `false`  | اطلاعات قابل شناسایی شخصی (PII) از درخواستها حذف شود (مستقل از `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                       |
| `PII_RESPONSE_SANITIZATION`             | بولی | `false`  | اطلاعات قابل شناسایی شخصی (PII) از پاسخهای ارائهدهنده پاکسازی شود.                                                                                                                                                                                                                            |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum | `redact` | حالت پاکسازی اطلاعات قابل شناسایی شخصی (PII) در پاسخ. مقادیر: `redact`، `warn`، `block`، `off`.                                                                                                                                                                                               |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | بولی | `true`   | درخواستهای خروجی به محدودههای IP خصوصی/داخلی مسدود شوند.                                                                                                                                                                                                                                      |
| `ALLOW_API_KEY_REVEAL`                  | بولی | `false`  | به کاربران احراز هویتشده داشبورد اجازه داده شود بهجای مشاهده صرفِ مقادیر پوشاندهشده، کلیدهای API ذخیرهشده را آشکار کنند.                                                                                                                                                                      |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | بولی | `false`  | پیشوند حساب در خطوط گزارش AUTH درج شود (برای مثال، «استفاده از حساب <provider>: abc12345...»). بهطور پیشفرض غیرفعال است تا شناسههای حساب در گزارشهای فرایند اشتراکی/چندمستاجری پوشانده شوند. این گزینه مستقل از حالت اشکالزدایی است؛ تغییر حالت اشکالزدایی باعث آشکار شدن این اطلاعات نمیشود. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | بولی | `false`  | هنگامی که OIDC فعال است، ورود با گذرواژه غیرفعال شود تا کاربران فقط بتوانند از طریق ورود یکپارچه OIDC احراز هویت کنند. در حالت غیرفعال (پیشفرض)، هم ورود با گذرواژه و هم OIDC در دسترس هستند.                                                                                                 |

### شبکه (۱۶)

| کلید                                            | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------------------- | ------- | ------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false` | ✓              | حالت پنهانسازی اثر انگشت TLS را فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false` |                | به مسیرهای /v1/audio/* اجازه میدهد از گرههای ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشوند استفاده کنند. بهطور پیشفرض غیرفعال است — مسیریابی صوت به یک میزبان راه دور، هویت خروجی را تغییر میدهد و باید تصمیمی صریح از سوی اپراتور باشد. گرههای loopback همیشه مجاز و بدون تأثیر هستند.                                                                                                                                                                                              |
| `RERANK_REMOTE_PROVIDER_NODES`                  | boolean | `false` |                | به POST /v1/rerank (و مرحله بازرتبهبندی loopback موتور حافظه) اجازه میدهد از گرههای ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشوند استفاده کند. بهطور پیشفرض غیرفعال است — مسیریابی به یک میزبان راه دور، هویت خروجی را تغییر میدهد و باید تصمیمی صریح از سوی اپراتور باشد. گرههای loopback همیشه مجاز هستند؛ گرههای راه دور باید سیاست URL خروجی ارائهدهنده را نیز با موفقیت بگذرانند.                                                                                               |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false` |                | وقتی هیچ پراکسیای به یک اتصال اختصاص داده نشده است، نخستین پراکسی فعال را بهطور خودکار از رجیستری انتخاب میکند. بهطور پیشفرض غیرفعال است (در غیر این صورت، هر پراکسی رجیستری به یک گزینه جایگزین سراسری تبدیل میشود — #3332).                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false` |                | به جریانهای اعتبارسنجی OAuth و ارائهدهنده اجازه میدهد در صورت شکست بررسیهای اولیه دسترسیپذیری پراکسی، پراکسی سنجاقشده را دور بزنند و مستقیماً متصل شوند. بهطور پیشفرض غیرفعال است، زیرا این کار میتواند IP خروجی را تغییر دهد.                                                                                                                                                                                                                                                                         |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`  |                | هنگام وقوع یک استثنای شبکه (پایان مهلت زمانی، رد یا بازنشانی اتصال) برای یک اجراکننده چرخش چندحسابی، اگر حساب ناموفق پراکسی اختصاصی نداشته باشد، یک دوره انتظار کوتاه اعمال میکند و بهجای تلاش مجدد برای تکتک حسابها، سایر حسابهای بدون پراکسی را تا پایان درخواست نادیده میگیرد. بهطور پیشفرض فعال است (ایمن: IP خروجی تغییر نمیکند و فقط خطر تأخیر یا دوره انتظار را برای حسابهای دارای خروجی مشترک کاهش میدهد). برای بازگرداندن انتشار فوری پس از نخستین خطای حساب بدون پراکسی، آن را غیرفعال کنید. |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false` |                | استخرهای پراکسی و چرخش هر حساب در opencode، ارائه مجدد پراکسیای را که بهتازگی ناموفق بوده است (بررسی TCP ردشده یا پاسخ 429 دریافتشده از طریق آن) برای مدتی در هر فرایند متوقف میکنند؛ این مدت با هر تکرار دو برابر میشود تا به سقف برسد. هیچ وضعیت پراکسیای نوشته نمیشود؛ اگر همه گزینهها کنار گذاشته شوند، انتخاب بدون تغییر باقی میماند. بهطور پیشفرض غیرفعال است.                                                                                                                                   |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false` |                | در داشبورد و زیر یک استخر پراکسی نشان میدهد که طی 24 ساعت گذشته، چند IP خروجی مشاهدهشده به اعضای آن سرویس دادهاند و چند اتصال از آنها استفاده کردهاند. فقطخواندنی است، از گزارش پراکسی محاسبه میشود و هرگز برای مسیریابی استفاده نمیشود. بهطور پیشفرض غیرفعال است.                                                                                                                                                                                                                                     |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false` |                | برای اجراکننده OpenCode، نخستین بایت بدنه یک پاسخ جریانی Responses را پایش میکند (بازه: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`، پیشفرض `15000`). یک جریان 2xx از Responses که پس از این بازه همچنان ساکت بماند، متوقفشده در نظر گرفته میشود: حساب وارد دوره انتظار میشود و درخواست یک بار به حساب بعدی میچرخد؛ توقف دوم فوراً با شکست مواجه میشود. بهطور پیشفرض غیرفعال است: جریانهای متوقفشده تا پایان مهلت زمانی آمادگی جریان، انتظار فعلی را ادامه میدهند.                                               |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false` |                | اجراکننده OpenCode: هنگام دریافت پاسخ 403/451 حاوی امتناع `user_blocked` (نه محدودیت جغرافیایی و نه رد اثرانگشت Cloudflare)، حساب ردشده را موقتاً از گردش خارج کرده و حداکثر یک بار در هر درخواست به حساب بعدی منتقل میشود؛ امتناع دوم بدون تغییر و بدون علامتگذاری موفقیت بازگردانده میشود. بهطور پیشفرض غیرفعال است: دور زدن مسدودسازی کاربر در سرویس بالادستی ممکن است بهعنوان تلاش برای اجتناب تلقی شود و این علامت را در سراسر مجموعه حسابها گسترش دهد.                                           |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false` |                | گردش OpenCode: پس از دو خطای گذرای متوالی در سرویس بالادستی (5xx یا پاسخ 400 با بدنه خالی)، پیش از رفتن به حساب بعدی مکث میشود — از 1.5 ثانیه شروع شده و با هر خطای بعدی دو برابر میشود، با سقف 6 ثانیه برای هر مکث و 10 ثانیه برای هر درخواست؛ در صورت قطع اتصال کارخواه، مکث انجام نمیشود؛ بدنه پاسخ ناموفق پیش از انتظار آزاد میشود. بهطور پیشفرض غیرفعال است: جایگزینی پس از خطا همچنان فوری باقی میماند.                                                                                          |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false` |                | گردش OpenCode: موج حسابها را در نخستین پاسخ 429 که بهعنوان محدودیت واقعی نرخ طبقهبندی شده است (دارای `Retry-After` قابلتجزیه، یا بدنهای که محدودیت نرخ/مصرف را ذکر میکند) متوقف کرده و همان پاسخ 429 سرویس بالادستی را بدون تغییر بازمیگرداند. پاسخهای 429 طبقهبندینشده به گردش ادامه میدهند. بهطور پیشفرض غیرفعال است: سطح رایگان بهازای هر IP خروجی محدود است (#9611)، بنابراین هر پاسخ 429 باعث گردش میشود و در صورت اتمام موج، آخرین پاسخ 429 سرویس بالادستی بازگردانده میشود.                     |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false` | ✓              | تأیید گواهی TLS را برای پراکسی MITM غیرفعال میکند. **خطرناک است.**                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false` |                | نشانیهای URL ارائهدهنده را که به شبکههای خصوصی/داخلی اشاره میکنند مجاز میکند.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`  |                | افزودن/اعتبارسنجی ارائهدهندگان روی نشانیهای محلی/خصوصی (127.0.0.1، localhost، LAN) را مجاز میکند. بهطور پیشفرض فعال است (اولویت با محیط محلی)؛ برای مسدودسازی سختگیرانه و محدود به نشانیهای عمومی، آن را غیرفعال کنید. فراداده ابری همچنان مسدود میماند.                                                                                                                                                                                                                                               |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false` | ✓              | حالت ارائهدهنده سازگار با Claude Code را فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

### سیاستها (5)

| کلید                            | نوع     | پیشفرض     | توضیحات                                                                                                                                                                                                                            |
| ------------------------------- | ------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled` | حالت اعمال سیاست استفاده از ابزار. مقادیر: `disabled`، `warn`، `block`.                                                                                                                                                            |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`    | محدودسازی نرخ را بهطور خودکار و بر اساس الگوهای مصرف فعال میکند.                                                                                                                                                                   |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`    | بررسی محلی OmniRoute برای پنجره زمینه / حداکثر توکنهای ورودی را در درخواستهای مستقیم تکمدلی نادیده میگیرد. محدودیتهای سرویس بالادستی همچنان اعمال میشوند.                                                                          |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`    | هنگامی که مدل هدف فاقد قابلیتهای موردنیاز باشد (بینایی، ابزارها، خروجی ساختاریافته، پنجره زمینه)، درخواستها را پیش از ارسال رد میکند. از درخواستهای مستقیم تکارائهدهندهای که فیلتر سازگاری لایه ترکیبی را دور میزنند محافظت میکند. |
| `RADAR_ENABLED`                 | boolean | `false`    | ماژول Radar در OmniRoute (صفحههای خوراک کاتالوگ و همگامسازی) را فعال میکند. بهطور پیشفرض غیرفعال است؛ فعالسازی فقط رابط کاربری را در دسترس قرار میدهد — همگامسازی دادهها همچنان نیازمند فعالسازی جداگانه است.                      |

### زمان اجرا (33)

| کلید                                        | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------- | ------- | ------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | بولی    | `true`  |                | هنگام تغییر مدلها توسط مسیریابی ترکیبی، خلاصههای مکالمه را تولید و تزریق میکند. برای مستقل در نظر گرفتن تغییر مدلها و جلوگیری از درخواستهای انتقال پسزمینه برای همه ترکیبهای فعلی و آینده، آن را غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | بولی    | `true`  |                | پیش از ارسال جریانهای عبوری Responses API به کلاینتها، موارد خروجی داخلی مربوط به مرحله توضیحات را حذف میکند. برای دریافت توضیحات خام بالادستی، آن را غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | بولی    | `true`  |                | محدودیتهای دامنه را برای دسترسی به ابزارهای MCP اعمال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | بولی    | `false` |                | توضیحات ابزارهای MCP را برای کاهش مصرف توکن فشرده میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | بولی    | `false` |                | پردازش وظایف پسزمینه را در زمان اجرا فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | بولی    | `false` | ✓              | همه سرویسهای پسزمینه (بهروزرسانی سهمیه، همگامسازی و غیره) را غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false` |                | اعتماد به فیلترهای RTK در سطح پروژه بدون اعتبارسنجی.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`  | ✓              | راهاندازی سرور WebSocket داشبورد بلادرنگ هنگام import (بهطور پیشفرض روی پورت 20132).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`  |                | اجازه به Codex برای استفاده از انتقال Responses-over-WebSocket. در صورت غیرفعال بودن، Codex به HTTP Responses بازمیگردد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`  |                | اجازه به Codex برای استفاده از انتقال محلی JSON-RPC مبتنی بر WebSocket در app-server (`codexTransport=app-server`). در صورت غیرفعال بودن، اتصالهایی که app-server را انتخاب کردهاند به سایر روشهای انتقال Codex بازمیگردند.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`  |                | هدایت درخواستهایی که بودجهشان تمام شده است به ارائهدهنده/مدل جایگزین رایگان اضطراری. (بخش [جایگزین اضطراری در صورت اتمام بودجه](#emergency-budget-fallback) را در ادامه ببینید.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false` |                | فعالسازی تلاش مجدد زودهنگام و شفاف برای جریانهای بالادستی SSE که ناقص قطع شدهاند، پیش از آنکه هرگونه بایت پاسخ به کلاینت برسد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false` |                | اجازه به بازیابی جریان برای ارسال مجدد درخواست و پیوند دادن پاسخ، پس از آنکه بایتها از قبل به کلاینت رسیدهاند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false` |                | ایمنسازی ادامه در میانه جریان برای فراخوانی ابزار: پس از انتشار یک فراخوانی ابزار، هرگز جریان قطعشده را از سر نگیرید (چه در حال اجرا باشد و چه قبلاً با `finish_reason` برابر با `tool_calls` پایان یافته باشد) و بهجای مصرف کل بودجه، پس از یک ادامه خالی اتصال را ببندید. در حالت غیرفعال: رفتار نسخه انتشاریافته.                                                                                                                                                                                                                                                                                                                                                                   |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false` |                | وقتی یک جریان SSE پیش از انتشار هرگونه فریم مفید بسته میشود و تلاش مجدد محدود روی همان اتصال به پایان رسیده است، یکبار به یک اتصال همسطح سوئیچ اضطراری انجام دهید؛ اگر اتصال همسطح قابلاستفادهای وجود نداشته باشد، خطای اصلی `STREAM_EARLY_EOF` با کد 502 بازگردانده میشود. بهطور پیشفرض غیرفعال است: EOF زودهنگام پس از تلاش مجدد روی همان اتصال، همچنان نهایی باقی میماند.                                                                                                                                                                                                                                                                                                           |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`  |                | فیلدهای نام مناسب برای نمایش را در پاسخهای `/v1/models` بگنجانید. برای کلاینتهایی که فقط انتظار شناسههای مدل را دارند، غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`  |                | نحوه پیشوندگذاری شناسههای مدل در /v1/models را کنترل میکند. 'dual' (پیشفرض) برای سازگاری با نسخههای قبلی، هم پیشوند نام مستعار و هم پیشوند شناسه متعارف ارائهدهنده را منتشر میکند. 'alias' فقط پیشوند کوتاه نام مستعار را منتشر میکند (برای مثال ds-web/model، نه deepseek-web/model). 'canonical' فقط پیشوند کامل شناسه ارائهدهنده را منتشر میکند. مقادیر: `dual`، `alias`، `canonical`.                                                                                                                                                                                                                                                                                              |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`  |                | همگامسازی دورهای ELO جدول امتیازات Arena AI را برای رتبهبندی هوشمندی مدلها فعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false` |                | شناسههای آینهای `claude/<provider>/<model>` را در `/v1/models` اعلام کنید تا کشف مدل در دروازه Claude Code، مدلهای غیر Claude را فهرست کند. سطح سراسریِ سازوکار سهسطحی (متغیر محیطی بر بازنویسی داشبورد اولویت دارد). به [پیکربندی Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker) مراجعه کنید.                                                                                                                                                                                                                                                                                                                  |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`  |                | کلید اصلی برای نامهای مستعار دروازه no-think/<provider>/<model>. در حالت فعال (پیشفرض): /v1/models برای هر مدل واجد شرایط Claude که قابلیت تفکر دارد، یک گونه بدون تفکر اعلام میکند و شناسهای با no-think/ که در یک درخواست ارسال شود، با سرکوب استدلال دوباره به مدل واقعی نگاشت میشود. در حالت غیرفعال: هیچ گونهای اعلام نمیشود و شناسهای با no-think/ مانند هر شناسه مدل ناشناخته دیگری تلقی میشود. تا زمانی که این گزینه فعال است، انتخاب فعال/غیرفعال برای هر مدل از طریق ModelSpec.noThinkingAlias همچنان اعمال میشود.                                                                                                                                                           |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false` |                | تولید گونههای سطح تفکر (برای مثال -low، -medium، -high) را در کاتالوگ /v1/models غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false` | ✓              | مسیرهای مجازی تطبیقی پذیرش بهازای هر مستأجر را برای ارسال به ارائهدهنده فعال کنید (#9654): افزایش ناگهانی بار یک مستأجر دیگر باعث خطای 503 برای مستأجر دیگر نمیشود. متغیر محیطی OMNIROUTE_CHAT_VIRTUAL_LANES بر این بازنویسی داشبورد اولویت دارد؛ تغییرات پس از راهاندازی مجدد سرور اعمال میشوند.                                                                                                                                                                                                                                                                                                                                                                                      |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false` |                | شناسههای آینهای <gateway-alias>/<model> را در /v1/models برای مدلهایی اعلام میکند که مالک اصلی آنها اعتبارنامهٔ فعالی ندارد، اما یک دروازهٔ عبوری با اعتبارنامهٔ فعال آنها را مسیریابی میکند. هشدار: در صورت فعالسازی سراسری، برای همهٔ کلاینتها ورودیهایی به کاتالوگ اضافه میکند.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false` |                | تشخیص موجودی را برای گرههای سازگار با تجمیعکنندههای New-API / One-API / Sub2API فعال میکند. در صورت فعال بودن، گرههای سازگاری که پرچم تجمیعکننده برایشان تنظیم شده است، موجودی خود را در داشبورد و مسیریابیِ پیشبررسی سهمیه گزارش میکنند.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false` |                | فراخوانیهای غیرجریانی ابزارهای تحت مالکیت سرور را تا زمانی ادامه میدهد که مدل پاسخی قابلاستفاده برای کلاینت برگرداند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false` |                | آمار جستجو و جستجوهای اخیر فقط ارائهدهندگانی را محاسبه میکنند که همچنان اتصال فعالی دارند (ارائهدهندگان بدون کلید مانند duckduckgo-free همیشه محاسبه میشوند). در حالت غیرفعال، همهٔ ردیفهای جستجوی نگهداریشده که دارای شناسهٔ ارائهدهنده هستند حفظ میشوند.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false` |                | صفحات ارائهدهندگان در داشبورد: نشان «رایگان» را فقط بر اساس سیگنالهایی نمایش میدهد که ارائهدهنده به آنها پایبند است؛ روش ابتکاری مبتنی بر نام نمایشی، فیلدهای رایگانِ غیربولین و پسوندهای :free را برای ارائهدهندگان ثبتشدهای که سطح رایگان مستندی ندارند کنار میگذارد. در حالت غیرفعال، قاعدهٔ تاریخی نمایش نشان حفظ میشود.                                                                                                                                                                                                                                                                                                                                                           |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false` |                | در پاسخهای تجمیعشدهٔ 429/503 مربوط به عدم دسترسپذیری، هنگامی که هیچ زمان مشخصی برای تلاش مجدد در آینده معلوم نیست، `Retry-After` را حذف میکند (بهجای مقدار مصنوعی 1 ثانیه)، `error.retry_after_provenance` (`signal` \| `none`) را اضافه میکند و به مسیرهای تخلیهٔ ترکیبی اجازه میدهد راهنمای متنیِ تلاش مجدد را از بدنههای JSON و متن سادهٔ بالادستی بخوانند. این فیلد فقط در پاسخهایی ظاهر میشود که توسط `unavailableResponse()` ساخته شدهاند؛ سایر بدنههای 429/503 بدون تغییر باقی میمانند.                                                                                                                                                                                         |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false` |                | هنگامی که یک مقصد ترکیبی `priority` که فقط در صورت اتمام سهمیه برای استفادهٔ پشتیبان علامتگذاری شده است، به دلیلی که بهطور اثباتپذیر مرتبط با سهمیه نیست (باز بودن قطعکنندهٔ مدار ارائهدهنده، رد کردن پیشبینانه بر اساس تأخیر) ترکیب را متوقف میکند، بهجای 503 که ظاهری مرتبط با سهمیه دارد، پاسخ 502 میدهد. توقفهای ناشی از قفلشدن، دورهٔ انتظار، عدم دسترسپذیری، اتمام منابع و سقف همزمانی همچنان 503 را حفظ میکنند.                                                                                                                                                                                                                                                                 |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false` |                | یک پاسخ سادهٔ 401 از Mistral (`{"detail":"Unauthorized"}`، بدون سیگنال صریح احراز هویت) برای یک کلید لغوشده و سهمیهٔ تمامشده یکسان است. در صورت فعال بودن، بهجای پارک کردن اتصال با وضعیت `expired`، آن را وارد دورهٔ انتظار میکند؛ این کار برای هر اتصال حداکثر 3 بار در ساعت انجام میشود و مورد بعدی اتصال را پارک میکند، بنابراین یک کلید لغوشده همچنان در نهایت به وضعیت پایدار میرسد. بهطور پیشفرض غیرفعال است: هر پاسخ سادهٔ 401 از Mistral مانند گذشته اتصال را پارک میکند.                                                                                                                                                                                                     |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false` |                | کاتالوگ زندهٔ مدلهای xAI را برای اتصالهای `xai-oauth` با استفاده از توکن حامل OAuth از `https://api.x.ai/v1/models` دریافت میکند، بهجای آنکه از دادهٔ اولیهٔ ثابت و منجمد استفاده کند. بهطور پیشفرض غیرفعال است: `xai-oauth` بدون تغییر به ارائهٔ دادهٔ اولیهٔ ثابت ادامه میدهد. در صورت بروز هرگونه خطای تفکیک، فرایند کشف به دادهٔ اولیه بازمیگردد (تأیید نشده است که آیا x.ai توکن حامل OAuth را در این نقطهٔ پایانی میپذیرد یا خیر).                                                                                                                                                                                                                                               |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false` |                | به پاکسازی خودکار اجازه میدهد کارهای Batch API نهاییشده (تکمیلشده/ناموفق/لغوشده/منقضیشده) قدیمیتر از `OMNIROUTE_BATCH_RETENTION_DAYS` را همراه با نقاط وارسی هر خط آنها حذف کند و محتوای BLOB فایلهای بارگذاریشدهای را که از `expires_at` خود گذشتهاند پاک کند. بهطور پیشفرض غیرفعال است: تا زمانی که اپراتور آن را فعال نکند، هر نصب موجود این دادهها را دقیقاً مانند قبل نگه میدارد. مسیر `DELETE /api/v1/batches/delete-completed` که توسط اپراتور فعال میشود، در هر دو حالت بدون تأثیر باقی میماند — این مسیر یک قرارداد API عمومی، مستقل و بدون شرط است.                                                                                                                          |
| `ANTIGRAVITY_ACCOUNT_LEASE_ENABLED`         | boolean | `false` |                | حساب Antigravity انتخابشده را برای چرخهٔ حیات جریانیِ درخواستی که آن را انتخاب کرده است رزرو میکند تا تلاش مجدد همزمان یا واگذاری اعتبارنامه نتواند حسابی را که پیشتر به یک جریان در حال اجرا اختصاص یافته است دوباره انتخاب کند. دامنهٔ رزرو به (اتصال، مدل بالادستی قابل فراخوانی) محدود است؛ بنابراین یک حساب همچنان میتواند همزمان به دو مدل متفاوت سرویس دهد. هنگامی که همهٔ حسابهای واجد شرایط از قبل برای آن مدل اجاره شده باشند، درخواست بهجای تحمیل بار بیشتر بر یک حساب مشغول، پاسخ ساختیافتهٔ 503 با کد `antigravity_pool_busy` و مقدار محدودشدهٔ `Retry-After` را برمیگرداند. بهطور پیشفرض غیرفعال است: انتخاب حساب دقیقاً مانند قبل باقی میماند و هیچ رزروی انجام نمیشود. |

### CLI (5)

| کلید                                  | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                       |
| ------------------------------------- | ------- | ------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | boolean | `false` | ✓              | حالت سازگاری را برای همهٔ کلاینتهای CLI فعال میکند.                                                                                                                                                                           |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false` |                | لایهٔ سازگاری نام مستعار مدل را فعال میکند.                                                                                                                                                                                   |
| `PRICING_SYNC_ENABLED`                | boolean | `false` |                | همگامسازی خودکار دادههای قیمتگذاری را فعال میکند (همچنین به متغیر محیطی `PRICING_SYNC_ENABLED` نیاز دارد).                                                                                                                    |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false` |                | پس از همگامسازی مدل ارائهدهنده، فایلهای نمایهٔ ~/.codex/*.config.toml را بهطور خودکار از کاتالوگ زنده (باز)نویسی میکند. هرگز پیکربندی فعال/پیشفرض Codex را تغییر نمیدهد. بهطور پیشفرض غیرفعال است.                            |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false` |                | پس از همگامسازی مدل ارائهدهنده، نمایههای Claude Code در ~/.claude/profiles/<name>/settings.json را بهطور خودکار از کاتالوگ زنده (باز)نویسی میکند. هرگز پیکربندی فعال/پیشفرض Claude را تغییر نمیدهد. بهطور پیشفرض غیرفعال است. |

### سلامت (5)

| کلید                                      | نوع  | پیشفرض  | توضیحات                                                                                                                                                                                                                                                                              |
| ----------------------------------------- | ---- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | بولی | `false` | نقطه پایانی بررسی سلامت نمونه محلی را غیرفعال میکند.                                                                                                                                                                                                                                 |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | بولی | `false` | بررسی سلامت اعتبارسنجی توکن را غیرفعال میکند.                                                                                                                                                                                                                                        |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | بولی | `false` | دسترسی به شبکه را در محیط سندباکس مهارتها فعال میکند.                                                                                                                                                                                                                                |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | بولی | `false` | در پیمایش سلامت پروکسی، اگر هدف یک کاوش را رد کند (401/403/429)، شمار شکستهای متوالی پروکسی بازنشانی میشود. بهطور پیشفرض غیرفعال است: رد شدن خنثی باقی میماند (#10654). پاسخ 5xx در هر دو حالت نامشخص باقی میماند؛ رد شدن هرگز باعث حذف، غیرفعالسازی یا فعالسازی مجدد پروکسی نمیشود. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | بولی | `false` | بررسی یکپارچگی/سلامت DB هنگام راهاندازی را پس از آنکه سرور پذیرش درخواستها را آغاز کرد (از طریق `setImmediate`) اجرا میکند، بهجای اینکه راهاندازی را تا زمان تکمیل آن مسدود کند (#13717). بهطور پیشفرض غیرفعال است: راهاندازی دقیقاً مانند قبل از این PR مسدود میشود.                |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` و نام مستعار قدیمی آن،
> `INJECTION_GUARD_BLOCK_THRESHOLD`، حالت `block` مربوط به
> `INJECTION_GUARD_MODE` را تنظیم میکنند، اما متغیرهای محیطی معمولی هستند که توسط
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts)
> خوانده میشوند و پرچم ویژگی نیستند: نه بازنویسی DB دارند و نه کلید تغییر وضعیت در داشبورد. بنگرید به
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> ستون `Restart` پرچمهایی را مشخص میکند که دارای `requiresRestart: true` هستند — مقدار
> فوراً ذخیره میشود، اما تنها پس از بارگذاری مجدد فرایند اثر میگذارد. پرچمهای
> enum هر مقداری خارج از مجموعه مجاز خود را رد میکنند (اعتبارسنجی در سمت سرور، هم در
> `setFeatureFlagOverride()` و هم در کنترلکننده REST برای `PUT` انجام میشود).

---

## تغییر وضعیت پرچمها

### داشبورد

به **داشبورد ← تنظیمات ← پرچمهای قابلیت**
(`/dashboard/settings/feature-flags`) بروید. شبکه
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
از موارد زیر پشتیبانی میکند:

- **جستجو** بر اساس کلید یا توضیحات، و **فیلتر** بر اساس دستهبندی (بهعلاوهٔ نمای مصنوعی
  **نیازمند راهاندازی مجدد**).
- یک **کلید تغییر وضعیت** برای پرچمهای بولی و یک **فهرست کشویی** برای پرچمهای enum
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- یک **نشان منبع** برای هر پرچم — `DB`، `ENV` یا `DEF` — که نشان میدهد
  مقدار مؤثر از کجا آمده است.
- یک دکمهٔ **بازنشانی** (که فقط برای پرچمهای دارای منبع `DB` نمایش داده میشود) برای حذف مقدار بازنویسیشده،
  و یک دکمهٔ **بازنشانی همهٔ مقادیر بازنویسیشده** در پایین صفحه.
- هنگام تغییر پرچمی با ویژگی `requiresRestart`، یک بنر **راهاندازی مجدد سرور** نمایش داده میشود.

### REST API

همهٔ عملیات از طریق یک مسیر واحد انجام میشوند:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
هر متد به یک نشست احرازهویتشدهٔ داشبورد نیاز دارد (در غیر این صورت `401`).

#### `GET /api/settings/feature-flags`

همهٔ پرچمها را همراه با مقدار مؤثر، منبع و خلاصهای از آنها برمیگرداند.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "نیاز به کلید API",
      "description": "برای همهٔ درخواستهای ورودی به یک کلید API نیاز باشد",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... همهٔ 74 پرچم
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

یک مقدار بازنویسیشده را تنظیم یا حذف میکند. بدنه: `{ key: string; value?: string }`.
حذف `value` باعث حذف مقدار بازنویسیشده میشود (و مقدار env / پیشفرض را بازیابی میکند).

```bash
# تنظیم یک مقدار بازنویسیشده در DB
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# حذف مقدار بازنویسیشده (بدون "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

پاسخ شامل `effectiveValue`/`source` جدید، `previousValue`/
`previousSource` و `requiresRestart` است. کلیدهای ناشناخته و مقادیر enum
خارج از محدوده با `400` رد میشوند.

#### `DELETE /api/settings/feature-flags`

همهٔ مقادیر بازنویسیشدهٔ DB را بهصورت همزمان پاک میکند و مقدار هر پرچم را به مقدار env / پیشفرض
آن بازمیگرداند. `{ cleared: <count>, message: "..." }` را برمیگرداند.

> [!NOTE]
> پرچمهایی که `requiresRestart: true` دارند، فقط پس از بارگذاری مجدد فرایند اعمال میشوند.
> جریان راهاندازی مجدد داشبورد، `POST /api/restart` را فراخوانی میکند و سپس
> `GET /api/health/ping` را تا زمانی که سرور دوباره در دسترس قرار گیرد، بررسی میکند.

---

## مسیر جایگزین اضطراری بودجه

`OMNIROUTE_EMERGENCY_FALLBACK` (دستهبندی `runtime`، مقدار پیشفرض `true`) مسیر
جایگزین رایگان اضطراری را در
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts)
کنترل میکند. هنگامی که فعال باشد، درخواستهایی که بودجهٔ خود را تمام میکنند، بهجای شکست کامل
به یک ارائهدهنده/مدل جایگزین رایگان هدایت میشوند. برای غیرفعال کردن این رفتار و شکست خوردن درخواستهایی
که بودجهشان تمام شده است، آن را — از طریق کلید تغییر وضعیت داشبورد، یک مقدار بازنویسیشده در DB یا متغیر محیطی
`OMNIROUTE_EMERGENCY_FALLBACK` — روی `false` (یا `0`) تنظیم کنید.
(در PRهای #3741 / #3752 بهصورت یک کلید تغییر وضعیت در داشبورد ارائه شده است.)

---

## همچنین ببینید

- [مرجع متغیرهای محیطی](./ENVIRONMENT.md) — بیشتر پرچمها دارای یک متغیر محیطی
  همنام هستند که در آنجا مستند شده است (مقدار بازنویسیشده در DB بر آن
  اولویت دارد).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — منبع معتبر برای همهٔ پرچمها.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — منطق تعیین مقدار (`resolveFeatureFlag`، `isFeatureFlagEnabled`،
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — ذخیرهسازی
  پایدار مقادیر بازنویسیشده در DB، در فضای نام `feature_flags` از جدول `key_value`.
