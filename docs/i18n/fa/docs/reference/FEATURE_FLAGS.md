# Feature Flags (فارسی)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇧🇦 [bs](../../../bs/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇷🇺 [ru](../../../ru/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> کلیدهای runtime که رفتار OmniRoute را **بدون استقرار مجدد** تغییر میدهند.
> تمام فلگهای فهرستشده در اینجا در
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> تعریف شدهاند — منبع واحد حقیقت. داشبورد و REST API هر دو از
> آن فایل میخوانند، بنابراین جدول زیر بهگونهای تولید شده است که دقیقاً بهصورت 1:1 با آن مطابقت داشته باشد.

---

## فلگهای قابلیت چیستند

فلگ قابلیت یک کلید نامگذاریشده (از نوع boolean یا enum) است که مقدار آن را میتوان در
runtime تغییر داد و در پایگاه داده ذخیره کرد، بدون اینکه نیاز به استقرار مجدد فرایند باشد. هر
فلگ با یک `FeatureFlagDefinition` توصیف میشود که شامل `key`، `label`،
`description`، `category`، `defaultValue`، `type` و راهنمای `requiresRestart` است.

### ترتیب تعیین مقدار

**مقدار مؤثر** یک فلگ توسط
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) و با ترتیب
اولویت زیر تعیین میشود (بالاترین اولویت برنده است):

1. **بازنویسی DB** — مقداری که در جدول `key_value` و در فضای نام
   `feature_flags` ذخیره شده است (از طریق داشبورد یا REST API تنظیم میشود).
2. **متغیر محیطی** — `process.env[<KEY>]`، در صورتی که تنظیم شده و خالی نباشد.
3. **مقدار پیشفرض تعریف** — مقدار `defaultValue` از `featureFlagDefinitions.ts`.

یک فلگ boolean زمانی **فعال** در نظر گرفته میشود که مقدار مؤثر آن `"true"`،
`"1"` یا `"yes"` باشد (`isFeatureFlagEnabled()` را ببینید).

> [!NOTE]
> بیشتر فلگها همچنین یک متغیر محیطی متناظر با **همان نام** دارند
> که در [`ENVIRONMENT.md`](./ENVIRONMENT.md) مستند شده است. بازنویسی DB فلگ
> نسبت به آن متغیر محیطی اولویت دارد. فلگی با
> `requiresRestart: true` بلافاصله ذخیره میشود، اما فقط هنگام راهاندازی
> فرایند دوباره خوانده میشود — تغییر آن، بنر **«راهاندازی مجدد سرور»** را در داشبورد نمایش میدهد.

---

## کاتالوگ پرچمها

77 پرچم در 6 دسته. **پیشفرض** تعریف پیشفرض است — مقداری که زمانی استفاده میشود که نه یک بازنویسی پایگاه داده و نه یک متغیر محیطی وجود داشته باشد.

### امنیت (10)

| کلید                                    | نوع     | پیشفرض   | توضیحات                                                                                                                                                                                                                                               |
| :-------------------------------------- | :------ | :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | boolean | `false`  | برای تمام درخواستهای ورودی، یک کلید API الزامی است.                                                                                                                                                                                                   |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`   | فعال کردن پاکسازی ورودی برای تمام درخواستها.                                                                                                                                                                                                          |
| `INJECTION_GUARD_MODE`                  | enum    | `off`    | حالت محافظت در برابر تزریق پرامپت. مقادیر: `off`, `warn`, `block`, `redact`.                                                                                                                                                                          |
| `PII_REDACTION_ENABLED`                 | boolean | `false`  | حذف اطلاعات شناسایی شخصی (PII) از درخواستها (مستقل از `INPUT_SANITIZER_MODE`).                                                                                                                                                                        |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`  | پاکسازی اطلاعات شناسایی شخصی (PII) از پاسخهای ارائهدهنده.                                                                                                                                                                                             |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact` | حالت برای پاکسازی پاسخ PII. مقادیر: `redact`, `warn`, `block`, `off`.                                                                                                                                                                                 |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`   | مسدود کردن درخواستهای خروجی به محدودههای IP خصوصی/داخلی.                                                                                                                                                                                              |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`  | به کاربران احراز هویت شده داشبورد اجازه میدهد تا کلیدهای API ذخیره شده را به جای دیدن فقط مقادیر پوشانده شده، آشکار کنند.                                                                                                                             |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`  | شامل کردن پیشوند حساب در خطوط لاگ AUTH (مثلاً "استفاده از حساب <provider>: abc12345..."). به طور پیشفرض غیرفعال است تا شناسههای حساب از لاگهای فرآیند مشترک/چندمستاجری حذف شوند. مستقل از حالت اشکالزدایی؛ تغییر حالت اشکالزدایی این را آشکار نمیکند. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`  | هنگامی که OIDC فعال است، ورود با رمز عبور را غیرفعال کنید تا کاربران فقط بتوانند از طریق OIDC Single Sign-On احراز هویت کنند. هنگامی که غیرفعال است (پیشفرض)، هم ورود با رمز عبور و هم OIDC در دسترس هستند.                                           |

### شبکه (19)

| Key                                             | Type    | Default | Restart | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| :---------------------------------------------- | :------ | :------ | :------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false` | ✓       | حالت پنهان اثر انگشت TLS را فعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false` |         | به مسیرهای /v1/audio/* اجازه میدهد تا از گرههای ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشوند، استفاده کنند. به طور پیشفرض غیرفعال است — مسیریابی صدا به یک میزبان راه دور هویت خروجی را تغییر میدهد و باید یک تصمیم صریح اپراتور باشد. گرههای لوپبک همیشه مجاز و بدون تأثیر هستند.                                                                                                                                                              |
| `RERANK_REMOTE_PROVIDER_NODES`                  | boolean | `false` |         | به POST /v1/rerank (و مرحله rerank لوپبک موتور حافظه) اجازه میدهد تا از گرههای ارائهدهنده سازگار با OpenAI که خارج از localhost میزبانی میشوند، استفاده کنند. به طور پیشفرض غیرفعال است — مسیریابی به یک میزبان راه دور هویت خروجی را تغییر میدهد و باید یک تصمیم صریح اپراتور باشد. گرههای لوپبک همیشه مجاز هستند؛ گرههای راه دور نیز باید از خطمشی URL خروجی ارائهدهنده عبور کنند.                                                                               |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false` |         | هنگامی که هیچ پروکسی به یک اتصال اختصاص داده نشده است، اولین پروکسی فعال را از رجیستری به صورت خودکار انتخاب کنید. به طور پیشفرض غیرفعال است (در غیر این صورت هر پروکسی رجیستری به یک جایگزین جهانی تبدیل میشود — #3332).                                                                                                                                                                                                                                          |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false` |         | به جریانهای اعتبارسنجی OAuth و ارائهدهنده اجازه میدهد تا یک پروکسی پینشده را دور بزنند و مستقیماً متصل شوند، زمانی که بررسیهای اولیه دسترسی پروکسی با شکست مواجه میشوند. به طور پیشفرض غیرفعال است زیرا این میتواند IP خروجی را تغییر دهد.                                                                                                                                                                                                                         |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`  |         | در صورت بروز استثنای شبکه (تایماوت، اتصال رد شده/بازنشانی شده) برای یک اجراکننده چرخش چندحسابی، هنگامی که حساب ناموفق پروکسی اختصاصی ندارد، یک دوره خنکسازی کوتاه اعمال کنید و سایر حسابهای بدون پروکسی را برای بقیه درخواست نادیده بگیرید به جای تلاش مجدد برای هر یک. به طور پیشفرض فعال است (ایمن: بدون تغییر IP خروجی، فقط خطر تأخیر/خنکسازی را در حسابهای با خروجی مشترک کاهش میدهد). برای بازیابی انتشار فوری در اولین خطای بدون پروکسی، آن را غیرفعال کنید. |
| `ROTATION_ATTRIBUTION`                          | boolean | `false` |         | چرخش Opencode ثبت میکند که کدام حساب سرویس داده یا نادیده گرفته شده است (فقط شناسههای ماسک شده، هرگز شناسههای کامل حساب) و ورودیهای لاگ پروکسی را به درخواست مربوطه پیوند میدهد، تا اپراتور بتواند حسابهای نادیده گرفته شده را از حسابهای استفاده نشده تشخیص دهد. به طور پیشفرض غیرفعال است.                                                                                                                                                                       |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false` |         | استخرهای پروکسی و چرخش opencode به ازای هر حساب، از ارائه مجدد پروکسی که به تازگی با شکست مواجه شده است (پروب TCP رد شده، یا کد 429 از طریق آن دریافت شده) برای یک دوره به ازای هر فرآیند که با هر تکرار دو برابر میشود، تا یک سقف مشخص، جلوگیری میکنند. هیچ وضعیت پروکسی نوشته نمیشود؛ با کنار گذاشتن هر کاندید، انتخاب بدون تغییر باقی میماند. به طور پیشفرض غیرفعال است.                                                                                        |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false` |         | در زیر یک استخر پروکسی در داشبورد، نشان میدهد که چند IP خروجی مشاهده شده در 24 ساعت گذشته به اعضای آن سرویس دادهاند و چند اتصال از آنها استفاده کردهاند. فقط خواندنی، از لاگ پروکسی محاسبه میشود، هرگز برای مسیریابی استفاده نمیشود. به طور پیشفرض غیرفعال است.                                                                                                                                                                                                    |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false` |         | برای اجرای OpenCode، اولین بایت بدنه یک پاسخ جریانی Responses را مشاهده کنید (پنجره: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`، پیشفرض `15000`). یک جریان Responses با کد 2xx که پس از این پنجره ساکت میماند، به عنوان متوقفشده (stalled) تلقی میشود: حساب خنک میشود و درخواست یک بار به حساب بعدی چرخش میکند؛ توقف دوم به سرعت با شکست مواجه میشود. به طور پیشفرض خاموش است: جریانهای متوقفشده تا زمان انقضای آمادگی جریان، انتظار امروز را حفظ میکنند.                   |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false` |         | اجرای OpenCode: در صورت دریافت کد 403/451 که حاوی رد `user_blocked` باشد (نه جغرافیایی، نه رد اثر انگشت Cloudflare)، حساب رد شده را خنک کرده و حداکثر یک بار در هر درخواست به حساب بعدی چرخش دهید؛ رد دوم بدون علامت موفقیت، همانطور که هست بازگردانده میشود. به طور پیشفرض خاموش است: دور زدن یک بلاک کاربری بالادستی میتواند شبیه به فرار به نظر برسد و پرچم را در سراسر ناوگان پخش کند.                                                                         |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false` |         | چرخش OpenCode: پس از دو شکست موقت متوالی بالادستی (5xx یا یک 400 خالی)، قبل از حساب بعدی مکث کنید — 1.5 ثانیه دو برابر شدن به ازای هر شکست بیشتر، با سقف 6 ثانیه در هر مکث و 10 ثانیه در هر درخواست، که در صورت قطع ارتباط مشتری نادیده گرفته میشود؛ بدنه شکستخورده قبل از انتظار آزاد میشود. به طور پیشفرض خاموش است: failover فوری باقی میماند.                                                                                                                  |
| `OPENCODE_PARK_AND_RESUME`                      | boolean | `false` |         | چرخش OpenCode: درخواست را پس از 429های موقت مکرر (یا یک نشانگر جدید فشار-استخر) با یک ضربان قلب پارک کنید، سپس به جای پخش کردن کل ناوگان، یک بخش محدود از حداکثر 3 حساب متوالی را دوباره پخش کنید. به طور پیشفرض خاموش است: هر 429 دقیقاً مانند قبل به حساب بعدی چرخش میکند.                                                                                                                                                                                       |
| `FLUSH_EMPTY_RETRY_ENABLED`                     | boolean | `false` |         | در نوبتهای جریانی ترجمهشده، هنگامی که نوبت بالادستی محتوای قابل استفادهای ندارد (تکمیل فقط-استدلالی یا صفر تکه ارزشمند)، قبل از اینکه چیزی به مشتری نمایش داده شود، تلاشهای مجدد محدود را از طریق مسیر اعتبارنامه عادی (تا `STREAM_RECOVERY.EMPTY_TURN_RETRY_MAX`) صادر کنید. به طور پیشفرض خاموش است: نوبتهای خالی رفتار فعلی را حفظ میکنند (200 خالی یا 502 با محتوای خالی).                                                                                     |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false` |         | چرخش OpenCode: موج حساب را در اولین 429 که به عنوان یک محدودیت نرخ واقعی طبقهبندی شده است (قابل تجزیه `Retry-After`، یا بدنهای که محدودیت نرخ/مصرف را نام میبرد) متوقف کنید و آن 429 بالادستی را بدون تغییر بازگردانید. 429های طبقهبندی نشده به چرخش ادامه میدهند. به طور پیشفرض خاموش است: رده رایگان به ازای هر IP خروجی محدود است (#9611)، بنابراین هر 429 چرخش میکند و یک موج خسته شده آخرین 429 بالادستی را بازمیگرداند.                                      |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false` | ✓       | غیرفعال کردن تأیید گواهی TLS برای پروکسی MITM. **خطر.**                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false` |         | اجازه دادن به URLهای ارائهدهنده که به شبکههای خصوصی/داخلی اشاره میکنند.                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`  |         | اجازه افزودن/اعتبارسنجی ارائهدهندگان در آدرسهای محلی/خصوصی (127.0.0.1، localhost، LAN). به طور پیشفرض روشن است (محلی-اول)؛ برای مسدودسازی سختگیرانه فقط عمومی غیرفعال کنید. فراداده ابری مسدود باقی میماند.                                                                                                                                                                                                                                                        |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false` | ✓       | فعال کردن حالت ارائهدهنده سازگار با Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                   |

### سیاستها (5)

| Key                             | Type    | Default    | Description                                                                                                                                                                                                             |
| :------------------------------ | :------ | :--------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled` | حالت اعمال سیاست استفاده از ابزار. مقادیر: `disabled`، `warn`، `block`.                                                                                                                                                 |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`    | فعالسازی خودکار محدودیت نرخ بر اساس الگوهای استفاده.                                                                                                                                                                    |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`    | نادیده گرفتن بررسی پنجره زمینه محلی / حداکثر توکن ورودی OmniRoute برای درخواستهای مستقیم تکمدلی. محدودیتهای بالادستی همچنان اعمال میشوند.                                                                               |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`    | رد درخواستها قبل از ارسال زمانی که مدل هدف فاقد قابلیتهای مورد نیاز (بینایی، ابزارها، خروجی ساختاریافته، پنجره زمینه) باشد. از درخواستهای مستقیم تکارائهدهنده که فیلتر سازگاری لایه ترکیبی را دور میزنند، محافظت میکند. |
| `RADAR_ENABLED`                 | boolean | `false`    | فعالسازی ماژول OmniRoute Radar (صفحات فید کاتالوگ و همگامسازی). به طور پیشفرض غیرفعال است؛ فعالسازی فقط رابط کاربری را باز میکند — همگامسازی دادهها یک انتخاب جداگانه باقی میماند.                                      |

### زمان اجرا (33)

| کلید                                        | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------- | ------- | ------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | boolean | `true`  |                | تولید و تزریق خلاصههای گفتگو زمانی که مسیریابی ترکیبی (combo routing) مدلها را تغییر میدهد. برای در نظر گرفتن تغییرات مدل به صورت مستقل و جلوگیری از درخواستهای انتقال پسزمینه برای تمام ترکیبهای موجود و آینده، این گزینه را غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | boolean | `true`  |                | حذف موارد خروجی فاز تفسیر داخلی از استریمهای عبوری Responses API قبل از ارسال به کلاینتها. برای دریافت تفسیر خام بالادستی، این گزینه را غیرفعال کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | boolean | `true`  |                | اعمال محدودیتهای دامنه (scope) بر دسترسی ابزار MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | boolean | `false` |                | فشردهسازی توضیحات ابزار MCP برای کاهش مصرف توکن.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | boolean | `false` |                | فعالسازی پردازش وظایف پسزمینه در زمان اجرا.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | boolean | `false` | ✓              | غیرفعالسازی تمام سرویسهای پسزمینه (بهروزرسانی سهمیه، همگامسازی و غیره).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false` |                | فیلترهای RTK در سطح پروژه را بدون اعتبارسنجی مورد اعتماد قرار دهید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`  | ✓              | سرور WebSocket داشبورد بلادرنگ را هنگام وارد کردن (به طور پیشفرض پورت 20132) راهاندازی کنید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`  |                | به Codex اجازه دهید از انتقال Responses-over-WebSocket استفاده کند. در صورت غیرفعال بودن، Codex به پاسخهای HTTP بازمیگردد.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`  |                | به Codex اجازه دهید از انتقال JSON-RPC WebSocket سرور برنامه محلی (codexTransport=app-server) استفاده کند. در صورت غیرفعال بودن، اتصالاتی که app-server را انتخاب کردهاند، به سایر انتقالهای Codex بازمیگردند.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`  |                | درخواستهای با بودجه تمام شده را به ارائهدهنده/مدل جایگزین اضطراری رایگان هدایت کنید. (به [جایگزین اضطراری بودجه](#emergency-budget-fallback) در زیر مراجعه کنید.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false` |                | فعال کردن تلاش مجدد شفاف و زودهنگام برای جریانهای SSE بالادستی قطع شده قبل از رسیدن هر بایت پاسخ به کلاینت.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false` |                | اجازه دادن به بازیابی جریان برای درخواست مجدد و اتصال یک پاسخ پس از اینکه بایتها قبلاً به کلاینت رسیدهاند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false` |                | ادامه میانی جریان را برای فراخوانی ابزار ایمن کنید: هرگز جریانی را که یک فراخوانی ابزار از آن صادر شده است (در حال اجرا یا قبلاً با finish_reason tool_calls به پایان رسیده است) از سر نگیرید، و پس از یک ادامه خالی به جای صرف کل بودجه، آن را ببندید. خاموش: رفتار انتشار.                                                                                                                                                                                                                                                                                                                                                                                      |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false` |                | یک بار به یک اتصال خواهر/برادر (sibling connection) سوییچ میکند (fail over) زمانی که یک جریان SSE قبل از انتشار هر فریم مفیدی بسته میشود و تلاش مجدد محدود شده در همان اتصال به پایان رسیده است؛ در صورت عدم وجود خواهر/برادر قابل استفاده، خطای اصلی `STREAM_EARLY_EOF` 502 برگردانده میشود. به طور پیشفرض خاموش است: EOF زودهنگام پس از تلاش مجدد در همان اتصال، نهایی باقی میماند.                                                                                                                                                                                                                                                                             |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`  |                | فیلدهای نام دوستانه برای نمایش را در پاسخهای `/v1/models` قرار میدهد. برای کلاینتهایی که فقط شناسههای مدل را انتظار دارند، غیرفعال شود.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`  |                | نحوه پیشوندگذاری شناسههای مدل را در /v1/models کنترل میکند. 'dual' (پیشفرض) هم پیشوندهای مستعار (alias) و هم پیشوندهای شناسه ارائهدهنده (canonical provider-id) را برای سازگاری با نسخههای قبلی منتشر میکند. 'alias' فقط پیشوند مستعار کوتاه را منتشر میکند (مثلاً ds-web/model، نه deepseek-web/model). 'canonical' فقط پیشوند کامل شناسه ارائهدهنده را منتشر میکند. مقادیر: `dual`, `alias`, `canonical`.                                                                                                                                                                                                                                                       |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`  |                | همگامسازی دورهای ELO جدول امتیازات Arena AI را برای رتبهبندی هوش مدل فعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false` |                | شناسههای آینهای `claude/<provider>/<model>` را در `/v1/models` تبلیغ میکند تا کشف مدل دروازه Claude Code مدلهای غیر-Claude را لیست کند. سطح جهانی دروازه سهسطحی (متغیر محیطی بر تنظیمات داشبورد ارجحیت دارد). به [پیکربندی Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker) مراجعه کنید.                                                                                                                                                                                                                                                                                                     |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`  |                | سوییچ اصلی برای مستعارهای دروازه no-think/<provider>/<model>. روشن (پیشفرض): /v1/models یک نوع بدون تفکر (no-thinking) را برای هر مدل Claude واجد شرایط و دارای قابلیت تفکر تبلیغ میکند، و یک شناسه no-think/ که در یک درخواست ارسال میشود، به مدل واقعی با سرکوب استدلال برمیگردد. خاموش: هیچ نوعی تبلیغ نمیشود و یک شناسه no-think/ مانند هر شناسه مدل ناشناخته دیگری رفتار میشود. ModelSpec.noThinkingAlias opt-in/opt-out به ازای هر مدل همچنان در صورت روشن بودن این گزینه اعمال میشود.                                                                                                                                                                      |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false` |                | تولید انواع سطح تفکر (مانند -low, -medium, -high) را در کاتالوگ /v1/models غیرفعال میکند.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false` | ✓              | خطوط پذیرش مجازی تطبیقی به ازای هر مستأجر را برای ارسال ارائهدهنده فعال میکند (#9654): افزایش ناگهانی بار یک مستأجر دیگر باعث خطای 503 برای مستأجر دیگر نمیشود. متغیر محیطی OMNIROUTE_CHAT_VIRTUAL_LANES بر این تنظیمات داشبورد ارجحیت دارد؛ تغییرات پس از راهاندازی مجدد سرور اعمال میشوند.                                                                                                                                                                                                                                                                                                                                                                      |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false` |                | شناسههای آینهای `<gateway-alias>/<model>` را در `/v1/models` برای مدلهایی که مالک اصلی آنها اعتبار فعال ندارد، اما یک دروازه عبوری با اعتبار فعال آنها را مسیریابی میکند، تبلیغ کنید. هشدار: در صورت فعالسازی جهانی، ورودیهای کاتالوگ را برای همه مشتریان اضافه میکند.                                                                                                                                                                                                                                                                                                                                                                                            |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false` |                | تشخیص تعادل را برای گرههای سازگار با تجمیعکننده New-API / One-API / Sub2API فعال کنید. هنگامی که فعال باشد، گرههای سازگار با پرچم تجمیعکننده، تعادل خود را در داشبورد و مسیریابی پیشپرواز سهمیه گزارش میدهند.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false` |                | فراخوانی ابزارهای غیرجریانی متعلق به سرور را تا زمانی که مدل یک پاسخ قابل استفاده برای مشتری برگرداند، ادامه دهید.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false` |                | آمار جستجو و جستجوهای اخیر فقط ارائهدهندگانی را شمارش میکنند که هنوز اتصال فعال دارند (ارائهدهندگان بدون کلید مانند duckduckgo-free همیشه شمارش میشوند). در حالت خاموش، هر ردیف جستجوی حفظ شده با شناسه ارائهدهنده حفظ میشود.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false` |                | صفحات ارائهدهنده داشبورد: نشان رایگان را فقط بر روی سیگنالهایی که ارائهدهنده رعایت میکند، نمایش دهید — اکتشافی نام نمایشی، فیلدهای رایگان غیربولی و پسوندهای `:free` را در ارائهدهندگان ثبتشده بدون رده رایگان مستند حذف میکند. در حالت خاموش، قانون نشان تاریخی حفظ میشود.                                                                                                                                                                                                                                                                                                                                                                                       |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false` |                | در پاسخهای تجمیعشده 429/503 غیرقابل دسترس، `Retry-After` را زمانی که زمان مشخصی برای تلاش مجدد در آینده مشخص نیست (به جای 1 ثانیه مصنوعی) حذف کنید، `error.retry_after_provenance` (`signal` \| `none`) را اضافه کنید، و اجازه دهید مسیرهای تخلیه ترکیبی، راهنماییهای تلاش مجدد متنی را از JSON و بدنه متنی ساده بالادستی بخوانند. این فیلد فقط در پاسخهایی که توسط `unavailableResponse()` ساخته شدهاند ظاهر میشود؛ سایر بدنههای 429/503 بدون تغییر باقی میمانند.                                                                                                                                                                                                |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false` |                | هنگامی که یک هدف ترکیبی `priority` که فقط در صورت اتمام سهمیه به عنوان بازگشتی علامتگذاری شده است، ترکیب را به دلیلی که به طور قطع سهمیه نیست (مدارشکن ارائهدهنده باز است، پرش پیشبینی تأخیر)، 502 را به جای 503 که شبیه سهمیه است، پاسخ دهید. قفلشدن، خنکسازی، عدم دسترسی، اتمام و توقفهای محدودیت همزمانی 503 را حفظ میکنند.                                                                                                                                                                                                                                                                                                                                    |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false` |                | یک 401 Mistral ساده (`{"detail":"Unauthorized"}`, بدون سیگنال احراز هویت صریح) برای یک کلید باطل شده و برای سهمیه تمام شده یکسان است. هنگامی که فعال باشد، اتصال را به جای پارک کردن آن به عنوان `expired`، حداکثر 3 بار در ساعت برای هر اتصال خنک میکند؛ بعدی آن را پارک میکند، بنابراین یک کلید باطل شده همچنان همگرا میشود. به طور پیشفرض خاموش است: هر 401 Mistral ساده اتصال را مانند قبل پارک میکند.                                                                                                                                                                                                                                                        |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false` |                | کاتالوگ مدل زنده xAI را برای اتصالات `xai-oauth` از `https://api.x.ai/v1/models` با استفاده از توکن حامل OAuth، به جای seed استاتیک ثابت، واکشی کنید. به طور پیشفرض خاموش است: `xai-oauth` همچنان seed استاتیک را بدون تغییر ارائه میدهد. در صورت بروز هرگونه خطای وضوح، کشف به seed بازمیگردد (تأیید نشده است که x.ai یک حامل OAuth را در این نقطه پایانی میپذیرد).                                                                                                                                                                                                                                                                                              |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false` |                | اجازه دهید پاکسازی خودکار، کارهای API دستهای ترمینال (تکمیلشده/ناموفق/لغوشده/منقضیشده) قدیمیتر از `OMNIROUTE_BATCH_RETENTION_DAYS` را به همراه نقاط بازرسی خط به خط آنها حذف کند و محتوای BLOB فایلهای آپلود شده را پس از `expires_at` خود پاک کند. به طور پیشفرض خاموش است: هر نصب موجود این دادهها را دقیقاً مانند قبل نگه میدارد تا زمانی که یک اپراتور آن را فعال کند. مسیر `DELETE /api/v1/batches/delete-completed` که توسط اپراتور فعال میشود، در هر صورت تحت تأثیر قرار نمیگیرد — این یک قرارداد API عمومی جداگانه و بدون قید و شرط است.                                                                                                                  |
| `ANTIGRAVITY_ACCOUNT_LEASE_ENABLED`         | boolean | `false` |                | حساب Antigravity انتخاب شده را برای چرخه عمر استریم درخواستی که آن را انتخاب کرده است، رزرو کنید، تا یک تلاش مجدد همزمان یا انتقال اعتبارنامه نتواند حسابی را که قبلاً به یک استریم در حال انجام اختصاص داده شده است، دوباره انتخاب کند. رزرو به (اتصال، مدل بالادستی قابل فراخوانی) محدود میشود، بنابراین یک حساب همچنان میتواند دو مدل مختلف را به طور همزمان ارائه دهد. هنگامی که هر حساب واجد شرایط قبلاً برای آن مدل اجاره شده باشد، درخواست یک 503 ساختاریافته `antigravity_pool_busy` با `Retry-After` محدود به جای انباشته شدن روی یک حساب مشغول، برمیگرداند. به طور پیشفرض خاموش است: انتخاب حساب دقیقاً مانند قبل باقی میماند و هیچ رزروی انجام نمیشود. |

### CLI (5)

| کلید                                  | نوع     | پیشفرض  | راهاندازی مجدد | توضیحات                                                                                                                                                                                                                          |
| :------------------------------------ | :------ | :------ | :------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | boolean | `false` | ✓              | حالت سازگاری را برای همه کلاینتهای CLI فعال کنید.                                                                                                                                                                                |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false` |                | لایه سازگاری نام مستعار مدل را فعال کنید.                                                                                                                                                                                        |
| `PRICING_SYNC_ENABLED`                | boolean | `false` |                | همگامسازی خودکار دادههای قیمتگذاری را فعال کنید (همچنین به متغیر محیطی `PRICING_SYNC_ENABLED` نیاز دارد).                                                                                                                        |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false` |                | پس از همگامسازی مدل ارائهدهنده، فایلهای پروفایل `~/.codex/*.config.toml` را به طور خودکار از کاتالوگ زنده بازنویسی کنید. هرگز پیکربندی فعال/پیشفرض Codex را تغییر نمیدهد. به طور پیشفرض خاموش است.                               |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false` |                | پس از همگامسازی مدل ارائهدهنده، فایلهای پروفایل Claude Code `~/.claude/profiles/<name>/settings.json` را به طور خودکار از کاتالوگ زنده بازنویسی کنید. هرگز پیکربندی فعال/پیشفرض Claude را تغییر نمیدهد. به طور پیشفرض خاموش است. |

### Health (5)

| کلید                                      | نوع     | پیشفرض  | توضیحات                                                                                                                                                                                                                                                                                           |
| ----------------------------------------- | ------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false` | غیرفعالسازی نقطه پایانی (endpoint) بررسی سلامت نمونه محلی.                                                                                                                                                                                                                                        |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false` | غیرفعالسازی بررسی سلامت اعتبارسنجی توکن.                                                                                                                                                                                                                                                          |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false` | فعالسازی دسترسی به شبکه در محیط sandbox مهارتها (skills).                                                                                                                                                                                                                                         |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false` | در پایش سلامت پروکسی، اگر هدف درخواست را رد کند (401/403/429)، توالی شکستهای متوالی پروکسی بازنشانی (reset) میشود. بهصورت پیشفرض غیرفعال است: رد درخواست خنثی باقی میماند (#10654). خطای 5xx در هر صورت نامشخص باقی میماند؛ رد درخواست هرگز باعث حذف، غیرفعالسازی یا فعالسازی مجدد پروکسی نمیشود. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false` | اجرای بررسی یکپارچگی/سلامت پایگاهداده در هنگام راهاندازی پس از اینکه سرور شروع به پذیرش درخواستها کرد (از طریق `setImmediate`)، به جای مسدود کردن راهاندازی تا زمان تکمیل آن (#13717). بهصورت پیشفرض غیرفعال است: راهاندازی دقیقاً مانند قبل از این PR مسدود میشود.                               |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` و نام مستعار قدیمی آن
> `INJECTION_GUARD_BLOCK_THRESHOLD` حالت `block` در
> `INJECTION_GUARD_MODE` را تنظیم میکنند، اما آنها متغیرهای محیطی معمولی هستند که توسط
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts) خوانده میشوند،
> نه پرچمهای ویژگی (feature flags): آنها هیچ جایگزینی (override) در پایگاهداده و هیچ سوییچی در داشبورد ندارند. به
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication) مراجعه کنید.

> [!NOTE]
> ستون `Restart` پرچمهایی را که دارای `requiresRestart: true` هستند مشخص میکند — مقدار
> بلافاصله ذخیره میشود اما فقط پس از بارگذاری مجدد فرآیند (process) اعمال میگردد. پرچمهای Enum
> هر مقداری خارج از مجموعه مجاز خود را رد میکنند (که در سمت سرور هم در
> `setFeatureFlagOverride()` و هم در هندلر `PUT` مربوط به REST اعتبارسنجی میشود).

---

## تغییر وضعیت پرچمها

### داشبورد

به **داشبورد ← تنظیمات ← پرچمهای ویژگی** (`/dashboard/settings/feature-flags`) بروید. گرید
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
از موارد زیر پشتیبانی میکند:

- **جستجو** بر اساس کلید یا توضیحات، و **فیلتر** بر اساس دستهبندی (به علاوه یک نمای ترکیبی **نیاز به راهاندازی مجدد**).
- یک **تغییر وضعیت (toggle)** برای پرچمهای بولی و یک **منوی کشویی (dropdown)** برای پرچمهای شمارشی
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- یک **نشان منبع** برای هر پرچم — `DB`، `ENV`، یا `DEF` — که نشان میدهد مقدار مؤثر از کجا آمده است.
- یک دکمه **بازنشانی (Reset)** (فقط برای پرچمهای با منبع `DB` نمایش داده میشود) برای حذف بازنویسی،
  و یک دکمه **بازنشانی همه بازنویسیها (Reset All Overrides)** در پایین.
- یک بنر **راهاندازی مجدد سرور (Restart Server)** هنگام تغییر پرچمی که `requiresRestart` است.

### API REST

تمام عملیات از طریق یک مسیر واحد انجام میشود:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
هر متد نیاز به یک نشست داشبورد احراز هویت شده دارد (در غیر این صورت `401`).

#### `GET /api/settings/feature-flags`

هر پرچم را با مقدار مؤثر، منبع و خلاصهای از آن برمیگرداند.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Require API Key",
      "description": "Require an API key for all incoming requests",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... همه ۷۷ پرچم
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

یک بازنویسی واحد را تنظیم یا حذف میکند. بدنه: `{ key: string; value?: string }`.
حذف `value` بازنویسی را حذف میکند (و مقدار محیطی / پیشفرض را بازیابی میکند).

```bash
# یک بازنویسی DB را تنظیم کنید
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# بازنویسی را حذف کنید (بدون "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

پاسخ، `effectiveValue`/`source` جدید، `previousValue`/
`previousSource` قبلی، و `requiresRestart` را بازتاب میدهد. کلیدهای ناشناخته و مقادیر شمارشی خارج از محدوده
با `400` رد میشوند.

#### `DELETE /api/settings/feature-flags`

**تمام** بازنویسیهای DB را یکباره پاک میکند و هر پرچم را به مقدار محیطی / پیشفرض خود بازمیگرداند. `{ cleared: <count>, message: "..." }` را برمیگرداند.

> [!NOTE]
> پرچمهایی که `requiresRestart: true` دارند، تنها پس از بارگذاری مجدد فرآیند اعمال میشوند.
> جریان راهاندازی مجدد داشبورد، `POST /api/restart` را فراخوانی میکند و سپس
> `GET /api/health/ping` را تا زمانی که سرور دوباره فعال شود، نظرسنجی میکند.

---

## مسیر جایگزین اضطراری بودجه

`OMNIROUTE_EMERGENCY_FALLBACK` (دستهبندی `runtime`، مقدار پیشفرض `true`) مسیر
جایگزین رایگان اضطراری را در
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts)
کنترل میکند. هنگامی که فعال باشد، درخواستهایی که بودجهٔ خود را تمام میکنند، بهجای شکست کامل
به یک ارائهدهنده/مدل جایگزین رایگان هدایت میشوند. برای غیرفعال کردن این رفتار و شکست خوردن درخواستهایی
که بودجهشان تمام شده است، آن را — از طریق کلید تغییر وضعیت داشبورد، یک مقدار بازنویسیشده در DB یا متغیر محیطی
`OMNIROUTE_EMERGENCY_FALLBACK` — روی `false` (یا `0`) تنظیم کنید.
(در PRهای #3741 / #3752 بهصورت یک کلید تغییر وضعیت در داشبورد ارائه شده است.)

---

## همچنین ببینید

- [مرجع متغیرهای محیطی](./ENVIRONMENT.md) — بیشتر پرچمها دارای یک متغیر محیطی
  همنام هستند که در آنجا مستند شده است (مقدار بازنویسیشده در DB بر آن
  اولویت دارد).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — منبع معتبر برای همهٔ پرچمها.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — منطق تعیین مقدار (`resolveFeatureFlag`، `isFeatureFlagEnabled`،
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — ذخیرهسازی
  پایدار مقادیر بازنویسیشده در DB، در فضای نام `feature_flags` از جدول `key_value`.
