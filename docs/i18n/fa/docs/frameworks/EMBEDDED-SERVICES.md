# Embedded Services (فارسی)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **نسخه:** v3.8.44
> **آخرین بهروزرسانی:** 2026-09-09
> **مخاطبان:** مهندسانی که سرویسهای تعبیهشده (9Router، CLIProxyAPI، Mux، Bifrost، open-wa) را اضافه، نگهداری یا اشکالزدایی میکنند.

سرویسهای تعبیهشده، ابزارهای پردازشی سایدکارِ نصبشده بهصورت محلی هستند که OmniRoute آنها را نصب و نظارت میکند و
بهعنوان مقصدهای مسیریابی درجهیک ارائه میدهد. برخلاف ارائهدهندگان خارجی (که از طریق اینترنت و
با استفاده از کلیدهای API در دسترس قرار میگیرند)، سرویسهای تعبیهشده روی همان دستگاهی اجرا میشوند که OmniRoute اجرا میشود و از طریق رابط loopback ارتباط برقرار میکنند.

---

## فهرست مطالب

1. [نمای کلی](#1-overview)
2. [معماری — ۴ لایه](#2-architecture--4-layers)
3. [ماشین حالت چرخهٔ حیات](#3-lifecycle-state-machine)
4. [مرجع API](#4-api-reference)
5. [امنیت](#5-security)
6. [افزودن یک سرویس تعبیهشدهٔ جدید](#6-adding-a-new-embedded-service)
7. [عیبیابی](#7-troubleshooting)
8. [پرسشهای متداول](#8-faq)

---

## 1. نمای کلی

### چرا سرویسهای تعبیهشده؟

شش سرویس تعبیه شدهاند:

| سرویس           | بسته npm                                    | پورت پیشفرض | هدف                                                                                                                                                                                              |
| --------------- | ------------------------------------------- | :---------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **9Router**     | `9router`                                   |    20130    | مسیریاب هوش مصنوعی که OmniRoute میتواند از آن بهعنوان یک ارائهدهنده فرعی استفاده کند. مدلها بهصورت `9router/{sub}/{model}` ارائه میشوند                                                          |
| **CLIProxyAPI** | فایل اجرایی منتشرشده در GitHub (`cliproxy`) |    8317     | آداپتور پراکسی محلی برای جریانهای احراز هویت Anthropic CLI. هنگامی که توکنهای OAuth منقضی میشوند، مسیریابی جایگزین را فراهم میکند                                                                |
| **Mux**         | `mux` (`mux server` بدون رابط گرافیکی)      |    8322     | دیمن محلی هماهنگسازی عاملها (coder/mux). فقط چرخه عمر آن مدیریت میشود — هدف مسیریابی نیست (هیچ پراکسی LLM انجام نمیدهد).                                                                         |
| **Bifrost**     | `@maximhq/bifrost`                          |    8080     | بکاند رله دروازه هوش مصنوعی مبتنی بر Go. هنگام اجرا، بهطور خودکار توسط مسیر رله (`/v1/relay/`) انتخاب میشود                                                                                      |
| **Dario**       | `@askalf/dario`                             |    3456     | پراکسی اشتراک Claude — جایگزین/پشتیبان CLIProxyAPI برای ترافیک با ساختار Claude Code؛ کلید تزریقشده به `DARIO_ADMIN_TOKEN` تبدیل میشود و دسترسی به صفحه کنترل OAuth در `/admin/*` را محدود میکند |
| **open-wa**     | `@open-wa/wa-automate`                      |    8323     | خودکارسازی WhatsApp Web (Chromium بدون رابط گرافیکی از طریق Puppeteer). فقط چرخه عمر آن مدیریت میشود — هدف مسیریابی نیست.                                                                        |

هر شش سرویس از مدل نظارتی یکسانی پیروی میکنند:

- OmniRoute آنها را در `DATA_DIR/services/{name}/` نصب میکند (جدا از `package.json` خود OmniRoute)
- OmniRoute آنها را بهعنوان فرایندهای فرزند راهاندازی و پایش میکند
- OmniRoute یک کلید API موقت را به محیط فرایند فرزند تزریق میکند و آن را بدون قطعی میچرخاند (در موارد قابلاعمال)
- همه مسیرهای مدیریتی (`/api/services/*`) **LOCAL_ONLY** هستند — فقط از طریق loopback قابلدسترسیاند (قانون قطعی شماره 17)

### تصمیمهای کلیدی (از طرح طراحی)

| تصمیم                               | مقدار                                                                                    |
| ----------------------------------- | ---------------------------------------------------------------------------------------- |
| دسترسی داشبورد به رابط بومی 9Router | پراکسی معکوس در `/dashboard/providers/services/9router/embed/*`                          |
| سازوکار نصب                         | `npm install {package}` از طریق `execFile` (بدون درونیابی پوسته)                         |
| حالت استفاده                        | ارائهدهنده با نام `9router/{sub}/{model}` در موتور مسیریابی ثبت میشود                    |
| مدیریت کلید API                     | OmniRoute آن را تولید، در حالت ذخیرهشده رمزگذاری (AES-256-GCM) و از طریق env تزریق میکند |
| محل داشبورد                         | `/dashboard/providers/services` (سه زبانه)                                               |
| شروع خودکار                         | کلید تغییر وضعیت برای هر سرویس، بهطور پیشفرض خاموش                                       |

---

## 2. معماری — ۴ لایه

```
┌────────────────────────────────────────────────────────────────────┐
│  لایه ۱ — رابط کاربری                                              │
│  /dashboard/providers/services  (زبانهها: CLIProxyAPI | 9Router | Mux)│
│  گزارشهای زنده (SSE)، شروع/توقف/راهاندازی مجدد/بهروزرسانی، تنظیمات، نصب│
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               پوسته + مسیریابی زبانه با ?tab=      │
│    ├── tabs/                  CliproxyServiceTab، NinerouterServiceTab،│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard، ServiceLifecycleButtons،│
│                               ServiceLogsPanel، ApiKeyCard، ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP ‏(fetch در Next.js)
┌──────────────────────▼─────────────────────────────────────────────┐
│  لایه ۲ — API ‏(LOCAL_ONLY — فقط loopback)                         │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (پراکسی معکوس HTTP + WebSocket ← سرویس بالادستی 9Router)        │
│                                                                    │
│  دروازه: LOCAL_ONLY_API_PREFIXES شامل "/api/services/" و           │
│          "/dashboard/providers/services/*/embed/" است              │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ فراخوانیهای درونپردازشی
┌──────────────────────▼─────────────────────────────────────────────┐
│  لایه ۳ — ServiceSupervisor ‏(src/lib/services/)                   │
│                                                                    │
│  ServiceSupervisor.ts   ناظر عمومی (child_process.spawn)           │
│    ├── نصب:        execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── شروع:       spawn(node, [entrypoint], {env, cwd})           │
│    ├── کلید API:   crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── پورت:       20130 برای 9Router (قابل پیکربندی)              │
│    ├── گزارشها:   بافر حلقوی stdio با حجم 5 MB ← رویدادهای SSE    │
│    ├── سلامت:      HTTP GET /health هر ۲ تا ۵ ثانیه، بازیابی تنبل  │
│    └── چرخه حیات:  SIGTERM بهمدت ۱۵ ثانیه → SIGKILL               │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       راهاندازی اولیه همه SERVICES[] هنگام شروع پردازش│
│  apiKey.ts          getOrCreateApiKey()، generateServiceApiKey()   │
│  modelSync.ts       GET دورهای /v1/models ← جدول service_models   │
│  ringBuffer.ts      بافر حلقوی گزارشها (5 MB برای هر سرویس)       │
│  healthCheck.ts     بررسی دورهای سلامت HTTP                       │
│  installers/        ninerouter.ts، cliproxy.ts، mux.ts، openwa.ts  │
│                      (آداپتورهای نصبکننده)                        │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP سازگار با OpenAI ‏(loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  لایه ۴ — ارائهدهنده / مسیریابی                                   │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    پورت و کلید API را برای هر درخواست دوباره واکشی میکند (بدون کش).│
│    پیشوند "9router/" را پیش از پراکسیکردن از شناسه مدل حذف میکند.│
│    اگر ناظر در وضعیت "running" نباشد، 503 service_not_running را برمیگرداند.│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    ورودی مربوط به "9router": isEmbeddedService: true               │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    مدلها بهصورت "9router/{sub}/{model}" ذخیره میشوند (با پیشوند).│
│    هر ۵ دقیقه توسط modelSync.ts همگامسازی میشوند.                │
│                                                                    │
│  Mux فقط از نظر چرخه حیات مدیریت میشود (لایههای ۱ تا ۳) — این یک │
│  دیمن هماهنگسازی عاملهاست، نه یک پراکسی LLM؛ بنابراین هیچ ورودی  │
│  executor/provider در لایه ۴ ندارد و هرگز هدف مسیریابی نیست.       │
└────────────────────────────────────────────────────────────────────┘
```

### فایلهای منبع کلیدی

| فایل                                        | نقش                                                    |
| ------------------------------------------- | ------------------------------------------------------ |
| `src/lib/services/ServiceSupervisor.ts`     | کلاس اصلی: چرخهٔ حیات، قفل، سلامت، بافر حلقوی          |
| `src/lib/services/bootstrap.ts`             | ثبت در سطح فرایند و راهاندازی خودکار                   |
| `src/lib/services/registry.ts`              | نگاشت تکنمونهٔ `ابزار → سرپرست`                        |
| `src/lib/services/apiKey.ts`                | تولید کلید، رمزنگاری AES-256-GCM برای دادههای ذخیرهشده |
| `src/lib/services/modelSync.ts`             | همگامسازی دورهای مدل (۵ دقیقه) + در صورت درخواست       |
| `src/lib/services/ringBuffer.ts`            | بافر حلقوی گزارش ۵ مگابایتی با اشتراک SSE              |
| `src/lib/services/healthCheck.ts`           | کاوش سلامت HTTP (با فاصلهٔ زمانی قابل پیکربندی)        |
| `src/lib/services/installers/ninerouter.ts` | نصب/بهروزرسانی/حذف 9Router با npm                      |
| `src/lib/services/installers/cliproxy.ts`   | نصب/بهروزرسانی/حذف CLIProxyAPI با npm                  |
| `src/lib/services/installers/mux.ts`        | نصب/بهروزرسانی/حذف Mux با npm                          |
| `src/lib/services/installers/openwa.ts`     | نصب/بهروزرسانی/حذف open-wa با npm                      |
| `src/app/api/services/9router/_lib.ts`      | تابع کمکی `getOrInitSupervisor()`                      |
| `src/app/api/services/[name]/logs/route.ts` | نقطهٔ پایانی مشترک گزارشهای SSE                        |
| `open-sse/executors/ninerouter.ts`          | اجراکنندهٔ ارائهدهنده (لایهٔ ۴)                        |

---

## 3. ماشین حالت چرخهٔ حیات

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
            کاوش سلامت موفق است     │         خرابی / SIGTERM    │
                               ┌────▼─────┐  (خروج ظرف 5 ثانیه) │
                               │ running  │──── خرابی ──────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

حالتها در جدول `version_manager` پایگاه داده (ستون `status`) ذخیره میشوند و در
حالت درونحافظهای `ServiceSupervisor` نیز بازتاب مییابند. برای یک فرایند در حال
اجرا، حالت درونحافظهای مرجع نهایی است؛ حالت پایگاه داده، جایگزین پایدار هنگام راهاندازی سیستم است.

### گذارهای حالت

| از              | رویداد                                        | به                     |
| --------------- | --------------------------------------------- | ---------------------- |
| `not_installed` | `install()` با موفقیت انجام میشود             | `stopped`              |
| `stopped`       | `start()` فراخوانی میشود                      | `starting`             |
| `starting`      | کاوش سلامت مقدار 200 را برمیگرداند            | `running`              |
| `starting`      | فرایند پیش از سالمشدن خارج میشود              | `error`                |
| `running`       | `stop()` فراخوانی میشود                       | `stopping` → `stopped` |
| `running`       | فرایند بهطور غیرمنتظره خارج میشود (< 5 ثانیه) | `error` (خرابی سریع)   |
| `running`       | فرایند بهطور غیرمنتظره خارج میشود (> 5 ثانیه) | `error`                |
| `error`         | `start()` فراخوانی میشود                      | `starting`             |
| هر حالت         | `stop()` هنگام `stopping`                     | بدون عملیات            |

### قفل عملیات

`ServiceSupervisor` عملیات چرخهٔ حیات را از طریق یک قفل عملیات ناهمگام
(`withLock()`) بهصورت ترتیبی اجرا میکند. فراخوانیهای همزمان `start()` روی یک سرپرست یکسان،
دقیقاً به ایجاد یک فرایند منجر میشوند؛ فراخوان دوم منتظر میماند و وضعیت موجود را برمیگرداند. این کار
از شرایط رقابتی جلوگیری میکند؛ برای مثال، زمانی که شروع خودکار و یک دکمهٔ رابط کاربری همزمان فعال میشوند.

---

## 4. مرجع API

تمام مسیرهای زیرمجموعهٔ `/api/services/` **LOCAL_ONLY** هستند (فقط loopback، قانون قطعی #17).
درخواستهای غیر-loopback، صرفنظر از توکن احراز هویت، پاسخ `403 LOCAL_ONLY` دریافت میکنند.

### 4.1 نقاط پایانی 9Router (11 مسیر)

#### `POST /api/services/9router/install`

9Router را از npm نصب میکند. مسیر `DATA_DIR/services/9router/` را با
`package.json` و `node_modules/` اختصاصی آن ایجاد میکند. با وابستگیهای خود OmniRoute تداخلی ندارد.

**بدنهٔ درخواست** (همه اختیاری):

```json
{ "version": "latest" }
```

| فیلد      | نوع      | پیشفرض     | توضیحات                         |
| --------- | -------- | ---------- | ------------------------------- |
| `version` | `string` | `"latest"` | تگ نسخهٔ npm یا semver برای نصب |

**پاسخها:**

| وضعیت | توضیحات                                                         |
| ----- | --------------------------------------------------------------- |
| `200` | `{ ok: true, installedVersion: "x.y.z", path: "..." }`          |
| `400` | بدنهٔ درخواست نامعتبر (خطای اعتبارسنجی Zod)                     |
| `409` | نصب از قبل در حال انجام است (قفل در اختیار است)                 |
| `500` | نصب npm ناموفق بود — برای خطای قابلفهم به `message` مراجعه کنید |

**یادداشتها:** از `execFile('npm', [...])` استفاده میکند — بدون shell و بدون درونیابی (قانون قطعی #13).
خطاهای EACCES بهصورت پیامهای قابلفهم نمایش داده میشوند.

---

#### `POST /api/services/9router/start`

9Router را راهاندازی میکند. اگر supervisor از قبل ثبت نشده باشد، آن را ثبت میکند و سپس
`supervisor.start()` را فراخوانی میکند. اگر سرویس از قبل در حال اجرا باشد، این عملیات idempotent است.

**بدنهٔ درخواست:** ندارد

**پاسخها:**

| وضعیت | توضیحات                                                    |
| ----- | ---------------------------------------------------------- |
| `200` | شیء `ServiceStatus` (طرحوارهٔ زیر را ببینید)               |
| `409` | 9Router نصب نشده است (`status: "not_installed"`)           |
| `503` | راهاندازی ناموفق بود (خطای پردازش — `lastError` را ببینید) |

**طرحوارهٔ ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

9Router را بهصورت امن متوقف میکند. SIGTERM را ارسال میکند، 15 ثانیه منتظر میماند و اگر پردازش همچنان فعال باشد، SIGKILL را ارسال میکند.
اگر سرویس از قبل متوقف شده باشد، این عملیات idempotent است.

**بدنهٔ درخواست:** ندارد

**پاسخها:**

| وضعیت | توضیحات                            |
| ----- | ---------------------------------- |
| `200` | `ServiceStatus` (state: "stopped") |
| `503` | توقف بهطور غیرمنتظره ناموفق بود    |

---

#### `POST /api/services/9router/restart`

معادل اجرای `stop()` و سپس `start()` تحت قفل عملیات است.

**بدنهٔ درخواست:** ندارد

**پاسخها:** مشابه `start` است (`ServiceStatus` نهایی را بازمیگرداند).

---

#### `POST /api/services/9router/update`

9Router را به نسخهٔ جدیدتر npm بهروزرسانی میکند. اگر سرویس در حال اجرا باشد، ابتدا متوقف میشود،
سپس نصب npm اجرا میشود (نسخهٔ جدیدتر در همان محل نصب میشود) و پس از آن
سرویس مجدداً راهاندازی میشود.

**بدنهٔ درخواست** (همه اختیاری):

```json
{ "version": "latest" }
```

**پاسخها:**

| وضعیت | توضیحات                                                         |
| ----- | --------------------------------------------------------------- |
| `200` | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400` | بدنه نامعتبر است                                                |
| `500` | بهروزرسانی npm ناموفق بود                                       |

---

#### `POST /api/services/9router/rotate-key`

یک کلید API جدید برای 9Router تولید میکند، آن را در حالت ذخیرهشده رمزنگاری میکند و سرویس را
(اگر در حال اجرا باشد) مجدداً راهاندازی میکند تا کلید جدید را از محیط خود دریافت کند. کلید قدیمی
بلافاصله نامعتبر میشود.

**بدنهٔ درخواست:** ندارد

**پاسخها:**

| وضعیت | توضیحات                                    |
| ----- | ------------------------------------------ |
| `200` | `{ keyRotated: true, restarted: boolean }` |
| `500` | چرخش کلید ناموفق بود                       |

**امنیت:** کلید جدید هرگز در پاسخ بازگردانده نمیشود (از نشت اعتبارنامه جلوگیری میشود).
این کلید بهصورت رمزنگاریشده (AES-256-GCM) در جدول `version_manager` ذخیره میشود.

---

#### `GET /api/services/9router/status`

وضعیت ترکیبی زنده + DB، شامل فرادادهٔ نسخه و پیشنمایش کلید API را بازمیگرداند.

**پاسخها:**

| وضعیت | توضیحات                 |
| ----- | ----------------------- |
| `200` | طرحوارهٔ زیر را ببینید  |
| `500` | خواندن وضعیت ناموفق بود |

**طرحوارهٔ پاسخ:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

پرچم راهاندازی خودکار را تغییر میدهد. وقتی `enabled: true` باشد، دفعهٔ بعد که OmniRoute راهاندازی میشود،
سرویس بهطور خودکار اجرا خواهد شد (بهشرط آنکه سرویس نصب شده باشد).

**بدنهٔ درخواست:**

```json
{ "enabled": true }
```

**پاسخها:**

| وضعیت | توضیحات               |
| ----- | --------------------- |
| `200` | `{ autoStart: true }` |
| `400` | بدنه نامعتبر است      |

---

#### `GET /api/services/9router/logs`

جریان SSE از لاگهای زندهٔ بافر حلقوی stdout/stderr مربوط به 9Router.

**پارامترهای کوئری:**

| پارامتر  | نوع       | پیشفرض | توضیحات                                                                              |
| -------- | --------- | ------ | ------------------------------------------------------------------------------------ |
| `tail`   | `integer` | 200    | تعداد خطوط تاریخی که ابتدا ارسال میشوند (حداکثر 1000)                                |
| `filter` | `string`  | ندارد  | فیلتر زیررشتهای بدون حساسیت به بزرگی و کوچکی حروف (بدون regex — ایمن در برابر ReDoS) |

**رویدادهای SSE:**

| رویداد      | داده        | توضیحات                         |
| ----------- | ----------- | ------------------------------- |
| `snapshot`  | `LogLine[]` | بخش انتهایی اولیهٔ لاگ تاریخی   |
| `log`       | `LogLine`   | خط لاگ زنده                     |
| `heartbeat` | `{}`        | زنده نگهداشتن اتصال هر 15 ثانیه |

**طرحوارهٔ LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**پاسخها:**

| وضعیت | توضیحات                                           |
| ----- | ------------------------------------------------- |
| `200` | `text/event-stream`                               |
| `400` | پارامتر `filter` بیشازحد طولانی است (> 200 نویسه) |
| `404` | سرویس پیدا نشد (سرپرست ثبت نشده است)              |

---

### 4.2 نقاط پایانی CLIProxyAPI (10 مسیر)

CLIProxyAPI همان ساختار نقاط پایانی 9Router را بدون `rotate-key` دارد و
`accounts`، `provider-expose` و `auto-restart-adopted` نیز به آن اضافه شدهاند. اکنون یک
کلید API اختصاصی صفحهٔ داده را دریافت میکند که هنگام ایجاد فرایند تزریق میشود (`needsApiKey: true` در
`bootstrap.ts`، برای همگامسازی مدل استفاده میشود)؛ `status` شامل فیلدهای کمتری است.

| متد    | مسیر                                | توضیحات                                        |
| ------ | ----------------------------------- | ---------------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | نصب CLIProxyAPI از npm                         |
| `POST` | `/api/services/cliproxy/start`      | راهاندازی CLIProxyAPI                          |
| `POST` | `/api/services/cliproxy/stop`       | توقف CLIProxyAPI                               |
| `POST` | `/api/services/cliproxy/restart`    | راهاندازی مجدد CLIProxyAPI                     |
| `POST` | `/api/services/cliproxy/update`     | بهروزرسانی به نسخهای جدیدتر                    |
| `GET`  | `/api/services/cliproxy/status`     | وضعیت زنده + پایگاه داده (بدون `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | فعال یا غیرفعال کردن شروع خودکار               |

نقطهٔ پایانی مشترک `GET /api/services/{name}/logs` (به §4.1 مراجعه کنید) برای هر
چهار سرویس با استفاده از بخش پویای `[name]` کار میکند.

---

### 4.3 نقاط پایانی Mux (8 مسیر)

Mux همان ساختار نقاط پایانی CLIProxyAPI را دارد — در سطح API هیچ مسیر `rotate-key`
وجود ندارد (توکن حامل به همان روش 9Router و از طریق
`getOrCreateApiKey("mux")` تولید و با متغیر محیطی `MUX_SERVER_AUTH_TOKEN` تزریق میشود، اما
هنوز نقطهٔ پایانی اختصاصی برای چرخش آن وجود ندارد). Mux فقط از نظر چرخهٔ عمر مدیریت میشود:
برخلاف 9Router، اجراکنندهٔ لایهٔ 4 ندارد و هرگز بهعنوان ارائهدهندهٔ مسیریابی ثبت نمیشود.

| متد    | مسیر                           | توضیحات                          |
| ------ | ------------------------------ | -------------------------------- |
| `POST` | `/api/services/mux/install`    | نصب Mux از npm (`npm i mux`)     |
| `POST` | `/api/services/mux/start`      | راهاندازی Mux (`mux server`)     |
| `POST` | `/api/services/mux/stop`       | توقف Mux                         |
| `POST` | `/api/services/mux/restart`    | راهاندازی مجدد Mux               |
| `POST` | `/api/services/mux/update`     | بهروزرسانی به نسخهٔ جدیدتر npm   |
| `GET`  | `/api/services/mux/status`     | وضعیت زنده + پایگاه داده         |
| `POST` | `/api/services/mux/auto-start` | فعال یا غیرفعال کردن شروع خودکار |

---

### 4.4 نقاط پایانی Bifrost (8 مسیر)

Bifrost یک بکاند رلهٔ درگاه هوش مصنوعی مبتنی بر Go است (`@maximhq/bifrost`). این سرویس از همان
ساختار نقاط پایانی CLIProxyAPI استفاده میکند (بدون `rotate-key` — Bifrost کلیدهای ارائهدهندگان
خود را در `config.json` و زیر `-app-dir` مدیریت میکند).

| متد    | مسیر                               | توضیحات                                                             |
| ------ | ---------------------------------- | ------------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | نصب Bifrost از npm (`@maximhq/bifrost`)                             |
| `POST` | `/api/services/bifrost/start`      | راهاندازی Bifrost روی پورت 8080 (پیشفرض)                            |
| `POST` | `/api/services/bifrost/stop`       | توقف Bifrost                                                        |
| `POST` | `/api/services/bifrost/restart`    | راهاندازی مجدد Bifrost                                              |
| `POST` | `/api/services/bifrost/update`     | بهروزرسانی به نسخهای جدیدتر                                         |
| `GET`  | `/api/services/bifrost/status`     | وضعیت زنده + پایگاه داده                                            |
| `POST` | `/api/services/bifrost/auto-start` | فعال یا غیرفعال کردن شروع خودکار                                    |
| `GET`  | `/api/services/bifrost/logs`       | دنبالهٔ زندهٔ گزارش با SSE (از طریق مسیر پویای مشترک `[name]/logs`) |

**اتصال مسیریابی:** وقتی `BIFROST_BASE_URL` تنظیم نشده باشد و نمونهٔ Bifrost تحت نظارت
در حال اجرا باشد، `getBifrostRoutingConfig()` (در `routingBackend.ts`) بهطور خودکار
از `http://127.0.0.1:{port}` بهعنوان URL پایهٔ رله استفاده میکند. متغیر محیطی صریح
`BIFROST_BASE_URL` همیشه اولویت دارد.

---

### 4.5 نقاط پایانی Dario (12 مسیر)

همان ساختار چرخهٔ عمر سایر سرویسها (`install`، `start`، `stop`، `restart`،
`update`، `status`، `auto-start`، `auto-restart-adopted`) را دارد، بهعلاوهٔ یک
صفحهٔ کنترل OAuth محافظتشده با توکن در زیر `admin/`:‏ `admin/accounts`، `admin/import-from-omniroute`،
`admin/login-start`، `admin/login-complete` (همگی پشت `DARIO_ADMIN_TOKEN`).

### 4.6 نقاط پایانی open-wa (7 مسیر)

open-wa (`@open-wa/wa-automate`) یک نمونهٔ بدون رابط گرافیکی Chromium را (از طریق
Puppeteer) برای خودکارسازی WhatsApp Web کنترل میکند. این سرویس همان ساختار نقاط پایانی Mux را دارد (هنوز
مسیر `rotate-key` ندارد). این سرویس فقط از نظر چرخهٔ عمر مدیریت میشود — هدف مسیریابی نیست و
هیچ ورودی اجراکننده/ارائهدهندهٔ لایهٔ 4 ندارد.

| متد    | مسیر                              | توضیحات                                                             |
| ------ | --------------------------------- | ------------------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | نصب open-wa از npm (`@open-wa/wa-automate`)                         |
| `POST` | `/api/services/openwa/start`      | راهاندازی open-wa روی پورت 8323 (پیشفرض)                            |
| `POST` | `/api/services/openwa/stop`       | توقف open-wa                                                        |
| `POST` | `/api/services/openwa/restart`    | راهاندازی مجدد open-wa                                              |
| `POST` | `/api/services/openwa/update`     | بهروزرسانی به نسخه جدیدتر                                           |
| `GET`  | `/api/services/openwa/status`     | وضعیت زنده + پایگاه داده                                            |
| `POST` | `/api/services/openwa/auto-start` | فعال یا غیرفعالکردن راهاندازی خودکار                                |
| `GET`  | `/api/services/openwa/logs`       | دنباله زنده گزارشها با SSE (از طریق مسیر پویای مشترک `[name]/logs`) |

**کلید API:** بهصورت `WA_KEY` تزریق میشود — بازنویسی عمومی متغیرهای محیطی open-wa با پیشوند `WA_*`، آن را به گزینه CLI یعنی `--key`/`-k` نگاشت میکند
(`dist/cli/setup.js::envArgs()`، با بسته نصبشده نسخه 4.76.0
تأیید شده است). هنگام تولید توسط `generateServiceApiKey()`، پیشوند `ow_` به آن
اضافه میشود. open-wa کلید را از هدر HTTP با نام `key`/`api_key` میخواند (نه
`Authorization: Bearer`)؛ مسیر `/api-docs*` صراحتاً از این بررسی معاف شده است
(`setupAuthenticationLayer` در `dist/cli/server.js`)، بنابراین پروب سلامت
به هدر احراز هویت نیاز ندارد.

**جفتسازی:** open-wa غیررسمی است و هیچ وابستگیای به WhatsApp ندارد — شماره
متصلشده بهدلیل سامانه تشخیص خودکارسازی خود WhatsApp با خطر مسدودشدن مواجه است.
در اولین راهاندازی، کد QR جفتسازی در stdout چاپ میشود و از طریق
پنل موجود Logs/جریان SSE نمایش داده میشود — در حال حاضر، هیچ نقطه پایانی
اختصاصی برای تصویر QR در این یکپارچهسازی وجود ندارد.

---

### 4.7 پروکسی معکوس (تعبیه داشبورد 9Router)

داشبورد، رابط وب 9Router را از طریق یک پروکسی معکوس داخلی درون iframe
تعبیه میکند:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

این پروکسی:

- درخواست را به `http://127.0.0.1:{port}/{path}` ارسال میکند (فقط loopback)
- هدرهای ورودی `cookie` و `authorization` را حذف میکند (برای جلوگیری از افشای نشست OmniRoute)
- برای احراز هویت 9Router، هدر `Authorization: Bearer {apiKey}` را تزریق میکند
- هدرهای `set-cookie`، `content-security-policy`، `x-frame-options` و `cross-origin-*` را از پاسخ حذف میکند
- پاسخهای HTML را برای تزریق `<base href>` و عادیسازی مسیرهای مطلق بازنویسی میکند (`/foo` → `/dashboard/.../embed/foo`)

ارتقاهای WebSocket مربوط به داشبورد تعبیهشده، توسط یک سرور همراه روی
پورتی اختصاصی مدیریت میشوند (به `src/lib/services/embedWsProxy.ts` مراجعه کنید).

**امنیت:** مسیرهای پروکسی تعبیهشده تحت `LOCAL_ONLY_API_PREFIXES`
طبقهبندی شدهاند و فقط از طریق loopback قابل دسترسی هستند. مهاجمی که از طریق
تونل Cloudflare/Ngrok به یک JWT دست یابد، نمیتواند به سرویسهای تعبیهشده پروکسی بزند.

---

## 5. امنیت

### اعمال LOCAL_ONLY (قانون سختگیرانه #17)

تمام مسیرهای زیرمجموعه `/api/services/` و `/dashboard/providers/services/*/embed/` در
`src/server/authz/routeGuard.ts` بهعنوان LOCAL_ONLY طبقهبندی شدهاند. بررسی loopback
بدون هیچ شرطی و پیش از هر شاخه احراز هویت اجرا میشود:

```
درخواست وارد میشود
  → isLocalOnlyPath(path)?
      → غیر-loopback → 403 LOCAL_ONLY (همیشه، پیش از بررسی احراز هویت)
      → loopback     → ادامه به احراز هویت عادی
```

این کار مانع میشود که یک JWT افشاشده (برای مثال، از طریق یک تونل) بتواند `npm install`
یا ایجاد فرایند را فعال کند. برای مشاهده ماتریس کامل سطوح، به
`docs/security/ROUTE_GUARD_TIERS.md` مراجعه کنید.

### تزریق کلید API

9Router و Mux برای endpointهای HTTP خود به یک کلید API یا bearer token نیاز دارند.
OmniRoute:

1. با استفاده از `crypto.randomBytes(32).toString("base64url")` و یک پیشوند
   مختص سرویس (`nr_` برای 9Router و `mx_` برای Mux) کلیدی تولید میکند.
2. آن را در حالت ذخیرهشده با AES-256-GCM رمزنگاری میکند (همان رمزی که برای اطلاعات احراز هویت ارائهدهنده استفاده میشود).
3. هنگام ایجاد فرایند، آن را رمزگشایی کرده و بهصورت یک متغیر محیطی تزریق میکند —
   `NINEROUTER_API_KEY` برای 9Router و `MUX_SERVER_AUTH_TOKEN` برای Mux (هرگز بهصورت
   یک پرچم CLI ارسال نمیشود، بنابراین توکن هیچگاه در فهرستهای `ps`/فرایند ظاهر نمیشود).
4. کلید متن ساده را هرگز در هیچ پاسخ HTTP بازنمیگرداند.

CLIProxyAPI یک کلید اختصاصی data-plane دریافت میکند که هنگام ایجاد فرایند تزریق میشود
(`needsApiKey: true` — برای همگامسازی مدل با adapter استفاده میشود).

### دفاع در برابر SSRF

پروکسی معکوس HTTP (`/dashboard/.../embed/[...path]`) بهصورت hardcoded تنظیم شده است تا
فقط درخواستها را به `http://127.0.0.1:{port}` هدایت کند. این پروکسی هرگز تغییرمسیرها
به مقصدهای غیر-loopback را دنبال نمیکند. از کتابخانه `ssrf-req-filter` برای رد کردن
هر URL بالادستی که به آدرسی خارج از محدوده loopback resolve شود، استفاده میشود.

### ایمنی shell (قانون سختگیرانه #13)

`npm install` از طریق `execFile('npm', ['install', pkg, '--prefix', dir])` فراخوانی میشود —
بدون template literal، بدون shell و بدون درونیابی مسیرهای خارجی در رشته فرمان.
مقادیر زمان اجرا (پورتها و کلیدهای API) از طریق شیء `env` فرایند فرزند ارسال میشوند.

### پاکسازی خطاها (قانون سختگیرانه #12)

تمام پاسخهای خطای `/api/services/*` از `buildErrorBody()` یا
`sanitizeErrorMessage()` عبور میکنند. مقادیر خام `err.stack` و `err.message` هرگز
عیناً به فراخواننده بازگردانده نمیشوند.

---

## 6. افزودن یک سرویس تعبیهشده جدید

این 8 مرحله را دنبال کنید. پیادهسازیهای موجود در `src/lib/services/installers/`
و `src/app/api/services/` را بهعنوان مرجع معیار مطالعه کنید.

### مرحله 1 — ایجاد نصبکننده

فایل `src/lib/services/installers/{name}.ts` را با الگوبرداری از `ninerouter.ts` ایجاد کنید:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // یک پورت آزاد انتخاب کنید

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

از `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` موجود در `installers/utils.ts`
استفاده کنید — هرگز از `execSync` یا درونیابی shell استفاده نکنید.

### مرحله 2 — ثبت در bootstrap

یک `ServiceEntry` به آرایه `SERVICES` در `src/lib/services/bootstrap.ts` اضافه کنید:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // اگر به کلید API نیازی نیست، false
}
```

`buildSpawnArgsFactory()` را برای پشتیبانی از `cfg.tool === "myservice"` گسترش دهید.

#### قرارداد provider-plugin قابل اتصال (فاز 1، #7333)

`src/lib/services/providerPlugins/` یک قرارداد `ServiceProviderPlugin` معرفی میکند که
فیلدهای `ServiceEntry` مربوط به `bootstrap.ts` یک backend و فیلدهای manifest-template
مربوط به `serviceBackends.ts` را در یک شیء بستهبندی میکند؛ بهجای اینکه ساختار همان
backend بهصورت جداگانه در دو فایل نامرتبط بیان شود. تا زمان نگارش این متن،
**فقط `9router` مهاجرت داده شده است** — `bootstrap.ts` ورودی `SERVICES[]` آن را از
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`)
استخراج میکند و اگر plugin وجود نداشته باشد، هنگام راهاندازی خطا ایجاد میکند.
`cliproxy`، `mux` و `bifrost` بدون تغییر همچنان از literalهای inline از پیش موجود
`SERVICES[]` استفاده میکنند.

`open-sse/config/providerPluginManifest.ts` همچنین یک helper افزایشی با نام
`createServiceBackendManifestEntry(pluginId, template)` دریافت کرده است که یک
`ProviderPluginManifestEntry` خوشساخت را از یک ورودی `SERVICE_BACKEND_MANIFEST_TEMPLATE`
میسازد — این helper هنوز به هیچ مسیر درخواست فعالی متصل نشده است (نه
`generateProviderPluginManifestFromRegistry()` و نه `/v1/providers/[provider]/models`)؛
پس از اثبات قرارداد برای دومین backend، این کار در ادامه انجام خواهد شد.

موارد زیر به PRهای بعدی موکول شدهاند و تحت issue شماره #7333 پیگیری میشوند:
مهاجرت `cliproxyapi` از طریق همان registry، تعمیم `mux`/`bifrost` در union نوع
`ServiceBackendPluginId`، ادغام حالتهای ویژه مسیریابی executor
(`open-sse/executors/index.ts`، `open-sse/handlers/chatCore/executorProxy.ts`) در قرارداد
plugin، و اتصال `createServiceBackendManifestEntry()` به یک مسیر کد فعال manifest/models.

### مرحله 3 — افزودن migration و seed پایگاه داده

با استفاده از یک migration در `src/lib/db/migrations/` مطمئن شوید که سرویس دارای یک
ردیف در `version_manager` است. این ردیف باید بهشکل زیر باشد:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### مرحله 4 — ایجاد 7 endpoint مربوط به API

در مسیر `src/app/api/services/{name}/`:

```
_lib.ts            helper مربوط به getOrInitSupervisor()
install/route.ts   POST — installer.install() را فراخوانی میکند
start/route.ts     POST — supervisor.start() را فراخوانی میکند
stop/route.ts      POST — supervisor.stop() را فراخوانی میکند
restart/route.ts   POST — supervisor.restart() را فراخوانی میکند
update/route.ts    POST — installer.update() را فراخوانی میکند
status/route.ts    GET  — وضعیت زنده و وضعیت DB را ادغام میکند
auto-start/route.ts POST — پرچم auto_start را تغییر میدهد
```

مسیر مشترک `GET /api/services/[name]/logs` از قبل متصل شده است — هیچ تغییری
در آن لازم نیست.

تمام پاسخهای خطا را از طریق `createErrorResponse()` / `buildErrorBody()` واگذار کنید.

### گام 5 — افزودن به LOCAL_ONLY_API_PREFIXES

در `src/server/authz/routeGuard.ts`، بررسی کنید که `/api/services/` از قبل فهرست شده باشد.
اگر پیشوند جدیدی معرفی میکنید (برای مثال، `/api/tools/`)، آن را هم به
`LOCAL_ONLY_API_PREFIXES` و، اگر فرایندهایی ایجاد میکند، به `SPAWN_CAPABLE_PREFIXES` اضافه کنید.
یک آزمون در `tests/unit/authz/routeGuard.test.ts` اضافه کنید.

### گام 6 — افزودن زبانه رابط کاربری

فایل `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx` را ایجاد کنید.
از مؤلفههای مشترک دوباره استفاده کنید:

- `ServiceStatusCard` — وضعیت زنده + نشان سلامت
- `ServiceLifecycleButtons` — شروع / توقف / راهاندازی مجدد / بهروزرسانی
- `ServiceLogsPanel` — دنباله زنده گزارشها با SSE (به `/api/services/{name}/logs` متصل میشود)
- `ApiKeyCard` — نمایش + تعویض کلید (اگر `needsApiKey: true` باشد)

زبانه را در `ServicesPageShell.tsx` ثبت کنید.

### گام 7 — افزودن ورودی ارائهدهنده (اگر سرویس یک مقصد مسیریابی است)

اگر سرویس تعبیهشده یک نقطه پایانی سازگار با OpenAI در `/v1/chat/completions` ارائه میدهد:

1. یک ورودی ارائهدهنده با `isEmbeddedService: true` در `src/shared/constants/providers.ts` اضافه کنید.
2. فایل `open-sse/executors/{name}.ts` را با گسترش `BaseExecutor` ایجاد کنید. پورت و
   کلید API را برای هر درخواست دوباره واکشی کنید (هرگز آنها را در سازنده ذخیره نکنید). زمانی که وضعیت ناظر
   برابر با `"running"` نیست، پاسخ `503 service_not_running` را برگردانید.
3. مدلها را با پیشوند سرویس (برای مثال، `myservice/sub/model`) در `open-sse/config/providerRegistry.ts`
   ثبت کنید. `modelSync.ts` آنها را بهروز نگه میدارد.

### گام 8 — مستندسازی و آزمون

1. فایل `docs/frameworks/EMBEDDED-SERVICES.md` (همین فایل) را بهروزرسانی کنید — سرویس را به
   جدول بخش §1 و هر نقطه پایانی جدید را به بخش §4 اضافه کنید.
2. آزمونهای واحد را در `tests/unit/services/` اضافه کنید (چرخه حیات، نصبکننده، ساختار API).
3. آزمون یکپارچهسازی را در `tests/integration/services/` اضافه کنید (مشروط به `RUN_SERVICES_INT=1`).
4. فایل `docs/openapi.yaml` را با نقطههای پایانی جدید بهروزرسانی کنید.

---

## ۷. عیبیابی

### سرویس راهاندازی نمیشود

**نشانهها:** دکمه شروع کد 503 را برمیگرداند و وضعیت روی `"error"` یا `"starting"` باقی میماند.

**فهرست بررسی:**

1. مسیر `GET /api/services/{name}/logs` (یا پنل Logs در داشبورد) را بررسی کنید. بهدنبال
   خطوطی مانند `Error: ENOENT`، `address already in use` یا `Cannot find module` بگردید.
2. مطمئن شوید `npm` در PATH قرار دارد: دستور `which npm` را از همان حساب کاربری اجرا کنید که OmniRoute را اجرا میکند.
3. مطمئن شوید سرویس نصب شده است: در پاسخ `GET /api/services/{name}/status`، مقدار
   `installedVersion` را بررسی کنید. اگر `null` است، ابتدا نصب را اجرا کنید.
4. بررسی کنید که `DATA_DIR/services/{name}/node_modules/` وجود داشته باشد و خالی نباشد.
5. برای مشاهده دلیل پالایششده خروج، فیلد `lastError` را در پاسخ وضعیت بررسی کنید.

---

### راهاندازی سرد کند است (بیش از ۱۰ ثانیه برای رسیدن به `running`)

**نشانهها:** وضعیت برای مدت زیادی روی `"starting"` باقی میماند و سپس به `"running"` یا `"error"` تغییر میکند.

**توضیح:** راهاندازی سرد 9Router شامل وارد کردن درختهای وابستگی بزرگ (ماژولهای DNS،
تونل و MITM) است. بازه پیشفرض بررسی سلامت ۲ ثانیه با ۳ تلاش است؛ پس از آن
ناظر وقوع مهلت زمانی را اعلام میکند (اما نظرسنجی را ادامه میدهد).

**راهحل:** مقدار `healthIntervalMs` و مهلت زمانی `waitForHealthy`
(`healthIntervalMs * 3`) در `bootstrap.ts` قابل پیکربندی هستند. برای سرویسهایی با زمان
راهاندازی طولانیتر، `healthIntervalMs` را به 5000 و `stopTimeoutMs` را به 30 000 افزایش دهید.

---

### تداخل پورت (`EADDRINUSE`)

**نشانهها:** گزارشها پیام `address already in use :::20130` را نشان میدهند.

**دلایل:**

- فرایند دیگری در حال استفاده از پورت 20130 است.
- فرایند قبلی 9Router بهطور کامل متوقف نشده است (PID زامبی).

**راهحل:**

1. پورت پیشفرض را از طریق متغیر محیطی `NINEROUTER_PORT` در `.env` تغییر دهید.
2. فرایند متداخل را پیدا کرده و متوقف کنید: `lsof -ti :20130 | xargs kill -9`.
3. پورت هر سرویس از طریق فیلد `port` در `bootstrap.ts` قابل پیکربندی است.

**توجه:** 9Router بهطور پیشفرض از پورت 20130 استفاده میکند تا مشخصاً با
پورت پیشفرض 20128 در OmniRoute تداخل نداشته باشد.

---

### رد شدن مجوز (EACCES) هنگام نصب

**نشانهها:** نصب کد 500 را برمیگرداند و گزارشها `EACCES` یا `permission denied` را نشان میدهند.

**دلایل:**

- `DATA_DIR` یا دایرکتوری والد آن توسط فرایند OmniRoute قابل نوشتن نیست.
- اجرا در Docker بدون دسترسی ریشه و بدون دسترسی نوشتن در ولوم نگاشتشده.

**راهحل:**

1. `DATA_DIR` را بررسی کنید (پیشفرض: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. مطمئن شوید کاربرِ فرایند OmniRoute مالک دایرکتوری است: `chown -R $USER ~/.omniroute/`
3. در Docker، مطمئن شوید اتصال ولوم مجوزهای صحیح را برای کاربر کانتینر دارد.

---

### بهروزرسانی ناموفق است (اتمام مهلت `npm install` یا خطای شبکه)

**نشانهها:** بهروزرسانی کد 500 را همراه با `InstallError` برمیگرداند و گزارشها اتمام مهلت شبکه را نشان میدهند.

**فهرست بررسی:**

1. در دسترس بودن رجیستری npm را تأیید کنید: `npm ping`.
2. وجود پراکسی سازمانی را بررسی کنید: `npm config get proxy`، `npm config get https-proxy`.
3. نصب را بهصورت دستی امتحان کنید: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. اگر پشت یک شبکه کاملاً ایزوله هستید، tarball را از قبل دانلود کنید و از `npm install /path/to/tarball.tgz` استفاده کنید.

---

### سرویس بلافاصله پس از شروع، وضعیت `"error"` را نشان میدهد (خرابی سریع)

**نشانهها:** وضعیت در کمتر از ۵ ثانیه از `"starting"` به `"error"` تغییر میکند.
`lastError` مقدار `"Fast crash (exited with code 1)"` را نشان میدهد.

**فهرست بررسی:**

1. انتهای کامل گزارش را بخوانید: `GET /api/services/{name}/logs?tail=500`.
2. دلیل رایج: متغیرهای محیطی مورد انتظار سرویس وجود ندارند.
3. برای 9Router: بررسی کنید که `NINEROUTER_DISABLE_MITM=true` و
   `NINEROUTER_DISABLE_TUNNEL=true` در محیط ارسالشده هنگام ایجاد فرایند قرار داشته باشند (به
   `resolveSpawnArgs` در `installers/ninerouter.ts` مراجعه کنید).

---

## 8. پرسشهای متداول

**س: آیا میتوانم endpointهای سرویسهای تعبیهشده را در دسترس کلاینتهای غیر-loopback قرار دهم؟**

خیر. سطح LOCAL_ONLY عامدانه در نظر گرفته شده است (قانون قطعی شمارهٔ 17). مسیرهایی که میتوانند
`npm install` را اجرا کنند یا پردازشهای `node` را ایجاد کنند، نباید از طریق ترافیک
غیر-loopback قابل دسترسی باشند؛ زیرا در غیر این صورت، افشای JWT از طریق یک تونل
(Cloudflare، Ngrok، Tailscale) امکان ایجاد پردازشهای دلخواه را فراهم میکند. هیچ استثنایی برای
نادیدهگرفتن این محدودیت در مورد `/api/services/` وجود ندارد — برخلاف `/api/mcp/`، این مسیر
در فهرست عبور از محدودیت manage-scope قرار نگرفته است. به `docs/security/ROUTE_GUARD_TIERS.md` مراجعه کنید.

---

**س: آیا 9Router و CLIProxyAPI در استقرارهای production/cloud در دسترس خواهند بود؟**

بله. هر دو سرویس از همان مدل local-first خود OmniRoute پیروی میکنند. آنها
روی همان ماشین اجرا میشوند و از طریق loopback ارتباط برقرار میکنند. منظور از «Production» در اینجا VPS
یا سرور محلیای است که OmniRoute روی آن مستقر شده است، نه یک ارائهدهندهٔ cloud راهدور.

---

**س: چگونه supervisor را اشکالزدایی کنم؟**

1. جریان لاگ SSE را دنبال کنید: `curl -N http://localhost:20128/api/services/9router/logs`.
2. لاگهای ساختیافته را در خروجی pino متعلق به OmniRoute که بر اساس فضای نام
   `service:supervisor` فیلتر شدهاند، بررسی کنید.
3. ردیف DB را بررسی کنید: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. برای مشاهدهٔ وضعیت زندهٔ فعلی، PID، سلامت،
   و `lastError` در یک فراخوانی، از `GET /api/services/9router/status` استفاده کنید.

---

**س: supervisor مقدار `health: "degraded"` یا `health: "unknown"` را نشان میدهد، اما state برابر با `"running"` است. آیا این مشکلساز است؟**

`"degraded"` یعنی پروب سلامت پاسخی غیر از 200 دریافت کرده است. `"unknown"` یعنی هنوز هیچ
پروبی تکمیل نشده است (شرایط رقابتی با نخستین poll). هر دو حالت هنگام راهاندازی موقتی هستند.
اگر سلامت پس از `"running"` شدن، بیش از `healthIntervalMs * 3` میلیثانیه
در وضعیت `"degraded"` باقی بماند، سرویس تعبیهشده در حال اجرا است، اما API مبتنی بر HTTP آن پاسخ نمیدهد. بررسی کنید
که آیا پورت در پاسخ وضعیت صحیح است و آیا سرویس واقعاً
روی آن پورت در حال گوشدادن است یا خیر.

---

**س: آیا میتوانم کلید API مربوط به 9Router را بدون راهاندازی مجدد کامل تغییر دهم؟**

خیر. کلید API هنگام ایجاد پردازش از طریق یک متغیر محیطی به 9Router ارسال میشود.
متغیرهای محیطی را نمیتوان در یک پردازش در حال اجرا تغییر داد. `POST .../rotate-key`
برای اعمال کلید جدید، سرویس را بهطور خودکار متوقف و دوباره راهاندازی میکند. چرخش کلید
در مدت `stopTimeoutMs` سرویس (مقدار پیشفرض 15 ثانیه) بهعلاوهٔ زمان راهاندازی آن
اعمال میشود.

---

**س: محدودیت ring buffer چقدر است و وقتی پر شود چه اتفاقی میافتد؟**

هر سرویس یک ring buffer اختصاصی 5 مگابایتی دارد. وقتی بافر پر شود، قدیمیترین
خطوط لاگ حذف میشوند تا برای خطوط جدید جا باز شود. رویداد `snapshot` در SSE،
جدیدترین خطوط را تا سقف محدودیت `tail` بازمیگرداند. لاگها روی دیسک ماندگار نمیشوند، مگر اینکه
`logsBufferPath` در ردیف DB تنظیم شده باشد.

---

## همچنین ببینید

- `docs/security/ROUTE_GUARD_TIERS.md` — جزئیات سطح LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 نگاشت ماژول سرویسهای تعبیهشده
- `docs/architecture/ARCHITECTURE.md` — زمینهٔ سطح سیستم
- `docs/openapi.yaml` — تعاریف machine-readable مربوط به endpointها
- `CLAUDE.md` §"Adding a New Embedded Service" — چکلیست مرجع سریع
