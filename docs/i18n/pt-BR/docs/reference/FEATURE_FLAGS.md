# Feature Flags (Português (Brasil))

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇮🇷 [fa](../../../fa/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇷🇺 [ru](../../../ru/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> Alternâncias em tempo de execução que alteram o comportamento do OmniRoute **sem uma nova implantação**.
> Todas as flags listadas aqui estão definidas em
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> — a única fonte da verdade. Tanto o painel quanto a API REST leem
> esse arquivo, portanto a tabela abaixo é gerada para corresponder a ele na proporção de 1:1.

---

## O que são feature flags

Uma feature flag é uma alternância nomeada (booleana ou enum) cujo valor pode ser alterado em
tempo de execução e persistido no banco de dados, sem exigir uma nova implantação do processo. Cada
flag é descrita por uma `FeatureFlagDefinition` com uma `key`, `label`,
`description`, `category`, `defaultValue`, `type` e uma indicação `requiresRestart`.

### Ordem de resolução

O **valor efetivo** de uma flag é resolvido por
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) com esta
precedência (a mais alta prevalece):

1. **Substituição no banco de dados** — um valor armazenado na tabela `key_value` sob o
   namespace `feature_flags` (definido pelo painel ou pela API REST).
2. **Variável de ambiente** — `process.env[<KEY>]`, caso esteja definida e não esteja vazia.
3. **Valor padrão da definição** — o `defaultValue` de `featureFlagDefinitions.ts`.

Uma flag booleana é considerada **habilitada** quando seu valor efetivo é `"true"`,
`"1"` ou `"yes"` (consulte `isFeatureFlagEnabled()`).

> [!NOTE]
> A maioria das flags também possui uma variável de ambiente correspondente com o **mesmo nome**,
> documentada em [`ENVIRONMENT.md`](./ENVIRONMENT.md). A substituição da flag no banco de dados
> tem precedência sobre essa variável de ambiente. Uma flag com
> `requiresRestart: true` é persistida imediatamente, mas só é lida novamente na inicialização
> do processo — alterná-la exibe um banner **"Reiniciar servidor"** no painel.

---

## Catálogo de flags

72 flags em 6 categorias. **Padrão** é o valor padrão da definição — o valor
usado quando não há uma substituição no banco de dados nem uma variável de ambiente presente.

### Segurança (10)

| Chave                                   | Tipo    | Padrão   | Descrição                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------- | ------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | boolean | `false`  | Exige uma chave de API para todas as solicitações recebidas.                                                                                                                                                                                                                                                                             |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`   | Habilita a sanitização de entrada para todas as solicitações.                                                                                                                                                                                                                                                                            |
| `INJECTION_GUARD_MODE`                  | enum    | `off`    | Modo de proteção contra injeção de prompt. Valores: `off`, `warn`, `block`, `redact`.                                                                                                                                                                                                                                                    |
| `PII_REDACTION_ENABLED`                 | boolean | `false`  | Oculta PII das solicitações (independentemente de `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                                                                               |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`  | Sanitiza PII das respostas dos provedores.                                                                                                                                                                                                                                                                                               |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact` | Modo de sanitização de PII em respostas. Valores: `redact`, `warn`, `block`, `off`.                                                                                                                                                                                                                                                      |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`   | Bloqueia solicitações de saída para intervalos de IP privados/internos.                                                                                                                                                                                                                                                                  |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`  | Permite que usuários autenticados do painel revelem chaves de API armazenadas, em vez de ver apenas valores mascarados.                                                                                                                                                                                                                  |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`  | Inclui o prefixo da conta nas linhas de log AUTH (por exemplo, "Usando conta do <provider>: abc12345..."). Desabilitado por padrão para que os identificadores de conta sejam ocultados em logs de processos compartilhados/multilocatários. Independente do Modo de Depuração; ativar o Modo de Depuração não revela essas informações. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`  | Quando o OIDC está habilitado, desabilita o login por senha para que os usuários só possam se autenticar pelo Single Sign-On OIDC. Quando desabilitado (padrão), tanto o login por senha quanto o OIDC ficam disponíveis.                                                                                                                |

### Rede (15)

| Chave                                           | Tipo    | Padrão  | Reinicialização | Descrição                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------------- | ------- | ------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false` | ✓               | Ativa o modo furtivo de impressão digital TLS.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false` |                 | Permite que as rotas /v1/audio/* usem nós de provedores compatíveis com a OpenAI hospedados fora do localhost. Desativado por padrão — rotear áudio para um host remoto altera a identidade de saída e deve ser uma decisão explícita do operador. Nós de loopback são sempre permitidos e não são afetados.                                                                                                                                                                                                                                          |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false` |                 | Quando nenhum proxy estiver atribuído a uma conexão, seleciona automaticamente o primeiro proxy funcional do registro. Desativado por padrão (caso contrário, qualquer proxy do registro se torna uma alternativa global — #3332).                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false` |                 | Permite que os fluxos de OAuth e de validação de provedores ignorem um proxy fixado e se conectem diretamente quando as pré-verificações de acessibilidade do proxy falharem. Desativado por padrão, pois isso pode alterar o IP de saída.                                                                                                                                                                                                                                                                                                            |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`  |                 | Em caso de exceção de rede (tempo limite, conexão recusada/redefinida) em um executor de rotação de várias contas, quando a conta com falha não tiver um proxy dedicado, aplica um breve período de espera e ignora outras contas sem proxy pelo restante da solicitação, em vez de tentar cada uma novamente. Ativado por padrão (seguro: não altera o IP de saída; apenas reduz o risco de latência/período de espera em contas com saída compartilhada). Desative para restaurar a propagação imediata na primeira exceção de uma conta sem proxy. |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false` |                 | Os pools de proxies e a rotação por conta do opencode deixam de reutilizar temporariamente um proxy que acabou de falhar (sondagem TCP recusada ou resposta 429 recebida por meio dele) durante um período por processo que dobra a cada reincidência, até um limite. Nenhum status de proxy é registrado; se todos os candidatos forem colocados de lado, a escolha permanece inalterada. Desativado por padrão.                                                                                                                                     |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false` |                 | Exibe, sob um pool de proxies no painel, quantos IPs de saída observados atenderam seus membros nas últimas 24 h e quantas conexões os utilizaram. Somente leitura, calculado com base no log de proxies e nunca usado para roteamento. Desativado por padrão.                                                                                                                                                                                                                                                                                        |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false` |                 | Para o executor OpenCode, monitora o primeiro byte do corpo de uma resposta Responses transmitida por streaming (janela: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`, padrão `15000`). Um stream Responses 2xx que permanece silencioso além da janela é tratado como paralisado: a conta entra em período de espera e a solicitação é redirecionada para a próxima conta uma vez; uma segunda paralisação causa uma falha imediata. Desativado por padrão: streams paralisados mantêm a espera atual até o tempo limite de prontidão do stream.                |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false` |                 | Executor OpenCode: ao receber um 403/451 contendo uma recusa `user_blocked` (não relacionada à localização geográfica nem a uma rejeição de impressão digital do Cloudflare), coloca a conta recusada em período de espera e alterna para a próxima conta no máximo uma vez por solicitação; uma segunda recusa é retornada sem alterações, sem marcação de sucesso. Desativado por padrão: contornar um bloqueio de usuário do serviço upstream pode parecer evasão e propagar a sinalização por toda a frota.                                       |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false` |                 | Rotação do OpenCode: após duas falhas transitórias consecutivas no upstream (5xx ou um 400 vazio), aguarda antes de usar a próxima conta — 1,5 s, dobrando a cada falha adicional, limitado a 6 s por pausa e 10 s por solicitação; ignorado quando o cliente se desconecta. O corpo da resposta com falha é liberado antes da espera. Desativado por padrão: o failover permanece imediato.                                                                                                                                                          |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false` |                 | Rotação do OpenCode: interrompe a sequência de contas no primeiro 429 classificado como um limite de taxa real (`Retry-After` analisável ou um corpo que mencione um limite de taxa/uso) e retorna esse 429 do upstream sem alterações. Respostas 429 não classificadas continuam a rotação. Desativado por padrão: o nível gratuito é limitado por IP de saída (#9611), portanto, cada 429 provoca uma rotação, e uma sequência esgotada retorna o último 429 do upstream.                                                                           |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false` | ✓               | Desativa a verificação do certificado TLS para o proxy MITM. **Perigo.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false` |                 | Permite URLs de provedores que apontem para redes privadas/internas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`  |                 | Permite adicionar/validar provedores em endereços locais/privados (127.0.0.1, localhost, LAN). Ativado por padrão (prioridade local); desative para aplicar um bloqueio estrito que permita apenas endereços públicos. Os metadados de nuvem continuam bloqueados.                                                                                                                                                                                                                                                                                    |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false` | ✓               | Ativa o modo de provedor compatível com o Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### Políticas (5)

| Chave                           | Tipo    | Padrão     | Descrição                                                                                                                                                                                                                                                                               |
| ------------------------------- | ------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled` | Modo de aplicação da política de uso de ferramentas. Valores: `disabled`, `warn`, `block`.                                                                                                                                                                                              |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`    | Ativa automaticamente a limitação de taxa com base nos padrões de uso.                                                                                                                                                                                                                  |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`    | Ignora a verificação local do OmniRoute para a janela de contexto/o máximo de tokens de entrada em solicitações diretas de modelo único. Os limites do upstream ainda se aplicam.                                                                                                       |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`    | Rejeita solicitações antes do encaminhamento quando o modelo de destino não possui os recursos necessários (visão, ferramentas, saída estruturada, janela de contexto). Protege solicitações diretas a um único provedor que contornam o filtro de compatibilidade da camada de combos. |
| `RADAR_ENABLED`                 | boolean | `false`    | Ativa o módulo OmniRoute Radar (telas e sincronização do feed do catálogo). Desativado por padrão; a ativação apenas desbloqueia a interface — a sincronização de dados continua sendo uma opção separada.                                                                              |

### Tempo de execução (32)

| Chave                                       | Tipo    | Padrão  | Reinicialização | Descrição                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------- | ------- | ------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | boolean | `true`  |                 | Gera e injeta resumos de conversas quando o roteamento combinado alterna entre modelos. Desative para tratar as alternâncias de modelo de forma independente e impedir solicitações de transferência em segundo plano para todas as combinações existentes e futuras.                                                                                                                                                                                                                                                                                                                                                    |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | boolean | `true`  |                 | Descarta itens de saída internos da fase de comentários dos fluxos de passagem da Responses API antes de encaminhá-los aos clientes. Desative para receber os comentários brutos do serviço upstream.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | boolean | `true`  |                 | Impõe restrições de escopo ao acesso às ferramentas MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | boolean | `false` |                 | Compacta as descrições das ferramentas MCP para reduzir o uso de tokens.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | boolean | `false` |                 | Habilita o processamento de tarefas em segundo plano durante a execução.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | boolean | `false` | ✓               | Desabilita todos os serviços em segundo plano (atualização de cota, sincronização etc.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false` |                 | Confia nos filtros RTK no nível do projeto sem validação.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`  | ✓               | Inicia o servidor WebSocket do painel em tempo real durante a importação (porta 20132 por padrão).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`  |                 | Permite que o Codex use o transporte Responses-over-WebSocket. Quando desativado, o Codex recorre ao HTTP Responses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`  |                 | Permite que o Codex use o transporte JSON-RPC via WebSocket do app-server local (codexTransport=app-server). Quando desativado, as conexões configuradas para usar o app-server recorrem aos outros transportes do Codex.                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`  |                 | Encaminha solicitações cujo orçamento foi esgotado para o provedor/modelo alternativo gratuito de emergência. (Consulte [Fallback de orçamento de emergência](#emergency-budget-fallback) abaixo.)                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false` |                 | Habilita uma nova tentativa antecipada e transparente para streams SSE truncados do upstream antes que quaisquer bytes da resposta cheguem ao cliente.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false` |                 | Permite que a recuperação do stream faça uma nova solicitação e concatene uma resposta depois que os bytes já tiverem chegado ao cliente.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false` |                 | Torna segura a continuação de chamadas de ferramenta no meio do stream: nunca retoma um stream interrompido depois que uma chamada de ferramenta tiver sido emitida (em andamento ou já concluída com finish_reason tool_calls) e encerra após uma continuação vazia, em vez de consumir todo o orçamento. Desativado: comportamento da versão de lançamento.                                                                                                                                                                                                                                                            |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false` |                 | Faz failover uma vez para uma conexão irmã quando um stream SSE é encerrado antes de emitir qualquer frame útil e a tentativa limitada na mesma conexão se esgota; sem uma conexão irmã utilizável, o erro 502 `STREAM_EARLY_EOF` original é retornado. Desativado por padrão: o EOF antecipado permanece terminal após a nova tentativa na mesma conexão.                                                                                                                                                                                                                                                               |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`  |                 | Inclui campos de nome amigáveis para exibição nas respostas de `/v1/models`. Desative para clientes que esperam somente IDs de modelo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`  |                 | Controla como os IDs de modelo recebem prefixos em /v1/models. 'dual' (padrão) emite tanto os prefixos de alias quanto os prefixos canônicos de ID do provedor para compatibilidade retroativa. 'alias' emite apenas o prefixo de alias curto (por exemplo, ds-web/model, não deepseek-web/model). 'canonical' emite apenas o prefixo completo de ID do provedor. Valores: `dual`, `alias`, `canonical`.                                                                                                                                                                                                                 |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`  |                 | Habilita a sincronização periódica de ELO da classificação da Arena AI para os rankings de inteligência dos modelos.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false` |                 | Anuncia ids espelho `claude/<provider>/<model>` em `/v1/models` para que a descoberta de modelos do gateway do Claude Code liste modelos que não sejam Claude. Nível global do controle de três níveis (a variável de ambiente tem precedência sobre a substituição do painel). Consulte [Configuração do Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker).                                                                                                                                                                                         |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`  |                 | Controle mestre dos aliases de gateway no-think/<provider>/<model>. Ativado (padrão): /v1/models anuncia uma variante sem raciocínio para cada modelo Claude elegível com capacidade de raciocínio, e um id no-think/ enviado em uma solicitação é resolvido de volta para o modelo real com o raciocínio suprimido. Desativado: nenhuma variante é anunciada e um id no-think/ é tratado como qualquer outro id de modelo desconhecido. A opção de adesão/recusa por modelo ModelSpec.noThinkingAlias ainda se aplica enquanto este controle estiver ativado.                                                           |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false` |                 | Desativa a geração de variantes de nível de raciocínio (por exemplo, -low, -medium, -high) no catálogo de /v1/models.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false` | ✓               | Ativa faixas virtuais adaptativas de admissão por locatário para o despacho do provedor (#9654): o pico de um locatário não faz mais com que outro receba erros 503. A variável de ambiente OMNIROUTE_CHAT_VIRTUAL_LANES tem precedência sobre esta substituição do painel; as alterações entram em vigor quando o servidor é reiniciado.                                                                                                                                                                                                                                                                                |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false` |                 | Anuncia ids espelho <gateway-alias>/<model> em /v1/models para modelos cujo proprietário canônico não possui uma credencial ativa, mas que são roteados por um gateway de passagem com uma credencial ativa. Aviso: quando ativado globalmente, adiciona entradas ao catálogo para todos os clientes.                                                                                                                                                                                                                                                                                                                    |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false` |                 | Ativa a detecção de saldo para nós agregadores compatíveis com New-API / One-API / Sub2API. Quando ativada, os nós compatíveis com o sinalizador de agregador definido informarão seu saldo no painel e no roteamento de pré-verificação de cota.                                                                                                                                                                                                                                                                                                                                                                        |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false` |                 | Continua as chamadas de ferramentas sem streaming controladas pelo servidor até que o modelo retorne uma resposta utilizável pelo cliente.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false` |                 | As estatísticas de pesquisa e as pesquisas recentes contabilizam apenas provedores que ainda possuem uma conexão ativa (provedores sem chave, como duckduckgo-free, são sempre contabilizados). Quando desativado, mantém todas as linhas de pesquisa retidas com um id de provedor.                                                                                                                                                                                                                                                                                                                                     |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false` |                 | Páginas de provedores do painel: exibe o selo Grátis somente mediante sinais que o provedor reconhece — desconsidera a heurística baseada no nome de exibição, campos de gratuidade não booleanos e sufixos :free em provedores registrados sem um nível gratuito documentado. Quando desativado, mantém a regra histórica do selo.                                                                                                                                                                                                                                                                                      |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false` |                 | Em respostas agregadas 429/503 de indisponibilidade, omite `Retry-After` quando não há um momento futuro concreto conhecido para uma nova tentativa (em vez de usar um valor sintético de 1s), adiciona `error.retry_after_provenance` (`signal` \| `none`) e permite que os fluxos de esgotamento de combinações leiam indicações textuais de nova tentativa em corpos JSON e de texto simples recebidos do upstream. O campo aparece apenas nas respostas criadas por `unavailableResponse()`; outros corpos 429/503 permanecem inalterados.                                                                           |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false` |                 | Quando um destino de combinação `priority` marcado como fallback apenas em caso de esgotamento de cota interrompe a combinação por uma causa que comprovadamente não está relacionada à cota (disjuntor do provedor aberto, desvio por latência preditiva), responde com 502 em vez do 503 que aparenta estar relacionado à cota. Interrupções por bloqueio, período de espera, indisponibilidade, esgotamento e limite de simultaneidade continuam retornando 503.                                                                                                                                                      |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false` |                 | Um 401 simples da Mistral (`{"detail":"Unauthorized"}`, sem sinal explícito de autenticação) é idêntico para uma chave revogada e para uma cota esgotada. Quando ativado, coloca a conexão em período de espera em vez de estacioná-la como `expired`, no máximo 3 vezes por hora por conexão; a próxima ocorrência a estaciona, de modo que uma chave revogada ainda converge para esse estado. Desativado por padrão: todo 401 simples da Mistral estaciona a conexão como antes.                                                                                                                                      |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false` |                 | Busca o catálogo de modelos ativo da xAI para conexões `xai-oauth` em `https://api.x.ai/v1/models` usando o token bearer OAuth, em vez da base estática congelada. Desativado por padrão: `xai-oauth` continua fornecendo a base estática sem alterações. Em qualquer erro de resolução, a descoberta recorre à base (não foi verificado se x.ai aceita um bearer OAuth neste endpoint).                                                                                                                                                                                                                                 |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false` |                 | Permite que a varredura de limpeza automática exclua trabalhos da Batch API em estado terminal (concluídos/com falha/cancelados/expirados) com mais de `OMNIROUTE_BATCH_RETENTION_DAYS`, juntamente com seus checkpoints por linha, e limpe o conteúdo BLOB de arquivos enviados após o respectivo `expires_at`. Desativado por padrão: toda instalação existente mantém esses dados exatamente como antes até que um operador opte por ativá-lo. A rota `DELETE /api/v1/batches/delete-completed` acionada pelo operador não é afetada em nenhum dos casos — ela é um contrato de API pública separado e incondicional. |

### CLI (5)

| Chave                                 | Tipo    | Padrão  | Reinício | Descrição                                                                                                                                                                                                                                             |
| ------------------------------------- | ------- | ------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | boolean | `false` | ✓        | Ativa o modo de compatibilidade para todos os clientes CLI.                                                                                                                                                                                           |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false` |          | Ativa a camada de compatibilidade de aliases de modelos.                                                                                                                                                                                              |
| `PRICING_SYNC_ENABLED`                | boolean | `false` |          | Ativa a sincronização automática de dados de preços (também requer a variável de ambiente `PRICING_SYNC_ENABLED`).                                                                                                                                    |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false` |          | Após a sincronização dos modelos de um provedor, (re)grava automaticamente os arquivos de perfil ~/.codex/*.config.toml com base no catálogo ativo. Nunca altera a configuração ativa/padrão do Codex. Desativado por padrão.                         |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false` |          | Após a sincronização dos modelos de um provedor, (re)grava automaticamente os perfis do Claude Code em ~/.claude/profiles/<name>/settings.json com base no catálogo ativo. Nunca altera a configuração ativa/padrão do Claude. Desativado por padrão. |

### Integridade (5)

| Chave                                     | Tipo    | Padrão  | Descrição                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------- | ------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false` | Desabilita o endpoint de verificação de integridade da instância local.                                                                                                                                                                                                                                                        |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false` | Desabilita a verificação de integridade da validação de token.                                                                                                                                                                                                                                                                 |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false` | Habilita o acesso à rede no ambiente sandbox de habilidades.                                                                                                                                                                                                                                                                   |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false` | Na varredura de integridade dos proxies, uma sondagem recusada pelo destino (401/403/429) zera a sequência de falhas consecutivas do proxy. Desabilitado por padrão: uma recusa permanece neutra (#10654). Uma resposta 5xx permanece inconclusiva em ambos os casos; uma recusa nunca remove, desabilita ou reativa um proxy. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false` | Executa a verificação de integridade/saúde do banco de dados na inicialização depois que o servidor começa a aceitar solicitações (via `setImmediate`), em vez de bloquear a inicialização até sua conclusão (#13717). Desabilitado por padrão: a inicialização é bloqueada exatamente como antes deste PR.                    |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` e seu alias legado
> `INJECTION_GUARD_BLOCK_THRESHOLD` ajustam o modo `block` de
> `INJECTION_GUARD_MODE`, mas são variáveis de ambiente comuns lidas por
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts),
> e não sinalizadores de recurso: elas não têm substituição no banco de dados nem opção de alternância no painel. Consulte
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> A coluna `Restart` identifica sinalizadores com `requiresRestart: true` — o valor é
> persistido instantaneamente, mas só entra em vigor após o processo ser recarregado. Sinalizadores
> de enumeração rejeitam qualquer valor fora do conjunto permitido (validado no lado do servidor em
> `setFeatureFlagOverride()` e no manipulador REST `PUT`).

---

## Alternando Flags

### Dashboard

Navegue até **Dashboard → Settings → Feature Flags**
(`/dashboard/settings/feature-flags`). A grade
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
oferece suporte a:

- **Pesquisa** por chave ou descrição e **filtro** por categoria (além de uma
  visualização sintética **Requires Restart**).
- Um **botão de alternância** para flags booleanas e uma **lista suspensa** para flags enum
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- Um **selo de origem** por flag — `DB`, `ENV` ou `DEF` — mostrando de onde veio o
  valor efetivo.
- Um botão **Reset** (exibido apenas para flags provenientes de `DB`) para remover a substituição
  e um botão **Reset All Overrides** na parte inferior.
- Um banner **Restart Server** quando uma flag `requiresRestart` é alterada.

### API REST

Todas as operações passam por uma única rota:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
Cada método exige uma sessão autenticada no dashboard (caso contrário, retorna `401`).

#### `GET /api/settings/feature-flags`

Retorna todas as flags com seus valores efetivos, origens e um resumo.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Require API Key",
      "description": "Require an API key for all incoming requests",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... todas as 72 flags
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

Define ou remove uma única substituição. Corpo: `{ key: string; value?: string }`.
Omitir `value` remove a substituição (restaurando o valor de env / padrão).

```bash
# Define uma substituição no DB
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# Remove a substituição (sem "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

A resposta retorna o novo `effectiveValue`/`source`, o `previousValue`/
`previousSource` e `requiresRestart`. Chaves desconhecidas e valores enum fora do intervalo
são rejeitados com `400`.

#### `DELETE /api/settings/feature-flags`

Remove **todas** as substituições do DB de uma só vez, restaurando cada flag para seu valor de env / padrão.
Retorna `{ cleared: <count>, message: "..." }`.

> [!NOTE]
> Flags com `requiresRestart: true` só entram em vigor após o processo ser recarregado.
> O fluxo de reinicialização do dashboard chama `POST /api/restart` e depois consulta repetidamente
> `GET /api/health/ping` até que o servidor volte a funcionar.

---

## Fallback Emergencial de Orçamento

`OMNIROUTE_EMERGENCY_FALLBACK` (categoria `runtime`, padrão `true`) controla o
caminho de fallback gratuito emergencial em
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts).
Quando habilitado, as solicitações que esgotam seu orçamento são encaminhadas para um
provedor/modelo de fallback gratuito em vez de falharem imediatamente. Defina-o como `false` (ou `0`) — por meio
do botão de alternância no dashboard, de uma substituição no DB ou da variável de ambiente
`OMNIROUTE_EMERGENCY_FALLBACK` — para desabilitar o comportamento e permitir que solicitações cujo orçamento
foi esgotado falhem. (Disponibilizado como um botão de alternância no dashboard nos PRs #3741 / #3752.)

---

## Veja também

- [Referência de variáveis de ambiente](./ENVIRONMENT.md) — a maioria das flags possui uma
  variável de ambiente com o mesmo nome documentada ali (a substituição definida no DB tem
  precedência sobre ela).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — fonte oficial de todas as flags.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — lógica de resolução (`resolveFeatureFlag`, `isFeatureFlagEnabled`,
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — persistência das substituições
  no DB no namespace `feature_flags` da tabela `key_value`.
