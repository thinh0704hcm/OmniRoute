# Agent Protocols Guide (Português (Brasil))

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENT_PROTOCOLS_GUIDE.md)

---

> **Origem:** `src/lib/{a2a,acp,cloudAgent}/`, `src/app/api/{a2a,acp,cloud}/`, `src/app/api/v1/agents/`
> **Última atualização:** 2026-06-28 — v3.8.40

O OmniRoute disponibiliza três interfaces diferentes relacionadas a agentes. À primeira vista, elas parecem semelhantes, mas resolvem problemas distintos. Use esta página para escolher a opção certa.

## Resumo

| Interface                         | Ideal para                                                                                                                                     | Transporte                                        | Padrão                          |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------- |
| **A2A — Agente para Agente**      | Colaboração entre agentes com agentes pares compatíveis com o protocolo A2A                                                                    | JSON-RPC 2.0 sobre HTTP                           | A2A v0.3 (especificação aberta) |
| **ACP — Registro de agentes CLI** | Detectar / registrar / iniciar agentes de programação CLI instalados na máquina do usuário (Cursor, Cline, Codex CLI, Claude Code, Aider etc.) | HTTP REST                                         | Específico do OmniRoute         |
| **Agentes na nuvem**              | Enviar tarefas de programação de longa duração para serviços externos na nuvem (Codex Cloud, Devin, Jules, Cursor Cloud)                       | HTTP REST + tarefas persistidas em banco de dados | Específico do OmniRoute         |

As três opções são independentes — escolha qualquer subconjunto delas.

## Árvore de decisão

```
Você precisa que um serviço na nuvem execute trabalho fora desta máquina (Codex Cloud / Cursor Cloud / Devin / Jules)?
├─ SIM → Agentes na nuvem (POST /api/v1/agents/tasks)
└─ NÃO → Continue
    │
    Você tem um agente par compatível com A2A que deseja colaborar?
    ├─ SIM → A2A (POST /a2a)
    └─ NÃO → Continue
        │
        Você precisa listar / configurar agentes de programação CLI instalados localmente?
        ├─ SIM → ACP (GET /api/acp/agents)
        └─ NÃO → Use apenas /v1/chat/completions
```

## 1. A2A — Agente para Agente

**Especificação:** [A2A v0.3](https://a2a-protocol.org)
**Endpoint do OmniRoute:** `POST /a2a` (JSON-RPC 2.0)
**Cartão do agente:** `GET /.well-known/agent.json`

### Quando usar

- Ao criar um sistema multiagente no qual o OmniRoute é um dos agentes pares
- Para disponibilizar a inteligência de roteamento do OmniRoute (roteamento inteligente, gerenciamento de cotas etc.) a agentes em frameworks como o Google ADK ou em malhas genéricas de agentes
- Para encapsular o OmniRoute em uma interface padronizada de descoberta + invocação

### Métodos

- `message/send` — envia uma mensagem e recebe uma resposta síncrona
- `message/stream` — envia uma mensagem e recebe eventos de progresso transmitidos via SSE
- `tasks/get` — lê uma tarefa por ID
- `tasks/cancel` — cancela uma tarefa em execução

### Habilidades integradas (6)

- `smart-routing` — encaminha um prompt pela combinação ideal
- `quota-management` — informa o estado da cota de cada provedor
- `provider-discovery` — lista os provedores instalados e seus recursos
- `cost-analysis` — estima o custo de uma solicitação/conversa
- `health-report` — agrega o estado de disjuntor, espera e bloqueio de cada provedor
- `list-capabilities` — enumera as habilidades e os metadados disponíveis do agente

### Aprofundamento

Consulte [A2A-SERVER.md](./A2A-SERVER.md) para obter detalhes sobre o transporte, a estrutura do cartão do agente, a configuração de TTL das tarefas e o modelo para adicionar novas habilidades.

## 2. ACP — Registro de agentes CLI

**Endpoint do OmniRoute:** `GET /api/acp/agents`
**Código-fonte:** `src/lib/acp/{index,manager,registry}.ts`

### O que é

O ACP é o **inventário local de agentes CLI** do OmniRoute. Ele detecta quais CLIs de programação estão instaladas no host (Cursor, Cline, Claude Code, Codex CLI, Continue etc.), identifica suas versões e as apresenta no painel para que o usuário possa configurar cada CLI para apontar para o OmniRoute.

Este NÃO é um protocolo externo — é um registro interno que alimenta a interface de "Ferramentas CLI" e o rastreamento de impressões digitais das CLIs (consulte [CLI-TOOLS.md](../reference/CLI-TOOLS.md)).

### O que faz

- Verifica o host em busca de binários de CLI instalados (usa `which` / `where`, conforme o sistema operacional)
- Lê a versão de cada CLI (executa `<bin> --version`)
- Opcionalmente, aceita agentes personalizados definidos pelo usuário (caminho do binário + verificação de versão + argumentos de inicialização)
- Persiste agentes personalizados nas configurações
- Retorna a lista unificada ao painel

### API REST

| Endpoint          | Método | Descrição                                                                 | Autenticação |
| ----------------- | ------ | ------------------------------------------------------------------------- | ------------ |
| `/api/acp/agents` | GET    | Lista agentes detectados + personalizados (contagens instalados/total)    | Chave de API |
| `/api/acp/agents` | POST   | Adiciona/atualiza/remove um agente personalizado (ação definida no corpo) | Chave de API |

Formato do corpo para POST (`customAgentBodySchema` em `src/app/api/acp/agents/route.ts`):

```json
{
  "action": "add|update|remove",
  "id": "cursor",
  "name": "Cursor",
  "binary": "/usr/local/bin/cursor",
  "versionCommand": "--version",
  "providerAlias": "cursor",
  "spawnArgs": ["--api-base", "http://localhost:20128"],
  "protocol": "stdio"
}
```

### Casos de uso

- A página "Ferramentas CLI" do painel lista o que está instalado e ajuda você a configurar cada ferramenta para apontar para o OmniRoute
- Agentes personalizados permitem que usuários avançados registrem CLIs internas/proprietárias que o OmniRoute não reconhece por padrão
- O resultado da detecção alimenta a matriz de impressões digitais `cli-tools`

### Quando NÃO usar o ACP

- O ACP não _executa_ tarefas. Ele apenas detecta e configura CLIs. Para realmente invocar uma CLI, você deve iniciá-la por conta própria com as variáveis de ambiente fornecidas pelo OmniRoute (`OPENAI_BASE_URL`, `OPENAI_API_KEY` etc.).

## 3. Agentes de nuvem

**Endpoints do OmniRoute:** `/api/v1/agents/tasks/*` (ciclo de vida) + `/api/cloud/*` (infraestrutura)
**Código-fonte:** `src/lib/cloudAgent/`

### O que é

Uma interface uniforme para agentes de programação em nuvem de terceiros. Você envia um prompt + URL do repositório, o OmniRoute encaminha a solicitação ao agente de nuvem correto, consulta periodicamente o status e retorna os resultados.

### Agentes compatíveis (4, todos confirmados em `src/lib/cloudAgent/agents/`)

- `codex-cloud` — OpenAI Codex Cloud
- `devin` — Cognition Devin
- `jules` — Google Jules

### Ciclo de vida

```
POST /api/v1/agents/tasks
  → BaseAgent.createTask() por classe de agente
  → o serviço externo inicia o trabalho
  → uma linha de tarefa é criada no banco de dados (cloud_agent_tasks)
  ↓
GET /api/v1/agents/tasks/[id]
  → sincronização tardia do status com o provedor
  → retorna o status atual + plano + registro de atividades
  ↓
POST /api/v1/agents/tasks/[id]   (ação: "approve" | "message" | "cancel")
  → encaminha ao provedor (ou marca como cancelada localmente)
  ↓
DELETE /api/v1/agents/tasks/[id]
  → cancelamento local
```

### Autenticação

⚠️ **Todos os endpoints `/api/v1/agents/tasks/*` exigem autenticação de gerenciamento** (commit `588a0333`). Chamadores que usam somente Bearer recebem 401 desde a v3.8.0.

### Análise detalhada

Consulte [CLOUD_AGENT.md](./CLOUD_AGENT.md) para ver o contrato `CloudAgentBase`, detalhes específicos de cada agente, detalhes do esquema e endpoints da infraestrutura de credenciais.

## Comparação: A2A vs Agentes de Nuvem

Ambos têm "tarefas de longa duração", mas em camadas diferentes:

| Aspecto                    | A2A                                                                                       | Agentes de Nuvem                                          |
| -------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| Padrão                     | Open A2A v0.3                                                                             | Específico do OmniRoute                                   |
| Onde o processamento roda  | Dentro do OmniRoute (usa combinações configuradas)                                        | Externamente (servidores Codex / Devin / Jules)           |
| Duração da tarefa          | TTL padrão de 5 min (configurável em `TaskManager`)                                       | De minutos a horas                                        |
| Compatível com repositório | Não (envia apenas prompts)                                                                | Sim (URL do repositório + branch)                         |
| Caso de uso                | Colaboração entre agentes, roteamento inteligente como serviço                            | Delegar "implementar a funcionalidade X no repositório Y" |
| Autenticação               | `OMNIROUTE_API_KEY` opcional para `/a2a`; gerenciamento para auxiliares REST `/api/a2a/*` | Sempre gerenciamento                                      |

## Exemplos de Integração

### Descobrir os recursos A2A do OmniRoute

```bash
curl http://localhost:20128/.well-known/agent.json
```

Retorna o Agent Card com todas as 6 habilidades, transportes e versão.

### Chamar o OmniRoute como um agente A2A

```bash
curl -X POST http://localhost:20128/a2a \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "message/send",
    "params": {
      "messages": [{"role": "user", "content": "Route this prompt"}],
      "skill": "smart-routing"
    },
    "id": 1
  }'
```

### Listar agentes de CLI instalados via ACP

```bash
curl http://localhost:20128/api/acp/agents \
  -H "Authorization: Bearer <api-key>"
```

### Adicionar um agente de CLI personalizado

```bash
curl -X POST http://localhost:20128/api/acp/agents \
  -H "Authorization: Bearer <api-key>" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "add",
    "id": "my-custom-cli",
    "name": "My Custom CLI",
    "binary": "/opt/mycli/bin/mycli",
    "versionCommand": "--version",
    "providerAlias": "openai"
  }'
```

### Enviar uma tarefa de Agente de Nuvem

```bash
curl -X POST http://localhost:20128/api/v1/agents/tasks \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{
    "providerId": "devin",
    "prompt": "Implement feature X in repo Y",
    "source": {
      "repoName": "user/repo",
      "repoUrl": "https://github.com/user/repo",
      "branch": "main"
    }
  }'
```

### Consultar o status da tarefa na nuvem

```bash
curl http://localhost:20128/api/v1/agents/tasks/<task-id> \
  -H "Cookie: auth_token=..."
```

## Quando Usar Cada Opção

- **Frontend de chatbot / copiloto** → `/v1/chat/completions` (compatível com OpenAI — não é um protocolo de agentes)
- **Colaboração entre múltiplos agentes** → A2A
- **Listagem de CLIs locais no painel** → ACP
- **Delegação de tarefas de programação de longa duração para serviços de nuvem** → Agentes de Nuvem

## Arquitetura interna

```
                ┌─────────────────────┐
                │   Núcleo OmniRoute  │
                └─────────────────────┘
                  ↑       ↑        ↑
        ┌─────────┘       │        └─────────┐
        │                 │                  │
    ┌───────┐        ┌─────────┐       ┌────────────┐
    │  A2A  │        │   ACP   │       │ Agentes na │
    │ (/a2a)│        │ (/acp)  │       │   nuvem    │
    └───────┘        └─────────┘       │ (/v1/agents│
        │                 │            │  /tasks)   │
        ↓                 ↓            └────────────┘
   Agentes pares     Binários locais         │
   externos que      de CLI no               ↓
   usam A2A v0.3     host               Codex Cloud, Cursor,
                                        Devin, Jules
```

## Veja também

- [A2A-SERVER.md](./A2A-SERVER.md) — Análise detalhada do A2A
- [CLOUD_AGENT.md](./CLOUD_AGENT.md) — Análise detalhada dos agentes na nuvem
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — Integrações externas de CLI (usam ACP)
- [SKILLS.md](./SKILLS.md) — Framework de habilidades (diferente das habilidades A2A — sandbox de execução local)
- [API_REFERENCE.md](../reference/API_REFERENCE.md#agents-protocol) — referência dos endpoints
- Fonte: `src/lib/{a2a,acp,cloudAgent}/`
