# Stealth Guide (Tiếng Việt)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/security/STEALTH_GUIDE.md) · 🇩🇰 [da](../../../da/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇮🇩 [id](../../../id/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇮🇹 [it](../../../it/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Nguồn chuẩn:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Cập nhật lần cuối:** 2026-09-02 — v3.8.51
> **Đối tượng:** Các kỹ sư duy trì những tích hợp ẩn mình dành riêng cho từng nhà cung cấp.

OmniRoute tích hợp với các nhà cung cấp có hệ thống biên chủ động nhận diện dấu vân tay của các client không chính thức (TLS JA3/JA4, thứ tự header, cấu trúc phần thân JSON, token toàn vẹn). Trang này mô tả các bề mặt ẩn mình mà OmniRoute cung cấp và nơi chúng được triển khai.

## Thông báo Pháp lý và Đạo đức

Các tính năng ẩn mình tồn tại để OmniRoute có thể hoạt động như một lớp tương thích giữa các tài khoản chính thức do người dùng sở hữu (Claude Code CLI, Codex, Antigravity, Cursor, v.v.) và API hợp nhất của OmniRoute. Chúng **không** nhằm mục đích né tránh cơ chế phát hiện gian lận, chia sẻ thông tin xác thực hoặc vi phạm Điều khoản Dịch vụ của nhà cung cấp. Đội ngũ bảo trì yêu cầu các đơn vị vận hành tuân thủ ToS của thượng nguồn mà họ đã chấp thuận khi tạo tài khoản.

---

## Lớp Nhận diện Dấu vân tay TLS

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Các phiên `wreq-js` duy trì lâu dài được tạo theo nhu cầu cho từng phạm vi tài khoản và proxy đã phân giải. `TlsClient` dùng chung toàn tiến trình quản lý một pool tối đa 128 phiên giả lập **Chrome 124 trên macOS** cho các thượng nguồn nằm sau Cloudflare. `TlsClient.fetch()` dừng an toàn khi runtime native không khả dụng; bên gọi có thể chủ động chọn một phương án dự phòng bên ngoài wrapper này.

- Hồ sơ phiên: `browser: "chrome_124", os: "macos"`
- Phân giải proxy (theo mức ưu tiên): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (bao gồm cả dạng chữ thường)
- Thời gian chờ: `TLS_CLIENT_TIMEOUT_MS` (kế thừa từ `FETCH_TIMEOUT_MS`, mặc định 600000)
- Response của `wreq-js` tương thích với fetch (`headers`, `text()`, `json()`, `clone()`, `body`).
- Cơ chế giám sát byte đầu tiên (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()` phân giải ngay khi nhận được các header từ thượng nguồn, vì vậy chỉ riêng `TLS_CLIENT_TIMEOUT_MS` không thể giới hạn một phần thân không bao giờ trả về byte đầu tiên. `guardTlsFirstByte()` cho lần `read()` đầu tiên của phần thân chạy đua với `TLS_FIRST_BYTE_WATCHDOG_MS` (mặc định `10000`, đặt thành `0` để vô hiệu hóa); phần thân hoạt động bình thường không bị ảnh hưởng, trong khi phần thân bị đình trệ sẽ khiến trình đọc wreq bị hủy và cho phép logic dự phòng TLS hiện có của `proxyFetch` chuyển tiếp sang bộ điều phối trực tiếp/proxy (một yêu cầu không an toàn khi phát lại, chẳng hạn như POST có phần thân, vẫn sẽ phát sinh lỗi thay vì được âm thầm thử lại).

### Phương thức truyền tải của nhà cung cấp dùng cookie web — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts` là adapter dùng chung cho năm phương thức truyền tải chuyên biệt dùng cookie web bên dưới. Mỗi wrapper nhà cung cấp tối giản chọn một hồ sơ trình duyệt/hệ điều hành. Adapter sử dụng trình tải runtime wreq duy nhất và pool truyền tải trong `open-sse/utils/tlsClient.ts`, được định danh theo hồ sơ + hệ điều hành + proxy đã phân giải, trong khi mọi yêu cầu đều sử dụng `cookieMode: "ephemeral"`. Do đó, các tài khoản và yêu cầu dùng chung kết nối ở cấp truyền tải nhưng không bao giờ dùng chung phiên wreq hoặc kho cookie.

| Nhà cung cấp | Hồ sơ         | Hệ điều hành giả lập | Chính sách EOF của luồng                   |
| ------------ | ------------- | -------------------- | ------------------------------------------ |
| Claude       | `chrome_146`  | Linux                | bao gồm `[DONE]`                           |
| Perplexity   | `firefox_148` | macOS                | bao gồm `event: end_of_stream`             |
| Grok         | `chrome_146`  | Linux                | loại trừ `[DONE]`                          |
| Notion       | `chrome_146`  | Windows              | bao gồm `[DONE]`                           |
| LMArena      | `chrome_146`  | Windows              | không có dấu kết thúc; đóng khi native EOF |

- Quá trình truyền trực tiếp sử dụng `ReadableStream` của phản hồi native; không tạo tệp tạm hoặc tiến trình sidecar.
- Tối đa 256 byte đầu tiên được kiểm tra trước khi công khai luồng. Các nhà cung cấp SSE sẽ lưu vào bộ đệm các lỗi không phải SSE; Grok/LMArena ánh xạ các thử thách Cloudflare thành `403` và các trang trung gian HTML thành `502`.
- Thời gian chờ của yêu cầu native vẫn được bao bọc bởi một thời hạn cứng tuyệt đối của JS. Khi xảy ra treo, chỉ phương thức truyền tải tương ứng với hồ sơ/hệ điều hành/proxy bị vô hiệu hóa và đóng lại trước khi được tạo lại ở yêu cầu tiếp theo.
- Thứ tự ưu tiên phân giải proxy là `proxyUrl` theo từng lần gọi → ngữ cảnh tài khoản/dashboard trong phạm vi yêu cầu → `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (bao gồm các biến thể chữ thường). Lỗi phân giải sẽ dừng an toàn thay vì làm rò rỉ một kết nối trực tiếp. LMArena chủ đích phân giải dựa trên `arena.ai`.
- `byteResponse` trả về một URL `data:` được định kiểu nội dung mà không gây hỏng UTF-8.
- Các lỗi gồm `TlsClientUnavailableError` (package/addon không khả dụng), `TlsClientHangError` (vượt quá thời hạn) và `WreqTransportCapacityError` (mã lỗi dung lượng phiên dùng chung) khi toàn bộ 128 vị trí hồ sơ/hệ điều hành/proxy có giới hạn đang hoạt động hoặc đang đóng.

Phiên `TlsClient` chung ở trên vẫn được chuyên biệt hóa cho trạng thái cookie duy trì lâu dài do trình duyệt hỗ trợ. Cả hai luồng xử lý đều tái sử dụng một trình tải mô-đun wreq được lưu vào bộ nhớ đệm và hook vòng đời tiến trình; các pool của chúng vẫn tách biệt vì thời gian tồn tại cookie được thiết kế khác nhau.

Các hồ sơ này được package đã ghim hỗ trợ, nhưng khả năng được WAF thực tế chấp nhận có thể thay đổi độc lập với các bài kiểm thử hợp đồng cục bộ. Hãy xác thực các thay đổi dấu vân tay bằng một tài khoản thực đã được cấp quyền rõ ràng trước khi tuyên bố đạt mức tương đương với trình duyệt thượng nguồn.

---

## Gói ẩn mình Claude Code

Khi `cliCompatMode` được bật, OmniRoute định hình lại các yêu cầu Claude gửi đi để chúng không thể phân biệt được với lưu lượng từ `claude-cli`. Ba mô-đun phối hợp với nhau:

### `claudeCodeFingerprint.ts`

Tính toán dấu vân tay `cc_version` gồm 3 ký tự được nhúng trong tiêu đề thanh toán:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (được mã hóa cứng; khớp với ứng dụng khách chính thức)
- Đầu vào: các ký tự tại chỉ mục 4, 7, 20 của văn bản tin nhắn người dùng đầu tiên + chuỗi phiên bản
- Đầu ra: tiền tố hex gồm 3 ký tự

### `claudeCodeCCH.ts` (Hàm băm nội dung ứng dụng khách)

Kiểm tra tính toàn vẹn phía máy chủ mà Claude Code CLI chính thức tính toán thông qua Bun/Zig. OmniRoute triển khai lại bằng `xxhash-wasm`:

1. Tuần tự hóa phần thân với phần giữ chỗ `cch=00000;`
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. Chuỗi hex chữ thường gồm 5 ký tự, được đệm bằng số 0
4. Thay thế `cch=00000;` bằng token đã tính toán

Các hằng số:

- Seed: `0x6e52736ac806831e`
- Mẫu: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Chèn một **ký tự nối có độ rộng bằng 0** Unicode (`U+200D`) sau ký tự đầu tiên của các tên ứng dụng khách "nhạy cảm" để bộ lọc thượng nguồn không thể tìm chúng bằng grep. Danh sách từ mặc định:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Áp dụng cho: các khối `system`, toàn bộ `messages[].content` và `tools[].description` / `tools[].function.description`. Người vận hành có thể ghi đè thông qua `setSensitiveWords()`.

### `claudeCodeCompatible.ts` — các nhà cung cấp `anthropic-compatible-cc-*`

Dành cho các relay Anthropic bên thứ ba chỉ chấp nhận lưu lượng "Claude Code thực":

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"` theo mặc định
- Nút chuyển "Bật bản beta ẩn nội dung suy luận" cho từng kết nối sẽ thêm `redact-thinking-2026-02-12` khi một thượng nguồn Tương thích CC yêu cầu cụ thể các luồng suy luận đã được ẩn
- Nút chuyển "Bật hiển thị suy luận tóm tắt" cho từng kết nối sẽ lưu `providerSpecificData.requestDefaults.summarizeThinking` và thêm `display: "summarized"` vào các yêu cầu suy luận Tương thích CC chưa đặt chế độ hiển thị
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (họ Opus/Sonnet 4.x)
- Đường dẫn mặc định: `/v1/messages?beta=true`

Các mô-đun liên quan trong cùng gói:

- `claudeCodeConstraints.ts` — các quy tắc về temperature + cache-control
- `claudeCodeToolRemapper.ts` — ánh xạ lại tên công cụ
- `claudeCodeExtraRemap.ts` — chuẩn hóa payload bổ sung

---

## Ẩn mình Antigravity

Các yêu cầu Antigravity giữ nguyên từng byte văn bản của bên gọi. OmniRoute không chèn ký tự có độ rộng bằng 0 vào prompt hoặc đổi tên/chèn công cụ để bắt chước một ứng dụng khách IDE.

### `antigravityHeaderScrub.ts`

Loại bỏ các dấu hiệu của Stainless SDK (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`) trước khi chuyển tiếp.

### ⚠️ Rủi ro: `ANTIGRAVITY_CREDITS=always` (điểm nóng gây khóa tài khoản)

`ANTIGRAVITY_CREDITS=always` (được sử dụng bởi `open-sse/executors/antigravity.ts`) định tuyến **mọi** yêu cầu qua phần tín dụng vượt mức Antigravity AI Credit Overages (tín dụng Google trả phí), thay vì để hạn ngạch gói miễn phí của Google giới hạn hoạt động. Điều này được ghi nhận trong tài liệu như một tính năng, nhưng đây **là loại báo cáo vi phạm Điều khoản dịch vụ phổ biến nhất mà chúng tôi ghi nhận** — nhiều tài khoản Google Ultra đã bị khóa với lỗi `403 / "service disabled for ToS violation" / insufficient_quota` sau khi chạy trong vài giờ với `=always`.

Việc thực thi được tiến hành ở **phía Google**, OmniRoute không thể ngăn chặn. Tên biến môi trường và tài liệu hiện có khiến tùy chọn này có vẻ an toàn để bật; thực tế không phải vậy.

**Lý do điều này kích hoạt cơ chế phát hiện lạm dụng mạnh hơn so với việc chỉ sử dụng gói miễn phí:**

- Hoạt động chi tiêu tự động kéo dài trên một tài khoản Google duy nhất bị đánh dấu khác với trường hợp gói miễn phí đạt hạn ngạch rồi dừng lại.
- Tín dụng vượt mức không có giới hạn tốc độ, vì vậy một ứng dụng khách cấu hình sai có thể tiêu hết vài trăm USD trong vài phút và trông giống hoạt động bán lại khóa API hoặc lưu lượng bot.
- Nhiều người dùng OmniRoute đồng thời sử dụng tín dụng vượt mức từ cùng một IP bên ngoài sẽ làm tín hiệu này trầm trọng hơn.

**Cấu hình khuyến nghị:**

1. Giữ mặc định `ANTIGRAVITY_CREDITS=off` trừ khi người vận hành chấp nhận rõ ràng rủi ro về tín dụng trả phí và biện pháp xử lý tài khoản. `retry` gửi yêu cầu thông thường trước và chỉ chèn tín dụng tối đa một lần sau lỗi hạn ngạch 429 đủ điều kiện; `always` chèn tín dụng ngay trong yêu cầu đầu tiên.
2. **Phân bổ tải giữa các nhà cung cấp thông qua Auto-Combo** (`model: "auto"` hoặc combo `kr/glm/etc`) thay vì làm quá tải một tài khoản Antigravity duy nhất.
3. **Đặt giới hạn RPM cho từng kết nối** trong trang chỉnh sửa của nhà cung cấp Antigravity (Bảng điều khiển → Nhà cung cấp → Antigravity → kết nối → giới hạn tốc độ). 30–60 RPM là giới hạn trên hợp lý cho việc sử dụng liên tục.
4. **Sử dụng mạng thượng nguồn ổn định do người vận hành kiểm soát** và tránh dùng chung một tài khoản cho những người dùng hoặc khối lượng công việc không liên quan.
5. **Nếu bị khóa**: gửi kháng nghị qua `support.google.com` → "Khôi phục quyền truy cập Không gian làm việc/Tài khoản" kèm theo chính xác nội dung phản hồi `quota_exceeded` / `service disabled` mà Google đã gửi. Việc khôi phục không được đảm bảo.

Tài liệu tham chiếu về môi trường mô tả các tác động đối với tài khoản và chi phí của từng chế độ tín dụng.

Các điểm liên quan:

- `open-sse/executors/antigravity.ts` — đọc `process.env.ANTIGRAVITY_CREDITS`
- `src/lib/oauth/providers/antigravity.ts` — hệ thống kết nối thông tin xác thực
- Báo cáo sự cố ban đầu: Thảo luận [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## Sổ đăng ký dấu vân tay CLI — `open-sse/config/cliFingerprints.ts`

Bảng theo từng nhà cung cấp cố định thứ tự **chính xác** của các header và trường trong phần thân JSON, được ghi lại từ các dấu vết mitmproxy của CLI chính thức. Hiện đã đăng ký: `codex`, `claude`, cùng các hồ sơ được tạo lúc chạy trong `providerHeaderProfiles.ts` dành cho `antigravity` và `github`.

```ts
interface CliFingerprint {
  headerOrder: string[]; // phân biệt chữ hoa chữ thường
  bodyFieldOrder: string[]; // các khóa JSON cấp cao nhất
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Bật/tắt theo từng nhà cung cấp thông qua biến môi trường (xem bên dưới). Khi bị tắt, các header/khóa phần thân xuất hiện theo bất kỳ thứ tự nào mà Node/JSON cung cấp — dễ bị nhận dạng dấu vân tay.

---

## Proxy MITM (Antigravity, Linux/macOS/Windows)

Đối với các CLI có tệp nhị phân không thể được chuyển hướng thông qua `OPENAI_BASE_URL`, OmniRoute chạy một proxy cục bộ có chức năng chấm dứt TLS. Các endpoint nằm trong `src/app/api/cli-tools/antigravity-mitm/`.

| Phương thức | Endpoint                                | Mục đích                                               |
| ----------- | --------------------------------------- | ------------------------------------------------------ |
| GET         | `/api/cli-tools/antigravity-mitm`       | Trạng thái — đang chạy, pid, dnsConfigured, certExists |
| POST        | `/api/cli-tools/antigravity-mitm`       | Khởi động MITM (yêu cầu `apiKey` + `sudoPassword`)     |
| DELETE      | `/api/cli-tools/antigravity-mitm`       | Dừng MITM                                              |
| GET         | `/api/cli-tools/antigravity-mitm/alias` | Liệt kê các bí danh mô hình                            |
| PUT         | `/api/cli-tools/antigravity-mitm/alias` | Lưu các bí danh mô hình cho một công cụ                |

Máy chủ đích bị chặn: **`daily-cloudcode-pa.googleapis.com`** (upstream của Antigravity).

### Trình tự khởi động (`src/mitm/manager.ts::startMitm`)

1. Tạo chứng chỉ tự ký thông qua `selfsigned` (RSA-2048, SHA-256, 1 năm) — `cert/generate.ts`
2. Cài đặt chứng chỉ vào kho tin cậy của hệ thống — `cert/install.ts`
3. Thêm mục hosts `127.0.0.1 daily-cloudcode-pa.googleapis.com` — `dns/dnsConfig.ts`
4. Khởi chạy `src/mitm/server.cjs` với `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (mặc định `443`)
5. Lưu PID vào `<DATA_DIR>/mitm/.mitm.pid`

### Phát hiện động kho tin cậy trên Linux — `cert/install.ts`

`getLinuxCertConfig()` duyệt qua danh sách ưu tiên và chọn thư mục tồn tại đầu tiên:

| Họ bản phân phối         | Thư mục                                     | Lệnh cập nhật            |
| ------------------------ | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu          | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS   | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                 | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Tên tệp chứng chỉ: `omniroute-mitm.crt`. Đối chiếu dấu vân tay thông qua `getCertFingerprint()` (SHA-1 của DER).

Ngoài ra, `updateNssDatabases()` cài đặt vào các cơ sở dữ liệu NSS theo từng người dùng khi `certutil` khả dụng: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb`, tất cả hồ sơ Firefox (bao gồm cả snap), với biệt danh **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** PowerShell với quyền nâng cao → `certutil -addstore Root`

### Xác thực

Tất cả endpoint MITM đều yêu cầu xác thực quản lý (`requireCliToolsAuth`). Mật khẩu sudo được lưu vào bộ nhớ đệm trong phạm vi mô-đun (không bao giờ trong `globalThis`) và bị xóa khi gọi `stopMitm()`.

---

## Ghi đè User-Agent — biến môi trường (`.env.example` mục 12)

| Biến                     | Mặc định                                                        |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.149.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

Được `open-sse/executors/base.ts::buildHeaders()` sử dụng thông qua tra cứu động. **Hãy tăng các phiên bản này khi nhà cung cấp phát hành phiên bản CLI mới** — các chuỗi UA cũ sẽ bắt đầu bị từ chối vì được xem là ứng dụng khách lỗi thời.

## Công tắc chế độ tương thích CLI (`.env.example` mục 13)

| Biến                       | Hiệu ứng                        |
| -------------------------- | ------------------------------- |
| `CLI_COMPAT_CODEX=1`       | Dấu vân tay Codex               |
| `CLI_COMPAT_CLAUDE=1`      | Dấu vân tay claude-cli          |
| `CLI_COMPAT_GITHUB=1`      | Dấu vân tay GitHub Copilot Chat |
| `CLI_COMPAT_ANTIGRAVITY=1` | Dấu vân tay Antigravity         |
| `CLI_COMPAT_KIRO=1`        | Kiro                            |
| `CLI_COMPAT_CURSOR=1`      | Cursor                          |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                     |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                        |
| `CLI_COMPAT_CLINE=1`       | Cline                           |
| `CLI_COMPAT_ALL=1`         | Bật tất cả tùy chọn ở trên      |

IP của nhà cung cấp **luôn được giữ nguyên** — công tắc này chỉ định hình lại biểu diễn trên đường truyền của yêu cầu, không chuyển đổi IP đầu ra.

---

## Làm sạch header đầu vào

OmniRoute loại bỏ các header của ứng dụng khách đầu vào trước khi chuyển tiếp, để một yêu cầu đến từ Cursor không làm lộ `User-Agent: Cursor/X.Y.Z` cho máy chủ thượng nguồn Claude. Xem `src/shared/constants/upstreamHeaders.ts` để biết danh sách từ chối, được duy trì đồng bộ chặt chẽ với các schema Zod và bài kiểm thử đơn vị.

---

## Cập nhật dấu vân tay khi nhà cung cấp thay đổi

1. Ghi lại lưu lượng CLI chính thức bằng `mitmproxy` (chặn TLS + kết xuất)
2. Trích xuất JA3/JA4 và thứ tự header chính xác
3. Cập nhật mục `CLI_FINGERPRINTS[...]` liên quan
4. Tăng phiên bản mặc định `*_USER_AGENT` tương ứng trong `.env.example`
5. Nếu chính quá trình bắt tay TLS đã thay đổi, hãy cập nhật trình bao bọc của nhà cung cấp liên quan hoặc tùy chọn wreq-js `browser:`
6. Chạy các bài kiểm thử TLS dành riêng cho nhà cung cấp và một phép thử canary thủ công với nhà cung cấp trực tiếp
7. Phát hành trong một bản vá; ghi lại trong `CHANGELOG.md`

---

## Kiểm thử

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — hành vi của trình bao bọc TLS dùng chung
- `tests/unit/anthropic-cache-fingerprint.test.ts` — tính xác định của dấu vân tay
- `tests/unit/chatgpt-web-source-retirement.test.ts` — nguồn ẩn mình ChatGPT Web dùng chung vẫn không xuất hiện trong khi Codex Web vẫn hiện diện

---

## Xem thêm

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — điều gì xảy ra khi một đường dẫn ẩn nhận được mã `403`
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — tài liệu tham khảo đầy đủ về các biến môi trường
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — góc nhìn của người vận hành về quy trình làm việc MITM
