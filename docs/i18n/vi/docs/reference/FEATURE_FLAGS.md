# Feature Flags (Tiếng Việt)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇧🇦 [bs](../../../bs/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇮🇷 [fa](../../../fa/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇷🇺 [ru](../../../ru/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> Các nút chuyển đổi thời gian chạy thay đổi hành vi của OmniRoute **mà không cần triển khai lại**.
> Mọi cờ được liệt kê ở đây đều được định nghĩa trong
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> — nguồn đáng tin cậy duy nhất. Bảng điều khiển và REST API đều đọc từ
> tệp đó, vì vậy bảng dưới đây được tạo để khớp 1:1.

---

## Cờ tính năng là gì

Cờ tính năng là một nút chuyển đổi được đặt tên (boolean hoặc enum) có giá trị có thể được thay đổi trong thời gian chạy và được lưu trữ trong cơ sở dữ liệu, không yêu cầu triển khai lại quy trình. Mỗi cờ được mô tả bởi một `FeatureFlagDefinition` với `key`, `label`, `description`, `category`, `defaultValue`, `type` và gợi ý `requiresRestart`.

### Thứ tự phân giải

**Giá trị hiệu quả** của một cờ được phân giải bởi
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) với thứ tự ưu tiên này (ưu tiên cao nhất sẽ thắng):

1. **Ghi đè DB** — một giá trị được lưu trữ trong bảng `key_value` dưới không gian tên
   `feature_flags` (được đặt thông qua bảng điều khiển hoặc REST API).
2. **Biến môi trường** — `process.env[<KEY>]`, nếu được đặt và không trống.
3. **Mặc định định nghĩa** — `defaultValue` từ `featureFlagDefinitions.ts`.

Một cờ boolean được coi là **đã bật** khi giá trị hiệu quả của nó là `"true"`,
`"1"`, hoặc `"yes"` (xem `isFeatureFlagEnabled()`).

> [!NOTE]
> Hầu hết các cờ cũng có một biến môi trường phù hợp có **cùng tên**
> được ghi lại trong [`ENVIRONMENT.md`](./ENVIRONMENT.md). Ghi đè DB của cờ
> có ưu tiên cao hơn biến môi trường đó. Một cờ với
> `requiresRestart: true` được lưu trữ ngay lập tức nhưng chỉ được đọc lại khi khởi động quy trình — bật nó sẽ hiển thị một biểu ngữ **"Khởi động lại máy chủ"** trong bảng điều khiển.

---

## Danh mục cờ

77 cờ thuộc 6 danh mục. **Mặc định** là giá trị mặc định của định nghĩa — giá trị
được sử dụng khi không có ghi đè DB cũng như biến môi trường.

### Bảo mật (10)

| Khóa                                    | Loại    | Mặc định | Mô tả                                                                                                                                                                                                                                                                                     |
| :-------------------------------------- | :------ | :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | boolean | `false`  | Yêu cầu khóa API cho tất cả các yêu cầu đến.                                                                                                                                                                                                                                              |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`   | Bật làm sạch đầu vào cho tất cả các yêu cầu.                                                                                                                                                                                                                                              |
| `INJECTION_GUARD_MODE`                  | enum    | `off`    | Chế độ bảo vệ chống chèn lời nhắc. Giá trị: `off`, `warn`, `block`, `redact`.                                                                                                                                                                                                             |
| `PII_REDACTION_ENABLED`                 | boolean | `false`  | Xóa PII khỏi các yêu cầu (độc lập với `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                                            |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`  | Làm sạch PII khỏi các phản hồi của nhà cung cấp.                                                                                                                                                                                                                                          |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact` | Chế độ làm sạch phản hồi PII. Giá trị: `redact`, `warn`, `block`, `off`.                                                                                                                                                                                                                  |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`   | Chặn các yêu cầu đi đến các dải IP riêng tư/nội bộ.                                                                                                                                                                                                                                       |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`  | Cho phép người dùng bảng điều khiển đã xác thực tiết lộ các khóa API đã lưu trữ thay vì chỉ xem các giá trị bị che.                                                                                                                                                                       |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`  | Bao gồm tiền tố tài khoản trong các dòng nhật ký AUTH (ví dụ: "Sử dụng tài khoản <nhà cung cấp>: abc12345..."). Mặc định bị tắt để các định danh tài khoản được xóa khỏi nhật ký quy trình dùng chung/đa người thuê. Độc lập với Chế độ gỡ lỗi; bật Chế độ gỡ lỗi không tiết lộ điều này. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`  | Khi OIDC được bật, tắt đăng nhập bằng mật khẩu để người dùng chỉ có thể xác thực qua OIDC Single Sign-On. Khi bị tắt (mặc định), cả đăng nhập bằng mật khẩu và OIDC đều khả dụng.                                                                                                         |

### Mạng (19)

| Key                                             | Type    | Default | Restart | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------- | ------- | ------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false` | ✓       | Bật chế độ ẩn danh dấu vân tay TLS.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false` |         | Cho phép các tuyến /v1/audio/* sử dụng các nút nhà cung cấp tương thích OpenAI được lưu trữ bên ngoài localhost. Tắt theo mặc định — định tuyến âm thanh đến một máy chủ từ xa làm thay đổi danh tính egress và phải là một quyết định rõ ràng của người vận hành. Các nút loopback luôn được phép và không bị ảnh hưởng.                                                                                                                                                                                                              |
| `RERANK_REMOTE_PROVIDER_NODES`                  | boolean | `false` |         | Cho phép POST /v1/rerank (và bước rerank loopback của công cụ bộ nhớ) sử dụng các nút nhà cung cấp tương thích OpenAI được lưu trữ bên ngoài localhost. Tắt theo mặc định — định tuyến đến một máy chủ từ xa làm thay đổi danh tính egress và phải là một quyết định rõ ràng của người vận hành. Các nút loopback luôn được phép; các nút từ xa cũng phải vượt qua chính sách URL đi ra của nhà cung cấp.                                                                                                                              |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false` |         | Khi không có proxy nào được gán cho một kết nối, tự động chọn proxy hoạt động đầu tiên từ registry. Tắt theo mặc định (nếu không, bất kỳ proxy registry nào cũng trở thành một phương án dự phòng toàn cầu — #3332).                                                                                                                                                                                                                                                                                                                   |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false` |         | Cho phép các luồng xác thực OAuth và nhà cung cấp bỏ qua một proxy được ghim và kết nối trực tiếp khi các kiểm tra trước về khả năng tiếp cận proxy thất bại. Tắt theo mặc định vì điều này có thể thay đổi IP egress.                                                                                                                                                                                                                                                                                                                 |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`  |         | Khi xảy ra ngoại lệ mạng (hết thời gian chờ, kết nối bị từ chối/đặt lại) đối với một trình thực thi xoay vòng nhiều tài khoản, nếu tài khoản bị lỗi không có proxy chuyên dụng, hãy áp dụng thời gian chờ ngắn và bỏ qua các tài khoản không có proxy khác trong phần còn lại của yêu cầu thay vì thử lại từng tài khoản. Bật theo mặc định (an toàn: không thay đổi IP egress, chỉ giảm độ trễ/rủi ro thời gian chờ trên các tài khoản egress dùng chung). Tắt để khôi phục việc truyền ngay lập tức khi lần đầu tiên không có proxy. |
| `ROTATION_ATTRIBUTION`                          | boolean | `false` |         | Xoay vòng Opencode ghi lại tài khoản nào đã phục vụ hoặc bị bỏ qua (chỉ ID được che, không bao giờ là ID tài khoản đầy đủ) và liên kết các mục nhật ký proxy với yêu cầu của chúng, để người vận hành có thể phân biệt các tài khoản bị bỏ qua với các tài khoản không được sử dụng. Tắt theo mặc định.                                                                                                                                                                                                                                |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false` |         | Các nhóm proxy và việc xoay vòng opencode theo từng tài khoản ngừng phục vụ lại một proxy vừa bị lỗi (từ chối thăm dò TCP, hoặc nhận được lỗi 429 thông qua nó) trong một khoảng thời gian theo từng tiến trình mà sẽ tăng gấp đôi sau mỗi lần lặp lại, cho đến một giới hạn. Không có trạng thái proxy nào được ghi; với mỗi ứng cử viên bị loại bỏ, lựa chọn không thay đổi. Tắt theo mặc định.                                                                                                                                      |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false` |         | Hiển thị, dưới một nhóm proxy trong bảng điều khiển, số lượng IP egress được quan sát đã phục vụ các thành viên của nó trong 24 giờ qua và số lượng kết nối đã sử dụng chúng. Chỉ đọc, được tính toán từ nhật ký proxy, không bao giờ được sử dụng để định tuyến. Tắt theo mặc định.                                                                                                                                                                                                                                                   |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false` |         | Đối với trình thực thi OpenCode, theo dõi byte đầu tiên của phần thân phản hồi Responses được truyền trực tuyến (thời gian chờ: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`, mặc định `15000`). Một luồng Responses 2xx im lặng quá thời gian chờ được coi là bị kẹt: tài khoản được làm nguội và yêu cầu chuyển sang tài khoản tiếp theo một lần; lần kẹt thứ hai sẽ thất bại nhanh chóng. Tắt theo mặc định: các luồng bị kẹt vẫn giữ thời gian chờ hiện tại cho đến khi hết thời gian chờ sẵn sàng của luồng.                                 |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false` |         | Trình thực thi OpenCode: khi gặp lỗi 403/451 mang thông báo từ chối `user_blocked` (không phải do địa lý, không phải do từ chối dấu vân tay Cloudflare), làm nguội tài khoản bị từ chối và chuyển sang tài khoản tiếp theo tối đa một lần cho mỗi yêu cầu; lần từ chối thứ hai được trả về nguyên trạng, không có dấu hiệu thành công. Tắt theo mặc định: việc định tuyến vòng qua một khối người dùng upstream có thể giống như hành vi trốn tránh và lan truyền cờ trên toàn bộ hệ thống.                                            |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false` |         | Luân chuyển OpenCode: sau hai lỗi upstream tạm thời liên tiếp (5xx hoặc 400 trống), tạm dừng trước khi chuyển sang tài khoản tiếp theo — 1.5 giây tăng gấp đôi cho mỗi lỗi tiếp theo, giới hạn 6 giây cho mỗi lần tạm dừng và 10 giây cho mỗi yêu cầu, bỏ qua khi client ngắt kết nối; phần thân lỗi được giải phóng trước khi chờ. Tắt theo mặc định: chuyển đổi dự phòng vẫn diễn ra ngay lập tức.                                                                                                                                   |
| `OPENCODE_PARK_AND_RESUME`                      | boolean | `false` |         | Luân chuyển OpenCode: tạm dừng yêu cầu sau các lỗi 429 tạm thời lặp lại (hoặc một dấu hiệu căng thẳng nhóm mới) với một nhịp tim, sau đó phát lại một chặng giới hạn tối đa 3 tài khoản tuần tự thay vì phân tán toàn bộ hệ thống. Tắt theo mặc định: mỗi lỗi 429 chuyển sang tài khoản tiếp theo chính xác như trước đây.                                                                                                                                                                                                             |
| `FLUSH_EMPTY_RETRY_ENABLED`                     | boolean | `false` |         | Trong các lượt truyền trực tuyến đã dịch, khi lượt upstream không mang nội dung hữu ích nào (chỉ hoàn thành lý luận hoặc không có đoạn dữ liệu có giá trị), thực hiện các lần thử lại có giới hạn thông qua đường dẫn thông tin xác thực thông thường (tối đa `STREAM_RECOVERY.EMPTY_TURN_RETRY_MAX`) trước khi bất kỳ điều gì được hiển thị cho client. Tắt theo mặc định: các lượt trống giữ nguyên hành vi hiện tại (200 trống hoặc 502 không có nội dung).                                                                         |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false` |         | Luân chuyển OpenCode: dừng làn sóng tài khoản ở lỗi 429 đầu tiên được phân loại là giới hạn tốc độ thực sự (`Retry-After` có thể phân tích cú pháp, hoặc phần thân nêu rõ giới hạn tốc độ/sử dụng) và trả về lỗi 429 upstream đó không thay đổi. Các lỗi 429 không được phân loại tiếp tục luân chuyển. Tắt theo mặc định: gói miễn phí bị giới hạn theo IP egress (#9611), vì vậy mỗi lỗi 429 sẽ luân chuyển và một làn sóng đã cạn kiệt sẽ trả về lỗi 429 upstream cuối cùng.                                                        |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false` | ✓       | Tắt xác minh chứng chỉ TLS cho proxy MITM. **Nguy hiểm.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false` |         | Cho phép các URL nhà cung cấp trỏ đến các mạng riêng/nội bộ.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`  |         | Cho phép thêm/xác thực các nhà cung cấp trên các địa chỉ cục bộ/riêng tư (127.0.0.1, localhost, LAN). Bật theo mặc định (ưu tiên cục bộ); tắt để chặn nghiêm ngặt chỉ các địa chỉ công cộng. Siêu dữ liệu đám mây vẫn bị chặn.                                                                                                                                                                                                                                                                                                         |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false` | ✓       | Bật chế độ nhà cung cấp tương thích với Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

### Chính sách (5)

| Key | Type | Default | Description |
| ------------------------------- | ------- ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE` | enum | `disabled` | Chế độ thực thi chính sách sử dụng công cụ. Các giá trị: `disabled`, `warn`, `block`. |
| `RATE_LIMIT_AUTO_ENABLE` | boolean | `false` | Tự động bật giới hạn tốc độ dựa trên các mẫu sử dụng. |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false` | Bỏ qua kiểm tra cửa sổ ngữ cảnh cục bộ / số lượng token đầu vào tối đa của OmniRoute đối với các yêu cầu trực tiếp đến một mô hình. Các giới hạn từ phía nhà cung cấp vẫn được áp dụng. |
| `CAPABILITY_FILTER_ENABLED` | boolean | `false` | Từ chối các yêu cầu trước khi gửi đi nếu mô hình mục tiêu thiếu các khả năng cần thiết (thị giác, công cụ, đầu ra có cấu trúc, cửa sổ ngữ cảnh). Bảo vệ các yêu cầu trực tiếp đến một nhà cung cấp duy nhất mà bỏ qua bộ lọc tương thích của lớp kết hợp. |
| `RADAR_ENABLED` | boolean | `false` | Bật mô-đun OmniRoute Radar (màn hình nguồn cấp dữ liệu danh mục và đồng bộ hóa). Mặc định là tắt; việc bật chỉ mở khóa giao diện người dùng — đồng bộ hóa dữ liệu vẫn là một tùy chọn riêng biệt. |

### Thời gian chạy (33)

| Key                                         | Type    | Default | Restart | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------- | ------- | ------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | boolean | `true`  |         | Tạo và chèn tóm tắt cuộc trò chuyện khi định tuyến kết hợp chuyển đổi mô hình. Tắt để xử lý các chuyển đổi mô hình một cách độc lập và ngăn chặn các yêu cầu chuyển giao nền cho tất cả các kết hợp hiện có và trong tương lai.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | boolean | `true`  |         | Loại bỏ các mục đầu ra giai đoạn bình luận nội bộ khỏi luồng chuyển tiếp API Responses trước khi chuyển tiếp đến máy khách. Tắt để nhận bình luận gốc từ nguồn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | boolean | `true`  |         | Thực thi các hạn chế phạm vi đối với quyền truy cập công cụ MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | boolean | `false` |         | Nén mô tả công cụ MCP để giảm mức sử dụng token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | boolean | `false` |         | Bật xử lý tác vụ nền trong thời gian chạy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | boolean | `false` | ✓       | Tắt tất cả các dịch vụ nền (làm mới hạn ngạch, đồng bộ hóa, v.v.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false` |         | Tin cậy các bộ lọc RTK cấp dự án mà không cần xác thực.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`  | ✓       | Khởi động máy chủ WebSocket của bảng điều khiển thời gian thực khi nhập (mặc định cổng 20132).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`  |         | Cho phép Codex sử dụng giao thức truyền tải Responses-over-WebSocket. Khi tắt, Codex sẽ chuyển về sử dụng HTTP Responses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`  |         | Cho phép Codex sử dụng giao thức truyền tải JSON-RPC WebSocket của máy chủ ứng dụng cục bộ (codexTransport=app-server). Khi tắt, các kết nối đã chọn app-server sẽ chuyển về sử dụng các giao thức truyền tải khác của Codex.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`  |         | Chuyển các yêu cầu đã hết ngân sách đến nhà cung cấp/mô hình dự phòng miễn phí khẩn cấp. (Xem [Emergency Budget Fallback](#emergency-budget-fallback) bên dưới.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false` |         | Bật tính năng thử lại sớm minh bạch cho các luồng SSE upstream bị cắt ngắn trước khi bất kỳ byte phản hồi nào đến được máy khách.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false` |         | Cho phép khôi phục luồng yêu cầu lại và ghép nối phản hồi sau khi các byte đã đến được máy khách.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false` |         | Giúp việc tiếp tục giữa luồng an toàn với lệnh gọi công cụ: không bao giờ tiếp tục một luồng bị cắt khi một lệnh gọi công cụ đã được phát ra (đang thực hiện hoặc đã hoàn thành với finish_reason tool_calls), và đóng sau một lần tiếp tục trống thay vì sử dụng hết toàn bộ ngân sách. Tắt: hành vi phát hành.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false` |         | Chuyển đổi dự phòng một lần sang kết nối anh em khi luồng SSE đóng trước khi phát ra bất kỳ khung hữu ích nào và số lần thử lại giới hạn trên cùng một kết nối đã hết; nếu không có kết nối anh em khả dụng, lỗi 502 `STREAM_EARLY_EOF` ban đầu sẽ được trả về. Tắt theo mặc định: early-EOF vẫn là lỗi cuối cùng sau khi thử lại trên cùng một kết nối.                                                                                                                                                                                                                                                                                                                                                              |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`  |         | Bao gồm các trường tên thân thiện với hiển thị trong phản hồi `/v1/models`. Tắt tính năng này đối với các client chỉ mong đợi ID mô hình.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`  |         | Kiểm soát cách các ID mô hình được thêm tiền tố trong /v1/models. 'dual' (mặc định) phát ra cả tiền tố bí danh và tiền tố ID nhà cung cấp chính tắc để tương thích ngược. 'alias' chỉ phát ra tiền tố bí danh ngắn (ví dụ: ds-web/model, không phải deepseek-web/model). 'canonical' chỉ phát ra tiền tố ID nhà cung cấp đầy đủ. Các giá trị: `dual`, `alias`, `canonical`.                                                                                                                                                                                                                                                                                                                                           |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`  |         | Bật đồng bộ hóa ELO bảng xếp hạng Arena AI định kỳ cho xếp hạng trí tuệ mô hình.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false` |         | Quảng cáo các ID gương `claude/<provider>/<model>` trên `/v1/models` để tính năng khám phá mô hình của cổng Claude Code liệt kê các mô hình không phải Claude. Cấp độ toàn cầu của cổng ba cấp (biến môi trường ưu tiên hơn ghi đè trên bảng điều khiển). Xem [cấu hình Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker).                                                                                                                                                                                                                                                                                                                        |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`  |         | Công tắc chính cho các bí danh cổng no-think/<provider>/<model>. Bật (mặc định): /v1/models quảng cáo một biến thể không suy nghĩ cho mọi mô hình Claude có khả năng suy nghĩ đủ điều kiện, và một ID no-think/ được gửi trong yêu cầu sẽ được giải quyết trở lại mô hình thực với khả năng suy luận bị chặn. Tắt: không có biến thể nào được quảng cáo và một ID no-think/ được xử lý như bất kỳ ID mô hình không xác định nào khác. Tùy chọn ModelSpec.noThinkingAlias cho từng mô hình vẫn được áp dụng khi tính năng này được bật.                                                                                                                                                                                |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false` |         | Tắt tạo các biến thể cấp độ suy nghĩ (ví dụ: -low, -medium, -high) trong danh mục /v1/models.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false` | ✓       | Bật các làn nhập ảo thích ứng cho mỗi tenant để điều phối nhà cung cấp (#9654): một đợt tăng đột biến của một tenant sẽ không còn gây lỗi 503 cho tenant khác. Biến môi trường OMNIROUTE_CHAT_VIRTUAL_LANES ưu tiên hơn ghi đè trên bảng điều khiển này; các thay đổi có hiệu lực khi khởi động lại máy chủ.                                                                                                                                                                                                                                                                                                                                                                                                          |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false` |         | Quảng cáo ID gương `<gateway-alias>/<model>` trên `/v1/models` cho các mô hình mà chủ sở hữu chính tắc không có thông tin xác thực đang hoạt động nhưng một cổng passthrough với thông tin xác thực đang hoạt động định tuyến chúng. Cảnh báo: thêm các mục danh mục cho tất cả các máy khách khi được bật trên toàn cầu.                                                                                                                                                                                                                                                                                                                                                                                             |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false` |         | Bật tính năng phát hiện số dư cho các nút tương thích với bộ tổng hợp New-API / One-API / Sub2API. Khi được bật, các nút tương thích có cờ bộ tổng hợp được đặt sẽ báo cáo số dư của chúng trong bảng điều khiển và định tuyến kiểm tra trước hạn mức.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false` |         | Tiếp tục các lệnh gọi công cụ không phát trực tuyến thuộc sở hữu của máy chủ cho đến khi mô hình trả về một phản hồi mà máy khách có thể sử dụng.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false` |         | Thống kê tìm kiếm và các tìm kiếm gần đây chỉ tính các nhà cung cấp vẫn có kết nối trực tiếp (các nhà cung cấp không cần khóa như `duckduckgo-free` luôn được tính). Tắt giữ lại mọi hàng tìm kiếm đã lưu với một ID nhà cung cấp.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false` |         | Trang nhà cung cấp trên bảng điều khiển: chỉ hiển thị huy hiệu Miễn phí trên các tín hiệu mà nhà cung cấp tuân thủ — bỏ qua phỏng đoán tên hiển thị, các trường miễn phí không phải boolean và hậu tố `:free` trên các nhà cung cấp đã đăng ký không có gói miễn phí được ghi lại. Tắt giữ nguyên quy tắc huy hiệu lịch sử.                                                                                                                                                                                                                                                                                                                                                                                           |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false` |         | Trên các phản hồi 429/503 không khả dụng tổng hợp, bỏ qua `Retry-After` khi không biết thời gian thử lại cụ thể trong tương lai (thay vì 1 giây tổng hợp), thêm `error.retry_after_provenance` (`signal` \| `none`), và cho phép các đường dẫn thoát kết hợp đọc các gợi ý thử lại bằng văn xuôi từ các nội dung JSON và văn bản thuần túy của upstream. Trường này chỉ xuất hiện trên các phản hồi được xây dựng bởi `unavailableResponse()`; các nội dung 429/503 khác không thay đổi.                                                                                                                                                                                                                              |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false` |         | Khi một mục tiêu kết hợp `priority` được đánh dấu chỉ dự phòng khi hết hạn mức dừng kết hợp vì một nguyên nhân không phải là hạn mức (ngắt mạch nhà cung cấp mở, bỏ qua độ trễ dự đoán), trả về 502 thay vì 503 trông giống hạn mức. Các dừng do khóa, thời gian chờ, không khả dụng, hết hạn mức và giới hạn đồng thời vẫn giữ 503.                                                                                                                                                                                                                                                                                                                                                                                  |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false` |         | Một phản hồi Mistral 401 trần (`{"detail":"Unauthorized"}`, không có tín hiệu xác thực rõ ràng) là giống nhau đối với một khóa bị thu hồi và đối với hạn mức đã cạn kiệt. Khi bật, nó làm nguội kết nối thay vì đặt nó ở trạng thái `expired`, tối đa 3 lần mỗi giờ cho mỗi kết nối; lần tiếp theo sẽ đặt nó, vì vậy một khóa bị thu hồi vẫn hội tụ. Tắt theo mặc định: mọi phản hồi Mistral 401 trần sẽ đặt kết nối như trước.                                                                                                                                                                                                                                                                                       |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false` |         | Lấy danh mục mô hình xAI trực tiếp cho các kết nối `xai-oauth` từ `https://api.x.ai/v1/models` bằng cách sử dụng mã thông báo OAuth bearer, thay vì hạt giống tĩnh đã đóng băng. Tắt theo mặc định: `xai-oauth` tiếp tục phục vụ hạt giống tĩnh không thay đổi. Khi có bất kỳ lỗi phân giải nào, quá trình khám phá sẽ quay lại hạt giống (chưa xác minh liệu x.ai có chấp nhận OAuth bearer tại điểm cuối này hay không).                                                                                                                                                                                                                                                                                            |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false` |         | Cho phép quá trình dọn dẹp tự động xóa các tác vụ API Batch đã kết thúc (hoàn thành/thất bại/đã hủy/hết hạn) cũ hơn `OMNIROUTE_BATCH_RETENTION_DAYS`, cùng với các điểm kiểm tra trên mỗi dòng của chúng, và xóa nội dung BLOB của các tệp đã tải lên sau thời gian `expires_at` của chúng. Tắt theo mặc định: mọi cài đặt hiện có giữ dữ liệu này chính xác như trước cho đến khi một nhà điều hành chọn tham gia. Tuyến `DELETE /api/v1/batches/delete-completed` do nhà điều hành kích hoạt không bị ảnh hưởng theo bất kỳ cách nào — nó là một hợp đồng API công khai riêng biệt, không điều kiện.                                                                                                                |
| `ANTIGRAVITY_ACCOUNT_LEASE_ENABLED`         | boolean | `false` |         | Đặt trước tài khoản Antigravity đã chọn cho vòng đời truyền phát của yêu cầu đã chọn nó, để một lần thử lại đồng thời hoặc việc chuyển giao thông tin xác thực không thể chọn lại một tài khoản đã được cam kết cho một luồng đang hoạt động. Việc đặt trước được giới hạn trong (kết nối, mô hình thượng nguồn có thể gọi được), do đó một tài khoản vẫn có thể phục vụ hai mô hình khác nhau cùng một lúc. Khi mọi tài khoản đủ điều kiện đã được thuê cho mô hình đó, yêu cầu trả về lỗi 503 có cấu trúc `antigravity_pool_busy` với `Retry-After` có giới hạn thay vì chất đống lên một tài khoản bận. Tắt theo mặc định: việc chọn tài khoản vẫn giữ nguyên như trước, và không có đặt trước nào được thực hiện. |

### CLI (5)

| Key                                   | Type    | Default | Restart | Description                                                                                                                                                                                                                                |
| ------------------------------------- | ------- | ------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `CLI_COMPAT_ALL`                      | boolean | `false` | ✓       | Bật chế độ tương thích cho tất cả các máy khách CLI.                                                                                                                                                                                       |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false` |         | Bật lớp tương thích bí danh mô hình.                                                                                                                                                                                                       |
| `PRICING_SYNC_ENABLED`                | boolean | `false` |         | Bật đồng bộ hóa dữ liệu giá tự động (cũng yêu cầu biến môi trường `PRICING_SYNC_ENABLED`).                                                                                                                                                 |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false` |         | Sau khi đồng bộ hóa mô hình nhà cung cấp, tự động (ghi lại) các tệp cấu hình hồ sơ ~/.codex/*.config.toml từ danh mục trực tiếp. Không bao giờ thay đổi cấu hình Codex đang hoạt động/mặc định. Tắt theo mặc định.                         |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false` |         | Sau khi đồng bộ hóa mô hình nhà cung cấp, tự động (ghi lại) các tệp cấu hình Claude Code ~/.claude/profiles/<name>/settings.json từ danh mục trực tiếp. Không bao giờ thay đổi cấu hình Claude đang hoạt động/mặc định. Tắt theo mặc định. |

### Tình trạng (5)

| Key                                       | Type    | Default | Description                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------- | ------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false` | Vô hiệu hóa điểm cuối kiểm tra tình trạng sức khỏe của phiên bản cục bộ.                                                                                                                                                                                                                                               |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false` | Vô hiệu hóa kiểm tra tình trạng sức khỏe xác thực token.                                                                                                                                                                                                                                                               |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false` | Bật quyền truy cập mạng trong môi trường sandbox kỹ năng.                                                                                                                                                                                                                                                              |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false` | Trong quá trình quét tình trạng proxy, một thăm dò mà mục tiêu từ chối (401/403/429) sẽ đặt lại chuỗi lỗi liên tiếp của proxy. Mặc định là tắt: một sự từ chối vẫn ở trạng thái trung lập (#10654). Một lỗi 5xx vẫn không có kết luận; một sự từ chối không bao giờ loại bỏ, vô hiệu hóa hoặc kích hoạt lại một proxy. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false` | Chạy kiểm tra tính toàn vẹn/tình trạng DB khi khởi động sau khi máy chủ bắt đầu chấp nhận yêu cầu (thông qua `setImmediate`) thay vì chặn khởi động cho đến khi hoàn tất (#13717). Mặc định là tắt: quá trình khởi động bị chặn chính xác như trước PR này.                                                            |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` và bí danh cũ của nó
> `INJECTION_GUARD_BLOCK_THRESHOLD` điều chỉnh chế độ `block` của
> `INJECTION_GUARD_MODE`, nhưng chúng là các biến môi trường thông thường được đọc bởi
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts),
> không phải là cờ tính năng: chúng không có ghi đè DB và không có nút chuyển đổi trên bảng điều khiển. Xem
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> Cột `Restart` đánh dấu các cờ có `requiresRestart: true` — giá trị được
> lưu trữ ngay lập tức nhưng chỉ có hiệu lực sau khi quá trình tải lại. Các cờ Enum
> từ chối bất kỳ giá trị nào nằm ngoài tập hợp cho phép của chúng (được xác thực phía máy chủ trong
> cả `setFeatureFlagOverride()` và trình xử lý REST `PUT`).

---

## Bật/Tắt Cờ

### Bảng điều khiển

Điều hướng đến **Bảng điều khiển → Cài đặt → Cờ tính năng**
(`/dashboard/settings/feature-flags`). Lưới
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
hỗ trợ:

- **Tìm kiếm** theo khóa hoặc mô tả, và **lọc** theo danh mục (cộng với một
  chế độ xem tổng hợp **Yêu cầu Khởi động lại**).
- Một nút **bật/tắt** cho các cờ boolean và một **menu thả xuống** cho các cờ enum
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- Một **huy hiệu nguồn** cho mỗi cờ — `DB`, `ENV`, hoặc `DEF` — hiển thị giá trị
  hiệu quả đến từ đâu.
- Một nút **Đặt lại** (chỉ hiển thị cho các cờ có nguồn `DB`) để bỏ qua ghi đè,
  và một nút **Đặt lại Tất cả Ghi đè** ở phía dưới.
- Một biểu ngữ **Khởi động lại Máy chủ** khi một cờ `requiresRestart` được thay đổi.

### API REST

Tất cả các thao tác đều thông qua một tuyến đường duy nhất:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
Mỗi phương thức yêu cầu một phiên bảng điều khiển đã được xác thực (nếu không sẽ là `401`).

#### `GET /api/settings/feature-flags`

Trả về mọi cờ với giá trị hiệu quả, nguồn và tóm tắt của nó.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Require API Key",
      "description": "Require an API key for all incoming requests",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... all 77 flags
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

Đặt hoặc xóa một ghi đè duy nhất. Nội dung: `{ key: string; value?: string }`.
Bỏ qua `value` sẽ xóa ghi đè (khôi phục env / default).

```bash
# Set a DB override
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# Remove the override (no "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

Phản hồi trả về `effectiveValue`/`source` mới, `previousValue`/
`previousSource`, và `requiresRestart`. Các khóa không xác định và giá trị enum
ngoài phạm vi sẽ bị từ chối với `400`.

#### `DELETE /api/settings/feature-flags`

Xóa **tất cả** các ghi đè DB cùng một lúc, khôi phục mọi cờ về giá trị env / default
của nó. Trả về `{ cleared: <count>, message: "..." }`.

> [!NOTE]
> Các cờ có `requiresRestart: true` chỉ có hiệu lực sau khi tải lại tiến trình.
> Luồng khởi động lại của bảng điều khiển gọi `POST /api/restart` và sau đó thăm dò
> `GET /api/health/ping` cho đến khi máy chủ hoạt động trở lại.

---

## Dự phòng Ngân sách Khẩn cấp

`OMNIROUTE_EMERGENCY_FALLBACK` (danh mục `runtime`, mặc định `true`) kiểm soát
đường dẫn dự phòng miễn phí khẩn cấp trong
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts).
Khi được bật, các yêu cầu đã hết ngân sách sẽ được chuyển hướng đến một nhà cung cấp/mô hình
dự phòng miễn phí thay vì thất bại hoàn toàn. Đặt nó thành `false` (hoặc `0`) — thông qua
nút bật/tắt của bảng điều khiển, ghi đè DB hoặc biến môi trường `OMNIROUTE_EMERGENCY_FALLBACK`
— để tắt hành vi này và cho phép các yêu cầu hết ngân sách thất bại. (Được hiển thị
dưới dạng nút bật/tắt trên bảng điều khiển trong PR #3741 / #3752.)

---

## Xem thêm

- [Tham chiếu Biến môi trường](./ENVIRONMENT.md) — hầu hết các cờ đều có một biến môi trường cùng tên được ghi lại ở đó (ghi đè DB sẽ được ưu tiên hơn).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — nguồn đáng tin cậy cho mọi cờ.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — logic phân giải (`resolveFeatureFlag`, `isFeatureFlagEnabled`,
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — lưu trữ ghi đè DB trong không gian tên `feature_flags` của bảng `key_value`.
