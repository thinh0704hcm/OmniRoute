# Traffic Inspector (Magyar)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇹 [am](../../../am/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇿 [az](../../../az/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇰 [da](../../../da/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇪 [de](../../../de/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇷 [el](../../../el/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇸 [es](../../../es/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇪 [et](../../../et/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇱 [he](../../../he/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇩 [id](../../../id/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇹 [it](../../../it/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇭 [km](../../../km/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇲 [my](../../../my/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇴 [no](../../../no/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [or](../../../or/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇰 [si](../../../si/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [te](../../../te/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇭 [th](../../../th/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/TRAFFIC_INSPECTOR.md)

---

A Traffic Inspector az OmniRoute beépített HTTPS-forgalom-hibakeresője — egy Charles Proxy / mitmweb / HTTP Toolkit jellegű eszköz, amely **LLM-tudatos** és **ügynöktudatos**. A `/dashboard/tools/traffic-inspector` útvonalon érhető el, és legfeljebb 5 egyidejű rögzítési forrás élő forgalmát fogadja.

**Irányítópult helye:** `/dashboard/tools/traffic-inspector`
**Oldalsáv csoportja:** Eszközök (az AgentBridge után)
**Lásd még:** [`AGENTBRIDGE.md`](./AGENTBRIDGE.md) — az AgentBridge az 1. rögzítési mód.

---

## §1 Áttekintés

### Mitől egyedi a Traffic Inspector?

| Funkció                                                                                     | mitmweb | Charles | Fiddler | **OmniRoute Traffic Inspector** |
| ------------------------------------------------------------------------------------------- | :-----: | :-----: | :-----: | :-----------------------------: |
| Webalapú                                                                                    |    ✓    |    ✗    |    ✗    |                ✓                |
| Nyílt forráskódú                                                                            |    ✓    |    ✗    | részben |                ✓                |
| **Ügynöktudatos** (tudja, ha a kérés az Antigravity/Copilot/stb. alkalmazástól érkezik)     |    ✗    |    ✗    |    ✗    |                ✓                |
| **LLM-tudatos** (értelmezi az OpenAI/Anthropic/Gemini-formátumot, a tokeneket és a modellt) |    ✗    |    ✗    |    ✗    |                ✓                |
| **Látható modellleképezés** (gemini-3-flash → claude-sonnet-4.7)                            |    ✗    |    ✗    |    ✗    |                ✓                |
| **A proxy és az upstream késleltetésének elkülönítése**                                     | részben |    ✗    |    ✗    |                ✓                |
| **Integrálva az OmniRoute-tal**: útválasztás, tartalék útvonal, költség                     |    ✗    |    ✗    |    ✗    |                ✓                |
| **Rendszerszintű proxyhibakeresés** (a gép bármely alkalmazásához)                          |    ✓    |    ✓    |    ✓    |                ✓                |
| **Egyéni gazdagépek forgalmának rögzítése** (gazdagépenkénti DNS-átirányítás)               |    ✓    |    ✓    |    ✓    |                ✓                |
| **HTTP_PROXY környezeti változós mód**                                                      |    ✓    |    ✓    |    ✓    |                ✓                |
| **Beszélgetésnézet** (többfordulós buborékok, tool_use/tool_result)                         |    ✗    |    ✗    |    ✗    |                ✓                |
| **SSE-adatfolyam-egyesítő** (rekonstrukció deltaeseményekből)                               |    ✗    |    ✗    |    ✗    |                ✓                |
| **Munkamenet-rögzítés** (elnevezhető, exportálható .har/.jsonl formátumban)                 |    ✗    |    ✓    |    ✓    |                ✓                |

### Az architektúra egy bekezdésben

A `TrafficBuffer` (`src/mitm/inspector/buffer.ts`) egy megosztott, memóriában tárolt körkörös puffer (alapértelmezés szerint 1000 bejegyzéssel, amely az `INSPECTOR_BUFFER_SIZE` használatával konfigurálható). Minden rögzítési forrás a `push()` használatával ír bele. A puffer minden bejegyzést a `kindDetector.ts` segítségével osztályoz (megállapítja, hogy LLM-kérésről van-e szó), kiszámít egy `contextKey` értéket (a rendszerprompt SHA-256-ujjlenyomatát), majd a `globalTrafficBuffer.subscribe()` használatával továbbítja az adatokat az összes WebSocket-előfizetőnek. Az irányítópult a `GET /api/tools/traffic-inspector/ws` végponton keresztül csatlakozik, csatlakozáskor pillanatképet kap, amelyet `new`/`update`/`clear` események követnek.

---

## §2 Rögzítési módok

A Traffic Inspector **5 egyidejű rögzítési forrást** támogat. Mindegyik egymástól függetlenül kapcsolható be és ki. Minden `InterceptedRequest` (`src/mitm/inspector/types.ts`) `source` mezője az `"agent-bridge"`, `"custom-host"`, `"http-proxy"`, `"system-proxy"` vagy `"tproxy"` értékek egyikét tartalmazza.

### 1. mód — AgentBridge (alapértelmezett, mindig aktív)

**Forrás:** AgentBridge-kezelők (`src/mitm/handlers/base.ts`)
**Működés:** A `MitmHandlerBase` minden `intercept()` hívása meghívja a `hookBufferStart()` függvényt a továbbítás előtt, majd a `hookBufferUpdate()` függvényt a befejezéskor. Nem igényel további konfigurációt — az AgentBridge elindulásakor azonnal működik.
**Hatókör:** Az AgentBridge-ben konfigurált 9 IDE-ügynök
**Megjegyzés:** Az `InterceptedRequest` `source` mezője = `"agent-bridge"`

### 2. mód — Egyéni hosztok (DNS-átirányítás)

**Forrás:** Felhasználó által meghatározott hosztlista (`inspector_custom_hosts` tábla)
**Működés:** Egy hoszt felhasználói felületen történő hozzáadása beírja a `127.0.0.1 <host>` sort az `/etc/hosts` fájlba (sudo szükséges). A meglévő AgentBridge MITM-kiszolgáló (443-as port) dinamikusan létrehoz egy SNI-tanúsítványt az új hoszthoz.
**Hatókör:** Bármely alkalmazás, amely a hozzáadott hosztot használja — nincs szükség az alkalmazás konfigurációjának módosítására
**Megjegyzés:** `source` = `"custom-host"`

Példák felhasználási esetekre:

- A Python-szkriptekből az `api.openai.com` felé irányuló forgalom figyelése
- A `my-internal-llm.company.com` hibakeresése
- Az azonos hálózaton lévő mobileszközök forgalmának rögzítése (ARP-hamisítással — haladó)

### 3. mód — HTTP_PROXY-figyelő (8080-as port)

**Forrás:** A `HTTP_PROXY`/`HTTPS_PROXY` környezeti változókat használó alkalmazások
**Működés:** Egy másodlagos, a 8080-as porton figyelő szolgáltatás (`src/mitm/inspector/httpProxyServer.ts`), amely szabványos explicit HTTP/HTTPS-proxyként működik. Fogadja a `CONNECT` alagutakat (HTTPS) és a közvetlen HTTP-kéréseket.
**Hatókör:** Bármely alkalmazás, amely figyelembe veszi a `HTTP_PROXY` környezeti változót — nincs szükség DNS-módosításra vagy sudo jogosultságra
**Megjegyzés:** `source` = `"http-proxy"`

```bash
# Gyors rögzítés egyetlen parancshoz:
HTTPS_PROXY=http://127.0.0.1:8080 curl https://api.openai.com/v1/models

# Folyamatos rögzítés egy parancsértelmező-munkamenetben:
export HTTP_PROXY=http://127.0.0.1:8080
export HTTPS_PROXY=http://127.0.0.1:8080
```

**TLS-korlátozás:** A HTTPS `CONNECT` alagutak esetében alapértelmezés szerint csak a metaadatokat (hoszt, port, időzítés) rögzíti — a TLS-törzs nincs visszafejtve. A teljes törzs vizsgálatához engedélyezze a „HTTPS visszafejtése proxy módban” kapcsolót (külön bekapcsolandó, és megköveteli, hogy az AgentBridge tanúsítványa megbízható legyen).

**Portütközés:** Ha a 8080-as port használatban van, az AgentBridge strukturált hibával ellátott 409-es választ ad vissza. A port az `INSPECTOR_HTTP_PROXY_PORT` környezeti változóval módosítható.

### 4. mód — Rendszerszintű proxy (haladó, külön bekapcsolandó)

**Forrás:** Operációs rendszer szintű proxybeállítások (a gépen lévő összes alkalmazásra érvényes)
**Működés:** Az operációs rendszer API-jait használja az összes HTTP/HTTPS-forgalom HTTP_PROXY-figyelőn keresztüli átirányításához:

- **macOS:** `networksetup -setwebproxy / -setsecurewebproxy`
- **Linux:** `gsettings set org.gnome.system.proxy` + `/etc/environment`
- **Windows:** `netsh winhttp set proxy 127.0.0.1:8080`
  **Hatókör:** A gépen lévő minden olyan alkalmazás, amely figyelembe veszi a rendszer proxybeállításait
  **Megjegyzés:** `source` = `"system-proxy"`

**Biztonsági mechanizmusok:**

- Automatikus kikapcsolási időzítő (alapértelmezés szerint 30 perc, az `INSPECTOR_SYSTEM_PROXY_GUARD_MINUTES` segítségével konfigurálható)
- A rendszer korábbi proxyállapota mentésre kerül az adatbázisban, és visszaállításkor helyreáll
- Az irányítópult „Rendszerproxy visszaállítása” üzenetet jelenít meg, ha a felhasználó aktív állapotban másik oldalra navigál
- A felhasználói felület `⚠ Haladó` jelvényt és külön megerősítő jelölőnégyzetet jelenít meg

### 5. mód — TPROXY transzparens visszafejtés (Linux, root, külön bekapcsolandó)

**Forrás:** Kernel TPROXY + házirend-alapú útválasztás (`src/mitm/tproxy/`)
**Működés:** Megjelöli a célportra (alapértelmezés szerint `443`) irányuló új helyi kimenő TCP-kapcsolatokat a `mangle OUTPUT` láncban, egy `ip rule` átirányítja a megjelölt csomagokat helyi kézbesítésre, majd a `mangle PREROUTING` lánc `TPROXY` célja átadja azokat egy transzparens (**IP_TRANSPARENT**) figyelőnek (alapértelmezett portja `8443`). A figyelő egy dinamikus hitelesítésszolgáltató által **igény szerint, SNI-hosztnevenként kiállított** végponti tanúsítvánnyal lezárja a TLS-kapcsolatot, rögzíti a visszafejtett adatcserét, majd újratitkosítva továbbítja a kérést az eredeti célhelyre.
**Hatókör:** **Tetszőleges** célhosztok a célporton — nincs szükség `/etc/hosts`-hamisításra, `HTTP_PROXY` környezeti változóra vagy a rendszerszintű proxy módosítására. Az elfogott folyamat konfigurációját nem kell módosítani, de meg kell bíznia a dinamikus hitelesítésszolgáltatóban.
**Megjegyzés:** `source` = `"tproxy"`

**Követelmények:** Kizárólag Linux (**IP_TRANSPARENT** csak Linuxon érhető el), **CAP_NET_ADMIN** képesség (root), valamint egy natív N-API-bővítmény, amelyet C-eszközlánccal kell lefordítani (`npm run build:native:tproxy`). Ha ezek nem érhetők el, az irányítópult kapcsolója le van tiltva, és a „A TPROXY-visszafejtéshez Linux + root + a natív bővítmény szükséges” elemleírás jelenik meg. A tűzfalszabályok alkalmazása és visszaállítása tranzakciósan történik (egy összeomlás soha nem hagy hátra `mangle` szabályt), újraindításkor pedig törlődnek. Egy SO_MARK-alapú hurok elleni védelem megakadályozza, hogy a proxy saját, újratitkosított továbbított forgalma ismét elfogásra kerüljön.

Ez egy jelentős alrendszer, saját, külön üzemeltetői útmutatóval — a teljes tűzfalkonfigurációért, az SNI-nkénti dinamikus hitelesítésszolgáltatóért és a megbízhatóságitár-telepítőért, a csak helyi útvonalért, a hurok elleni védelem részleteiért és a konfigurációs sémáért tekintse meg a `docs/security/MITM-TPROXY-DECRYPT.md` fájlt (git; nincs beépítve a `/docs` dokumentációba). A kapcsolót a `GET / POST / DELETE /api/tools/agent-bridge/tproxy` vezérli (megjegyzés: az útvonal az AgentBridge előtag alatt található, nem a Traffic Inspector előtagja alatt).

### A rögzítési módok összehasonlítása

| Mód                    | Beállítás                          |            Sudo?            | Hatókör                                              | Megjegyzések                                                                                                                                                  |
| ---------------------- | ---------------------------------- | :-------------------------: | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. AgentBridge         | Automatikus                        | Egyszer (tanúsítvány+hosts) | 9 IDE-ügynök                                         | Alapértelmezetten bekapcsolva                                                                                                                                 |
| 2. Egyéni hostok       | Hostonkénti bevitel                |      Igen (hosts fájl)      | Az adott hostot használó bármely alkalmazás          | Adatbázisban megőrizve                                                                                                                                        |
| 3. HTTP_PROXY          | `export HTTPS_PROXY=...`           |             Nem             | A környezeti változókat figyelembe vevő alkalmazások | 8080-as port, alapértelmezetten nincs TLS-visszafejtés                                                                                                        |
| 4. Rendszerszintű      | Kapcsoló + megerősítés             |            Igen             | A gép összes alkalmazása                             | Automatikus kikapcsolás 30 perc után                                                                                                                          |
| 5. TPROXY-visszafejtés | Kapcsoló (Linux + natív bővítmény) | Igen (root + CA-telepítés)  | Bármely host a célporton                             | Tetszőleges hostokat fejt vissza; alapértelmezetten kikapcsolva — lásd: `docs/security/MITM-TPROXY-DECRYPT.md` (git; nincs belefordítva a `/docs` könyvtárba) |

---

## §3 Felhasználói felület

### 3.1 Elrendezés

```
┌─ Forgalomvizsgáló ─────────────────────────────────────────────────────┐
│ ┌─ Rögzítési források eszköztára ──────────────────────────────────┐   │
│ │ [✓ AgentBridge]  [✓ Egyéni gazdagépek (3)] [○ HTTP_PROXY] [○ Rendszer]│
│ └─────────────────────────────────────────────────────────────────────┘  │
│ ┌─ Szűrő- és vezérlősáv ───────────────────────────────────────────┐   │
│ │ Profil: (●) Csak LLM  (○) Egyéni  (○) Összes                      │   │
│ │ [⎉ Szünet] [🗑 Törlés] [⬇ .har] [● MUNKAMENET RÖGZ.] ● élő 482/1k│   │
│ └─────────────────────────────────────────────────────────────────────┘  │
├══◀▶══════════════════════════════╬══════════════════════════════════════╤╡
│ KÉRÉSLISTA (átméretezhető)       ║ RÉSZLETEK PANEL                     ▲ │
│ ────────────────────────────── │ ║ [Beszélgetés][Fejlécek][Kérés]      │ │
│ ▎ 14:32 POST 200 12k AG openai ║ [Válasz][Időzítés][LLM][Statisztika]│ │
│ ▎ 14:31 POST 200 8k  CP openai ║                                     ▼ │
│ ▎ 14:31 POST 503 ⚠   KR ...   ║                                       │
│ ▎ 14:30 GET  200 3k  🌐 egyéni ║                                       │
└══════════════════════════════════╝══════════════════════════════════════╝
```

### 3.2 Kéréslista (bal oldali panel)

- **Virtualizált** (`useVirtualList` + `ResizeObserver`): 1000 elemet is képes lefagyás nélkül kezelni
- **Automatikus görgetés**, amely egy kapcsolóval szüneteltethető a vizsgálat idejére
- **Színkódolt állapot**: zöld (2xx), sárga (3xx), piros (4xx/5xx), szürke (folyamatban)
- **Ügynökhangulatjelek**: 🔵 Antigravity, 🟢 Copilot, 🟠 Kiro, 🟣 Codex, 🔷 Cursor, 🟤 Zed, 🟡 Claude Code, ⚫ Open Code, 🌐 egyéni gazdagép
- **Környezetet jelző színsáv**: 1 képpont széles, a `contextKey` alapján színezett bal oldali szegély (a rendszerprompt SHA-256-kivonata) — vizuálisan csoportosítja a kapcsolódó beszélgetéseket
- **Lusta törzsbetöltés**: csak a kijelölt kérés törzse materializálódik a részletező lapokon (így elkerülhető 1000 × 1 MB-os törzs megjelenítése)

### 3.3 Részletek panel — 7 lap

| Lap               | Tartalom                                                                                        | Megjegyzések                                                                                                             |
| ----------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| **Beszélgetés**   | Többfordulós csevegési buborékok (rendszer/felhasználó/asszisztens + tool_use/tool_result)      | Bármely szolgáltatói formátumból normalizálva; csak akkor jelenik meg, ha `detectedKind === "llm"`                       |
| **Fejlécek**      | A kérés és a válasz fejléctáblázatai                                                            | Az érzékeny fejlécek (Authorization, Cookie, api-key) alapértelmezés szerint maszkoltak; „Titkok megjelenítése” kapcsoló |
| **Kérés**         | Nyers törzs, JSON-fanézet, a modellmező jelvénye                                                | Formázott JSON vagy nyers szöveg                                                                                         |
| **Válasz**        | Nyers törzs vagy SSE-eseménylista; „Nyers ↔ Egyesített” kapcsoló                                | Az SSE-egyesítő a deltaeseményekből rekonstruálja a végső üzenetet                                                       |
| **Időzítés**      | Vízesésdiagram: a proxy többletterhelése és a felsőbb szintű szolgáltatás késleltetése          | Teljes idő, TTFB és méret                                                                                                |
| **LLM részletei** | Szolgáltató, modell, üzenetek száma, bemeneti/kimeneti tokenek, becsült költség, leképezett cél | Csak LLM-kéréseknél jelenik meg                                                                                          |
| **Statisztika**   | Recharts: késleltetési idővonal, tokenoszlopdiagram, eszközhívások pontdiagramja                | Csak betöltött, rögzített munkamenet esetén jelenik meg                                                                  |

### 3.4 Eszköztár vezérlőelemei

| Vezérlő                | Művelet                                                                                   |
| ---------------------- | ----------------------------------------------------------------------------------------- |
| ⎉ Szünet               | Leállítja az új kérések megjelenítését; az „X új” jelvény értéke tovább növekszik         |
| 🗑 Törlés               | Törli a felhasználói felület listáját (a kiszolgáló puffere változatlan marad)            |
| ⬇ .har exportálása     | HAR-fájlként letölti az aktuálisan szűrt listát                                           |
| ● Munkamenet rögzítése | Elindít egy elnevezett rögzítési munkamenetet                                             |
| Profilválasztó         | Csak LLM / Egyéni gazdagépek / Összes                                                     |
| Gazdagépszűrő          | Részszöveges egyezés a `host` mezőben                                                     |
| Ügynökszűrő            | Legördülő lista: Összes / ügynökönként                                                    |
| Állapotszűrő           | Összes / 2xx / 3xx / 4xx / 5xx / hiba                                                     |
| Forrásszűrő            | Összes / agent-bridge / custom-host / http-proxy / system-proxy / tproxy                  |
| **Élő** szűrő          | Csak a folyamatban lévő (nyitott) kérések megjelenítése — `liveOnly` kapcsoló (lásd §4.6) |

### 3.5 Átméretezhető panelek

- A listát és a részletek panelt húzható fogantyú választja el
- Lista szélessége: legalább 280px, legfeljebb 720px, az érték a `localStorage` tárban marad meg (`inspector.listWidth`)
- 48px széles, csak ikonokat tartalmazó sávvá csukható össze; a sáv valamelyik sorára kattintva kinyílik

---

## §4 LLM-tudatos funkciók

### 4.1 Típusfelismerő (`src/mitm/inspector/kindDetector.ts`)

Minden kérést `"llm"`, `"app"` vagy `"unknown"` típusúnak minősít 4 jel alapján:

1. **Hosztregiszter** — ~18 ismert LLM API-hosztnév (OpenAI, Anthropic, Gemini, Groq, Mistral, Together, Fireworks, Cohere, Perplexity, Hugging Face, OpenRouter, xAI, Moonshot stb.)
2. **Útvonalminták** — `/v1/chat/completions`, `/v1/messages`, `/generateContent`, `/v1/responses` stb.
3. **Törzs szerkezete** — felismeri a `messages[]` (OpenAI/Claude), `contents[]` (Gemini), `prompt`, `input` mezőket
4. **User-agent jelzések** — `codex`, `claude`, `gemini`, `antigravity`, `kiro`, `copilot`, `cursor` az UA-karakterláncban

A 2. módban hozzáadott egyéni hosztok a `kind` értéküket az űrlap mezőjéből öröklik (alapértelmezés szerint `"custom"`).

### 4.2 SSE-egyesítő (`src/mitm/inspector/sseMerger.ts`)

**Független, tiszta szobás implementáció.** Az események elemzése a
[WHATWG kiszolgáló által küldött események algoritmusát](https://html.spec.whatwg.org/multipage/server-sent-events.html#parsing-an-event-stream)
követi, míg a rekonstrukció a nyilvános [OpenAI](https://platform.openai.com/docs/api-reference/chat/create),
[Anthropic](https://platform.claude.com/docs/en/build-with-claude/streaming) és
[Gemini](https://ai.google.dev/api/generate-content#method:-models.streamgeneratecontent)
streamelési sémáit követi.

A nyers SSE-deltaeseményekből rekonstruálja az asszisztens végső üzenetét:

- **Anthropic**: index szerint összegzi a `content_block_delta` elemeket; kezeli a `text_delta`, `input_json_delta` (eszközhívások), `thinking_delta` típusokat
- **OpenAI**: index szerint összegzi a Chat Completions választásait/eszközhívásait és a Responses API kimeneti elemeit
- **Gemini**: összegzi a `candidates[i].content.parts` elemeit
- **Ismeretlen**: változatlanul adja vissza a nyers eseményeket

A Válasz lap egy kapcsolót jelenít meg: **„Nyers események ↔ Egyesített”**.

### 4.3 Beszélgetésnormalizáló (`src/mitm/inspector/conversationNormalizer.ts`)

**Független, tiszta szobás implementáció.** A normalizálást helyi feketedobozos
szerződések, valamint az OpenAI, az Anthropic és a Gemini nyilvános üzenetsémái határozzák meg; nem használ külső
implementációból származó forráskódot.

Az OpenAI, az Anthropic és a Gemini üzenetformátumait a megjelenítés előtt egyetlen `NormalizedConversation` formátummá alakítja:

```ts
interface NormalizedConversation {
  request: NormalizedTurn[]; // üzenetek / tartalmak / prompt a kérés törzséből
  response: NormalizedTurn[]; // asszisztensi válasz (az sseMerger által egyesítve)
  contextKey: string | null; // a rendszerprompt SHA-256-ujjlenyomata
}
```

Blokktípusok: `text`, `tool_use`, `tool_result`. A Beszélgetés lap szolgáltatótól függetlenül ezt a struktúrát használja.

### 4.4 Kontextuskulcs színezése (`src/mitm/inspector/contextKey.ts`)

- Kiszámítja a rendszerprompt `SHA-256`-kivonatát (az első `role:system` üzenet, a `system` mező vagy a Gemini `systemInstruction` alapján)
- Egy 12 karakteres hexadecimális előtagot ad vissza (`"a3f9c2..."`)
- A frontend a kulcsot egy determinisztikus HSL-színhez rendeli a bal oldali szegélysávhoz
- **„Azonos kontextus” szűrő**: a `ctx #a3f` címkére kattintva hozzáad egy szűrőt, amely csak az azonos ujjlenyomattal rendelkező kéréseket jeleníti meg

Ez megkönnyíti az ugyanazon ügynökmenetben futó különböző „személyiségek” vagy feladatok vizuális megkülönböztetését.

### 4.5 LLM-metaadatok kinyerése

LLM-kérések esetén az LLM részletei lap a következőket nyeri ki:

```ts
interface LlmMetadata {
  provider: string | null; // "openai" | "anthropic" | "gemini" | ...
  apiKind: string | null; // "chat.completions" | "messages" | "embeddings" | ...
  model: string | null; // a kérés törzséből vagy a válaszból
  messages: number; // fordulók száma
  tokensIn: number | null; // usage.prompt_tokens / usage.input_tokens
  tokensOut: number | null; // usage.completion_tokens / usage.output_tokens
  streamed: boolean; // true, ha SSE-válasz
  mappedTo: string | null; // x-omniroute-mapped fejléc
  costEstimateUsd: number | null; // az OmniRoute díjszabásán alapuló becsült költség
}
```

### 4.6 Élő, folyamatban lévő kérések szűrője

A kérés `status` mezője `number | "in-flight" | "error"` típusú — a bejegyzés
`"in-flight"` értékkel kerül be azonnal a kérés indulásakor, majd **helyben frissül**,
amikor megérkezik a válasz (vagy a hiba). Az eszköztár **„Élő”** kapcsolója
(`liveOnly`, i18n-kulcs: `trafficInspector.liveOnly`) azokra a bejegyzésekre
korlátozza a listát, amelyeknél `status === "in-flight"`, így valós időben figyelhetők a nyitott kapcsolatok.

A szűrő egy tiszta, kliensoldali predikátum a
`src/lib/inspector/matchesTrafficFilter.ts` fájlban:

```ts
if (f.liveOnly && req.status !== "in-flight") return false;
```

A kapcsoló állapota a `useTrafficFilters` elemben található (az ellenőrző irányítópult hookjai), és
a többi szűrővel (profil, hoszt, ügynök, forrás, állapot, kontextus) együtt érvényesül.

### 4.7 Folyamat-hozzárendelés (Linux)

Linux rendszeren minden elfogott kérés hozzárendelhető az azt **kezdeményező helyi
folyamathoz**. Két opcionális mező kerül az `InterceptedRequest` típusba:

```ts
pid?: number;          // a kezdeményező folyamat azonosítója (csak Linux)
processName?: string;  // a kezdeményező folyamat neve (csak Linux)
```

A `src/mitm/inspector/processAttribution.ts` a kapcsolat _kliensoldali_
ideiglenes portját a következő módon rendeli hozzá egy PID-hez és névhez:

1. Beolvassa a `/proc/net/tcp` és `/proc/net/tcp6` fájlokat, hogy megkeresse a
   porthoz tartozó socket inode-ot (`parseProcNetTcpForInode`, egy tiszta, fixture-ökkel tesztelhető elemző).
2. Átvizsgálja a `/proc/<pid>/fd/` könyvtárat a `socket:[<inode>]` célra mutató szimbolikus hivatkozásért.
3. Beolvassa a folyamat nevét a `/proc/<pid>/comm` fájlból.

Egy 1 másodperces TTL-gyorsítótár terhelés alatt korlátozza a procfs átvizsgálásának költségét. A hozzárendelés
**legjobb igyekezet szerinti** — bármilyen hiba esetén `null` értéket ad vissza, és soha nem blokkolja a rögzítést.
macOS/Windows rendszeren a függvény `null` értéket ad vissza (helykitöltő; az `lsof`/`GetExtendedTcpTable`
támogatása egy későbbi feladat).

---

## §5 Munkamenetek

### 5.1 Munkamenet rögzítése

1. Kattintson az eszköztár **„● Munkamenet rögzítése”** elemére → adjon meg egy nevet (nem kötelező)
2. Az élő követés a megszokott módon folytatódik; egy pirosan pulzáló jelző ezt mutatja: `◉ FELVÉTEL · <név> · 00:42 · 23 kérés`
3. Kattintson a **„⏹ Leállítás”** elemre → a rendszer a munkamenet pillanatképét az `inspector_sessions` és az `inspector_session_requests` tárolókba menti

### 5.2 Rögzített munkamenet megtekintése

Az eszköztár **Munkamenetek** legördülő menüje felsorolja a mentett munkameneteket. Egy munkamenet kiválasztásakor:

- Betöltődik a munkamenet pillanatképe (rögzített állapot)
- Megjelenik egy szalaghirdetés: `A(z) „<név>” rögzített munkamenet megtekintése — [Vissza az élő nézethez]`
- Elérhetővé válik a Statisztikák lap a Recharts összesítéseivel

### 5.3 Exportálási formátumok

Minden munkamenet az alábbi formátumokban exportálható:

| Formátum                   | Felhasználás                                                                                     |
| -------------------------- | ------------------------------------------------------------------------------------------------ |
| **HAR** (HTTP Archive 1.2) | Kompatibilis a Chrome DevTools, Charles és Fiddler eszközökkel — importálható offline elemzéshez |
| **JSONL**                  | Soronként egy `InterceptedRequest` — kompatibilis az `llm-interceptor` formátumával              |

Az exportálás a `GET /api/tools/traffic-inspector/sessions/{id}/export.har` végponton vagy a Munkamenetek legördülő menü ⬇ gombjával végezhető el.

---

## §6 Biztonság

A Traffic Inspector **minden elfogott HTTPS-forgalmat** megjelenít, beleértve az engedélyezési fejléceket és a kéréstörzseket is. A következő védelmi intézkedések vannak érvényben:

| Védelmi intézkedés                              | Részletek                                                                                                                                                                                                   |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **LOCAL_ONLY**                                  | Minden útvonal és a WebSocket-végpont kizárólag a visszacsatolási felületről érhető el (ezt a `routeGuard.ts` még a hitelesítés előtt kikényszeríti)                                                        |
| **Titkok maszkolása**                           | A lineáris `maskSecret()` kereső kitakarja az RFC 6750 Bearer hitelesítő adatokat, a szolgáltatói előtaggal ellátott kulcsokat és a hosszú, átlátszatlan tokeneket a `TrafficBuffer.push()` meghívása előtt |
| **Törzsméret korlátozása**                      | Az `INSPECTOR_MAX_BODY_KB` értéknél (alapértelmezés szerint 1024 KB) nagyobb törzsek csonkolva jelennek meg a `„(a teljesítmény érdekében csonkolva)”` értesítéssel                                         |
| **Fejlécek megtisztítása**                      | A neveket kisbetűssé alakítja; a keretezési, ugrásonkénti és proxyhitelesítési fejléceket eldobja; a sütiket teljesen kitakarja; a hitelesítő adatok értékeinek kezelését a `maskSecret()` függvényre bízza |
| **CSP**                                         | A Traffic Inspector oldalain szigorú Content Security Policy akadályozza meg a beszúrt választörzseken keresztüli XSS-támadásokat                                                                           |
| **Alapértelmezés szerint nincs tartós tárolás** | A `TrafficBuffer` a memóriában található, és a kiszolgáló újraindításakor elvész. A munkamenetek csak kifejezett rögzítés esetén kerülnek tartós tárolásra                                                  |

### Alkalmazott szigorú szabályok

| Szabály                           | Alkalmazás                                                                             |
| --------------------------------- | -------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`    | A Traffic Inspector útvonalainak minden HTTP-hibaválasza megtisztításra kerül          |
| **#15 + #17** `isLocalOnlyPath()` | A `/api/tools/traffic-inspector/` LOCAL_ONLY + SPAWN_CAPABLE (rendszerproxy-parancsok) |

### Ismert korlátozások

- **A rendszerszintű proxymód** a gépen futó összes alkalmazást érinti, beleértve a VPN-klienseket és az SSO-t is. Mindig az automatikus kikapcsolási időzítővel együtt használja. Megosztott gépeken ne használja.
- **CONNECT-alagutas HTTPS**: A 3. mód (HTTP_PROXY) HTTPS-célhelyek esetén csak az alagút metaadatait rögzíti, kivéve, ha engedélyezve van a TLS-elfogás. Ez szándékos — ha az adott alkalmazások nem tekintenék megbízhatónak az AgentBridge tanúsítványát, az átlátszó rögzítés megszakítaná a TLS-ellenőrzést.
- **Egyes komponensekbe beégetett karakterláncok**: Néhány felhasználóifelület-komponensben (F7/F8) található néhány olyan beégetett karakterlánc, amelyet az i18n-kulcsok még nem fednek le. Ezek az i18n-hiányosságokról szóló jelentés Ismert korlátozás szakaszában vannak dokumentálva; egy későbbi frissítés során kerülnek áttelepítésre. Az érintett karakterláncok a felhasználói felület olyan dekoratív címkéi, amelyek funkcionális használatához nincs szükség fordításra.

---

## §7 Hibaelhárítás

### WebSocket-kapcsolat megszakadása

Ha az élő naplóban a „Kapcsolat megszakadt” üzenet jelenik meg:

1. Ellenőrizze, hogy a kiszolgáló még fut-e: `GET /api/tools/traffic-inspector/capture-modes`
2. Töltse újra az oldalt — a WebSocket újracsatlakozik, és friss pillanatképet kap
3. Ha a kiszolgálót újraindították, a memóriában tárolt puffer törlődött — a régi bejegyzések elvesztek, kivéve, ha munkamenetben rögzítették őket

### Ütközés a 8080-as porton

Ha a HTTP_PROXY mód nem indul el:

```bash
lsof -i :8080    # a folyamat megkeresése
```

Módosítsa a portot:

```bash
# .env
INSPECTOR_HTTP_PROXY_PORT=8888
```

### A rendszerproxy nem lett visszaállítva

Ha az OmniRoute összeomlik, miközben a rendszerszintű proxy mód aktív:

**macOS:**

```bash
networksetup -setwebproxystate Wi-Fi off
networksetup -setsecurewebproxystate Wi-Fi off
```

**Linux (GNOME):**

```bash
gsettings set org.gnome.system.proxy mode 'none'
```

**Windows:**

```cmd
netsh winhttp reset proxy
```

A vezérlőpult a következő betöltéskor felajánlja a „Rendszerproxy visszaállítása” lehetőséget is, ha az adatbázis állapota alapján azt észleli, hogy a proxy aktív volt.

### Megtelt puffer

Amikor a puffer eléri az `INSPECTOR_BUFFER_SIZE` értékét (alapértelmezés szerint 1000), az új bejegyzések kiszorítják a legrégebbieket. Ha fontos kérések vesznek el:

- Növelje az `INSPECTOR_BUFFER_SIZE` értékét (például 5000-re) — ez több memóriát használ a hosszabb megőrzés érdekében
- Rögzítsen egy munkamenetet, hogy a releváns időszak adatai megmaradjanak az adatbázisban

---

## §8 API-referencia

Minden útvonal `LOCAL_ONLY` (csak loopbacken érhető el) és `SPAWN_CAPABLE` (rendszerproxy-parancsok). Lásd: `src/server/authz/routeGuard.ts`.

Alapútvonal: `/api/tools/traffic-inspector/`

### Kérések kezelése

| Metódus | Útvonal                     | Leírás                                                                               |
| ------- | --------------------------- | ------------------------------------------------------------------------------------ |
| GET     | `/requests`                 | Kérések listázása (szűrhető: `?profile=llm&host=&agent=&status=&source=&sessionId=`) |
| GET     | `/requests/{id}`            | Egyetlen kérés részletei                                                             |
| DELETE  | `/requests`                 | A memóriában tárolt puffer törlése                                                   |
| POST    | `/requests/{id}/replay`     | Ugyanazon kérés újbóli végrehajtása az OmniRoute útválasztóján keresztül             |
| PUT     | `/requests/{id}/annotation` | Kéréshez tartozó megjegyzés mentése vagy frissítése                                  |

### WebSocket

| Metódus | Útvonal | Leírás                                                                                            |
| ------- | ------- | ------------------------------------------------------------------------------------------------- |
| GET     | `/ws`   | Élő WebSocket-adatfolyam. Csatlakozáskor `snapshot`, majd `new`/`update`/`clear` eseményeket küld |

### Exportálás

| Metódus | Útvonal       | Leírás                                                  |
| ------- | ------------- | ------------------------------------------------------- |
| GET     | `/export.har` | Az aktuális szűrt lista exportálása HAR 1.2 formátumban |

### Egyéni gazdagépek

| Metódus | Útvonal         | Leírás                                                               |
| ------- | --------------- | -------------------------------------------------------------------- |
| GET     | `/hosts`        | Egyéni gazdagépek listázása                                          |
| POST    | `/hosts`        | Gazdagép hozzáadása (automatikusan szerkeszti az `/etc/hosts` fájlt) |
| DELETE  | `/hosts/{host}` | Gazdagép eltávolítása                                                |
| PATCH   | `/hosts/{host}` | Az `enabled` állapot átváltása                                       |

### Rögzítési módok

| Metódus | Útvonal                        | Leírás                                                                                                       |
| ------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------ |
| GET     | `/capture-modes`               | Az AgentBridge / egyéni gazdagépek / HTTP_PROXY / rendszerproxy módok állapota és a `tls-intercept` kapcsoló |
| POST    | `/capture-modes/http-proxy`    | A HTTP_PROXY-figyelő indítása/leállítása (`{action: "start"\|"stop"}`)                                       |
| POST    | `/capture-modes/system-proxy`  | A rendszerszintű proxy alkalmazása/visszaállítása (`{action: "apply"\|"revert"}`)                            |
| POST    | `/capture-modes/tls-intercept` | A HTTPS-törzs visszafejtésének átváltása proxy módban (`{enabled: boolean}`)                                 |

> A **TPROXY-visszafejtést** (5. rögzítési mód) egy **különálló**, az
> AgentBridge előtag alatti útvonal vezérli — `GET / POST / DELETE /api/tools/agent-bridge/tproxy` —, nem
> az `/api/tools/traffic-inspector/` alatt. Lásd:
> `docs/security/MITM-TPROXY-DECRYPT.md` (git; nincs beépítve a `/docs` könyvtárba).

### Munkamenetek

| Metódus | Útvonal                     | Leírás                                                                 |
| ------- | --------------------------- | ---------------------------------------------------------------------- |
| POST    | `/sessions`                 | Rögzítés indítása (`{name?: string}`)                                  |
| PATCH   | `/sessions/{id}`            | Leállítás vagy átnevezés (`{action: "stop"\|"rename", name?: string}`) |
| GET     | `/sessions`                 | Az összes mentett munkamenet listázása                                 |
| GET     | `/sessions/{id}`            | Munkamenet-pillanatkép (minden kérés)                                  |
| DELETE  | `/sessions/{id}`            | Munkamenet törlése                                                     |
| GET     | `/sessions/{id}/export.har` | Munkamenet exportálása HAR 1.2 formátumban                             |

### Belső adatfogadás (D4 tartalékmechanizmus)

| Metódus | Útvonal            | Leírás                                                                                                                        |
| ------- | ------------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| POST    | `/internal/ingest` | Elfogadja a `server.cjs` átengedési útvonaláról érkező elfogott kérést; az `INSPECTOR_INTERNAL_INGEST_TOKEN` fejléc szükséges |

Teljes OpenAPI-sémák: `docs/openapi.yaml` → `Traffic Inspector` címke.
