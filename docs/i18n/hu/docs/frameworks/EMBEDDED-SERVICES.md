# Embedded Services (Magyar)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Verzió:** v3.8.44
> **Utolsó frissítés:** 2026-09-09
> **Célközönség:** Beágyazott szolgáltatásokat (9Router, CLIProxyAPI, Mux, Bifrost, open-wa) hozzáadó, karbantartó vagy hibakereső mérnökök.

A beágyazott szolgáltatások helyileg telepített, kiegészítő folyamatként működő eszközök, amelyeket az OmniRoute telepít, felügyel és
teljes értékű útválasztási célként tesz elérhetővé. A külső szolgáltatókkal ellentétben (amelyek API-kulcsok használatával,
az interneten keresztül érhetők el) a beágyazott szolgáltatások az OmniRoute-tal azonos gépen futnak, és a visszacsatolási interfészen keresztül kommunikálnak.

---

## Tartalomjegyzék

1. [Áttekintés](#1-overview)
2. [Architektúra — 4 réteg](#2-architecture--4-layers)
3. [Életciklus-állapotgép](#3-lifecycle-state-machine)
4. [API-referencia](#4-api-reference)
5. [Biztonság](#5-security)
6. [Új beágyazott szolgáltatás hozzáadása](#6-adding-a-new-embedded-service)
7. [Hibaelhárítás](#7-troubleshooting)
8. [GYIK](#8-faq)

---

## 1. Áttekintés

### Miért beágyazott szolgáltatások?

Hat szolgáltatás van beágyazva:

| Szolgáltatás    | npm-csomag                                    | Alapértelmezett port | Cél                                                                                                                                                                                                   |
| --------------- | --------------------------------------------- | :------------------: | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                                     |        20130         | AI-útválasztó, amelyet az OmniRoute alszolgáltatóként használhat. A modellek `9router/{sub}/{model}` formában érhetők el                                                                              |
| **CLIProxyAPI** | GitHub-kiadási bináris (`cliproxy`)           |         8317         | Helyi proxyadapter az Anthropic CLI hitelesítési folyamataihoz. Tartalék útválasztást biztosít az OAuth-tokenek lejáratakor                                                                           |
| **Mux**         | `mux` (grafikus felület nélküli `mux server`) |         8322         | Helyi ügynök-vezénylési démon (coder/mux). Csak az életciklusa felügyelt — nem útválasztási cél (nincs LLM-proxyzás).                                                                                 |
| **Bifrost**     | `@maximhq/bifrost`                            |         8080         | Go-alapú AI-átjáró továbbító háttérrendszer. Futás közben a továbbítási útvonal (`/v1/relay/`) automatikusan ezt választja                                                                            |
| **Dario**       | `@askalf/dario`                               |         3456         | Claude-előfizetési proxy — a CLIProxyAPI alternatívája vagy tartaléka a Claude Code formátumú forgalomhoz; a beinjektált kulcs `DARIO_ADMIN_TOKEN` lesz, amely védi az `/admin/*` OAuth-vezérlősíkját |
| **open-wa**     | `@open-wa/wa-automate`                        |         8323         | WhatsApp Web-automatizálás (grafikus felület nélküli Chromium a Puppeteer segítségével). Csak az életciklusa felügyelt — nem útválasztási cél.                                                        |

Mind a hat ugyanazt a felügyeleti modellt követi:

- Az OmniRoute a `DATA_DIR/services/{name}/` könyvtárba telepíti őket (az OmniRoute saját `package.json` fájljától elkülönítve)
- Az OmniRoute gyermekfolyamatként indítja és felügyeli őket
- Az OmniRoute egy ideiglenes API-kulcsot injektál a gyermekfolyamat környezetébe, és ahol alkalmazható, leállás nélkül cseréli azt
- Minden felügyeleti útvonal (`/api/services/*`) **LOCAL_ONLY** — kizárólag a visszacsatolási címről érhető el (a 17. megváltoztathatatlan szabály)

### Fő döntések (a tervezési tervből)

| Döntés                                                 | Érték                                                                                                             |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------- |
| A 9Router natív felületének elérése az irányítópultról | Fordított proxy a `/dashboard/providers/services/9router/embed/*` útvonalon                                       |
| Telepítési mechanizmus                                 | `npm install {package}` az `execFile` használatával (parancsértelmező-behelyettesítés nélkül)                     |
| Használati mód                                         | A szolgáltató `9router/{sub}/{model}` formában van regisztrálva az útválasztási motorban                          |
| API-kulcsok kezelése                                   | Az OmniRoute generálja, nyugalmi állapotban titkosítja (AES-256-GCM), és környezeti változón keresztül injektálja |
| Az irányítópult helye                                  | `/dashboard/providers/services` (három lap)                                                                       |
| Automatikus indítás                                    | Szolgáltatásonként kapcsolható, alapértelmezés szerint KIKAPCSOLVA                                                |

---

## 2. Architektúra — 4 réteg

```
┌────────────────────────────────────────────────────────────────────┐
│  1. réteg — Felhasználói felület                                  │
│  /dashboard/providers/services  (lapok: CLIProxyAPI | 9Router | Mux)│
│  Élő naplók (SSE), Indítás/Leállítás/Újraindítás/Frissítés,        │
│  Beállítások, Telepítés                                            │
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Keret + lapválasztás a ?tab= alapján │
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  2. réteg — API (LOCAL_ONLY — csak visszacsatolási interfész)      │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (fordított HTTP- és WebSocket-proxy → 9Router upstream)         │
│                                                                    │
│  Védelem: a LOCAL_ONLY_API_PREFIXES tartalmazza az                 │
│        "/api/services/" és                                        │
│        "/dashboard/providers/services/*/embed/" útvonalakat       │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ folyamaton belüli hívások
┌──────────────────────▼─────────────────────────────────────────────┐
│  3. réteg — ServiceSupervisor (src/lib/services/)                  │
│                                                                    │
│  ServiceSupervisor.ts   Általános felügyelő (child_process.spawn)  │
│    ├── telepítés:  execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── indítás:    spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── port:       20130 a 9Router számára (konfigurálható)        │
│    ├── naplók:     5 MB-os stdio körkörös puffer → SSE-események   │
│    ├── állapot:    HTTP GET /health 2–5 mp-enként, lusta helyreállítás│
│    └── életciklus: SIGTERM 15 mp → SIGKILL                         │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Az összes SERVICES[] indítása a folyamat kezdetén│
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       Időszakos GET /v1/models → service_models tábla│
│  ringBuffer.ts      Körkörös naplópuffer (szolgáltatásonként 5 MB) │
│  healthCheck.ts     Lekérdezéses HTTP-állapotvizsgálat             │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (telepítőadapterek)                           │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ OpenAI-kompatibilis HTTP (visszacsatolás)
┌──────────────────────▼─────────────────────────────────────────────┐
│  4. réteg — Szolgáltató / Útválasztás                              │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Kérésenként újra lekéri a portot és az API-kulcsot (nincs gyorsítótárazás).│
│    Proxyzás előtt eltávolítja a "9router/" előtagot a modellazonosítóból.│
│    503 service_not_running választ ad, ha a felügyelő nem "running" állapotú.│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    A "9router" bejegyzése: isEmbeddedService: true                 │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    A modellek "9router/{sub}/{model}" formában tárolódnak (előtaggal).│
│    A modelSync.ts 5 percenként szinkronizálja őket.                │
│                                                                    │
│  A Mux CSAK életciklus-kezelést kap (1–3. réteg) — ez egy ügynök-  │
│  vezénylési démon, nem LLM-proxy, ezért nincs 4. rétegbeli         │
│  végrehajtó-/szolgáltatóbejegyzése, és soha nem útválasztási cél.  │
└────────────────────────────────────────────────────────────────────┘
```

### Fő forrásfájlok

| Fájl                                        | Szerep                                                     |
| ------------------------------------------- | ---------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Alaposztály: életciklus, zárolás, állapot, körkörös puffer |
| `src/lib/services/bootstrap.ts`             | Folyamatszintű regisztráció és automatikus indítás         |
| `src/lib/services/registry.ts`              | Egyke példánytárkép: `eszköz → felügyelő`                  |
| `src/lib/services/apiKey.ts`                | Kulcsgenerálás, nyugalmi AES-256-GCM-titkosítás            |
| `src/lib/services/modelSync.ts`             | Időszakos modellszinkronizálás (5 perc) + igény szerint    |
| `src/lib/services/ringBuffer.ts`            | 5 MB-os körkörös naplópuffer SSE-feliratkozással           |
| `src/lib/services/healthCheck.ts`           | HTTP-állapotellenőrzés (konfigurálható időközzel)          |
| `src/lib/services/installers/ninerouter.ts` | npm-telepítés/-frissítés/-eltávolítás a 9Routerhez         |
| `src/lib/services/installers/cliproxy.ts`   | npm-telepítés/-frissítés/-eltávolítás a CLIProxyAPI-hoz    |
| `src/lib/services/installers/mux.ts`        | npm-telepítés/-frissítés/-eltávolítás a Muxhoz             |
| `src/lib/services/installers/openwa.ts`     | npm-telepítés/-frissítés/-eltávolítás az open-wa-hoz       |
| `src/app/api/services/9router/_lib.ts`      | `getOrInitSupervisor()` segédfüggvény                      |
| `src/app/api/services/[name]/logs/route.ts` | Megosztott SSE-naplózási végpont                           |
| `open-sse/executors/ninerouter.ts`          | Szolgáltatói végrehajtó (4. réteg)                         |

---

## 3. Életciklus-állapotgép

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
            sikeres állapotpróba   │         összeomlás / SIGTERM│
                               ┌────▼─────┐  (kilépés 5 mp-en belül)
                               │ running  │── összeomlás ───────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Az állapotokat a `version_manager` adatbázistábla (`status` oszlop) tárolja, és
a `ServiceSupervisor` memóriabeli állapota tükrözi. Futó folyamat esetén a memóriabeli
állapot a mérvadó; rendszerindításkor az adatbázis állapota biztosítja a tartós tartalékot.

### Állapotátmenetek

| Innen           | Esemény                                          | Ide                        |
| --------------- | ------------------------------------------------ | -------------------------- |
| `not_installed` | az `install()` sikeres                           | `stopped`                  |
| `stopped`       | a `start()` meghívása                            | `starting`                 |
| `starting`      | az állapotpróba 200-as választ ad                | `running`                  |
| `starting`      | a folyamat még az egészséges állapot előtt kilép | `error`                    |
| `running`       | a `stop()` meghívása                             | `stopping` → `stopped`     |
| `running`       | a folyamat váratlanul kilép (< 5 mp)             | `error` (gyors összeomlás) |
| `running`       | a folyamat váratlanul kilép (> 5 mp)             | `error`                    |
| `error`         | a `start()` meghívása                            | `starting`                 |
| bármely         | `stop()` `stopping` állapotban                   | nincs művelet              |

### Műveleti zárolás

A `ServiceSupervisor` egy aszinkron műveleti záron (`withLock()`) keresztül
sorosítja az életciklus-műveleteket. Az ugyanazon felügyelőn végrehajtott egyidejű
`start()` hívások pontosan egy folyamatindítást eredményeznek; a második hívó vár,
majd visszaadja a meglévő állapotot. Ez megakadályozza a versenyhelyzeteket például
akkor, amikor az automatikus indítás és egy felhasználói felületi gomb egyszerre aktiválódik.

---

## 4. API-referencia

A `/api/services/` alatti összes útvonal **LOCAL_ONLY** (csak visszacsatolási címről érhető el, kötelező szabály #17).
A nem visszacsatolási címről érkező kérések a hitelesítési tokentől függetlenül `403 LOCAL_ONLY` választ kapnak.

### 4.1 9Router-végpontok (11 útvonal)

#### `POST /api/services/9router/install`

Telepíti a 9Routert az npm használatával. Létrehozza a `DATA_DIR/services/9router/` könyvtárat saját
`package.json` és `node_modules/` tartalommal. Nem ütközik az OmniRoute saját függőségeivel.

**Kérés törzse** (minden mező opcionális):

```json
{ "version": "latest" }
```

| Mező      | Típus    | Alapértelmezett | Leírás                                    |
| --------- | -------- | --------------- | ----------------------------------------- |
| `version` | `string` | `"latest"`      | A telepítendő npm-verziócímke vagy semver |

**Válaszok:**

| Állapot | Leírás                                                                        |
| ------- | ----------------------------------------------------------------------------- |
| `200`   | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                        |
| `400`   | Érvénytelen kéréstörzs (sikertelen Zod-validáció)                             |
| `409`   | A telepítés már folyamatban van (a zárolás aktív)                             |
| `500`   | Az npm-telepítés sikertelen — a közérthető hiba a `message` mezőben található |

**Megjegyzések:** Az `execFile('npm', [...])` használatával működik — parancsértelmező és interpoláció nélkül (kötelező szabály #13).
Az EACCES-hibák közérthető üzenetekként jelennek meg.

---

#### `POST /api/services/9router/start`

Elindítja a 9Routert. Regisztrál egy felügyelőt, ha még nincs regisztrálva, majd meghívja a
`supervisor.start()` metódust. Ha már fut, a művelet idempotens.

**Kérés törzse:** nincs

**Válaszok:**

| Állapot | Leírás                                                   |
| ------- | -------------------------------------------------------- |
| `200`   | `ServiceStatus` objektum (lásd az alábbi sémát)          |
| `409`   | A 9Router nincs telepítve (`status: "not_installed"`)    |
| `503`   | Az indítás sikertelen (folyamathiba — lásd: `lastError`) |

**ServiceStatus séma:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Szabályosan leállítja a 9Routert. SIGTERM jelet küld, 15 másodpercet vár, majd SIGKILL jelet küld, ha a folyamat még mindig fut.
Ha már leállt, a művelet idempotens.

**Kérés törzse:** nincs

**Válaszok:**

| Állapot | Leírás                               |
| ------- | ------------------------------------ |
| `200`   | `ServiceStatus` (`state: "stopped"`) |
| `503`   | A leállítás váratlanul sikertelen    |

---

#### `POST /api/services/9router/restart`

Egyenértékű a `stop()`, majd a `start()` meghívásával a műveleti zároláson belül.

**Kérés törzse:** nincs

**Válaszok:** megegyeznek a `start` válaszaival (a végleges `ServiceStatus` objektumot adja vissza).

---

#### `POST /api/services/9router/update`

Frissíti a 9Routert egy újabb npm-verzióra. Ha a szolgáltatás fut, először
leállítja, majd futtatja az npm-telepítést (helyben telepítve az újabb verziót), végül
újraindítja a szolgáltatást.

**Kérés törzse** (minden mező opcionális):

```json
{ "version": "latest" }
```

**Válaszok:**

| Állapot | Leírás                                                          |
| ------- | --------------------------------------------------------------- |
| `200`   | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`   | Érvénytelen törzs                                               |
| `500`   | Az npm-frissítés sikertelen                                     |

---

#### `POST /api/services/9router/rotate-key`

Új API-kulcsot hoz létre a 9Routerhez, titkosítja azt tároláskor, és újraindítja a szolgáltatást
(ha fut), hogy az új kulcsot betöltse a környezetéből. A régi kulcs
azonnal érvénytelenné válik.

**Kérés törzse:** nincs

**Válaszok:**

| Állapot | Leírás                                     |
| ------- | ------------------------------------------ |
| `200`   | `{ keyRotated: true, restarted: boolean }` |
| `500`   | A kulcscsere sikertelen                    |

**Biztonság:** Az új kulcs soha nem szerepel a válaszban (nincs hitelesítőadat-szivárgás).
AES-256-GCM algoritmussal titkosítva tárolódik a `version_manager` táblában.

---

#### `GET /api/services/9router/status`

Visszaadja az egyesített élő és adatbázisbeli állapotot, beleértve a verzió metaadatait és az API-kulcs előnézetét.

**Válaszok:**

| Állapot | Leírás                           |
| ------- | -------------------------------- |
| `200`   | Lásd az alábbi sémát             |
| `500`   | Az állapot beolvasása sikertelen |

**Válaszséma:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Be- vagy kikapcsolja az automatikus indítás jelzőjét. Ha `enabled: true`, a szolgáltatás automatikusan
elindul az OmniRoute következő rendszerindításakor (ha a szolgáltatás telepítve van).

**Kérés törzse:**

```json
{ "enabled": true }
```

**Válaszok:**

| Állapot | Leírás                |
| ------- | --------------------- |
| `200`   | `{ autoStart: true }` |
| `400`   | Érvénytelen törzs     |

---

#### `GET /api/services/9router/logs`

A 9Router stdout/stderr gyűrűs pufferéből származó élő naplók SSE-adatfolyama.

**Lekérdezési paraméterek:**

| Paraméter | Típus     | Alapértelmezett | Leírás                                                                                       |
| --------- | --------- | --------------- | -------------------------------------------------------------------------------------------- |
| `tail`    | `integer` | 200             | Az először elküldendő korábbi sorok száma (legfeljebb 1000)                                  |
| `filter`  | `string`  | nincs           | Kis- és nagybetűket figyelmen kívül hagyó részszűrő (nem reguláris kifejezés — ReDoS-biztos) |

**SSE-események:**

| Esemény     | Adatok      | Leírás                               |
| ----------- | ----------- | ------------------------------------ |
| `snapshot`  | `LogLine[]` | Kezdeti korábbi naplórészlet         |
| `log`       | `LogLine`   | Élő naplósor                         |
| `heartbeat` | `{}`        | Életben tartó jel 15 másodpercenként |

**LogLine séma:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Válaszok:**

| Állapot | Leírás                                                         |
| ------- | -------------------------------------------------------------- |
| `200`   | `text/event-stream`                                            |
| `400`   | A `filter` paraméter túl hosszú (> 200 karakter)               |
| `404`   | A szolgáltatás nem található (a supervisor nincs regisztrálva) |

---

### 4.2 CLIProxyAPI-végpontok (10 útvonal)

A CLIProxyAPI végpontstruktúrája megegyezik a 9Routerével, a `rotate-key` kivételével, továbbá az
`accounts`, `provider-expose` és `auto-restart-adopted` végpontokkal egészül ki. Mostantól egy
dedikált, az indításkor beinjektált adatsíkbeli API-kulcsot kap (`needsApiKey: true` a
`bootstrap.ts` fájlban, amelyet a modellszinkronizáláshoz használ); a `status` kevesebb mezőt tartalmaz.

| Metódus | Útvonal                             | Leírás                                     |
| ------- | ----------------------------------- | ------------------------------------------ |
| `POST`  | `/api/services/cliproxy/install`    | A CLIProxyAPI telepítése npm-ből           |
| `POST`  | `/api/services/cliproxy/start`      | A CLIProxyAPI elindítása                   |
| `POST`  | `/api/services/cliproxy/stop`       | A CLIProxyAPI leállítása                   |
| `POST`  | `/api/services/cliproxy/restart`    | A CLIProxyAPI újraindítása                 |
| `POST`  | `/api/services/cliproxy/update`     | Frissítés újabb verzióra                   |
| `GET`   | `/api/services/cliproxy/status`     | Élő + DB-állapot (`apiKeyMasked` nélkül)   |
| `POST`  | `/api/services/cliproxy/auto-start` | Az automatikus indítás be- és kikapcsolása |

A megosztott `GET /api/services/{name}/logs` végpont (lásd: §4.1) mind a
négy szolgáltatáshoz használható a dinamikus `[name]` szegmenssel.

---

### 4.3 Mux-végpontok (8 útvonal)

A Mux végpontstruktúrája megegyezik a CLIProxyAPI-éval — az API-felületen nincs
`rotate-key` útvonal (a bearer token a 9Routeréhez hasonlóan, a
`getOrCreateApiKey("mux")` használatával jön létre, és a `MUX_SERVER_AUTH_TOKEN`
környezeti változón keresztül kerül beinjektálásra, de külön rotációs végpont még
nincs). A Mux csak életciklus-kezelést kap: a 9Routerrel ellentétben nem rendelkezik 4. rétegbeli végrehajtóval, és soha nincs útválasztási szolgáltatóként regisztrálva.

| Metódus | Útvonal                        | Leírás                                     |
| ------- | ------------------------------ | ------------------------------------------ |
| `POST`  | `/api/services/mux/install`    | A Mux telepítése npm-ből (`npm i mux`)     |
| `POST`  | `/api/services/mux/start`      | A Mux elindítása (`mux server`)            |
| `POST`  | `/api/services/mux/stop`       | A Mux leállítása                           |
| `POST`  | `/api/services/mux/restart`    | A Mux újraindítása                         |
| `POST`  | `/api/services/mux/update`     | Frissítés újabb npm-verzióra               |
| `GET`   | `/api/services/mux/status`     | Élő + DB-állapot                           |
| `POST`  | `/api/services/mux/auto-start` | Az automatikus indítás be- és kikapcsolása |

---

### 4.4 Bifrost-végpontok (8 útvonal)

A Bifrost egy Go nyelven készült AI-átjáró továbbító háttérrendszer (`@maximhq/bifrost`). Ugyanazt a
végpontstruktúrát használja, mint a CLIProxyAPI (`rotate-key` nélkül — a Bifrost a saját szolgáltatói
kulcsait a `config.json` fájlban, a `-app-dir` alatt kezeli).

| Metódus | Útvonal                            | Leírás                                                                      |
| ------- | ---------------------------------- | --------------------------------------------------------------------------- |
| `POST`  | `/api/services/bifrost/install`    | A Bifrost telepítése npm-ből (`@maximhq/bifrost`)                           |
| `POST`  | `/api/services/bifrost/start`      | A Bifrost elindítása a 8080-as porton (alapértelmezett)                     |
| `POST`  | `/api/services/bifrost/stop`       | A Bifrost leállítása                                                        |
| `POST`  | `/api/services/bifrost/restart`    | A Bifrost újraindítása                                                      |
| `POST`  | `/api/services/bifrost/update`     | Frissítés újabb verzióra                                                    |
| `GET`   | `/api/services/bifrost/status`     | Élő + DB-állapot                                                            |
| `POST`  | `/api/services/bifrost/auto-start` | Az automatikus indítás be- és kikapcsolása                                  |
| `GET`   | `/api/services/bifrost/logs`       | SSE-naplókövetés (a megosztott dinamikus `[name]/logs` útvonalon keresztül) |

**Útválasztási összekapcsolás:** Ha a `BIFROST_BASE_URL` nincs beállítva, és a felügyelt Bifrost-példány
fut, a `getBifrostRoutingConfig()` (a `routingBackend.ts` fájlban) automatikusan a
`http://127.0.0.1:{port}` címet használja a továbbító alap-URL-jeként. Az explicit módon beállított
`BIFROST_BASE_URL` környezeti változó mindig elsőbbséget élvez.

---

### 4.5 Dario-végpontok (12 útvonal)

Az életciklus-struktúra megegyezik a többi szolgáltatáséval (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`), és kiegészül egy tokennel védett OAuth
vezérlősíkkal az `admin/` alatt: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (mindegyiket a `DARIO_ADMIN_TOKEN` védi).

### 4.6 open-wa-végpontok (7 útvonal)

Az open-wa (`@open-wa/wa-automate`) egy headless Chromium-példányt vezérel (a
Puppeteer segítségével) a WhatsApp Web automatizálásához. Ugyanazt a végpontstruktúrát
használja, mint a Mux (`rotate-key` útvonal még nincs). Csak életciklus-kezelést kap —
nem útválasztási cél, és nincs 4. rétegbeli végrehajtó-/szolgáltatóbejegyzése.

| Metódus | Útvonal                           | Leírás                                                                     |
| ------- | --------------------------------- | -------------------------------------------------------------------------- |
| `POST`  | `/api/services/openwa/install`    | Az open-wa telepítése npm-ből (`@open-wa/wa-automate`)                     |
| `POST`  | `/api/services/openwa/start`      | Az open-wa elindítása a 8323-as porton (alapértelmezett)                   |
| `POST`  | `/api/services/openwa/stop`       | Az open-wa leállítása                                                      |
| `POST`  | `/api/services/openwa/restart`    | Az open-wa újraindítása                                                    |
| `POST`  | `/api/services/openwa/update`     | Frissítés újabb verzióra                                                   |
| `GET`   | `/api/services/openwa/status`     | Élő + adatbázisbeli állapot                                                |
| `POST`  | `/api/services/openwa/auto-start` | Automatikus indítás be- vagy kikapcsolása                                  |
| `GET`   | `/api/services/openwa/logs`       | SSE-naplófolyam (a megosztott `[name]/logs` dinamikus útvonalon keresztül) |

**API-kulcs:** `WA_KEY` értékként kerül átadásra — az open-wa általános, `WA_*` előtaggal ellátott környezetiváltozó-felülbírálása ezt a `--key`/`-k` CLI-beállításhoz rendeli
(`dist/cli/setup.js::envArgs()`, ellenőrizve a telepített 4.76.0
csomaggal). A `generateServiceApiKey()` általi létrehozáskor `ow_` előtagot kap. Az open-wa
a kulcsot egy `key`/`api_key` HTTP-fejlécből olvassa vissza (nem az `Authorization:
Bearer` fejlécből); a `/api-docs*` kifejezetten mentesül az ellenőrzés alól
(`setupAuthenticationLayer` a `dist/cli/server.js` fájlban), így az állapotellenőrzéshez
nincs szükség hitelesítési fejlécre.

**Párosítás:** az open-wa nem hivatalos, és nem áll kapcsolatban a WhatsApp-pal — a
csatlakoztatott számot kitiltási kockázatnak teszi ki a WhatsApp saját automatizálásészlelése.
Az első indításkor a párosításhoz szükséges QR-kód a szabványos kimenetre kerül, és a
meglévő Naplók panelen/SSE-adatfolyamon keresztül jelenik meg — ebben az integrációban
egyelőre nincs külön QR-kép-végpont.

---

### 4.7 Fordított proxy (a 9Router irányítópult beágyazása)

Az irányítópult egy belső fordított proxyn keresztül iframe-be ágyazza a 9Router webes
felhasználói felületét a következő címen:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Ez a proxy:

- Továbbítja a kérést a `http://127.0.0.1:{port}/{path}` címre (csak visszacsatolási interfész)
- Eltávolítja a bejövő `cookie` és `authorization` fejléceket (így az OmniRoute-munkamenet nem szivárog ki)
- Beilleszti az `Authorization: Bearer {apiKey}` fejlécet a 9Router-hitelesítéshez
- Eltávolítja a válaszból a `set-cookie`, `content-security-policy`, `x-frame-options` és `cross-origin-*` fejléceket
- Átírja a HTML-válaszokat egy `<base href>` elem beillesztéséhez és az abszolút útvonalak normalizálásához (`/foo` → `/dashboard/.../embed/foo`)

A beágyazott irányítópult WebSocket-frissítéseit egy külön porton működő kiegészítő
kiszolgáló kezeli (lásd: `src/lib/services/embedWsProxy.ts`).

**Biztonság:** A beágyazási proxy útvonalai a `LOCAL_ONLY_API_PREFIXES` alá vannak besorolva,
és csak a visszacsatolási interfészről érhetők el. Az a támadó, aki egy
Cloudflare-/Ngrok-alagúton keresztül JWT-t szerez, nem tud hozzáférni a beágyazott
szolgáltatásokhoz a proxyn keresztül.

---

## 5. Biztonság

### LOCAL_ONLY kikényszerítése (17. szigorú szabály)

A `/api/services/` és `/dashboard/providers/services/*/embed/` alatti összes útvonal
LOCAL_ONLY besorolású a `src/server/authz/routeGuard.ts` fájlban. A loopback-ellenőrzés
minden hitelesítési ág előtt, feltétel nélkül lefut:

```
a kérés beérkezik
  → isLocalOnlyPath(path)?
      → nem loopback → 403 LOCAL_ONLY (mindig, a hitelesítés ellenőrzése előtt)
      → loopback     → továbblépés a normál hitelesítéshez
```

Ez megakadályozza, hogy egy kiszivárgott JWT (például egy tunnelen keresztül) `npm install`
futtatását vagy folyamatindítást váltson ki. A teljes szintmátrixot lásd a
`docs/security/ROUTE_GUARD_TIERS.md` fájlban.

### API-kulcs injektálása

A 9Router és a Mux saját HTTP-végpontjaihoz API-kulcsot/bearer tokent igényel.
Az OmniRoute:

1. Kulcsot generál a `crypto.randomBytes(32).toString("base64url")` használatával,
   szolgáltatásspecifikus előtaggal (`nr_` a 9Router, `mx_` a Mux esetén).
2. AES-256-GCM használatával titkosítja a kulcsot tároláskor (ugyanazzal a titkosítóval,
   amelyet a szolgáltatói hitelesítő adatokhoz használ).
3. A folyamat indításakor visszafejti és környezeti változóként injektálja —
   `NINEROUTER_API_KEY` a 9Router, `MUX_SERVER_AUTH_TOKEN` a Mux esetén (soha nem
   CLI-jelzőként, így a token soha nem jelenik meg a `ps` kimenetében vagy a
   folyamatlistákban).
4. Soha nem adja vissza a titkosítatlan kulcsot egyetlen HTTP-válaszban sem.

A CLIProxyAPI egy dedikált adatsíkbeli kulcsot kap a folyamat indításakor
(`needsApiKey: true` — az adapterrel végzett modellszinkronizáláshoz használatos).

### SSRF elleni védelem

A fordított HTTP-proxy (`/dashboard/.../embed/[...path]`) rögzített módon kizárólag a
`http://127.0.0.1:{port}` címre továbbít. Soha nem követ nem loopback célokra mutató
átirányításokat. Az `ssrf-req-filter` könyvtár elutasít minden olyan upstream URL-t,
amely a loopback-tartományon kívüli címre oldódik fel.

### Shellbiztonság (13. szigorú szabály)

Az `npm install` meghívása az `execFile('npm', ['install', pkg, '--prefix', dir])`
használatával történik — nincsenek sablonliterálok, nincs shell, és a külső elérési utak
nincsenek beillesztve a parancssztringbe. A futásidejű értékek (portok, API-kulcsok) a
gyermekfolyamat `env` objektumán keresztül kerülnek átadásra.

### Hibák megtisztítása (12. szigorú szabály)

Az `/api/services/*` összes hibaválasza a `buildErrorBody()` vagy a
`sanitizeErrorMessage()` függvényen halad keresztül. A nyers `err.stack` és
`err.message` soha nem kerül változtatás nélkül visszaadásra a hívónak.

---

## 6. Új beágyazott szolgáltatás hozzáadása

Kövesse ezt a 8 lépést. Tekintse a `src/lib/services/installers/` és
`src/app/api/services/` meglévő implementációit irányadó referenciának.

### 1. lépés — A telepítő létrehozása

Hozza létre a `src/lib/services/installers/{name}.ts` fájlt a `ninerouter.ts` mintájára:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // válasszon egy szabad portot

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Használja az `installers/utils.ts` fájlból származó
`runNpm(['install', NAME_PACKAGE, '--prefix', dir])` hívást — soha ne használjon
`execSync`-et vagy shell-interpolációt.

### 2. lépés — Regisztráció a bootstrapban

Adjon hozzá egy `ServiceEntry` elemet a `src/lib/services/bootstrap.ts` fájlban található
`SERVICES` tömbhöz:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false, ha nincs szükség API-kulcsra
}
```

Bővítse a `buildSpawnArgsFactory()` függvényt a `cfg.tool === "myservice"` esetének
kezelésével.

#### Bővíthető szolgáltatói beépülőmodul-szerződés (1. fázis, #7333)

A `src/lib/services/providerPlugins/` egy `ServiceProviderPlugin` szerződést vezet be,
amely egyetlen objektumba csomagolja egy háttérrendszer `bootstrap.ts` fájlbeli
`ServiceEntry` mezőit és a `serviceBackends.ts` jegyzéksablonmezőit, ahelyett, hogy
ugyanazon háttérrendszer struktúrája két, egymástól független fájlban külön-külön lenne
megadva. Jelenleg **csak a `9router` lett migrálva** — a `bootstrap.ts` a `SERVICES[]`
bejegyzését a `getServiceProviderPlugin("9router")`
(`src/lib/services/providerPlugins/registry.ts`) alapján származtatja, és indítási hibát
dob, ha a beépülő modul valaha hiányzik. A `cliproxy`, `mux` és `bifrost` változatlanul
a korábban meglévő, soron belüli `SERVICES[]` literálokat használja.

Az `open-sse/config/providerPluginManifest.ts` egy additív
`createServiceBackendManifestEntry(pluginId, template)` segédfüggvénnyel is bővült,
amely egy helyesen kialakított `ProviderPluginManifestEntry` elemet hoz létre egy
`SERVICE_BACKEND_MANIFEST_TEMPLATE` bejegyzésből — ez **még nincs** bekötve egyetlen
éles kérésútvonalba sem (sem a `generateProviderPluginManifestFromRegistry()`, sem a
`/v1/providers/[provider]/models` útvonalba); ez egy későbbi feladat marad, miután a
szerződés egy második háttérrendszeren is bizonyított.

A következő PR-ekre halasztva, a #7333 probléma alatt nyomon követve: a `cliproxyapi`
migrálása ugyanezen regiszteren keresztül, a `mux`/`bifrost` általánosítása a
`ServiceBackendPluginId` unióba, a végrehajtó-útválasztás speciális eseteinek
(`open-sse/executors/index.ts`, `open-sse/handlers/chatCore/executorProxy.ts`) beépítése
a beépülőmodul-szerződésbe, valamint a `createServiceBackendManifestEntry()` bekötése
egy éles jegyzék-/modellkódútvonalba.

### 3. lépés — Migráció és adatbázis-seed hozzáadása

Egy `src/lib/db/migrations/` alatti migrációval gondoskodjon arról, hogy a
szolgáltatáshoz tartozzon egy sor a `version_manager` táblában. A sornak a következőt
kell tartalmaznia:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### 4. lépés — A 7 API-végpont létrehozása

A `src/app/api/services/{name}/` alatt:

```
_lib.ts            getOrInitSupervisor() segédfüggvény
install/route.ts   POST — az installer.install() függvényt hívja
start/route.ts     POST — a supervisor.start() függvényt hívja
stop/route.ts      POST — a supervisor.stop() függvényt hívja
restart/route.ts   POST — a supervisor.restart() függvényt hívja
update/route.ts    POST — az installer.update() függvényt hívja
status/route.ts    GET  — egyesíti az élő és az adatbázisbeli állapotot
auto-start/route.ts POST — átkapcsolja az auto_start jelzőt
```

A megosztott `GET /api/services/[name]/logs` útvonal már be van kötve — ott nincs szükség módosításra.

Minden hibaválaszt a `createErrorResponse()` / `buildErrorBody()` függvényeken keresztül delegálj.

### 5. lépés — Hozzáadás a LOCAL_ONLY_API_PREFIXES listához

A `src/server/authz/routeGuard.ts` fájlban ellenőrizd, hogy a `/api/services/` már szerepel-e.
Ha új előtagot vezetsz be (például `/api/tools/`), add hozzá a
`LOCAL_ONLY_API_PREFIXES` listához, és ha folyamatokat indít, akkor a
`SPAWN_CAPABLE_PREFIXES` listához is.
Adj hozzá egy tesztet a `tests/unit/authz/routeGuard.test.ts` fájlban.

### 6. lépés — A felhasználói felület lapjának hozzáadása

Hozd létre a `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx` fájlt.
Használd újra a megosztott komponenseket:

- `ServiceStatusCard` — élő állapot + állapotjelző jelvény
- `ServiceLifecycleButtons` — Indítás / Leállítás / Újraindítás / Frissítés
- `ServiceLogsPanel` — SSE-naplókövetés (a `/api/services/{name}/logs` végponthoz kapcsolódik)
- `ApiKeyCard` — kulcs megjelenítése + cseréje (ha `needsApiKey: true`)

Regisztráld a lapot a `ServicesPageShell.tsx` fájlban.

### 7. lépés — A szolgáltatói bejegyzés hozzáadása (ha a szolgáltatás útválasztási cél)

Ha a beágyazott szolgáltatás OpenAI-kompatibilis `/v1/chat/completions` végpontot tesz elérhetővé:

1. Adj hozzá egy szolgáltatói bejegyzést a `src/shared/constants/providers.ts` fájlban `isEmbeddedService: true` értékkel.
2. Hozd létre a `BaseExecutor` osztályt kiterjesztő `open-sse/executors/{name}.ts` fájlt. Minden kérésnél keresd ki újra a portot és az API-kulcsot (soha ne gyorsítótárazd ezeket a konstruktorban). Adj vissza `503 service_not_running` választ, ha a felügyelő állapota nem `"running"`.
3. Regisztráld a modelleket az `open-sse/config/providerRegistry.ts` fájlban a szolgáltatás előtagjával (például `myservice/sub/model`). A `modelSync.ts` naprakészen fogja tartani őket.

### 8. lépés — Dokumentálás és tesztelés

1. Frissítsd a `docs/frameworks/EMBEDDED-SERVICES.md` fájlt (ezt a fájlt) — add hozzá a szolgáltatást az 1. § táblázatához, az új végpontokat pedig a 4. §-hoz.
2. Adj hozzá egységteszteket a `tests/unit/services/` könyvtárban (életciklus, telepítő, API-alak).
3. Adj hozzá integrációs tesztet a `tests/integration/services/` könyvtárban (a `RUN_SERVICES_INT=1` mögött).
4. Frissítsd a `docs/openapi.yaml` fájlt az új végpontokkal.

---

## 7. Hibaelhárítás

### A szolgáltatás nem indul el

**Tünetek:** Az indítási gomb 503-as hibát ad vissza, az állapot pedig `"error"` vagy `"starting"` marad.

**Ellenőrzőlista:**

1. Ellenőrizze a `GET /api/services/{name}/logs` végpontot (vagy az irányítópult Naplók paneljét). Keressen olyan sorokat, mint az `Error: ENOENT`, `address already in use` vagy `Cannot find module`.
2. Ellenőrizze, hogy az `npm` szerepel-e a PATH-ban: futtassa a `which npm` parancsot ugyanabból a felhasználói fiókból, amely az OmniRoute-ot futtatja.
3. Ellenőrizze, hogy a szolgáltatás telepítve van-e: keresse meg az `installedVersion` mezőt a `GET /api/services/{name}/status` válaszában. Ha az értéke `null`, először futtassa a telepítést.
4. Ellenőrizze, hogy létezik-e és nem üres-e a `DATA_DIR/services/{name}/node_modules/` könyvtár.
5. Ellenőrizze az állapotválasz `lastError` mezőjében a megtisztított kilépési okot.

---

### A hidegindítás lassú (több mint 10 másodperc a `running` állapot eléréséig)

**Tünetek:** Az állapot hosszú ideig `"starting"` marad, mielőtt `"running"` vagy `"error"` állapotba váltana.

**Magyarázat:** A 9Router hidegindítása nagy függőségi fák importálását foglalja magában (DNS-, alagút- és MITM-modulok). Az alapértelmezett állapot-ellenőrzési időköz 2 másodperc, és a felügyelő 3 kísérlet után időtúllépést állapít meg (de folytatja a lekérdezést).

**Javítás:** A `healthIntervalMs` és a `waitForHealthy` időtúllépése (`healthIntervalMs * 3`) a `bootstrap.ts` fájlban konfigurálható. Hosszabb indulási idejű szolgáltatások esetén növelje a `healthIntervalMs` értékét 5000-re, a `stopTimeoutMs` értékét pedig 30 000-re.

---

### Portütközés (`EADDRINUSE`)

**Tünetek:** A naplókban az `address already in use :::20130` üzenet jelenik meg.

**Okok:**

- Egy másik folyamat már használja a 20130-as portot.
- Egy korábbi 9Router-folyamat nem állt le teljesen (zombi PID).

**Javítás:**

1. Módosítsa az alapértelmezett portot a `.env` fájlban található `NINEROUTER_PORT` környezeti változóval.
2. Keresse meg és állítsa le az ütközést okozó folyamatot: `lsof -ti :20130 | xargs kill -9`.
3. A port szolgáltatásonként konfigurálható a `bootstrap.ts` fájl `port` mezőjével.

**Megjegyzés:** A 9Router alapértelmezés szerint kifejezetten a 20130-as portot használja, hogy elkerülje az ütközést az OmniRoute alapértelmezett 20128-as portjával.

---

### Hozzáférés megtagadva (EACCES) a telepítés során

**Tünetek:** A telepítés 500-as hibát ad vissza, a naplókban pedig `EACCES` vagy `permission denied` jelenik meg.

**Okok:**

- A `DATA_DIR` vagy annak szülőkönyvtára nem írható az OmniRoute-folyamat számára.
- Root nélküli Docker-környezetben fut, és nincs írási hozzáférése a csatlakoztatott kötethez.

**Javítás:**

1. Ellenőrizze a `DATA_DIR` könyvtárat (alapértelmezett: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Győződjön meg arról, hogy a könyvtár az OmniRoute-folyamat felhasználójának tulajdonában van: `chown -R $USER ~/.omniroute/`
3. Docker használata esetén győződjön meg arról, hogy a kötetcsatolás megfelelő jogosultságokkal rendelkezik a konténer felhasználója számára.

---

### A frissítés sikertelen (`npm install` időtúllépés vagy hálózati hiba)

**Tünetek:** A frissítés 500-as hibát ad vissza `InstallError` értékkel, a naplókban pedig hálózati időtúllépés jelenik meg.

**Ellenőrzőlista:**

1. Ellenőrizze, hogy az npm-nyilvántartás elérhető-e: `npm ping`.
2. Ellenőrizze a vállalati proxyt: `npm config get proxy`, `npm config get https-proxy`.
3. Próbálja meg manuálisan telepíteni: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Fizikailag leválasztott hálózat esetén töltse le előre a tarball-fájlt, majd használja az `npm install /path/to/tarball.tgz` parancsot.

---

### A szolgáltatás közvetlenül az indítás után `"error"` állapotot mutat (gyors összeomlás)

**Tünetek:** Az állapot 5 másodpercen belül `"starting"` értékről `"error"` értékre vált. A `lastError` mezőben a `"Fast crash (exited with code 1)"` üzenet jelenik meg.

**Ellenőrzőlista:**

1. Olvassa el a napló teljes végét: `GET /api/services/{name}/logs?tail=500`.
2. Gyakori ok: hiányoznak a szolgáltatás által elvárt környezeti változók.
3. A 9Router esetén ellenőrizze, hogy a `NINEROUTER_DISABLE_MITM=true` és a `NINEROUTER_DISABLE_TUNNEL=true` szerepel-e az indításkor átadott környezetben (lásd: `installers/ninerouter.ts`, `resolveSpawnArgs`).

---

## 8. GYIK

**K: Elérhetővé tehetem a beágyazott szolgáltatások végpontjait nem loopback kliensek számára?**

Nem. A LOCAL_ONLY szint szándékosan ilyen kialakítású (17. szigorú szabály). Azok az útvonalak, amelyek meghívhatják az `npm install` parancsot, vagy `node` folyamatokat indíthatnak, nem lehetnek elérhetők nem loopback forgalomból, mert egy alagúton (Cloudflare, Ngrok, Tailscale) keresztül kiszivárgott JWT egyébként tetszőleges folyamatok indítását tenné lehetővé. Az `/api/services/` esetében nincs lehetőség ezen korlátozás felülbírálására — az `/api/mcp/` útvonallal ellentétben ez nem szerepel a manage hatókört megkerülő listán. Lásd: `docs/security/ROUTE_GUARD_TIERS.md`.

---

**K: Elérhető lesz a 9Router és a CLIProxyAPI éles/felhőalapú telepítésekben?**

Igen. Mindkét szolgáltatás ugyanazt a helyi működést előtérbe helyező modellt követi, mint maga az OmniRoute. Ugyanazon a gépen futnak, és loopback kapcsolaton keresztül kommunikálnak. Az „éles” itt azt a VPS-t vagy helyi kiszolgálót jelenti, amelyen az OmniRoute telepítve van, nem pedig egy távoli felhőszolgáltatót.

---

**K: Hogyan végezhetem el a felügyelő hibakeresését?**

1. Kövesse élőben az SSE-naplófolyamot: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Ellenőrizze az OmniRoute pino-kimenetében található strukturált naplókat a `service:supervisor` névtérre szűrve.
3. Vizsgálja meg az adatbázis sorát: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. A `GET /api/services/9router/status` használatával egyetlen hívással megtekintheti az aktuális élő állapotot, a PID-et, az állapotellenőrzés eredményét és a `lastError` értékét.

---

**K: A felügyelőnél a `health: "degraded"` vagy a `health: "unknown"` látható, miközben az állapot `"running"`. Ez probléma?**

A `"degraded"` azt jelenti, hogy az állapotellenőrző próba nem 200-as választ adott vissza. Az `"unknown"` azt jelenti, hogy még egyetlen próba sem fejeződött be (versenyhelyzet az első lekérdezéssel). Indítás közben mindkettő átmeneti állapot. Ha a `"running"` állapot elérését követően az állapotellenőrzés `healthIntervalMs * 3` ezredmásodpercnél tovább marad `"degraded"`, akkor a beágyazott szolgáltatás fut, de a HTTP API-ja nem válaszol. Ellenőrizze, hogy az állapotválaszban szereplő port helyes-e, és hogy a szolgáltatás valóban figyel-e azon a porton.

---

**K: Módosíthatom a 9Router API-kulcsát teljes újraindítás nélkül?**

Nem. Az API-kulcs a folyamat indításakor, környezeti változón keresztül kerül átadásra a 9Router számára. Egy futó folyamat környezeti változói nem módosíthatók. A `POST .../rotate-key` automatikusan leállítja és újraindítja a szolgáltatást az új kulcs alkalmazásához. A kulcs cseréje a szolgáltatás `stopTimeoutMs` időkorlátján (alapértelmezés szerint 15 s) és az indítási idején belül lép érvénybe.

---

**K: Mekkora a körkörös puffer korlátja, és mi történik, amikor megtelik?**

Minden szolgáltatás külön 5 MB-os körkörös pufferrel rendelkezik. Amikor a puffer megtelik, a legrégebbi naplósorok törlődnek, hogy helyet biztosítsanak az újaknak. Az SSE `snapshot` esemény a `tail` korláton belüli legutóbbi sorokat adja vissza. A naplók nem kerülnek lemezre, kivéve, ha az adatbázis sorában be van állítva a `logsBufferPath`.

---

## Lásd még

- `docs/security/ROUTE_GUARD_TIERS.md` — a LOCAL_ONLY szint részletei
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2, a beágyazott szolgáltatások moduljainak leképezése
- `docs/architecture/ARCHITECTURE.md` — rendszerszintű kontextus
- `docs/openapi.yaml` — géppel olvasható végpontdefiníciók
- `CLAUDE.md` §„Új beágyazott szolgáltatás hozzáadása” — gyors ellenőrzőlista
