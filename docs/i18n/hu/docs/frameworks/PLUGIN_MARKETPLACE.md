# Plugin Marketplace (Magyar)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇹 [am](../../../am/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇿 [az](../../../az/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇰 [da](../../../da/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇪 [de](../../../de/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇷 [el](../../../el/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇸 [es](../../../es/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇪 [et](../../../et/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇱 [he](../../../he/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇩 [id](../../../id/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇹 [it](../../../it/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇭 [km](../../../km/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇲 [my](../../../my/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇴 [no](../../../no/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [or](../../../or/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇰 [si](../../../si/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [te](../../../te/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇭 [th](../../../th/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/PLUGIN_MARKETPLACE.md)

---

> **Hiteles forrás:** `src/lib/plugins/` (`marketplace.ts`, `manager.ts`, `manifest.ts`,
> `scanner.ts`, `loader.ts`), `src/app/api/plugins/`, valamint
> `src/app/(dashboard)/dashboard/plugins/`
> **Utolsó frissítés:** 2026-06-28 — v3.8.40

Az OmniRoute egy WordPress-stílusú bővítményrendszerrel érkezik. A bővítmények önálló
könyvtárak — mindegyik egy `plugin.json` manifeszttel és egy belépési fájllal —, amelyek
bekapcsolódnak a kérésfeldolgozási folyamatba (`onRequest` / `onResponse` / `onError`), valamint
az életciklus-eseményekbe (`onInstall` / `onActivate` / `onDeactivate` / `onUninstall`).

A **Bővítménypiactér** erre a rendszerre épülő felderítési réteg. Egy böngészhető
katalógust biztosít a telepíthető bővítményekhez. Alapértelmezés szerint a katalógus egy
kisméretű, beépített kezdeti regisztrációs adatbázis; az üzemeltető beállíthat egy egyéni távoli
regisztrációs URL-t, amely esetben a lekérést egy DNS-feloldást végző SSRF-védelem erősíti meg
(lásd: [Biztonság](#security)).

Minden bővítményútvonal **csak visszacsatolási címről érhető el** (1. szint — `LOCAL_ONLY`): a bővítmények
gyermekfolyamatokban töltenek be és futtatnak kódot, ezért az útvonalak a hitelesítéstől függetlenül
nem érhetők el nem visszacsatolási eredetről. Lásd:
[`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md).

## A rendszer felépítése

```
Vezérlőpult (/dashboard/plugins)
  ├─ „Telepítve” lap  → GET /api/plugins            (listPlugins)
  │                     POST /api/plugins/scan      (pluginManager.scan)
  │                     POST /api/plugins/{name}/activate|deactivate
  │                     DELETE /api/plugins/{name}   (eltávolítás)
  └─ „Piactér” lap → GET /api/plugins/marketplace
                          → listMarketplacePlugins()
                            ├─ nincs egyéni URL → beépített SEED_REGISTRY
                            └─ egyéni URL → isSafeMarketplaceUrl() SSRF-védelem
                                          → safeOutboundFetch(guard:"public-only")
```

- **Regisztrációs réteg** — `src/lib/plugins/marketplace.ts`: listázza és keresi a
  katalógust, hiba esetén pedig a kezdeti regisztrációs adatbázist használja tartalékként.
- **Életciklusréteg** — `src/lib/plugins/manager.ts` (`pluginManager` singleton):
  telepítés, frissítés, aktiválás, deaktiválás, eltávolítás, vizsgálat és indításkori betöltés.
- **Manifeszt réteg** — `src/lib/plugins/manifest.ts`: Zod-séma és alapértelmezések a
  `plugin.json` fájlhoz.
- **Vizsgáló** — `src/lib/plugins/scanner.ts`: felderíti a lemezen, a
  bővítménykönyvtárban található bővítményeket.
- **Betöltő** — `src/lib/plugins/loader.ts`: minden bővítményt különálló
  gyermekfolyamatban indít el, és IPC-n keresztül közvetíti a hookhívásokat.

## Piactéri katalógus

A `listMarketplacePlugins()` (`src/lib/plugins/marketplace.ts`) `MarketplaceEntry`
objektumok listáját adja vissza:

| Mező          | Típus    | Megjegyzés                                   |
| ------------- | -------- | -------------------------------------------- |
| `name`        | string   | kebab-case formátumú bővítménynév            |
| `version`     | string   | semver                                       |
| `description` | string   | Rövid összefoglaló                           |
| `author`      | string   | Szerző / szervezet                           |
| `license`     | string   | SPDX-stílusú licencazonosító                 |
| `downloadUrl` | string   | A forrás letöltési URL-je (üres is lehet)    |
| `repository`  | string?  | Opcionális adattár-URL                       |
| `tags`        | string[] | Keresési/szűrési címkék                      |
| `downloads`   | number   | Letöltések száma                             |
| `rating`      | number   | 0–5                                          |
| `verified`    | boolean  | A bejegyzés ellenőrzöttként van-e megjelölve |
| `lastUpdated` | string   | ISO-szerű dátumkarakterlánc                  |

Ha nincs egyéni regisztrációs URL beállítva, a katalógus a beépített
`SEED_REGISTRY` értéket használja (jelenleg `request-logger`, `rate-limiter`, `cost-tracker` és
`theme-manager`). A kezdeti regisztrációs adatbázis mindig elérhető — ha egy beállított távoli
regisztrációs adatbázis nem érhető el, nem `200` állapotkódot ad vissza, vagy nem felismerhető
törzset küld vissza, a `listMarketplacePlugins()` figyelmeztetést naplóz, és tartalékként a
kezdeti listát használja.

> Megjegyzés: a piactéri **katalógus** (böngészés/keresés) teljes egészében össze van kötve, de
> a katalógusból történő egykattintásos piactéri **telepítés** még nincs megvalósítva — a
> vezérlőpulton egy piactéri bejegyzés „Telepítés” gombja jelenleg egy
> „hamarosan elérhető” értesítést jelenít meg. A telepítés jelenleg a helyi elérési utas telepítési
> folyamaton (`POST /api/plugins`) és a lemezen történő felderítésen (`POST /api/plugins/scan`) keresztül történik.

## REST API

Minden végpont kezelői hitelesítést (`requireManagementAuth`) igényel, **és**
kizárólag loopback-interfészről érhető el — az `/api/plugins` és az `/api/plugins/`
szerepel a `LOCAL_ONLY_API_PREFIXES` listában
(`src/server/authz/routeGuard.ts`).

| Végpont                          | Metódus | Leírás                                                                            |
| -------------------------------- | ------- | --------------------------------------------------------------------------------- |
| `/api/plugins`                   | GET     | Telepített bővítmények listázása (opcionális `?status=` szűrővel)                 |
| `/api/plugins`                   | POST    | Bővítmény telepítése abszolút helyi elérési útról                                 |
| `/api/plugins/scan`              | POST    | A bővítménykönyvtár átvizsgálása és az új bővítmények regisztrálása               |
| `/api/plugins/marketplace`       | GET     | A piactér katalógusbejegyzéseinek listázása                                       |
| `/api/plugins/[name]`            | GET     | Egy telepített bővítmény részleteinek lekérése                                    |
| `/api/plugins/[name]`            | DELETE  | Egy bővítmény eltávolítása                                                        |
| `/api/plugins/[name]/activate`   | POST    | Aktiválás (betöltés + hookok regisztrálása)                                       |
| `/api/plugins/[name]/deactivate` | POST    | Deaktiválás (az `onDeactivate` meghívása, hookok regisztrációjának megszüntetése) |
| `/api/plugins/[name]/config`     | GET     | A bővítmény konfigurációjának és konfigurációs sémájának lekérése                 |
| `/api/plugins/[name]/config`     | PUT     | A bővítmény konfigurációjának frissítése (a séma alapján ellenőrizve)             |

A `GET /api/plugins` végpont `status` szűrője az
`installed` / `active` / `inactive` / `error` értékek egyikét fogadja el. Érvénytelen érték esetén `400` választ ad vissza.

### Telepített bővítmények listázása

```bash
curl http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..."
```

### Telepítés helyi elérési útról

```bash
curl -X POST http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "path": "/absolute/path/to/my-plugin" }'
```

A `path` értékének **abszolútnak** kell lennie, és nem tartalmazhat `..`
útvonal-bejárási szegmenseket vagy nullbájtokat (ezt a Zod kényszeríti ki). A
forráskönyvtárnak érvényes `plugin.json` fájlt kell tartalmaznia (vagy egy ilyet
tartalmazó könyvtár szülőjének kell lennie). Siker esetén a válasz `201`, és
tartalmazza a telepített bővítmény rekordját.

### Böngészés a piactéren

```bash
curl http://localhost:20128/api/plugins/marketplace \
  -H "Cookie: auth_token=..."
```

### Bővítmény-konfiguráció frissítése

```bash
curl -X PUT http://localhost:20128/api/plugins/my-plugin/config \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "config": { "level": "debug", "maxItems": 100 } }'
```

A `PUT .../config` minden megadott értéket ellenőriz a bővítmény
`configSchema` sémája alapján (amelyet a manifest deklarál): a `number` mezők
figyelembe veszik a `min`/`max` korlátokat, a `select` mezőknek pedig meg kell
felelniük a deklarált `enum` értékek egyikének. A sémában nem szereplő kulcsok
változtatás nélkül átengedhetők.

## Konfiguráció

### Bővítménykönyvtár

A bővítmények az OmniRoute adatkönyvtárában találhatók:

```
~/.omniroute/plugins/<plugin-name>/
  ├─ plugin.json
  └─ index.js          # (vagy amire a manifest.main mutat)
```

A `getDefaultPluginDir()` (`src/lib/plugins/scanner.ts`) három lépésben oldja fel
ezt a könyvtárat:

1. **`OMNIROUTE_PLUGINS_DIR`**, ha be van állítva — változtatás nélkül használja, függetlenül a `HOME` értékétől. Ez a
   Docker/K8s explicit beállítási lehetősége, ahol a bővítményfa bind mounttal olyan elérési útra
   van csatolva, amelynek általában semmi köze a konténer saját könyvtárához (#11827).
2. `<home>/.omniroute/plugins`, ahol a `<home>` értéke a `HOME` / `USERPROFILE`
   környezeti változókból származik.
3. `/tmp/.omniroute/plugins`, ha a folyamat egyáltalán nem exportál saját könyvtárat.

A feloldott könyvtárat a rendszer indításkor egyszer naplózza `scanner.dir_resolved`
néven, megjelölve a kiválasztott bemenetet (`OMNIROUTE_PLUGINS_DIR`, `home` vagy
`no-home-fallback`) — így egy olyan image, amely észrevétlenül a 3. lépést használja,
ezt is jelzi, ahelyett hogy csak egy üres bővítménylistáról számolna be.
A `POST /api/plugins/scan` felderíti az itt található, érvényes `plugin.json`
fájlt tartalmazó alkönyvtárakat, és regisztrálja őket; ugyanez a könyvtár az a
gyökérkönyvtár, amelybe a `pluginManager.install()` bemásolja a bővítményeket,
így a felülbírálás a felderítést és a telepítést együtt helyezi át.

> **Az `OMNIROUTE_PLUGINS_DIR` nem azonos az `OMNIROUTE_PLUGIN_PATH` változóval.** Az utóbbit csak
> a CLI parancsbővítmény-betöltője (`bin/cli/plugins.mjs`) olvassa az `omniroute-cmd-*` npm
> csomagok megtalálásához, amelyek `omniroute` alparancsokat adnak hozzá — nincs hatással az itt
> ismertetett futásidejű szkennerre. Erről az oldalról lásd a [PLUGINS.md](./PLUGINS.md) fájlt.

### Egyéni piactéri nyilvántartás URL-je

A piactér katalógusának forrása a `pluginMarketplaceUrl` beállításból származik
(a `src/lib/plugins/marketplace.ts` a `settings.pluginMarketplaceUrl` értéket olvassa).
Ha egy `http(s)` URL-re van beállítva, a `listMarketplacePlugins()` lekéri ezt az
URL-t, és vagy egy felső szintű, bejegyzéseket tartalmazó JSON-tömböt, vagy egy
`plugins` tömböt tartalmazó objektumot fogad el; a karakterlánc típusú `name`
mezővel nem rendelkező bejegyzéseket kiszűri. Ha nincs beállítva (vagy ha a
lekérés meghiúsul az SSRF-védelem miatt / hibás választ ad), a beépített kezdeti
nyilvántartást használja.

Az irányítópult „Piactér” lapján található egy mező ehhez az URL-hez (az értéket
a `GET /api/settings` végpontról olvassa vissza).

> Megvalósítási megjegyzés: az irányítópult „Mentés” művelete a
> `pluginMarketplaceUrl` értéket küldi el a `PATCH /api/settings` végpontnak. A
> dokumentum írásakor ez a kulcs nincs deklarálva az `updateSettingsSchema`
> sémában (`src/shared/validation/settingsSchemas.ts`), ezért ellenőrizze az érték
> megőrzését az Ön kiadásában, mielőtt támaszkodna rá — az **olvasási** útvonal
> (`getSettings()` → `listMarketplacePlugins()`) figyelembe veszi a kulcsot, amint
> az szerepel a beállítástárban.

## Biztonság

### Útvonal-besorolás — csak visszacsatolási interfész

A bővítmények elindított gyermekfolyamatokban hajtanak végre kódot, ezért a teljes `/api/plugins`
felület `LOCAL_ONLY` (1. szint) besorolású. A visszacsatolási interfész megkövetelése
feltétel nélkül, **minden** hitelesítési ellenőrzés előtt történik, így még egy
alagúton keresztül a gépre eljutó, kiszivárgott felügyeleti token sem képes
bővítményt telepíteni, aktiválni vagy eltávolítani.
Lásd: [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) és
a 15. / 17. szigorú szabály.

### A piactéri nyilvántartás SSRF-védelme

Az egyéni nyilvántartási URL támadó által befolyásolható konfiguráció, ezért
a lekérése előtt a `listMarketplacePlugins()` két védelmi rétegen futtatja át:

1. **`isSafeMarketplaceUrl(url)`** (`src/lib/plugins/marketplace.ts`):
   - Elutasít mindent, ami nem `http:` / `https:`.
   - Elutasítja a literális privát/visszacsatolási/link-local/ULA-gazdagépeket
     (IPv4 **és** IPv6, beleértve az IPv4-re leképezett címeket is) a kanonikus
     `isPrivateHost` (`src/shared/network/outboundUrlGuard.ts`) használatával.
   - **Mind az** `A`, **mind az** `AAAA` rekordokat feloldja, és elutasítja az
     URL-t, ha **bármelyik** feloldott cím privát — ezzel megszüntetve a
     nyilvános gazdagépnév → privát IP-cím megkerülési lehetőséget.
   - **Biztonságosan zár**: a DNS-feloldás sikertelensége esetén elutasítja az
     URL-t.
2. **`safeOutboundFetch(url, { guard: "public-only", timeoutMs: 5000 })`**
   (`src/shared/network/safeOutboundFetch.ts`): a lekéréskor ismét alkalmazza
   a csak nyilvános URL-eket engedélyező védelmet, és **blokkolja az
   átirányításokat** (nincs nyilvános → privát `30x` átfordulás).

Ha egy URL bármelyik rétegen megbukik, az nem szakítja meg a kérést — a piactér
észrevétlenül visszaáll a beépített kezdeti nyilvántartásra, és figyelmeztetést
naplóz.

> Ezt a védelmet a #3774 PR-ben erősítették meg kifejezetten azért, hogy az A +
> AAAA rekordokat egyaránt feloldja, és a kizárólag IPv4-címeket vizsgáló
> ellenőrzés helyett a kanonikus `isPrivateHost` függvényt használja.

### A bővítmények végrehajtásának elkülönítése

- **Folyamatelkülönítés** — a `loadPlugin()` (`src/lib/plugins/loader.ts`)
  minden bővítményt külön Node.js-gyermekfolyamatban indít el, és IPC-n keresztül
  kommunikál vele. A hook-hívások időkorláttal rendelkeznek, `SIGTERM` →
  `SIGKILL` eszkalációval.
- **Környezeti változók engedélyezési listája** — a gyermekfolyamat csak az
  engedélyezési listán szereplő környezeti változókat kapja meg; a bővebb
  készletet csak akkor kapja meg, ha a jegyzék kéri az `env` engedélyt.
- **Útvonal-korlátozás** — a telepítési/frissítési/eltávolítási műveletek minden
  másolás vagy rekurzív törlés előtt ellenőrzik, hogy a bővítmény könyvtára és
  a `manifest.main` feloldott útvonala a felügyelt bővítménygyökéren **belül**
  található-e (védelem a manipulált adatbázis-útvonalak és a `manifest.main`
  értékében lévő `../` útvonalbejárás ellen). Az aktiválás a szimbolikus
  hivatkozásokat a `realpath` segítségével oldja fel, és megtagadja az olyan
  belépési pont betöltését, amely kilép a bővítmény könyvtárából.
- **Opcionális integritásrögzítés** — a jegyzék megadhat egy `integrity`
  (`sha256-<base64>`, SRI-formátum) mezőt. Ha jelen van, a betöltő betöltéskor
  ellenőrzi a belépési fájl kivonatát, és eltérés esetén megtagadja az
  aktiválást. Ez önkéntesen használható manipulációészlelés, **nem** biztonsági
  határ — a valódi határokat a csak visszacsatolási interfészen elérhető
  útválasztás és az engedélymodell jelentik.

## Jegyzék (`plugin.json`)

A `PluginManifestSchema` (`src/lib/plugins/manifest.ts`) ellenőrzi:

| Mező               | Típus     | Megjegyzések                                                          |
| ------------------ | --------- | --------------------------------------------------------------------- |
| `name`             | string    | Kötelező; kebab-case (`^[a-z0-9-]+$`), 1–100 karakter                 |
| `version`          | string    | Kötelező; semver (`MAJOR.MINOR.PATCH`)                                |
| `description`      | string?   | ≤ 500 karakter                                                        |
| `author`           | string?   | ≤ 200 karakter                                                        |
| `license`          | string?   | Alapértelmezett értéke `MIT`                                          |
| `main`             | string?   | Belépési fájl; alapértelmezett értéke `index.js`                      |
| `source`           | enum?     | `local` \| `marketplace` (alapértelmezett: `local`)                   |
| `tags`             | string[]? | Keresési címkék                                                       |
| `requires`         | object?   | `{ omniroute?, permissions[] }`                                       |
| `hooks`            | object?   | A bővítmény által megvalósított hookokat jelző logikai értékek        |
| `skills`           | object[]? | Opcionális készségdefiníciók                                          |
| `enabledByDefault` | boolean?  | Automatikus aktiválás telepítéskor                                    |
| `configSchema`     | object?   | Konfigurációs mezők leképezése (`string`/`number`/`boolean`/`select`) |
| `integrity`        | string?   | Opcionális `sha256-<base64>` belépésifájl-rögzítés                    |

Az engedélyek a következő felsorolásból származnak:
`network` / `file-read` / `file-write` / `env` / `exec`.

## Életciklus folyamata

```
telepítés (POST /api/plugins, elérési út)
  → manifeszt ellenőrzése/érvényesítése → másolás az előkészítési területre → annak ellenőrzése, hogy a main a könyvtáron belül van
  → atomi átnevezés a ~/.omniroute/plugins/<name> helyre → adatbázissor beszúrása
  → onInstall meghívása → ha enabledByDefault: aktiválás

aktiválás (POST /api/plugins/{name}/activate)
  → realpath-alapú elérésiút-ellenőrzés → loadPlugin() (gyermekfolyamat indítása)
  → deklarált hookok regisztrálása → status = "active" → onActivate meghívása

deaktiválás (POST /api/plugins/{name}/deactivate)
  → onDeactivate meghívása (a regisztráció megszüntetése ELŐTT) → hookok regisztrációjának megszüntetése
  → gyermekfolyamat leállítása → status = "inactive"

eltávolítás (DELETE /api/plugins/{name})
  → deaktiválás, ha aktív → onUninstall meghívása
  → a bővítménykönyvtár elérésiút-ellenőrzött rekurzív törlése → adatbázissor törlése
```

Az `install` újbóli futtatása olyan könyvtáron, amelynek manifesztverziója **szigorúan
újabb** a telepített verziónál, automatikus frissítést végez (tiszta újratelepítés; a konfiguráció
visszaáll az alapértelmezett értékekre). Az azonos vagy régebbi verzió elutasításra kerül.

## Adatbázis

A `plugins` tábla (`076_create_plugins.sql` migráció):

| Oszlop          | Típus   | Megjegyzések                                             |
| --------------- | ------- | -------------------------------------------------------- |
| `id`            | TEXT PK | UUID                                                     |
| `name`          | TEXT    | Egyedi                                                   |
| `version`       | TEXT    | semver; alapértelmezett: `1.0.0`                         |
| `description`   | TEXT    | Opcionális                                               |
| `author`        | TEXT    | Opcionális                                               |
| `license`       | TEXT    | Alapértelmezett: `MIT`                                   |
| `main`          | TEXT    | Belépési fájl; alapértelmezett: `index.js`               |
| `source`        | TEXT    | Alapértelmezett: `local`                                 |
| `tags`          | TEXT    | JSON-tömb; alapértelmezett: `[]`                         |
| `status`        | TEXT    | `installed` \| `active` \| `inactive` \| `error`         |
| `enabled`       | INT     | 0/1; alapértelmezett: 0                                  |
| `manifest`      | TEXT    | Teljes manifeszt JSON                                    |
| `config`        | TEXT    | JSON; alapértelmezett: `{}`                              |
| `config_schema` | TEXT    | JSON; alapértelmezett: `{}`                              |
| `hooks`         | TEXT    | A deklarált hooknevek JSON-tömbje; alapértelmezett: `[]` |
| `permissions`   | TEXT    | JSON-tömb; alapértelmezett: `[]`                         |
| `plugin_dir`    | TEXT    | Abszolút telepítési könyvtár                             |
| `error_message` | TEXT    | Akkor kerül beállításra, ha `status = "error"`           |
| `installed_at`  | TEXT    | `datetime('now')`                                        |
| `updated_at`    | TEXT    | `datetime('now')`                                        |
| `activated_at`  | TEXT    | Aktiváláskor kerül beállításra                           |

A bővítmények metrikáit és analitikai adatait további táblák követik nyomon
(`090_plugin_metrics.sql`, `091_plugin_analytics.sql`).

## Irányítópult

A `/dashboard/plugins` címen található irányítópultoldal
(`src/app/(dashboard)/dashboard/plugins/page.tsx`) két lapot biztosít:

- **Telepítve** — felsorolja a telepített bővítményeket a deklarált hookjaikkal, egy
  aktiválási/deaktiválási kapcsolóval, egy eltávolítási gombbal és egy „Bővítmények keresése”
  művelettel (`POST /api/plugins/scan`).
- **Piactér** — megjeleníti a `GET /api/plugins/marketplace` által visszaadott katalógust egy,
  az egyéni regisztrációs adatbázis URL-jének beállítására szolgáló mezővel.

A bővítményenkénti konfigurációs oldal a `/dashboard/plugins/[name]/config`
címen található (`src/app/(dashboard)/dashboard/plugins/[name]/config/page.tsx`).

## Lásd még

- [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) —
  miért csak visszacsatolási címről érhető el az `/api/plugins` (1. szint)
- [`docs/frameworks/SKILLS.md`](./SKILLS.md) — a kapcsolódó képességkeretrendszer
  (`src/lib/skills/`); a bővítmények képességeket deklarálhatnak a jegyzékfájljukban
- [`docs/frameworks/WEBHOOKS.md`](./WEBHOOKS.md) — eseményvezérelt kimenő
  integrációk
- [`docs/security/ERROR_SANITIZATION.md`](../security/ERROR_SANITIZATION.md) —
  a `buildErrorBody()` minta, amelyet minden bővítmény-útvonal használ a hibaválaszokhoz
