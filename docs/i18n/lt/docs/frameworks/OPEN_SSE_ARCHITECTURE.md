# open-sse Architecture (Lietuvių)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Trumpai**: `open-sse/` yra pagrindinis srautinio perdavimo variklis, apdorojantis kiekvieną LLM užklausą sistemoje „OmniRoute“. Jį sudaro ~1 400 failų, kuriuose įgyvendintas užklausų apdorojimo konvejeris, vykdyklės, paslaugos, MCP serveris ir vertimo sluoksnis. Šiame vadove paaiškinama, kaip šios dalys susijungia į bendrą visumą.

**Šaltinis:** `open-sse/` (darbo srities paketas, ~1 440 ne testinių `.ts` failų)

---

## Kodėl naudojamas atskiras darbo srities paketas?

`open-sse/` yra **savarankiška darbo sritis** „OmniRoute“ monorepozitorijoje dėl kelių priežasčių:

1. **Pakartotinis panaudojimas** — `open-sse` publikuojamas npm kaip `@omniroute/open-sse`, todėl kiti projektai gali jį naudoti nepriklausomai
2. **Aiškios ribos** — srautinio perdavimo variklis atskirtas nuo „OmniRoute“ specifinio UI / DB sluoksnio
3. **Našumas** — variklis neturi Next.js priklausomybių, todėl CLI ir serverio neturinčiose aplinkose paleidimas iš neaktyvios būsenos yra spartesnis
4. **Versijų valdymas** — `open-sse` gali leisti versijas pagal savo tvarkaraštį

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Aukščiausio lygmens struktūra

```
open-sse/
├── index.ts              # Viešasis įėjimo taškas
├── types.d.ts            # Viešųjų tipų eksportai
├── package.json          # @omniroute/open-sse
├── config/               # Teikėjų konfigūracijos, konstantos, registrai
├── executors/            # Atskirų teikėjų HTTP vykdyklės (67 + base.ts/index.ts)
├── handlers/             # Užklausų apdorojimo moduliai (chatCore, responses ir kt.)
├── lib/                  # Vidinės pagalbinės priemonės
├── mcp-server/           # Model Context Protocol serveris
├── services/             # ~298 paslaugų moduliai
├── transformer/          # Responses API formato transformavimo priemonė
├── translator/           # Formatų vertimas (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Bendros pagalbinės priemonės (žurnalams, klaidoms, srautams ir kt.)
```

### Modulių skaičius

| Katalogas | Failai | Paskirtis |
| `executors/` | 167 | Atskirų teikėjų HTTP vykdyklės (suvienodintos naudojant DefaultExecutor gamyklą) |
| `handlers/` | 157 | Užklausų įėjimo taškai (chatCore, responses, embeddings) |
| `services/` | ~536 | Maršruto parinkimas, podėliavimas, dažnio ribojimas, atnaujinimas ir kt. |
| `translator/` | 56 | Formatų konvertavimas (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP įrankiai ir perdavimo mechanizmai |
| `utils/` | ~108 | Skersinės pagalbinės priemonės (žurnalams, klaidoms, srautams) |
| `config/` | ~339 | Teikėjų konfigūracijos, konstantos, registrai |

---

## Užklausų apdorojimo konvejeris

Kiekviena LLM užklausa apdorojama **5 etapų konvejeriu**:

```
                              ┌──────────────────┐
   HTTP užklausa               │  1. MARŠRUTAS    │   derinio nustatymas, modelio parinkimas
   (Next.js maršrutas)         └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  2. VERTIMAS      │   formato konvertavimas (OpenAI ↔ Claude ↔ Gemini)
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  3. VYKDYMAS      │   teikėjo vykdyklė, HTTP, kartojimas, grandinės pertraukiklis
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  4. SRAUTAS       │   SSE transformavimas, atgalinis slėgis
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  5. REGISTRAVIMAS │   naudojimo stebėjimas, iškvietimų žurnalas, klaidų klasifikavimas
                              └────────┬─────────┘
                                       │
                                       ▼
                              HTTP atsakas (SSE arba JSON)
```

### 1 etapas: maršruto parinkimas (services/combo.ts)

**Įėjimo taškas**: `handleComboChat()`, esantis `services/combo.ts`

Užklausa susiejama su konkrečiu `(provider, model, account, credentials)` rinkiniu:

- Derinys surandamas pagal ID (arba sukuriamas virtualus `auto/*` modelių derinys)
- Pritaikoma maršruto parinkimo strategija (prioritetinė, svertinė, ciklinė ir kt.)
- Atmetami sutrikę teikėjai (grandinės pertraukiklis)
- Parenkamas kitas tinkamas tikslas

Naudojant `auto/*` modelius, šiame etape taip pat:

- Paleidžiamas **16 veiksnių vertinimo** algoritmas (`services/autoCombo/`)
- Pagal veikimo būklę, kainą, delsą ir kitus veiksnius parenkama `provider+model` pora

### 2 etapas: vertimas (translator/)

Jei pradinis formatas (pvz., OpenAI) skiriasi nuo tikslinio formato (pvz., Claude), užklausa **išverčiama**:

- Sistemos raginimas → sistemos pranešimas
- Įrankių apibrėžtys → konkrečiam teikėjui skirtas įrankių formatas
- Samprotavimo / mąstymo parametrai → konkrečiam teikėjui skirti atitikmenys
- Pranešimų vaidmenų normalizavimas (`developer` → `system`, kai naudojamas ne OpenAI)

`translator/index.ts` pateikia:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### 3 etapas: vykdymas (executors/)

**Įėjimo taškas**: `getExecutor(providerId).execute(request, options)`

Visi teikėjai naudoja `DefaultExecutor` (`executors/default.ts`) per atsarginę `getExecutor()` gamyklą. Vykdyklė:

- Sukuria išorinės paslaugos URL (`buildUrl()`)
- Prideda konkrečiam teikėjui skirtas antraštes (`buildHeaders()`)
- Transformuoja užklausos turinį (`transformRequest()`)
- Siunčia HTTP užklausą, taikydama pakartotinius bandymus ir eksponentinį laukimo intervalo didinimą
- Prireikus atnaujina autentifikavimą (OAuth teikėjams)

Visos vykdyklės išplečia `BaseExecutor` (`executors/base.ts`, 1170 kodo eilučių), kuris suteikia:

- Bendrą pakartotinių bandymų logiką
- Integraciją su tarpiniu serveriu
- Integraciją su grandinės pertraukikliu
- Naudojimo registravimo kablius

### 4 etapas: srautas (utils/stream.ts)

Srautinių atsakų atveju vykdyklė grąžina **ReadableStream**. Apdorojimo modulis:

- Perduoda duomenis per SSE transformavimo srautą (`createSSETransformStreamWithLogger`)
- Periodiškai siunčia ryšio palaikymo signalus neaktyviems ryšiams aptikti
- Sklandžiai apdoroja kliento atsijungimą (`pipeWithDisconnect`)
- Nesrautiniams klientams transformuoja SSE → JSON

Nesrautinių atsakų atveju vykdyklė grąžina išanalizuotą JSON objektą, kuris perduodamas nepakeistas.

### 5 etapas: registravimas (services/usage.ts)

Po atsako (sėkmingo ar nesėkmingo) užregistruojami naudojimo duomenys:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` iš atsako
- `cost_usd`, apskaičiuota pagal kainodaros duomenis
- `latency_ms`, `status`, `error_class`, jei įvyko klaida
- Išsaugoma lentelėje `usage_history`

Iškvietimų žurnalų artefaktai (jei įjungta) įrašomi į `${DATA_DIR}/call_logs/`.

---

## Išsami pagrindinių failų apžvalga

### chatCore.ts (5977 eilutės)

**Pagrindinė užklausų apdorojimo funkcija**. Nepaisant dydžio, jos struktūra yra aiški:

```ts
// Pseudostruktūra faile chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Autentifikavimas ir CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Užklausos turinio tikrinimas
  const body = await parseRequestBody(request);

  // 3. Formato aptikimas ir konvertavimas
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Derinio maršruto parinkimas
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Tęsti su kitu tikslu
    }
  }

  // 5. Avarinis atsarginis variantas
  return await emergencyFallback(body);
}
```

Nors tai viena milžiniška funkcija, ji suskirstyta į **komentarais pažymėtas dalis**, atitinkančias 5 etapų konvejerį.

### combo.ts (4456 kodo eilutės)

**Maršruto parinkimo mechanizmas**, kuris deriniui nustato išrikiuotus tikslus.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Palaiko **19 maršruto parinkimo strategijų** (žr. `src/shared/constants/routingStrategies.ts`):

| Strategija          | Veikimas                                                                                         |
| ------------------- | ------------------------------------------------------------------------------------------------ |
| `priority`          | Sąrašas, surikiuotas pirmenybę teikiant pirmajam tikslui                                         |
| `weighted`          | Tikimybinis parinkimas pagal kiekvieno tikslo svorį                                              |
| `round-robin`       | Tikslai cikliškai parenkami eilės tvarka                                                         |
| `context-relay`     | Kontekstas perduodamas tarp tikslų                                                               |
| `fill-first`        | Prieš pereinant prie kito tikslo išnaudojama kvota                                               |
| `p2c`               | Dviejų pasirinkimų galia                                                                         |
| `random`            | Tolygus atsitiktinis parinkimas                                                                  |
| `least-used`        | Pasirenkamas rečiausiai pastaruoju metu naudotas tikslas                                         |
| `cost-optimized`    | Pirmiausia pasirenkamas pigiausias veikiantis tikslas                                            |
| `reset-aware`       | Atsižvelgiama į paslaugų teikėjo atkūrimo intervalus                                             |
| `reset-window`      | Maršrutas parenkamas pagal atkūrimo intervalus                                                   |
| `headroom`          | Pirmiausia pasirenkamas tikslas, turintis didžiausią likusios kvotos rezervą                     |
| `strict-random`     | Tikrai tolygus parinkimas (be kokybės svorių)                                                    |
| `auto`              | Naudojamas 16 veiksnių vertinimas (`autoCombo/`)                                                 |
| `lkgp`              | Pirmiausia pasirenkamas paskutinis žinomas tinkamai veikęs paslaugų teikėjas                     |
| `context-optimized` | Geriausiai tinka ilgo konteksto užklausoms                                                       |
| `fusion`            | Užklausa lygiagrečiai paskirstoma grupei, tada rezultatai susintetinami vertintojo (`fusion.ts`) |

### base.ts (1170 kodo eilučių)

**Abstraktus vykdiklis**, kurį išplečia visi 107 vykdikliai. Jame yra:

- `buildUrl()` — numatytasis URL sudarymas (poklasiai gali jį pakeisti pasirinktiniu)
- `buildHeaders()` — numatytosios antraštės (autentifikavimas, turinio tipas)
- `transformRequest()` — pagal numatytąją nuostatą perduoda nepakeistą užklausą
- `execute()` — pagrindinis HTTP ciklas su pakartotiniais bandymais, delsos didinimu ir grandinės pertraukikliu

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Tvarko visus su OpenAI/Anthropic suderinamus paslaugų teikėjus
  // Paslaugų teikėjai registruoja konfigūracijas (URL, autentifikavimą, antraštes), tačiau naudoja bendrą vykdiklio logiką
}
```

Konkrečiam paslaugų teikėjui būdinga elgsena (autentifikavimo antraštės, bazinis URL, versijos antraštės) konfigūruojama paslaugų teikėjų registre, o ne atskirose vykdiklių klasėse.

````

---

## Paslaugos (117 modulių)

Paslaugos yra **specializuoti, vienos paskirties moduliai**, kuriuos komponuoja tvarkyklės. Pagrindinės kategorijos:

### Maršruto parinkimas ir kombinavimas

- `combo.ts` — kombinuotai maršrutizuojamų užklausų pradinis taškas
- `services/autoCombo/` — 16 veiksnių vertinimas, 8 automatinio maršruto parinkimo strategijos
- `wildcardRouter.ts` — atitinka maršrutus su pakaitos simboliais (`gpt-*`)
- `modelFamilyFallback.ts` — T5 atsarginis perjungimas modelių šeimos viduje

### Užklausų dažnio ribojimas ir kvota

- `rateLimitManager.ts` — kiekvieno rakto ir teikėjo žetonų kibiras
- `usage.ts` — naudojimo registravimas
- `quotaCache.ts` — atmintyje saugomos kvotos momentinės kopijos

### Paskyra ir žetonas

- `tokenRefresh.ts` — OAuth atnaujinimas gavus 401
- `accountFallback.ts` — perjungimas į alternatyvią paskyrą
- `sessionManager.ts` — kelių sąveikos etapų seanso būsena

### Intelektas

- `intentClassifier.ts` — užklausos tikslo klasifikavimas
- `taskAwareRouter.ts` — maršruto parinkimas pagal užduoties tipą
- `thinkingBudget.ts` — mąstymo žetonų paskirstymas
- `contextManager.ts` — maršruto parinkimo konteksto įterpimas

### Atsparumas

- `resilience.ts` — pakartotinių bandymų, delsos didinimo ir grandinės pertraukiklio koordinavimas
- `emergencyFallback.ts` — paskutinės išeities atsarginis perjungimas
- `modelDeprecation.ts` — automatinis maršruto nukreipimas į modelius-įpėdinius

### Būsena

- `signatureCache.ts` — dublių šalinimas pagal užklausos parašą
- `volumeDetector.ts` — apkrovos mažinimas
- `contextHandoff.ts` — seanso serializavimas

### Glaudinimas

- `compression/` (pakatalogis) — visa glaudinimo apdorojimo seka
- 39 failai, apimantys variklius, taisyklių rinkinius ir adapterius

### Įgūdžiai

- (aprašyta [SKILLS.md](./SKILLS.md))

### Atmintis

- (aprašyta [MEMORY.md](./MEMORY.md))

---

## Vykdytojai (75+ failų)

Po vieną failą kiekvienam teikėjui. Jie visi išplečia `BaseExecutor` ir perrašo tik tai, kas skiriasi.

### Bendrieji šablonai

Teikėjai parenkami naudojant `getExecutor(providerId)`, kuri grąžina sukonfigūruotą vykdytoją. Su OpenAI / Anthropic suderinami teikėjai naudoja `DefaultExecutor` (`executors/default.ts`). Konkrečiam teikėjui būdinga elgsena (bazinis URL, autentifikavimo antraštės, API versija) konfigūruojama kataloge `open-sse/config/providers/`, o užklausos turinio transformacijos atliekamos kataloge `open-sse/translator/`.

**Pasirinktinis URL** nustatomas teikėjo konfigūracijoje:

```ts
// Teikėjo konfigūracija kataloge open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Pasirinktinis autentifikavimas** tvarkomas naudojant teikėjų registro autentifikavimo konfigūraciją (API raktą, OAuth, antraščių profilius).

**Pasirinktinės užklausos turinio** transformacijos (pvz., kai Anthropic atskiria `system` nuo `messages`) registruojamos kiekvienam teikėjui kataloge `open-sse/translator/`.

````

### Vykdytojų gamykla

`executors/index.ts` eksportuoja `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Parinkimas vykdomas per `ExecutorRegistry` (`executors/registry.ts`): kiekvienas specializuotas vykdytojas deklaruojamas integruotoje `executors/index.ts` lentelėje ir įkeliant modulį užregistruojamas naudojant `registerExecutor(alias, instance)`; `getExecutor()` tikrina registrą ir, jei teikėjas neturi specializuoto įrašo, naudoja įsimenamą `DefaultExecutor`. Visas pseudonimų → vykdytojų susiejimas apibrėžtas etaloniniame teste `tests/unit/executor-map-golden.test.ts`.

---

## Vertikliai

Verčia tarp **3 formatų**: OpenAI, Anthropic, Gemini, taip pat naujosios Responses API.

### Kada atliekamas vertimas

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Dažniausi vertimai:

- `OpenAI → Anthropic`: atskiras `system` laukas, `x-api-key` antraštė
- `OpenAI → Gemini`: `contents` vietoje `messages`, `systemInstruction`
- `OpenAI → Responses API`: `input` masyvas, `previous_response_id` būsena

### Apdorojami išskirtiniai atvejai

- `developer` vaidmuo → `system` ne OpenAI teikėjams
- `system` vaidmuo → sujungiamas su pirmuoju naudotojo pranešimu GLM/ERNIE sistemose
- `json_schema` → Gemini `responseMimeType` + `responseSchema`
- `tools` → teikėjui būdingas įrankių formatas
- Mąstymo parametrai (o1, Claude) → teikėjui būdingi atitikmenys

---

## MCP serveris

`open-sse/mcp-server/` įgyvendina **Model Context Protocol** serverį:

- **110 įrankių** (teikėjų valdymas, deriniai, atmintis, podėlis, glaudinimas, tarpinis serveris, įgūdžiai, žaidybinimas, papildiniai, Notion, Obsidian, vietinis tekstynas)
- **3 transportai**: stdio, SSE, Streamable HTTP
- **33 aprėptys**, skirtos detaliam įgaliojimų valdymui

### Įrankių registravimas

Įrankiai registruojami kaip atskiri failai kataloge `open-sse/mcp-server/tools/`; kiekvienas jų eksportuoja pavadinimą, schemą, apdorojimo funkciją ir aprėptį:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Gauti sistemos būklės momentinę kopiją",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transportai

```ts
// stdio (CLI naudojimas)
startMcpStdio(server);

// SSE (HTTP pagrįstas srautinis perdavimas)
startMcpSse(server, port);

// Streamable HTTP (šiuolaikinis MCP)
startMcpStreamable(server, port);
```

### Įgaliojimų tikrinimas

Kiekvienam įrankio iškvietimui atliekamos aprėpties patikros (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Nepakankama aprėptis");
}
```

---

## Transformatoriai

`open-sse/transformer/` konvertuoja tarp **Chat Completions** ir **Responses API** formatų.

### Kodėl naudojamas atskiras transformatorius?

Responses API yra naujasis OpenAI formatas, palaikantis **būseną išlaikančius pokalbius** (`previous_response_id`). Kai klientas siunčia Responses užklausą, OmniRoute:

1. Viduje konvertuoja Responses → Chat Completions
2. Išsiunčia teikėjui (bet kuriam teikėjui, palaikančiam Chat Completions)
3. Konvertuoja atsakymą atgal į Responses formatą
4. Srautiniu būdu perduoda konvertuotą atsakymą klientui

Transformatorius (`transformer/responsesTransformer.ts`) pateikia:

```ts
createResponsesApiTransformStream(): TransformStream
```

Jis apdoroja:

- `response.output_item.added` įvykius
- `response.output_text.delta` įvykius
- `response.completed` įvykį
- Įrankių iškvietimų susiejimą (`function_call` ↔ `tool_calls`)

---

## Konfigūracija

`open-sse/config/` saugomas konfigūracijos sluoksnis:

| Failas                        | Paskirtis                                                  |
| ----------------------------- | ---------------------------------------------------------- |
| `providerRegistry.ts`         | Pokalbių modelių registras, apimantis 352 teikėjų katalogą |
| `providerModels.ts`           | Modelių alternatyvūs pavadinimai, formatų susiejimas       |
| `constants.ts`                | Skirtieji laikai, ribos, būsenos kodai                     |
| `defaultThinkingSignature.ts` | Numatytasis Claude mąstymo parašas                         |
| `modelStrip.ts` (tarnybose)   | Kiekvienam teikėjui pritaikytas laukų pašalinimas          |

### Teikėjų registro schema

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Zod patikra modulio įkėlimo metu užtikrina, kad visos teikėjų konfigūracijos būtų tinkamos.

---

## Našumo apribojimai

Maršrutizavimo varikliui taikomi griežti našumo reikalavimai:

| Operacija                                                   | Tikslas | Matavimas                            |
| ----------------------------------------------------------- | ------- | ------------------------------------ |
| Derinio nustatymas                                          | <10ms   | 50 paskirties taškų                  |
| Užklausų dažnio limito patikra                              | <1ms    | Atmintyje laikomas žetonų kibiras    |
| Modelių šeimos atsarginio varianto parinkimas               | <5ms    | Talpykloje laikomi šeimų apibrėžimai |
| Užklausos maršrutizavimo perdavimas                         | <2ms    | Kritinis vykdymo kelias              |
| **Jokio blokuojančio I/O kritiniame maršrutizavimo kelyje** | —       | Viskas asinchroniška                 |

---

## Antimodeliai

❌ **Sinchroniniai DB iškvietimai faile `combo.ts`** — iš anksto apskaičiuokite ir laikykite talpykloje
❌ **Pakartotinių bandymų logika apdorojimo funkcijose** — naudokite `retry()` iš atsparumo paslaugos
❌ **Tiesioginė prieiga prie teikėjo konfigūracijos** — naudokite `providerRegistry` prieigos metodus
❌ **Programiniame kode fiksuotos atsarginių variantų grandinės** — apibrėžkite faile `modelFamilyFallback.ts`
❌ **Būsenos keitimas tarp lygiagrečių užklausų** — naudokite tik užklausos aprėpties kontekstą

---

## Naujo komponento pridėjimas

### Naujos paslaugos pridėjimas

1. Sukurkite `open-sse/services/[serviceName].ts`, turintį aiškiai apibrėžtą atsakomybę
2. Eksportuokite pagrindinę apdorojimo funkciją ir visas konstantas
3. Pridėkite vienetinius testus faile `tests/unit/services/[serviceName].test.mjs`
4. Integruokite į užklausų apdorojimo konvejerį faile `handlers/chatCore.ts` (jei susiję su maršrutizavimu)
5. Atnaujinkite maršrutizavimo logiką faile `combo.ts`, jei paslauga turi įtakos paskirties taško pasirinkimui
6. Aprašykite šiame faile

### Naujo vykdiklio pridėjimas

1. Sukurkite `open-sse/executors/[provider].ts`, išplečiantį `BaseExecutor`
2. Užregistruokite faile `config/providerRegistry.ts`
3. Pridėkite prie `executors/index.ts` fabriko
4. Pridėkite vykdiklio vienetinius testus
5. Aprašykite faile `docs/architecture/ARCHITECTURE.md`

### Naujo MCP įrankio pridėjimas

1. Sukurkite arba atnaujinkite `open-sse/mcp-server/tools/[category]Tools.ts`
2. Apibrėžkite įvesčių Zod schemą
3. Užregistruokite įrankį faile `mcp-server/index.ts`
4. Pridėkite prie aprėpčių matricos kataloge `mcp-server/auth/`
5. Pridėkite vienetinius testus

---

## Taip pat žr.

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — aukšto lygio architektūra
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — inžinerinis žinynas
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — kiekvieno katalogo aprašas
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 veiksnių vertinimas
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP serveris
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A serveris
- Šaltinis: `open-sse/` (daugiau nei 400 failų, ~143 tūkst. kodo eilučių)
