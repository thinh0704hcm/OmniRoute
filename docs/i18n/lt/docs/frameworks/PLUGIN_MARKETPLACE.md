# Plugin Marketplace (Lietuvių)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇹 [am](../../../am/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇿 [az](../../../az/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇰 [da](../../../da/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇪 [de](../../../de/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇷 [el](../../../el/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇸 [es](../../../es/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇪 [et](../../../et/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇱 [he](../../../he/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇩 [id](../../../id/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇹 [it](../../../it/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇭 [km](../../../km/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇲 [my](../../../my/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇴 [no](../../../no/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [or](../../../or/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇰 [si](../../../si/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [te](../../../te/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇭 [th](../../../th/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/PLUGIN_MARKETPLACE.md)

---

> **Tiesos šaltinis:** `src/lib/plugins/` (`marketplace.ts`, `manager.ts`, `manifest.ts`,
> `scanner.ts`, `loader.ts`), `src/app/api/plugins/` ir
> `src/app/(dashboard)/dashboard/plugins/`
> **Paskutinį kartą atnaujinta:** 2026-06-28 — v3.8.40

„OmniRoute“ pateikiama su į „WordPress“ panašia papildinių sistema. Papildiniai yra savarankiški
katalogai — kiekviename jų yra `plugin.json` manifestas ir įvesties failas — kurie prisijungia
prie užklausų apdorojimo grandinės (`onRequest` / `onResponse` / `onError`) ir
gyvavimo ciklo įvykių (`onInstall` / `onActivate` / `onDeactivate` / `onUninstall`).

**Papildinių prekyvietė** yra virš šios sistemos veikiantis paieškos ir atradimo sluoksnis. Ji
pateikia naršomą įdiegiamų papildinių katalogą. Pagal numatytuosius nustatymus katalogas yra
nedidelis integruotas pradinis registras; operatorius gali nurodyti pasirinktinį nuotolinio
registro URL. Tokiu atveju užklausą apsaugo DNS vardus išsprendžianti SSRF apsauga
(žr. [Saugumas](#security)).

Visi papildinių maršrutai yra pasiekiami **tik per vietinę sąsają** (1 lygis — `LOCAL_ONLY`):
papildiniai įkelia ir vykdo kodą antriniuose procesuose, todėl šie maršrutai nepasiekiami
iš ne vietinės kilmės šaltinio, neatsižvelgiant į autentifikavimą. Žr.
[`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md).

## Kaip visa tai veikia kartu

```
Valdymo skydelis (/dashboard/plugins)
  ├─ Skirtukas „Įdiegti“ → GET /api/plugins            (listPlugins)
  │                        POST /api/plugins/scan      (pluginManager.scan)
  │                        POST /api/plugins/{name}/activate|deactivate
  │                        DELETE /api/plugins/{name}   (pašalinimas)
  └─ Skirtukas „Prekyvietė“ → GET /api/plugins/marketplace
                                → listMarketplacePlugins()
                                  ├─ nėra pasirinktinio URL → integruotas SEED_REGISTRY
                                  └─ pasirinktinis URL → isSafeMarketplaceUrl() SSRF apsauga
                                                        → safeOutboundFetch(guard:"public-only")
```

- **Registro sluoksnis** — `src/lib/plugins/marketplace.ts`: pateikia katalogo
  sąrašą / atlieka jame paiešką, o įvykus bet kokiai klaidai naudoja pradinį registrą.
- **Gyvavimo ciklo sluoksnis** — `src/lib/plugins/manager.ts` (`pluginManager` vienetinis objektas):
  diegimas, naujovinimas, aktyvinimas, pasyvinimas, pašalinimas, nuskaitymas, įkėlimas paleidžiant.
- **Manifesto sluoksnis** — `src/lib/plugins/manifest.ts`: „Zod“ schema ir numatytosios
  `plugin.json` reikšmės.
- **Skaitytuvas** — `src/lib/plugins/scanner.ts`: aptinka papildinių kataloge
  diske esančius papildinius.
- **Įkėliklis** — `src/lib/plugins/loader.ts`: paleidžia kiekvieną papildinį izoliuotame
  antriniame procese ir tarpininkauja iškviečiant kablius per IPC.

## Prekyvietės katalogas

`listMarketplacePlugins()` (`src/lib/plugins/marketplace.ts`) grąžina
`MarketplaceEntry` objektų sąrašą:

| Laukas        | Tipas    | Pastabos                                      |
| ------------- | -------- | --------------------------------------------- |
| `name`        | string   | kebab-case formato papildinio pavadinimas     |
| `version`     | string   | semver                                        |
| `description` | string   | Trumpa santrauka                              |
| `author`      | string   | Autorius / organizacija                       |
| `license`     | string   | SPDX formato licencijos identifikatorius      |
| `downloadUrl` | string   | Šaltinio atsisiuntimo URL (gali būti tuščias) |
| `repository`  | string?  | Pasirinktinis saugyklos URL                   |
| `tags`        | string[] | Paieškos / filtravimo žymos                   |
| `downloads`   | number   | Atsisiuntimų skaičius                         |
| `rating`      | number   | 0–5                                           |
| `verified`    | boolean  | Ar įrašas pažymėtas kaip patvirtintas         |
| `lastUpdated` | string   | Apytikslio ISO formato datos eilutė           |

Kai nepasirinktas pasirinktinio registro URL, naudojamas integruotas
`SEED_REGISTRY` katalogas (šiuo metu `request-logger`, `rate-limiter`, `cost-tracker` ir
`theme-manager`). Pradinis registras pasiekiamas visada — jei sukonfigūruotas nuotolinis
registras nepasiekiamas, grąžina ne `200` būseną arba neatpažįstamo formato
turinį, `listMarketplacePlugins()` užregistruoja įspėjimą ir grįžta prie pradinio sąrašo.

> Pastaba: prekyvietės **katalogas** (naršymas / paieška) visiškai prijungtas prie sistemos, tačiau
> diegimas vienu spustelėjimu iš prekyvietės **katalogo** dar neįgyvendintas —
> valdymo skydelio mygtukas „Įdiegti“ prekyvietės įraše šiuo metu rodo pranešimą
> „netrukus“. Šiuo metu diegiama naudojant diegimo iš vietinio kelio procesą
> (`POST /api/plugins`) ir aptikimą diske (`POST /api/plugins/scan`).

## REST API

Visiems galiniams taškams reikalingas valdymo autentifikavimas (`requireManagementAuth`),
be to, jie pasiekiami tik per grįžtamojo ryšio sąsają — `/api/plugins` ir
`/api/plugins/` yra įtraukti į `LOCAL_ONLY_API_PREFIXES`
(`src/server/authz/routeGuard.ts`).

| Galinis taškas                   | Metodas | Aprašymas                                                             |
| -------------------------------- | ------- | --------------------------------------------------------------------- |
| `/api/plugins`                   | GET     | Pateikti įdiegtų papildinių sąrašą (pasirinktinis `?status=` filtras) |
| `/api/plugins`                   | POST    | Įdiegti papildinį iš absoliutaus vietinio kelio                       |
| `/api/plugins/scan`              | POST    | Nuskaityti papildinių katalogą ir užregistruoti naujus papildinius    |
| `/api/plugins/marketplace`       | GET     | Pateikti prekyvietės katalogo įrašų sąrašą                            |
| `/api/plugins/[name]`            | GET     | Gauti įdiegto papildinio informaciją                                  |
| `/api/plugins/[name]`            | DELETE  | Pašalinti papildinį                                                   |
| `/api/plugins/[name]/activate`   | POST    | Aktyvinti (įkelti ir užregistruoti kablius)                           |
| `/api/plugins/[name]/deactivate` | POST    | Pasyvinti (iškviesti `onDeactivate`, išregistruoti kablius)           |
| `/api/plugins/[name]/config`     | GET     | Gauti papildinio konfigūraciją ir konfigūracijos schemą               |
| `/api/plugins/[name]/config`     | PUT     | Atnaujinti papildinio konfigūraciją (patikrinant pagal schemą)        |

`GET /api/plugins` filtras `status` priima vieną iš šių reikšmių:
`installed` / `active` / `inactive` / `error`. Neteisinga reikšmė grąžina `400`.

### Įdiegtų papildinių sąrašas

```bash
curl http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..."
```

### Diegimas iš vietinio kelio

```bash
curl -X POST http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "path": "/absolute/path/to/my-plugin" }'
```

`path` turi būti **absoliutus** ir jame negali būti `..` perėjimo segmentų arba
nulinių baitų (tai užtikrina Zod). Šaltinio kataloge turi būti tinkamas
`plugin.json` (arba katalogas turi būti jo pirminis katalogas). Sėkmės atveju
atsakymas yra `201` su įdiegto papildinio įrašu.

### Prekyvietės naršymas

```bash
curl http://localhost:20128/api/plugins/marketplace \
  -H "Cookie: auth_token=..."
```

### Papildinio konfigūracijos atnaujinimas

```bash
curl -X PUT http://localhost:20128/api/plugins/my-plugin/config \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "config": { "level": "debug", "maxItems": 100 } }'
```

`PUT .../config` patikrina kiekvieną pateiktą reikšmę pagal papildinio
`configSchema` (deklaruotą manifeste): `number` laukuose atsižvelgiama į
`min`/`max`, o `select` laukai turi atitikti deklaruotą `enum`. Schemoje
nenurodyti raktai yra leidžiami.

## Konfigūracija

### Papildinių katalogas

Papildiniai laikomi OmniRoute duomenų kataloge:

```
~/.omniroute/plugins/<plugin-name>/
  ├─ plugin.json
  └─ index.js          # (arba failas, į kurį nurodo manifest.main)
```

`getDefaultPluginDir()` (`src/lib/plugins/scanner.ts`) šį katalogą nustato
trimis etapais:

1. **`OMNIROUTE_PLUGINS_DIR`**, jei nustatytas — naudojamas pažodžiui,
   neatsižvelgiant į `HOME` reikšmę. Tai aiški Docker/K8s parinktis, kai papildinių
   medis prijungiamas prie kelio, kuris paprastai neturi nieko bendra su konteinerio
   namų katalogu (#11827).
2. `<home>/.omniroute/plugins`, kur `<home>` gaunamas iš `HOME` / `USERPROFILE`
   aplinkos kintamųjų.
3. `/tmp/.omniroute/plugins`, kai procesas apskritai neeksportuoja namų katalogo.

Nustatytas katalogas vieną kartą paleidimo metu įrašomas į žurnalą kaip
`scanner.dir_resolved`, nurodant pasirinktą įvestį (`OMNIROUTE_PLUGINS_DIR`,
`home` arba `no-home-fallback`) — todėl atvaizdas, tyliai pasirinkęs 3 etapą, apie
tai praneša, užuot rodęs tik tuščią papildinių sąrašą.
`POST /api/plugins/scan` aptinka kiekvieną jame esantį pakatalogį, kuriame yra
tinkamas `plugin.json`, ir jį užregistruoja; tas pats katalogas yra šakninis
katalogas, į kurį `pluginManager.install()` kopijuoja papildinius, todėl pakeitus
katalogą kartu perkeliamos ir aptikimo, ir diegimo vietos.

> **`OMNIROUTE_PLUGINS_DIR` nėra `OMNIROUTE_PLUGIN_PATH`.** Pastarąjį nuskaito tik
> CLI komandų papildinių įkėliklis (`bin/cli/plugins.mjs`), kad rastų `omniroute-cmd-*`
> npm paketus, pridedančius `omniroute` antrines komandas — jis neturi jokios įtakos
> čia aprašytam vykdymo aplinkos skaitytuvui. Apie tai žr. [PLUGINS.md](./PLUGINS.md).

### Pasirinktinis prekyvietės registro URL

Prekyvietės katalogo šaltinis nuskaitomas iš `pluginMarketplaceUrl` nustatymo
(`src/lib/plugins/marketplace.ts` nuskaito `settings.pluginMarketplaceUrl`). Kai
nustatytas `http(s)` URL, `listMarketplacePlugins()` gauna duomenis iš to URL ir
priima arba aukščiausio lygio JSON įrašų masyvą, arba objektą su `plugins` masyvu;
įrašai, kurių `name` nėra eilutė, atmetami. Kai reikšmė nenustatyta (arba kai
užklausa nepraeina SSRF apsaugos / grąžina netinkamą atsakymą), naudojamas
integruotasis pradinis registras.

Valdymo skydelio kortelėje „Prekyvietė“ pateikiamas šio URL laukas (reikšmė
nuskaitoma iš `GET /api/settings`).

> Įgyvendinimo pastaba: valdymo skydelio veiksmas „Išsaugoti“ siunčia
> `pluginMarketplaceUrl` į `PATCH /api/settings`. Šio teksto rašymo metu šis
> raktas nėra deklaruotas `updateSettingsSchema`
> (`src/shared/validation/settingsSchemas.ts`), todėl prieš juo pasikliaudami
> savo laidoje patikrinkite, ar reikšmė išsaugoma — **skaitymo** kelias
> (`getSettings()` → `listMarketplacePlugins()`) atsižvelgia į šį raktą, kai jis
> yra nustatymų saugykloje.

## Saugumas

### Maršruto lygis — tik grįžtamasis ryšys

Papildiniai vykdo kodą sukurtuose antriniuose procesuose, todėl visa `/api/plugins`
sritis priskiriama `LOCAL_ONLY` (1 lygis). Grįžtamojo ryšio apribojimas taikomas
besąlygiškai **prieš** bet kokią autentifikavimo patikrą, todėl nutekintas valdymo
atpažinimo ženklas, pasiekiantis įrenginį per tunelį, vis tiek negali įdiegti,
aktyvuoti ar pašalinti papildinio.
Žr. [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) ir
griežtąsias taisykles Nr. 15 / Nr. 17.

### Prekyvietės registro SSRF apsauga

Pasirinktinio registro URL yra konfigūracija, kurią gali paveikti užpuolikas,
todėl prieš jį gaudama `listMarketplacePlugins()` pritaiko du apsaugos sluoksnius:

1. **`isSafeMarketplaceUrl(url)`** (`src/lib/plugins/marketplace.ts`):
   - Atmeta viską, kas nėra `http:` / `https:`.
   - Naudodama kanoninę `isPrivateHost`
     (`src/shared/network/outboundUrlGuard.ts`) atmeta tiesiogiai nurodytus
     privačius, grįžtamojo ryšio, vietinio ryšio ir ULA pagrindinius kompiuterius
     (tiek IPv4, **tiek** IPv6, įskaitant IPv4 atvaizdavimą).
   - Išsprendžia **ir** `A`, **ir** `AAAA` įrašus bei atmeta URL, jei **bet kuris**
     išspręstas adresas yra privatus — taip užkertant kelią viešo pagrindinio
     kompiuterio vardo → privataus IP apėjimui.
   - **Gedimo atveju uždraudžia**: nepavykus DNS vardų išsprendimui, URL
     atmetamas.
2. **`safeOutboundFetch(url, { guard: "public-only", timeoutMs: 5000 })`**
   (`src/shared/network/safeOutboundFetch.ts`): gavimo metu dar kartą pritaiko
   tik viešiesiems adresams skirtą URL apsaugą ir **blokuoja peradresavimus**
   (neleidžiamas viešo → privataus adreso `30x` perėjimas).

Nė vieno sluoksnio patikros nepraėjęs URL nenutraukia užklausos — prekyvietė
tyliai grįžta prie integruoto pradinio registro ir užregistruoja įspėjimą.

> Ši apsauga buvo sustiprinta PR #3774, kad būtų konkrečiai išsprendžiami A + AAAA
> įrašai ir vietoje tik IPv4 skirtos patikros naudojama kanoninė `isPrivateHost`.

### Papildinių vykdymo izoliavimas

- **Procesų izoliavimas** — `loadPlugin()` (`src/lib/plugins/loader.ts`) paleidžia
  kiekvieną papildinį atskirame antriniame Node.js procese ir palaiko ryšį per
  IPC. Kablių iškvietimams taikomas skirtasis laikas su `SIGTERM` → `SIGKILL`
  eskalavimu.
- **Aplinkos leidžiamųjų sąrašas** — antrinis procesas gauna tik į leidžiamųjų
  sąrašą įtrauktą aplinkos kintamųjų rinkinį; platesnis rinkinys suteikiamas tik
  tada, kai manifeste prašoma `env` teisės.
- **Kelio apribojimas** — diegiant, atnaujinant ar šalinant patikrinama, ar
  papildinio katalogas ir `manifest.main` išsprendžiami **valdomos papildinių
  šakninės vietos ribose**, prieš atliekant bet kokį kopijavimą ar rekursyvų
  šalinimą (taip apsisaugoma nuo pakeistų DB kelių ir `../` perėjimo
  `manifest.main` reikšmėje). Aktyvavimo metu simbolinės nuorodos išsprendžiamos
  naudojant `realpath`, o įkėlimo vieta, išeinanti už papildinio katalogo ribų,
  atmetama.
- **Pasirinktinis vientisumo susiejimas** — manifeste gali būti nurodytas
  `integrity` (`sha256-<base64>`, SRI formatas) laukas. Jei jis pateiktas,
  įkėlimo programa įkėlimo metu patikrina įvesties failo maišą ir, jai
  nesutapus, atsisako aktyvuoti papildinį. Tai yra pasirenkamas pakeitimų
  aptikimo mechanizmas, **o ne** saugumo riba — tik grįžtamajam ryšiui skirtas
  maršrutizavimas ir leidimų modelis yra tikrosios ribos.

## Manifestas (`plugin.json`)

Tikrinamas naudojant `PluginManifestSchema` (`src/lib/plugins/manifest.ts`):

| Laukas             | Tipas     | Pastabos                                                              |
| ------------------ | --------- | --------------------------------------------------------------------- |
| `name`             | string    | Privalomas; kebab-case (`^[a-z0-9-]+$`), 1–100 simbolių               |
| `version`          | string    | Privalomas; semver (`MAJOR.MINOR.PATCH`)                              |
| `description`      | string?   | ≤ 500 simbolių                                                        |
| `author`           | string?   | ≤ 200 simbolių                                                        |
| `license`          | string?   | Numatytoji reikšmė — `MIT`                                            |
| `main`             | string?   | Įvesties failas; numatytoji reikšmė — `index.js`                      |
| `source`           | enum?     | `local` \| `marketplace` (numatytoji reikšmė — `local`)               |
| `tags`             | string[]? | Paieškos žymos                                                        |
| `requires`         | object?   | `{ omniroute?, permissions[] }`                                       |
| `hooks`            | object?   | Loginės reikšmės, nurodančios, kuriuos kablius įgyvendina papildinys  |
| `skills`           | object[]? | Pasirinktiniai gebėjimų apibrėžimai                                   |
| `enabledByDefault` | boolean?  | Automatiškai aktyvuoti įdiegus                                        |
| `configSchema`     | object?   | Konfigūracijos laukų žemėlapis (`string`/`number`/`boolean`/`select`) |
| `integrity`        | string?   | Pasirinktinis įvesties failo `sha256-<base64>` susiejimas             |

Leidimai pasirenkami iš išvardijimo
`network` / `file-read` / `file-write` / `env` / `exec`.

## Gyvavimo ciklo eiga

```
diegimas (POST /api/plugins, kelias)
  → nuskaityti / patikrinti manifestą → nukopijuoti į tarpinę sritį → užtikrinti, kad pagrindinis failas yra kataloge
  → atomiškai pervadinti į ~/.omniroute/plugins/<name> → įterpti eilutę į DB
  → iškviesti onInstall → jei enabledByDefault: aktyvuoti

aktyvavimas (POST /api/plugins/{name}/activate)
  → realpath aprėpties patikra → loadPlugin() (paleisti antrinį procesą)
  → užregistruoti deklaruotus kablius → būsena = "active" → iškviesti onActivate

išaktyvinimas (POST /api/plugins/{name}/deactivate)
  → iškviesti onDeactivate (PRIEŠ išregistruojant) → išregistruoti kablius
  → nutraukti antrinį procesą → būsena = "inactive"

pašalinimas (DELETE /api/plugins/{name})
  → išaktyvinti, jei aktyvus → iškviesti onUninstall
  → patikrinus aprėptį rekursyviai ištrinti papildinio katalogą → ištrinti DB eilutę
```

Pakartotinai vykdant `install` katalogui, kurio manifesto versija yra **griežtai
naujesnė** už įdiegtą versiją, papildinys automatiškai atnaujinamas (švarus
perdiegimas; konfigūracija atkuriama į numatytąsias reikšmes). Tokia pati arba
senesnė versija atmetama.

## Duomenų bazė

Lentelė `plugins` (migracija `076_create_plugins.sql`):

| Stulpelis       | Tipas   | Pastabos                                                           |
| --------------- | ------- | ------------------------------------------------------------------ |
| `id`            | TEXT PK | UUID                                                               |
| `name`          | TEXT    | Unikalus                                                           |
| `version`       | TEXT    | semver; numatytoji reikšmė `1.0.0`                                 |
| `description`   | TEXT    | Neprivalomas                                                       |
| `author`        | TEXT    | Neprivalomas                                                       |
| `license`       | TEXT    | Numatytoji reikšmė `MIT`                                           |
| `main`          | TEXT    | Įvesties failas; numatytoji reikšmė `index.js`                     |
| `source`        | TEXT    | Numatytoji reikšmė `local`                                         |
| `tags`          | TEXT    | JSON masyvas; numatytoji reikšmė `[]`                              |
| `status`        | TEXT    | `installed` \| `active` \| `inactive` \| `error`                   |
| `enabled`       | INT     | 0/1; numatytoji reikšmė 0                                          |
| `manifest`      | TEXT    | Visas manifesto JSON                                               |
| `config`        | TEXT    | JSON; numatytoji reikšmė `{}`                                      |
| `config_schema` | TEXT    | JSON; numatytoji reikšmė `{}`                                      |
| `hooks`         | TEXT    | Deklaruotų kablių pavadinimų JSON masyvas; numatytoji reikšmė `[]` |
| `permissions`   | TEXT    | JSON masyvas; numatytoji reikšmė `[]`                              |
| `plugin_dir`    | TEXT    | Absoliutus diegimo katalogas                                       |
| `error_message` | TEXT    | Nustatoma, kai `status = "error"`                                  |
| `installed_at`  | TEXT    | `datetime('now')`                                                  |
| `updated_at`    | TEXT    | `datetime('now')`                                                  |
| `activated_at`  | TEXT    | Nustatoma aktyvuojant                                              |

Papildinių metrika ir analizės duomenys sekami papildomose lentelėse
(`090_plugin_metrics.sql`, `091_plugin_analytics.sql`).

## Valdymo skydelis

Valdymo skydelio puslapyje `/dashboard/plugins`
(`src/app/(dashboard)/dashboard/plugins/page.tsx`) pateikiami du skirtukai:

- **Įdiegti** — pateikiamas įdiegtų papildinių sąrašas su jų deklaruotais
  kabliais, aktyvavimo / išaktyvinimo jungikliu, pašalinimo mygtuku ir veiksmu
  „Ieškoti papildinių“ (`POST /api/plugins/scan`).
- **Prekyvietė** — rodomas katalogas iš `GET /api/plugins/marketplace` ir
  laukas pasirinktiniam registro URL nustatyti.

Atskiro papildinio konfigūracijos puslapis pasiekiamas adresu
`/dashboard/plugins/[name]/config`
(`src/app/(dashboard)/dashboard/plugins/[name]/config/page.tsx`).

## Taip pat žr.

- [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) —
  kodėl `/api/plugins` pasiekiamas tik per grįžtamojo ryšio sąsają (1 lygis)
- [`docs/frameworks/SKILLS.md`](./SKILLS.md) — susijusi gebėjimų sistema
  (`src/lib/skills/`); papildiniai gali deklaruoti gebėjimus savo manifeste
- [`docs/frameworks/WEBHOOKS.md`](./WEBHOOKS.md) — įvykiais grindžiamos išeinančiosios
  integracijos
- [`docs/security/ERROR_SANITIZATION.md`](../security/ERROR_SANITIZATION.md) —
  `buildErrorBody()` šablonas, kurį klaidų atsakams naudoja kiekvienas papildinio maršrutas
