# 📖 Setup Guide — OmniRoute (ខ្មែរ)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/SETUP_GUIDE.md) · 🇪🇹 [am](../../../am/docs/guides/SETUP_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/guides/SETUP_GUIDE.md) · 🇦🇿 [az](../../../az/docs/guides/SETUP_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/guides/SETUP_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/guides/SETUP_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/guides/SETUP_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/guides/SETUP_GUIDE.md) · 🇩🇰 [da](../../../da/docs/guides/SETUP_GUIDE.md) · 🇩🇪 [de](../../../de/docs/guides/SETUP_GUIDE.md) · 🇬🇷 [el](../../../el/docs/guides/SETUP_GUIDE.md) · 🇪🇸 [es](../../../es/docs/guides/SETUP_GUIDE.md) · 🇪🇪 [et](../../../et/docs/guides/SETUP_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/guides/SETUP_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/guides/SETUP_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/guides/SETUP_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/guides/SETUP_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/guides/SETUP_GUIDE.md) · 🇮🇱 [he](../../../he/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/guides/SETUP_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/guides/SETUP_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/guides/SETUP_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/guides/SETUP_GUIDE.md) · 🇮🇩 [id](../../../id/docs/guides/SETUP_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/guides/SETUP_GUIDE.md) · 🇮🇹 [it](../../../it/docs/guides/SETUP_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/guides/SETUP_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/guides/SETUP_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/guides/SETUP_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/guides/SETUP_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/guides/SETUP_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/guides/SETUP_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/guides/SETUP_GUIDE.md) · 🇲🇲 [my](../../../my/docs/guides/SETUP_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/guides/SETUP_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/guides/SETUP_GUIDE.md) · 🇳🇴 [no](../../../no/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [or](../../../or/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/guides/SETUP_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/guides/SETUP_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/guides/SETUP_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/guides/SETUP_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/SETUP_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/guides/SETUP_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/guides/SETUP_GUIDE.md) · 🇱🇰 [si](../../../si/docs/guides/SETUP_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/guides/SETUP_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/guides/SETUP_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/guides/SETUP_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/guides/SETUP_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/guides/SETUP_GUIDE.md) · 🇮🇳 [te](../../../te/docs/guides/SETUP_GUIDE.md) · 🇹🇭 [th](../../../th/docs/guides/SETUP_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/guides/SETUP_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/SETUP_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/guides/SETUP_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/guides/SETUP_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/guides/SETUP_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/guides/SETUP_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/SETUP_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/SETUP_GUIDE.md)

---

> ឯកសារយោងពេញលេញសម្រាប់ការដំឡើង OmniRoute។ សម្រាប់កំណែខ្លី សូមមើល [ការចាប់ផ្ដើមរហ័សក្នុង README](../README.md#-quick-start)។

## តារាងមាតិកា

- [វិធីសាស្ត្រដំឡើង](#install-methods)
- [ការកំណត់រចនាសម្ព័ន្ធឧបករណ៍ CLI](#cli-tool-configuration)
- [ការដំឡើងប្រូតូកូល (MCP + A2A)](#protocol-setup-mcp--a2a)
- [ការកំណត់ពេលផុតកំណត់](#timeout-configuration)
- [របៀបច្រកដាច់ដោយឡែក](#split-port-mode)
- [Void Linux (xbps-src)](#void-linux-xbps-src-template)
- [ការលុបចេញ](#uninstalling)

---

## វិធីសាស្ត្រដំឡើង

### npm (បានណែនាំ)

```bash
npm install -g omniroute
omniroute
```

ផ្ទាំងគ្រប់គ្រងបើកនៅ `http://localhost:20128` ហើយ URL មូលដ្ឋានរបស់ API គឺ `http://localhost:20128/v1`។

### pnpm

```bash
pnpm add -g omniroute@latest --allow-build=better-sqlite3 --allow-build=@swc/core
omniroute
```

> **អ្នកប្រើ pnpm៖** ត្រូវការទង់ `--allow-build` ដើម្បីបើកស្គ្រីប build ដើមសម្រាប់ `better-sqlite3` និង `@swc/core`។ ពាក្យបញ្ជា `pnpm approve-builds -g` មិនត្រូវបានគាំទ្រសម្រាប់ការដំឡើងជាសកលលើ pnpm v11 ទេ។

### Arch Linux (AUR)

```bash
yay -S omniroute-bin
systemctl --user enable --now omniroute.service
```

[កញ្ចប់ AUR](https://aur.archlinux.org/packages/omniroute-bin) ដំឡើង OmniRoute និងផ្ដល់សេវាអ្នកប្រើប្រាស់ systemd។

### ពីកូដប្រភព

```bash
npm install
PORT=20128 DASHBOARD_PORT=20129 NEXT_PUBLIC_BASE_URL=http://localhost:20129 npm run dev
```

> **ចំណាំសម្រាប់ Windows៖** តាមលំនាំដើម OmniRoute ប្រើ `%APPDATA%\omniroute` នៅពេលមិនមានថតចាស់ `%USERPROFILE%\.omniroute`។ កំណត់ `DATA_DIR` ដើម្បីជ្រើសរើសទីតាំងថតទិន្នន័យផ្សេង។

> **ចំណាំ៖** `npm install` បង្កើត `.env` ដោយស្វ័យប្រវត្តិពី `.env.example` នៅពេលដំណើរការលើកដំបូង។ ការដំឡើងបន្ទាប់នឹងមិនសរសេរជាន់លើ `.env` ដែលមានស្រាប់ទេ ដូច្នេះការកែតម្រូវផ្ទាល់ខ្លួនត្រូវបានរក្សាទុក។ ដើម្បីបង្កើតវាឡើងវិញ សូមលុប `.env` មុនពេលដំណើរការម្ដងទៀត។

### Docker

សូមមើល [មគ្គុទ្ទេសក៍ Docker](./DOCKER_GUIDE.md) សម្រាប់ការដំឡើង Docker ពេញលេញ រួមទាំងប្រូហ្វាល់ Compose និង Caddy HTTPS។

### កម្មវិធីផ្ទៃតុ (Electron)

OmniRoute ផ្ដល់ជូនកម្មវិធីគ្របសម្រាប់ផ្ទៃតុដែលបង្កើតឡើងលើ Electron 41 + electron-builder 26.10។ ស្គ្រីបដែលអាចប្រើបាន (ថតឫសរបស់ workspace)៖

```bash
npm run electron:dev          # ដំណើរការកម្មវិធីផ្ទៃតុជាមួយការផ្ទុកឡើងវិញដោយស្វ័យប្រវត្តិ
npm run electron:build        # Build សម្រាប់ OS បច្ចុប្បន្ន (រកឃើញដោយស្វ័យប្រវត្តិ)
npm run electron:build:win    # កម្មវិធីដំឡើង Windows (NSIS + portable)
npm run electron:build:mac    # macOS (dmg + zip, arm64+x64)
npm run electron:build:linux  # Linux (AppImage + deb + rpm)
npm run electron:smoke:packaged  # សាកល្បង smoke លើ build ដែលបានវេចខ្ចប់
```

កំណែចេញផ្សាយនៃកម្មវិធីដំឡើងផ្ទៃតុត្រូវបានភ្ជាប់ជាមួយ GitHub Releases។ សម្រាប់ព័ត៌មានលម្អិតស៊ីជម្រៅពេញលេញអំពី Electron (ការចុះហត្ថលេខា, ស្ពាន IPC, ការចែកចាយ) សូមមើល [`ELECTRON_GUIDE.md`](./ELECTRON_GUIDE.md) _(បានបង្កើតនៅដំណាក់កាលក្រោយ)_។

### ម៉ាស៊ីនមេគ្មានចំណុចប្រទាក់ (CI/ស្វ័យប្រវត្តិកម្ម)

សម្រាប់ការដំឡើងដោយមិនត្រូវការអ្នកមើលការខុសត្រូវ (Docker, Kubernetes, CI) សូមប្រើ៖

```bash
omniroute setup --non-interactive
omniroute providers test-batch
```

នៅពេលប្រើរួមគ្នាជាមួយអថេរបរិស្ថាន (`INITIAL_PASSWORD`, `OMNIROUTE_WS_BRIDGE_SECRET` ជាដើម) វាអនុញ្ញាតឱ្យអ្នកដំណើរការ instance របស់ OmniRoute ដែលអាចកំណត់ដោយស្គ្រីបបានទាំងស្រុង។

### ជម្រើស CLI

| ពាក្យបញ្ជា              | សេចក្ដីពិពណ៌នា                                                                           |
| ----------------------- | ---------------------------------------------------------------------------------------- |
| `omniroute`             | ចាប់ផ្ដើមម៉ាស៊ីនមេ (`PORT=20128`, API និងផ្ទាំងគ្រប់គ្រងនៅលើច្រកតែមួយ)                   |
| `omniroute setup`       | ការណែនាំតាម CLI សម្រាប់ពាក្យសម្ងាត់ និងអ្នកផ្ដល់សេវាដំបូង                                |
| `omniroute doctor`      | ដំណើរការការត្រួតពិនិត្យសុខភាពក្នុងមូលដ្ឋាន ដោយមិនចាប់ផ្ដើមម៉ាស៊ីនមេ                      |
| `omniroute providers`   | ស្វែងរក រាយបញ្ជី ផ្ទៀងផ្ទាត់ និងសាកល្បងអ្នកផ្ដល់សេវាពី CLI                               |
| `omniroute config`      | ការកំណត់រចនាសម្ព័ន្ធឧបករណ៍ CLI — រាយបញ្ជី ទាញយក កំណត់ និងផ្ទៀងផ្ទាត់ការកំណត់រចនាសម្ព័ន្ធ |
| `omniroute status`      | ផ្ទាំងគ្រប់គ្រងស្ថានភាពក្រៅបណ្ដាញ — កំណែ, DB, ឧបករណ៍, ការកំណត់រចនាសម្ព័ន្ធ               |
| `omniroute logs`        | បង្ហាញកំណត់ហេតុការប្រើប្រាស់បន្តផ្ទាល់ពី API (គាំទ្រ `--follow`)                         |
| `omniroute update`      | ពិនិត្យមើល ឬអនុវត្តការធ្វើបច្ចុប្បន្នភាព OmniRoute                                       |
| `omniroute provider`    | គ្រប់គ្រងការតភ្ជាប់អ្នកផ្ដល់សេវា — បន្ថែម រាយបញ្ជី លុប សាកល្បង កំណត់ជាលំនាំដើម           |
| `omniroute --port 3000` | កំណត់ច្រក canonical/API ទៅជា 3000                                                        |
| `omniroute --mcp`       | ចាប់ផ្ដើមម៉ាស៊ីនមេ MCP (ការដឹកជញ្ជូន stdio)                                              |
| `omniroute --no-open`   | កុំបើកកម្មវិធីរុករកដោយស្វ័យប្រវត្តិ                                                      |
| `omniroute --help`      | បង្ហាញជំនួយ                                                                              |

ការដំឡើងគ្មានចំណុចប្រទាក់អាចកំណត់ដោយស្គ្រីបដោយប្រើទង់ ឬអថេរបរិស្ថាន៖

```bash
omniroute setup --non-interactive --password "$OMNIROUTE_PASSWORD"
omniroute setup --non-interactive --add-provider --provider openai --api-key "$OPENAI_API_KEY"
omniroute setup --non-interactive --add-provider --provider openai --api-key "$OPENAI_API_KEY" --test-provider
```

ដំណើរការការវិនិច្ឆ័យក្នុងមូលដ្ឋានដោយមិនបើកផ្ទាំងគ្រប់គ្រង៖

```bash
omniroute doctor
omniroute doctor --json
omniroute doctor --no-liveness
```

គ្រប់គ្រងអ្នកផ្ដល់សេវាតាមរយៈ SSH ឬស្គ្រីបដោយមិនបើកផ្ទាំងគ្រប់គ្រង៖

```bash
omniroute providers available
omniroute providers available --search openai
omniroute providers available --category api-key
omniroute providers list
omniroute providers test <id-or-name>
omniroute providers test-all
omniroute providers validate
```

---

## ការកំណត់រចនាសម្ព័ន្ធឧបករណ៍ CLI

### 1) ភ្ជាប់អ្នកផ្ដល់សេវា និងបង្កើត API Key

1. បើក Dashboard → `Providers` ហើយភ្ជាប់អ្នកផ្ដល់សេវាយ៉ាងហោចណាស់មួយ (OAuth ឬ API key)។
2. បើក Dashboard → `Endpoints` ហើយបង្កើត API key មួយ។
3. (ជាជម្រើស) បើក Dashboard → `Combos` ហើយកំណត់ខ្សែសង្រ្គោះបម្រុងរបស់អ្នក។

### 2) ភ្ជាប់ឧបករណ៍សរសេរកូដរបស់អ្នក

```txt
Base URL: http://localhost:20128/v1
API Key:  [copy from Endpoint page]
Model:    if/qwen3.8-max-preview (or any provider/model prefix)
```

ប្រសិនបើកម្មវិធីកែសម្រួលរបស់អ្នកមិនអាចផ្ញើ `Authorization: Bearer ...` បានទេ សូមប្រើ base ដែលត្រូវគ្នា និងមាន token ជំនួសវិញ៖

```txt
Base URL: http://localhost:20128/api/v1/vscode/YOUR_KEY/
Models URL: http://localhost:20128/api/v1/vscode/YOUR_KEY/models
Chat URL: http://localhost:20128/api/v1/vscode/YOUR_KEY/chat/completions
Ollama Tags URL: http://localhost:20128/api/v1/vscode/YOUR_KEY/api/tags
```

ដំណើរការជាមួយ Claude Code, Codex CLI, Cursor, Cline, OpenClaw, OpenCode និង SDK ដែលត្រូវគ្នាជាមួយ OpenAI។

#### កំណត់រចនាសម្ព័ន្ធដោយស្វ័យប្រវត្តិជាមួយ `setup-*`

ជំនួសឱ្យការបិទភ្ជាប់ base URL និង key ដោយដៃ អនុញ្ញាតឱ្យ OmniRoute សរសេរការកំណត់រចនាសម្ព័ន្ធផ្ទាល់ខ្លួនរបស់ឧបករណ៍នីមួយៗ
ពីកាតាឡុកម៉ូដែលដែលកំពុងដំណើរការ។ ប្រើពាក្យបញ្ជាមួយសម្រាប់ឧបករណ៍នីមួយៗ៖

```bash
omniroute setup-codex        # ទម្រង់ ~/.codex/<name>.config.toml
omniroute setup-claude       # ~/.claude/profiles/<name>/settings.json
omniroute setup-opencode     # ~/.config/opencode/opencode.json (ត្រូវគ្នាជាមួយ openai)
omniroute setup-cline        # ការកំណត់ Cline CLI + ផ្នែកបន្ថែម VS Code
omniroute setup-kilo         # Kilo Code
omniroute setup-continue     # ~/.continue/config.yaml (Continue / cn)
omniroute setup-cursor       # បង្ហាញជំហានក្នុងកម្មវិធីរបស់ Cursor
omniroute setup-roo          # ការនាំចូល Roo Code + ទ្រនិច autoImport
omniroute setup-crush        # ~/.config/crush/crush.json
omniroute setup-goose        # ~/.config/goose/config.yaml
omniroute setup-aider        # ~/.aider.conf.yml
omniroute setup-qwen         # ~/.qwen/settings.json + ~/.qwen/.env
```

ពាក្យបញ្ជានីមួយៗទទួលយក `--remote <url> --api-key <key>` ដើម្បីកំណត់រចនាសម្ព័ន្ធឧបករណ៍មូលដ្ឋានឱ្យប្រើជាមួយ OmniRoute
**ពីចម្ងាយ** ព្រមទាំង `--dry-run` ដើម្បីមើលជាមុន។ ដើម្បីចាប់ផ្ដើម CLI ដោយបានបញ្ចូល env ត្រឹមត្រូវ
និងមិនសរសេរការកំណត់រចនាសម្ព័ន្ធអ្វីទាំងអស់ សូមប្រើកម្មវិធីចាប់ផ្ដើមទូទៅ
`omniroute run <target>` (claude, codex, aider, goose, opencode, qwen, gemini);
កម្មវិធីចាប់ផ្ដើមជាក់លាក់តាមឧបករណ៍ចាស់ៗ `omniroute launch` (Claude Code) និង
`omniroute launch-codex` (Codex) នៅតែអាចប្រើបាន។

សម្រាប់តារាងពេញលេញ (អ្វីដែលពាក្យបញ្ជានីមួយៗសរសេរ, flag ទាំងអស់, មូលដ្ឋានធៀបនឹងពីចម្ងាយ, អនុសញ្ញា base-URL
`/v1`) សូមមើល **[ការរួមបញ្ចូល CLI](./CLI-INTEGRATIONS.md)**។

សម្រាប់ការកំណត់រចនាសម្ព័ន្ធលម្អិតតាមឧបករណ៍នីមួយៗ (Claude Code, Codex CLI, Cursor, Cline, OpenClaw, Kilo Code, Copilot និងច្រើនទៀត) សូមមើល **[មគ្គុទ្ទេសក៍ឧបករណ៍ CLI](../reference/CLI-TOOLS.md)** ដែលផ្ដល់ជូនដោយឡែក។

---

## ការរៀបចំពិធីការ (MCP + A2A)

### ការរៀបចំ MCP (Model Context Protocol)

ចាប់ផ្ដើម transport MCP ក្នុងរបៀប stdio៖

```bash
omniroute --mcp
```

លំហូរផ្ទៀងផ្ទាត់ដែលបានណែនាំ៖

```bash
# 1. ចាប់ផ្ដើមម៉ាស៊ីនមេ MCP
omniroute --mcp

# 2. ពីម៉ាស៊ីនភ្ញៀវ MCP របស់អ្នក សូមហៅ៖
omniroute_get_health        # គួរតែត្រឡប់ស្ថានភាពសុខភាពប្រព័ន្ធ
omniroute_list_combos       # គួរតែត្រឡប់ combos ដែលសកម្ម

# 3. ឬដំណើរការឈុត E2E ពេញលេញ៖
npm run test:protocols:e2e
```

#### ការកំណត់រចនាសម្ព័ន្ធម៉ាស៊ីនភ្ញៀវ MCP

**Claude Code:**

```bash
claude mcp add-server omniroute --type http --url http://localhost:20128/api/mcp/stream
```

**Cursor / Cline:**

បន្ថែមទៅក្នុងការកំណត់ MCP របស់អ្នក៖

```json
{
  "mcpServers": {
    "omniroute": {
      "command": "omniroute",
      "args": ["--mcp"],
      "env": {}
    }
  }
}
```

**ឯកសារ MCP ពេញលេញ៖** [README របស់ម៉ាស៊ីនមេ MCP](../../open-sse/mcp-server/README.md) — ឧបករណ៍ 110, ការកំណត់ IDE, ម៉ាស៊ីនភ្ញៀវ Python/TS/Go។

### ការរៀបចំ A2A (Agent-to-Agent Protocol)

ផ្ទៀងផ្ទាត់ Agent Card៖

```bash
curl http://localhost:20128/.well-known/agent.json
```

ផ្ញើភារកិច្ចមួយ៖

```bash
curl -X POST http://localhost:20128/a2a \
  -H 'content-type: application/json' \
  -d '{"jsonrpc":"2.0","id":"quickstart","method":"message/send","params":{"skill":"quota-management","messages":[{"role":"user","content":"Give me a short quota summary."}]}}'
```

**ឯកសារ A2A ពេញលេញ៖** [README របស់ម៉ាស៊ីនមេ A2A](../../src/lib/a2a/README.md) — JSON-RPC 2.0, ជំនាញ, ការស្ទ្រីម, វដ្តជីវិតភារកិច្ច។

---

## ការកំណត់រចនាសម្ព័ន្ធ Timeout

### Timeout មូលដ្ឋាន

សម្រាប់ការដាក់ឱ្យដំណើរការភាគច្រើន អ្នកត្រូវការតែអថេរទាំងពីរនេះប៉ុណ្ណោះ៖

| អថេរ                     | តម្លៃលំនាំដើម                   | គោលបំណង                                                                                                                                              |
| ------------------------ | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUEST_TIMEOUT_MS`     | `600000`                        | តម្លៃគោលរួមសម្រាប់ timeout នៃការចាប់ផ្តើមការឆ្លើយតបពី upstream, timeout លាក់របស់ Undici, សំណើ TLS fingerprint និង timeout សំណើ/proxy របស់ API bridge |
| `STREAM_IDLE_TIMEOUT_MS` | ទទួលមរតកពី `REQUEST_TIMEOUT_MS` | ចន្លោះពេលអតិបរមារវាង streaming chunks មុនពេល OmniRoute បោះបង់ SSE stream                                                                             |

ភាពឆបគ្នាថយក្រោយត្រូវបានរក្សាទុក៖ `FETCH_TIMEOUT_MS`, `API_BRIDGE_PROXY_TIMEOUT_MS` និងអថេរ timeout ផ្សេងទៀតសម្រាប់ស្រទាប់នីមួយៗដែលមានស្រាប់នៅតែដំណើរការ ហើយអាចកំណត់ជំនួសតម្លៃគោលរួមបាន។

### កំណត់សម្គាល់ជាក់លាក់តាមអ្នកផ្តល់សេវា

សម្រាប់ upstream ដែលឆបគ្នាជាមួយ Claude Code (`anthropic-compatible-cc-*`) OmniRoute កំណត់តម្លៃ header `X-Stainless-Timeout` សម្រាប់សំណើចេញ ដោយផ្អែកលើ fetch timeout ដែលបានដោះស្រាយ ដើម្បីឱ្យ read timeout នៅផ្នែកអ្នកផ្តល់សេវានៅស្របនឹងការកំណត់រចនាសម្ព័ន្ធ env របស់អ្នក។

សម្រាប់ reverse proxy ភាគីទីបីដែលឆបគ្នាជាមួយ Claude Code OmniRoute រក្សាសំណុំ `anthropic-beta` លំនាំដើមឱ្យមានលក្ខណៈប្រុងប្រយ័ត្ន ហើយនៅពេល `Client Cache Control` ត្រូវបានទុកជា `Auto` វាបញ្ជូនបន្តតែ marker `cache_control` ដែលផ្តល់ដោយ client ប៉ុណ្ណោះ។ បើកប៊ូតុងបិទបើក "Enable redact-thinking beta" សម្រាប់ការតភ្ជាប់នីមួយៗ តែនៅពេល upstream តម្រូវឱ្យមាន Claude thinking stream ដែលត្រូវបានលាក់ជាក់លាក់ប៉ុណ្ណោះ។

### ការកំណត់ជំនួស Timeout កម្រិតខ្ពស់

| អថេរ                                     | តម្លៃលំនាំដើម                              | គោលបំណង                                                                            |
| ---------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------- |
| `FETCH_TIMEOUT_MS`                       | ទទួលមរតកពី `REQUEST_TIMEOUT_MS`            | Timeout នៃការចាប់ផ្តើមការឆ្លើយតបពី upstream ដែលប្រើរហូតដល់ទទួលបាន response headers |
| `FETCH_HEADERS_TIMEOUT_MS`               | ទទួលមរតកពី `FETCH_TIMEOUT_MS`              | រយៈពេលកំណត់របស់ Undici សម្រាប់ទទួល response headers ពី upstream                    |
| `FETCH_BODY_TIMEOUT_MS`                  | ទទួលមរតកពី `FETCH_TIMEOUT_MS`              | រយៈពេលកំណត់របស់ Undici រវាង body chunks ពី upstream (`0` បិទវា)                    |
| `FETCH_CONNECT_TIMEOUT_MS`               | `30000`                                    | Timeout សម្រាប់ការតភ្ជាប់ TCP របស់ Undici                                          |
| `FETCH_KEEPALIVE_TIMEOUT_MS`             | `4000`                                     | Timeout សម្រាប់ idle keep-alive socket របស់ Undici                                 |
| `TLS_CLIENT_TIMEOUT_MS`                  | ទទួលមរតកពី `FETCH_TIMEOUT_MS`              | Timeout សម្រាប់សំណើ TLS fingerprint ដែលធ្វើឡើងតាមរយៈ `wreq-js`                     |
| `API_BRIDGE_PROXY_TIMEOUT_MS`            | ទទួលមរតកពី `REQUEST_TIMEOUT_MS` ឬ `600000` | Timeout សម្រាប់ការបញ្ជូនបន្ត proxy `/v1` ពី API port ទៅ dashboard port             |
| `API_BRIDGE_SERVER_REQUEST_TIMEOUT_MS`   | `max(API_BRIDGE_PROXY_TIMEOUT_MS, 300000)` | Timeout នៃសំណើចូលលើ API bridge server                                              |
| `API_BRIDGE_SERVER_HEADERS_TIMEOUT_MS`   | `60000`                                    | Timeout នៃ header ចូលលើ API bridge server                                          |
| `API_BRIDGE_SERVER_KEEPALIVE_TIMEOUT_MS` | `5000`                                     | Keep-alive timeout លើ API bridge server                                            |
| `API_BRIDGE_SERVER_SOCKET_TIMEOUT_MS`    | `0`                                        | Timeout នៃភាពអសកម្មរបស់ socket លើ API bridge server (`0` បិទវា)                    |

> **ចំណាំ៖** សម្រាប់ streaming requests, `FETCH_TIMEOUT_MS` គ្របដណ្តប់តែការរៀបចំការតភ្ជាប់ / ការរង់ចាំការឆ្លើយតបដំបូងពី upstream ប៉ុណ្ណោះ។ នៅពេល stream សកម្ម OmniRoute នឹងបោះបង់តែក្នុងករណីមានការជាប់គាំងពិតប្រាកដ (`STREAM_IDLE_TIMEOUT_MS`) ឬភាពអសកម្មរបស់ body ក្នុង Undici (`FETCH_BODY_TIMEOUT_MS`)។

### ភាពឆបគ្នាជាមួយ Reverse Proxy

ប្រសិនបើអ្នកដំណើរការ OmniRoute នៅខាងក្រោយ Nginx, Caddy, Cloudflare ឬ reverse proxy ផ្សេងទៀត សូមប្រាកដថា timeout របស់ proxy ក៏ខ្ពស់ជាង stream/fetch timeout របស់ OmniRoute ផងដែរ។

---

## របៀបបំបែកច្រក

ដំណើរការ API និង Dashboard នៅលើច្រកផ្សេងគ្នាសម្រាប់សេណារីយ៉ូកម្រិតខ្ពស់ (ប្រូកស៊ីបញ្ច្រាស បណ្ដាញកុងតឺន័រ)៖

```bash
PORT=20128 DASHBOARD_PORT=20129 omniroute
# API៖       http://localhost:20128/v1
# Dashboard៖ http://localhost:20129
```

---

## ពុម្ពសម្រាប់ Void Linux (xbps-src)

សម្រាប់អ្នកប្រើ Void Linux អ្នកអាចបង្កើតកញ្ចប់ដើមបានដោយប្រើ `xbps-src`។ រក្សាទុកប្លុកនេះជា `srcpkgs/omniroute/template`៖

```bash
# ឯកសារពុម្ពសម្រាប់ 'omniroute'
pkgname=omniroute
version=3.8.0
revision=1
hostmakedepends="nodejs python3 make"
depends="openssl"
short_desc="ច្រកផ្លូវ AI សកលដែលមានការបញ្ជូនផ្លូវឆ្លាតវៃសម្រាប់អ្នកផ្ដល់សេវា LLM ជាច្រើន"
maintainer="zenobit <zenobit@disroot.org>"
license="MIT"
homepage="https://github.com/diegosouzapw/OmniRoute"
distfiles="https://github.com/diegosouzapw/OmniRoute/archive/refs/tags/v${version}.tar.gz"
# បង្កើត checksum ឡើងវិញសម្រាប់ការចេញផ្សាយនីមួយៗដោយប្រើ៖
#   curl -L -o /tmp/omniroute.tar.gz "https://github.com/diegosouzapw/OmniRoute/archive/refs/tags/v${version}.tar.gz" && sha256sum /tmp/omniroute.tar.gz
checksum=PLACEHOLDER_REGENERATE_PER_RELEASE
system_accounts="_omniroute"
omniroute_homedir="/var/lib/omniroute"
export NODE_ENV=production
export npm_config_engine_strict=false
export npm_config_loglevel=error
export npm_config_fund=false
export npm_config_audit=false

do_build() {
	local _gyp_arch
	case "$XBPS_TARGET_MACHINE" in
		aarch64*) _gyp_arch=arm64 ;;
		armv7*|armv6*) _gyp_arch=arm ;;
		i686*) _gyp_arch=ia32 ;;
		*) _gyp_arch=x64 ;;
	esac

	NODE_ENV=development npm ci --ignore-scripts
	npm run build
	cp -r .next/static .next/standalone/.next/static
	[ -d public ] && cp -r public .next/standalone/public || true

	local _node_gyp=/usr/lib/node_modules/npm/node_modules/node-gyp/bin/node-gyp.js
	(cd node_modules/better-sqlite3 && node "$_node_gyp" rebuild --arch="$_gyp_arch")

	local _bs3_release=.next/standalone/node_modules/better-sqlite3/build/Release
	mkdir -p "$_bs3_release"
	cp node_modules/better-sqlite3/build/Release/better_sqlite3.node "$_bs3_release/"

	rm -rf .next/standalone/node_modules/@img

	for _mod in pino-abstract-transport split2 process-warning; do
		cp -r "node_modules/$_mod" .next/standalone/node_modules/
	done
}

do_check() {
	npm run test:unit
}

do_install() {
	vmkdir usr/lib/omniroute/.next
	vcopy .next/standalone/. usr/lib/omniroute/.next/standalone

	for _d in \
		.next/standalone/.next/server/app/dashboard \
		.next/standalone/.next/server/app/dashboard/settings \
		.next/standalone/.next/server/app/dashboard/providers; do
		touch "${DESTDIR}/usr/lib/omniroute/${_d}/.keep"
	done

	cat > "${WRKDIR}/omniroute" <<'EOF'
#!/bin/sh
export PORT="${PORT:-20128}"
export DATA_DIR="${DATA_DIR:-${XDG_DATA_HOME:-${HOME}/.local/share}/omniroute}"
export APP_LOG_TO_FILE="${APP_LOG_TO_FILE:-false}"
mkdir -p "${DATA_DIR}"
exec node /usr/lib/omniroute/.next/standalone/server.js "$@"
EOF
	vbin "${WRKDIR}/omniroute"
}

post_install() {
	vlicense LICENSE
}
```

---

## ការលុបការដំឡើង

| ពាក្យបញ្ជា               | សកម្មភាព                                                                                                 |
| ------------------------ | -------------------------------------------------------------------------------------------------------- |
| `npm run uninstall`      | លុបកម្មវិធីចេញពីប្រព័ន្ធ ប៉ុន្តែ **រក្សាទុក DB និងការកំណត់រចនាសម្ព័ន្ធរបស់អ្នក** នៅក្នុង `~/.omniroute`។ |
| `npm run uninstall:full` | លុបកម្មវិធី ហើយ **លុបជាអចិន្ត្រៃយ៍នូវការកំណត់រចនាសម្ព័ន្ធ សោ និងមូលដ្ឋានទិន្នន័យទាំងអស់**។               |

> សម្រាប់សេចក្ដីណែនាំលម្អិតអំពីការលុបការដំឡើងតាមវិធីសាស្ត្រទាំងអស់ សូមមើល [UNINSTALL.md](./UNINSTALL.md)។
