# Plugin Marketplace (ខ្មែរ)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇹 [am](../../../am/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇿 [az](../../../az/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇰 [da](../../../da/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇪 [de](../../../de/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇷 [el](../../../el/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇸 [es](../../../es/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇪 [et](../../../et/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇱 [he](../../../he/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇩 [id](../../../id/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇹 [it](../../../it/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇲 [my](../../../my/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇴 [no](../../../no/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [or](../../../or/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇰 [si](../../../si/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [te](../../../te/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇭 [th](../../../th/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/PLUGIN_MARKETPLACE.md)

---

> **ប្រភពសេចក្តីពិត:** `src/lib/plugins/` (`marketplace.ts`, `manager.ts`, `manifest.ts`,
> `scanner.ts`, `loader.ts`), `src/app/api/plugins/`, និង
> `src/app/(dashboard)/dashboard/plugins/`
> **បានធ្វើបច្ចុប្បន្នភាពចុងក្រោយ:** 2026-06-28 — v3.8.40

OmniRoute ភ្ជាប់មកជាមួយប្រព័ន្ធកម្មវិធីជំនួយបែប WordPress។ កម្មវិធីជំនួយគឺជាថតដាច់ដោយឡែក
ដែលថតនីមួយៗមាន manifest `plugin.json` និងឯកសារចំណុចចូលមួយ ដើម្បីតភ្ជាប់
ទៅក្នុងលំហូរដំណើរការសំណើ (`onRequest` / `onResponse` / `onError`) និងទៅក្នុង
ព្រឹត្តិការណ៍វដ្តជីវិត (`onInstall` / `onActivate` / `onDeactivate` / `onUninstall`)។

**ទីផ្សារកម្មវិធីជំនួយ** គឺជាស្រទាប់ស្វែងរកនៅពីលើប្រព័ន្ធនោះ។ វា
ផ្តល់កាតាឡុកដែលអាចរុករកបាននៃកម្មវិធីជំនួយដែលអាចដំឡើងបាន។ តាមលំនាំដើម កាតាឡុកនេះគឺជា
បញ្ជីចុះឈ្មោះគ្រាប់ពូជដែលភ្ជាប់មកស្រាប់ និងមានទំហំតូច។ ប្រតិបត្តិករអាចកំណត់ឱ្យវាប្រើ URL នៃ
បញ្ជីចុះឈ្មោះពីចម្ងាយផ្ទាល់ខ្លួន ដែលក្នុងករណីនេះ ការទាញយកត្រូវបានពង្រឹងសុវត្ថិភាពដោយយន្តការការពារ SSRF
ដែលដោះស្រាយ DNS (សូមមើល [សុវត្ថិភាព](#security))។

គ្រប់ route របស់កម្មវិធីជំនួយគឺ **សម្រាប់តែ loopback ប៉ុណ្ណោះ** (កម្រិត 1 — `LOCAL_ONLY`)៖ កម្មវិធីជំនួយផ្ទុក
និងប្រតិបត្តិកូដក្នុង child process ដូច្នេះ route ទាំងនេះមិនអាចចូលប្រើបានពី
ប្រភពដែលមិនមែនជា loopback ទេ ដោយមិនគិតពីការផ្ទៀងផ្ទាត់អត្តសញ្ញាណ។ សូមមើល
[`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md)។

## របៀបដែលផ្នែកទាំងអស់ដំណើរការរួមគ្នា

```
ផ្ទាំងគ្រប់គ្រង (/dashboard/plugins)
  ├─ ផ្ទាំង "បានដំឡើង"  → GET /api/plugins            (listPlugins)
  │                       POST /api/plugins/scan      (pluginManager.scan)
  │                       POST /api/plugins/{name}/activate|deactivate
  │                       DELETE /api/plugins/{name}   (លុបការដំឡើង)
  └─ ផ្ទាំង "ទីផ្សារ" → GET /api/plugins/marketplace
                          → listMarketplacePlugins()
                            ├─ គ្មាន URL ផ្ទាល់ខ្លួន → SEED_REGISTRY ដែលភ្ជាប់មកស្រាប់
                            └─ URL ផ្ទាល់ខ្លួន → យន្តការការពារ SSRF isSafeMarketplaceUrl()
                                             → safeOutboundFetch(guard:"public-only")
```

- **ស្រទាប់បញ្ជីចុះឈ្មោះ** — `src/lib/plugins/marketplace.ts`៖ រាយបញ្ជី / ស្វែងរកក្នុង
  កាតាឡុក និងត្រឡប់ទៅប្រើបញ្ជីចុះឈ្មោះគ្រាប់ពូជវិញនៅពេលមានការបរាជ័យណាមួយ។
- **ស្រទាប់វដ្តជីវិត** — `src/lib/plugins/manager.ts` (`pluginManager` singleton)៖
  ដំឡើង ដំឡើងកំណែ ធ្វើឱ្យសកម្ម ធ្វើឱ្យអសកម្ម លុបការដំឡើង ស្កេន និងផ្ទុកនៅពេលចាប់ផ្តើម។
- **ស្រទាប់ manifest** — `src/lib/plugins/manifest.ts`៖ Zod schema + តម្លៃលំនាំដើមសម្រាប់
  `plugin.json`។
- **កម្មវិធីស្កេន** — `src/lib/plugins/scanner.ts`៖ រកឃើញកម្មវិធីជំនួយនៅលើថាសក្រោម
  ថតកម្មវិធីជំនួយ។
- **កម្មវិធីផ្ទុក** — `src/lib/plugins/loader.ts`៖ ចាប់ផ្តើមកម្មវិធីជំនួយនីមួយៗក្នុង
  child process ដាច់ដោយឡែក និងសម្របសម្រួលការហៅ hook តាម IPC។

## កាតាឡុកទីផ្សារ

`listMarketplacePlugins()` (`src/lib/plugins/marketplace.ts`) ត្រឡប់បញ្ជីនៃ
វត្ថុ `MarketplaceEntry`៖

| វាល           | ប្រភេទ   | កំណត់សម្គាល់                                   |
| ------------- | -------- | ---------------------------------------------- |
| `name`        | string   | ឈ្មោះកម្មវិធីជំនួយជាទម្រង់ kebab-case          |
| `version`     | string   | semver                                         |
| `description` | string   | សេចក្តីសង្ខេបខ្លី                              |
| `author`      | string   | អ្នកនិពន្ធ / អង្គការ                           |
| `license`     | string   | លេខសម្គាល់អាជ្ញាបណ្ណបែប SPDX                   |
| `downloadUrl` | string   | URL ទាញយកប្រភព (អាចទទេ)                        |
| `repository`  | string?  | URL ឃ្លាំងស្រេចចិត្ត                           |
| `tags`        | string[] | ស្លាកសម្រាប់ស្វែងរក/ត្រង                       |
| `downloads`   | number   | ចំនួនទាញយក                                     |
| `rating`      | number   | 0–5                                            |
| `verified`    | boolean  | ថាតើធាតុនេះត្រូវបានសម្គាល់ថាបានផ្ទៀងផ្ទាត់ឬអត់ |
| `lastUpdated` | string   | ខ្សែអក្សរកាលបរិច្ឆេទស្រដៀង ISO                 |

នៅពេលមិនបានកំណត់ URL បញ្ជីចុះឈ្មោះផ្ទាល់ខ្លួន កាតាឡុកគឺជា
`SEED_REGISTRY` ដែលភ្ជាប់មកស្រាប់ (បច្ចុប្បន្នមាន `request-logger`, `rate-limiter`, `cost-tracker`, និង
`theme-manager`)។ បញ្ជីចុះឈ្មោះគ្រាប់ពូជតែងតែមានជានិច្ច — ប្រសិនបើបញ្ជីចុះឈ្មោះពីចម្ងាយដែលបានកំណត់
មិនអាចចូលដំណើរការបាន ត្រឡប់ស្ថានភាពដែលមិនមែនជា `200` ឬត្រឡប់ body ដែលមិនស្គាល់
`listMarketplacePlugins()` នឹងកត់ត្រាការព្រមាន ហើយត្រឡប់ទៅប្រើបញ្ជីគ្រាប់ពូជវិញ។

> ចំណាំ៖ **កាតាឡុក** ទីផ្សារ (រុករក/ស្វែងរក) ត្រូវបានតភ្ជាប់ពេញលេញពីដើមដល់ចប់ ប៉ុន្តែ
> ការ **ដំឡើង** ពីទីផ្សារដោយចុចតែម្តងពីកាតាឡុកមិនទាន់ត្រូវបានអនុវត្តនៅឡើយទេ —
> ប៊ូតុង "ដំឡើង" របស់ផ្ទាំងគ្រប់គ្រងនៅលើធាតុទីផ្សារ បច្ចុប្បន្នបង្ហាញការជូនដំណឹងថា
> "នឹងមកដល់ឆាប់ៗនេះ"។ ការដំឡើងនាពេលបច្ចុប្បន្នធ្វើឡើងតាមរយៈលំហូរដំឡើងពី path មូលដ្ឋាន
> (`POST /api/plugins`) និងការរកឃើញនៅលើថាស (`POST /api/plugins/scan`)។

## REST API

ចំណុចបញ្ចប់ទាំងអស់ទាមទារការផ្ទៀងផ្ទាត់សិទ្ធិគ្រប់គ្រង (`requireManagementAuth`) **ហើយ**
អាចចូលប្រើបានតែតាម loopback ប៉ុណ្ណោះ — `/api/plugins` និង `/api/plugins/` ត្រូវបានរាយក្នុង
`LOCAL_ONLY_API_PREFIXES` (`src/server/authz/routeGuard.ts`)។

| ចំណុចបញ្ចប់                      | វិធីសាស្ត្រ | ការពិពណ៌នា                                                       |
| -------------------------------- | ----------- | ---------------------------------------------------------------- |
| `/api/plugins`                   | GET         | រាយបញ្ជី plugin ដែលបានដំឡើង (តម្រង `?status=` ជាជម្រើស)          |
| `/api/plugins`                   | POST        | ដំឡើង plugin ពី path មូលដ្ឋានដាច់ខាត                             |
| `/api/plugins/scan`              | POST        | ស្កេនថត plugin ហើយចុះបញ្ជី plugin ថ្មីៗ                          |
| `/api/plugins/marketplace`       | GET         | រាយធាតុនៅក្នុងកាតាឡុក marketplace                                |
| `/api/plugins/[name]`            | GET         | ទទួលព័ត៌មានលម្អិតអំពី plugin ដែលបានដំឡើង                         |
| `/api/plugins/[name]`            | DELETE      | លុបការដំឡើង plugin                                               |
| `/api/plugins/[name]/activate`   | POST        | ធ្វើឱ្យសកម្ម (ផ្ទុក + ចុះបញ្ជី hooks)                            |
| `/api/plugins/[name]/deactivate` | POST        | បិទដំណើរការ (ដំណើរការ `onDeactivate`, លុបការចុះបញ្ជី hooks)      |
| `/api/plugins/[name]/config`     | GET         | ទទួល config របស់ plugin + config schema                          |
| `/api/plugins/[name]/config`     | PUT         | ធ្វើបច្ចុប្បន្នភាព config របស់ plugin (បានផ្ទៀងផ្ទាត់តាម schema) |

តម្រង `status` របស់ `GET /api/plugins` ទទួលយកតម្លៃមួយក្នុងចំណោម
`installed` / `active` / `inactive` / `error`។ តម្លៃមិនត្រឹមត្រូវនឹងត្រឡប់ `400`។

### រាយបញ្ជី plugin ដែលបានដំឡើង

```bash
curl http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..."
```

### ដំឡើងពី path មូលដ្ឋាន

```bash
curl -X POST http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "path": "/absolute/path/to/my-plugin" }'
```

`path` ត្រូវតែជា path **ដាច់ខាត** ហើយមិនអាចមាន segment ឆ្លងកាត់ `..` ឬ
null bytes ទេ (អនុវត្តដោយ Zod)។ ថតប្រភពត្រូវតែមាន
`plugin.json` ត្រឹមត្រូវ (ឬជាថតមេរបស់ថតដែលមានវា)។ នៅពេលជោគជ័យ response គឺ `201` ជាមួយ
ជួរទិន្នន័យរបស់ plugin ដែលបានដំឡើង។

### រុករក marketplace

```bash
curl http://localhost:20128/api/plugins/marketplace \
  -H "Cookie: auth_token=..."
```

### ធ្វើបច្ចុប្បន្នភាព config របស់ plugin

```bash
curl -X PUT http://localhost:20128/api/plugins/my-plugin/config \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "config": { "level": "debug", "maxItems": 100 } }'
```

`PUT .../config` ផ្ទៀងផ្ទាត់តម្លៃនីមួយៗដែលបានផ្តល់ដោយផ្អែកលើ
`configSchema` របស់ plugin (បានប្រកាសក្នុង manifest)៖ field ប្រភេទ `number` គោរពតាម `min`/`max`
ហើយ field ប្រភេទ `select` ត្រូវតែត្រូវគ្នានឹង `enum` ដែលបានប្រកាស។ Key ដែលមិនមានក្នុង schema
ត្រូវបានអនុញ្ញាតឱ្យឆ្លងកាត់។

## ការកំណត់រចនាសម្ព័ន្ធ

### ថត plugin

Plugin ស្ថិតនៅក្រោមថតទិន្នន័យ OmniRoute៖

```
~/.omniroute/plugins/<plugin-name>/
  ├─ plugin.json
  └─ index.js          # (ឬអ្វីក៏ដោយដែល manifest.main ចង្អុលទៅ)
```

`getDefaultPluginDir()` (`src/lib/plugins/scanner.ts`) កំណត់ថតនោះតាម
បីជំហាន៖

1. **`OMNIROUTE_PLUGINS_DIR`** នៅពេលបានកំណត់ — ប្រើដូចដែលបានផ្តល់ឱ្យ ដោយមិនគិតថា `HOME` បញ្ជាក់អ្វីឡើយ។ នេះជា
   ជម្រើសកំណត់ជាក់លាក់សម្រាប់ Docker/K8s ដែល plugin tree ត្រូវបាន bind-mount នៅ path មួយ
   ដែលជាធម្មតាមិនពាក់ព័ន្ធនឹងថត home របស់ container ទេ (#11827)។
2. `<home>/.omniroute/plugins` ដែល `<home>` បានមកពី environment variable `HOME` / `USERPROFILE`។
3. `/tmp/.omniroute/plugins` នៅពេល process មិនបាន export home ទាល់តែសោះ។

ថតដែលបានកំណត់ត្រូវបាន log ម្តងនៅពេលចាប់ផ្តើមជា `scanner.dir_resolved` ដោយបញ្ជាក់
input ដែលត្រូវបានជ្រើស (`OMNIROUTE_PLUGINS_DIR`, `home`, ឬ `no-home-fallback`) — ដូច្នេះ image
ដែលស្ងាត់ៗធ្លាក់ទៅជំហានទី 3 នឹងបង្ហាញដូច្នោះ ជំនួសឱ្យការរាយការណ៍តែបញ្ជី plugin ទទេ។
`POST /api/plugins/scan` ស្វែងរក subdirectory ណាមួយនៅទីនោះដែលមាន
`plugin.json` ត្រឹមត្រូវ ហើយចុះបញ្ជីវា; ថតដូចគ្នានេះគឺជា root ដែល
`pluginManager.install()` ចម្លង plugin ចូល ដូច្នេះ override នឹងផ្លាស់ទីទាំងការស្វែងរក និង
ការដំឡើងជាមួយគ្នា។

> **`OMNIROUTE_PLUGINS_DIR` មិនមែនជា `OMNIROUTE_PLUGIN_PATH` ទេ។** តម្លៃចុងក្រោយនេះត្រូវបានអានតែដោយ
> កម្មវិធីផ្ទុក command-plugin របស់ CLI (`bin/cli/plugins.mjs`) ដើម្បីស្វែងរក package npm ឈ្មោះ `omniroute-cmd-*`
> ដែលបន្ថែម subcommand របស់ `omniroute` — វាមិនមានឥទ្ធិពលលើ runtime scanner
> ដែលបានពិពណ៌នានៅទីនេះទេ។ សូមមើល [PLUGINS.md](./PLUGINS.md) សម្រាប់ផ្នែកនោះ។

### URL របស់ registry marketplace ផ្ទាល់ខ្លួន

ប្រភពកាតាឡុក marketplace ត្រូវបានអានពី setting `pluginMarketplaceUrl`
(`src/lib/plugins/marketplace.ts` អាន `settings.pluginMarketplaceUrl`)។ នៅពេល
កំណត់ជា URL `http(s)` នោះ `listMarketplacePlugins()` នឹង fetch URL នោះ ហើយទទួលយក
ទាំង JSON array កម្រិតកំពូលនៃធាតុ ឬ object មួយដែលមាន array `plugins`;
ធាតុដែលគ្មាន `name` ប្រភេទ string នឹងត្រូវបានត្រងចេញ។ នៅពេលមិនបានកំណត់ (ឬនៅពេល fetch
បរាជ័យដោយសារ SSRF guard / ត្រឡប់ response មិនត្រឹមត្រូវ) seed registry ដែលមានស្រាប់នឹងត្រូវបាន
ប្រើ។

ផ្ទាំង "Marketplace" ក្នុង dashboard បង្ហាញ field សម្រាប់ URL នេះ (អានត្រឡប់មកវិញពី
`GET /api/settings`)។

> កំណត់សម្គាល់អំពីការអនុវត្ត៖ សកម្មភាព "Save" ក្នុង dashboard ផ្ញើ
> `pluginMarketplaceUrl` ទៅ `PATCH /api/settings`។ នៅពេលសរសេរឯកសារនេះ
> key នេះមិនត្រូវបានប្រកាសក្នុង `updateSettingsSchema`
> (`src/shared/validation/settingsSchemas.ts`) ទេ ដូច្នេះសូមផ្ទៀងផ្ទាត់ការរក្សាទុកក្នុង
> release របស់អ្នក មុនពេលពឹងផ្អែកលើវា — ផ្លូវ **អាន** (`getSettings()` →
> `listMarketplacePlugins()`) គោរពតាម key នេះ នៅពេលវាមានវត្តមានក្នុង settings
> store។

## សុវត្ថិភាព

### កម្រិត Route — សម្រាប់តែ loopback ប៉ុណ្ណោះ

Plugin ប្រតិបត្តិកូដក្នុង child process ដែលបាន spawn ដូច្នេះផ្ទៃ `/api/plugins`
ទាំងមូលត្រូវបានចាត់ថ្នាក់ជា `LOCAL_ONLY` (កម្រិត 1)។ ការអនុវត្ត loopback ដំណើរការ
ដោយគ្មានលក្ខខណ្ឌ **មុន** ការត្រួតពិនិត្យការផ្ទៀងផ្ទាត់អត្តសញ្ញាណណាមួយ ដូច្នេះ management token ដែលបានលេចធ្លាយ ហើយចូលដល់
ម៉ាស៊ីនតាម tunnel នៅតែមិនអាចដំឡើង ដំណើរការ ឬលុប plugin បានទេ។
សូមមើល [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) និង
វិធានតឹងរ៉ឹង #15 / #17។

### របាំងការពារ SSRF សម្រាប់ registry របស់ Marketplace

URL របស់ registry ផ្ទាល់ខ្លួនគឺជាការកំណត់រចនាសម្ព័ន្ធដែលអ្នកវាយប្រហារអាចមានឥទ្ធិពលលើបាន ដូច្នេះមុនពេល
ទាញយកវា `listMarketplacePlugins()` ដំណើរការវាឆ្លងកាត់ការការពារពីរជាន់៖

1. **`isSafeMarketplaceUrl(url)`** (`src/lib/plugins/marketplace.ts`):
   - បដិសេធអ្វីៗដែលមិនមែនជា `http:` / `https:`។
   - បដិសេធ host ឯកជន/loopback/link-local/ULA ដែលបានបញ្ជាក់ដោយផ្ទាល់ (ទាំង IPv4 **និង** IPv6
     រួមទាំង IPv4-mapped) តាមរយៈ `isPrivateHost` ស្តង់ដារ
     (`src/shared/network/outboundUrlGuard.ts`)។
   - ដោះស្រាយ record **ទាំង** `A` និង `AAAA` ហើយបដិសេធ ប្រសិនបើអាសយដ្ឋាន
     ដែលបានដោះស្រាយ **ណាមួយ** ជាអាសយដ្ឋានឯកជន — ដើម្បីបិទចន្លោះឆ្លងកាត់ពី hostname សាធារណៈ → IP ឯកជន។
   - **បរាជ័យដោយបិទជានិច្ច**៖ ការបរាជ័យក្នុងការដោះស្រាយ DNS នឹងធ្វើឱ្យ URL ត្រូវបានបដិសេធ។
2. **`safeOutboundFetch(url, { guard: "public-only", timeoutMs: 5000 })`**
   (`src/shared/network/safeOutboundFetch.ts`)៖ អនុវត្តរបាំង URL សម្រាប់តែសាធារណៈឡើងវិញ
   នៅពេល fetch និង **ទប់ស្កាត់ redirect** (មិនអនុញ្ញាត pivot `30x`
   ពីសាធារណៈ → ឯកជន)។

URL ដែលបរាជ័យនៅស្រទាប់ណាមួយ មិនធ្វើឱ្យ request ត្រូវបានបញ្ឈប់ទេ — marketplace
នឹងត្រឡប់ទៅប្រើ seed registry ដែលមានស្រាប់ដោយស្ងាត់ៗ ហើយកត់ត្រាការព្រមាន។

> របាំងនេះត្រូវបានពង្រឹងនៅក្នុង PR #3774 ជាពិសេស ដើម្បីដោះស្រាយ A + AAAA និងប្រើ
> `isPrivateHost` ស្តង់ដារ ជំនួសឱ្យការត្រួតពិនិត្យតែ IPv4។

### ការញែកដាច់ដោយឡែកនៃការប្រតិបត្តិ Plugin

- **ការញែក process ដាច់ដោយឡែក** — `loadPlugin()` (`src/lib/plugins/loader.ts`) spawn
  plugin នីមួយៗក្នុង child process របស់ Node.js ដាច់ដោយឡែក ហើយទាក់ទងគ្នាតាម IPC។
  ការហៅ hook មាន timeout ជាមួយនឹងការបង្កើនកម្រិតពី `SIGTERM` → `SIGKILL`។
- **បញ្ជីអនុញ្ញាត Env** — child ទទួលបានតែសំណុំ environment variable
  ដែលស្ថិតក្នុងបញ្ជីអនុញ្ញាតប៉ុណ្ណោះ។ សំណុំដែលទូលំទូលាយជាងនេះត្រូវបានផ្តល់ជូន តែនៅពេល manifest ស្នើសុំ
  permission `env`។
- **ការកំណត់ path ឱ្យស្ថិតក្នុងដែន** — ប្រតិបត្តិការ install/upgrade/uninstall ផ្ទៀងផ្ទាត់ថា directory របស់ plugin
  និង `manifest.main` ត្រូវបានដោះស្រាយ **នៅខាងក្នុង** root របស់ plugin ដែលបានគ្រប់គ្រង
  មុនពេល copy ឬ recursive delete ណាមួយ (ការពារប្រឆាំងនឹង path របស់ DB ដែលត្រូវបានកែប្រែដោយខុសច្បាប់ និង
  traversal `../` នៅក្នុង `manifest.main`)។ ការដំណើរការ plugin ដោះស្រាយ symlink តាមរយៈ
  `realpath` ហើយបដិសេធមិនផ្ទុក entry point ដែលគេចចេញពី directory របស់ plugin។
- **ការខ្ទាស់ integrity ជាជម្រើស** — manifest អាចប្រកាស field `integrity`
  (`sha256-<base64>` ទម្រង់ SRI)។ នៅពេលមានវត្តមាន loader នឹងផ្ទៀងផ្ទាត់ hash របស់
  entry file នៅពេលផ្ទុក ហើយបដិសេធមិនដំណើរការ ប្រសិនបើមិនត្រូវគ្នា។ វាជាការរកឃើញការកែប្រែដោយខុសច្បាប់ដែលអាចជ្រើសរើសប្រើបាន
  **មិនមែន** ជាព្រំដែនសុវត្ថិភាពទេ — routing សម្រាប់តែ loopback
  និងគំរូ permission គឺជាព្រំដែនពិតប្រាកដ។

## Manifest (`plugin.json`)

ត្រូវបានផ្ទៀងផ្ទាត់ដោយ `PluginManifestSchema` (`src/lib/plugins/manifest.ts`)៖

| Field              | Type      | កំណត់សម្គាល់                                                          |
| ------------------ | --------- | --------------------------------------------------------------------- |
| `name`             | string    | ទាមទារ; kebab-case (`^[a-z0-9-]+$`), 1–100 តួអក្សរ                    |
| `version`          | string    | ទាមទារ; semver (`MAJOR.MINOR.PATCH`)                                  |
| `description`      | string?   | ≤ 500 តួអក្សរ                                                         |
| `author`           | string?   | ≤ 200 តួអក្សរ                                                         |
| `license`          | string?   | លំនាំដើមជា `MIT`                                                      |
| `main`             | string?   | entry file; លំនាំដើមជា `index.js`                                     |
| `source`           | enum?     | `local` \| `marketplace` (លំនាំដើមជា `local`)                         |
| `tags`             | string[]? | tag សម្រាប់ស្វែងរក                                                    |
| `requires`         | object?   | `{ omniroute?, permissions[] }`                                       |
| `hooks`            | object?   | តម្លៃ Boolean ដែលប្រកាសថា plugin អនុវត្ត hook ណាខ្លះ                  |
| `skills`           | object[]? | និយមន័យ skill ជាជម្រើស                                                |
| `enabledByDefault` | boolean?  | ដំណើរការដោយស្វ័យប្រវត្តិនៅពេលដំឡើង                                    |
| `configSchema`     | object?   | Map នៃ field កំណត់រចនាសម្ព័ន្ធ (`string`/`number`/`boolean`/`select`) |
| `integrity`        | string?   | ការខ្ទាស់ entry-file `sha256-<base64>` ជាជម្រើស                       |

Permission ត្រូវបានយកពី enum
`network` / `file-read` / `file-write` / `env` / `exec`។

## លំហូរវដ្តជីវិត

```
install (POST /api/plugins, ផ្លូវ)
  → ស្កេន/ផ្ទៀងផ្ទាត់ manifest → ចម្លងទៅ staging → ផ្ទៀងផ្ទាត់ថា main ស្ថិតក្នុងថត
  → ប្តូរឈ្មោះជាលក្ខណៈ atomic ទៅជា ~/.omniroute/plugins/<name> → បញ្ចូលជួរទិន្នន័យក្នុង DB
  → ដំណើរការ onInstall → ប្រសិនបើ enabledByDefault: activate

activate (POST /api/plugins/{name}/activate)
  → ពិនិត្យ realpath containment → loadPlugin() (បង្កើត child process)
  → ចុះឈ្មោះ hooks ដែលបានប្រកាស → status = "active" → ដំណើរការ onActivate

deactivate (POST /api/plugins/{name}/deactivate)
  → ដំណើរការ onDeactivate (មុនពេលលុបការចុះឈ្មោះ) → លុបការចុះឈ្មោះ hooks
  → បញ្ឈប់ child process → status = "inactive"

uninstall (DELETE /api/plugins/{name})
  → deactivate ប្រសិនបើ active → ដំណើរការ onUninstall
  → លុបថត plugin ជាបន្តបន្ទាប់ដោយមានការពិនិត្យ containment → លុបជួរទិន្នន័យក្នុង DB
```

ការដំណើរការ `install` ម្តងទៀតលើថតដែលមានកំណែ manifest **ថ្មីជាងយ៉ាងដាច់ខាត**
ធៀបនឹងកំណែដែលបានដំឡើង នឹងធ្វើការដំឡើងកំណែដោយស្វ័យប្រវត្តិ (ដំឡើងឡើងវិញទាំងស្រុង; ការកំណត់រចនាសម្ព័ន្ធត្រូវបានកំណត់ឡើងវិញ
ទៅតម្លៃលំនាំដើម)។ កំណែដូចគ្នា ឬចាស់ជាង នឹងត្រូវបានបដិសេធ។

## មូលដ្ឋានទិន្នន័យ

តារាង `plugins` (migration `076_create_plugins.sql`):

| ជួរឈរ           | ប្រភេទ  | កំណត់សម្គាល់                                       |
| --------------- | ------- | -------------------------------------------------- |
| `id`            | TEXT PK | UUID                                               |
| `name`          | TEXT    | តែមួយគត់                                           |
| `version`       | TEXT    | semver; លំនាំដើម `1.0.0`                           |
| `description`   | TEXT    | ជាជម្រើស                                           |
| `author`        | TEXT    | ជាជម្រើស                                           |
| `license`       | TEXT    | លំនាំដើម `MIT`                                     |
| `main`          | TEXT    | ឯកសារចាប់ផ្តើម; លំនាំដើម `index.js`                |
| `source`        | TEXT    | លំនាំដើម `local`                                   |
| `tags`          | TEXT    | អារេ JSON; លំនាំដើម `[]`                           |
| `status`        | TEXT    | `installed` \| `active` \| `inactive` \| `error`   |
| `enabled`       | INT     | 0/1; លំនាំដើម 0                                    |
| `manifest`      | TEXT    | manifest JSON ពេញលេញ                               |
| `config`        | TEXT    | JSON; លំនាំដើម `{}`                                |
| `config_schema` | TEXT    | JSON; លំនាំដើម `{}`                                |
| `hooks`         | TEXT    | អារេ JSON នៃឈ្មោះ hook ដែលបានប្រកាស; លំនាំដើម `[]` |
| `permissions`   | TEXT    | អារេ JSON; លំនាំដើម `[]`                           |
| `plugin_dir`    | TEXT    | ថតដំឡើងដាច់ខាត                                     |
| `error_message` | TEXT    | កំណត់នៅពេល `status = "error"`                      |
| `installed_at`  | TEXT    | `datetime('now')`                                  |
| `updated_at`    | TEXT    | `datetime('now')`                                  |
| `activated_at`  | TEXT    | កំណត់នៅពេល activate                                |

ម៉ែត្រ/ការវិភាគរបស់ plugin ត្រូវបានតាមដានក្នុងតារាងបន្ថែម
(`090_plugin_metrics.sql`, `091_plugin_analytics.sql`)។

## ផ្ទាំងគ្រប់គ្រង

ទំព័រផ្ទាំងគ្រប់គ្រងនៅ `/dashboard/plugins`
(`src/app/(dashboard)/dashboard/plugins/page.tsx`) ផ្តល់ជូនផ្ទាំងពីរ៖

- **បានដំឡើង** — រាយបញ្ជី plugin ដែលបានដំឡើង ព្រមទាំង hooks ដែលបានប្រកាស ប៊ូតុងបិទ/បើក
  activate/deactivate ប៊ូតុង uninstall និងសកម្មភាព "ស្កេនរក plugins"
  (`POST /api/plugins/scan`)។
- **ទីផ្សារ** — បង្ហាញកាតាឡុកពី `GET /api/plugins/marketplace` ជាមួយនឹង
  វាលសម្រាប់កំណត់ registry URL ផ្ទាល់ខ្លួន។

ទំព័រកំណត់រចនាសម្ព័ន្ធសម្រាប់ plugin នីមួយៗស្ថិតនៅ `/dashboard/plugins/[name]/config`
(`src/app/(dashboard)/dashboard/plugins/[name]/config/page.tsx`)។

## សូមមើលផងដែរ

- [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) —
  មូលហេតុដែល `/api/plugins` អាចចូលប្រើបានតែតាម loopback ប៉ុណ្ណោះ (Tier 1)
- [`docs/frameworks/SKILLS.md`](./SKILLS.md) — ក្របខណ្ឌជំនាញដែលពាក់ព័ន្ធ
  (`src/lib/skills/`); កម្មវិធីបន្ថែមអាចប្រកាសជំនាញនៅក្នុង manifest របស់វា
- [`docs/frameworks/WEBHOOKS.md`](./WEBHOOKS.md) — ការរួមបញ្ចូលប្រព័ន្ធខាងក្រៅ
  ដែលដំណើរការដោយព្រឹត្តិការណ៍
- [`docs/security/ERROR_SANITIZATION.md`](../security/ERROR_SANITIZATION.md) —
  លំនាំ `buildErrorBody()` ដែល route របស់កម្មវិធីបន្ថែមនីមួយៗប្រើសម្រាប់ការឆ្លើយតបកំហុស
