# Usage, Quota & Spend Tracking (Српски)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/USAGE_QUOTA_GUIDE.md) · 🇪🇹 [am](../../../am/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇦🇿 [az](../../../az/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇩🇰 [da](../../../da/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇩🇪 [de](../../../de/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇬🇷 [el](../../../el/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇪🇸 [es](../../../es/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇪🇪 [et](../../../et/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇱 [he](../../../he/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇩 [id](../../../id/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇹 [it](../../../it/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇭 [km](../../../km/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇲🇲 [my](../../../my/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇴 [no](../../../no/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [or](../../../or/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇱🇰 [si](../../../si/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇮🇳 [te](../../../te/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇭 [th](../../../th/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/USAGE_QUOTA_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/USAGE_QUOTA_GUIDE.md)

---

> **УКРАТКО**: OmniRoute прати потрошњу токена за сваки захтев, израчунава трошкове, примењује квоте за сваки API кључ и приказује аналитику на контролној табли. Овај водич објашњава како све то функционише.

**Извори:**

- `open-sse/services/usage.ts` (~70KB) — главно праћење потрошње
- `src/lib/usageAnalytics.ts` (~10KB) — агрегација за контролну таблу
- `src/lib/db/quotaSnapshots.ts` — историјски подаци о квотама
- `src/lib/db/usage*.ts` — више DB модула повезаних са потрошњом

---

## Преглед

Сваки захтев који пролази кроз OmniRoute генерише **запис о потрошњи** који бележи:

- **Идентитет**: који API кључ, добављач, модел и комбинација
- **Токене**: токене упита, токене одговора, кеширане токене и укупан број
- **Трошак**: износ у USD (израчунат на основу података о ценама)
- **Временске податке**: кашњење и временске ознаке почетка/завршетка
- **Статус**: успех, грешка, ограничење брзине итд.

Ови записи се агрегирају у **аналитику**, чувају као **снимци квота** и користе за примену **ограничења буџета по кључу**.

```
Захтев ──▶ chatCore ──▶ usage.record() ──▶ SQLite
                                  │
                          ┌───────┼───────┐
                          ▼       ▼       ▼
                    аналитика  квота   наплата
                   (контр. табла) (примена) (извоз)
```

---

## Шта се бележи

Сервис `usage.ts` бележи **догађај потрошње** за сваки захтев:

| Поље               | Тип     | Извор                                                      |
| ------------------ | ------- | ---------------------------------------------------------- |
| `id`               | string  | UUID генерисан приликом бележења                           |
| `apiKeyId`         | string  | API кључ који је покренуо захтев                           |
| `provider`         | string  | ID добављача (openai, anthropic итд.)                      |
| `model`            | string  | ID модела (gpt-5, claude-opus-4-6 итд.)                    |
| `comboId`          | string? | ID комбинације ако је захтев усмерен кроз комбинацију      |
| `promptTokens`     | number  | Из одговора узводног сервиса                               |
| `completionTokens` | number  | Из одговора узводног сервиса                               |
| `cachedTokens`     | number  | Токени пронађени у кешу (Anthropic кеширање упита итд.)    |
| `totalTokens`      | number  | упит + одговор                                             |
| `costUsd`          | number  | Израчунато на основу података о ценама                     |
| `latencyMs`        | number  | Укупно трајање захтева од почетка до краја                 |
| `status`           | enum    | `success`, `error`, `rate_limited`, `timeout`, `cancelled` |
| `errorClass`       | string? | Класа грешке ако status != success                         |
| `timestamp`        | string  | ISO 8601 UTC                                               |
| `metadata`         | object  | Прилагођени подаци које је додао прикључак                 |

### Одакле потичу токени

Токени се издвајају из одговора узводног добављача у **обрађивачу одговора**:

```ts
// Из open-sse/handlers/chatCore.ts
const response = await providerExecutor.execute(provider, request);
const usage = response.usage || {
  prompt_tokens: 0,
  completion_tokens: 0,
  cached_tokens: 0,
};
```

За добављаче који не враћају податке о потрошњи (неки добављачи засновани на веб-колачићима), OmniRoute **процењује** број токена помоћу хеуристике `~4 знака по токену` (погледајте `open-sse/services/autoCombo/pipelineRouter.ts`).

### Кеширани токени

OmniRoute прати `cached_tokens` одвојено од `prompt_tokens` зато што:

- Anthropic кеширање упита наплаћује кеширане токене по сниженој тарифи (10% уобичајене цене)
- Неки добављачи враћају `cache_read_input_tokens`, чија цена треба да се израчуна другачије
- Аналитика може да прикаже **стопу погодака кеша** = `cached_tokens / prompt_tokens`

---

## Обрачун трошкова

Трошкови се израчунавају на основу **података о ценама** синхронизованих из LiteLLM-а (`src/lib/pricingSync.ts`):

| Модел             | Улаз $/1M | Излаз $/1M | Кеширано $/1M |
| ----------------- | --------- | ---------- | ------------- |
| gpt-5             | $2.50     | $10.00     | —             |
| claude-opus-4-6   | $15.00    | $75.00     | $1.50         |
| claude-sonnet-4-5 | $3.00     | $15.00     | $0.30         |
| gemini-2.5-pro    | $1.25     | $10.00     | —             |

Формула за израчунавање трошкова (`src/lib/usage/costCalculator.ts`):

```ts
cost =
  (prompt_tokens - cached_tokens) * input_price +
  cached_tokens * cached_price +
  completion_tokens * output_price;
```

> **Зашто се кеширани токени одузимају од токена упита?** Кеширани део се обрачунава засебно; примена улазне цене на цео упит довела би до двоструког обрачуна.

### Синхронизација цена

Подаци о ценама се аутоматски синхронизују из LiteLLM-а преко крајње тачке `/api/pricing/sync` (покреће је уграђени cron задатак, а не кориснички доступна променљива окружења):

```bash
# Ручно покретање
curl -X POST http://localhost:20128/api/pricing/sync
```

За моделе без података о ценама, OmniRoute прелази на **процену трошкова** помоћу интерних просечних тарифа (изведених из LiteLLM података о ценама).

---

## Агрегација по временском опсегу

Модул `usageAnalytics.ts` израчунава виџете контролне табле на основу необрађених података о коришћењу. Подржава 7 временских опсега:

| Опсег    | Период                            | Намена                                    |
| -------- | --------------------------------- | ----------------------------------------- |
| `1d`     | Последња 24 сата                  | Откривање наглих скокова трошкова по сату |
| `7d`     | Последњих 7 дана                  | Недељни преглед                           |
| `30d`    | Последњих 30 дана                 | Месечни обрачун                           |
| `90d`    | Последњих 90 дана                 | Квартална анализа                         |
| `ytd`    | Од 1. јануара текуће године       | Праћење годишњег буџета                   |
| `all`    | Све време                         | Статистика за целокупан период            |
| `custom` | Почетак/крај које задаје корисник | Ревизије, једнократни упити               |

### Израчунати виџети контролне табле

За сваки временски опсег, аналитички слој израчунава:

| Виџет                        | Опис                                                                     |
| ---------------------------- | ------------------------------------------------------------------------ |
| **Картице сажетка**          | Укупан број захтева, укупан трошак, укупан број токена, стопа успешности |
| **Графикон дневног тренда**  | Трошак + токени по дану, груписани по моделу                             |
| **Топлотна мапа активности** | Мрежа сат у дану × дан у недељи, боја = број захтева                     |
| **Расподела по моделу**      | Кружни графикон трошкова по моделу                                       |
| **Расподела по добављачу**   | Тракасти графикон захтева по добављачу                                   |
| **Најактивнији API кључеви** | Табела 10 кључева са највећим трошковима                                 |
| **Анализа грешака**          | Стопа грешака током времена, најчешће класе грешака                      |

### Програмски приступ

````ts
import { computeAnalytics } from "@/lib/usageAnalytics";

const analytics = await computeAnalytics(
  history,              // записи историје коришћења
  "7d",                 // временски опсег: "1d" | "7d" | "30d" | "90d" | "ytd" | "all" | "custom"
  connectionMap,        // мапа веза добављача (connectionId → назив налога)
  {
    startDate: "2025-01-01",  // опционално: за опсег "custom"
    endDate: "2025-06-01",   // опционално: за опсег "custom"
  }
);

console.log(analytics.summary.totalCost);   // 12.34 (центи)
console.log(analytics.byModel[0]);           // { model, cost, requests, promptTokens, completionTokens }

---

## Спровођење квота

Квота по API кључу спроводи се на два места:

1. **Меко ограничење** (`quotaWarnAt`): упозорење на контролној табли када коришћење премаши праг
2. **Тврдо ограничење** (`quotaLimit`): захтев се одбија са HTTP 429 када се ограничење премаши

### Конфигурација

```ts
// По API кључу
await updateApiKey(keyId, {
  quotaWarnAt: 5_00,    // $5.00 — прикажи упозорење
  quotaLimit: 10_00,    // $10.00 — потпуно заустављање
  quotaWindow: "month", // "day" | "week" | "month" | "all"
});
````

### Ток спровођења

```
Захтев ──▶ quotaCheck()
              │
              ├── Унутар ограничења?  ──▶ дозволи
              │
              └── Преко ограничења?  ──▶ 429 Превише захтева
                                          са заглављем Retry-After
```

### Снимци квота

Табела `quotaSnapshots` чува **историјско стање квота** ради анализе трендова:

| Поље | Опис |
| ----------- | -------------------------------- | ------ | ------- |
| `apiKeyId` | Кључ који се прати |
| `window` | "day" | "week" | "month" |
| `used` | Трошак у овом периоду (центи) |
| `limit` | Ограничење (центи) |
| `resetAt` | Када се период ресетује |
| `createdAt` | Када је снимак направљен |

Снимци се праве **при сваком захтеву** који има трошак > 0 и користе се за:

- Приказ траке напретка квоте на контролној табли
- Приказ графикона тренда квоте током 30 дана
- Покретање упозорења када се коришћење приближи ограничењу

---

## REST API

### Листа записа о коришћењу

```bash
GET /api/usage?range=7d&limit=100
GET /api/usage?apiKeyId=key-123&range=30d
GET /api/usage?provider=openai&range=1d
```

Одговор:

```json
{
  "records": [
    {
      "id": "uuid",
      "apiKeyId": "key-123",
      "provider": "openai",
      "model": "gpt-5",
      "promptTokens": 1234,
      "completionTokens": 567,
      "totalTokens": 1801,
      "costUsd": 0.005,
      "latencyMs": 1234,
      "status": "success",
      "timestamp": "2026-06-08T12:00:00Z"
    }
  ],
  "total": 1234,
  "nextCursor": "..."
}
```

### Преузимање сажетка аналитике

```bash
GET /api/usage/analytics?range=7d&groupBy=model
```

Одговор:

```json
{
  "summary": {
    "totalCost": 12.34,
    "totalRequests": 5678,
    "totalTokens": 12345678,
    "successRate": 0.987,
    "avgLatencyMs": 1234
  },
  "models": [
    { "model": "gpt-5", "cost": 8.5, "requests": 1234, "tokens": 4567890 },
    {
      "model": "claude-opus-4-6",
      "cost": 3.84,
      "requests": 234,
      "tokens": 234567
    }
  ],
  "daily": [
    { "date": "2026-06-01", "cost": 1.5, "requests": 800 },
    { "date": "2026-06-02", "cost": 2.0, "requests": 1000 }
  ]
}
```

### Упити за аналитику коришћења

Подацима о коришћењу приступа се преко контролне табле или MCP алата, а не преко директних REST крајњих тачака за извоз. Доступне аналитике:

- **`/api/usage/analytics`** — обједињене метрике коришћења (груписане по моделу, добављачу, кључу)
- **`/api/usage/quota`** — тренутни статус квоте по API кључу
- **`/api/usage/history`** — евиденције историје захтева

---

## MCP алати

Два MCP алата излажу податке о коришћењу агентима (погледајте `open-sse/mcp-server/tools/`):

| Алат                    | Опис                                                     |
| ----------------------- | -------------------------------------------------------- |
| `omniroute_cost_report` | Генерише извештај о трошковима по кључу за задати период |
| `omniroute_check_quota` | Враћа тренутни статус квоте за API кључ                  |

Пример позива агента:

```json
{
  "tool": "omniroute_cost_report",
  "args": { "period": "week" }
}
```

---

## Задржавање и чишћење

Подаци о коришћењу расту за ~1–10KB по захтеву. При великом обиму, то може бити значајно.

### Подешавања задржавања

Задржавање историје коришћења подешава се преко подешавања базе података у корисничком интерфејсу или преко `/api/settings/database`.

Подразумевано, историја коришћења се чува **90 дана**.

### Чишћење

Старе записе чисти `src/lib/db/cleanup.ts`:

- Покреће их позадински cron процес
- Брише записе из `usage_history` који су старији од подешене вредности задржавања `usageHistory`

### Процена простора за складиштење

| Стопа захтева       | Простор за 30 дана | Простор за 90 дана |
| ------------------- | ------------------ | ------------------ |
| 100 захтева/дан     | ~3MB               | ~9MB               |
| 1.000 захтева/дан   | ~30MB              | ~90MB              |
| 10.000 захтева/дан  | ~300MB             | ~900MB             |
| 100.000 захтева/дан | ~3GB               | ~9GB               |

За веома велики саобраћај, размотрите:

- Смањење периода задржавања преко подешавања базе података
- Коришћење `aggregated_metrics` уместо необрађених записа (само за аналитику)

---

## Савети за оптимизацију трошкова

### 1. Користите одговарајући модел

```bash
# Брз одговор — користите јефтин + брз модел
curl -d '{"model":"auto/fast","messages":[...]}'

# Сложен задатак — користите квалитетан модел
curl -d '{"model":"auto/smart","messages":[...]}'
```

### 2. Омогућите кеширање

Anthropic кеширање упита штеди **90% при поновљеном контексту**:

```ts
// Кеширање је аутоматско — само укључите исти велики системски упит
const response = await openai.chat({
  model: "claude-sonnet-4-5",
  system: longSystemPrompt, // Биће аутоматски кеширан
  messages: [{ role: "user", content: "..." }],
});
```

### 3. Користите компресију

RTK + Caveman компресија штеди **15–95% у сесијама са интензивним коришћењем алата**:

```ts
const config = {
  compression: {
    engine: "rtk",
    intensity: "aggressive",
  },
};
```

### 4. Поставите квоте по кључу

Увек поставите `quotaLimit` да бисте спречили неконтролисане трошкове:

```ts
await updateApiKey(keyId, { quotaLimit: 10_00 }); // Ограничење од $10 месечно
```

### 5. Проверите највеће потрошаче

Користите контролну таблу или **`/api/usage/analytics`** да бисте груписали по API кључу и сортирали по трошку:

```bash
GET /api/usage/analytics?groupBy=apiKey
```

---

## Решавање проблема

### „Трошак је већи од очекиваног“

1. Проверите **`/api/usage/analytics?groupBy=model`** — пронађите скуп модел
2. Проверите **`/api/usage/analytics?groupBy=apiKey`** — пронађите великог потрошача
3. Проверите да ли су подаци о ценама ажурирани: `POST /api/pricing/sync`

### „Записи недостају“

- Проверите подешавања задржавања базе података у Контролна табла → База података → Чишћење — старе записе брише периодични задатак чишћења (`src/lib/db/cleanup.ts`)
- Проверите да ли постоје грешке у `src/lib/db/usage*.ts` — грешке при упису у базу података се евидентирају, али се не приказују
- Проверите да ли је захтев заиста стигао до `chatCore` — проверите комбиновано усмеравање

### „Квота се не примењује“

- Проверите подешавање `quotaLimit` за кључ
- Проверите да ли је `quotaWindow` правилно подешен
- Потражите записе `quotaSnapshots` — требало би да се креирају при сваком захтеву

---

## Такође погледајте

- [DATABASE_GUIDE.md](../ops/DATABASE_GUIDE.md) — Шема табела коришћења
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md#18-pricing-sync) — променљиве окружења за синхронизацију цена
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — Како `auto/fast`, `auto/cheap` смањују трошкове
- [API_REFERENCE.md](../reference/API_REFERENCE.md) — Потпуна референца за `/api/usage/*`
- Извор: `open-sse/services/usage.ts`, `src/lib/usageAnalytics.ts`, `src/lib/db/usage*.ts`
