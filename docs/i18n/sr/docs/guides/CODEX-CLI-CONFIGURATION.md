# Codex CLI — Configuration with OmniRoute (Српски)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇦 [bs](../../../bs/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CODEX-CLI-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CODEX-CLI-CONFIGURATION.md)

---

Комплетан водич за коришћење Codex CLI-ја усмереног на OmniRoute као позадински систем компатибилан са OpenAI-јем.

---

> **TOML је једини формат који се заиста примењује.** Савремени Codex чита искључиво
> `~/.codex/config.toml` (проверено са codex-cli 0.147.0: `codex --help` наводи да
> `-c/--config` замене „учитавају из `~/.codex/config.toml`“). Стари
> `~/.codex/config.yaml` припадао је застарелом npm CLI-ју и неприметно се игнорише.
> Генератор на контролној табли (`/api/cli-tools/apply`, алатка `codex`) уписује TOML уз
> конзервативно спајање — постојећи кључеви и блокови других провајдера остају сачувани,
> API кључ остаје у `OMNIROUTE_API_KEY` (никада у датотеци), а преостали застарели
> `config.yaml` пријављује се као напомена о миграцији без икаквих измена.

## config.toml спреман за налепљивање

Замените `<YOUR_HOST>` и `<YOUR_KEY>` својим вредностима:

```toml
# ~/.codex/config.toml
model                          = "cx/gpt-5.5"
model_provider                 = "omniroute"
model_reasoning_effort         = "xhigh"
model_context_window           = 400000
model_auto_compact_token_limit = 350000
tool_output_token_limit        = 32768    # ограничење чувања историје по позиву алатке

[model_providers.omniroute]
name                 = "OmniRoute"
base_url             = "http://<YOUR_HOST>:20128/v1"
env_key              = "OMNIROUTE_API_KEY"
requires_openai_auth = false
wire_api             = "responses"
```

```bash
# ~/.bashrc или ~/.zshrc — стварна вредност кључа, никада у config.toml
export OMNIROUTE_API_KEY="<YOUR_KEY>"
```

### macOS: Codex укључен у ChatGPT апликацију

Ако сте Codex инсталирали преко ChatGPT десктоп апликације, бинарна датотека `codex` можда
постоји само унутар пакета апликације и још увек није на `PATH` путањи ваше љуске. Додајте
директоријум са ресурсима у датотеку која се учитава при покретању љуске:

```bash
export PATH="/Applications/ChatGPT.app/Contents/Resources:$PATH"
```

Отворите нову љуску, а затим проверите:

```bash
command -v codex
codex --version
```

### Локални OmniRoute без аутентификације: довољан је заменски кључ

Codex проверава да ли променљива окружења наведена у `env_key` постоји
**пре** него што први захтев напусти CLI. Ако ваша **локална** OmniRoute
инстанца не захтева аутентификацију, довољна је било која непразна заменска вредност:

```bash
export OMNIROUTE_API_KEY="${OMNIROUTE_API_KEY:-local}"
```

Уместо тога користите стварни кључ када је ваш OmniRoute сервер заштићен или удаљен.

> **Уобичајене опције за хост**
>
> | Приступ        | URL                           |
> | -------------- | ----------------------------- |
> | Локална мрежа  | `http://192.168.0.1:20128/v1` |
> | Tailscale      | `http://100.x.x.x:20128/v1`   |
> | Повратна петља | `http://localhost:20128/v1`   |

---

## `wire_api = "responses"` — зашто ради са свим моделима

Codex CLI је у фебруару 2026. означио `wire_api = "chat"` (Chat Completions) као застарео и сада **захтева** `wire_api = "responses"` (OpenAI Responses API). Подешавање `wire_api = "chat"` изазива тренутни пад при покретању почев од v0.138.

Многи провајдери, укључујући GLM и Kimi, и даље нуде само Chat Completions крајњу тачку. DeepSeek V4 сада нуди и изворни Responses API и крајњу тачку компатибилну са Anthropic-ом; OmniRoute подразумевано користи Responses и омогућава да свака DeepSeek веза изабере компатибилност са Anthropic-ом.

**OmniRoute ово решава транспарентно:**

```
Codex CLI
  → wire_api = "responses"
  → POST /v1/responses (OmniRoute)
    → OmniRoute бира изворни протокол провајдера и преводи када је потребно
    → POST /responses (DeepSeek V4) или /chat/completions (Mistral / GLM / Kimi / остали)
```

Када користите OmniRoute, никада вам није потребан засебан прокси за превођење. **Сви модели користе `wire_api = "responses"`** — OmniRoute се стара за остало.

> **`wire_api` је подразумевана вредност** — поље подразумевано има вредност `"responses"` и може се потпуно изоставити из `config.toml`. Експлицитно га задајте само ако документујете намеру.

---

## Контекстни прозор и сажимање

### Поља за конфигурацију токена

| Поље                             | Опис                                                                                                                                                                                              |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `model_context_window`           | Укупан буџет токена за активни модел. Подесите га на декларисано ограничење модела.                                                                                                               |
| `model_auto_compact_token_limit` | Праг који покреће аутоматско сажимање историје. **Максимум: 90% од `model_context_window`** — вредности изнад 90% се неприметно игноришу.                                                         |
| `tool_output_token_limit`        | Ограничење броја токена који се чувају у историји по излазу позива алатке. Спречава да један велики одговор алатке попуни прозор. **Ово није максимални излаз** — већ ограничење чувања историје. |
| `compact_prompt`                 | Уграђено замењивање системског упита који се користи током сажимања (v0.138+).                                                                                                                    |

> **Напомена о `model_max_output_tokens`**: Ово поље **није део конфигурационе шеме Codex CLI-ја** (не постоји у Codex Rust бази кода). Ако се подеси, неприметно се игнорише. Немојте се ослањати на њега — користите `tool_output_token_limit` да бисте контролисали колико се излаза алатке чува у историји.

### Контекстни прозори по моделу

| Модел                                | OmniRoute ID                         | Контекстни прозор        | `auto_compact` | `tool_output_limit` |
| ------------------------------------ | ------------------------------------ | ------------------------ | -------------- | ------------------- |
| GPT-5.5                              | `cx/gpt-5.5`                         | 400k поуздано (1M макс.) | 350,000        | 32,768              |
| Kimi K2.7 (размишљање)               | `kmc/kimi-k2.7`                      | 131,072                  | 112,000        | 32,768              |
| Kimi K2.6                            | `kmc/kimi-k2.6`                      | 131,072                  | 112,000        | 32,768              |
| GLM-5.2 / 5.2-max (размишљање)       | `glm/glm-5.2`                        | 131,072                  | 112,000        | 32,768              |
| MiMo V2.5 Pro (размишљање)           | `opencode-go/mimo-v2.5-pro`          | 131,072                  | 112,000        | 32,768              |
| Qwen 3.7 Plus (размишљање)           | `opencode-go/qwen3.7-plus`           | 32,768                   | 28,000         | 16,384              |
| DeepSeek V4 Pro (OllamaCloud)        | `ollamacloud/deepseek-v4-pro`        | 131,072                  | 112,000        | 32,768              |
| DeepSeek V4 Pro                      | `ds/deepseek-v4-pro`                 | 1,000,000                | 900,000        | 65,536              |
| MiMo V2.5                            | `opencode-go/mimo-v2.5`              | 131,072                  | 112,000        | 32,768              |
| Gemma 4 31B (OllamaCloud)            | `ollamacloud/gemma4:31b`             | 32,768                   | 28,000         | 16,384              |
| Nemotron 3 Super (OllamaCloud)       | `ollamacloud/nemotron-3-super`       | 32,768                   | 28,000         | 16,384              |
| GPT-OSS 20B (OllamaCloud)            | `ollamacloud/gpt-oss:20b`            | 32,768                   | 28,000         | 16,384              |
| DeepSeek V4 Flash (OllamaCloud)      | `ollamacloud/deepseek-v4-flash`      | 65,536                   | 56,000         | 16,384              |
| Gemini 3 Flash Preview (OllamaCloud) | `ollamacloud/gemini-3-flash-preview` | 1,000,000                | 850,000        | 32,768              |
| GLM-5 Turbo                          | `glm/glm-5-turbo`                    | 131,072                  | 112,000        | 16,384              |
| GLM-4.7 Flash                        | `glm/glm-4.7-flash`                  | 131,072                  | 112,000        | 16,384              |
| Mistral Large Latest                 | `mistral/mistral-large-latest`       | 262,144                  | 220,000        | 16,384              |

> **Формула за сажимање:** `effective_window = model_context_window - min(tool_output_token_limit, 20000)`. Вредности изнад 20k не мењају праг за покретање сажимања.

> **Практично правило:** подесите `model_auto_compact_token_limit` на 85–88% од `model_context_window`. Никада не прелазите 90% — вредност ће бити неприметно игнорисана.

---

## Префикс модела: `cx/`

Сви Codex модели у OmniRoute-у користе префикс `cx/`:

| Назив у Codex CLI-ју    | OmniRoute модел    |
| ----------------------- | ------------------ |
| `cx/gpt-5.5`            | GPT-5.5 стандардни |
| `cx/gpt-5.4`            | GPT-5.4 стандардни |
| `cx/gpt-5.4-mini`       | GPT-5.4 mini       |
| `cx/gpt-5.1-codex-mini` | GPT-5.1 Codex mini |

Други провајдери користе сопствени префикс (`kmc/`, `glm/`, `ds/`, `ollamacloud/`, `opencode-go/`, `mistral/`) — префикс одговара псеудониму провајдера у OmniRoute-у.

---

## Интензитет резоновања

Контролише колико модел „размишља“ пре него што одговори.

| Вредност | Користити за                                                 |
| -------- | ------------------------------------------------------------ |
| `none`   | Без резоновања — директан одговор                            |
| `low`    | Тривијалне задатке (преименовање, форматирање)               |
| `medium` | **Подразумевана вредност сервера** ако није задата           |
| `high`   | Задатке средње сложености (рефакторисање, отклањање грешака) |
| `xhigh`  | Архитектуру, дубоку анализу, сложене проблеме                |

```bash
# Замена вредности за појединачно покретање
codex -c model_reasoning_effort=low "rename variable x to count"
codex -c model_reasoning_effort=xhigh "design the auth module"
```

Такође подесите **сажетак** резоновања како би Desktop могао да прикаже текст размишљања (а не само шифроване блокове):

```toml
# ~/.codex/config.toml
model_reasoning_effort = "xhigh"   # или ultra када је подржано
model_reasoning_summary = "detailed"  # auto | concise | detailed | none
```

### OmniRoute буџет за размишљање (подешавање сервера)

На OmniRoute хосту, **Settings → AI → Thinking Budget** мора бити подешено на **`passthrough`** како би Codex интензитет/сажетак стигао до узводног сервиса. Режим **`auto` уклања** сва клијентска поља `reasoning` / `reasoning_effort` и испразниће панеле за размишљање чак и када је Codex правилно конфигурисан.

Комплетан водич: [THINKING_BUDGET.md](./THINKING_BUDGET.md).

Компресија и кеш упита су независни и настављају да раде у режиму `passthrough`.

---

## Профили — именоване конфигурације по моделу/току рада

Профили вам омогућавају да промените модел + контекстни прозор помоћу једне опције. Сваки профил је једноставна
`~/.codex/<name>.config.toml` датотека која се примењује преко основне `config.toml` датотеке.

> **Правило именовања (Codex CLI v0.137+):** датотека мора бити `~/.codex/<name>.config.toml` — **без префикса `profile-`**.
> CLI разрешава `-p kimi-k27` → `~/.codex/kimi-k27.config.toml`. Ако датотека није пронађена, подразумевана конфигурација се примењује без упозорења.

```bash
codex --profile kimi-k27 "analyze 10k lines of this codebase"
codex -p glm52 "architecture review"
codex --profile deepseek-flash "rename variable"   # брзо, јефтино
```

### Профили интензитета (исти модел, различит интензитет)

```bash
codex -p low      # cx/gpt-5.5, интензитет=low
codex -p medium   # cx/gpt-5.5, интензитет=medium
codex -p high     # cx/gpt-5.5, интензитет=high
codex -p xhigh    # cx/gpt-5.5, интензитет=xhigh (подразумевано)
codex -p chat     # cx/gpt-5.5, интензитет није подешен (подразумевана вредност сервера)
```

### Модели за размишљање (дубоко размишљање) — xhigh + детаљан сажетак

| Профил       | Модел                       | Контекст | Користити за                       |
| ------------ | --------------------------- | -------- | ---------------------------------- |
| `kimi-k27`   | `kmc/kimi-k2.7`             | 128k     | Најбољи квалитет размишљања (Kimi) |
| `glm52`      | `glm/glm-5.2`               | 128k     | GLM размишљање                     |
| `glm52max`   | `glm/glm-5.2-max`           | 128k     | Максимално GLM размишљање          |
| `mimo-pro`   | `opencode-go/mimo-v2.5-pro` | 128k     | MiMo размишљање                    |
| `qwen37plus` | `opencode-go/qwen3.7-plus`  | 32k      | Qwen размишљање                    |

### Добри модели — висок интензитет

| Профил         | Модел                         | Контекст | Користити за                            |
| -------------- | ----------------------------- | -------- | --------------------------------------- |
| `kimi-k26`     | `kmc/kimi-k2.6`               | 128k     | Општу намену (Kimi)                     |
| `deepseek-pro` | `ollamacloud/deepseek-v4-pro` | 128k     | DeepSeek Pro преко OllamaCloud-а        |
| `deepseek`     | `ds/deepseek-v4-pro`          | 1M       | Директни DeepSeek Pro, огроман контекст |
| `mimo`         | `opencode-go/mimo-v2.5`       | 128k     | MiMo за општу намену                    |

### Једноставни модели — без интензитета резоновања

| Профил     | Модел                          | Контекст | Користити за                |
| ---------- | ------------------------------ | -------- | --------------------------- |
| `gemma4`   | `ollamacloud/gemma4:31b`       | 32k      | Исплативе, способне задатке |
| `nemotron` | `ollamacloud/nemotron-3-super` | 32k      | NVIDIA Nemotron             |
| `gptoss`   | `ollamacloud/gpt-oss:20b`      | 32k      | GPT отвореног кода          |

### Брзи модели — низак интензитет

| Профил           | Модел                                | Контекст | Користити за                 |
| ---------------- | ------------------------------------ | -------- | ---------------------------- |
| `deepseek-flash` | `ollamacloud/deepseek-v4-flash`      | 64k      | Брзе задатке                 |
| `gemini-flash`   | `ollamacloud/gemini-3-flash-preview` | 1M       | Веома брзо, огроман контекст |
| `glm5turbo`      | `glm/glm-5-turbo`                    | 128k     | GLM Turbo                    |
| `glm47flash`     | `glm/glm-4.7-flash`                  | 128k     | GLM Flash                    |
| `mistral`        | `mistral/mistral-large-latest`       | 256k     | Mistral Large                |

### Табела за брзо одлучивање

| Задатак                                  | Препоручени профил                                |
| ---------------------------------------- | ------------------------------------------------- |
| Преименовање, форматирање, шаблонски код | `--profile deepseek-flash` или `-p low`           |
| Објашњење, кратак преглед                | `-p chat` или `-p gemini-flash`                   |
| Отклањање грешака, умерено рефакторисање | `-p medium` или `-p kimi-k26`                     |
| Нова функционалност, сложени тестови     | `-p high` или `-p mimo`                           |
| Архитектура, дубинска анализа            | `-p kimi-k27` или `-p glm52` или `-p xhigh`       |
| Анализа базе кода (захтева 1M контекста) | `--profile deepseek` или `--profile gemini-flash` |
| Максимални квалитет резоновања           | `-p glm52max` или `-p mimo-pro`                   |
| Ниски трошкови                           | `-p gemma4` или `-p gptoss`                       |

---

## Аутоматско генерисање профила помоћу `omniroute setup-codex`

Ако покрећете OmniRoute на VPS-у, можете аутоматски генерисати датотеке профила из активног каталога модела:

```bash
# Са VPS-а (користи локални OmniRoute на порту 20128)
omniroute setup-codex

# Са било ког рачунара — усмерите на свој VPS
omniroute setup-codex --remote http://100.x.x.x:20128 --api-key sk-xxx

# Преглед без уписивања датотека
omniroute setup-codex --remote http://100.x.x.x:20128 --dry-run

# Генеришите само GLM и Kimi профиле
omniroute setup-codex --only glm,kimi

# Уписивање у прилагођени директоријум
omniroute setup-codex --codex-home /path/to/.codex
```

Команда преузима `/v1/models`, користи подешене профиле за познате моделе, за остале компатибилне текстуалне моделе користи метаподатке каталога као резервну опцију и за сваки од њих уписује `~/.codex/<name>.config.toml`. Идемпотентна је — можете је безбедно поново покретати.

OmniRoute такође може **аутоматски синхронизовати** исте датотеке профила након што успешно откривање/увоз модела добављача измени активни каталог. Ова могућност се **укључује ручно и подразумевано је искључена**: укључите је на **CLI Code контролној табли** („CLI profile auto-sync“ → Codex) или подесите `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES=true` (такође поштује `CLI_ALLOW_CONFIG_WRITES`, који је подразумевано укључен). Када је омогућена, уписује само засебне датотеке профила `~/.codex/*.config.toml`; никада не мења активни/подразумевани `~/.codex/config.toml`, Codex-lb подешавања, аутентификацију нити избор добављача.

---

## Покретање Codex-а помоћу `omniroute launch-codex`

Проверава исправност ваше OmniRoute инстанце пре покретања Codex-а:

```bash
# Покретање са локалним OmniRoute-ом (подразумевани порт 20128)
omniroute launch-codex

# Покретање са одређеним профилом
omniroute launch-codex --profile kimi-k27

# Покретање са удаљеним VPS-ом
omniroute launch-codex --remote http://100.x.x.x:20128/v1 --api-key sk-xxx

# Прослеђивање додатних аргумената Codex-у
omniroute launch-codex --profile glm52 -- --yolo "fix this bug"
```

Codex је такође циљ две генеричке улазне тачке засноване на манифесту
(`bin/cli/cli-manifest.mjs`):

```bash
# Интерактивни избор модела → уписује ~/.codex/<name>.config.toml (TOML, env_key)
omniroute configure codex

# Покреће Codex са добављачем omniroute уметнутим преко -c опција (без уписивања конфигурације)
omniroute run codex
```

---

## Нове функције Codex CLI-ја (v0.138–v0.141)

| Верзија | Функција                                                                                                                                                                                   |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| v0.138  | Прослеђивање десктоп апликацији (`/app`), v2 токени за лични приступ, `--profile` као искључиви бирач профила (застареле `[profiles]` табеле у датотеци изазивају пад при покретању)       |
| v0.139  | `web_search = "live"` — изворна веб-претрага из режима за код; `oneOf`/`allOf` у шемама MCP алата; `codex doctor` дијагностика окружења                                                    |
| v0.140  | `/usage` приказ токена током сесије; `/import` из Claude Code сесија; подкоманда `codex delete <SESSION_ID>`; Amazon Bedrock аутентификација преко `aws` објекта у конфигурацији добављача |
| v0.141  | E2E шифровани Noise релеј за удаљене извршиоце; исправка за SQLite WAL; подршка за P-521 TLS                                                                                               |

### Нова поља у `config.toml` (након v0.137)

```toml
# Изворна веб-претрага (v0.139)
web_search = "live"   # "disabled" | "cached" | "live"

# Засебан системски упит за програмера (v0.138)
developer_instructions = "Always prefer functional style."

# Прилагођени упит за сажимање
compact_prompt = "Summarise the above as bullet points."

# Усмерите /review на јефтинији модел
review_model = "glm/glm-5-turbo"

# Ниво OpenAI услуге
service_tier = "fast"   # "fast" | "flex"
```

### Нова поља у `[model_providers.<id>]`

```toml
[model_providers.omniroute]
base_url             = "http://100.x.x.x:20128/v1"
env_key              = "OMNIROUTE_API_KEY"
requires_openai_auth = false

# Статичка додатна заглавља у сваком захтеву
[model_providers.omniroute.http_headers]
"X-Custom-Header" = "value"

# Заглавља прочитана из променљивих окружења
[model_providers.omniroute.env_http_headers]
"X-Trace-Id" = "TRACE_ID"

# Додатни параметри URL упита (корисно за Azure api-version)
[model_providers.omniroute.query_params]
"api-version" = "2024-12-01-preview"
```

### Amazon Bedrock аутентификација (v0.140)

```toml
[model_providers.bedrock]
base_url = "https://bedrock-runtime.us-east-1.amazonaws.com"

[model_providers.bedrock.aws]
profile = "default"   # Профил из ~/.aws/credentials
region  = "us-east-1"
```

---

## Више сервера

```toml
[model_providers.omniroute-main]
base_url = "http://192.168.0.1:20128/v1"
env_key  = "OMNIROUTE_API_KEY"

[model_providers.omniroute-tailscale]
base_url = "http://100.x.x.x:20128/v1"
env_key  = "OMNIROUTE_API_KEY"
```

---

## Claude Code — еквивалентна конфигурација

| Codex CLI (`config.toml`)         | Claude Code (променљива окружења)     | Ефекат                             |
| --------------------------------- | ------------------------------------- | ---------------------------------- |
| `tool_output_token_limit = 32768` | _(није директно изложено)_            | Ограничење историје по алату       |
| `model_context_window = 400000`   | _(одређује модел)_                    | Контекстни прозор                  |
| —                                 | `CLAUDE_CODE_MAX_OUTPUT_TOKENS=65536` | Максималан број токена по одговору |

```bash
# ~/.bashrc — ограничење токена за Claude Code
export CLAUDE_CODE_MAX_OUTPUT_TOKENS=65536
```

---

## Брзи преглед — CLI опције

| Опција                | Кратко | Ефекат                                                    |
| --------------------- | ------ | --------------------------------------------------------- |
| `--model <id>`        | `-m`   | Замeњује `model` за ово покретање                         |
| `--profile <name>`    | `-p`   | Учитава `~/.codex/<name>.config.toml`                     |
| `--config key=value`  | `-c`   | Замeњује било које поље у config.toml (може се понављати) |
| `--enable <feature>`  | —      | Принудно омогућава заставицу функције                     |
| `--disable <feature>` | —      | Принудно онемогућава заставицу функције                   |
| `--search`            | —      | Омогућава веб-претрагу уживо за ово покретање             |

Ново у v0.140:

```bash
codex delete <SESSION_ID>          # брише сесију
codex delete <SESSION_ID> --force  # прескаче потврду
codex debug models --bundled       # приказује уграђени каталог модела као JSON
```

Унутар интерактивне сесије:

| Команда   | Ефекат                                           |
| --------- | ------------------------------------------------ |
| `/model`  | Отвара бирач модела                              |
| `/usage`  | Приказује употребу токена за ову сесију (v0.140) |
| `/app`    | Пребацује на десктоп апликацију (v0.138)         |
| `/import` | Увози Claude Code сесију (v0.140)                |
| `/help`   | Приказује све команде са косом цртом             |

---

## Дуготрајни задаци

Две подразумеване вредности OmniRoute-а могу неприметно да угрозе вишесатне Codex CLI сесије. Ниједна од њих није Codex CLI подешавање — обе се налазе на страни OmniRoute-а. Корисници који преносе конфигурацију са посредничких сервера узводног система, који везују налоге и онемогућавају прекиде због неактивности, често наиђу на оба проблема и закључе да OmniRoute „не може да одржи дугу сесију“.

| Симптом                                                                       | Вероватан узрок                                                                    | Поставка                 |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------ |
| Сесија стално мења налоге / континуитет кеша упита се губи између интеракција | TTL афинитета сесије је `0` (онемогућен)                                           | `sessionAffinityTtlMs`   |
| Веза се прекида усред закључивања без упита видљивог клијенту                 | Надзор неактивног тока се активирао након 10 минута без сегмента са узводне стране | `STREAM_IDLE_TIMEOUT_MS` |

Повезане дискусије: [#7126](https://github.com/diegosouzapw/OmniRoute/discussions/7126) (прекиди дугих задатака), [#5718](https://github.com/diegosouzapw/OmniRoute/discussions/5718) (зашто је афинитет подразумевано искључен). Праћење: [#7287](https://github.com/diegosouzapw/OmniRoute/issues/7287).

### 1. Афинитет сесије — вежите један разговор за један налог

**Подразумевано:** `sessionAffinityTtlMs = 0` (онемогућено).

**Где се подешава**

- Контролна табла → **Подешавања → Усмеравање** → **Афинитет сесије** → **TTL афинитета (секунде)** (`ComboDefaultsTab`)
- Или PATCH-ом ажурирајте подешавања помоћу `sessionAffinityTtlMs` у **милисекундама** (Zod опсег `0`–`86_400_000`, односно до 24 сата)

> Преименовано у #7274 из `codexSessionAffinityTtlMs`, који је био намењен само Codex-у. Стари кључ је и даље прихваћен као псеудоним само за читање; нове конфигурације треба да користе `sessionAffinityTtlMs`. Афинитет се сада примењује на **сваког** добављача када је TTL већи од `0`, а не само на Codex — погледајте [`docs/architecture/RESILIENCE_GUIDE.md`](../architecture/RESILIENCE_GUIDE.md) → Афинитет сесије.

**Шта престаје да ради када вредност остане 0**

Свака интеракција у Codex разговору са више интеракција усмерава се независно, у складу са активном комбинованом стратегијом, и свака може завршити на **другом налогу**. То нарушава континуитет сесије / кеша упита на узводној страни. OmniRoute узима у обзир Codex заглавља сесије (`x-codex-session-id` / `x-session-id` / `x-omniroute-session`) и поља тела као што су `prompt_cache_key` / `session_id` само када је TTL већи од `0` (`extractSessionAffinityKey` у `src/sse/services/auth.ts`).

**Препорука за један вишесатни задатак**

Подесите TTL **изнад очекиваног стварног трајања задатка** (максимум у корисничком интерфејсу је **86400 секунди** = 24 сата):

| Очекивано трајање задатка | TTL афинитета (интерфејс, секунде) | `sessionAffinityTtlMs` |
| ------------------------- | ---------------------------------- | ---------------------- |
| Неколико сати             | `14400` (4 ч)                      | `14400000`             |
| Преко ноћи / ~12 ч        | `43200` (12 ч)                     | `43200000`             |
| Цео дан                   | `86400` (24 ч, максимум)           | `86400000`             |

Изричито укључивање је намерно: онемогућавање афинитета даје предност расподели оптерећења између налога; омогућавање афинитета даје предност континуитету једне дуге сесије агента. Овај водич **не** мења подразумевану вредност — оператери који покрећу дуге Codex задатке морају сами да је укључе.

### 2. Временско ограничење неактивног тока — не прекидајте тихе интеракције закључивања

**Подразумевано:** `STREAM_IDLE_TIMEOUT_MS = 600000` (10 минута). Ако није подешено, наслеђује вредност од `REQUEST_TIMEOUT_MS`; заједничка основна вредност је такође 600000. Погледајте [`docs/guides/SETUP_GUIDE.md`](SETUP_GUIDE.md) → Временска ограничења.

**Шта престаје да ради са подразумеваном вредношћу**

Codex reasoning / tool korak koji ostane tih duže od 10 minuta bez **ijednog stvarnog upstream segmenta** prinudno zatvara SSE idle watchdog (`open-sse/utils/stream.ts`). Klijent često vidi samo prekid veze — što odgovara opisu „automatski je zaustavljeno bez ikakvog obaveštenja“.

Ključni detalj: OmniRoute-ov sintetički SSE **heartbeat ne resetuje** idle sat. Samo stvarni segment upstream tela ažurira `lastChunkTime`. Tih model koji još uvek „razmišlja“ iz perspektive watchdog-a izgleda isto kao zaustavljen upstream.

Povezana neaktivnost Undici tela: `FETCH_BODY_TIMEOUT_MS` (takođe podrazumevano koristi istu osnovnu vrednost od 10 minuta; `0` je onemogućava). Za strimovanje, `FETCH_TIMEOUT_MS` pokriva samo uspostavljanje veze / prva zaglavlja — kada strim postane aktivan, zastoji su kontrolisani parametrima `STREAM_IDLE_TIMEOUT_MS` i `FETCH_BODY_TIMEOUT_MS`.

**Preporučeno za pojedinačni zadatak koji traje više sati**

U okruženju OmniRoute procesa (`.env` / compose / systemd):

```bash
# Onemogućite ograničenja neaktivnosti strima i tela za duge korake rezonovanja
STREAM_IDLE_TIMEOUT_MS=0
FETCH_BODY_TIMEOUT_MS=0
```

Ili ih povećajte iznad najdužeg očekivanog perioda tišine (vrednosti su u milisekundama):

```bash
# Primer: dozvolite do 2 sata tišine između upstream segmenata
STREAM_IDLE_TIMEOUT_MS=7200000
FETCH_BODY_TIMEOUT_MS=7200000
```

Ponovo pokrenite OmniRoute nakon izmene ovih promenljivih okruženja.

### Konkretno uputstvo — višesatni Codex zadatak

1. **Vežite nalog:** Kontrolna tabla → Podešavanja → Usmeravanje → Afinitet sesije → TTL afiniteta = `43200` (12h) ili `86400` (maksimalno 24h).
2. **Povećajte / onemogućite idle ograničenja** u OmniRoute okruženju:

```bash
STREAM_IDLE_TIMEOUT_MS=0
FETCH_BODY_TIMEOUT_MS=0
```

3. Zadržite uobičajeni Codex `config.toml` (`wire_api = "responses"`, ispravan `base_url`, `OMNIROUTE_API_KEY`) — u Codex-u ne postoje podešavanja za afinitet/neaktivnost koja kontrolišu ova dva ponašanja.
4. Ponovo pokrenite OmniRoute, a zatim pokrenite dugi Codex zadatak.

### Odluka o podrazumevanim vrednostima (#7287)

| Parametar                | Podrazumevana vrednost | Promeniti u ovom vodiču?                                                                                     |
| ------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------ |
| `sessionAffinityTtlMs`   | `0` (isključeno)       | **Ne** — ostaje opciono (balansiranje opterećenja naspram kontinuiteta; pogledajte Diskusiju #5718)          |
| `STREAM_IDLE_TIMEOUT_MS` | `600000` (10 min)      | **Ne** — ostaje 10 minuta za opšti saobraćaj; operateri dugih Codex zadataka ga povećavaju ili onemogućavaju |

Globalna promena bilo koje od ovih podrazumevanih vrednosti promenila bi ponašanje za svakog klijenta instance, a ne samo za Codex. Dokumentujte parametre; ostavite podrazumevane vrednosti nepromenjene dok eksplicitna odluka operatera ne odredi drugačije.

### Dijagnostikovanje prekida zbog neaktivnosti

Kada se idle watchdog aktivira, OmniRoute beleži red ovog oblika:

```text
[STREAM] Idle timeout: no data from codex for 600000ms (model: cx/gpt-5.5)
```

Pretražite logove za `Idle timeout: no data from` (ili kod `stream_idle_timeout` / naziv greške `StreamIdleTimeoutError`). Segment pružaoca je vrednost koju je OmniRoute koristio za taj zahtev (`codex`, ID drugog pružaoca ili `provider` ako je nepoznat) — to nije uvek doslovno niska `codex`.

---

## Решавање проблема

**`Error: wire_api = "chat" више није подржан`**
Уклоните `wire_api = "chat"` из конфигурације. Подесите `wire_api = "responses"` или изоставите поље (подразумевана вредност је `"responses"` од верзије v0.138).

**`Error: модел није пронађен`**
Проверите да ли модел постоји у OmniRoute-у са исправним префиксом. Користите `omniroute models list` или отворите `/dashboard/providers/<provider>`.

**`Грешка при аутентификацији`**
Проверите да ли је `OMNIROUTE_API_KEY` извезен: `echo $OMNIROUTE_API_KEY`.

**`ERROR: Недостаје променљива окружења: OMNIROUTE_API_KEY`**
Codex проверава да ли променљива окружења постоји пре слања првог захтева. Извезите
стварни кључ за заштићене сервере или непразну заменску вредност, као што је
`OMNIROUTE_API_KEY=local`, када ваша **локална** OmniRoute инстанца не захтева
аутентификацију. Поново покрените љуску ако сте је додали у `~/.bashrc` или `~/.zshrc`.

**`Веза је одбијена`**
Проверите да ли је OmniRoute покренут и да ли су хост/порт у `base_url` исправни за вашу мрежу (локална мрежа, Tailscale или VPS).

**Сесија престаје са радом близу ограничења контекста**
Експлицитно подесите `model_context_window` и `model_auto_compact_token_limit`. Погледајте горњу табелу прозора контекста.

**Сажимање се покреће прекасно**
Смањите `model_auto_compact_token_limit` на 80–85% прозора. Никада га не постављајте изнад 90%.

**Профил се не учитава (`-p <name>` се неприметно игнорише)**
Проверите да ли датотека постоји на путањи `~/.codex/<name>.config.toml` (без префикса `profile-`). Покрените `ls ~/.codex/*.config.toml`.

**Дуготрајни Codex задатак се прекида током извршавања / налози се мењају између корака**
Погледајте [Дуготрајни задаци](#long-running-tasks). Омогућите афинитет сесије (TTL дужи од трајања задатка) и повећајте или онемогућите `STREAM_IDLE_TIMEOUT_MS` / `FETCH_BODY_TIMEOUT_MS`. Претражите OmniRoute евиденције помоћу grep-а за `Idle timeout: no data from`.
