# open-sse Architecture (Српски)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Укратко**: `open-sse/` је основни механизам за стримовање који покреће сваки LLM захтев у OmniRoute-у. Садржи око 1.400 датотека које имплементирају ток обраде захтева, извршиоце, сервисе, MCP сервер и слој за превођење. Овај водич објашњава како се ти делови уклапају.

**Извор:** `open-sse/` (пакет радног простора, око 1.440 `.ts` датотека које нису тестови)

---

## Зашто засебан пакет радног простора?

`open-sse/` је **самосталан радни простор** у OmniRoute монорепозиторијуму из неколико разлога:

1. **Поновна употребљивост** — `open-sse` се објављује као `@omniroute/open-sse` на npm-у, па други пројекти могу да га користе независно
2. **Јасне границе** — механизам за стримовање је одвојен од слоја корисничког интерфејса/базе података специфичног за OmniRoute
3. **Перформансе** — механизам нема Next.js зависности, што омогућава брже хладно покретање у CLI/serverless окружењима
4. **Верзионисање** — `open-sse` може да објављује издања сопственим темпом

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Структура највишег нивоа

```
open-sse/
├── index.ts              # Јавна улазна тачка
├── types.d.ts            # Јавни извози типова
├── package.json          # @omniroute/open-sse
├── config/               # Конфигурације добављача, константе, регистри
├── executors/            # HTTP извршиоци по добављачу (67 + base.ts/index.ts)
├── handlers/             # Обрађивачи захтева (chatCore, responses итд.)
├── lib/                  # Интерни помоћни алати
├── mcp-server/           # Model Context Protocol сервер
├── services/             # Око 298 сервисних модула
├── transformer/          # Трансформатор формата Responses API-ја
├── translator/           # Превођење формата (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Дељени помоћни алати (евидентирање, грешке, стримови итд.)
```

### Број модула

| Директоријум | Датотеке | Намена |
| `executors/` | 167 | HTTP извршиоци по добављачу (обједињени преко DefaultExecutor фабрике) |
| `handlers/` | 157 | Улазне тачке захтева (chatCore, responses, embeddings) |
| `services/` | ~536 | Усмеравање, кеширање, ограничавање брзине, освежавање итд. |
| `translator/` | 56 | Конверзија формата (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP алати и транспорти |
| `utils/` | ~108 | Помоћни алати који обухватају више области (евидентирање, грешке, стримови) |
| `config/` | ~339 | Конфигурације добављача, константе, регистри |

---

## Ток обраде захтева

Сваки LLM захтев пролази кроз **ток обраде од 5 фаза**:

```
                              ┌──────────────┐
   HTTP захтев                 │  1. УСМЕРИ   │   разрешавање комбинације, избор модела
   (Next.js рута)              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. ПРЕВЕДИ  │   конверзија формата (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. ИЗВРШИ   │   извршилац добављача, HTTP, поновни покушај, прекидач
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. СТРИМУЈ  │   SSE трансформација, повратни притисак
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. ЗАБЕЛЕЖИ │   праћење употребе, дневник позива, класификација грешака
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP одговор (SSE или JSON)
```

### Фаза 1: Усмеравање (services/combo.ts)

**Улазна тачка**: `handleComboChat()` у `services/combo.ts`

Разрешава захтев у конкретну `(provider, model, account, credentials)` n-торку:

- Проналази комбинацију према ID-у (или гради виртуелну комбинацију за `auto/*` моделе)
- Примењује стратегију усмеравања (приоритетну, пондерисану, кружну итд.)
- Филтрира неисправне добављаче (прекидач кола)
- Бира следеће одрживо одредиште

За `auto/*` моделе, ова фаза такође:

- Покреће алгоритам **оцењивања на основу 16 фактора** (`services/autoCombo/`)
- Бира пар `provider+model` на основу исправности, трошкова, кашњења итд.

### Фаза 2: Превођење (translator/)

Ако се изворни формат (нпр. OpenAI) разликује од циљног формата (нпр. Claude), захтев се **преводи**:

- Системски промпт → системска порука
- Дефиниције алата → формат алата специфичан за добављача
- Параметри закључивања/размишљања → еквиваленти специфични за добављача
- Нормализација улога порука (`developer` → `system` за системе који нису OpenAI)

`translator/index.ts` излаже:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Фаза 3: Извршавање (executors/)

**Улазна тачка**: `getExecutor(providerId).execute(request, options)`

Сви добављачи користе `DefaultExecutor` (`executors/default.ts`) преко резервне опције фабрике `getExecutor()`. Извршилац:

- Гради URL узводног сервиса (`buildUrl()`)
- Додаје заглавља специфична за добављача (`buildHeaders()`)
- Трансформише тело захтева (`transformRequest()`)
- Шаље HTTP захтев уз поновне покушаје и експоненцијално повећавање паузе
- По потреби обавља освежавање аутентификације (OAuth добављачи)

Сви извршиоци проширују `BaseExecutor` (`executors/base.ts`, 1170 линија кода), који обезбеђује:

- Заједничку логику поновних покушаја
- Интеграцију са проксијем
- Интеграцију са прекидачем кола
- Куке за евидентирање употребе

### Фаза 4: Стримовање (utils/stream.ts)

За стримоване одговоре, извршилац враћа **ReadableStream**. Обрађивач:

- Прослеђује податке кроз SSE трансформацију (`createSSETransformStreamWithLogger`)
- Примењује heartbeat пингове ради откривања прекинутих веза
- Коректно обрађује прекид везе са клијентом (`pipeWithDisconnect`)
- Трансформише SSE → JSON за клијенте који не користе стримовање

За одговоре који се не стримују, извршилац враћа рашчлањени JSON објекат који се прослеђује без измена.

### Фаза 5: Евидентирање (services/usage.ts)

Након одговора (успешног или неуспешног), бележе се подаци о коришћењу:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` из одговора
- `cost_usd` израчунат на основу података о ценама
- `latency_ms`, `status`, `error_class` у случају неуспеха
- Чувају се у табели `usage_history`

Артефакти евиденције позива (ако су омогућени) уписују се у `${DATA_DIR}/call_logs/`.

---

## Детаљна анализа кључних датотека

### chatCore.ts (5977 линија)

**Главни обрађивач захтева**. Упркос величини, има јасну структуру:

```ts
// Псеудоструктура датотеке chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Аутентификација + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Валидација тела
  const body = await parseRequestBody(request);

  // 3. Откривање формата + превођење
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Комбиновано усмеравање
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Настави са следећим циљем
    }
  }

  // 5. Резервна опција за хитне случајеве
  return await emergencyFallback(body);
}
```

Упркос томе што је у питању једна огромна функција, организована је у **одељке са коментарима** који одговарају процесу од 5 фаза.

### combo.ts (4456 линија кода)

**Механизам за усмеравање** који комбиновану конфигурацију разрешава у уређену листу циљева.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Подржава **19 стратегија усмеравања** (погледајте `src/shared/constants/routingStrategies.ts`):

| Стратегија          | Понашање                                                                     |
| ------------------- | ---------------------------------------------------------------------------- |
| `priority`          | Уређена листа у којој први циљ има предност                                  |
| `weighted`          | Вероватносни избор према тежини сваког циља                                  |
| `round-robin`       | Кружно пролази кроз циљеве по редоследу                                      |
| `context-relay`     | Прослеђује контекст између циљева                                            |
| `fill-first`        | Попуњава квоту пре преласка на следећи циљ                                   |
| `p2c`               | Моћ избора између две опције                                                 |
| `random`            | Равномерни насумични избор                                                   |
| `least-used`        | Бира онај са најмање недавних коришћења                                      |
| `cost-optimized`    | Прво бира најјефтинији исправан циљ                                          |
| `reset-aware`       | Узима у обзир периоде ресетовања добављача                                   |
| `reset-window`      | Усмеравање засновано на периоду ресетовања                                   |
| `headroom`          | Прво бира циљ са највећом преосталом резервом квоте                          |
| `strict-random`     | Заиста равномерно (без пондерисања према квалитету)                          |
| `auto`              | Користи оцењивање на основу 16 фактора (`autoCombo/`)                        |
| `lkgp`              | Прво бира последњег познатог исправног добављача                             |
| `context-optimized` | Најбоље за захтеве са дугачким контекстом                                    |
| `fusion`            | Паралелно шаље панелу, а затим синтетише резултат преко судије (`fusion.ts`) |

### base.ts (1170 линија кода)

**Апстрактни извршилац** који наслеђује свих 107 извршилаца. Садржи:

- `buildUrl()` — подразумевано конструисање URL-а (поткласе га замењују прилагођеном имплементацијом)
- `buildHeaders()` — подразумевана заглавља (аутентификација, тип садржаја)
- `transformRequest()` — подразумевано прослеђивање без измена
- `execute()` — главна HTTP петља са поновним покушајима, одлагањем и прекидачем

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Обрађује све добављаче компатибилне са OpenAI/Anthropic
  // Добављачи региструју конфигурације (URL, аутентификацију, заглавља), али деле логику извршиоца
}
```

Понашање специфично за добављача (заглавља за аутентификацију, основни URL, заглавља верзије) конфигурише се преко регистра добављача, а не преко засебних класа извршилаца.

````

---

## Сервиси (117 модула)

Сервиси су **усмерени модули са једном наменом** које руковаоци комбинују. Главне категорије су:

### Усмеравање и комбиновање

- `combo.ts` — улазна тачка за захтеве са комбинованим усмеравањем
- `services/autoCombo/` — бодовање на основу 16 фактора, 8 стратегија аутоматског усмеравања
- `wildcardRouter.ts` — подудара руте са џокер-знацима (`gpt-*`)
- `modelFamilyFallback.ts` — T5 резервни избор унутар породице

### Ограничавање брзине и квоте

- `rateLimitManager.ts` — канта токена по кључу и добављачу
- `usage.ts` — евидентирање употребе
- `quotaCache.ts` — снимци квота у меморији

### Налог и токен

- `tokenRefresh.ts` — OAuth освежавање при одговору 401
- `accountFallback.ts` — прелазак на алтернативни налог
- `sessionManager.ts` — стање сесије са више размена

### Интелигенција

- `intentClassifier.ts` — класификује намеру захтева
- `taskAwareRouter.ts` — усмерава према типу задатка
- `thinkingBudget.ts` — додељује токене за размишљање
- `contextManager.ts` — умеће контекст усмеравања

### Отпорност

- `resilience.ts` — оркестрација поновних покушаја, одлагања и прекидача
- `emergencyFallback.ts` — крајњи резервни избор
- `modelDeprecation.ts` — аутоматски усмерава на моделе наследнике

### Стање

- `signatureCache.ts` — уклањање дупликата према потпису захтева
- `volumeDetector.ts` — растерећење оптерећења
- `contextHandoff.ts` — серијализација сесије

### Компресија

- `compression/` (потдиректоријум) — комплетан ток обраде компресије
- 39 датотека које обухватају механизме, пакете правила и адаптере

### Вештине

- (обрађено у [SKILLS.md](./SKILLS.md))

### Меморија

- (обрађено у [MEMORY.md](./MEMORY.md))

---

## Извршиоци (75+ датотека)

По једна датотека за сваког добављача. Сви проширују `BaseExecutor` и замењују оно што се разликује.

### Уобичајени обрасци

Добављачи се разрешавају помоћу `getExecutor(providerId)`, који враћа конфигурисани извршилац. Добављачи компатибилни са OpenAI/Anthropic користе `DefaultExecutor` (`executors/default.ts`). Понашање специфично за добављача (основни URL, заглавља за аутентификацију, верзија API-ја) конфигурише се у `open-sse/config/providers/`, док се трансформације тела захтева обављају у `open-sse/translator/`.

**Прилагођени URL** поставља се путем конфигурације добављача:

```ts
// Конфигурација добављача у open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Прилагођена аутентификација** обавља се путем конфигурације аутентификације у регистру добављача (API кључ, OAuth, профили заглавља).

**Прилагођене трансформације тела захтева** (нпр. када Anthropic издваја `system` из `messages`) региструју се засебно за сваког добављача у `open-sse/translator/`.

````

### Фабрика извршилаца

`executors/index.ts` извози `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Разрешавање се обавља путем `ExecutorRegistry` (`executors/registry.ts`): сваки специјализовани извршилац декларисан је у уграђеној табели датотеке `executors/index.ts` и региструје се помоћу `registerExecutor(alias, instance)` при учитавању модула; `getExecutor()` претражује регистар и, за сваког добављача без специјализованог уноса, као резерву користи мемоизовани `DefaultExecutor`. Комплетно мапирање псеудоним → извршилац описано је референтним тестом `tests/unit/executor-map-golden.test.ts`.

---

## Преводиоци

Превођење између **3 формата**: OpenAI, Anthropic, Gemini, као и новог Responses API-ја.

### Када се превођење обавља

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Уобичајена превођења:

- `OpenAI → Anthropic`: засебно поље `system`, заглавље `x-api-key`
- `OpenAI → Gemini`: `contents` уместо `messages`, `systemInstruction`
- `OpenAI → Responses API`: низ `input`, стање `previous_response_id`

### Обрађени рубни случајеви

- улога `developer` → `system` за системе који нису OpenAI
- улога `system` → спаја се са првом корисничком поруком за GLM/ERNIE
- `json_schema` → Gemini-јеви `responseMimeType` + `responseSchema`
- `tools` → формат алата специфичан за провајдера
- Параметри размишљања (o1, Claude) → еквиваленти специфични за провајдера

---

## MCP сервер

`open-sse/mcp-server/` имплементира сервер **Model Context Protocol**:

- **110 алата** (управљање провајдерима, комбинације, меморија, кеш, компресија, прокси, вештине, гејмификација, додаци, Notion, Obsidian, локални корпус)
- **3 транспорта**: stdio, SSE, Streamable HTTP
- **33 опсега** за прецизну ауторизацију

### Регистрација алата

Алатке се региструју као самосталне датотеке у `open-sse/mcp-server/tools/`, при чему свака извози назив, шему, обрађивач и опсег:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Транспорти

```ts
// stdio (употреба преко CLI-ја)
startMcpStdio(server);

// SSE (стримовање засновано на HTTP-у)
startMcpSse(server, port);

// Streamable HTTP (савремени MCP)
startMcpStreamable(server, port);
```

### Ауторизација

Сваки позив алата пролази кроз провере опсега (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Трансформатори

`open-sse/transformer/` конвертује између формата **Chat Completions** и **Responses API**.

### Зашто засебан трансформатор?

Responses API је нови OpenAI формат са **разговорима са стањем** (`previous_response_id`). Када клијент пошаље Responses захтев, OmniRoute:

1. Интерно конвертује Responses → Chat Completions
2. Шаље провајдеру (било ком провајдеру који подржава Chat Completions)
3. Конвертује одговор назад у Responses формат
4. Стримује конвертовани одговор клијенту

Трансформатор (`transformer/responsesTransformer.ts`) обезбеђује:

```ts
createResponsesApiTransformStream(): TransformStream
```

Овим се обрађују:

- догађаји `response.output_item.added`
- догађаји `response.output_text.delta`
- догађај `response.completed`
- Мапирање позива алата (`function_call` ↔ `tool_calls`)

---

## Конфигурација

`open-sse/config/` садржи конфигурациони слој:

| Датотека                      | Намена                                                   |
| ----------------------------- | -------------------------------------------------------- |
| `providerRegistry.ts`         | Регистар модела за ћаскање из каталога са 352 провајдера |
| `providerModels.ts`           | Алијаси модела, мапирање формата                         |
| `constants.ts`                | Временска ограничења, лимити, статусни кодови            |
| `defaultThinkingSignature.ts` | Подразумевани Claude потпис размишљања                   |
| `modelStrip.ts` (у сервисима) | Уклањање поља по провајдеру                              |

### Шема регистра провајдера

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Zod валидација при учитавању модула осигурава да су све конфигурације провајдера важеће.

---

## Ограничења перформанси

Механизам за усмеравање има строга ограничења перформанси:

| Операција                                                | Циљ   | Мерење                        |
| -------------------------------------------------------- | ----- | ----------------------------- |
| Разрешавање комбинације                                  | <10ms | За 50 одредишта               |
| Провера ограничења брзине                                | <1ms  | Токенски контејнер у меморији |
| Резервни избор породице модела                           | <5ms  | Кеширане дефиниције породица  |
| Прослеђивање захтева на обраду                           | <2ms  | Критична путања               |
| **Без блокирајућег I/O-а у критичној путањи усмеравања** | —     | Све је асинхроно              |

---

## Антиобрасци

❌ **Синхрони позиви бази података у `combo.ts`** — унапред израчунајте и кеширајте
❌ **Логика поновних покушаја у обрађивачима** — користите `retry()` из сервиса за отпорност
❌ **Директан приступ конфигурацији добављача** — користите getter функције из `providerRegistry`
❌ **Хардкодирани ланци резервних избора** — дефинишите их у `modelFamilyFallback.ts`
❌ **Промене стања кроз конкурентне захтеве** — користите искључиво контекст ограничен на захтев

---

## Додавање нове компоненте

### Додавање новог сервиса

1. Креирајте `open-sse/services/[serviceName].ts` са јасно усмереном одговорношћу
2. Извезите главну функцију обрађивача и све константе
3. Додајте јединичне тестове у `tests/unit/services/[serviceName].test.mjs`
4. Интегришите у ток обраде захтева у `handlers/chatCore.ts` (ако је повезано са усмеравањем)
5. Ажурирајте логику усмеравања у `combo.ts` ако сервис утиче на избор одредишта
6. Документујте у овој датотеци

### Додавање новог извршиоца

1. Креирајте `open-sse/executors/[provider].ts` који проширује `BaseExecutor`
2. Региструјте у `config/providerRegistry.ts`
3. Додајте у фабрику `executors/index.ts`
4. Додајте јединичне тестове за извршиоца
5. Документујте у `docs/architecture/ARCHITECTURE.md`

### Додавање новог MCP алата

1. Креирајте или ажурирајте `open-sse/mcp-server/tools/[category]Tools.ts`
2. Дефинишите Zod шему за улазне податке
3. Региструјте алат у `mcp-server/index.ts`
4. Додајте у матрицу опсега у `mcp-server/auth/`
5. Додајте јединичне тестове

---

## Такође погледајте

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — архитектура високог нивоа
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — инжењерска референца
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — преглед директоријум по директоријум
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — оцењивање на основу 16 фактора
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP сервер
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A сервер
- Изворни код: `open-sse/` (400+ датотека, ~143K линија кода)
