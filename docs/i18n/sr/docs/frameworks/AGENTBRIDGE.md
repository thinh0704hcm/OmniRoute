# AgentBridge (Српски)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENTBRIDGE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENTBRIDGE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENTBRIDGE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENTBRIDGE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENTBRIDGE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENTBRIDGE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENTBRIDGE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENTBRIDGE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENTBRIDGE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENTBRIDGE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENTBRIDGE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENTBRIDGE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENTBRIDGE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENTBRIDGE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENTBRIDGE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENTBRIDGE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENTBRIDGE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENTBRIDGE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENTBRIDGE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENTBRIDGE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENTBRIDGE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENTBRIDGE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENTBRIDGE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENTBRIDGE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENTBRIDGE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENTBRIDGE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENTBRIDGE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENTBRIDGE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENTBRIDGE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENTBRIDGE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENTBRIDGE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENTBRIDGE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENTBRIDGE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENTBRIDGE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENTBRIDGE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENTBRIDGE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENTBRIDGE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENTBRIDGE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENTBRIDGE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENTBRIDGE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENTBRIDGE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENTBRIDGE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENTBRIDGE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENTBRIDGE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENTBRIDGE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENTBRIDGE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENTBRIDGE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENTBRIDGE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENTBRIDGE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENTBRIDGE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENTBRIDGE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENTBRIDGE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENTBRIDGE.md)

---

AgentBridge је OmniRoute-ов MITM (Man-in-the-Middle) прокси који пресреће HTTPS саобраћај IDE AI агената и преусмерава га кроз OmniRoute-ов обједињени механизам за рутирање. Подржава **10 IDE агената** — Antigravity, Kiro, GitHub Copilot, GHE Copilot, OpenAI Codex, Cursor, Zed, Claude Code, Open Code и Trae (у фази истраживања) — што чини OmniRoute MITM проксијем са најширом подршком за AI асистенте за програмирање на тржишту.

**Локација контролне табле:** `/dashboard/tools/agent-bridge`
**Група на бочној траци:** Алатке (после Cloud Agents)
**Погледајте и:** [`TRAFFIC_INSPECTOR.md`](./TRAFFIC_INSPECTOR.md) — надгледајте сав пресретнути саобраћај у реалном времену; `docs/security/MITM-TPROXY-DECRYPT.md` (git; није компајлирано у `/docs`) — Linux TPROXY режим снимања са транспарентним дешифровањем, којим управља рута `/api/tools/agent-bridge/tproxy`.

---

## §1 Преглед

### Шта је AgentBridge?

Када IDE агент (нпр. GitHub Copilot, Cursor, Claude Code) упути API позив, он се директно повезује са надређеним AI добављачем (OpenAI, Anthropic итд.). AgentBridge транспарентно пресреће ту везу на TLS нивоу — без потребе за било каквом променом конфигурације агента — и преусмерава захтев кроз OmniRoute.

То значи да можете:

- **Преусмерити било ког агента ка било ком добављачу**: Copilot комуницира са OpenAI-јем? Преусмерите га на Anthropic Claude, Gemini или било ког од 352 OmniRoute добављача.
- **Применити мапирања модела**: `gemini-3-flash` → `claude-sonnet-4.7` транспарентно на нивоу руковаоца.
- **Надгледати сав саобраћај агената**: сваки пресретнути захтев објављује се у [Инспектору саобраћаја](./TRAFFIC_INSPECTOR.md).
- **Применити OmniRoute механизме отпорности**: комбиновано рутирање, аутоматски прекидачи, резервне опције и праћење трошкова функционишу и за саобраћај IDE агената.

### Позиционирање у односу на тржиште

| Функционалност                          | 9router | anti-api | llm-interceptor | **OmniRoute AgentBridge** |
| --------------------------------------- | :-----: | :------: | :-------------: | :-----------------------: |
| Antigravity                             |    ✓    |    ✓     |        —        |             ✓             |
| GitHub Copilot                          |    ✓    |    ✓     |        —        |             ✓             |
| Kiro (AWS)                              |    ✓    |    ✓     |        —        |             ✓             |
| OpenAI Codex                            |    —    |    ✓     |        —        |             ✓             |
| Cursor IDE                              |    ✓    |    ✓     |        —        |             ✓             |
| Zed Industries                          |    —    |    ✓     |        —        |             ✓             |
| Claude Code                             |    —    |    —     |        ✓        |             ✓             |
| Open Code                               |    —    |    —     |        ✓        |             ✓             |
| Trae                                    |    —    |    —     |        —        |      🔍 Истражује се      |
| Кориснички интерфејс контролне табле    |    ✓    |    ✗     |        ✗        |             ✓             |
| Инспектор саобраћаја                    |    ✗    |    ✗     |        ✓        |             ✓             |
| OmniRoute рутирање                      |    ✗    |    ✗     |        ✗        |             ✓             |
| Кориснички интерфејс за мапирање модела |    ✗    |    ✗     |        ✗        |             ✓             |
| Листа заобилажења                       |    ✗    |    ✗     |        ✓        |             ✓             |
| Сертификат надређеног CA-а              |    ✗    |    ✗     |        ✓        |             ✓             |

---

## §2 Архитектура

### 2.1 Преглед компоненти

```
IDE агент (VS Code / Cursor / итд.)
    │  HTTPS (порт 443)
    ▼
/etc/hosts — 127.0.0.1 api.githubcopilot.com   ← DNS преусмеравање
    │
    ▼
src/mitm/server.cjs  (порт 443, CJS подређени процес)
    │  разрешава одредиште према SNI-ју из Host заглавља
    │  генерише TLS сертификат за сваки SNI, потписан AgentBridge CA сертификатом
    ├── Подударање са листом заобилажења? → TCP директно прослеђивање (без дешифровања)
    ├── Подударање са одредиштем? → fetch → OmniRoute рутер (порт 20128)
    │       └── handler.intercept() — TypeScript
    │               ├── maskSecrets() над телом/заглављима захтева
    │               ├── TrafficBuffer.push() — објављује у Инспектор саобраћаја
    │               └── fetchRouter() → /v1/chat/completions
    └── Нема подударања? → TCP директно прослеђивање (без дешифровања)
```

### 2.2 MITM сервер (`src/mitm/server.cjs`)

Основни MITM сервер ради као Node.js CJS подређени процес (да би се избегло преписивање постојеће CJS базе кода). Он:

- Ослушкује порт 443 (захтева привилегију или `authbind`/`setcap`)
- Прима CONNECT тунеле од оперативног система (преко DNS преусмеравања у `/etc/hosts`)
- Генерише TLS сертификате за сваки SNI, потписане AgentBridge CA сертификатом (`DATA_DIR/mitm/ca.crt`)
- Разрешава одредишног агента према Host заглављу преко регистра `targets/index.ts`
- Прослеђује TypeScript слоју руковаоца путем HTTP-а на `http://127.0.0.1:20128`

`TARGET_HOSTS` се учитава из `DATA_DIR/mitm/targets.json` (који `targets/index.ts` уписује при покретању), што омогућава динамичка ажурирања без поновног покретања CJS сервера.

> **Модел коренског CA сертификата (#6684).** Горе описани модел сертификата за сваки SNI, потписаног CA сертификатом,
> јесте постојани модел коренског CA сертификата додат у #6684 (`src/mitm/cert/rootCa.ts` +
> `src/mitm/_internal/rootCaShim.cjs`, који поново користи CA/leaf криптографију већ
> доказану за TPROXY у `src/mitm/tproxy/dynamicCert.ts`) — он замењује
> старији појединачни статички самопотписани leaf сертификат (`src/mitm/cert/generate.ts`, који је и даље
> ограничен само на antigravity хостове), на који указује постојање само пара `server.crt`/`server.key`
> на диску. **Понашање при миграцији**: нова инсталација (без претходног
> `server.crt`) аутоматски добија модел коренског CA сертификата; инсталација која већ
> верује старом статичком leaf сертификату наставља да га користи док оператер не постави
> `MITM_ROOT_CA_ENABLED=true` и поново покрене мост (`src/mitm/cert/migration.ts`
> је чиста функција одлучивања — поуздани MITM CA сертификат који може да потпише leaf сертификат за
> **било који** хост знатно је моћнији од старог leaf сертификата са фиксним SAN-ом, па се
> прелаз никада не обавља неприметно за инсталацију којој се већ верује). CA сертификат се инсталира
> у исту `omniroute-mitm.crt` позицију складишта поверења коју је користио стари leaf сертификат
> (`cert/install.ts::installCaCert`) — није потребно чишћење двоструког поверења.

### 2.3 Основа руковаоца (`src/mitm/handlers/base.ts`)

Сви руковаоци агената проширују `MitmHandlerBase`:

```ts
export abstract class MitmHandlerBase {
  abstract readonly agentId: AgentId;

  abstract intercept(
    req: IncomingMessage,
    res: ServerResponse,
    body: Buffer,
    mappedModel: string
  ): Promise<void>;

  // Заштићене помоћне методе: fetchRouter, pipeSSE, hookBufferStart, hookBufferUpdate
}
```

Сваки руковалац позива `hookBufferStart()` пре проксирања и `hookBufferUpdate()` по завршетку. Ове методе додају `InterceptedRequest` уносе у `globalTrafficBuffer` (погледајте [Инспектор саобраћаја](./TRAFFIC_INSPECTOR.md) §4).

### 2.4 Регистар одредишта (`src/mitm/targets/`)

Сваки агент има декларативну датотеку одредишта:

```ts
// src/mitm/targets/copilot.ts
export const COPILOT_TARGET: MitmTarget = {
  id: "copilot",
  name: "GitHub Copilot",
  hosts: ["api.githubcopilot.com", "copilot-proxy.githubusercontent.com"],
  port: 443,
  endpointPatterns: ["/chat/completions", "/v1/chat/completions"],
  defaultModels: [{ id: "gpt-4o", name: "GPT-4o", alias: "gpt-4o" }],
  handler: () => import("../handlers/copilot"),
  riskNoticeKey: "providers.riskNotice.oauth",
};
```

Регистар (`targets/index.ts`) извози `ALL_TARGETS` и генерише `DATA_DIR/mitm/targets.json` при покретању.

### 2.5 Директно прослеђивање и листа заобилажења (`src/mitm/passthrough.ts`)

**Листа заобилажења** (проверава се прва и има предност над подударањем са одредиштем):

- Подразумевани обрасци: хостови банака, `.gov.`, OAuth/SSO добављачи (Okta, Auth0), итд.
- Кориснички обрасци: чувају се у табели базе података `agent_bridge_bypass`
- Заобиђени хостови добијају транспарентни TCP тунел — TLS се **никада не дешифрује**

**Подразумевано директно прослеђивање** (нема подударања са одредиштем и није на листи заобилажења):

- Такође добија TCP тунел — везе се никада не прекидају
- Спречава AgentBridge да омета општи системски HTTPS саобраћај

Приоритет рутирања:

```
листа заобилажења → подударање са одредиштем → директно прослеђивање
```

### 2.6 Узводни CA сертификат (`src/mitm/upstreamTrust.ts`)

За корпоративна мрежна окружења са прилагођеним CA сертификатом:

```bash
AGENTBRIDGE_UPSTREAM_CA_CERT=/path/to/corporate-ca.pem
```

Када је постављено, конфигурише глобални диспечер пакета `undici` са додатним CA сертификатом, омогућавајући AgentBridge-у да приступи узводним добављачима преко корпоративних проксија за завршавање TLS-а.

### 2.7 Маскирање тајни (`src/mitm/maskSecrets.ts`)

Независни clean-room скенер примењује се на тела захтева и заглавља са акредитивима
**пре** него што уђу у бафер Инспектора саобраћаја или било који дневник. Он обавља један линеарни пролаз:

- Токени са префиксима `sk-` / `ak-` / `pk-` (у стилу OpenAI/Anthropic)
- RFC 6750 акредитиви `Authorization: Bearer <token>`, са приоритетом целог токена
- Генерички дуги непрозирни токени (≥40 знакова), укључујући облике са тачкама и допунама

`sanitizeHeaders()` претвара задржана имена у мала слова, детерминистички спаја вредности низова, одбацује
заједничку листу забрањених hop-by-hop/оквирних заглавља (укључујући прокси аутентификацију), у потпуности редигује `cookie` и
`set-cookie` и прослеђује вредности акредитива скенеру.

---

## §3 Подешавање

### 3.1 Покретање/заустављање MITM сервера

Користите картицу AgentBridge сервера на `/dashboard/tools/agent-bridge`:

| Радња                          | Опис                                                                              |
| ------------------------------ | --------------------------------------------------------------------------------- |
| Покрени сервер                 | Покреће `src/mitm/server.cjs` на порту 443                                        |
| Заустави сервер                | Контролисано зауставља подређени процес                                           |
| Поново покрени сервер          | Заустављање + покретање (примењује измене циљева)                                 |
| Потврди поузданост сертификата | Инсталира `DATA_DIR/mitm/ca.crt` у складиште поузданих сертификата ОС-а           |
| Преузми сертификат             | Преузима `ca.crt` ради ручне инсталације                                          |
| Поново генериши сертификат     | Креира нови пар CA кључева (сви постојећи сертификати по агенту постају неважећи) |

### 3.2 Потврда поузданости сертификата

Оперативни систем мора сматрати AgentBridge CA сертификат поузданим да би IDE окружења прихватила MITM везу.

**Linux (NSS — Chrome/Firefox):**

```bash
certutil -A -d sql:$HOME/.pki/nssdb -n "OmniRoute AgentBridge" -t CT,, -i ~/.omniroute/mitm/ca.crt
```

**macOS (Keychain):**

```bash
sudo security add-trusted-cert -d -r trustRoot \
  -k /Library/Keychains/System.keychain ~/.omniroute/mitm/ca.crt
```

**Windows (certmgr):**

```powershell
certutil -addstore -f Root $env:USERPROFILE\.omniroute\mitm\ca.crt
```

Или користите дугме „Потврди поузданост сертификата“ на контролној табли (покреће одговарајућу команду за ваш ОС, уз sudo упит ако је потребно).

#### IDE окружења заснована на Electron-у игноришу складиште поузданих сертификата ОС-а (`NODE_EXTRA_CA_CERTS`)

Нека IDE окружења — пре свега **Antigravity IDE**, као и друге апликације изведене из Electron-а / VS Code-а — садрже
сопствено Node.js окружење за извршавање које **не користи складиште поузданих сертификата ОС-а** за одлазне
`fetch`/HTTPS захтеве. Потврђивање поузданости CA сертификата на нивоу ОС-а/NSS-а довољно је за изворни **позадински систем**
IDE окружења (нпр. Go језички сервер, који користи CA пакет ОС-а), али ће **Electron кориснички интерфејс**
и даље пријављивати TLS грешку — што се манифестује као да је корисник _одјављен_ из апликације или као _„грешка при повезивању“_,
иако MITM евиденција показује да почетни позиви позадинског система враћају `200`. Потребна су
два корака и оба су важна:

1. Експлицитно усмерите окружење за извршавање на CA сертификат:
   ```bash
   export NODE_EXTRA_CA_CERTS=/path/to/omniroute-agentbridge-ca.crt
   ```
2. **Покрените IDE из тог терминала.** Покретање преко иконе на радној површини / Dock-а / менија Start
   **не** наслеђује променљиве извезене из терминала, док се `~/.config/environment.d/*.conf` примењује тек након
   новог графичког пријављивања. Најпре у потпуности затворите IDE — Electron-ово закључавање једне инстанце значи да друго
   покретање само поставља фокус на постојећи процес, па се ново окружење игнорише.

Претходно описани корак подешавања поверења на нивоу ОС-а + NSS-а и даље је неопходан (Chromium мрежни стек који користе неки токови
аутентификације чита корисничко NSS складиште и има сопствене статичке везе за `*.googleapis.com` које
локално поуздан CA сертификат замењује). `NODE_EXTRA_CA_CERTS` додатно покрива Node `fetch` путању.

### 3.3 DNS усмеравање

За сваки агент који желите да пресрећете, његови API хостови морају да се разрешавају на `127.0.0.1`. AgentBridge аутоматски управља уносима у `/etc/hosts` када укључите или искључите DNS за агента у чаробњаку за подешавање.

Пример уноса у `/etc/hosts` за GitHub Copilot:

```
127.0.0.1 api.githubcopilot.com
127.0.0.1 copilot-proxy.githubusercontent.com
```

### 3.4 Мапирање модела

Користите табелу за мапирање модела у картици сваког агента да бисте дефинисали мапирања извор → циљ:

| Изворни модел (матични за агента) | Циљни модел (OmniRoute) |
| --------------------------------- | ----------------------- |
| `gpt-4o`                          | `claude-sonnet-4.7`     |
| `*` (џокер)                       | `claude-haiku-4.7`      |

Џокер `*` мапира сваки непрепознати модел на наведени циљ. Подаци се трајно чувају у табели `agent_bridge_mappings`.

> **Савет — откријте стварне ID-ове модела агента.** IDE може да шаље називе модела који се разликују од
> ознака у његовом корисничком интерфејсу и који се мењају између главних верзија. На пример, **Antigravity 2** преко мреже шаље
> `gemini-3.1-pro-low`, `gemini-pro-agent` и `gemini-3.1-flash-lite` — а не
> `gemini-2.5-pro` приказан у старијој документацији. Пошаљите једну поруку у ћаскању без одговарајућег мапирања: MITM
> бележи тачну долазну вредност `model:` и прослеђује захтев. Мапирајте ту дословну вредност, након чега ће
> следећи захтев бити пресретнут и усмерен ка вашем циљу.

### 3.5 Обавештење о ризику

AgentBridge пресреће акредитиве (OAuth токене, API кључеве) које IDE користи за аутентификацију код узводних добављача. Они су **маскирани пре евидентирања** (погледајте §2.7), али су видљиви MITM слоју OmniRoute-а. При првом активирању сваког агента приказује се модални прозор са обавештењем о ризику који се може затворити.

### 3.6 Одржавање и дијагностика

Контролна табла садржи картицу **Одржавање и дијагностика** (`AgentBridgeMaintenanceCard`, у `src/app/(dashboard)/dashboard/tools/agent-bridge/components/`) која приказује оперативне MITM руте које раније нису имале кориснички интерфејс. Њен поднаслов гласи: _„Самостално тестирајте процес прикупљања, поништите преостало стање система и пренесите своје подешавање са једног рачунара на други.“_ Клијентске помоћне функције картице налазе се у `src/lib/inspector/agentBridgeMaintenanceApi.ts`.

| Дугме                    | Рута                                   | Шта ради                                                                                                                                                                                                |
| ------------------------ | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Дијагностикуј**        | `GET /api/tools/agent-bridge/diagnose` | Покреће самотестирање процеса снимања и приказује извештај за сваку проверу (✓/✗ + савет за отклањање проблема).                                                                                        |
| **Поправи**              | `POST /api/tools/agent-bridge/repair`  | Поништава заостало MITM стање система (DNS spoof уносе, коренски CA, системски proxy) преостало након пада или сигнала SIGKILL. Идемпотентно — пријављује „Нема шта да се поправи“ када је стање чисто. |
| **Уклони CA**            | `DELETE /api/tools/agent-bridge/cert`  | Укида поверење и уклања MITM коренски CA из складишта поверења ОС-а (експлицитно, идемпотентно). Приказује се само када је CA тренутно поуздан; захтева непосредну потврду „Уклонити CA?“.              |
| **Извези конфигурацију** | `GET /api/tools/agent-bridge/config`   | Преузима преносиву JSON конфигурацију (погледајте §3.7).                                                                                                                                                |
| **Увези конфигурацију**  | `POST /api/tools/agent-bridge/config`  | Отпрема претходно извезену JSON конфигурацију (погледајте §3.7).                                                                                                                                        |

**Дијагностичке провере** (`summarizeDiagnostics()` у `src/mitm/inspector/diagnostics.ts`). Рута покреће пробу са споредним ефектима за сваку проверу и прослеђује логичке вредности чистом сажимачу; враћају се јединствени исход `healthy` и савет за сваки неуспех:

| Назив провере      | Шта проверава                                         | Савет у случају неуспеха                                                                                                                                    |
| ------------------ | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server-running`   | Процес MITM сервера је активан                        | „MITM сервер није покренут. Покрените га са картице AgentBridge.“                                                                                           |
| `server-reachable` | MITM сервер прихвата везе на свом порту (TCP провера) | „MITM сервер не прихвата везе на свом порту. Проверите да ли је порт слободан и да ли имате привилегије да га повежете.“                                    |
| `cert-exists`      | MITM сертификат је генерисан на диску                 | „MITM сертификат још није генерисан. Генеришите га са картице AgentBridge.“                                                                                 |
| `cert-trusted`     | MITM коренски CA се налази у складишту поверења ОС-а  | „Складиште ОС-а не верује MITM коренском CA-у, па TLS пресретање неће успети. Означите сертификат као поуздан са картице AgentBridge.“                      |
| `dns-configured`   | Циљна имена домаћина су лажирана у `/etc/hosts`       | „Циљна имена домаћина нису лажирана у /etc/hosts, па саобраћај никада не стиже до proxy сервера. Омогућите DNS за агенте чији саобраћај желите да снимате.“ |

**Банер за заостало стање:** када страница открије стање преостало након пада (DNS spoof / CA / системски proxy), картица приказује банер боје ћилибара — _„Претходна сесија је оставила системско стање (DNS spoof, CA или системски proxy). Покрените Поправку да бисте га очистили.“_ — и истиче дугме **Поправи**. `Repair` је еквивалент ProxyBridge-ове опције `--cleanup` на нивоу апликације (делегира функцији `repairMitm()` у `src/mitm/manager.ts`).

> MITM коренски CA остаје инсталиран између заустављања и покретања како би се избегли поновљени sudo
> упити (исто понашање као код mitmproxy/Charles), па је његово уклањање експлицитна радња
> **Уклони CA**, уместо нечега што се аутоматски дешава при заустављању.

### 3.7 Увоз/извоз преносиве конфигурације

AgentBridge може да серијализује стање **које оператер може да подешава** у верзионирани JSON објекат како би се поставка могла реплицирати на више машина. Серијализатор је `src/lib/inspector/configPortability.ts` (`exportConfig()` / `importConfig()`), а валидацију обавља `AgentBridgeConfigSchema`.

Извоз обухвата тачно три ставке (уграђене подразумеване вредности намерно се **НЕ** извозе, тако да их увоз никада не дуплира нити долази у сукоб са њима):

| Поље             | Извор                                                               | Напомене                                                                 |
| ---------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `bypassPatterns` | кориснички дефинисани обрасци заобилажења (`agent_bridge_bypass`)   | подразумевани обрасци за банке/државне институције/okta су изузети       |
| `customHosts`    | прилагођени домаћини Traffic Inspector-а (`inspector_custom_hosts`) | сваки: `{ host, kind: "llm"\|"app"\|"custom", label? }`                  |
| `agentMappings`  | мапирања модела по агенту (`agent_bridge_mappings`)                 | `{ [agentId]: [{ source, target }] }` за сваког агента који има мапирања |

```jsonc
// GET /api/tools/agent-bridge/config
{
  "version": 1,
  "bypassPatterns": ["*.internal.example.com"],
  "customHosts": [{ "host": "api.example.com", "kind": "llm", "label": null }],
  "agentMappings": {
    "copilot": [{ "source": "gpt-4o", "target": "claude-sonnet-4.7" }],
  },
}
```

**Понашање при увозу** (`POST /api/tools/agent-bridge/config`): обрасци заобилажења и мапирања по агенту се **у потпуности замењују**; прилагођени домаћини се додају **идемпотентно** (`INSERT OR IGNORE`). Одговор наводи колико је ставки сваке врсте примењено:

```jsonc
{ "ok": true, "bypassPatterns": 1, "customHosts": 1, "agents": 1 }
```

Шта **НИЈЕ** у конфигурацији: стање покренутости сервера, путање до сертификата, DNS стање по агенту, путања до изворног CA сертификата и TPROXY подешавања — то су стања хоста/окружења за извршавање, а не преносива подешавања.

---

## §4 Референца по агентима

| #   | Агент              | Статус          | Пресретнути хостови                                                | Тип аутентификације |
| --- | ------------------ | --------------- | ------------------------------------------------------------------ | ------------------- |
| 1   | **Antigravity**    | ✅ Подржано     | `daily-cloudcode-pa.googleapis.com`, `cloudcode-pa.googleapis.com` | Firebase OAuth      |
| 2   | **Kiro (AWS)**     | ✅ Подржано     | `prod.kiro.aws`, `dev.kiro.aws`                                    | AWS SigV4           |
| 3   | **GitHub Copilot** | ✅ Подржано     | `api.githubcopilot.com`, `copilot-proxy.githubusercontent.com`     | GitHub OAuth        |
| 4   | **OpenAI Codex**   | ✅ Подржано     | `api.openai.com` (Codex путање), `chatgpt.com`                     | OpenAI кључ         |
| 5   | **Cursor IDE**     | ✅ Подржано     | `api2.cursor.sh`, `api.cursor.sh`                                  | Cursor OAuth        |
| 6   | **Zed Industries** | ✅ Подржано     | `api.zed.dev`, `llm.zed.dev`                                       | Zed OAuth           |
| 7   | **Claude Code**    | ✅ Подржано     | `api.anthropic.com` (уз експлицитно укључивање)                    | Anthropic кључ      |
| 8   | **Open Code**      | ✅ Подржано     | `openrouter.ai`, `api.openai.com` (zen путање)                     | API кључ            |
| 9   | **Trae**           | 🔍 Истражује се | Биће утврђено — погледајте §8                                      | Биће утврђено       |

### Кораци чаробњака за подешавање (по агенту)

Свака картица агента има чаробњак за подешавање у 3 корака:

1. **Проверите предуслове** — Да ли је сервер покренут? Да ли је сертификат поуздан? Да ли је IDE инсталиран (аутоматски откривено)?
2. **Омогућите DNS** — Додаје уносе у `/etc/hosts` (захтева sudo). Приказује тачно које ће линије бити додате.
3. **Мапирајте моделе** — Опциона табела за мапирање модела. Џокер-знакови су прихваћени.

### Откривање агената

За агенте 1–8, AgentBridge покушава да аутоматски открије инсталацију IDE-а:

```ts
export async function detectAgent(agentId: AgentId): Promise<DetectionResult>;
// Враћа: { installed: boolean, version?: string, path?: string }
```

Откривање користи путање специфичне за оперативни систем и провере бинарних датотека (нпр. `code --list-extensions | grep github.copilot` за Copilot, `~/.config/antigravity/` за Antigravity).

---

## §5 Безбедност

### Примењена строга правила

| Правило                             | Примена                                                                                           |
| ----------------------------------- | ------------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`      | Све грешке обрађивача се чисте пре одговора или уписа у бафер                                     |
| **#13** Прослеђивање окружења љусци | Измене `/etc/hosts` користе опцију `env` — без интерполације путања у ниску                       |
| **#15 + #17** `isLocalOnlyPath()`   | `/api/tools/agent-bridge/` је LOCAL_ONLY + SPAWN_CAPABLE — loopback се намеће пре аутентификације |

### Листа заобилажења за осетљиве хостове

Листа заобилажења обезбеђује да се саобраћај финансијских институција, OAuth/SSO добављача и других осетљивих хостова **никада не дешифрује**. Њихов TLS саобраћај пролази као транспарентан TCP тунел — OmniRoute никада не види отворени текст.

Подразумевани обрасци заобилажења обухватају:

- `*.bank.*`, `*.gov.*` (финансије/државна управа)
- `*.okta.com`, `*.auth0.com`, `*.microsoft.com` (SSO/идентитет)
- `*.apple.com`, `*.icloud.com` (Apple системске услуге)

Обрасци заобилажења које дода корисник чувају се у табели `agent_bridge_bypass` и имају предност над свим осталим.

### Маскирање тајни

Примењује се `maskSecrets()` из `src/mitm/maskSecrets.ts`:

- На тело сваког захтева пре `TrafficBuffer.push()`
- На свако заглавље пре евидентирања или емитовања

Обрасци: токени са префиксом `sk-`/`ak-`/`pk-`, `Bearer` токени и генерички токени дужине ≥40 знакова.

### Узводни CA сертификат

Када је `AGENTBRIDGE_UPSTREAM_CA_CERT` подешен, датотека се чита при покретању. Ако путања постоји, али датотека није читљива, AgentBridge евидентира јасну грешку и одбија да се покрене (чиме се спречавају неприметни TLS кварови у корпоративним окружењима).

### Позната ограничења

- **Порт 443 захтева привилегије**: На Linux-у, AgentBridge захтева `setcap 'cap_net_bind_service=+ep'` над Node бинарном датотеком или покретање преко `authbind`. Чаробњак за подешавање приказује упутства специфична за оперативни систем.
- **Потребно је поновно покретање IDE-а**: Након DNS преусмеравања, IDE мора бити поново покренут да би ново разрешавање хоста ступило на снагу.
- **Хардкодовани OAuth токени**: Неки агенти (Kiro, Antigravity) локално чувају OAuth токене за освежавање. Они су транспарентни за AgentBridge — он види `Bearer` токен у сваком захтеву, који се маскира пре евидентирања.
- **Electron кориснички интерфејси захтевају `NODE_EXTRA_CA_CERTS`**: IDE-ови чији кориснички интерфејс ради у оквиру уграђеног Node/Electron окружења игноришу складиште поузданости ОС-а/NSS-а и морају бити покренути из љуске са подешеним `NODE_EXTRA_CA_CERTS` (погледајте §3.2). Симптом када недостаје: позадински систем IDE-а се аутентификује (MITM приказује одговоре `200`), али кориснички интерфејс остаје одјављен.
- **Више инсталација истог IDE-а су независне**: системска инсталација (нпр. `/usr/share/antigravity/antigravity`) и корисничка локална „Full“ инсталација (нпр. `~/AntigravityIDE_Full/antigravity-ide`) представљају засебне процесе са сопственим окружењима — сваки мора бити поново покренут са уметнутим CA сертификатом. Пре поновног покретања, утврдите која инсталација је активна на основу путање њене бинарне датотеке.
- **Идентитет одређује системски упит агента, а не усмерени модел**: када поново мапирате модел агента на другог добављача, одговор и даље наводи изворни идентитет агента (нпр. Antigravity одговара „Покреће ме Gemini“) зато што IDE то умеће у системски упит. Стварни позадински систем потврдите у `call_logs` / `proxy_logs` (`provider`, `model`, `target_format`), а не тако што ћете питати модел ко је.

---

## §6 Решавање проблема

### Конфликт на порту 443

Ако неки други процес већ ослушкује порт 443 (веб-сервер, VPN итд.):

```bash
lsof -i :443          # пронађите процес
sudo fuser -k 443/tcp  # принудно заустављање (користите опрезно)
```

Алтернативно, у подешавањима AgentBridge-а конфигуришите непривилеговани порт и поставите правила за преусмеравање помоћу `iptables` / `pf`.

### Сертификат није поуздан

Ако IDE приказује TLS грешке након покретања AgentBridge-а:

1. Проверите да ли је сертификат инсталиран: `security find-certificate -c "OmniRoute AgentBridge"` (macOS) или `certutil -L -d sql:$HOME/.pki/nssdb` (Linux/NSS)
2. Неке апликације одржавају сопствено складиште поузданих сертификата (Firefox, Chrome на Linux-у). Поново покрените „Trust Cert“ и проверите складиште сертификата специфично за NSS/Firefox.
3. Поново покрените IDE након што означите сертификат као поуздан — активне TLS сесије користе претходно стање поверења.

### IDE је одјављен / „грешка при повезивању“ упркос поузданом CA сертификату

Симптом: након преусмеравања DNS-а и означавања CA сертификата као поузданог, IDE заснован на Electron-у (нпр. Antigravity)
отвара се у **одјављеном стању** или приказује грешку при аутентификацији/повезивању, иако MITM евиденција показује да
почетни позиви (`loadCodeAssist`, `fetchAvailableModels`, …) враћају `200`.

Узрок: **уграђено Node/Electron извршно окружење IDE-а игнорише системско складиште поузданих сертификата**. Изворни
позадински систем (сервер језика написан у Go-у) верује системском CA сертификату и успешно обавља аутентификацију, али Electron интерфејс
не верује — па кориснички интерфејс сматра да је ван мреже.

Решење (оба корака): извезите `NODE_EXTRA_CA_CERTS=<ca.crt>` **и поново покрените IDE из тог
окружења командне линије**, а не преко иконе на радној површини. Прво у потпуности затворите IDE — Electron-ово закључавање једне инстанце значи да
друго покретање само активира постојећи процес, док се ново окружење игнорише. Погледајте §3.2.
Ово одговара отвореном извештају у узводном пројекту у ком самостални агент ради преко MITM-а, али IDE
варијанта не ради са истим подешавањем.

### DNS промене нису пропагиране

Проверите да ли је `/etc/hosts` ажуриран:

```bash
grep "omniroute\|127.0.0.1.*github\|127.0.0.1.*cursor" /etc/hosts
```

Испразните DNS кеш:

```bash
# macOS
sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder
# Linux (systemd-resolved)
sudo systemctl restart systemd-resolved
# Windows
ipconfig /flushdns
```

### IDE није откривен

Аутоматско откривање користи уобичајене путање за инсталацију. Ако откривање не успе, али је IDE инсталиран:

- Проверите да ли се извршна датотека IDE-а налази на нестандардној локацији
- Чаробњак за подешавање и даље ради — неуспешно откривање само значи да значка неће приказати путању инсталације

### Грешке руковаоца (добављање од узводног сервиса не успева)

Ако AgentBridge пресреће захтеве, али сви захтеви не успевају:

1. Проверите да ли је бар један добављач повезан на `/dashboard/providers`
2. Проверите евиденцију OmniRoute сервера: `APP_LOG_LEVEL=debug` у `.env`
3. Проверите да ли `OMNIROUTE_BASE_URL` упућује на исправну крајњу тачку рутера (подразумевано: `http://127.0.0.1:20128`)

---

## §7 Референца API-ја

Све руте су `LOCAL_ONLY` (доступне само преко повратне петље, што се спроводи пре аутентификације) и `SPAWN_CAPABLE`. Погледајте `src/server/authz/routeGuard.ts`.

Основна путања: `/api/tools/agent-bridge/`

| Метод               | Путања                                         | Опис                                                                                                                                  |
| ------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| GET                 | `/api/tools/agent-bridge/state`                | Глобално стање сервера + детекција/статус по агенту                                                                                   |
| GET                 | `/api/tools/agent-bridge/agents`               | Листа регистрованих агената (id, назив, домаћини, употребљивост, стање)                                                               |
| GET                 | `/api/tools/agent-bridge/agents/{id}`          | Стање једног агента (конфигурација циља + детекција + сачувано стање)                                                                 |
| PATCH               | `/api/tools/agent-bridge/agents/{id}`          | Ажурирање вредности `setup_completed` за агента                                                                                       |
| GET                 | `/api/tools/agent-bridge/agents/{id}/detect`   | Покретање провере детекције агента (`installed`, `version?`, `path?`)                                                                 |
| POST                | `/api/tools/agent-bridge/agents/{id}/dns`      | Омогућавање/онемогућавање DNS-а за агента (`{enabled: boolean}`)                                                                      |
| GET                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Мапирања модела за агента                                                                                                             |
| PUT                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Замена мапирања модела                                                                                                                |
| POST                | `/api/tools/agent-bridge/server`               | Покретање/заустављање/поновно покретање сервера (`action: "start"\|"stop"\|"restart"\|"trust-cert"\|"regenerate-cert"`)               |
| GET                 | `/api/tools/agent-bridge/cert`                 | Статус сертификата (`exists`, `trusted`, `path`)                                                                                      |
| POST                | `/api/tools/agent-bridge/cert`                 | Додавање поверења (инсталирање) кореном CA сертификату за MITM                                                                        |
| DELETE              | `/api/tools/agent-bridge/cert`                 | Уклањање поверења (деинсталирање) кореног CA сертификата за MITM — идемпотентно (видети §3.6)                                         |
| POST                | `/api/tools/agent-bridge/cert/regenerate`      | Поновно генерисање самопотписаног MITM сертификата                                                                                    |
| GET                 | `/api/tools/agent-bridge/cert/download`        | Стримовање PEM сертификата ради преузимања                                                                                            |
| GET                 | `/api/tools/agent-bridge/bypass`               | Листа образаца за заобилажење (`default` + `user`)                                                                                    |
| POST                | `/api/tools/agent-bridge/bypass`               | Потпуна замена кориснички дефинисаних образаца за заобилажење                                                                         |
| DELETE              | `/api/tools/agent-bridge/bypass?pattern=...`   | Уклањање једног кориснички дефинисаног обрасца за заобилажење                                                                         |
| GET                 | `/api/tools/agent-bridge/diagnose`             | Самотестирање процеса пресретања (видети §3.6)                                                                                        |
| POST                | `/api/tools/agent-bridge/repair`               | Поништавање заосталог MITM стања система (видети §3.6)                                                                                |
| GET                 | `/api/tools/agent-bridge/config`               | Извоз преносиве JSON конфигурације (видети §3.7)                                                                                      |
| POST                | `/api/tools/agent-bridge/config`               | Увоз преносиве JSON конфигурације (видети §3.7)                                                                                       |
| GET                 | `/api/tools/agent-bridge/upstream-ca`          | Добављање конфигурисане путање до узводног CA сертификата                                                                             |
| POST                | `/api/tools/agent-bridge/upstream-ca`          | Провера ваљаности + чување путање до узводног CA сертификата                                                                          |
| POST                | `/api/tools/agent-bridge/upstream-ca/test`     | Само провера ваљаности (пробно покретање) путање до узводног CA сертификата — без чувања                                              |
| GET / POST / DELETE | `/api/tools/agent-bridge/tproxy`               | TPROXY режим пресретања са транспарентним дешифровањем — видети `docs/security/MITM-TPROXY-DECRYPT.md` (git; није уграђено у `/docs`) |

Комплетне OpenAPI шеме: `docs/openapi.yaml` → ознака `AgentBridge`.

---

## §8 План развоја

### Истраживање алата Trae

Trae је релативно нов помоћник за програмирање заснован на вештачкој интелигенцији. Пре имплементације обрађивача:

1. Идентификујте бинарну датотеку/проширење на VS Code / JetBrains продавницама или као самосталну апликацију
2. Снимите саобраћај помоћу mitmproxy да бисте открили API хостове и структуре крајњих тачака
3. Утврдите механизам аутентификације
4. Процените да ли наставити или одустати на основу услова коришћења и могућности откривања API-ја

Док се истраживање не заврши, Trae картица на контролној табли приказује ознаку „Истраживање је у току“ са везом „Пријави одрживост“. Костур обрађивача у `src/mitm/handlers/trae.ts` пријављује структурирану грешку `Још није имплементирано`.

### Агенти у листи задатака (MITM је обавезан — нема подршке за прилагођени основни URL)

Следећи алати у својим тренутним верзијама не подржавају прилагођене основне URL-ове, због чега је MITM једини начин пресретања. Процена одрживости је на чекању:

- **Windsurf** (Codeium/Cognition)
- **Amp** (Sourcegraph)
- **Amazon Q / Kiro CLI** (AWS Bedrock — одвојено од Kiro IDE)
- **Cowork** (Anthropic десктоп апликација)

Напомена: GitHub Copilot CLI ≥v1.0.19 подржава `COPILOT_PROVIDER_BASE_URL` — за тај алат користите директну конфигурацију уместо MITM-а.
