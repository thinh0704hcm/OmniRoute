# open-sse Architecture (اردو)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **خلاصہ**: `open-sse/` بنیادی اسٹریمنگ انجن ہے جو OmniRoute میں ہر LLM درخواست کو چلاتا ہے۔ اس میں تقریباً 1,400 فائلیں ہیں جو درخواست پائپ لائن، ایگزیکیوٹرز، سروسز، MCP سرور، اور ترجمے کی تہہ کو نافذ کرتی ہیں۔ یہ رہنما وضاحت کرتا ہے کہ یہ اجزا کس طرح باہم مربوط ہیں۔

**ماخذ:** `open-sse/` (ورک اسپیس پیکیج، تقریباً 1,440 غیر ٹیسٹ `.ts` فائلیں)

---

## ایک علیحدہ ورک اسپیس پیکیج کیوں؟

`open-sse/` کئی وجوہات کی بنا پر OmniRoute monorepo کے اندر ایک **خود مختار ورک اسپیس** ہے:

1. **دوبارہ استعمال کی صلاحیت** — `open-sse` کو npm پر `@omniroute/open-sse` کے طور پر شائع کیا جاتا ہے، لہٰذا دوسرے پروجیکٹس اسے آزادانہ طور پر استعمال کر سکتے ہیں
2. **واضح حدود** — اسٹریمنگ انجن کو OmniRoute کی مخصوص UI/DB تہہ سے الگ رکھا گیا ہے
3. **کارکردگی** — انجن کا Next.js پر کوئی انحصار نہیں، جس سے CLI/serverless سیاق و سباق میں کولڈ اسٹارٹس زیادہ تیز ہوتے ہیں
4. **ورژننگ** — `open-sse` اپنے شیڈول کے مطابق ریلیز ہو سکتا ہے

```json
// package.json
"workspaces": ["open-sse"]
```

---

## اعلیٰ سطحی ساخت

```
open-sse/
├── index.ts              # عوامی داخلی نقطہ
├── types.d.ts            # عوامی ٹائپ ایکسپورٹس
├── package.json          # @omniroute/open-sse
├── config/               # پرووائیڈر کنفیگز، مستقلات، رجسٹریاں
├── executors/            # ہر پرووائیڈر کے HTTP ایگزیکیوٹرز (67 + base.ts/index.ts)
├── handlers/             # درخواست ہینڈلرز (chatCore، responses، وغیرہ)
├── lib/                  # اندرونی یوٹیلیٹیز
├── mcp-server/           # Model Context Protocol سرور
├── services/             # تقریباً 298 سروس ماڈیولز
├── transformer/          # Responses API فارمیٹ ٹرانسفارمر
├── translator/           # فارمیٹ کا ترجمہ (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # مشترکہ یوٹیلیٹیز (لاگنگ، خرابی، اسٹریم، وغیرہ)
```

### ماڈیولز کی تعداد

| ڈائریکٹری | فائلیں | مقصد |
| `executors/` | 167 | ہر پرووائیڈر کے HTTP ایگزیکیوٹرز (`DefaultExecutor` فیکٹری کے ذریعے یکجا) |
| `handlers/` | 157 | درخواست کے داخلی نقاط (`chatCore`، `responses`، `embeddings`) |
| `services/` | ~536 | روٹنگ، کیشنگ، شرح کی تحدید، ریفریش، وغیرہ |
| `translator/` | 56 | فارمیٹ کی تبدیلی (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP ٹولز اور ٹرانسپورٹس |
| `utils/` | ~108 | مختلف حصوں میں استعمال ہونے والی یوٹیلیٹیز (لاگنگ، خرابی، اسٹریم) |
| `config/` | ~339 | پرووائیڈر کنفیگز، مستقلات، رجسٹریاں |

---

## درخواست پائپ لائن

ہر LLM درخواست ایک **5 مراحل پر مشتمل پائپ لائن** سے گزرتی ہے:

```
                              ┌──────────────┐
   HTTP درخواست               │  1. روٹنگ    │   combo ریزولیوشن، ماڈل کا انتخاب
   (Next.js روٹ)              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. ترجمہ    │   فارمیٹ کی تبدیلی (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. نفاذ     │   پرووائیڈر ایگزیکیوٹر، HTTP، دوبارہ کوشش، بریکر
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. اسٹریم   │   SSE ٹرانسفارمیشن، بیک پریشر
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. ریکارڈ   │   استعمال کی ٹریکنگ، کال لاگ، خرابی کی درجہ بندی
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP جواب (SSE یا JSON)
```

### مرحلہ 1: روٹنگ (services/combo.ts)

**داخلی نقطہ**: `services/combo.ts` میں `handleComboChat()`

درخواست کو ایک واضح `(provider, model, account, credentials)` ٹیپل سے منسلک کرتا ہے:

- ID کے ذریعے combo تلاش کریں (یا `auto/*` ماڈلز کے لیے ایک ورچوئل combo بنائیں)
- روٹنگ حکمت عملی لاگو کریں (ترجیح، وزنی، راؤنڈ رابن، وغیرہ)
- غیر صحت مند پرووائیڈرز کو خارج کریں (سرکٹ بریکر)
- اگلا قابل عمل ہدف منتخب کریں

`auto/*` ماڈلز کے لیے، یہ مرحلہ یہ کام بھی کرتا ہے:

- **16 عوامل پر مبنی اسکورنگ** الگورتھم چلاتا ہے (`services/autoCombo/`)
- صحت، لاگت، تاخیر، وغیرہ کی بنیاد پر ایک `provider+model` جوڑا منتخب کرتا ہے

### مرحلہ 2: ترجمہ (translator/)

اگر ماخذ فارمیٹ (مثلاً OpenAI) ہدف فارمیٹ (مثلاً Claude) سے مختلف ہو تو درخواست کا **ترجمہ** کیا جاتا ہے:

- سسٹم پرامپٹ → سسٹم پیغام
- ٹول کی تعریفیں → پرووائیڈر کے لیے مخصوص ٹول فارمیٹ
- استدلال/تفکر کے پیرامیٹرز → پرووائیڈر کے لیے مخصوص متبادلات
- پیغام کے کرداروں کو معمول پر لانا (غیر OpenAI کے لیے `developer` → `system`)

`translator/index.ts` یہ فراہم کرتا ہے:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### مرحلہ 3: نفاذ (executors/)

**داخلی نقطہ**: `getExecutor(providerId).execute(request, options)`

تمام پرووائیڈرز `getExecutor()` فیکٹری فال بیک کے ذریعے `DefaultExecutor` (`executors/default.ts`) استعمال کرتے ہیں۔ ایگزیکیوٹر:

- اپ اسٹریم URL بناتا ہے (`buildUrl()`)
- پرووائیڈر کے لیے مخصوص ہیڈرز شامل کرتا ہے (`buildHeaders()`)
- درخواست کی باڈی کو تبدیل کرتا ہے (`transformRequest()`)
- دوبارہ کوشش اور ایکسپونینشل بیک آف کے ساتھ HTTP درخواست بھیجتا ہے
- ضرورت پڑنے پر تصدیقی معلومات ریفریش کرتا ہے (OAuth پرووائیڈرز)

تمام ایگزیکیوٹرز `BaseExecutor` (`executors/base.ts`، 1170 LOC) کو توسیع دیتے ہیں، جو یہ سہولیات فراہم کرتا ہے:

- دوبارہ کوشش کی مشترکہ منطق
- پراکسی انضمام
- سرکٹ بریکر انضمام
- استعمال ریکارڈ کرنے کے ہکس

### مرحلہ 4: اسٹریم (utils/stream.ts)

اسٹریمنگ جوابات کے لیے، ایگزیکیوٹر ایک **ReadableStream** واپس کرتا ہے۔ ہینڈلر:

- SSE ٹرانسفارم سے گزارتا ہے (`createSSETransformStreamWithLogger`)
- غیر فعال کنکشنز کا پتا لگانے کے لیے ہارٹ بیٹ پنگز لاگو کرتا ہے
- کلائنٹ کے منقطع ہونے کو بخوبی سنبھالتا ہے (`pipeWithDisconnect`)
- غیر اسٹریمنگ کلائنٹس کے لیے SSE → JSON تبدیل کرتا ہے

غیر اسٹریمنگ جوابات کے لیے، ایگزیکیوٹر پارس کیا ہوا JSON آبجیکٹ واپس کرتا ہے، جسے بغیر تبدیلی کے آگے بھیج دیا جاتا ہے۔

### مرحلہ 5: ریکارڈنگ (services/usage.ts)

جواب (کامیابی یا ناکامی) کے بعد، استعمال کا ریکارڈ درج کیا جاتا ہے:

- جواب سے `prompt_tokens`، `completion_tokens`، `cached_tokens`
- قیمتوں کے ڈیٹا سے شمار کردہ `cost_usd`
- ناکامی کی صورت میں `latency_ms`، `status`، `error_class`
- `usage_history` ٹیبل میں محفوظ کیا جاتا ہے

کال لاگ کے نمونے (اگر فعال ہوں) `${DATA_DIR}/call_logs/` میں لکھے جاتے ہیں۔

---

## کلیدی فائلوں کا تفصیلی جائزہ

### chatCore.ts (5977 لائنیں)

**مرکزی درخواست ہینڈلر**۔ اپنے حجم کے باوجود، اس کی ساخت واضح ہے:

```ts
// chatCore.ts کی فرضی ساخت
export async function handleChat(request: NextRequest) {
  // 1. توثیق + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. باڈی کی توثیق
  const body = await parseRequestBody(request);

  // 3. فارمیٹ کی شناخت + ترجمہ
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. کومبو روٹنگ
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // اگلے ہدف پر جاری رکھیں
    }
  }

  // 5. ہنگامی فال بیک
  return await emergencyFallback(body);
}
```

ایک بہت بڑا فنکشن ہونے کے باوجود، اسے **تبصروں پر مشتمل حصوں** میں منظم کیا گیا ہے جو 5 مرحلوں والی پائپ لائن سے مطابقت رکھتے ہیں۔

### combo.ts (4456 LOC)

یہ **روٹنگ انجن** ہے جو کسی کومبو کو ترتیب شدہ اہداف میں حل کرتا ہے۔

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

یہ **19 روٹنگ حکمت عملیوں** کو سپورٹ کرتا ہے (`src/shared/constants/routingStrategies.ts` دیکھیں):

| حکمت عملی           | طرزِ عمل                                                                             |
| ------------------- | ------------------------------------------------------------------------------------ |
| `priority`          | پہلے ہدف کو ترجیح دینے والی ترتیب شدہ فہرست                                          |
| `weighted`          | ہر ہدف کے وزن کی بنیاد پر احتمالی انتخاب                                             |
| `round-robin`       | اہداف میں ترتیب وار چکر لگانا                                                        |
| `context-relay`     | اہداف کے درمیان سیاق منتقل کرنا                                                      |
| `fill-first`        | اگلے ہدف پر جانے سے پہلے کوٹا مکمل استعمال کرنا                                      |
| `p2c`               | دو انتخابات کی طاقت                                                                  |
| `random`            | یکساں بے ترتیب انتخاب                                                                |
| `least-used`        | حالیہ استعمالات میں سب سے کم استعمال ہونے والے کو منتخب کرنا                         |
| `cost-optimized`    | سب سے سستا صحت مند ہدف پہلے                                                          |
| `reset-aware`       | فراہم کنندہ کی ری سیٹ ونڈوز سے آگاہ                                                  |
| `reset-window`      | ری سیٹ ونڈو پر مبنی روٹنگ                                                            |
| `headroom`          | سب سے زیادہ باقی ماندہ کوٹا گنجائش والا ہدف پہلے                                     |
| `strict-random`     | حقیقی طور پر یکساں (معیار کی کوئی وزن دہی نہیں)                                      |
| `auto`              | 16 عوامل پر مبنی اسکورنگ استعمال کرنا (`autoCombo/`)                                 |
| `lkgp`              | آخری معلوم کامیاب فراہم کنندہ پہلے                                                   |
| `context-optimized` | طویل سیاق والی درخواستوں کے لیے بہترین                                               |
| `fusion`            | متوازی طور پر ایک پینل کو درخواستیں بھیجنا، پھر جج کے ذریعے ترکیب کرنا (`fusion.ts`) |

### base.ts (1170 LOC)

یہ **تجریدی ایگزیکیوٹر** ہے جسے تمام 107 ایگزیکیوٹرز توسیع دیتے ہیں۔ اس میں شامل ہیں:

- `buildUrl()` — ڈیفالٹ URL کی تشکیل (ذیلی کلاسیں حسبِ ضرورت اسے اوور رائیڈ کرتی ہیں)
- `buildHeaders()` — ڈیفالٹ ہیڈرز (توثیق، مواد کی قسم)
- `transformRequest()` — ڈیفالٹ طور پر بغیر تبدیلی کے آگے بھیجنا
- `execute()` — دوبارہ کوشش/وقفہ/بریکر کے ساتھ مرکزی HTTP لوپ

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // تمام OpenAI/Anthropic سے مطابقت رکھنے والے فراہم کنندگان کو سنبھالتا ہے
  // فراہم کنندگان کنفیگریشنز (URL، توثیق، ہیڈرز) رجسٹر کرتے ہیں، لیکن ایگزیکیوٹر منطق مشترک ہوتی ہے
}
```

فراہم کنندہ سے مخصوص طرزِ عمل (توثیقی ہیڈرز، بنیادی URL، ورژن ہیڈرز) کو علیحدہ ایگزیکیوٹر کلاسوں کے بجائے فراہم کنندہ رجسٹری کے ذریعے کنفیگر کیا جاتا ہے۔

````

---

## سروسز (117 ماڈیولز)

سروسز **مخصوص، واحد مقصد والے ماڈیولز** ہیں جنہیں ہینڈلرز یکجا کرتے ہیں۔ بڑی اقسام یہ ہیں:

### روٹنگ اور کومبو

- `combo.ts` — کومبو روٹ شدہ درخواستوں کا نقطۂ آغاز
- `services/autoCombo/` — 16 عوامل پر مبنی اسکورنگ، 8 خودکار روٹنگ حکمتِ عملیاں
- `wildcardRouter.ts` — وائلڈ کارڈ روٹس (`gpt-*`) سے مماثلت کرتا ہے
- `modelFamilyFallback.ts` — T5 خاندان کے اندر فال بیک

### شرح کی تحدید اور کوٹا

- `rateLimitManager.ts` — ہر key+provider کے لیے ٹوکن بکٹ
- `usage.ts` — استعمال کی ریکارڈنگ
- `quotaCache.ts` — اِن میموری کوٹا اسنیپ شاٹس

### اکاؤنٹ اور ٹوکن

- `tokenRefresh.ts` — 401 پر OAuth ریفریش
- `accountFallback.ts` — متبادل اکاؤنٹ پر منتقل ہونا
- `sessionManager.ts` — متعدد مراحل والی سیشن حالت

### ذہانت

- `intentClassifier.ts` — درخواست کے ارادے کی درجہ بندی
- `taskAwareRouter.ts` — کام کی قسم کے لحاظ سے روٹنگ
- `thinkingBudget.ts` — استدلالی ٹوکنز مختص کرنا
- `contextManager.ts` — روٹنگ سیاق شامل کرنا

### پائیداری

- `resilience.ts` — دوبارہ کوشش، بیک آف، اور بریکر کی تنظیم
- `emergencyFallback.ts` — آخری چارۂ کار فال بیک
- `modelDeprecation.ts` — جانشین ماڈلز کی جانب خودکار روٹنگ

### حالت

- `signatureCache.ts` — درخواست کے دستخط کے ذریعے نقلیں ختم کرنا
- `volumeDetector.ts` — لوڈ شیڈنگ
- `contextHandoff.ts` — سیشن سیریلائزیشن

### کمپریشن

- `compression/` (ذیلی ڈائریکٹری) — مکمل کمپریشن پائپ لائن
- انجنز، رول پیکس، اور اڈاپٹرز کا احاطہ کرنے والی 39 فائلیں

### مہارتیں

- ([SKILLS.md](./SKILLS.md) میں شامل ہیں)

### میموری

- ([MEMORY.md](./MEMORY.md) میں شامل ہے)

---

## ایگزیکیوٹرز (75+ فائلیں)

ہر provider کے لیے ایک فائل۔ یہ سب `BaseExecutor` کو توسیع دیتے ہیں اور صرف مختلف حصوں کو اوور رائیڈ کرتے ہیں۔

### عمومی پیٹرنز

Providers کو `getExecutor(providerId)` کے ذریعے حل کیا جاتا ہے، جو تشکیل شدہ executor واپس کرتا ہے۔ OpenAI/Anthropic سے مطابقت رکھنے والے providers، `DefaultExecutor` (`executors/default.ts`) استعمال کرتے ہیں۔ Provider سے مخصوص رویہ (بنیادی URL، توثیقی ہیڈرز، API ورژن) `open-sse/config/providers/` میں تشکیل دیا جاتا ہے، جبکہ درخواست کی باڈی کی تبدیلیاں `open-sse/translator/` میں سنبھالی جاتی ہیں۔

**حسبِ ضرورت URL** کو provider کی تشکیل کے ذریعے مقرر کیا جاتا ہے:

```ts
// open-sse/config/providers/ میں Provider کی تشکیل
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**حسبِ ضرورت توثیق** کو provider رجسٹری کی توثیقی تشکیل (API کلید، OAuth، ہیڈر پروفائلز) کے ذریعے سنبھالا جاتا ہے۔

**حسبِ ضرورت درخواست باڈی** کی تبدیلیاں (مثلاً Anthropic کا `system` کو `messages` سے الگ کرنا) ہر provider کے لیے `open-sse/translator/` میں رجسٹر کی جاتی ہیں۔

````

### ایگزیکیوٹر فیکٹری

`executors/index.ts`، `getExecutor(providerId)` ایکسپورٹ کرتی ہے:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

حل کرنے کا عمل `ExecutorRegistry` (`executors/registry.ts`) سے گزرتا ہے: ہر مخصوص executor کو `executors/index.ts` کے بلٹ اِن ٹیبل میں بیان کیا جاتا ہے اور ماڈیول لوڈ ہوتے وقت `registerExecutor(alias, instance)` کے ذریعے رجسٹر کیا جاتا ہے؛ `getExecutor()` رجسٹری سے رجوع کرتا ہے اور کسی ایسے provider کے لیے، جس کا کوئی مخصوص اندراج موجود نہ ہو، میموائزڈ `DefaultExecutor` پر فال بیک کرتا ہے۔ مکمل alias → executor میپنگ کو گولڈن ٹیسٹ `tests/unit/executor-map-golden.test.ts` کے ذریعے بیان کیا گیا ہے۔

---

## مترجمین

**3 فارمیٹس** کے درمیان ترجمہ کریں: OpenAI، Anthropic، Gemini، نیز نیا Responses API۔

### ترجمہ کب ہوتا ہے

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

عام تراجم:

- `OpenAI → Anthropic`: علیحدہ `system` فیلڈ، `x-api-key` ہیڈر
- `OpenAI → Gemini`: `messages` کی بجائے `contents`، `systemInstruction`
- `OpenAI → Responses API`: `input` ارے، `previous_response_id` اسٹیٹ

### سنبھالے گئے خصوصی حالات

- `developer` رول → غیر OpenAI کے لیے `system`
- `system` رول → GLM/ERNIE کے لیے پہلے صارف کے پیغام میں ضم کیا جاتا ہے
- `json_schema` → Gemini کا `responseMimeType` + `responseSchema`
- `tools` → فراہم کنندہ کے لیے مخصوص ٹول فارمیٹ
- تھنکنگ پیرامیٹرز (o1، Claude) → فراہم کنندہ کے لیے مخصوص متبادلات

---

## MCP سرور

`open-sse/mcp-server/`، **Model Context Protocol** سرور نافذ کرتا ہے:

- **110 ٹولز** (فراہم کنندہ مینجمنٹ، کمبوز، میموری، کیش، کمپریشن، پراکسی، اسکلز، گیمیفیکیشن، پلگ انز، Notion، Obsidian، مقامی کارپس)
- **3 ٹرانسپورٹس**: stdio، SSE، Streamable HTTP
- باریک سطح کی اجازت دہی کے لیے **33 اسکوپس**

### ٹول رجسٹریشن

ٹولز کو `open-sse/mcp-server/tools/` میں علیحدہ فائلوں کے طور پر رجسٹر کیا جاتا ہے، جن میں سے ہر ایک نام، اسکیما، ہینڈلر اور اسکوپ ایکسپورٹ کرتی ہے:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### ٹرانسپورٹس

```ts
// stdio (CLI استعمال)
startMcpStdio(server);

// SSE (HTTP پر مبنی اسٹریمنگ)
startMcpSse(server, port);

// Streamable HTTP (جدید MCP)
startMcpStreamable(server, port);
```

### اجازت دہی

ہر ٹول کال اسکوپ چیکس (`open-sse/mcp-server/auth/`) سے گزرتی ہے:

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## ٹرانسفارمرز

`open-sse/transformer/`، **Chat Completions** اور **Responses API** فارمیٹس کے درمیان تبدیلی کرتا ہے۔

### علیحدہ ٹرانسفارمر کیوں؟

Responses API، OpenAI کا نیا فارمیٹ ہے جس میں **حالت برقرار رکھنے والی گفتگوئیں** (`previous_response_id`) شامل ہیں۔ جب کوئی کلائنٹ Responses درخواست بھیجتا ہے، تو OmniRoute:

1. اندرونی طور پر Responses → Chat Completions میں تبدیل کرتا ہے
2. فراہم کنندہ کو بھیجتا ہے (کوئی بھی فراہم کنندہ جو Chat Completions کو سپورٹ کرتا ہو)
3. جواب کو واپس Responses فارمیٹ میں تبدیل کرتا ہے
4. تبدیل شدہ جواب کو کلائنٹ تک اسٹریم کرتا ہے

ٹرانسفارمر (`transformer/responsesTransformer.ts`) یہ فراہم کرتا ہے:

```ts
createResponsesApiTransformStream(): TransformStream
```

یہ درج ذیل کو سنبھالتا ہے:

- `response.output_item.added` ایونٹس
- `response.output_text.delta` ایونٹس
- `response.completed` ایونٹ
- ٹول کال میپنگ (`function_call` ↔ `tool_calls`)

---

## کنفیگریشن

`open-sse/config/` میں کنفیگریشن لیئر موجود ہے:

| فائل                          | مقصد                                           |
| ----------------------------- | ---------------------------------------------- |
| `providerRegistry.ts`         | 352 فراہم کنندگان کے کیٹلاگ پر چیٹ ماڈل رجسٹری |
| `providerModels.ts`           | ماڈل عرفی نام، فارمیٹ میپنگ                    |
| `constants.ts`                | ٹائم آؤٹس، حدود، اسٹیٹس کوڈز                   |
| `defaultThinkingSignature.ts` | ڈیفالٹ Claude تھنکنگ سگنیچر                    |
| `modelStrip.ts` (سروسز میں)   | فی فراہم کنندہ فیلڈ اسٹرپنگ                    |

### فراہم کنندہ رجسٹری اسکیما

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

ماڈیول لوڈ ہوتے وقت Zod ویلیڈیشن یقینی بناتی ہے کہ تمام فراہم کنندہ کنفیگز درست ہیں۔

---

## کارکردگی کی پابندیاں

راؤٹنگ انجن کے لیے کارکردگی کی سخت حدود مقرر ہیں:

| عمل                                          | ہدف   | پیمائش                |
| -------------------------------------------- | ----- | --------------------- |
| کومبو ریزولیوشن                              | <10ms | 50 اہداف کے لیے       |
| ریٹ لمٹ کی جانچ                              | <1ms  | اِن میموری ٹوکن بکٹ   |
| ماڈل فیملی فال بیک                           | <5ms  | کیش شدہ فیملی تعریفیں |
| درخواست راؤٹنگ ڈسپیچ                         | <2ms  | ہاٹ پاتھ              |
| **راؤٹنگ ہاٹ پاتھ میں کوئی بلاکنگ I/O نہیں** | —     | سب کچھ async          |

---

## نامناسب طریقے

❌ **`combo.ts` میں سنکرونس DB کالز** — پہلے سے کمپیوٹ کرکے کیش کریں
❌ **ہینڈلرز میں ری ٹرائی منطق** — ریزیلینس سروس سے `retry()` استعمال کریں
❌ **پرووائیڈر کنفیگ تک براہِ راست رسائی** — `providerRegistry` گیٹرز استعمال کریں
❌ **ہارڈ کوڈ کردہ فال بیک چینز** — `modelFamilyFallback.ts` میں متعین کریں
❌ **ہم وقت درخواستوں کے درمیان اسٹیٹ میں تبدیلیاں** — صرف درخواست کے دائرۂ کار کا کانٹیکسٹ استعمال کریں

---

## نیا کمپوننٹ شامل کرنا

### نئی سروس شامل کرنا

1. مخصوص ذمہ داری کے ساتھ `open-sse/services/[serviceName].ts` بنائیں
2. مرکزی ہینڈلر فنکشن اور تمام مستقل اقدار ایکسپورٹ کریں
3. `tests/unit/services/[serviceName].test.mjs` میں یونٹ ٹیسٹس شامل کریں
4. درخواست پائپ لائن میں `handlers/chatCore.ts` کے اندر انٹیگریٹ کریں (اگر راؤٹنگ سے متعلق ہو)
5. اگر سروس ہدف کے انتخاب کو متاثر کرتی ہے تو `combo.ts` میں راؤٹنگ منطق اپ ڈیٹ کریں
6. اس فائل میں دستاویز کریں

### نیا ایگزیکیوٹر شامل کرنا

1. `BaseExecutor` کو ایکسٹینڈ کرتے ہوئے `open-sse/executors/[provider].ts` بنائیں
2. `config/providerRegistry.ts` میں رجسٹر کریں
3. `executors/index.ts` فیکٹری میں شامل کریں
4. ایگزیکیوٹر کے لیے یونٹ ٹیسٹس شامل کریں
5. `docs/architecture/ARCHITECTURE.md` میں دستاویز کریں

### نیا MCP ٹول شامل کرنا

1. `open-sse/mcp-server/tools/[category]Tools.ts` بنائیں یا اپ ڈیٹ کریں
2. اِن پٹس کے لیے Zod اسکیما متعین کریں
3. ٹول کو `mcp-server/index.ts` میں رجسٹر کریں
4. `mcp-server/auth/` میں اسکوپ میٹرکس میں شامل کریں
5. یونٹ ٹیسٹس شامل کریں

---

## مزید دیکھیں

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — اعلیٰ سطحی آرکیٹیکچر
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — انجینئرنگ حوالہ
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — ڈائریکٹری بہ ڈائریکٹری
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 عوامل پر مبنی اسکورنگ
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP سرور
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A سرور
- ماخذ: `open-sse/` (400+ فائلیں، ~143K LOC)
