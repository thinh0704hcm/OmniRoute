# AgentBridge (اردو)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENTBRIDGE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENTBRIDGE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENTBRIDGE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENTBRIDGE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENTBRIDGE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENTBRIDGE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/AGENTBRIDGE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENTBRIDGE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENTBRIDGE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENTBRIDGE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENTBRIDGE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENTBRIDGE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENTBRIDGE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENTBRIDGE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENTBRIDGE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENTBRIDGE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENTBRIDGE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENTBRIDGE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENTBRIDGE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENTBRIDGE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENTBRIDGE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENTBRIDGE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENTBRIDGE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENTBRIDGE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENTBRIDGE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENTBRIDGE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENTBRIDGE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENTBRIDGE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENTBRIDGE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENTBRIDGE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENTBRIDGE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENTBRIDGE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENTBRIDGE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENTBRIDGE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENTBRIDGE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENTBRIDGE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENTBRIDGE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENTBRIDGE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENTBRIDGE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENTBRIDGE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENTBRIDGE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENTBRIDGE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENTBRIDGE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENTBRIDGE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENTBRIDGE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENTBRIDGE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENTBRIDGE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENTBRIDGE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENTBRIDGE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENTBRIDGE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENTBRIDGE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENTBRIDGE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENTBRIDGE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENTBRIDGE.md)

---

AgentBridge، OmniRoute کا MITM (Man-in-the-Middle) پراکسی ہے جو IDE کے AI ایجنٹس سے آنے والی HTTPS ٹریفک کو انٹرسیپٹ کرتا ہے اور اسے OmniRoute کے متحدہ روٹنگ انجن کے ذریعے دوبارہ روٹ کرتا ہے۔ یہ **10 IDE ایجنٹس** — Antigravity، Kiro، GitHub Copilot، GHE Copilot، OpenAI Codex، Cursor، Zed، Claude Code، Open Code، اور Trae (زیرِ تحقیق) — کو سپورٹ کرتا ہے، جس سے OmniRoute مارکیٹ میں AI کوڈنگ اسسٹنٹس کے لیے سب سے وسیع کوریج والا MITM پراکسی بن جاتا ہے۔

**ڈیش بورڈ کا مقام:** `/dashboard/tools/agent-bridge`
**سائیڈ بار گروپ:** ٹولز (Cloud Agents کے بعد)
**یہ بھی دیکھیں:** [`TRAFFIC_INSPECTOR.md`](./TRAFFIC_INSPECTOR.md) — تمام انٹرسیپٹ شدہ ٹریفک کو ریئل ٹائم میں مانیٹر کریں؛ `docs/security/MITM-TPROXY-DECRYPT.md` (git؛ `/docs` میں کمپائل نہیں کیا گیا) — Linux کا TPROXY شفاف-ڈکرپٹ کیپچر موڈ، جو `/api/tools/agent-bridge/tproxy` روٹ کے ذریعے چلتا ہے۔

---

## §1 جائزہ

### AgentBridge کیا ہے؟

جب کوئی IDE ایجنٹ (مثلاً GitHub Copilot، Cursor، Claude Code) API کال کرتا ہے، تو وہ براہِ راست اپ اسٹریم AI فراہم کنندہ (OpenAI، Anthropic وغیرہ) سے جڑتا ہے۔ AgentBridge کسی بھی ایجنٹ کی کنفیگریشن تبدیل کیے بغیر، TLS کی سطح پر اس کنکشن کو شفاف انداز میں انٹرسیپٹ کرتا ہے — اور درخواست کو OmniRoute کے ذریعے دوبارہ لکھتا ہے۔

اس کا مطلب ہے کہ آپ:

- **کسی بھی ایجنٹ کو کسی بھی فراہم کنندہ کی طرف دوبارہ روٹ کر سکتے ہیں**: کیا Copilot، OpenAI سے رابطہ کر رہا ہے؟ اسے Anthropic Claude، Gemini، یا OmniRoute کے 352 فراہم کنندگان میں سے کسی کی طرف ری ڈائریکٹ کریں۔
- **ماڈل میپنگز لاگو کر سکتے ہیں**: ہینڈلر کی سطح پر `gemini-3-flash` → `claude-sonnet-4.7` شفاف انداز میں۔
- **ایجنٹ کی تمام ٹریفک کا مشاہدہ کر سکتے ہیں**: ہر انٹرسیپٹ شدہ درخواست [Traffic Inspector](./TRAFFIC_INSPECTOR.md) کو شائع کی جاتی ہے۔
- **OmniRoute کی لچک پذیری لاگو کر سکتے ہیں**: کومبو روٹنگ، سرکٹ بریکرز، فال بیکس، اور لاگت کی ٹریکنگ IDE ایجنٹ ٹریفک کے لیے بھی کام کرتے ہیں۔

### مارکیٹ کے مقابلے میں پوزیشن

| خصوصیت                | 9router | anti-api | llm-interceptor | **OmniRoute AgentBridge** |
| --------------------- | :-----: | :------: | :-------------: | :-----------------------: |
| Antigravity           |    ✓    |    ✓     |        —        |             ✓             |
| GitHub Copilot        |    ✓    |    ✓     |        —        |             ✓             |
| Kiro (AWS)            |    ✓    |    ✓     |        —        |             ✓             |
| OpenAI Codex          |    —    |    ✓     |        —        |             ✓             |
| Cursor IDE            |    ✓    |    ✓     |        —        |             ✓             |
| Zed Industries        |    —    |    ✓     |        —        |             ✓             |
| Claude Code           |    —    |    —     |        ✓        |             ✓             |
| Open Code             |    —    |    —     |        ✓        |             ✓             |
| Trae                  |    —    |    —     |        —        |       🔍 زیرِ تحقیق       |
| ڈیش بورڈ UI           |    ✓    |    ✗     |        ✗        |             ✓             |
| Traffic Inspector     |    ✗    |    ✗     |        ✓        |             ✓             |
| OmniRoute روٹنگ       |    ✗    |    ✗     |        ✗        |             ✓             |
| ماڈل میپنگ UI         |    ✗    |    ✗     |        ✗        |             ✓             |
| بائی پاس فہرست        |    ✗    |    ✗     |        ✓        |             ✓             |
| اپ اسٹریم CA سرٹیفکیٹ |    ✗    |    ✗     |        ✓        |             ✓             |

---

## §2 آرکیٹیکچر

### 2.1 اجزاء کا جائزہ

```
IDE ایجنٹ (VS Code / Cursor / وغیرہ)
    │  HTTPS (پورٹ 443)
    ▼
/etc/hosts — 127.0.0.1 api.githubcopilot.com   ← DNS ری ڈائریکٹ
    │
    ▼
src/mitm/server.cjs  (پورٹ 443، CJS چائلڈ پراسیس)
    │  Host ہیڈر SNI کے ذریعے ہدف کا تعین کرتا ہے
    │  AgentBridge CA سے دستخط شدہ فی-SNI TLS سرٹیفکیٹ تیار کرتا ہے
    ├── بائی پاس فہرست سے مطابقت؟ → TCP پاس تھرو (کوئی ڈکرپشن نہیں)
    ├── ہدف سے مطابقت؟ → fetch → OmniRoute راؤٹر (پورٹ 20128)
    │       └── handler.intercept() — TypeScript
    │               ├── درخواست کی باڈی/ہیڈرز پر maskSecrets()
    │               ├── TrafficBuffer.push() — Traffic Inspector کو شائع کرتا ہے
    │               └── fetchRouter() → /v1/chat/completions
    └── کوئی مطابقت نہیں؟ → TCP پاس تھرو (کوئی ڈکرپشن نہیں)
```

### 2.2 MITM سرور (`src/mitm/server.cjs`)

بنیادی MITM سرور Node.js CJS چائلڈ پراسیس کے طور پر چلتا ہے (موجودہ CJS کوڈ بیس کو دوبارہ لکھنے سے بچنے کے لیے)۔ یہ:

- پورٹ 443 پر سنتا ہے (خصوصی مراعات یا `authbind`/`setcap` درکار ہیں)
- OS سے CONNECT ٹنلز وصول کرتا ہے (`/etc/hosts` کے DNS ری ڈائریکٹ کے ذریعے)
- AgentBridge CA (`DATA_DIR/mitm/ca.crt`) سے دستخط شدہ فی-SNI TLS سرٹیفکیٹس تیار کرتا ہے
- `targets/index.ts` رجسٹری کے ذریعے Host ہیڈر کی بنیاد پر ہدف ایجنٹ کا تعین کرتا ہے
- HTTP کے ذریعے `http://127.0.0.1:20128` پر موجود TypeScript ہینڈلر پرت کو درخواست بھیجتا ہے

`TARGET_HOSTS` کو `DATA_DIR/mitm/targets.json` سے لوڈ کیا جاتا ہے (جسے بوٹ کے وقت `targets/index.ts` لکھتا ہے)، جس سے CJS سرور کو دوبارہ شروع کیے بغیر متحرک اپ ڈیٹس ممکن ہوتی ہیں۔

> **روٹ-CA ماڈل (#6684)۔** اوپر بیان کردہ CA سے دستخط شدہ فی-SNI سرٹیفکیٹ
> والا طریقہ #6684 میں شامل کیا گیا مستقل روٹ-CA ماڈل ہے (`src/mitm/cert/rootCa.ts` +
> `src/mitm/_internal/rootCaShim.cjs`، جو `src/mitm/tproxy/dynamicCert.ts` میں TPROXY کے لیے
> پہلے سے ثابت شدہ CA/leaf کرپٹو کو دوبارہ استعمال کرتا ہے) — یہ پرانے واحد جامد
> خود دستخط شدہ leaf (`src/mitm/cert/generate.ts`، جو اب بھی صرف antigravity ہوسٹس تک
> محدود ہے) کی جگہ لیتا ہے، جس کی موجودگی ڈسک پر محض `server.crt`/`server.key`
> جوڑے سے ظاہر ہوتی ہے۔ **مائیگریشن کا طرزِ عمل**: ایک نئی انسٹالیشن (جہاں پہلے سے
> `server.crt` موجود نہ ہو) کو روٹ-CA ماڈل خودکار طور پر ملتا ہے؛ ایسی انسٹالیشن جو
> پہلے ہی پرانے جامد leaf پر اعتماد کرتی ہو، اسے اس وقت تک استعمال کرتی رہتی ہے جب تک
> آپریٹر `MITM_ROOT_CA_ENABLED=true` مقرر کرکے برج کو دوبارہ شروع نہ کرے
> (`src/mitm/cert/migration.ts` خالص فیصلہ کرنے والا فنکشن ہے — ایک قابلِ اعتماد MITM CA
> جو **کسی بھی** ہوسٹ کے لیے leaf پر دستخط کر سکتا ہو، پرانے مقررہ-SAN leaf سے نمایاں طور پر
> زیادہ طاقتور ہے، اس لیے پہلے سے قابلِ اعتماد انسٹالیشن میں تبدیلی کبھی خاموشی سے نہیں
> ہوتی)۔ CA سرٹیفکیٹ اسی `omniroute-mitm.crt` ٹرسٹ اسٹور سلاٹ میں انسٹال ہوتا ہے جسے
> پرانا leaf استعمال کرتا تھا (`cert/install.ts::installCaCert`) — دوہری ٹرسٹ کی صفائی
> کی ضرورت نہیں ہے۔

### 2.3 ہینڈلر بیس (`src/mitm/handlers/base.ts`)

تمام ایجنٹ ہینڈلرز `MitmHandlerBase` کو وسعت دیتے ہیں:

```ts
export abstract class MitmHandlerBase {
  abstract readonly agentId: AgentId;

  abstract intercept(
    req: IncomingMessage,
    res: ServerResponse,
    body: Buffer,
    mappedModel: string
  ): Promise<void>;

  // محفوظ ہیلپرز: fetchRouter، pipeSSE، hookBufferStart، hookBufferUpdate
}
```

ہر ہینڈلر پراکسی کرنے سے پہلے `hookBufferStart()` اور مکمل ہونے پر `hookBufferUpdate()` کو کال کرتا ہے۔ یہ `InterceptedRequest` اندراجات کو `globalTrafficBuffer` میں پُش کرتے ہیں ([Traffic Inspector](./TRAFFIC_INSPECTOR.md) §4 دیکھیں)۔

### 2.4 اہداف کی رجسٹری (`src/mitm/targets/`)

ہر ایجنٹ کی ایک اعلانیہ ہدف فائل ہوتی ہے:

```ts
// src/mitm/targets/copilot.ts
export const COPILOT_TARGET: MitmTarget = {
  id: "copilot",
  name: "GitHub Copilot",
  hosts: ["api.githubcopilot.com", "copilot-proxy.githubusercontent.com"],
  port: 443,
  endpointPatterns: ["/chat/completions", "/v1/chat/completions"],
  defaultModels: [{ id: "gpt-4o", name: "GPT-4o", alias: "gpt-4o" }],
  handler: () => import("../handlers/copilot"),
  riskNoticeKey: "providers.riskNotice.oauth",
};
```

رجسٹری (`targets/index.ts`) `ALL_TARGETS` کو ایکسپورٹ کرتی ہے اور بوٹ کے وقت `DATA_DIR/mitm/targets.json` تیار کرتی ہے۔

### 2.5 پاس تھرو اور بائی پاس فہرست (`src/mitm/passthrough.ts`)

**بائی پاس فہرست** (سب سے پہلے جانچی جاتی ہے اور ہدف سے مطابقت پر فوقیت رکھتی ہے):

- ڈیفالٹ پیٹرنز: بینکنگ ہوسٹس، `.gov.`، OAuth/SSO فراہم کنندگان (Okta، Auth0)، وغیرہ
- صارف کے پیٹرنز: DB ٹیبل `agent_bridge_bypass` میں محفوظ ہوتے ہیں
- بائی پاس کیے گئے ہوسٹس کو شفاف TCP ٹنل ملتا ہے — TLS کو **کبھی ڈکرپٹ نہیں کیا جاتا**

**ڈیفالٹ پاس تھرو** (ہدف سے کوئی مطابقت نہ ہو اور بائی پاس میں بھی نہ ہو):

- اسے بھی TCP ٹنل ملتا ہے — کنکشنز کبھی منقطع نہیں کیے جاتے
- AgentBridge کو عمومی سسٹم HTTPS ٹریفک میں خلل ڈالنے سے روکتا ہے

راؤٹنگ کی ترجیح:

```
بائی پاس فہرست → ہدف سے مطابقت → پاس تھرو
```

### 2.6 اپ اسٹریم CA سرٹیفکیٹ (`src/mitm/upstreamTrust.ts`)

حسبِ ضرورت CA والے کارپوریٹ نیٹ ورک ماحول کے لیے:

```bash
AGENTBRIDGE_UPSTREAM_CA_CERT=/path/to/corporate-ca.pem
```

مقرر ہونے پر، یہ اضافی CA سرٹیفکیٹ کے ساتھ `undici` کے عالمی ڈسپیچر کو کنفیگر کرتا ہے، جس سے AgentBridge کارپوریٹ TLS ٹرمینیشن پراکسیز کے ذریعے اپ اسٹریم فراہم کنندگان تک رسائی حاصل کر سکتا ہے۔

### 2.7 رازوں کی ماسکنگ (`src/mitm/maskSecrets.ts`)

خودمختار کلین روم اسکینر کو درخواست کی باڈیز اور اسناد والے ہیڈرز پر اس وقت **پہلے** لاگو کیا جاتا ہے جب وہ Traffic Inspector بفر یا کسی لاگ میں داخل ہوں۔ یہ ایک ہی خطی پاس انجام دیتا ہے:

- `sk-` / `ak-` / `pk-` سابقے والے ٹوکنز (OpenAI/Anthropic طرز)
- RFC 6750 کے `Authorization: Bearer <token>` اسناد، جہاں مکمل ٹوکن کو ترجیح حاصل ہے
- عمومی طویل مبہم ٹوکنز (≥40 حروف)، بشمول نقطوں اور پیڈنگ والی صورتیں

`sanitizeHeaders()` برقرار رکھے گئے ناموں کو چھوٹے حروف میں تبدیل کرتا ہے، ارے کی اقدار کو قطعی انداز میں جوڑتا ہے، مشترکہ hop-by-hop/framing ممنوعہ فہرست (بشمول پراکسی توثیق) کو خارج کرتا ہے، `cookie` اور `set-cookie` کو مکمل طور پر چھپاتا ہے، اور اسناد کی اقدار اسکینر کے سپرد کرتا ہے۔

---

## §3 سیٹ اپ

### 3.1 MITM سرور شروع/بند کریں

`/dashboard/tools/agent-bridge` پر موجود AgentBridge Server Card استعمال کریں:

| کارروائی                        | تفصیل                                                                                   |
| ------------------------------- | --------------------------------------------------------------------------------------- |
| سرور شروع کریں                  | پورٹ 443 پر `src/mitm/server.cjs` چلاتا ہے                                              |
| سرور بند کریں                   | چائلڈ پراسیس کو محفوظ انداز میں بند کرتا ہے                                             |
| سرور دوبارہ شروع کریں           | بند + شروع کرتا ہے (ہدف کی تبدیلیاں لاگو کرتا ہے)                                       |
| سرٹیفکیٹ کو قابلِ اعتماد بنائیں | `DATA_DIR/mitm/ca.crt` کو OS کے ٹرسٹ اسٹور میں انسٹال کرتا ہے                           |
| سرٹیفکیٹ ڈاؤن لوڈ کریں          | دستی تنصیب کے لیے `ca.crt` ڈاؤن لوڈ کرتا ہے                                             |
| سرٹیفکیٹ دوبارہ بنائیں          | ایک نیا CA کلیدی جوڑا بناتا ہے (ہر ایجنٹ کے تمام موجودہ سرٹیفکیٹس غیر مؤثر ہو جاتے ہیں) |

### 3.2 سرٹیفکیٹ کو قابلِ اعتماد بنائیں

اس سے پہلے کہ IDEs، MITM کنکشن قبول کریں، AgentBridge CA سرٹیفکیٹ کو OS پر قابلِ اعتماد بنانا ضروری ہے۔

**Linux (NSS — Chrome/Firefox):**

```bash
certutil -A -d sql:$HOME/.pki/nssdb -n "OmniRoute AgentBridge" -t CT,, -i ~/.omniroute/mitm/ca.crt
```

**macOS (Keychain):**

```bash
sudo security add-trusted-cert -d -r trustRoot \
  -k /Library/Keychains/System.keychain ~/.omniroute/mitm/ca.crt
```

**Windows (certmgr):**

```powershell
certutil -addstore -f Root $env:USERPROFILE\.omniroute\mitm\ca.crt
```

یا ڈیش بورڈ میں "سرٹیفکیٹ کو قابلِ اعتماد بنائیں" بٹن استعمال کریں (یہ آپ کے OS کے لیے موزوں کمانڈ چلاتا ہے اور ضرورت پڑنے پر sudo کا پرامپٹ دکھاتا ہے)۔

#### Electron پر مبنی IDEs، OS ٹرسٹ اسٹور کو نظر انداز کرتے ہیں (`NODE_EXTRA_CA_CERTS`)

کچھ IDEs — بالخصوص **Antigravity IDE** اور دیگر Electron / VS Code سے ماخوذ ایپس — اپنا
Node.js رن ٹائم شامل کرتے ہیں جو آؤٹ باؤنڈ `fetch`/HTTPS کے لیے **OS ٹرسٹ اسٹور سے رجوع نہیں کرتا**۔
CA کو OS/NSS سطح پر قابلِ اعتماد بنانا IDE کے مقامی **بیک اینڈ** کے لیے کافی ہے
(مثلاً Go لینگویج سرور، جو OS CA بنڈل استعمال کرتا ہے)، لیکن **Electron فرنٹ اینڈ** میں
TLS پھر بھی ناکام رہے گا — یہ اس صورت میں ظاہر ہوتا ہے کہ ایپ سے _لاگ آؤٹ_ ہو گیا ہے یا
_"کنکشن کی خرابی"_ دکھائی دیتی ہے، حالاں کہ MITM لاگ میں بیک اینڈ کی بوٹ اسٹریپ کالز `200`
لوٹاتی نظر آتی ہیں۔ دو مراحل درکار ہیں، اور دونوں اہم ہیں:

1. رن ٹائم کو واضح طور پر CA کی جانب رہنمائی کریں:
   ```bash
   export NODE_EXTRA_CA_CERTS=/path/to/omniroute-agentbridge-ca.crt
   ```
2. **IDE کو اسی شیل سے چلائیں۔** اسے ڈیسک ٹاپ آئیکن / Dock / Start مینیو سے شروع کرنے پر
   شیل ایکسپورٹس منتقل **نہیں** ہوتے، اور `~/.config/environment.d/*.conf` صرف
   نئے گرافیکل لاگ اِن کے بعد لاگو ہوتا ہے۔ پہلے IDE کو مکمل طور پر بند کریں — Electron کے سنگلٹن لاک کا مطلب ہے کہ دوسری بار
   چلانے سے صرف موجودہ پراسیس پر فوکس ہوتا ہے اور نئے ماحول کو نظر انداز کر دیا جاتا ہے۔

اوپر دیا گیا OS-trust + NSS مرحلہ اب بھی ضروری ہے (تصدیق کے بعض
فلوز کے زیرِ استعمال Chromium نیٹ ورک اسٹیک فی صارف NSS اسٹور کو پڑھتا ہے، اور `*.googleapis.com` کے لیے
اس کے اپنے جامد پنز ہوتے ہیں جنہیں مقامی طور پر قابلِ اعتماد CA اوور رائیڈ کرتا ہے)۔ اس کے علاوہ `NODE_EXTRA_CA_CERTS`، Node کے `fetch` راستے کا احاطہ کرتا ہے۔

### 3.3 DNS روٹنگ

ہر اس ایجنٹ کے لیے جسے آپ انٹرسیپٹ کرنا چاہتے ہیں، اس کے API ہوسٹ (یا ہوسٹس) کو `127.0.0.1` پر ریزولو ہونا چاہیے۔ جب آپ Setup Wizard میں کسی ایجنٹ کے لیے DNS ٹوگل کرتے ہیں تو AgentBridge، `/etc/hosts` اندراجات کا خودکار طور پر انتظام کرتا ہے۔

GitHub Copilot کے لیے `/etc/hosts` اندراجات کی مثال:

```
127.0.0.1 api.githubcopilot.com
127.0.0.1 copilot-proxy.githubusercontent.com
```

### 3.4 ماڈل میپنگ

ماخذ → ہدف میپنگز متعین کرنے کے لیے ہر ایجنٹ کارڈ میں Model Mapping Table استعمال کریں:

| ماخذ ماڈل (ایجنٹ کا مقامی) | ہدف ماڈل (OmniRoute) |
| -------------------------- | -------------------- |
| `gpt-4o`                   | `claude-sonnet-4.7`  |
| `*` (وائلڈ کارڈ)           | `claude-haiku-4.7`   |

وائلڈ کارڈ `*` کسی بھی غیر شناخت شدہ ماڈل کو مخصوص ہدف سے میپ کرتا ہے۔ اسے `agent_bridge_mappings` ٹیبل میں محفوظ رکھا جاتا ہے۔

> **مشورہ — ایجنٹ کی اصل ماڈل IDs معلوم کریں۔** کوئی IDE ایسے ماڈل نام بھیج سکتا ہے جو
> اس کے UI لیبلز سے مختلف ہوں اور بڑے ورژنز کے درمیان تبدیل ہوتے رہیں۔ مثال کے طور پر **Antigravity 2**
> نیٹ ورک پر `gemini-3.1-pro-low`، `gemini-pro-agent`، اور `gemini-3.1-flash-lite` بھیجتا ہے —
> پرانی دستاویزات میں دکھایا گیا `gemini-2.5-pro` نہیں۔ کوئی مماثل میپنگ رکھے بغیر ایک چیٹ بھیجیں: MITM
> عین موصول ہونے والے `model:` کو لاگ کرتا ہے اور درخواست کو آگے گزرنے دیتا ہے۔ اسی تحریری قدر کو میپ کریں، پھر
> اگلی درخواست انٹرسیپٹ ہو کر آپ کے ہدف کی جانب روٹ ہو جائے گی۔

### 3.5 خطرے کا نوٹس

AgentBridge ان اسناد (OAuth ٹوکنز، API کلیدیں) کو انٹرسیپٹ کرتا ہے جنہیں IDE، اپ اسٹریم فراہم کنندگان کے ساتھ توثیق کے لیے استعمال کرتا ہے۔ لاگنگ سے پہلے انہیں **ماسک کر دیا جاتا ہے** (§2.7 دیکھیں)، لیکن یہ OmniRoute کی MITM تہہ کو نظر آتی ہیں۔ ہر ایجنٹ کو پہلی بار فعال کرنے پر ایک قابلِ برخاست خطرے کے نوٹس کا موڈل دکھائی دیتا ہے۔

### 3.6 دیکھ بھال اور تشخیص

ڈیش بورڈ ایک **دیکھ بھال اور تشخیص** کارڈ (`AgentBridgeMaintenanceCard`، جو `src/app/(dashboard)/dashboard/tools/agent-bridge/components/` میں ہے) فراہم کرتا ہے، جو ان آپریشنل MITM روٹس کو نمایاں کرتا ہے جن کا پہلے کوئی UI نہیں تھا۔ اس کا ذیلی عنوان ہے: _"کیپچر پائپ لائن کا خودکار ٹیسٹ کریں، باقی رہ جانے والی سسٹم حالت کو واپس پلٹائیں، اور اپنا سیٹ اپ مشینوں کے درمیان منتقل کریں۔"_ کارڈ کے کلائنٹ ہیلپرز `src/lib/inspector/agentBridgeMaintenanceApi.ts` میں موجود ہیں۔

| بٹن                  | روٹ                                    | یہ کیا کرتا ہے                                                                                                                                                                                          |
| -------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **تشخیص**            | `GET /api/tools/agent-bridge/diagnose` | کیپچر پائپ لائن کا خودکار ٹیسٹ چلاتا ہے اور ہر جانچ کی الگ رپورٹ دکھاتا ہے (✓/✗ + تدارکی اشارہ)۔                                                                                                        |
| **مرمت**             | `POST /api/tools/agent-bridge/repair`  | کریش یا SIGKILL کے بعد رہ جانے والی یتیم MITM سسٹم حالت (DNS اسپوف اندراجات، روٹ CA، سسٹم پراکسی) کو واپس درست کرتا ہے۔ یہ idempotent ہے — حالت صاف ہونے پر "مرمت کے لیے کچھ نہیں ہے" کی اطلاع دیتا ہے۔ |
| **CA ہٹائیں**        | `DELETE /api/tools/agent-bridge/cert`  | OS ٹرسٹ اسٹور میں MITM روٹ CA کا اعتماد ختم کرکے اسے ہٹاتا ہے (واضح، idempotent)۔ صرف اس وقت دکھایا جاتا ہے جب CA فی الحال قابلِ اعتماد ہو؛ ان لائن "CA ہٹائیں؟" تصدیق درکار ہوتی ہے۔                   |
| **کنفیگ برآمد کریں** | `GET /api/tools/agent-bridge/config`   | قابلِ منتقلی کنفیگ JSON ڈاؤن لوڈ کرتا ہے (§3.7 دیکھیں)۔                                                                                                                                                 |
| **کنفیگ درآمد کریں** | `POST /api/tools/agent-bridge/config`  | پہلے برآمد کیا گیا کنفیگ JSON اپ لوڈ کرتا ہے (§3.7 دیکھیں)۔                                                                                                                                             |

**تشخیصی جانچیں** (`src/mitm/inspector/diagnostics.ts` میں `summarizeDiagnostics()`)۔ روٹ ہر جانچ کے لیے مؤثر پروب چلاتا ہے اور بولین قدروں کو خالص خلاصہ ساز میں بھیجتا ہے؛ ایک واحد `healthy` فیصلہ اور ہر ناکامی کے لیے اشارہ واپس کیا جاتا ہے:

| جانچ کا نام        | یہ کیا تصدیق کرتی ہے                                 | ناکامی کی صورت میں اشارہ                                                                                                                                       |
| ------------------ | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server-running`   | MITM سرور پراسیس فعال ہے                             | "MITM سرور نہیں چل رہا۔ اسے AgentBridge ٹیب سے شروع کریں۔"                                                                                                     |
| `server-reachable` | MITM سرور اپنی پورٹ پر کنکشن قبول کرتا ہے (TCP پروب) | "MITM سرور اپنی پورٹ پر کنکشن قبول نہیں کر رہا۔ یقینی بنائیں کہ پورٹ خالی ہے اور اسے بائنڈ کرنے کے لیے آپ کے پاس مطلوبہ مراعات موجود ہیں۔"                     |
| `cert-exists`      | MITM سرٹیفکیٹ ڈسک پر تیار کیا جا چکا ہے              | "ابھی تک کوئی MITM سرٹیفکیٹ تیار نہیں کیا گیا۔ AgentBridge ٹیب سے ایک سرٹیفکیٹ تیار کریں۔"                                                                     |
| `cert-trusted`     | MITM روٹ CA، OS ٹرسٹ اسٹور میں موجود ہے              | "OS اسٹور MITM روٹ CA پر اعتماد نہیں کرتا، لہٰذا TLS انٹرسیپشن ناکام ہو جائے گی۔ AgentBridge ٹیب سے سرٹیفکیٹ کو قابلِ اعتماد بنائیں۔"                          |
| `dns-configured`   | ہدف ہوسٹ ناموں کو `/etc/hosts` میں اسپوف کیا گیا ہے  | "ہدف ہوسٹ نام `/etc/hosts` میں اسپوف نہیں کیے گئے، اس لیے ٹریفک کبھی پراکسی تک نہیں پہنچتی۔ جن ایجنٹس کی ٹریفک کیپچر کرنا چاہتے ہیں، ان کے لیے DNS فعال کریں۔" |

**یتیم حالت کا بینر:** جب صفحہ کسی کریش کے بعد رہ جانے والی حالت (DNS اسپوف / CA / سسٹم پراکسی) کا پتا لگاتا ہے، تو کارڈ عنبری رنگ کا بینر دکھاتا ہے — _"ایک سابقہ سیشن سسٹم کی حالت پیچھے چھوڑ گیا ہے (DNS اسپوف، CA، یا سسٹم پراکسی)۔ اسے صاف کرنے کے لیے مرمت چلائیں۔"_ — اور **مرمت** بٹن کو نمایاں کرتا ہے۔ `Repair`، ProxyBridge کے `--cleanup` فلیگ کا ایپلیکیشن سطحی مماثل ہے (یہ `src/mitm/manager.ts` میں `repairMitm()` کو کام سونپتا ہے)۔

> بار بار sudo پرامپٹس سے بچنے کے لیے MITM روٹ CA کو روکنے/شروع کرنے کے دوران
> انسٹال رکھا جاتا ہے (mitmproxy/Charles جیسا ہی طرزِ عمل)، اس لیے اسے ہٹانا ایک واضح
> **CA ہٹائیں** کارروائی ہے، نہ کہ کوئی ایسا کام جو رکنے پر خودکار طور پر ہو جائے۔

### 3.7 قابلِ منتقلی کنفیگ کی درآمد/برآمد

AgentBridge **آپریٹر کے ذریعے قابلِ ترتیب** حالت کو ایک ورژن شدہ JSON بلاب میں سیریلائز کر سکتا ہے، تاکہ ایک سیٹ اپ کو مختلف مشینوں پر نقل کیا جا سکے۔ سیریلائزر `src/lib/inspector/configPortability.ts` (`exportConfig()` / `importConfig()`) ہے، جس کی توثیق `AgentBridgeConfigSchema` کرتا ہے۔

برآمد میں عین تین حصے شامل ہوتے ہیں (بلٹ اِن ڈیفالٹس جان بوجھ کر برآمد **نہیں** کیے جاتے، تاکہ درآمد کبھی انہیں نقل نہ کرے یا ان سے متصادم نہ ہو):

| فیلڈ             | ماخذ                                                       | نوٹس                                                                    |
| ---------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------- |
| `bypassPatterns` | صارف کے متعین کردہ بائی پاس پیٹرنز (`agent_bridge_bypass`) | ڈیفالٹ bank/gov/okta پیٹرنز شامل نہیں کیے جاتے                          |
| `customHosts`    | Traffic Inspector کے کسٹم ہوسٹس (`inspector_custom_hosts`) | ہر ایک: `{ host, kind: "llm"\|"app"\|"custom", label? }`                |
| `agentMappings`  | ہر ایجنٹ کی ماڈل میپنگز (`agent_bridge_mappings`)          | میپنگز رکھنے والے ہر ایجنٹ کے لیے `{ [agentId]: [{ source, target }] }` |

```jsonc
// GET /api/tools/agent-bridge/config
{
  "version": 1,
  "bypassPatterns": ["*.internal.example.com"],
  "customHosts": [{ "host": "api.example.com", "kind": "llm", "label": null }],
  "agentMappings": {
    "copilot": [{ "source": "gpt-4o", "target": "claude-sonnet-4.7" }],
  },
}
```

**درآمد کا طرزِ عمل** (`POST /api/tools/agent-bridge/config`): بائی پاس پیٹرنز اور ہر ایجنٹ کی میپنگز **مکمل طور پر تبدیل** ہو جاتی ہیں؛ کسٹم ہوسٹس کو **idempotent انداز میں** شامل کیا جاتا ہے (`INSERT OR IGNORE`)۔ جواب بتاتا ہے کہ ہر قسم کے کتنے آئٹمز لاگو کیے گئے:

```jsonc
{ "ok": true, "bypassPatterns": 1, "customHosts": 1, "agents": 1 }
```

کنفیگ میں کیا **شامل نہیں** ہے: سرور کے چلنے کی حالت، سرٹیفکیٹ کے پاتھ، ہر ایجنٹ کی DNS حالت، اپ اسٹریم CA کا پاتھ، اور TPROXY کی ترتیبات — یہ ہوسٹ/رن ٹائم کی حالتیں ہیں، قابلِ منتقلی ترجیحات نہیں۔

---

## §4 فی ایجنٹ حوالہ

| #   | ایجنٹ              | حیثیت           | انٹرسیپٹ کیے گئے ہوسٹس                                             | تصدیق کی قسم   |
| --- | ------------------ | --------------- | ------------------------------------------------------------------ | -------------- |
| 1   | **Antigravity**    | ✅ معاونت یافتہ | `daily-cloudcode-pa.googleapis.com`, `cloudcode-pa.googleapis.com` | Firebase OAuth |
| 2   | **Kiro (AWS)**     | ✅ معاونت یافتہ | `prod.kiro.aws`, `dev.kiro.aws`                                    | AWS SigV4      |
| 3   | **GitHub Copilot** | ✅ معاونت یافتہ | `api.githubcopilot.com`, `copilot-proxy.githubusercontent.com`     | GitHub OAuth   |
| 4   | **OpenAI Codex**   | ✅ معاونت یافتہ | `api.openai.com` (Codex کے راستے)، `chatgpt.com`                   | OpenAI کلید    |
| 5   | **Cursor IDE**     | ✅ معاونت یافتہ | `api2.cursor.sh`, `api.cursor.sh`                                  | Cursor OAuth   |
| 6   | **Zed Industries** | ✅ معاونت یافتہ | `api.zed.dev`, `llm.zed.dev`                                       | Zed OAuth      |
| 7   | **Claude Code**    | ✅ معاونت یافتہ | `api.anthropic.com` (اختیاری)                                      | Anthropic کلید |
| 8   | **Open Code**      | ✅ معاونت یافتہ | `openrouter.ai`, `api.openai.com` (zen کے راستے)                   | API کلید       |
| 9   | **Trae**           | 🔍 زیرِ تحقیق   | طے ہونا باقی — §8 دیکھیں                                           | طے ہونا باقی   |

### سیٹ اپ وزارڈ کے مراحل (فی ایجنٹ)

ہر ایجنٹ کارڈ میں 3 مراحل پر مشتمل سیٹ اپ وزارڈ ہوتا ہے:

1. **پیشگی شرائط کی توثیق کریں** — کیا سرور چل رہا ہے؟ کیا سرٹیفکیٹ قابلِ اعتماد ہے؟ کیا IDE انسٹال ہے (خودکار طور پر دریافت کیا گیا)؟
2. **DNS فعال کریں** — `/etc/hosts` میں اندراجات شامل کرتا ہے (sudo درکار ہے)۔ بالکل وہی سطریں دکھاتا ہے جو شامل کی جائیں گی۔
3. **ماڈلز میپ کریں** — اختیاری ماڈل میپنگ جدول۔ وائلڈ کارڈز قابلِ قبول ہیں۔

### ایجنٹ کی دریافت

ایجنٹس 1–8 کے لیے، AgentBridge خودکار طور پر IDE کی تنصیب دریافت کرنے کی کوشش کرتا ہے:

```ts
export async function detectAgent(agentId: AgentId): Promise<DetectionResult>;
// واپس کرتا ہے: { installed: boolean, version?: string, path?: string }
```

دریافت میں OS کے لیے مخصوص راستے اور بائنری جانچ استعمال ہوتی ہے (مثلاً Copilot کے لیے `code --list-extensions | grep github.copilot`، اور Antigravity کے لیے `~/.config/antigravity/`)۔

---

## §5 سیکیورٹی

### لاگو کردہ سخت اصول

| اصول                              | اطلاق                                                                                              |
| --------------------------------- | -------------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`    | جواب یا بفر اندراج سے پہلے تمام ہینڈلر کی خرابیوں کو صاف کیا جاتا ہے                               |
| **#13** شیل env پاس کرنا          | `/etc/hosts` میں ترامیم `env` اختیار استعمال کرتی ہیں — راستوں کی کوئی اسٹرنگ انٹرپولیشن نہیں      |
| **#15 + #17** `isLocalOnlyPath()` | `/api/tools/agent-bridge/`، LOCAL_ONLY + SPAWN_CAPABLE ہے — تصدیق سے پہلے لوپ بیک نافذ کیا جاتا ہے |

### حساس ہوسٹس کے لیے بائی پاس فہرست

بائی پاس فہرست یقینی بناتی ہے کہ مالیاتی اداروں، OAuth/SSO فراہم کنندگان، اور دیگر حساس ہوسٹس کو **کبھی ڈی کرپٹ نہ کیا جائے**۔ ان کی TLS ٹریفک ایک شفاف TCP ٹنل کے طور پر گزرتی ہے — OmniRoute کبھی سادہ متن نہیں دیکھتا۔

ڈیفالٹ بائی پاس پیٹرنز میں شامل ہیں:

- `*.bank.*`، `*.gov.*` (مالیاتی/سرکاری)
- `*.okta.com`، `*.auth0.com`، `*.microsoft.com` (SSO/شناخت)
- `*.apple.com`، `*.icloud.com` (Apple سسٹم سروسز)

صارف کے شامل کردہ بائی پاس پیٹرنز `agent_bridge_bypass` جدول میں محفوظ ہوتے ہیں اور ہر چیز پر فوقیت رکھتے ہیں۔

### رازوں کی ماسکنگ

`src/mitm/maskSecrets.ts` سے `maskSecrets()` کا اطلاق ہوتا ہے:

- `TrafficBuffer.push()` سے پہلے ہر درخواست کی باڈی پر
- لاگ کرنے یا براڈکاسٹ کرنے سے پہلے ہر ہیڈر پر

پیٹرنز: `sk-`/`ak-`/`pk-` سابقے والے ٹوکنز، `Bearer` ٹوکنز، اور کم از کم 40 حروف پر مشتمل عمومی ٹوکنز۔

### اپ اسٹریم CA سرٹیفکیٹ

جب `AGENTBRIDGE_UPSTREAM_CA_CERT` سیٹ ہو، تو فائل کو آغاز کے وقت پڑھا جاتا ہے۔ اگر راستہ موجود ہو لیکن فائل قابلِ مطالعہ نہ ہو، تو AgentBridge ایک واضح خرابی لاگ کرتا ہے اور شروع ہونے سے انکار کر دیتا ہے (کارپوریٹ ماحول میں خاموش TLS ناکامیوں کو روکتا ہے)۔

### معلوم حدود

- **پورٹ 443 کے لیے استحقاق درکار ہے**: Linux پر، AgentBridge کو Node بائنری پر `setcap 'cap_net_bind_service=+ep'` درکار ہے، یا اسے `authbind` کے ذریعے چلائیں۔ سیٹ اپ وزارڈ OS کے لیے مخصوص ہدایات دکھاتا ہے۔
- **IDE کو دوبارہ شروع کرنا ضروری ہے**: DNS ری ڈائریکٹ کے بعد، نئی ہوسٹ ریزولیوشن مؤثر بنانے کے لیے IDE کو دوبارہ شروع کرنا ضروری ہے۔
- **ہارڈ کوڈ شدہ OAuth ٹوکنز**: کچھ ایجنٹس (Kiro، Antigravity) OAuth ریفریش ٹوکنز مقامی طور پر محفوظ کرتے ہیں۔ یہ AgentBridge کے لیے شفاف ہوتے ہیں — اسے ہر درخواست میں Bearer ٹوکن نظر آتا ہے، جسے لاگ کرنے سے پہلے ماسک کر دیا جاتا ہے۔
- **Electron فرنٹ اینڈز کو `NODE_EXTRA_CA_CERTS` درکار ہے**: وہ IDEs جن کا فرنٹ اینڈ بنڈل شدہ Node/Electron رن ٹائم پر چلتا ہے، OS/NSS ٹرسٹ اسٹور کو نظر انداز کرتے ہیں اور انہیں ایسے شیل سے لانچ کرنا ضروری ہے جس میں `NODE_EXTRA_CA_CERTS` سیٹ ہو (§3.2 دیکھیں)۔ اس کے نہ ہونے کی علامت: IDE بیک اینڈ تصدیق کر لیتا ہے (MITM میں `200` دکھائی دیتے ہیں) لیکن UI لاگ آؤٹ ہی رہتا ہے۔
- **ایک ہی IDE کی متعدد تنصیبات آزاد ہوتی ہیں**: سسٹم تنصیب (مثلاً `/usr/share/antigravity/antigravity`) اور صارف کی مقامی "Full" تنصیب (مثلاً `~/AntigravityIDE_Full/antigravity-ide`) اپنے الگ رن ٹائمز کے ساتھ الگ پراسیسز ہیں — ہر ایک کو CA انجیکٹ کر کے دوبارہ لانچ کرنا ضروری ہے۔ دوبارہ لانچ کرنے سے پہلے اس کے بائنری راستے سے شناخت کریں کہ کون سی تنصیب چل رہی ہے۔
- **شناخت روٹ کیے گئے ماڈل سے نہیں بلکہ ایجنٹ کے سسٹم پرامپٹ سے متعین ہوتی ہے**: جب آپ کسی ایجنٹ کے ماڈل کو کسی مختلف فراہم کنندہ پر دوبارہ میپ کرتے ہیں، تب بھی جواب ایجنٹ کی مقامی شناخت کا دعویٰ کرتا ہے (مثلاً Antigravity جواب دیتا ہے "مجھے Gemini تقویت دیتا ہے") کیونکہ IDE اسے سسٹم پرامپٹ میں شامل کرتا ہے۔ اصل بیک اینڈ کی تصدیق ماڈل سے اس کی شناخت پوچھ کر نہیں بلکہ `call_logs` / `proxy_logs` (`provider`، `model`، `target_format`) میں کریں۔

---

## §6 مسائل کا ازالہ

### پورٹ 443 کا ٹکراؤ

اگر کوئی دوسرا پراسیس پہلے ہی پورٹ 443 پر سن رہا ہو (ویب سرور، VPN، وغیرہ):

```bash
lsof -i :443          # پراسیس تلاش کریں
sudo fuser -k 443/tcp  # زبردستی بند کریں (احتیاط سے استعمال کریں)
```

متبادل طور پر، AgentBridge کی ترتیبات میں ایک غیر مراعات یافتہ پورٹ ترتیب دیں اور `iptables` / `pf` ری ڈائریکٹ قواعد قائم کریں۔

### سرٹیفکیٹ قابلِ اعتماد نہیں ہے

اگر AgentBridge شروع کرنے کے بعد IDE میں TLS کی خرابیاں دکھائی دیں:

1. تصدیق کریں کہ سرٹیفکیٹ انسٹال ہو چکا ہے: `security find-certificate -c "OmniRoute AgentBridge"` (macOS) یا `certutil -L -d sql:$HOME/.pki/nssdb` (Linux/NSS)
2. کچھ ایپس اپنا الگ ٹرسٹ اسٹور برقرار رکھتی ہیں (Firefox، Linux پر Chrome)۔ "Trust Cert" دوبارہ چلائیں اور NSS/Firefox کے مخصوص سرٹیفکیٹ اسٹور کی جانچ کریں۔
3. اعتماد قائم کرنے کے بعد IDE دوبارہ شروع کریں — پہلے سے جاری TLS سیشنز اعتماد کی پرانی حالت استعمال کرتے ہیں۔

### قابلِ اعتماد CA کے باوجود IDE لاگ آؤٹ / "connection error"

علامت: DNS ری ڈائریکٹ کرنے اور CA پر اعتماد قائم کرنے کے بعد، Electron پر مبنی IDE (مثلاً Antigravity)
**لاگ آؤٹ شدہ** حالت میں کھلتا ہے یا توثیق/کنکشن کی خرابی دکھاتا ہے، حالانکہ MITM لاگ میں
بوٹسٹریپ کالز (`loadCodeAssist`، `fetchAvailableModels`، …) کو `200` واپس کرتے ہوئے دکھایا جاتا ہے۔

وجہ: IDE کا **بنڈل شدہ Node/Electron رن ٹائم OS کے ٹرسٹ اسٹور کو نظر انداز کرتا ہے**۔ مقامی
بیک اینڈ (ایک Go لینگویج سرور) OS کے CA پر اعتماد کرتا ہے اور توثیق کر لیتا ہے، لیکن Electron فرنٹ اینڈ
ایسا نہیں کرتا — اس لیے UI سمجھتا ہے کہ وہ آف لائن ہے۔

حل (دونوں مراحل): `NODE_EXTRA_CA_CERTS=<ca.crt>` ایکسپورٹ کریں **اور اسی
شیل سے IDE دوبارہ لانچ کریں**، ڈیسک ٹاپ آئیکن سے نہیں۔ پہلے IDE کو مکمل طور پر بند کریں — Electron کے سنگلٹن لاک کا مطلب ہے کہ
دوسری بار لانچ کرنے سے صرف موجودہ پراسیس فوکس ہوتا ہے اور نیا ماحول نظر انداز کر دیا جاتا ہے۔ §3.2 دیکھیں۔
یہ ایک کھلی اپ اسٹریم رپورٹ سے مطابقت رکھتا ہے، جہاں ایک اسٹینڈ الون ایجنٹ MITM کے ذریعے کام کرتا ہے لیکن IDE
ویریئنٹ اسی سیٹ اپ کے تحت ناکام ہو جاتا ہے۔

### DNS کی تبدیلیاں نافذ نہیں ہوئیں

جانچ کریں کہ `/etc/hosts` اپ ڈیٹ ہوا ہے:

```bash
grep "omniroute\|127.0.0.1.*github\|127.0.0.1.*cursor" /etc/hosts
```

DNS کیش صاف کریں:

```bash
# macOS
sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder
# Linux (systemd-resolved)
sudo systemctl restart systemd-resolved
# Windows
ipconfig /flushdns
```

### IDE کا پتہ نہیں چلا

خودکار شناخت عام انسٹالیشن پاتھ استعمال کرتی ہے۔ اگر شناخت ناکام ہو جائے لیکن IDE انسٹال ہو:

- جانچ کریں کہ آیا IDE بائنری کسی غیر معیاری مقام پر ہے
- Setup Wizard پھر بھی کام کرتا ہے — شناخت کی ناکامی کا صرف یہ مطلب ہے کہ بیج انسٹالیشن پاتھ نہیں دکھائے گا

### ہینڈلر کی خرابیاں (اپ اسٹریم فیچ ناکام)

اگر AgentBridge انٹرسیپٹ تو کرتا ہے لیکن تمام درخواستیں ناکام ہو جاتی ہیں:

1. تصدیق کریں کہ `/dashboard/providers` پر کم از کم ایک پرووائیڈر منسلک ہے
2. OmniRoute سرور لاگز کی جانچ کریں: `.env` میں `APP_LOG_LEVEL=debug`
3. تصدیق کریں کہ `OMNIROUTE_BASE_URL` درست روٹر اینڈ پوائنٹ کی طرف اشارہ کرتا ہے (ڈیفالٹ: `http://127.0.0.1:20128`)

---

## §7 API حوالہ

تمام روٹس `LOCAL_ONLY` ہیں (صرف لوپ بیک، توثیق سے پہلے نافذ) اور `SPAWN_CAPABLE` ہیں۔ `src/server/authz/routeGuard.ts` دیکھیں۔

بنیادی پاتھ: `/api/tools/agent-bridge/`

| طریقہ               | راستہ                                          | تفصیل                                                                                                               |
| ------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| GET                 | `/api/tools/agent-bridge/state`                | عالمی سرور کی حالت + ہر ایجنٹ کی شناخت/حالت                                                                         |
| GET                 | `/api/tools/agent-bridge/agents`               | رجسٹرڈ ایجنٹس کی فہرست (id، نام، hosts، قابلِ عمل ہونے کی کیفیت، حالت)                                              |
| GET                 | `/api/tools/agent-bridge/agents/{id}`          | ایک ایجنٹ کی حالت (ہدف کی کنفیگریشن + شناخت + محفوظ شدہ حالت)                                                       |
| PATCH               | `/api/tools/agent-bridge/agents/{id}`          | ایجنٹ کے لیے `setup_completed` اپ ڈیٹ کریں                                                                          |
| GET                 | `/api/tools/agent-bridge/agents/{id}/detect`   | ایجنٹ کے لیے شناختی جانچ چلائیں (`installed`، `version?`، `path?`)                                                  |
| POST                | `/api/tools/agent-bridge/agents/{id}/dns`      | ایجنٹ کے لیے DNS فعال/غیر فعال کریں (`{enabled: boolean}`)                                                          |
| GET                 | `/api/tools/agent-bridge/agents/{id}/mappings` | ایجنٹ کے لیے ماڈل میپنگز                                                                                            |
| PUT                 | `/api/tools/agent-bridge/agents/{id}/mappings` | ماڈل میپنگز تبدیل کریں                                                                                              |
| POST                | `/api/tools/agent-bridge/server`               | سرور شروع/بند/دوبارہ شروع کریں (`action: "start"\|"stop"\|"restart"\|"trust-cert"\|"regenerate-cert"`)              |
| GET                 | `/api/tools/agent-bridge/cert`                 | سرٹیفکیٹ کی حالت (`exists`، `trusted`، `path`)                                                                      |
| POST                | `/api/tools/agent-bridge/cert`                 | MITM روٹ CA پر اعتماد کریں (انسٹال کریں)                                                                            |
| DELETE              | `/api/tools/agent-bridge/cert`                 | MITM روٹ CA پر اعتماد ختم کریں (ہٹائیں) — idempotent (دیکھیے §3.6)                                                  |
| POST                | `/api/tools/agent-bridge/cert/regenerate`      | خود دستخط شدہ MITM سرٹیفکیٹ دوبارہ تیار کریں                                                                        |
| GET                 | `/api/tools/agent-bridge/cert/download`        | ڈاؤن لوڈ کے لیے PEM سرٹیفکیٹ اسٹریم کریں                                                                            |
| GET                 | `/api/tools/agent-bridge/bypass`               | بائی پاس پیٹرنز کی فہرست (`default` + `user`)                                                                       |
| POST                | `/api/tools/agent-bridge/bypass`               | صارف کے متعین کردہ بائی پاس پیٹرنز کو مکمل طور پر تبدیل کریں                                                        |
| DELETE              | `/api/tools/agent-bridge/bypass?pattern=...`   | صارف کا متعین کردہ ایک بائی پاس پیٹرن ہٹائیں                                                                        |
| GET                 | `/api/tools/agent-bridge/diagnose`             | کیپچر پائپ لائن کی خودکار جانچ (دیکھیے §3.6)                                                                        |
| POST                | `/api/tools/agent-bridge/repair`               | لاوارث MITM سسٹم حالت کو واپس کریں (دیکھیے §3.6)                                                                    |
| GET                 | `/api/tools/agent-bridge/config`               | قابلِ منتقلی کنفیگریشن JSON برآمد کریں (دیکھیے §3.7)                                                                |
| POST                | `/api/tools/agent-bridge/config`               | قابلِ منتقلی کنفیگریشن JSON درآمد کریں (دیکھیے §3.7)                                                                |
| GET                 | `/api/tools/agent-bridge/upstream-ca`          | کنفیگر کردہ upstream CA کا راستہ حاصل کریں                                                                          |
| POST                | `/api/tools/agent-bridge/upstream-ca`          | upstream CA کے راستے کی توثیق کریں + محفوظ کریں                                                                     |
| POST                | `/api/tools/agent-bridge/upstream-ca/test`     | upstream CA کے راستے کی صرف توثیق (dry-run) کریں — محفوظ نہیں کرتا                                                  |
| GET / POST / DELETE | `/api/tools/agent-bridge/tproxy`               | TPROXY شفاف-ڈیکرپٹ کیپچر موڈ — دیکھیے `docs/security/MITM-TPROXY-DECRYPT.md` (git؛ `/docs` میں کمپائل نہیں کیا گیا) |

مکمل OpenAPI اسکیمے: `docs/openapi.yaml` → ٹیگ `AgentBridge`۔

---

## §8 روڈ میپ

### Trae کی تحقیقات

Trae نسبتاً نیا AI کوڈنگ اسسٹنٹ ہے۔ ہینڈلر نافذ کرنے سے پہلے:

1. VS Code / JetBrains مارکیٹ پلیسز یا اسٹینڈ الون ایپ میں بائنری/ایکسٹینشن کی شناخت کریں
2. API ہوسٹس اور اینڈ پوائنٹس کی ساخت دریافت کرنے کے لیے mitmproxy کے ذریعے ٹریفک کیپچر کریں
3. توثیق کے طریقۂ کار کا تعین کریں
4. شرائطِ خدمت اور API کی قابلِ دریافت ہونے کی بنیاد پر آگے بڑھنے یا نہ بڑھنے کا جائزہ لیں

تحقیقات مکمل ہونے تک، ڈیش بورڈ میں Trae کارڈ پر "زیرِ تحقیق" بیج اور "قابلِ عمل ہونے کی رپورٹ دیں" لنک دکھائی دیتا ہے۔ `src/mitm/handlers/trae.ts` میں موجود ہینڈلر اسٹب ایک منظم `ابھی نافذ نہیں کیا گیا` خرابی پھینکتا ہے۔

### بیک لاگ ایجنٹس (MITM درکار ہے — حسبِ ضرورت بنیادی URL کی معاونت موجود نہیں)

درج ذیل ٹولز کے موجودہ ورژنز حسبِ ضرورت بنیادی URLs کی معاونت نہیں کرتے، جس کی وجہ سے MITM مداخلت کا واحد راستہ ہے۔ قابلِ عمل ہونے کا جائزہ ابھی زیرِ التوا ہے:

- **Windsurf** (Codeium/Cognition)
- **Amp** (Sourcegraph)
- **Amazon Q / Kiro CLI** (AWS Bedrock — Kiro IDE سے الگ)
- **Cowork** (Anthropic ڈیسک ٹاپ)

نوٹ: GitHub Copilot CLI ≥v1.0.19، `COPILOT_PROVIDER_BASE_URL` کی معاونت کرتا ہے — اس ٹول کے لیے MITM کی بجائے براہِ راست کنفیگریشن استعمال کریں۔
