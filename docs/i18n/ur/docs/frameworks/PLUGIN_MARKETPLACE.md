# Plugin Marketplace (اردو)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇹 [am](../../../am/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇿 [az](../../../az/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇰 [da](../../../da/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇩🇪 [de](../../../de/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇷 [el](../../../el/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇸 [es](../../../es/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇪🇪 [et](../../../et/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇱 [he](../../../he/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇩 [id](../../../id/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇹 [it](../../../it/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇭 [km](../../../km/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇲🇲 [my](../../../my/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇴 [no](../../../no/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [or](../../../or/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇱🇰 [si](../../../si/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇮🇳 [te](../../../te/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇭 [th](../../../th/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/PLUGIN_MARKETPLACE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/PLUGIN_MARKETPLACE.md)

---

> **حقیقت کا مستند ماخذ:** `src/lib/plugins/` (`marketplace.ts`, `manager.ts`, `manifest.ts`,
> `scanner.ts`, `loader.ts`)، `src/app/api/plugins/`، اور
> `src/app/(dashboard)/dashboard/plugins/`
> **آخری بار اپ ڈیٹ کیا گیا:** 2026-06-28 — v3.8.40

OmniRoute ایک WordPress طرز کے پلگ اِن سسٹم کے ساتھ آتا ہے۔ پلگ اِن خود کفیل
ڈائریکٹریاں ہیں — ہر ایک میں ایک `plugin.json` مینی فیسٹ اور ایک انٹری فائل ہوتی ہے — جو
درخواست کی پائپ لائن (`onRequest` / `onResponse` / `onError`) اور
لائف سائیکل ایونٹس (`onInstall` / `onActivate` / `onDeactivate` / `onUninstall`) سے
منسلک ہوتے ہیں۔

**پلگ اِن مارکیٹ پلیس** اس سسٹم کے اوپر موجود دریافت کی تہہ ہے۔ یہ
انسٹال کیے جا سکنے والے پلگ اِنز کا قابلِ براؤز کیٹلاگ فراہم کرتی ہے۔ بطور ڈیفالٹ یہ کیٹلاگ
ایک مختصر پہلے سے موجود سیڈ رجسٹری ہے؛ آپریٹر اسے حسبِ ضرورت ریموٹ
رجسٹری URL کی طرف بھیج سکتا ہے، ایسی صورت میں fetch کو DNS-resolving SSRF گارڈ
کے ذریعے محفوظ بنایا جاتا ہے
([سیکیورٹی](#security) دیکھیں)۔

ہر پلگ اِن روٹ **صرف loopback کے لیے** ہے (Tier 1 — `LOCAL_ONLY`): پلگ اِنز
child processes میں کوڈ لوڈ اور execute کرتے ہیں، اس لیے auth سے قطع نظر یہ روٹس
کسی non-loopback origin سے قابلِ رسائی نہیں ہوتے۔ دیکھیے
[`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md)۔

## یہ سب مل کر کیسے کام کرتا ہے

```
ڈیش بورڈ (/dashboard/plugins)
  ├─ "انسٹال شدہ" ٹیب  → GET /api/plugins            (listPlugins)
  │                     POST /api/plugins/scan      (pluginManager.scan)
  │                     POST /api/plugins/{name}/activate|deactivate
  │                     DELETE /api/plugins/{name}   (اَن انسٹال)
  └─ "مارکیٹ پلیس" ٹیب → GET /api/plugins/marketplace
                          → listMarketplacePlugins()
                            ├─ کوئی حسبِ ضرورت URL نہیں → بلٹ اِن SEED_REGISTRY
                            └─ حسبِ ضرورت URL → isSafeMarketplaceUrl() SSRF گارڈ
                                          → safeOutboundFetch(guard:"public-only")
```

- **رجسٹری کی تہہ** — `src/lib/plugins/marketplace.ts`: کیٹلاگ کی فہرست سازی /
  تلاش کرتی ہے اور کسی بھی ناکامی کی صورت میں سیڈ رجسٹری پر واپس آ جاتی ہے۔
- **لائف سائیکل کی تہہ** — `src/lib/plugins/manager.ts` (`pluginManager` singleton):
  انسٹال، اپ گریڈ، فعال، غیر فعال، اَن انسٹال، اسکین، اسٹارٹ اَپ لوڈ۔
- **مینی فیسٹ کی تہہ** — `src/lib/plugins/manifest.ts`: `plugin.json` کے لیے
  Zod اسکیما + ڈیفالٹس۔
- **اسکینر** — `src/lib/plugins/scanner.ts`: پلگ اِن ڈائریکٹری کے تحت
  ڈسک پر موجود پلگ اِنز دریافت کرتا ہے۔
- **لوڈر** — `src/lib/plugins/loader.ts`: ہر پلگ اِن کو ایک الگ تھلگ
  child process میں شروع کرتا ہے اور IPC کے ذریعے hook calls کی بروکری کرتا ہے۔

## مارکیٹ پلیس کیٹلاگ

`listMarketplacePlugins()` (`src/lib/plugins/marketplace.ts`)
`MarketplaceEntry` آبجیکٹس کی ایک فہرست لوٹاتا ہے:

| فیلڈ          | قسم      | نوٹس                                       |
| ------------- | -------- | ------------------------------------------ |
| `name`        | string   | kebab-case پلگ اِن نام                     |
| `version`     | string   | semver                                     |
| `description` | string   | مختصر خلاصہ                                |
| `author`      | string   | مصنف / ادارہ                               |
| `license`     | string   | SPDX طرز کا لائسنس id                      |
| `downloadUrl` | string   | ماخذ کا ڈاؤن لوڈ URL (خالی ہو سکتا ہے)     |
| `repository`  | string?  | اختیاری ریپوزٹری URL                       |
| `tags`        | string[] | تلاش/فلٹر کے ٹیگز                          |
| `downloads`   | number   | ڈاؤن لوڈز کی تعداد                         |
| `rating`      | number   | 0–5                                        |
| `verified`    | boolean  | آیا اندراج کو تصدیق شدہ نشان زد کیا گیا ہے |
| `lastUpdated` | string   | ISO طرز کی تاریخ کی اسٹرنگ                 |

جب کوئی حسبِ ضرورت رجسٹری URL ترتیب نہ دیا گیا ہو تو کیٹلاگ بلٹ اِن
`SEED_REGISTRY` ہوتا ہے (فی الحال `request-logger`، `rate-limiter`، `cost-tracker`، اور
`theme-manager`)۔ سیڈ رجسٹری ہمیشہ دستیاب رہتی ہے — اگر ترتیب دی گئی ریموٹ
رجسٹری ناقابلِ رسائی ہو، non-`200` اسٹیٹس لوٹائے، یا ناقابلِ شناخت
body لوٹائے، تو `listMarketplacePlugins()` ایک انتباہ لاگ کرتا ہے اور سیڈ فہرست پر واپس آ جاتا ہے۔

> نوٹ: مارکیٹ پلیس **کیٹلاگ** (براؤز/تلاش) ایک سرے سے دوسرے سرے تک مربوط ہے، لیکن
> کیٹلاگ سے ایک کلک میں مارکیٹ پلیس **انسٹالیشن** ابھی نافذ نہیں کی گئی — کسی مارکیٹ پلیس
> اندراج پر ڈیش بورڈ کا "انسٹال کریں" بٹن فی الحال
> "جلد آ رہا ہے" کا نوٹس دکھاتا ہے۔ آج انسٹالیشن مقامی پاتھ کے انسٹال
> فلو (`POST /api/plugins`) اور ڈسک پر دریافت (`POST /api/plugins/scan`) کے ذریعے ہوتی ہے۔

## REST API

تمام endpoints کے لیے management auth (`requireManagementAuth`) درکار ہے **اور** یہ
صرف loopback تک محدود ہیں — `/api/plugins` اور `/api/plugins/` کو
`LOCAL_ONLY_API_PREFIXES` (`src/server/authz/routeGuard.ts`) میں درج کیا گیا ہے۔

| Endpoint                         | طریقہ  | تفصیل                                                       |
| -------------------------------- | ------ | ----------------------------------------------------------- |
| `/api/plugins`                   | GET    | نصب شدہ plugins کی فہرست (اختیاری `?status=` فلٹر)          |
| `/api/plugins`                   | POST   | کسی absolute مقامی path سے plugin نصب کریں                  |
| `/api/plugins/scan`              | POST   | plugin ڈائریکٹری اسکین کر کے نئے plugins رجسٹر کریں         |
| `/api/plugins/marketplace`       | GET    | marketplace catalog entries کی فہرست                        |
| `/api/plugins/[name]`            | GET    | نصب شدہ plugin کی تفصیلات حاصل کریں                         |
| `/api/plugins/[name]`            | DELETE | plugin اَن انسٹال کریں                                      |
| `/api/plugins/[name]/activate`   | POST   | فعال کریں (لوڈ کریں + hooks رجسٹر کریں)                     |
| `/api/plugins/[name]/deactivate` | POST   | غیر فعال کریں (`onDeactivate` چلائیں، hooks اَن رجسٹر کریں) |
| `/api/plugins/[name]/config`     | GET    | plugin config + config schema حاصل کریں                     |
| `/api/plugins/[name]/config`     | PUT    | plugin config اپ ڈیٹ کریں (schema کے مطابق توثیق شدہ)       |

`GET /api/plugins` کا `status` فلٹر ان میں سے ایک قدر قبول کرتا ہے:
`installed` / `active` / `inactive` / `error`۔ کوئی غیر درست قدر `400` واپس کرتی ہے۔

### نصب شدہ plugins کی فہرست

```bash
curl http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..."
```

### مقامی path سے انسٹال کرنا

```bash
curl -X POST http://localhost:20128/api/plugins \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "path": "/absolute/path/to/my-plugin" }'
```

`path` کا **absolute** ہونا ضروری ہے اور اس میں `..` traversal segments یا
null bytes شامل نہیں ہو سکتے (Zod کے ذریعے نافذ کردہ)۔ source ڈائریکٹری میں ایک درست
`plugin.json` موجود ہونا چاہیے (یا یہ ایسی ڈائریکٹری کی parent ہو)۔ کامیابی پر response
نصب شدہ plugin row کے ساتھ `201` ہوتا ہے۔

### marketplace براؤز کرنا

```bash
curl http://localhost:20128/api/plugins/marketplace \
  -H "Cookie: auth_token=..."
```

### plugin config اپ ڈیٹ کرنا

```bash
curl -X PUT http://localhost:20128/api/plugins/my-plugin/config \
  -H "Cookie: auth_token=..." \
  -H "Content-Type: application/json" \
  -d '{ "config": { "level": "debug", "maxItems": 100 } }'
```

`PUT .../config` فراہم کردہ ہر قدر کو plugin کے
`configSchema` (manifest میں اعلان کردہ) کے مطابق validate کرتا ہے: `number` fields
`min`/`max` کی پابندی کرتے ہیں، جبکہ `select` fields کو اعلان کردہ `enum` سے مطابقت
رکھنا ضروری ہے۔ schema میں غیر موجود keys کو گزرنے کی اجازت ہے۔

## Configuration

### Plugin ڈائریکٹری

Plugins، OmniRoute data ڈائریکٹری کے تحت موجود ہوتے ہیں:

```
~/.omniroute/plugins/<plugin-name>/
  ├─ plugin.json
  └─ index.js          # (یا وہ فائل جس کی طرف manifest.main اشارہ کرتا ہے)
```

`getDefaultPluginDir()` (`src/lib/plugins/scanner.ts`) اس ڈائریکٹری کو
تین مراحل میں resolve کرتا ہے:

1. **`OMNIROUTE_PLUGINS_DIR`**، جب سیٹ ہو — `HOME` کی قدر سے قطع نظر، اسے من و عن استعمال کیا جاتا ہے۔ یہ
   Docker/K8s کے لیے واضح knob ہے، جہاں plugin tree کسی ایسے path پر bind-mounted ہوتا ہے
   جس کا عموماً container کی home ڈائریکٹری سے کوئی تعلق نہیں ہوتا (#11827)۔
2. `<home>/.omniroute/plugins`، جہاں `<home>` کی قدر `HOME` / `USERPROFILE`
   environment variables سے حاصل ہوتی ہے۔
3. `/tmp/.omniroute/plugins`، جب process کسی home کو export نہ کرے۔

resolve شدہ ڈائریکٹری startup پر ایک بار `scanner.dir_resolved` کے طور پر log کی جاتی ہے،
اور اس input کا نام بھی دیا جاتا ہے جو منتخب ہوا (`OMNIROUTE_PLUGINS_DIR`، `home`، یا
`no-home-fallback`) — تاکہ step 3 پر خاموشی سے پہنچنے والی image محض خالی plugin list
رپورٹ کرنے کے بجائے اس کی وضاحت بھی کرے۔ `POST /api/plugins/scan` وہاں موجود ہر ایسی
subdirectory کو دریافت اور رجسٹر کرتا ہے جس میں درست `plugin.json` ہو؛ یہی ڈائریکٹری وہ
root بھی ہے جس میں `pluginManager.install()` plugins کو copy کرتا ہے، اس لیے override
سے discovery اور installation دونوں ایک ساتھ منتقل ہوتے ہیں۔

> **`OMNIROUTE_PLUGINS_DIR`، `OMNIROUTE_PLUGIN_PATH` نہیں ہے۔** مؤخر الذکر کو صرف
> CLI command-plugin loader (`bin/cli/plugins.mjs`) پڑھتا ہے تاکہ ایسے `omniroute-cmd-*` npm
> packages تلاش کیے جا سکیں جو `omniroute` subcommands شامل کرتے ہیں — اس کا یہاں بیان کردہ
> runtime scanner پر کوئی اثر نہیں ہوتا۔ اس پہلو کے لیے [PLUGINS.md](./PLUGINS.md) دیکھیں۔

### حسبِ ضرورت marketplace registry URL

marketplace catalog کا source، `pluginMarketplaceUrl` setting سے پڑھا جاتا ہے
(`src/lib/plugins/marketplace.ts`، `settings.pluginMarketplaceUrl` کو پڑھتا ہے)۔ جب
اسے کسی `http(s)` URL پر سیٹ کیا جائے تو `listMarketplacePlugins()` اس URL کو fetch کرتا
ہے اور entries کی top-level JSON array یا `plugins` array رکھنے والا object قبول کرتا
ہے؛ جن entries میں string `name` موجود نہ ہو انہیں filter کر دیا جاتا ہے۔ جب یہ سیٹ نہ
ہو (یا جب fetch، SSRF guard میں ناکام ہو / خراب response واپس کرے)، تو built-in seed
registry استعمال کی جاتی ہے۔

dashboard کا "Marketplace" tab اس URL کے لیے ایک field فراہم کرتا ہے (جسے
`GET /api/settings` سے دوبارہ پڑھا جاتا ہے)۔

> نفاذ سے متعلق نوٹ: dashboard کا "Save" action،
> `pluginMarketplaceUrl` کو `PATCH /api/settings` پر بھیجتا ہے۔ تحریر کے وقت یہ
> key، `updateSettingsSchema`
> (`src/shared/validation/settingsSchemas.ts`) میں اعلان شدہ نہیں ہے، اس لیے اس پر
> انحصار کرنے سے پہلے اپنی release میں persistence کی تصدیق کریں — **read** path
> (`getSettings()` → `listMarketplacePlugins()`) settings store میں key موجود ہونے
> کے بعد اسے استعمال کرتا ہے۔

## سیکیورٹی

### روٹ درجہ — صرف لوپ بیک

پلگ اِنز الگ سے شروع کیے گئے چائلڈ پراسیسز میں کوڈ چلاتے ہیں، اس لیے مکمل `/api/plugins`
سطح کو `LOCAL_ONLY` (درجہ 1) کے طور پر درجہ بند کیا گیا ہے۔ لوپ بیک کا نفاذ
ہر توثیقی جانچ سے **پہلے** غیر مشروط طور پر ہوتا ہے، اس لیے کسی ٹنل کے ذریعے
مشین تک پہنچنے والا افشا شدہ مینجمنٹ ٹوکن بھی کسی پلگ اِن کو انسٹال، فعال یا اَن انسٹال نہیں کر سکتا۔
[`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) اور
سخت قواعد #15 / #17 دیکھیں۔

### مارکیٹ پلیس رجسٹری SSRF گارڈ

حسبِ ضرورت رجسٹری URL ایک ایسی کنفیگریشن ہے جس پر حملہ آور اثر انداز ہو سکتا ہے، اس لیے
اسے حاصل کرنے سے پہلے `listMarketplacePlugins()` اسے دو حفاظتی تہوں سے گزارتا ہے:

1. **`isSafeMarketplaceUrl(url)`** (`src/lib/plugins/marketplace.ts`):
   - `http:` / `https:` کے علاوہ ہر چیز کو مسترد کرتا ہے۔
   - کینونیکل `isPrivateHost`
     (`src/shared/network/outboundUrlGuard.ts`) کے ذریعے صریح نجی/لوپ بیک/لنک-لوکل/ULA ہوسٹس (IPv4 **اور** IPv6،
     بشمول IPv4-mapped) کو مسترد کرتا ہے۔
   - `A` اور `AAAA` **دونوں** ریکارڈز کو ریزولو کرتا ہے اور اگر ریزولو شدہ پتوں میں سے **کوئی بھی**
     نجی ہو تو مسترد کر دیتا ہے — یوں عوامی ہوسٹ نام → نجی IP بائی پاس بند ہو جاتا ہے۔
   - **ناکامی کی صورت میں بند رہتا ہے**: DNS ریزولیوشن کی ناکامی URL کو مسترد کر دیتی ہے۔
2. **`safeOutboundFetch(url, { guard: "public-only", timeoutMs: 5000 })`**
   (`src/shared/network/safeOutboundFetch.ts`): فیچ کے وقت صرف عوامی URL
   گارڈ کو دوبارہ لاگو کرتا ہے اور **ری ڈائریکٹس کو بلاک کرتا ہے** (عوامی → نجی `30x`
   منتقلی نہیں ہوتی)۔

اگر کوئی URL کسی بھی تہہ میں ناکام ہو جائے تو درخواست منسوخ نہیں ہوتی — مارکیٹ پلیس
خاموشی سے پہلے سے موجود سیڈ رجسٹری استعمال کرتا ہے اور ایک تنبیہ لاگ کرتا ہے۔

> اس گارڈ کو PR #3774 میں خاص طور پر اس لیے مزید مضبوط بنایا گیا تھا کہ A + AAAA دونوں کو ریزولو کیا جائے اور
> صرف IPv4 کی جانچ کے بجائے کینونیکل `isPrivateHost` استعمال کیا جائے۔

### پلگ اِن عمل درآمد کی علیحدگی

- **پراسیس کی علیحدگی** — `loadPlugin()` (`src/lib/plugins/loader.ts`)
  ہر پلگ اِن کو ایک الگ Node.js چائلڈ پراسیس میں شروع کرتا ہے اور IPC کے ذریعے مواصلت کرتا ہے۔
  ہُک کالز کے لیے ٹائم آؤٹ ہوتا ہے، جس میں `SIGTERM` → `SIGKILL` تک شدت بڑھائی جاتی ہے۔
- **ماحولیاتی متغیرات کی اجازت یافتہ فہرست** — چائلڈ کو صرف اجازت یافتہ فہرست میں شامل ماحولیاتی
  متغیرات ملتے ہیں؛ زیادہ وسیع مجموعہ صرف اس وقت دیا جاتا ہے جب مینی فیسٹ
  `env` اجازت طلب کرے۔
- **پاتھ کی حد بندی** — انسٹال/اپ گریڈ/اَن انسٹال کے عمل اس بات کی تصدیق کرتے ہیں کہ پلگ اِن
  ڈائریکٹری اور `manifest.main` کسی بھی نقل یا تکراری حذف سے پہلے
  منظم پلگ اِن روٹ کے **اندر** ریزولو ہوں (یہ تبدیل شدہ DB پاتھز اور
  `manifest.main` میں `../` ٹریورسل سے تحفظ دیتا ہے)۔ فعال کرنے کا عمل
  `realpath` کے ذریعے علامتی روابط کو ریزولو کرتا ہے اور ایسے انٹری پوائنٹ کو لوڈ کرنے سے انکار کرتا ہے جو پلگ اِن
  ڈائریکٹری سے باہر نکلتا ہو۔
- **اختیاری سالمیت پِن** — مینی فیسٹ ایک `integrity`
  (`sha256-<base64>`، SRI فارمیٹ) فیلڈ کا اعلان کر سکتا ہے۔ موجود ہونے پر لوڈر
  لوڈ کے وقت انٹری فائل کے ہیش کی تصدیق کرتا ہے اور عدم مطابقت کی صورت میں فعال کرنے سے انکار کر دیتا ہے۔ یہ
  اختیاری چھیڑ چھاڑ کی شناخت ہے، سیکیورٹی کی **حد نہیں** — صرف لوپ بیک روٹنگ
  اور اجازتوں کا ماڈل ہی حقیقی حدود ہیں۔

## مینی فیسٹ (`plugin.json`)

`PluginManifestSchema` (`src/lib/plugins/manifest.ts`) کے ذریعے توثیق شدہ:

| فیلڈ               | قسم       | نوٹس                                                           |
| ------------------ | --------- | -------------------------------------------------------------- |
| `name`             | string    | لازمی؛ kebab-case (`^[a-z0-9-]+$`)، 1–100 حروف                 |
| `version`          | string    | لازمی؛ semver (`MAJOR.MINOR.PATCH`)                            |
| `description`      | string?   | ≤ 500 حروف                                                     |
| `author`           | string?   | ≤ 200 حروف                                                     |
| `license`          | string?   | ڈیفالٹ `MIT` ہے                                                |
| `main`             | string?   | انٹری فائل؛ ڈیفالٹ `index.js` ہے                               |
| `source`           | enum?     | `local` \| `marketplace` (ڈیفالٹ `local` ہے)                   |
| `tags`             | string[]? | تلاش کے ٹیگز                                                   |
| `requires`         | object?   | `{ omniroute?, permissions[] }`                                |
| `hooks`            | object?   | بولین قدریں جو بتاتی ہیں کہ پلگ اِن کون سے ہُکس نافذ کرتا ہے   |
| `skills`           | object[]? | اختیاری مہارت کی تعریفیں                                       |
| `enabledByDefault` | boolean?  | انسٹال ہونے پر خودکار طور پر فعال کریں                         |
| `configSchema`     | object?   | کنفیگریشن فیلڈز کا نقشہ (`string`/`number`/`boolean`/`select`) |
| `integrity`        | string?   | اختیاری `sha256-<base64>` انٹری فائل پِن                       |

اجازتیں درج ذیل enum سے لی جاتی ہیں:
`network` / `file-read` / `file-write` / `env` / `exec`۔

## لائف سائیکل کا بہاؤ

```
install (POST /api/plugins، path)
  → manifest کو اسکین/توثیق کریں → staging میں کاپی کریں → یقینی بنائیں کہ main ڈائریکٹری کے اندر ہے
  → اٹامک rename کے ذریعے ~/.omniroute/plugins/<name> میں منتقل کریں → DB میں قطار داخل کریں
  → onInstall چلائیں → اگر enabledByDefault ہو: activate کریں

activate (POST /api/plugins/{name}/activate)
  → realpath containment کی جانچ → loadPlugin() (child process شروع کریں)
  → اعلان کردہ hooks رجسٹر کریں → status = "active" → onActivate چلائیں

deactivate (POST /api/plugins/{name}/deactivate)
  → onDeactivate چلائیں (unregister کرنے سے پہلے) → hooks کو unregister کریں
  → child process ختم کریں → status = "inactive"

uninstall (DELETE /api/plugins/{name})
  → اگر active ہو تو deactivate کریں → onUninstall چلائیں
  → containment کی جانچ کے ساتھ plugin ڈائریکٹری کو بار بار حذف کریں → DB کی قطار حذف کریں
```

کسی ایسی ڈائریکٹری پر `install` دوبارہ چلانے سے، جس کے manifest کا version نصب شدہ version سے **سختی سے
نیا** ہو، خودکار طور پر اپ گریڈ ہو جاتا ہے (صاف از سرِ نو تنصیب؛ config دوبارہ
ڈیفالٹ اقدار پر سیٹ ہو جاتی ہے)۔ یکساں یا پرانا version مسترد کر دیا جاتا ہے۔

## ڈیٹابیس

ٹیبل `plugins` (مائیگریشن `076_create_plugins.sql`):

| کالم            | قسم     | نوٹس                                             |
| --------------- | ------- | ------------------------------------------------ |
| `id`            | TEXT PK | UUID                                             |
| `name`          | TEXT    | منفرد                                            |
| `version`       | TEXT    | semver؛ ڈیفالٹ `1.0.0`                           |
| `description`   | TEXT    | اختیاری                                          |
| `author`        | TEXT    | اختیاری                                          |
| `license`       | TEXT    | ڈیفالٹ `MIT`                                     |
| `main`          | TEXT    | داخلہ فائل؛ ڈیفالٹ `index.js`                    |
| `source`        | TEXT    | ڈیفالٹ `local`                                   |
| `tags`          | TEXT    | JSON array؛ ڈیفالٹ `[]`                          |
| `status`        | TEXT    | `installed` \| `active` \| `inactive` \| `error` |
| `enabled`       | INT     | 0/1؛ ڈیفالٹ 0                                    |
| `manifest`      | TEXT    | مکمل manifest JSON                               |
| `config`        | TEXT    | JSON؛ ڈیفالٹ `{}`                                |
| `config_schema` | TEXT    | JSON؛ ڈیفالٹ `{}`                                |
| `hooks`         | TEXT    | اعلان کردہ hook ناموں کی JSON array؛ ڈیفالٹ `[]` |
| `permissions`   | TEXT    | JSON array؛ ڈیفالٹ `[]`                          |
| `plugin_dir`    | TEXT    | تنصیب کی مطلق ڈائریکٹری                          |
| `error_message` | TEXT    | جب `status = "error"` ہو تو سیٹ کیا جاتا ہے      |
| `installed_at`  | TEXT    | `datetime('now')`                                |
| `updated_at`    | TEXT    | `datetime('now')`                                |
| `activated_at`  | TEXT    | activation پر سیٹ کیا جاتا ہے                    |

Plugin کی metrics/analytics اضافی ٹیبلز میں ٹریک کی جاتی ہیں
(`090_plugin_metrics.sql`، `091_plugin_analytics.sql`)۔

## ڈیش بورڈ

`/dashboard/plugins` پر موجود ڈیش بورڈ صفحہ
(`src/app/(dashboard)/dashboard/plugins/page.tsx`) دو ٹیب فراہم کرتا ہے:

- **نصب شدہ** — نصب شدہ plugins کو ان کے اعلان کردہ hooks، activate/deactivate
  toggle، uninstall بٹن، اور "Plugins اسکین کریں" کارروائی
  (`POST /api/plugins/scan`) کے ساتھ فہرست میں دکھاتا ہے۔
- **مارکیٹ پلیس** — `GET /api/plugins/marketplace` سے حاصل شدہ catalog دکھاتا ہے، اور
  حسبِ ضرورت registry URL سیٹ کرنے کے لیے ایک فیلڈ فراہم کرتا ہے۔

ہر plugin کا config صفحہ `/dashboard/plugins/[name]/config` پر موجود ہے
(`src/app/(dashboard)/dashboard/plugins/[name]/config/page.tsx`)۔

## مزید دیکھیں

- [`docs/security/ROUTE_GUARD_TIERS.md`](../security/ROUTE_GUARD_TIERS.md) —
  `/api/plugins` کے صرف لوپ بیک تک محدود ہونے کی وجہ (Tier 1)
- [`docs/frameworks/SKILLS.md`](./SKILLS.md) — متعلقہ اسکلز فریم ورک
  (`src/lib/skills/`)؛ پلگ اِنز اپنے مینی فیسٹ میں اسکلز کا اعلان کر سکتے ہیں
- [`docs/frameworks/WEBHOOKS.md`](./WEBHOOKS.md) — ایونٹ سے چلنے والی آؤٹ باؤنڈ
  انٹیگریشنز
- [`docs/security/ERROR_SANITIZATION.md`](../security/ERROR_SANITIZATION.md) —
  خرابی کے جوابات کے لیے ہر پلگ اِن روٹ کے زیرِ استعمال `buildErrorBody()` پیٹرن
