# open-sse Architecture (हिन्दी)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **TL;DR**: `open-sse/` मुख्य स्ट्रीमिंग इंजन है, जो OmniRoute में प्रत्येक LLM अनुरोध को संचालित करता है। इसमें अनुरोध पाइपलाइन, एक्ज़ीक्यूटर्स, सेवाएँ, MCP सर्वर और अनुवाद परत को कार्यान्वित करने वाली ~1,400 फ़ाइलें हैं। यह मार्गदर्शिका बताती है कि ये घटक एक साथ कैसे काम करते हैं।

**स्रोत:** `open-sse/` (वर्कस्पेस पैकेज, ~1,440 गैर-परीक्षण `.ts` फ़ाइलें)

---

## अलग वर्कस्पेस पैकेज क्यों?

`open-sse/`, OmniRoute मोनोरेपो में एक **स्वतंत्र वर्कस्पेस** है, जिसके कई कारण हैं:

1. **पुन: उपयोग की क्षमता** — `open-sse` को npm पर `@omniroute/open-sse` के रूप में प्रकाशित किया जाता है, ताकि अन्य प्रोजेक्ट इसे स्वतंत्र रूप से उपयोग कर सकें
2. **स्पष्ट सीमाएँ** — स्ट्रीमिंग इंजन को OmniRoute-विशिष्ट UI/DB परत से अलग रखा गया है
3. **प्रदर्शन** — इंजन में कोई Next.js निर्भरता नहीं है, जिससे CLI/serverless संदर्भों में कोल्ड स्टार्ट तेज़ होते हैं
4. **संस्करण प्रबंधन** — `open-sse` अपनी समय-सारणी के अनुसार रिलीज़ हो सकता है

```json
// package.json
"workspaces": ["open-sse"]
```

---

## शीर्ष-स्तरीय संरचना

```
open-sse/
├── index.ts              # सार्वजनिक प्रवेश बिंदु
├── types.d.ts            # सार्वजनिक प्रकार निर्यात
├── package.json          # @omniroute/open-sse
├── config/               # प्रदाता कॉन्फ़िगरेशन, स्थिरांक, रजिस्ट्रियाँ
├── executors/            # प्रत्येक प्रदाता के HTTP एक्ज़ीक्यूटर (67 + base.ts/index.ts)
├── handlers/             # अनुरोध हैंडलर (chatCore, responses आदि)
├── lib/                  # आंतरिक उपयोगिताएँ
├── mcp-server/           # Model Context Protocol सर्वर
├── services/             # ~298 सेवा मॉड्यूल
├── transformer/          # Responses API प्रारूप ट्रांसफ़ॉर्मर
├── translator/           # प्रारूप अनुवाद (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # साझा उपयोगिताएँ (लॉगिंग, त्रुटि, स्ट्रीम आदि)
```

### मॉड्यूल की संख्या

| डायरेक्टरी | फ़ाइलें | उद्देश्य |
| `executors/` | 167 | प्रत्येक प्रदाता के HTTP एक्ज़ीक्यूटर (`DefaultExecutor` फ़ैक्टरी के माध्यम से एकीकृत) |
| `handlers/` | 157 | अनुरोध प्रवेश बिंदु (`chatCore`, `responses`, एम्बेडिंग) |
| `services/` | ~536 | रूटिंग, कैशिंग, दर सीमित करना, रीफ़्रेश आदि |
| `translator/` | 56 | प्रारूप रूपांतरण (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP टूल और ट्रांसपोर्ट |
| `utils/` | ~108 | सभी भागों में प्रयुक्त उपयोगिताएँ (लॉगिंग, त्रुटि, स्ट्रीम) |
| `config/` | ~339 | प्रदाता कॉन्फ़िगरेशन, स्थिरांक, रजिस्ट्रियाँ |

---

## अनुरोध पाइपलाइन

प्रत्येक LLM अनुरोध एक **5-चरणीय पाइपलाइन** से होकर गुजरता है:

```
                              ┌──────────────┐
   HTTP अनुरोध                │  1. रूट      │   कॉम्बो समाधान, मॉडल चयन
   (Next.js रूट)              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. अनुवाद   │   प्रारूप रूपांतरण (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. निष्पादन │   प्रदाता एक्ज़ीक्यूटर, HTTP, पुनः प्रयास, ब्रेकर
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. स्ट्रीम   │   SSE रूपांतरण, बैकप्रेशर
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. रिकॉर्ड  │   उपयोग ट्रैकिंग, कॉल लॉग, त्रुटि वर्गीकरण
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP प्रतिक्रिया (SSE या JSON)
```

### चरण 1: रूट (services/combo.ts)

**प्रवेश बिंदु**: `services/combo.ts` में `handleComboChat()`

अनुरोध को एक ठोस `(provider, model, account, credentials)` टपल में समाधान करता है:

- ID द्वारा कॉम्बो खोजें (या `auto/*` मॉडल के लिए एक वर्चुअल कॉम्बो बनाएँ)
- रूटिंग रणनीति लागू करें (प्राथमिकता, भारित, राउंड-रॉबिन आदि)
- अस्वस्थ प्रदाताओं को फ़िल्टर करें (सर्किट ब्रेकर)
- अगला व्यवहार्य लक्ष्य चुनें

`auto/*` मॉडल के लिए यह चरण निम्न कार्य भी करता है:

- **16-कारक स्कोरिंग** एल्गोरिदम (`services/autoCombo/`) चलाता है
- स्वास्थ्य, लागत, विलंबता आदि के आधार पर एक `provider+model` जोड़ी चुनता है

### चरण 2: अनुवाद (translator/)

यदि स्रोत प्रारूप (जैसे OpenAI) लक्ष्य प्रारूप (जैसे Claude) से अलग है, तो अनुरोध का **अनुवाद** किया जाता है:

- सिस्टम प्रॉम्प्ट → सिस्टम संदेश
- टूल परिभाषाएँ → प्रदाता-विशिष्ट टूल प्रारूप
- रीजनिंग/थिंकिंग पैरामीटर → प्रदाता-विशिष्ट समकक्ष
- संदेश भूमिका सामान्यीकरण (गैर-OpenAI के लिए `developer` → `system`)

`translator/index.ts` निम्नलिखित को एक्सपोज़ करता है:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### चरण 3: निष्पादन (executors/)

**प्रवेश बिंदु**: `getExecutor(providerId).execute(request, options)`

सभी प्रदाता `getExecutor()` फ़ैक्टरी फ़ॉलबैक के माध्यम से `DefaultExecutor` (`executors/default.ts`) का उपयोग करते हैं। एक्ज़ीक्यूटर:

- अपस्ट्रीम URL बनाता है (`buildUrl()`)
- प्रदाता-विशिष्ट हेडर जोड़ता है (`buildHeaders()`)
- अनुरोध बॉडी को रूपांतरित करता है (`transformRequest()`)
- पुनः प्रयास + एक्सपोनेंशियल बैकऑफ़ के साथ HTTP अनुरोध भेजता है
- आवश्यकता होने पर प्रमाणीकरण रीफ़्रेश संभालता है (OAuth प्रदाता)

सभी एक्ज़ीक्यूटर `BaseExecutor` (`executors/base.ts`, 1170 LOC) को एक्सटेंड करते हैं, जो निम्नलिखित प्रदान करता है:

- सामान्य पुनः प्रयास लॉजिक
- प्रॉक्सी एकीकरण
- सर्किट ब्रेकर एकीकरण
- उपयोग रिकॉर्डिंग हुक

### चरण 4: स्ट्रीम (utils/stream.ts)

स्ट्रीमिंग प्रतिक्रियाओं के लिए, एक्ज़ीक्यूटर एक **ReadableStream** लौटाता है। हैंडलर:

- SSE ट्रांसफ़ॉर्म (`createSSETransformStreamWithLogger`) के माध्यम से पाइप करता है
- निष्क्रिय कनेक्शन का पता लगाने के लिए हार्टबीट पिंग लागू करता है
- क्लाइंट डिस्कनेक्ट को सुचारु रूप से संभालता है (`pipeWithDisconnect`)
- गैर-स्ट्रीमिंग क्लाइंट के लिए SSE → JSON रूपांतरण करता है

गैर-स्ट्रीमिंग प्रतिक्रियाओं के लिए, एक्ज़ीक्यूटर पार्स किया हुआ JSON ऑब्जेक्ट लौटाता है, जिसे बिना बदलाव के आगे भेज दिया जाता है।

### चरण 5: रिकॉर्ड (services/usage.ts)

प्रतिक्रिया (सफलता या विफलता) के बाद, उपयोग दर्ज किया जाता है:

- प्रतिक्रिया से `prompt_tokens`, `completion_tokens`, `cached_tokens`
- मूल्य निर्धारण डेटा से परिकलित `cost_usd`
- विफल होने पर `latency_ms`, `status`, `error_class`
- `usage_history` तालिका में स्थायी रूप से संग्रहीत

कॉल लॉग आर्टिफ़ैक्ट (यदि सक्षम हों) `${DATA_DIR}/call_logs/` में लिखे जाते हैं।

---

## प्रमुख फ़ाइलों का गहन विश्लेषण

### chatCore.ts (5977 पंक्तियाँ)

**मुख्य अनुरोध हैंडलर**। इसके आकार के बावजूद, इसकी संरचना स्पष्ट है:

```ts
// chatCore.ts की छद्म-संरचना
export async function handleChat(request: NextRequest) {
  // 1. प्रमाणीकरण + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. बॉडी सत्यापन
  const body = await parseRequestBody(request);

  // 3. प्रारूप का पता लगाना + अनुवाद
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. कॉम्बो रूटिंग
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // अगले लक्ष्य पर जारी रखें
    }
  }

  // 5. आपातकालीन फ़ॉलबैक
  return await emergencyFallback(body);
}
```

एक विशाल फ़ंक्शन होने के बावजूद, इसे **टिप्पणी किए गए अनुभागों** में व्यवस्थित किया गया है, जो 5-चरणीय पाइपलाइन के अनुरूप हैं।

### combo.ts (4456 LOC)

वह **रूटिंग इंजन** जो किसी कॉम्बो को क्रमबद्ध लक्ष्यों में रिज़ॉल्व करता है।

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

**19 रूटिंग रणनीतियों** का समर्थन करता है (`src/shared/constants/routingStrategies.ts` देखें):

| रणनीति              | व्यवहार                                                                                         |
| ------------------- | ----------------------------------------------------------------------------------------------- |
| `priority`          | पहले लक्ष्य को प्राथमिकता देने वाली क्रमबद्ध सूची                                               |
| `weighted`          | प्रत्येक लक्ष्य के भार के अनुसार संभाव्यता-आधारित चयन                                           |
| `round-robin`       | लक्ष्यों के बीच क्रम से चक्र चलाना                                                              |
| `context-relay`     | लक्ष्यों के बीच संदर्भ हस्तांतरित करना                                                          |
| `fill-first`        | अगले लक्ष्य पर जाने से पहले कोटा भरना                                                           |
| `p2c`               | दो विकल्पों की शक्ति                                                                            |
| `random`            | एकसमान यादृच्छिक चयन                                                                            |
| `least-used`        | हाल में सबसे कम बार उपयोग किए गए लक्ष्य को चुनना                                                |
| `cost-optimized`    | सबसे सस्ता स्वस्थ लक्ष्य पहले                                                                   |
| `reset-aware`       | प्रदाता की रीसेट विंडो को ध्यान में रखना                                                        |
| `reset-window`      | रीसेट विंडो-आधारित रूटिंग                                                                       |
| `headroom`          | सबसे अधिक शेष कोटा हेडरूम वाला लक्ष्य पहले                                                      |
| `strict-random`     | पूर्णतः एकसमान (कोई गुणवत्ता भार नहीं)                                                          |
| `auto`              | 16-कारक स्कोरिंग का उपयोग (`autoCombo/`)                                                        |
| `lkgp`              | अंतिम ज्ञात अच्छा प्रदाता पहले                                                                  |
| `context-optimized` | लंबे-संदर्भ वाले अनुरोधों के लिए सर्वोत्तम                                                      |
| `fusion`            | समानांतर रूप से एक पैनल को अनुरोध भेजना, फिर निर्णायक के माध्यम से संश्लेषित करना (`fusion.ts`) |

### base.ts (1170 LOC)

वह **अमूर्त निष्पादक** जिसे सभी 107 निष्पादक विस्तारित करते हैं। इसमें शामिल हैं:

- `buildUrl()` — डिफ़ॉल्ट URL निर्माण (कस्टम व्यवहार के लिए उपवर्ग इसे ओवरराइड करते हैं)
- `buildHeaders()` — डिफ़ॉल्ट हेडर (प्रमाणीकरण, सामग्री-प्रकार)
- `transformRequest()` — डिफ़ॉल्ट रूप से पास-थ्रू
- `execute()` — पुनः प्रयास/बैकऑफ़/ब्रेकर वाला मुख्य HTTP लूप

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // सभी OpenAI/Anthropic-संगत प्रदाताओं को संभालता है
  // प्रदाता कॉन्फ़िगरेशन (URL, प्रमाणीकरण, हेडर) पंजीकृत करते हैं, लेकिन निष्पादक तर्क साझा करते हैं
}
```

प्रदाता-विशिष्ट व्यवहार (प्रमाणीकरण हेडर, आधार URL, संस्करण हेडर) को अलग निष्पादक क्लासों के बजाय प्रदाता रजिस्ट्री के माध्यम से कॉन्फ़िगर किया जाता है।

````

---

## सेवाएँ (117 मॉड्यूल)

सेवाएँ **केंद्रित, एकल-उद्देश्यीय मॉड्यूल** हैं, जिन्हें हैंडलर संयोजित करते हैं। प्रमुख श्रेणियाँ:

### रूटिंग और कॉम्बो

- `combo.ts` — कॉम्बो-रूट किए गए अनुरोधों का प्रवेश-बिंदु
- `services/autoCombo/` — 16-कारक स्कोरिंग, 8 स्वचालित रूटिंग रणनीतियाँ
- `wildcardRouter.ts` — वाइल्डकार्ड रूट (`gpt-*`) से मिलान करता है
- `modelFamilyFallback.ts` — T5 परिवार-अंतर्गत फ़ॉलबैक

### दर सीमित करना और कोटा

- `rateLimitManager.ts` — प्रत्येक कुंजी+प्रदाता के लिए टोकन बकेट
- `usage.ts` — उपयोग का रिकॉर्ड रखना
- `quotaCache.ts` — इन-मेमोरी कोटा स्नैपशॉट

### खाता और टोकन

- `tokenRefresh.ts` — 401 पर OAuth रीफ़्रेश
- `accountFallback.ts` — वैकल्पिक खाते पर स्विच करना
- `sessionManager.ts` — मल्टी-टर्न सत्र स्थिति

### इंटेलिजेंस

- `intentClassifier.ts` — अनुरोध के आशय का वर्गीकरण करना
- `taskAwareRouter.ts` — कार्य के प्रकार के अनुसार रूट करना
- `thinkingBudget.ts` — थिंकिंग टोकन आवंटित करना
- `contextManager.ts` — रूटिंग संदर्भ सम्मिलित करना

### प्रत्यास्थता

- `resilience.ts` — पुनः प्रयास, बैकऑफ़ और ब्रेकर का समन्वय
- `emergencyFallback.ts` — अंतिम उपाय वाला फ़ॉलबैक
- `modelDeprecation.ts` — उत्तराधिकारी मॉडल पर स्वचालित रूप से रूट करना

### स्थिति

- `signatureCache.ts` — अनुरोध हस्ताक्षर के आधार पर डुप्लिकेट हटाना
- `volumeDetector.ts` — लोड शेडिंग
- `contextHandoff.ts` — सत्र क्रमांकन

### संपीड़न

- `compression/` (उपनिर्देशिका) — संपूर्ण संपीड़न पाइपलाइन
- इंजन, नियम पैक और अडैप्टर को समाहित करने वाली 39 फ़ाइलें

### कौशल

- ([SKILLS.md](./SKILLS.md) में वर्णित)

### मेमोरी

- ([MEMORY.md](./MEMORY.md) में वर्णित)

---

## एक्ज़ीक्यूटर (75+ फ़ाइलें)

प्रत्येक प्रदाता के लिए एक फ़ाइल। वे सभी `BaseExecutor` को विस्तारित करते हैं और भिन्न व्यवहार को ओवरराइड करते हैं।

### सामान्य पैटर्न

प्रदाताओं को `getExecutor(providerId)` के माध्यम से रिज़ॉल्व किया जाता है, जो कॉन्फ़िगर किया गया एक्ज़ीक्यूटर लौटाता है। OpenAI/Anthropic-संगत प्रदाता `DefaultExecutor` (`executors/default.ts`) का उपयोग करते हैं। प्रदाता-विशिष्ट व्यवहार (बेस URL, प्रमाणीकरण हेडर, API संस्करण) को `open-sse/config/providers/` में कॉन्फ़िगर किया जाता है, जबकि अनुरोध बॉडी के रूपांतरण `open-sse/translator/` में संभाले जाते हैं।

**कस्टम URL** प्रदाता कॉन्फ़िगरेशन के माध्यम से सेट किया जाता है:

```ts
// open-sse/config/providers/ में प्रदाता कॉन्फ़िगरेशन
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**कस्टम प्रमाणीकरण** प्रदाता रजिस्ट्री के प्रमाणीकरण कॉन्फ़िगरेशन (API कुंजी, OAuth, हेडर प्रोफ़ाइल) के माध्यम से संभाला जाता है।

**कस्टम अनुरोध बॉडी** रूपांतरण (जैसे कि Anthropic द्वारा `system` को `messages` से अलग करना) `open-sse/translator/` में प्रत्येक प्रदाता के लिए पंजीकृत किए जाते हैं।

````

### एक्ज़ीक्यूटर फ़ैक्टरी

`executors/index.ts`, `getExecutor(providerId)` को निर्यात करता है:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

रिज़ॉल्यूशन `ExecutorRegistry` (`executors/registry.ts`) के माध्यम से होता है: प्रत्येक विशिष्ट एक्ज़ीक्यूटर को `executors/index.ts` की अंतर्निहित तालिका में घोषित किया जाता है और मॉड्यूल लोड होने पर `registerExecutor(alias, instance)` के माध्यम से पंजीकृत किया जाता है; `getExecutor()` रजिस्ट्री से परामर्श करता है और किसी विशिष्ट प्रविष्टि से रहित प्रदाता के लिए मेमोआइज़ किए गए `DefaultExecutor` पर फ़ॉलबैक करता है। संपूर्ण उपनाम → एक्ज़ीक्यूटर मैपिंग को गोल्डन परीक्षण `tests/unit/executor-map-golden.test.ts` द्वारा परिभाषित किया गया है।

---

## अनुवादक

**3 प्रारूपों** के बीच अनुवाद करें: OpenAI, Anthropic, Gemini, साथ ही नया Responses API।

### अनुवाद कब होता है

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

सामान्य अनुवाद:

- `OpenAI → Anthropic`: अलग `system` फ़ील्ड, `x-api-key` हेडर
- `OpenAI → Gemini`: `messages` के बजाय `contents`, `systemInstruction`
- `OpenAI → Responses API`: `input` ऐरे, `previous_response_id` स्थिति

### संभाले गए विशेष मामले

- `developer` भूमिका → गैर-OpenAI के लिए `system`
- `system` भूमिका → GLM/ERNIE के लिए पहले उपयोगकर्ता संदेश में मर्ज की जाती है
- `json_schema` → Gemini का `responseMimeType` + `responseSchema`
- `tools` → प्रदाता-विशिष्ट टूल प्रारूप
- थिंकिंग पैरामीटर (o1, Claude) → प्रदाता-विशिष्ट समकक्ष

---

## MCP सर्वर

`open-sse/mcp-server/` **Model Context Protocol** सर्वर को कार्यान्वित करता है:

- **110 टूल** (प्रदाता प्रबंधन, कॉम्बो, मेमोरी, कैश, संपीड़न, प्रॉक्सी, कौशल, गेमिफ़िकेशन, प्लगइन, Notion, Obsidian, स्थानीय कॉर्पस)
- **3 ट्रांसपोर्ट**: stdio, SSE, Streamable HTTP
- सूक्ष्म-स्तरीय प्राधिकरण के लिए **33 स्कोप**

### टूल पंजीकरण

टूल `open-sse/mcp-server/tools/` में स्वतंत्र फ़ाइलों के रूप में पंजीकृत होते हैं, जिनमें से प्रत्येक एक नाम, स्कीमा, हैंडलर और स्कोप निर्यात करती है:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### ट्रांसपोर्ट

```ts
// stdio (CLI उपयोग)
startMcpStdio(server);

// SSE (HTTP-आधारित स्ट्रीमिंग)
startMcpSse(server, port);

// Streamable HTTP (आधुनिक MCP)
startMcpStreamable(server, port);
```

### प्राधिकरण

प्रत्येक टूल कॉल स्कोप जाँच (`open-sse/mcp-server/auth/`) से होकर गुजरती है:

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## ट्रांसफ़ॉर्मर

`open-sse/transformer/`, **Chat Completions** और **Responses API** प्रारूपों के बीच रूपांतरण करता है।

### अलग ट्रांसफ़ॉर्मर क्यों?

Responses API, **स्टेटफ़ुल वार्तालापों** (`previous_response_id`) वाला OpenAI का नया प्रारूप है। जब कोई क्लाइंट Responses अनुरोध भेजता है, तो OmniRoute:

1. आंतरिक रूप से Responses → Chat Completions में रूपांतरित करता है
2. प्रदाता को भेजता है (Chat Completions का समर्थन करने वाला कोई भी प्रदाता)
3. प्रतिक्रिया को वापस Responses प्रारूप में रूपांतरित करता है
4. रूपांतरित प्रतिक्रिया को क्लाइंट तक स्ट्रीम करता है

ट्रांसफ़ॉर्मर (`transformer/responsesTransformer.ts`) यह प्रदान करता है:

```ts
createResponsesApiTransformStream(): TransformStream
```

यह इन्हें संभालता है:

- `response.output_item.added` इवेंट
- `response.output_text.delta` इवेंट
- `response.completed` इवेंट
- टूल कॉल मैपिंग (`function_call` ↔ `tool_calls`)

---

## कॉन्फ़िगरेशन

`open-sse/config/` में कॉन्फ़िगरेशन परत होती है:

| फ़ाइल                         | उद्देश्य                                  |
| ----------------------------- | ----------------------------------------- |
| `providerRegistry.ts`         | 352-प्रदाता कैटलॉग पर Chat-मॉडल रजिस्ट्री |
| `providerModels.ts`           | मॉडल उपनाम, प्रारूप मैपिंग                |
| `constants.ts`                | टाइमआउट, सीमाएँ, स्थिति कोड               |
| `defaultThinkingSignature.ts` | डिफ़ॉल्ट Claude थिंकिंग सिग्नेचर          |
| `modelStrip.ts` (सेवाओं में)  | प्रति-प्रदाता फ़ील्ड हटाना                |

### प्रदाता रजिस्ट्री स्कीमा

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

मॉड्यूल लोड होने पर Zod सत्यापन सुनिश्चित करता है कि सभी प्रदाता कॉन्फ़िगरेशन मान्य हैं।

---

## प्रदर्शन संबंधी बाधाएँ

रूटिंग इंजन के लिए सख्त प्रदर्शन सीमाएँ हैं:

| ऑपरेशन                                       | लक्ष्य | मापन                        |
| -------------------------------------------- | ------ | --------------------------- |
| कॉम्बो रिज़ॉल्यूशन                           | <10ms  | 50 लक्ष्यों के लिए          |
| दर सीमा जाँच                                 | <1ms   | इन-मेमोरी टोकन बकेट         |
| मॉडल फ़ैमिली फ़ॉलबैक                         | <5ms   | कैश की गई फ़ैमिली परिभाषाएँ |
| अनुरोध रूटिंग डिस्पैच                        | <2ms   | हॉट पाथ                     |
| **रूटिंग हॉट पाथ में कोई ब्लॉकिंग I/O नहीं** | —      | सभी एसिंक्रोनस              |

---

## एंटी-पैटर्न

❌ **`combo.ts` में सिंक्रोनस DB कॉल** — पहले से गणना करके कैश करें
❌ **हैंडलर्स में रीट्राई लॉजिक** — रेज़िलिएंस सेवा से `retry()` का उपयोग करें
❌ **प्रोवाइडर कॉन्फ़िग तक सीधी पहुँच** — `providerRegistry` गेटर्स का उपयोग करें
❌ **हार्डकोडेड फ़ॉलबैक चेन** — `modelFamilyFallback.ts` में परिभाषित करें
❌ **समवर्ती अनुरोधों में स्टेट म्यूटेशन** — केवल अनुरोध-स्कोप्ड कॉन्टेक्स्ट का उपयोग करें

---

## नया कंपोनेंट जोड़ना

### नई सेवा जोड़ना

1. केंद्रित उत्तरदायित्व के साथ `open-sse/services/[serviceName].ts` बनाएँ
2. मुख्य हैंडलर फ़ंक्शन और सभी कॉन्स्टेंट्स एक्सपोर्ट करें
3. `tests/unit/services/[serviceName].test.mjs` में यूनिट टेस्ट जोड़ें
4. अनुरोध पाइपलाइन के `handlers/chatCore.ts` में एकीकृत करें (यदि रूटिंग से संबंधित हो)
5. यदि सेवा लक्ष्य चयन को प्रभावित करती है, तो `combo.ts` में रूटिंग लॉजिक अपडेट करें
6. इस फ़ाइल में दस्तावेज़ीकरण करें

### नया एक्ज़ीक्यूटर जोड़ना

1. `BaseExecutor` को एक्सटेंड करते हुए `open-sse/executors/[provider].ts` बनाएँ
2. `config/providerRegistry.ts` में रजिस्टर करें
3. `executors/index.ts` फ़ैक्टरी में जोड़ें
4. एक्ज़ीक्यूटर के लिए यूनिट टेस्ट जोड़ें
5. `docs/architecture/ARCHITECTURE.md` में दस्तावेज़ीकरण करें

### नया MCP टूल जोड़ना

1. `open-sse/mcp-server/tools/[category]Tools.ts` बनाएँ या अपडेट करें
2. इनपुट के लिए Zod स्कीमा परिभाषित करें
3. टूल को `mcp-server/index.ts` में रजिस्टर करें
4. `mcp-server/auth/` की स्कोप मैट्रिक्स में जोड़ें
5. यूनिट टेस्ट जोड़ें

---

## यह भी देखें

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — उच्च-स्तरीय आर्किटेक्चर
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — इंजीनियरिंग संदर्भ
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — डायरेक्टरी-दर-डायरेक्टरी
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16-फ़ैक्टर स्कोरिंग
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP सर्वर
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A सर्वर
- स्रोत: `open-sse/` (400+ फ़ाइलें, ~143K LOC)
