# OpenCode Integration (ไทย)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPENCODE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPENCODE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPENCODE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPENCODE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPENCODE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPENCODE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPENCODE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPENCODE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPENCODE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPENCODE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPENCODE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPENCODE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPENCODE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPENCODE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPENCODE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPENCODE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPENCODE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPENCODE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPENCODE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPENCODE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPENCODE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPENCODE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPENCODE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPENCODE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPENCODE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPENCODE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPENCODE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPENCODE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPENCODE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPENCODE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPENCODE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPENCODE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPENCODE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPENCODE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPENCODE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPENCODE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPENCODE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPENCODE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPENCODE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPENCODE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPENCODE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPENCODE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPENCODE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPENCODE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPENCODE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPENCODE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPENCODE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPENCODE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPENCODE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPENCODE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPENCODE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPENCODE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPENCODE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPENCODE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPENCODE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPENCODE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPENCODE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPENCODE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPENCODE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPENCODE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPENCODE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPENCODE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPENCODE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPENCODE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPENCODE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPENCODE.md)

---

> **สถานะ:** พร้อมใช้งานทั่วไป
> **กลุ่มเป้าหมาย:** ผู้ดูแลระบบที่เชื่อมต่อ OpenCode เข้ากับการติดตั้งใช้งาน OmniRoute
> **แหล่งข้อมูลอ้างอิงหลัก (สคีมาการกำหนดค่า):** `src/shared/services/opencodeConfig.ts`
> **แหล่งข้อมูลอ้างอิงหลัก (แพ็กเกจ npm):** `@omniroute/opencode-provider/` (เวิร์กสเปซที่เผยแพร่ได้)

[OpenCode](https://opencode.ai) เป็นไคลเอนต์ AI แบบเอเจนต์สำหรับ CLI/เดสก์ท็อป โดยอ่านแค็ตตาล็อกผู้ให้บริการจาก `~/.config/opencode/opencode.json` (หรือ `opencode.jsonc`) และเป็นไปตามสคีมาที่ `https://opencode.ai/config.json` OmniRoute จะแสดงตัวต่อ OpenCode ในฐานะหนึ่งในผู้ให้บริการเหล่านั้น โดยทุกคำขอจะไหลผ่านพื้นผิว `/v1` มาตรฐานของ OmniRoute ที่เข้ากันได้กับ OpenAI ทำให้ OpenCode ได้รับประโยชน์โดยอัตโนมัติจากการกำหนดเส้นทางแบบ Auto-Combo, ตัวตัดวงจร, นโยบายคีย์, ความสามารถในการสังเกตการณ์ และอื่นๆ

มี**เส้นทางการผสานรวมที่รองรับสองเส้นทาง** เลือกใช้เพียงหนึ่งเส้นทาง โดยทั้งสองจะสร้างการกำหนดค่าเดียวกัน

---

## เส้นทางที่ 1 — ตัวสร้างผ่าน CLI (ไม่ต้องติดตั้ง npm)

แนะนำสำหรับผู้ใช้ปลายทาง มาพร้อมกับ OmniRoute และเขียน `opencode.json` ลงในตำแหน่งเดิม

```bash
# หลังจากติดตั้ง OmniRoute (npm i -g @omniroute/cli หรือโคลนไว้ภายในเครื่อง)
omniroute config opencode \
  --base-url http://localhost:20128 \
  --api-key "$OMNIROUTE_API_KEY"
```

เบื้องหลัง CLI จะเรียก `mergeOpenCodeConfigText()` (`src/shared/services/opencodeConfig.ts:104`) ดังนั้น `opencode.json` ที่มีอยู่จะยังคงเก็บผู้ให้บริการรายอื่นและความคิดเห็นไว้ รายการของ OmniRoute จะถูกเพิ่มหรือแทนที่แบบอะตอมมิก

ไฟล์ที่ได้ (แค็ตตาล็อกโมเดลเริ่มต้น):

```jsonc
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "omniroute": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "OmniRoute",
      "options": {
        "baseURL": "http://localhost:20128/v1",
        "apiKey": "<your-key>",
      },
      "models": {
        "claude-opus-4-5-thinking": { "name": "claude-opus-4-5-thinking" },
        "claude-sonnet-4-5-thinking": { "name": "claude-sonnet-4-5-thinking" },
        "gemini-3.1-pro-high": { "name": "gemini-3.1-pro-high" },
        "gemini-3-flash": { "name": "gemini-3-flash" },
      },
    },
  },
}
```

---

## เส้นทางที่ 2 — แพ็กเกจ npm `@omniroute/opencode-provider`

แนะนำเมื่อคุณเขียนสคริปต์สร้างการกำหนดค่าจาก Node/TS (ไปป์ไลน์ CI, โมโนรีโพ, โฟลว์ตัวติดตั้งแบบกำหนดเอง)

```bash
npm install --save-dev @omniroute/opencode-provider
```

```ts
import { writeFileSync } from "node:fs";
import { buildOmniRouteOpenCodeConfig } from "@omniroute/opencode-provider";

const config = buildOmniRouteOpenCodeConfig({
  baseURL: "http://localhost:20128",
  apiKey: process.env.OMNIROUTE_API_KEY ?? "sk_omniroute",
  // ไม่บังคับ: แทนที่แค็ตตาล็อกโมเดลที่เปิดให้ OpenCode ใช้งาน
  models: ["auto", "claude-opus-4-7", "gpt-5.5"],
  modelLabels: { auto: "Auto-Combo" },
});

writeFileSync("opencode.json", JSON.stringify(config, null, 2));
```

สำหรับการผสานรวมกับไฟล์ที่มีอยู่โดยไม่ทำลายข้อมูล ให้จำลอง `mergeOpenCodeConfigText()` จาก `opencodeConfig.ts` หรือเรียกใช้ตัวสร้างผ่าน CLI

ดู API ฉบับเต็มได้ที่ [README ของแพ็กเกจ](../../@omniroute/opencode-provider/README.md)

---

## สิ่งที่รันไทม์ทำจริง

ทั้งสองเส้นทางจะสร้าง `provider.omniroute.npm: "@ai-sdk/openai-compatible"` เหมือนกัน ขณะรันไทม์ OpenCode จะโหลด `@ai-sdk/openai-compatible` (ซึ่งเป็นทรานซิทีฟดีเพนเดนซีของ OpenCode อยู่แล้ว) และกำหนดค่าด้วย `baseURL` + `apiKey` จากนั้นกระบวนการจะเป็นดังนี้:

```
UI/เอเจนต์ของ OpenCode
   → @ai-sdk/openai-compatible
      → HTTP POST {baseURL}/chat/completions          (พื้นผิว OpenAI ของ OmniRoute)
         → ตัวจัดการ /v1/chat/completions ของ OmniRoute     (open-sse/handlers/chatCore.ts)
            → การกำหนดเส้นทางแบบคอมโบ / Auto-Combo / ตัวดำเนินการ
               → ผู้ให้บริการต้นทาง
```

ปลั๊กอินไม่ดำเนินการกับ HTTP เลย โดยทำหน้าที่เพียงสร้างการกำหนดค่าเท่านั้น

---

## ค่าเริ่มต้นของแค็ตตาล็อกโมเดล

```ts
export const OMNIROUTE_DEFAULT_OPENCODE_MODELS = [
  "claude-opus-4-5-thinking",
  "claude-sonnet-4-5-thinking",
  "gemini-3.1-pro-high",
  "gemini-3-flash",
] as const;
```

คุณสามารถเขียนทับได้ผ่าน `models: [...]` รายการที่แนะนำให้เพิ่ม:

- `"auto"` — แสดงเราเตอร์แบบไม่ต้องกำหนดค่า [Auto-Combo](../routing/AUTO-COMBO.md) ของ OmniRoute ช่วยให้ OpenCode เลือก "โมเดลที่ดีที่สุดซึ่งพร้อมใช้งาน" ได้โดยที่คุณไม่ต้องกำหนดแค็ตตาล็อกไว้ตายตัว
- `"<combo-name>"` — คอมโบใดๆ ที่คุณกำหนดไว้ในแดชบอร์ด โดย OmniRoute จะจัดการคอมโบนั้นอย่างโปร่งใส

---

## การปรับ URL ให้เป็นรูปแบบมาตรฐาน

ตัวช่วยรองรับทั้งสองรูปแบบและจะสร้าง `/v1` เพียงหนึ่งรายการเสมอ:

| อินพุต                         | เอาต์พุต (`options.baseURL`) |
| ------------------------------ | ---------------------------- |
| `http://localhost:20128`       | `http://localhost:20128/v1`  |
| `http://localhost:20128/`      | `http://localhost:20128/v1`  |
| `http://localhost:20128/v1`    | `http://localhost:20128/v1`  |
| `http://localhost:20128/v1///` | `http://localhost:20128/v1`  |

การกำจัดส่วนที่ซ้ำกันนี้เป็น **สาเหตุของความขัดข้องที่พบบ่อยที่สุด** ในการกำหนดค่ารุ่นเก่า หากคุณมี `opencode.json` จากเวอร์ชันก่อน v3.8.0 ซึ่งชี้ไปที่ `/v1/v1/...` ให้เรียกใช้ตัวสร้างอีกครั้งหรือเรียก `createOmniRouteProvider` ใหม่

---

## โหมดการยืนยันตัวตน

| การตั้งค่า OmniRoute                                          | ค่า `apiKey` ที่แนะนำ                                   |
| ------------------------------------------------------------- | ------------------------------------------------------- |
| `REQUIRE_API_KEY=false` (ค่าเริ่มต้นสำหรับการใช้งานในเครื่อง) | `sk_omniroute` (ค่าตัวยึดตำแหน่งแบบตัวอักษร)            |
| `REQUIRE_API_KEY=true`                                        | คีย์ API จริงสำหรับผู้ใช้แต่ละรายจากแดชบอร์ด → คีย์ API |

สำหรับไคลเอนต์รูปแบบ Anthropic ที่ส่ง `x-api-key` + `anthropic-version` ฟังก์ชัน `extractApiKey` ของ OmniRoute ยังรองรับคีย์จาก `x-api-key` ด้วย OpenCode ใช้อินเทอร์เฟซ OpenAI ดังนั้นจึงส่ง `Authorization: Bearer ${apiKey}` เสมอ — กรณีพิเศษของ Anthropic จึงไม่มีผลในที่นี้

---

## การแก้ไขปัญหา

| อาการ                                                             | สาเหตุ                                                                           | วิธีแก้ไข                                                                                                        |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `404` ในทุกคำขอที่มี URL ซึ่งประกอบด้วย `/v1/v1/`                 | การกำหนดค่าเก่าจากปลั๊กอินก่อน v3.8 ที่เติมส่วนต่อท้าย `/v1` ซ้ำสองครั้ง         | สร้างใหม่ผ่านเส้นทางที่ 1 หรือ 2                                                                                 |
| `401 Invalid API key`                                             | OmniRoute ตั้งค่า `REQUIRE_API_KEY=true` และไม่รู้จักคีย์ดังกล่าว                | สร้างคีย์ในแดชบอร์ด หรือตั้งค่า `REQUIRE_API_KEY=false` (สำหรับการใช้งานในเครื่องเท่านั้น) และใช้ `sk_omniroute` |
| รายการโมเดลว่างเปล่าใน UI ของ OpenCode                            | โมเดลเริ่มต้นทั้ง 4 รายการถูกซ่อนในการตั้งค่าการมองเห็นของผู้ให้บริการ OmniRoute | ส่ง `models: ["auto", ...]` เพื่อแสดงโมเดลที่คุณเปิดใช้งานไว้                                                    |
| OpenCode แสดงข้อผิดพลาด 500 พร้อม `cannot read property 'models'` | OpenCode รุ่นเก่า (< 0.1.x) ไม่รองรับ `models` แบบอินไลน์                        | อัปเกรด OpenCode เป็นเวอร์ชันที่เป็นไปตามสคีมา v1 (`opencode.ai/config.json`)                                    |

---

## ดูเพิ่มเติม

- [เอกสารอ้างอิง API](../reference/API_REFERENCE.md) — รายละเอียด REST API ทั้งหมดของ OmniRoute
- [Auto-Combo](../routing/AUTO-COMBO.md) — ความหมายของ `model: "auto"`
- [README ของ `@omniroute/opencode-provider`](../../@omniroute/opencode-provider/README.md)
- ซอร์สโค้ด: `src/shared/services/opencodeConfig.ts`, `src/lib/cli-helper/config-generator/opencode.ts`, `@omniroute/opencode-provider/src/index.ts`
