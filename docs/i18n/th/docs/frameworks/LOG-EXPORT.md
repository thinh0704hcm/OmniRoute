# Log export (ไทย)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/LOG-EXPORT.md) · 🇪🇹 [am](../../../am/docs/frameworks/LOG-EXPORT.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/LOG-EXPORT.md) · 🇦🇿 [az](../../../az/docs/frameworks/LOG-EXPORT.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/LOG-EXPORT.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/LOG-EXPORT.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/LOG-EXPORT.md) · 🇩🇰 [da](../../../da/docs/frameworks/LOG-EXPORT.md) · 🇩🇪 [de](../../../de/docs/frameworks/LOG-EXPORT.md) · 🇬🇷 [el](../../../el/docs/frameworks/LOG-EXPORT.md) · 🇪🇸 [es](../../../es/docs/frameworks/LOG-EXPORT.md) · 🇪🇪 [et](../../../et/docs/frameworks/LOG-EXPORT.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/LOG-EXPORT.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/LOG-EXPORT.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/LOG-EXPORT.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/LOG-EXPORT.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/LOG-EXPORT.md) · 🇮🇱 [he](../../../he/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/LOG-EXPORT.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/LOG-EXPORT.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/LOG-EXPORT.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/LOG-EXPORT.md) · 🇮🇩 [id](../../../id/docs/frameworks/LOG-EXPORT.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/LOG-EXPORT.md) · 🇮🇹 [it](../../../it/docs/frameworks/LOG-EXPORT.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/LOG-EXPORT.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/LOG-EXPORT.md) · 🇰🇭 [km](../../../km/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/LOG-EXPORT.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/LOG-EXPORT.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/LOG-EXPORT.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/LOG-EXPORT.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/LOG-EXPORT.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/LOG-EXPORT.md) · 🇲🇲 [my](../../../my/docs/frameworks/LOG-EXPORT.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/LOG-EXPORT.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/LOG-EXPORT.md) · 🇳🇴 [no](../../../no/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [or](../../../or/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/LOG-EXPORT.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/LOG-EXPORT.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/LOG-EXPORT.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/LOG-EXPORT.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/LOG-EXPORT.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/LOG-EXPORT.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/LOG-EXPORT.md) · 🇱🇰 [si](../../../si/docs/frameworks/LOG-EXPORT.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/LOG-EXPORT.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/LOG-EXPORT.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/LOG-EXPORT.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/LOG-EXPORT.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/LOG-EXPORT.md) · 🇮🇳 [te](../../../te/docs/frameworks/LOG-EXPORT.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/LOG-EXPORT.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/LOG-EXPORT.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/LOG-EXPORT.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/LOG-EXPORT.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/LOG-EXPORT.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/LOG-EXPORT.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/LOG-EXPORT.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/LOG-EXPORT.md)

---

การส่งออกบันทึกการเรียกของ OmniRoute ไปยังคลังข้อมูลวิเคราะห์ภายนอกอย่างต่อเนื่องและเป็นส่วนเพิ่ม

แท็บแดชบอร์ด Logs เก็บประวัติคำขอไว้ใน SQLite (`call_logs`) ซึ่งมีขอบเขตจำกัดด้วย
การหมุนเวียนและระยะเวลาการเก็บรักษา การส่งออกบันทึกจะส่งชุดระเบียนเดียวกันออกไปตามกำหนดเวลา เพื่อให้ข้อมูลมีอายุยาวนานกว่า
ฐานข้อมูลภายในเครื่องและสามารถนำไปรวมกับข้อมูลอื่นได้ BigQuery เป็นปลายทางแรก ส่วน
ไปป์ไลน์เป็นรีจิสทรี ดังนั้นจึงสามารถเพิ่มปลายทางอื่นได้

---

## 1. วิธีการทำงาน

```
call_logs (SQLite)
  → callLogExportSource.getCallLogsForExport(cursor, batchSize)
    → LogExportRecord[]  (ชุดฟิลด์ของแท็บ Logs)
      → destination client.send(batch)
        → advanceLogExportCursor(id, lastRowId, count)
```

- **กำหนดเวลา** — งาน cron ของ `JobRegistry` หนึ่งงานชื่อ `log_export` โดยมีค่าเริ่มต้นเป็น `0 * * * *` (ทุกชั่วโมง
  ตามเวลา UTC) ลงทะเบียนใน `src/lib/initCloudSync.ts` และสามารถเขียนทับได้ด้วย `OMNIROUTE_LOG_EXPORT_CRON`
  ในแต่ละรอบ ระบบจะระบายข้อมูลจากปลายทางที่ **เปิดใช้งาน** ทุกแห่งตามลำดับ
- **เคอร์เซอร์** — ใช้ `call_logs.rowid` โดยนัยของ SQLite และจัดเก็บแยกตามปลายทางใน
  `log_export_destinations.cursor_row_id` โดยจงใจไม่ใช้ `timestamp` เป็นเคอร์เซอร์ เนื่องจากผู้เรียก
  อาจระบุค่าของตนเองได้ ดังนั้นคำขอที่ประมวลผลช้าอาจถูกเขียนหลังคำขอที่ประมวลผลเร็วกว่าแต่เริ่ม
  ภายหลัง และเคอร์เซอร์แบบประทับเวลาจะข้ามคำขอดังกล่าว
- **การแบ่งเป็นชุด** — ส่ง `batch_size` แถวต่อคำขอ (ค่าเริ่มต้น 500) และ `max_rows_per_run` แถวต่อการทำงานหนึ่งรอบ
  (ค่าเริ่มต้น 10000) เพื่อให้ข้อมูลค้างจำนวนมากค่อย ๆ ถูกระบายในหลายรอบ แทนที่จะบล็อกอยู่ในรอบเดียว
- **การส่งมอบ** — เคอร์เซอร์จะเลื่อนไปข้างหน้าหลังจาก `send()` สำเร็จแล้วเท่านั้น ชุดข้อมูลที่ส่งล้มเหลวจะปล่อยให้
  เคอร์เซอร์อยู่ที่เดิม เพื่อให้ลองส่งแถวเดิมอีกครั้งในการทำงานรอบถัดไป การรับประกันคือ
  ส่งอย่างน้อยหนึ่งครั้งร่วมกับการขจัดข้อมูลซ้ำที่ฝั่งปลายทาง ไม่ใช่แบบครั้งเดียวอย่างแท้จริง: BigQuery กำหนดคีย์ให้แต่ละ
  แถวด้วย ID ของบันทึกการเรียก ซึ่งระบบจะพยายามใช้เพื่อขจัดข้อมูลซ้ำภายในช่วงเวลาการขจัดข้อมูลซ้ำของตนเอง
- **การป้องกันการทำงานซ้อนกัน** — รอบ cron และ `POST .../run` อาจทำงานพร้อมกันได้ ปลายทางที่
  กำลังระบายข้อมูลอยู่แล้วจะถูกข้ามแทนที่จะระบายซ้ำสองครั้ง (`skipped: true` ในผลลัพธ์การทำงาน)
  เพื่อให้การทำงานพร้อมกันไม่สามารถส่งชุดข้อมูลซ้ำหรือเขียนเคอร์เซอร์ย้อนกลับได้
- **การกู้คืนหลังการล้างข้อมูล** — หาก `cursor_row_id` มีค่าสูงกว่า `MAX(rowid)` (ตารางทั้งหมดถูก
  ล้างและ rowid เริ่มต้นใหม่) ตัวดำเนินการจะย้อนกลับไปที่ 0 แทนที่จะมองไม่เห็นข้อมูลใหม่อย่างถาวร

### เพย์โหลด (พรอมต์และคำตอบที่สร้างขึ้น)

โดยค่าเริ่มต้น การส่งออกจะมีเฉพาะฟิลด์สรุปที่แสดงใน **รายการ** ของ Logs การเปิดใช้
**ส่งออกพรอมต์และคำตอบ** (`includeBodies`) จะส่งข้อมูลที่แผง **รายละเอียด** ของ Logs
แสดงสำหรับการเรียกแต่ละครั้งเพิ่มเติมด้วย:

| ฟิลด์                            | ข้อมูลที่จัดเก็บ                                          |
| -------------------------------- | --------------------------------------------------------- |
| `request_body` / `response_body` | เพย์โหลดของการเรียกตามที่แดชบอร์ดแสดงผล                   |
| `pipeline_route_decision`        | เป้าหมายและโมเดลที่เราเตอร์เลือก                          |
| `pipeline_client_request`        | คำขอดิบตรงตามที่ไคลเอ็นต์ส่งมา                            |
| `pipeline_openai_request`        | หลังจากแปลงเป็นรูปแบบ OpenAI ภายใน                        |
| `pipeline_provider_request`      | ข้อมูลที่ส่งไปยังต้นทางจริงในรูปแบบของผู้ให้บริการ        |
| `pipeline_provider_response`     | การตอบกลับดิบจากต้นทาง                                    |
| `pipeline_client_response`       | ข้อมูลที่ส่งกลับให้ผู้เรียก                               |
| `pipeline_error`                 | รายละเอียดข้อผิดพลาดระดับไปป์ไลน์สำหรับการเรียกที่ล้มเหลว |
| `bodies_truncated`               | เป็นจริงเมื่อฟิลด์ใด ๆ ข้างต้นถึงขีดจำกัด `maxBodyBytes`  |

ข้อมูลนี้เป็นเนื้อหาพรอมต์ จึง **ปิดไว้โดยค่าเริ่มต้น** และจงใจกำหนดให้เลือกแยกตามปลายทาง
ข้อมูลที่ส่งออกคือข้อมูลเดียวกับที่แดชบอร์ดแสดง เนื่องจากทั้งสองส่วนอ่านผ่าน `getCallLogById`: เพย์โหลดได้รับการ
ลบข้อมูล PII และปกปิดข้อมูลลับตั้งแต่ตอนเขียนแล้ว และการเรียกที่ใช้
คีย์ API แบบ `noLog` จะไม่จัดเก็บเพย์โหลดใด ๆ เลย จึงไม่มีข้อมูลให้ส่งออก

เพย์โหลดจะถูกอ่านทีละแถวจากอาร์ติแฟกต์ในระบบไฟล์ ดังนั้นการเติมข้อมูลจึงทำงานเฉพาะกับปลายทาง
ที่ร้องขอเท่านั้น แถวที่มีอาร์ติแฟกต์สูญหายหรือเสียหายจะส่งออกข้อมูลสรุปพร้อม
เพย์โหลดที่เป็น null แทนที่จะทำให้ทั้งชุดล้มเหลวและทำให้เคอร์เซอร์ค้างอยู่

`maxBodyBytes` (ค่าเริ่มต้น 262144) จำกัดขนาดของแต่ละฟิลด์ เพย์โหลดที่ยาวกว่าจะถูก **ตัดทอนแทนที่จะ
ทิ้ง** — พรอมต์ที่ถูกตัดยังคงตอบคำถามว่า "มีการถามอะไร" ได้ — และแถวดังกล่าวจะถูกทำเครื่องหมายด้วย
`bodies_truncated` เดลตาที่สตรีมมาทีละส่วนจะไม่ถูกส่งออก เนื่องจากการตอบกลับที่ประกอบเสร็จแล้ว
มีอยู่ใน `pipeline_provider_response` และ `pipeline_client_response` อยู่แล้ว

---

## 2. ไฟล์

| ส่วนประกอบ                   | ตำแหน่ง                                      |
| ---------------------------- | -------------------------------------------- |
| สัญญาของปลายทาง              | `src/lib/logExport/types.ts`                 |
| รีจิสทรี                     | `src/lib/logExport/registry.ts`              |
| การจัดการข้อมูลลับ           | `src/lib/logExport/secrets.ts`               |
| ตัวดำเนินการ (ลูปเคอร์เซอร์) | `src/lib/logExport/runner.ts`                |
| การฉายข้อมูลสำหรับ API       | `src/lib/logExport/presenter.ts`             |
| ปลายทาง BigQuery             | `src/lib/logExport/destinations/bigquery.ts` |
| การยืนยันตัวตนด้วย Google SA | `src/lib/logExport/googleServiceAccount.ts`  |
| แหล่งข้อมูลบันทึกการเรียก    | `src/lib/usage/callLogExportSource.ts`       |
| การจัดเก็บถาวร               | `src/lib/db/logExportDestinations.ts`        |
| งาน Cron                     | `src/lib/jobs/logExportJob.ts`               |
| เลเยอร์ REST                 | `src/app/api/log-export/`                    |
| หน้าแดชบอร์ด                 | `src/app/(dashboard)/dashboard/log-export/`  |

สคีมา: `src/lib/db/migrations/170_log_export_destinations.sql`

---

## 3. REST API

ทุกเส้นทางผ่านการยืนยันตัวตนสำหรับการจัดการ (`requireManagementAuth`) ระบบจะไม่ส่งคืนข้อมูลลับ:
ข้อมูลลับที่จัดเก็บไว้จะถูกส่งกลับเป็นค่าตรงตัว `__stored__` และการส่งค่านี้กลับมาในการอัปเดต
จะคงข้อมูลรับรองที่จัดเก็บไว้เดิม

การสร้างหรืออัปเดตปลายทางที่ชนิดของปลายทางระบุว่ามีข้อมูลลับ **จำเป็นต้องมี
`STORAGE_ENCRYPTION_KEY`** หากไม่มี `encrypt()` จะส่งค่าผ่านโดยไม่มีการแจ้งเตือน ดังนั้นระบบจึง
ปฏิเสธการเขียนด้วยสถานะ 400 แทนที่จะบันทึกข้อมูลรับรองลงใน SQLite เป็นข้อความธรรมดา (ใช้การป้องกันแบบเดียวกับ
เว็บฮุก Telegram)

| เมธอด    | พาธ                                      | วัตถุประสงค์                                                  |
| -------- | ---------------------------------------- | ------------------------------------------------------------- |
| `GET`    | `/api/log-export/types`                  | ชนิดของปลายทาง + รายการฟิลด์การกำหนดค่าของแต่ละชนิด           |
| `GET`    | `/api/log-export/destinations`           | แสดงรายการปลายทาง (ปกปิดข้อมูลลับ)                            |
| `POST`   | `/api/log-export/destinations`           | สร้างปลายทาง                                                  |
| `GET`    | `/api/log-export/destinations/{id}`      | อ่านข้อมูลหนึ่งรายการ                                         |
| `PUT`    | `/api/log-export/destinations/{id}`      | อัปเดตชื่อ / สถานะเปิดใช้งาน / การกำหนดค่า / การจัดชุดข้อมูล  |
| `DELETE` | `/api/log-export/destinations/{id}`      | ลบ                                                            |
| `POST`   | `/api/log-export/destinations/{id}/test` | ทดสอบข้อมูลรับรองโดยไม่เขียนข้อมูลใด ๆ                        |
| `POST`   | `/api/log-export/destinations/{id}/run`  | ระบายข้อมูลทันที โดยใช้เส้นทางเดียวกับการเรียกใช้ตามกำหนดเวลา |
| `GET`    | `/api/log-export/status`                 | สถานะ Cron, การเรียกใช้ล่าสุด, งานค้างแยกตามเป้าหมาย          |

`GET /api/log-export/types` คือสิ่งที่ทำให้ UI เป็นแบบทั่วไป: ฟอร์มแดชบอร์ดจะถูกเรนเดอร์จาก
ตัวอธิบายฟิลด์ที่ส่งกลับมา ดังนั้นปลายทางใหม่จึงไม่ต้องแก้ไข UI

---

## 4. ปลายทาง BigQuery

คีย์การกำหนดค่า (`type: "bigquery"`):

| คีย์                 | หมายเหตุ                                                       |
| -------------------- | -------------------------------------------------------------- |
| `projectId`          | โปรเจกต์ GCP ที่มีชุดข้อมูลอยู่                                |
| `datasetId`          | `[A-Za-z0-9_]+`                                                |
| `tableId`            | `[A-Za-z0-9_]+`                                                |
| `location`           | ใช้เฉพาะเมื่อต้องสร้างชุดข้อมูล (ค่าเริ่มต้น `EU`)             |
| `serviceAccountJson` | คีย์บัญชีบริการ ข้อมูลลับ: เข้ารหัสขณะจัดเก็บและไม่ส่งคืน      |
| `autoCreate`         | สร้างชุดข้อมูลและตารางในการส่งออกครั้งแรก (ค่าเริ่มต้น `true`) |

บัญชีบริการต้องมีสิทธิ์ `bigquery.tables.updateData` บนตารางเป้าหมาย รวมถึง
`bigquery.datasets.create` / `bigquery.tables.create` เมื่อเปิด `autoCreate`

ชุดข้อมูลที่กำหนดค่าไว้เป็นหน่วยของ **เคอร์เซอร์** ไม่ใช่หน่วย HTTP: `send()` จะแบ่งชุดข้อมูลออกเป็นการเรียก insertAll
ครั้งละไม่เกิน 500 แถว ดังนั้น `batch_size` ขนาดใหญ่จึงไม่ทำให้เกินขีดจำกัดคำขอ 10 MB ของ BigQuery
สถานะชั่วคราว (408/429/500/502/503/504) จะถูกลองใหม่สูงสุดสามครั้งด้วยการหน่วงเวลาแบบเอ็กซ์โพเนนเชียล
โดยใช้ insertIds เดิมซ้ำ ส่วนข้อผิดพลาดด้านการยืนยันตัวตนและสคีมาจะ throw ตั้งแต่ครั้งแรก แทนที่จะ
ใช้รอบการทำงานไปโดยเปล่าประโยชน์

ตารางที่เพิ่งสร้างเมื่อไม่กี่วินาทีก่อนยังไม่ปรากฏต่อเอนด์พอยต์สตรีมมิง ซึ่งจะตอบกลับ
404 เป็นเวลาสองสามวินาที ระบบจะลองใหม่เมื่อได้รับ 404 แต่ **เฉพาะเมื่อการทำงานรอบนี้เป็นผู้สร้างตาราง** เท่านั้น —
ตารางที่ไม่มีอยู่จริงจะยังคงล้มเหลวทันที โปรดทราบว่าการสร้างตารางใหม่โดยใช้ชื่อที่เพิ่งถูกลบไป
จะทำให้ BigQuery ปฏิเสธการแทรกข้อมูลแบบสตรีมมิงเป็นเวลาหลายนาที ซึ่งเป็นคุณสมบัติของการลบแล้วสร้างใหม่
ดังนั้นควรใช้ชื่อตารางใหม่แทนการลบแล้วเพิ่มตารางเดิมกลับเข้ามา

**ความล้มเหลวบางส่วนจะมาในรูปแบบ HTTP 200 พร้อม `insertErrors[]` ที่ไม่ว่างเปล่า** ระบบจะถือว่า
นี่เป็นความล้มเหลวและ throw ซึ่งจะหยุดไม่ให้เคอร์เซอร์เลื่อนผ่านแถวที่ BigQuery ไม่เคย
ยอมรับ โดย `tests/unit/log-export-bigquery.test.ts` จะตรึงพฤติกรรมนี้ไว้

การรับส่งข้อมูลใช้ REST โดยตรง — assertion แบบ RS256 ที่ลงนามด้วยตนเองจะถูกแลกเป็นโทเค็นการเข้าถึงที่
`https://oauth2.googleapis.com/token` จากนั้นแถวข้อมูลจะถูกส่งไปยัง `tabledata.insertAll` ไม่มีการดึง
Google SDK เข้ามาใช้ โทเค็นการเข้าถึงจะถูกแคชภายในโปรเซสแยกตามคู่ (บัญชีบริการ, ขอบเขต)

ตารางที่สร้างขึ้นจะมีหนึ่งคอลัมน์ต่อหนึ่งฟิลด์ในแท็บ Logs รวมถึง `exported_at` และถูกจัดวางให้เหมาะกับ
รูปแบบการคิวรีบันทึกการเรียกใช้งานจริง:

- **แบ่งพาร์ทิชันรายวันตาม `timestamp`** ดังนั้นคิวรีที่จำกัดขอบเขตด้วยวันที่จะสแกนเฉพาะวันเหล่านั้น
- **คลัสเตอร์ตาม `api_key_name`, `provider`, `model`, `status`** (ตามลำดับนี้) ดังนั้นการกรองตาม
  ผู้ที่เรียกใช้ ปลายทางที่ส่งไป หรือสถานะว่าล้มเหลวหรือไม่ จะช่วยตัดบล็อกภายในแต่ละพาร์ทิชันออก BigQuery
  อนุญาตคอลัมน์คลัสเตอร์ได้สูงสุดสี่คอลัมน์และลำดับมีความสำคัญ: การกรองเฉพาะ `api_key_name`
  ช่วยตัดบล็อกได้ แต่การกรองเฉพาะ `status` ทำไม่ได้
- **การเก็บรักษาพาร์ทิชันแบบเลือกได้** ผ่าน `partitionExpirationDays` (0 หมายถึงเก็บทุกอย่างไว้) ซึ่งจะถูกนำไปใช้
  เมื่อสร้างตาราง

การตั้งค่าทั้งสองมีผลในขณะสร้าง ตารางที่มีอยู่แล้วจะคงรูปแบบเดิมไว้ ดังนั้น
หากต้องการใช้การตั้งค่าเหล่านี้ ให้กำหนดปลายทางไปยัง id ของตารางใหม่

`tests/unit/log-export-bigquery.test.ts` ตรวจสอบว่า mapper และสคีมาของตารางสอดคล้องกันอยู่เสมอ
ดังนั้นคอลัมน์ call-log ใหม่จะไม่ถูกละทิ้งโดยไม่มีการแจ้งเตือนระหว่างการส่งออก

แบตช์จะถูกแบ่งเป็นส่วนย่อยตาม **ทั้ง** จำนวนแถวและจำนวนไบต์หลังการแปลงเป็นข้อมูลแบบลำดับ จำนวนแถวเพียงอย่างเดียวนั้นไม่เพียงพอ
เมื่อมีการส่งออก payload: 500 แถวที่มี prompt อาจมีขนาดหลายสิบเมกะไบต์ และ insertAll
จะปฏิเสธคำขอที่มีขนาดเกิน 10 MB แต่ละส่วนย่อยจะสิ้นสุดเมื่อครบ 500 แถวหรือมีขนาดถึง 9 MB แล้วแต่ว่าเงื่อนไขใดจะเกิดขึ้นก่อน

---

## 5. การเพิ่มปลายทาง

1. สร้าง `src/lib/logExport/destinations/<name>.ts` ซึ่งส่งออก `LogExportDestinationType`:
   โดยมี `configSchema` ของ Zod, อาร์เรย์ตัวอธิบาย `fields` สำหรับ UI, `secretFields` และ
   `createClient(config)` ที่คืนค่า `test()` / `prepare()` / `send(records)`
2. เพิ่มรายการดังกล่าวลงในอาร์เรย์ `DESTINATIONS` ใน `src/lib/logExport/registry.ts`
3. เขียนการทดสอบไว้ภายใต้ `tests/unit/`

การเปลี่ยนแปลงมีเพียงเท่านี้: ส่วนการจัดเก็บข้อมูล งาน cron เลเยอร์ REST การเข้ารหัสข้อมูลลับ และ
แบบฟอร์มแดชบอร์ด ล้วนอ้างอิงรีจิสทรี

กฎสองข้อสำหรับปลายทางใหม่:

- `send()` **ต้อง throw** เมื่อเกิดความล้มเหลวบางส่วน การ resolve หมายความว่า "ปลายทางมีแถวเหล่านี้แล้ว"
  และเคอร์เซอร์จะเลื่อนผ่านแถวเหล่านั้นอย่างถาวร
- ปลายทางที่รับ URL จากผู้ใช้ต้องตรวจสอบ URL ผ่าน
  `parseAndValidateWebhookUrl` (`src/shared/network/outboundUrlGuardPolicy.ts`) ก่อนทำการ fetch
  เช่นเดียวกับที่ webhook ทำ ส่วน BigQuery ไม่จำเป็นต้องทำเช่นนี้ เนื่องจากโฮสต์ของ BigQuery เป็นค่าคงที่

---

## 6. การใช้งาน

- **แดชบอร์ด**: Integrations → Log export เพิ่มปลายทาง เรียกใช้ **Test** เพื่อตรวจสอบข้อมูลประจำตัว
  โดยไม่เขียนแถวข้อมูล จากนั้นเปิดใช้งานปลายทาง
- **งานค้าง**: การ์ดของแต่ละปลายทางจะแสดงแถวที่รอดำเนินการและเคอร์เซอร์ ส่วน `GET
/api/log-export/status` จะส่งคืนตัวเลขเดียวกันพร้อมกับประวัติการทำงาน 20 ครั้งล่าสุด
- **ปลายทางหนึ่งที่ล้มเหลวจะไม่ทำให้ปลายทางอื่นล้มเหลว** — สรุปการทำงานจะบันทึกสถานะของแต่ละปลายทาง
  ไว้ใน `last_status` / `last_error` และประวัติการทำงานจะเก็บผลรวมโดยรวมไว้
- **การลบปลายทางจะลบเคอร์เซอร์ของปลายทางนั้นด้วย** การเพิ่มปลายทางนั้นกลับเข้ามาใหม่จะเริ่มจาก
  บันทึกการเรียกที่เก่าที่สุดซึ่งยังคงเก็บรักษาไว้ ทำให้มีการส่งแถวที่ปลายทางอาจมีอยู่แล้วซ้ำอีกครั้ง
  สำหรับ BigQuery ค่า `insertId` ของแต่ละแถวจะจัดการข้อมูลซ้ำได้เฉพาะภายในช่วงเวลาการขจัดข้อมูลซ้ำ
  ของ BigQuery เองเท่านั้น ดังนั้นควรปิดใช้งานปลายทางแทนการลบ
