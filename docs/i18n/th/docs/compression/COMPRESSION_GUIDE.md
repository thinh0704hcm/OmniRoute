# 🗜️ Prompt Compression Guide — OmniRoute (ไทย)

🌐 **Languages:** 🇺🇸 [English](../../../../compression/COMPRESSION_GUIDE.md) · 🇪🇹 [am](../../../am/docs/compression/COMPRESSION_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/compression/COMPRESSION_GUIDE.md) · 🇦🇿 [az](../../../az/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/compression/COMPRESSION_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/compression/COMPRESSION_GUIDE.md) · 🇩🇰 [da](../../../da/docs/compression/COMPRESSION_GUIDE.md) · 🇩🇪 [de](../../../de/docs/compression/COMPRESSION_GUIDE.md) · 🇬🇷 [el](../../../el/docs/compression/COMPRESSION_GUIDE.md) · 🇪🇸 [es](../../../es/docs/compression/COMPRESSION_GUIDE.md) · 🇪🇪 [et](../../../et/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/compression/COMPRESSION_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/compression/COMPRESSION_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇱 [he](../../../he/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/compression/COMPRESSION_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/compression/COMPRESSION_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/compression/COMPRESSION_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇩 [id](../../../id/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇹 [it](../../../it/docs/compression/COMPRESSION_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/compression/COMPRESSION_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/compression/COMPRESSION_GUIDE.md) · 🇰🇭 [km](../../../km/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/compression/COMPRESSION_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/compression/COMPRESSION_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/compression/COMPRESSION_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/compression/COMPRESSION_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/compression/COMPRESSION_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/compression/COMPRESSION_GUIDE.md) · 🇲🇲 [my](../../../my/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇴 [no](../../../no/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [or](../../../or/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/compression/COMPRESSION_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/compression/COMPRESSION_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/compression/COMPRESSION_GUIDE.md) · 🇱🇰 [si](../../../si/docs/compression/COMPRESSION_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/compression/COMPRESSION_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/compression/COMPRESSION_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/compression/COMPRESSION_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/compression/COMPRESSION_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [te](../../../te/docs/compression/COMPRESSION_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/compression/COMPRESSION_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/compression/COMPRESSION_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/compression/COMPRESSION_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/compression/COMPRESSION_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/compression/COMPRESSION_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/compression/COMPRESSION_GUIDE.md)

---

> ประหยัดบริบทที่เข้าเกณฑ์โดยอัตโนมัติ 15-95% สำหรับภาพรวมโดยย่อ โปรดดู[ส่วนการบีบอัดใน README](../README.md#%EF%B8%8F-prompt-compression--save-15-95-eligible-tokens-automatically)

## ภาพรวม

OmniRoute ใช้ไปป์ไลน์การบีบอัดพรอมต์แบบโมดูลาร์ซึ่งทำงาน **เชิงรุก** ก่อนที่คำขอจะไปถึงผู้ให้บริการต้นทาง ซึ่งหมายความว่าคุณจะประหยัดโทเค็นได้อย่างโปร่งใส โดยไม่ต้องเปลี่ยนแปลงเวิร์กโฟลว์

```
คำขอจากไคลเอนต์
  → ตัวเลือกกลยุทธ์การบีบอัด
    → มีการแทนที่ด้วยคอมโบหรือไม่? → ใช้การตั้งค่าคอมโบ
    → ถึงเกณฑ์ทริกเกอร์อัตโนมัติหรือไม่? → ใช้โหมดอัตโนมัติ
    → มีโหมดเริ่มต้นหรือไม่? → ใช้การตั้งค่าส่วนกลาง
    → ปิดอยู่หรือไม่? → ข้ามการบีบอัด
  → โหมดการบีบอัดที่เลือก
    → ปิด: ไม่มีการบีบอัด
    → เบา: ล้างช่องว่าง/การจัดรูปแบบอย่างปลอดภัย (~15%)
    → มาตรฐาน: ตัดคำฟุ่มเฟือยแบบภาษามนุษย์ถ้ำ (~30%)
    → เข้มข้น: ลดทอนประวัติตามอายุ + สรุปเนื้อหา (~50%)
    → สูงสุด: ตัดทอนด้วยฮิวริสติก + ลดทอนบล็อกโค้ด (~75%)
    → RTK: กรองเอาต์พุตจากเทอร์มินัล/เครื่องมือโดยคำนึงถึงคำสั่ง (ช่วงต้นทาง 60-90%)
    → แบบซ้อน: ไปป์ไลน์หลายเอนจินตามลำดับ โดยทั่วไปคือ RTK แล้วตามด้วย Caveman (ช่วงที่เข้าเกณฑ์ 78-95%)
  → คำขอที่บีบอัดแล้ว → ผู้ให้บริการ
```

---

## โหมดการบีบอัด

### ปิด

ไม่มีการบีบอัด ข้อความทั้งหมดจะถูกส่งผ่านโดยไม่มีการเปลี่ยนแปลง

### โหมดเบา (ประหยัด ~15%, เวลาแฝง <1ms)

โหมดที่ปลอดภัยที่สุด — ไม่มีการเปลี่ยนแปลงความหมาย มีเพียงการล้างการจัดรูปแบบ:

| เทคนิค                   | คำอธิบาย                                            |
| ------------------------ | --------------------------------------------------- |
| `collapseWhitespace`     | รวมบรรทัดว่างที่ต่อเนื่องกันและลบช่องว่างท้ายบรรทัด |
| `dedupSystemPrompt`      | ลบข้อความระบบที่ซ้ำกัน                              |
| `compressToolResults`    | บีบอัดเอาต์พุตแบบละเอียดจากเครื่องมือ/ฟังก์ชัน      |
| `removeRedundantContent` | ลบคำสั่งที่ซ้ำกัน                                   |
| `replaceImageUrls`       | ย่อ URI ข้อมูลรูปภาพแบบ base64                      |

**เหมาะที่สุดสำหรับ:** การใช้งานแบบเปิดไว้ตลอดและเวิร์กโฟลว์ที่ให้ความสำคัญกับความปลอดภัย

### โหมดมาตรฐาน (ประหยัด ~30%)

ได้รับแรงบันดาลใจจาก [Caveman](https://github.com/JuliusBrussee/caveman) — ตัดคำเติมและถ้อยคำฟุ่มเฟือยโดยยังคงความหมายไว้:

- ตัดคำเติม ("please", "I think", "basically", "actually")
- ย่อวลีฟุ่มเฟือย ("in order to" → "to", "as a result of" → "because")
- ตัดถ้อยคำสุภาพที่แสดงความลังเล ("Would you mind...", "If you could possibly...")
- กฎ regex มากกว่า 30 ข้อที่ปรับแต่งมาสำหรับพรอมต์การเขียนโค้ด

**เหมาะที่สุดสำหรับ:** เวิร์กโฟลว์การเขียนโค้ดประจำวันและทีมที่คำนึงถึงต้นทุน

### โหมดเข้มข้น (ประหยัด ~50%)

การจัดการประวัติอย่างชาญฉลาดสำหรับเซสชันที่ยาวนาน:

- **การลดทอนข้อความตามอายุ** — ข้อความเก่าจะถูกบีบอัดมากขึ้นเรื่อยๆ
- **การสรุปผลลัพธ์จากเครื่องมือ** — เอาต์พุตจากเครื่องมือที่ยาวจะถูกแทนที่ด้วยบทสรุป
- **กลไกป้องกันความสมบูรณ์ของโครงสร้าง** — ทำให้แน่ใจว่าคู่ `tool_use` + `tool_result` ยังคงสอดคล้องกัน
- **การรับรู้ขนาดหน้าต่างบริบท** — เคารพขีดจำกัดโทเค็นของแต่ละโมเดล

**เหมาะที่สุดสำหรับ:** เซสชันดีบักที่ยาวนานและฐานโค้ดขนาดใหญ่

### โหมดสูงสุด (ประหยัด ~75%)

การบีบอัดสูงสุดสำหรับสถานการณ์ที่โทเค็นมีความสำคัญอย่างยิ่ง:

- **การตัดทอนด้วยฮิวริสติก** — ลบข้อความที่มีค่าต่ำกว่าเกณฑ์ความเกี่ยวข้อง
- **การลดทอนบล็อกโค้ด** — บีบอัดตัวอย่างโค้ดที่ซ้ำซาก
- **การตัดข้อความด้วยการค้นหาแบบทวิภาค** — ค้นหาจุดตัดที่เหมาะสมที่สุดสำหรับหน้าต่างบริบท
- รวมคุณสมบัติทั้งหมดของโหมดเข้มข้น

**เหมาะที่สุดสำหรับ:** เมื่อคุณชนขีดจำกัดบริบทซ้ำๆ

### โหมด RTK (ช่วงต้นทาง 60-90%)

โหมด RTK ได้รับการปรับให้เหมาะกับเอาต์พุตแบบละเอียดจากเครื่องมือที่ปรากฏในเซสชันของเอเจนต์เขียนโค้ด:

- ตรวจหาคลาสของคำสั่ง/เอาต์พุต เช่น `git status`, `git diff`, `git log`, ตัวรันทดสอบ,
  การบิลด์ด้วย TypeScript/Vite/Webpack, ESLint/Biome/Prettier, การตรวจสอบ/ติดตั้งด้วย npm, ล็อก Docker, เอาต์พุต
  โครงสร้างพื้นฐาน และเอาต์พุตเชลล์ทั่วไป
- ใช้ชุดตัวกรอง JSON จาก `open-sse/services/compression/engines/rtk/filters/`
- นำเข้าตัวกรองสคีมา RTK TOML v1 จากไฟล์ `filters.toml` ของโปรเจกต์หรือส่วนกลาง พร้อมการตรวจสอบ
  การทดสอบแบบอินไลน์และการควบคุมตามความเชื่อถือสำหรับไฟล์โปรเจกต์
- มาพร้อมตัวกรองในตัว 49 รายการพร้อมตัวอย่างการตรวจสอบแบบอินไลน์
- ลบลำดับควบคุม ANSI, แถบความคืบหน้า, บรรทัดที่ซ้ำกัน และสัญญาณรบกวนที่ไม่ก่อให้เกิดการดำเนินการ
- เก็บรักษาความล้มเหลว ข้อผิดพลาด คำเตือน ไฟล์ที่เปลี่ยนแปลง บทสรุป และส่วนท้ายของเอาต์พุตที่ยาว
- รองรับตัวกรองโปรเจกต์ที่ควบคุมตามความเชื่อถือ ตัวกรองส่วนกลาง และตัวเลือกการกู้คืนเอาต์พุตดิบที่ผ่านการปกปิดข้อมูล

**เหมาะที่สุดสำหรับ:** เซสชันของเอเจนต์ที่มีทรานสคริปต์จากเชลล์ การบิลด์ การทดสอบ git, grep และเอาต์พุตไฟล์

### โหมดแบบซ้อน (ช่วงที่เข้าเกณฑ์ 78-95%)

โหมดแบบซ้อนเรียกใช้เอนจินการบีบอัดหลายตัวตามลำดับที่กำหนดแน่นอน ไปป์ไลน์เริ่มต้นคือ:

```txt
RTK -> Caveman
```

ลำดับนี้จะทำให้เอาต์พุตจากเทอร์มินัล/เครื่องมือมีขนาดกะทัดรัดก่อน จากนั้นจึงใช้การย่อความหมายของ Caveman กับ
พรอมต์ภาษาธรรมชาติที่เหลืออยู่ ไปป์ไลน์แบบซ้อนสามารถกำหนดค่าได้ทั้งแบบส่วนกลางหรือผ่าน
คอมโบการบีบอัดที่กำหนดให้กับคอมโบการกำหนดเส้นทาง

**เหมาะที่สุดสำหรับ:** บริบทแบบผสมที่มีทั้งล็อกจากเครื่องมือขนาดใหญ่ รวมถึงคำสั่งจากมนุษย์หรือบทสรุปจากผู้ช่วย

---

## การคำนวณการประหยัดจากต้นทาง

OmniRoute อธิบายการประหยัดจากการบีบอัดโดยอ้างอิงจากสองแหล่ง ได้แก่ ผลการวัดประสิทธิภาพของโปรเจกต์ต้นทางและ
การทำงานร่วมกันของเอนจินภายใน OmniRoute เอง

| แหล่งที่มา | ตัวเลขจาก README ต้นทางที่ใช้ในที่นี้                                                                                           |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Caveman    | โทเค็นเอาต์พุตน้อยลง `~75%`, ประหยัดเอาต์พุตโดยเฉลี่ยจากการวัดประสิทธิภาพ `65%`, ช่วง `22-87%` และเครื่องมือบีบอัดอินพุต `~46%` |
| RTK        | ประหยัดเอาต์พุตคำสั่ง `60-90%`; เซสชันตัวอย่างลดจาก `~118,000 -> ~23,900` โทเค็น หรือประหยัด `79.7%` (`~80%`)                   |

สำหรับเพย์โหลดเครื่องมือ/บริบทที่ทับซ้อนกัน ชุดการทำงานร่วมกันเริ่มต้นของ OmniRoute จะเรียงเอนจินดังนี้:

```txt
RTK -> Caveman
```

การประหยัดรวมคำนวณแบบคูณ ไม่ใช่แบบบวก:

```txt
รวม       = 1 - (1 - การประหยัดของ RTK) * (1 - การประหยัดอินพุตของ Caveman)
ค่าเฉลี่ย = 1 - (1 - 0.80) * (1 - 0.46) = 89.2%
ช่วง      = 1 - (1 - 0.60..0.90) * (1 - 0.46) = 78.4-94.6%
```

ตัวเลข `78-95%` นี้ใช้ได้เมื่อทั้ง RTK และ Caveman สามารถลดเพย์โหลดอินพุต/บริบทเดียวกันได้
โหมดเอาต์พุตการตอบกลับของ Caveman เป็นอีกส่วนหนึ่ง: เมื่อเปิดใช้งาน ให้ใช้ตัวเลขการประหยัดเอาต์พุตของ Caveman เอง (`65%`
โดยเฉลี่ย, `~75%` ตามตัวเลขหลักที่นำเสนอ, ช่วง `22-87%`) การประหยัดค่าใช้จ่ายทั้งหมดขึ้นอยู่กับสัดส่วนพรอมต์/เอาต์พุตของคุณ

### ความหมายที่แท้จริงของคำว่า "เข้าเกณฑ์"

ช่วงตัวเลขหลัก 15-95% เป็นตัวเลขจริง แต่ใช้ได้เฉพาะกับเนื้อหาที่ **ซ้ำซ้อนหรือเยิ่นเย้อ** เท่านั้น เช่น บรรทัดข้อผิดพลาด
ที่ซ้ำกัน, บันทึกการบิลด์ที่แสดงคำเตือนเดิมซ้ำจำนวนมาก หรือผลลัพธ์จาก `grep`/การอ่านไฟล์ที่มีขนาดใหญ่เกินไป ทั้งนี้
**ไม่ได้** หมายความว่าทุกคำขอจะประหยัดได้มากขนาดนั้น

มีการยืนยันจากการทดลองแล้ว (`tests/unit/compression/stacked-compression-tool-result-savings.test.ts`): การรัน
`stacked` (RTK + Caveman) กับบล็อก `tool_result` รูปแบบ Anthropic ซึ่งมีบรรทัดข้อผิดพลาดเหมือนกัน 300 บรรทัด
ให้ผลลัพธ์เป็น **การประหยัดโทเค็น 95.93% / การประหยัดอักขระ 96.26%** ซึ่งอยู่ในช่วงที่ระบุไว้อย่างชัดเจน
แต่เมื่อใช้ไปป์ไลน์เดียวกันกับเอาต์พุตเครื่องมือปกติที่ไม่ซ้ำซ้อน (รายการผลลัพธ์ `grep` ที่สะอาด,
การอ่านไฟล์ขนาดสั้น, ข้อความสนทนาทั่วไป) ระบบจะให้ผลเป็น **การประหยัดเกือบเป็นศูนย์** อย่างถูกต้อง เพราะ
ไม่มีสิ่งที่ซ้ำให้ตัดออก และ `validateCompression()` (`validation.ts`) จะปฏิเสธการส่ง
ข้อความที่เขียนใหม่ หากการเขียนใหม่นั้นทำให้บล็อกโค้ด, URL, หัวข้อ, เวอร์ชัน หรือตัวระบุค่าคงที่แบบ ALL-CAPS สูญหายหรือเปลี่ยนแปลง

นี่เป็นพฤติกรรมที่ปลอดภัยและเป็นไปตามที่คาดไว้ ไม่ใช่บั๊ก: เซสชันเขียนโค้ดที่ส่วนใหญ่เป็นการอ่าน/ค้นหาไฟล์ที่สะอาด
จะประหยัดโดยรวมได้เพียงเล็กน้อย แม้จะเปิดใช้งานการบีบอัดเต็มรูปแบบแล้วก็ตาม ขณะที่เซสชันซึ่งพบลูปที่ล้มเหลว
หรือลินเตอร์ที่แสดงข้อความจำนวนมาก จะประหยัดได้เต็มช่วง 78-95% สำหรับทราฟฟิกส่วนนั้น อย่าใช้เปอร์เซ็นต์การประหยัดรวม
ที่ต่ำจากเซสชันเดียวเป็นหลักฐานว่าการบีบอัดตั้งค่าผิด — ให้ตรวจสอบก่อนว่า
เอาต์พุตเครื่องมือต้นทางมีความซ้ำซ้อนจริงหรือไม่

---

## การแสดงภาพการประหยัดโทเค็น

```
ไม่มีการบีบอัด:         ส่ง 47K โทเค็นไปยัง LLM
ใช้ Lite:                ส่ง 40K โทเค็น          (ประหยัด 15% — ปลอดภัยและเปิดใช้งานตลอดเวลา)
ใช้ Standard:            ส่ง 33K โทเค็น          (ประหยัด 30% — กฎการพูดแบบ Caveman)
ใช้ Aggressive:          ส่ง 24K โทเค็น          (ประหยัด 50% — การลดความสำคัญตามอายุ + การสรุป)
ใช้ Ultra:               ส่ง 12K โทเค็น          (ประหยัด 75% — การตัดทอนด้วยฮิวริสติก)
ใช้ RTK:                 ส่ง 19K-5K โทเค็น       (ประหยัด 60-90% สำหรับเอาต์พุตคำสั่ง/เครื่องมือ)
ใช้ Stacked:             ส่ง 10K-2.5K โทเค็น     (ช่วง 78-95% สำหรับ RTK+Caveman ที่เข้าเกณฑ์)
```

---

## การกำหนดค่า

### แดชบอร์ด

ไปที่ `แดชบอร์ด → บริบทและแคช`:

- **Caveman** — การเลือกโหมด, ชุดภาษา, การแสดงตัวอย่าง, และค่าเริ่มต้นทั่วโลก
- **RTK** — การแสดงตัวอย่างตัวกรองคำสั่ง, การตั้งค่าความปลอดภัยของ RTK, และแค็ตตาล็อกตัวกรอง
- **Compression Combos** — ไปป์ไลน์เอนจินที่มีชื่อซึ่งกำหนดให้กับคอมโบการกำหนดเส้นทาง
- **เกณฑ์การทริกเกอร์อัตโนมัติ** — เปิดใช้งานการบีบอัดโดยอัตโนมัติเมื่อจำนวนโทเค็นเกินเกณฑ์

### การแทนที่ต่อคอมโบ

ใน `แดชบอร์ด → บริบทและแคช → Compression Combos` ให้กำหนดคอมโบการบีบอัดให้กับคอมโบการกำหนดเส้นทาง:

```txt
Combo: "free-tier-fallback"
  Compression Combo: "coding-agent-stack"
  Pipeline: RTK -> Caveman
  Targets:
    1. if/kimi-k2.7-code
    2. if/qwen3.8-max-preview
```

สิ่งนี้ช่วยให้คุณสามารถใช้การบีบอัดแบบซ้อนกันบนผู้ให้บริการฟรี/การเขียนโค้ด ในขณะที่ยังคงใช้โหมด Lite สำหรับการสมัครสมาชิกแบบชำระเงิน

การกำหนด "การแทนที่ต่อคอมโบ" นี้เป็นการควบคุมที่แตกต่างจากการแทนที่ **โหมดการบีบอัดคอมโบการกำหนดเส้นทาง** (Default/Off/Lite/Standard/Aggressive/Ultra) — การแทนที่นั้นไม่ได้เลือกไปป์ไลน์คอมโบการบีบอัดที่มีชื่อ; มันเพียงแค่ตั้งค่าฟิลด์ `compressionMode` ที่ `resolveCompressionPlan` ใช้ในการพิจารณา สามารถตั้งค่าได้ทั้งบนการ์ดคอมโบ (`แดชบอร์ด → คอมโบ`) หรือ ตั้งแต่ #6760 เป็นต้นมา สามารถตั้งค่าต่อคอมโบการกำหนดเส้นทางในรายการ "กำหนดให้กับการกำหนดเส้นทาง" บน `แดชบอร์ด → บริบทและแคช → Compression Combos` ซึ่งอยู่ถัดจากช่องทำเครื่องหมายการกำหนดไปป์ไลน์ที่กล่าวถึงข้างต้น ทั้งสองส่วนนี้จะคงอยู่ผ่านปลายทาง `PUT /api/combos/{id}` เดียวกัน

### การแทนที่ต่อคำขอ

ส่งส่วนหัวคำขอ `x-omniroute-compression` เพื่อแทนที่แผนการบีบอัดสำหรับคำขอเดียว มีลำดับความสำคัญสูงสุด — มันจะแทนที่การแทนที่คอมโบการกำหนดเส้นทาง, โปรไฟล์ที่ใช้งานอยู่, การทริกเกอร์อัตโนมัติ, และค่าเริ่มต้นของแผงควบคุม ค่าที่ไม่รู้จักจะถูกละเว้น (คำขอจะไม่ถูกปฏิเสธ) และสวิตช์หลักทั่วโลกยังคงควบคุมทุกอย่าง: เมื่อการบีบอัดถูกปิดทั่วโลก ส่วนหัวนี้ไม่สามารถเปิดใช้งานได้ ค่า:

| ค่า           | ผลกระทบ                                                                                 |
| ------------- | --------------------------------------------------------------------------------------- |
| `off`         | ไม่มีการบีบอัดสำหรับคำขอนี้                                                             |
| `default`     | โปรไฟล์เริ่มต้นที่ได้จากแผงควบคุม (ละเว้นโปรไฟล์ที่ใช้งานอยู่) เอนจินแบบ Lossy จะถูกปิด |
| `safe`        | เหมือนกับการละเว้นส่วนหัว: การลบข้อมูลซ้ำและการยุบช่องว่างเท่านั้น                      |
| `allow-lossy` | คงแผนการดำเนินการของคำขอนี้ไว้ รวมถึงสรุป, ตัวกรองความเกี่ยวข้อง, และการเขียนสไตล์ใหม่  |
| `engine:<id>` | เอนจินเดียวเมื่อเปิดใช้งาน เช่น `engine:rtk` นี่คือการเลือกใช้เอนจินนั้นต่อคำขอ         |
| `<combo>`     | คอมโบที่มีชื่อ, จับคู่ตามชื่อ (ไม่คำนึงถึงตัวพิมพ์เล็ก/ใหญ่) ก่อน, จากนั้นตาม ID        |

หากไม่มี `allow-lossy`, `engine:<id>`, หรือคอมโบที่มีชื่อ เอนจินแบบ Lossy จะไม่ถูกนำมาใช้ คำขอยังคงได้รับการลบข้อมูลซ้ำในเซสชันและการยุบช่องว่างเมื่อเปิดใช้งานการบีบอัด

แผนที่ถูกนำไปใช้จะถูกส่งกลับในส่วนหัวการตอบกลับ `X-OmniRoute-Compression: <mode>; source=<source>` โดยที่ `<source>` เป็นหนึ่งใน `request-header`, `routing-override`, `active-profile`, `auto-trigger`, `default`, หรือ `off`

### API

```bash
# รับการตั้งค่าการบีบอัด
curl http://localhost:20128/api/settings/compression

# อัปเดตการตั้งค่าการบีบอัด
curl -X PUT http://localhost:20128/api/settings/compression \
  -H "Content-Type: application/json" \
  -d '{"defaultMode":"stacked","autoTriggerMode":"stacked","autoTriggerTokens":32000}'

# ดูตัวอย่างเพย์โหลด RTK/stacked ที่เฉพาะเจาะจง
curl -X POST http://localhost:20128/api/compression/preview \
  -H "Content-Type: application/json" \
  -d '{"mode":"rtk","messages":[{"role":"tool","content":"npm test output here"}]}'

# แสดงรายการชุดตัวกรอง RTK
curl http://localhost:20128/api/context/rtk/filters

# ทดสอบ RTK โดยตรงพร้อมข้อมูลเมตาคำสั่งเสริม
curl -X POST http://localhost:20128/api/context/rtk/test \
  -H "Content-Type: application/json" \
  -d '{"command":"npm test","text":"FAIL tests/example.test.ts\nError: boom"}'
```

---

## สิ่งที่ได้รับการปกป้อง

กลไกการบีบอัด **จะคงสิ่งต่อไปนี้ไว้เสมอ:**

- ✅ บล็อกโค้ด (ทั้งแบบ fenced และ inline)
- ✅ URL และพาธไฟล์
- ✅ โครงสร้าง JSON และข้อมูลที่มีโครงสร้าง
- ✅ ตัวระบุและโทเค็นทางเทคนิคที่ได้รับการปกป้อง
- ✅ นิพจน์ทางคณิตศาสตร์
- ✅ นิยามการเรียกใช้เครื่องมือ/ฟังก์ชัน
- ✅ พรอมต์ระบบ (ในโหมด lite)

การกู้คืนเอาต์พุตดิบของ RTK จะปกปิด API key ที่พบบ่อย, bearer token, Slack token, AWS access key,
รหัสผ่าน, โทเค็น และข้อมูลลับ ก่อนที่จะจัดเก็บข้อมูลใดๆ

---

## สถิติการบีบอัด

ทุกคำขอที่ถูกบีบอัดจะรวมสถิติไว้ในบันทึกของเซิร์ฟเวอร์:

```json
{
  "originalTokens": 47200,
  "compressedTokens": 40120,
  "savingsPercent": 15.0,
  "techniquesUsed": ["collapseWhitespace", "dedupSystemPrompt"],
  "mode": "lite",
  "engine": "caveman",
  "compressionComboId": "coding-agent-stack",
  "durationMs": 0.8,
  "rtkRawOutputPointers": []
}
```

---

## แผนงานตามเฟส

| เฟส      | โหมด                                                                                                                                          | สถานะ         |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| Phase 1  | ปิด, ไลต์                                                                                                                                     | ✅ จัดส่งแล้ว |
| Phase 2  | มาตรฐาน, ก้าวร้าว, อัลตร้า                                                                                                                    | ✅ จัดส่งแล้ว |
| Phase 3  | RTK, สแต็ก, การรวมการบีบอัด                                                                                                                   | ✅ จัดส่งแล้ว |
| Phase 4  | สไตล์เอาต์พุต, อัลตร้าระดับ SLM, ชุดประเมินผล                                                                                                 | ✅ จัดส่งแล้ว |
| Phase 4C | งบประมาณบริบทแบบปรับได้ ("dial") — เอ็นจิ้นการคำนวณ + API (`contextBudget` บน `PUT /api/settings/compression`) + การควบคุมโหมด/นโยบายแดชบอร์ด | ✅ จัดส่งแล้ว |

---

## กิตติกรรมประกาศ

กฎการบีบอัดในโหมด Standard ได้รับแรงบันดาลใจจาก **[Caveman](https://github.com/JuliusBrussee/caveman)** โดย **[JuliusBrussee](https://github.com/JuliusBrussee)** (⭐ 51K+) — โปรเจกต์ไวรัล "ทำไมต้องใช้หลายโทเค็น ในเมื่อใช้ไม่กี่โทเค็นก็ได้ผล" Caveman รายงานว่าลดจำนวนโทเค็นเอาต์พุตได้ `~75%`, ประหยัดเอาต์พุตโดยเฉลี่ยจากเกณฑ์มาตรฐานได้ `65%`, มีช่วงการลดเอาต์พุตอยู่ที่ `22-87%` และมีเครื่องมือบีบอัดอินพุตที่ลดได้ `~46%`

โหมด RTK ได้รับแรงบันดาลใจจาก **[RTK - Rust Token Killer](https://github.com/rtk-ai/rtk)** โดย **[RTK AI](https://github.com/rtk-ai)** — โปรเจกต์บีบอัดเอาต์พุตคำสั่งประสิทธิภาพสูงสำหรับเทอร์มินัล, การบิลด์, การทดสอบ, git และการกรองเอาต์พุตจากเครื่องมือ RTK รายงานว่าประหยัดได้ `60-90%` โดยเซสชันตัวอย่างใน README แสดงว่าประหยัดได้ `~80%`

---

## ระบบบีบอัดขั้นสูง

นอกเหนือจาก 7 โหมดมาตรฐานแล้ว OmniRoute ยังมีระบบบีบอัดขั้นสูงหลายระบบที่ทำงานโดยอัตโนมัติตามบริบท

### การบีบอัดที่รับรู้แคช

ผู้ให้บริการบางราย (เช่น Anthropic ที่มีการแคชพรอมต์) รองรับ **การแคชพรอมต์** ซึ่งช่วยให้พวกเขาสามารถแคชส่วนต่างๆ ของพรอมต์เพื่อลดต้นทุนและเวลาแฝงได้ เมื่อเปิดใช้งานการแคช การบีบอัดแบบ aggressive อาจ **ส่งผลเสีย** ต่อประสิทธิภาพการทำงานได้จริง เนื่องจากมันจะเปลี่ยนโทเค็นที่ถูกแคช ทำให้แคชไม่ถูกต้อง

โมดูล `cachingAware.ts` แก้ปัญหานี้โดย **การตรวจจับบริบทการแคช** และ **การปรับกลยุทธ์การบีบอัด** ให้เหมาะสม

#### วิธีการทำงาน

1.  **ตรวจจับบริบทการแคช** — สแกนเนื้อหาคำขอเพื่อหาเครื่องหมาย `cache_control`
2.  **ระบุผู้ให้บริการแคช** — ตรวจสอบว่าผู้ให้บริการเป้าหมายรองรับการแคชหรือไม่
3.  **ปรับกลยุทธ์** — ลดระดับ `aggressive`/`ultra` เป็น `standard` สำหรับผู้ให้บริการแคช
4.  **ข้ามพรอมต์ระบบ** — พรอมต์ระบบมักจะถูกแคช ดังนั้นจึงไม่บีบอัด
5.  **ใช้การแปลงแบบกำหนดผลลัพธ์ได้** — ใช้เฉพาะการแปลงที่ให้ผลลัพธ์ที่สอดคล้องกันเท่านั้น

#### ตัวอย่างโค้ด

```ts
import {
  detectCachingContext,
  getCacheAwareStrategy,
} from "@omniroute/open-sse/services/compression/cachingAware";

const body = {
  model: "anthropic/claude-sonnet-4.5",
  messages: [{ role: "user", content: "Hello" }],
  cache_control: { type: "ephemeral" }, // ← เครื่องหมายแคช
};

const ctx = detectCachingContext(body, { provider: "anthropic" });
// → { hasCacheControl: true, provider: "anthropic", isCachingProvider: true }

const strategy = getCacheAwareStrategy("aggressive", ctx);
// → { strategy: "standard", skipSystemPrompt: true, deterministicOnly: true }
```

#### เวลาที่ใช้

การบีบอัดที่รับรู้แคชจะ **เปิดใช้งานตลอดเวลา** — ไม่ต้องมีการกำหนดค่าใดๆ มันจะทำงานก็ต่อเมื่อ:

- คำขอมีเครื่องหมาย `cache_control`
- ผู้ให้บริการเป้าหมายรองรับการแคชพรอมต์ (Anthropic, OpenAI, เป็นต้น)

### การลดคุณภาพตามอายุแบบก้าวหน้า

การสนทนาที่ยาวนานจะสะสมข้อความหลายรอบ แต่รอบที่เก่ากว่าจะมีความเกี่ยวข้องน้อยลง โมดูล `progressiveAging.ts` จะ **ลดคุณภาพข้อความตามระยะห่างของรอบ**:

- **รอบล่าสุด (0-3)**: เก็บไว้ตามเดิม (รายละเอียดครบถ้วน)
- **รอบปานกลาง (4-8)**: การบีบอัดแบบเบา (ลบช่องว่าง, จัดรูปแบบให้เรียบร้อย)
- **รอบเก่า (9+)**: การบีบอัดแบบ Caveman (ลบคำฟุ่มเฟือย, สรุป)
- **รอบที่เก่ามาก (20+)**: สรุปอย่างมากหรือถูกละทิ้ง

#### ตัวอย่างโค้ด

```ts
import { applyAging } from "@omniroute/open-sse/services/compression/progressiveAging";

const messages = [
  { role: "system", content: "You are a helpful assistant" },
  { role: "user", content: "What is 2+2?" },
  { role: "assistant", content: "4" },
  // ... อีก 50 รอบ ...
];

const { messages: aged, saved } = applyAging(messages, {
  verbatim: 3, // 3 รอบแรก: ตามเดิม
  light: 8, // รอบที่ 4-8: การบีบอัดแบบเบา
  moderate: 20, // รอบที่ 9-20: การบีบอัดแบบ Caveman
  // รอบที่ 21 ขึ้นไป: การสรุปอย่างมาก
});

// saved = จำนวนโทเค็นที่ประหยัดได้
```

#### เวลาที่ใช้

การลดคุณภาพตามอายุแบบก้าวหน้าจะ **เปิดใช้งานตลอดเวลา** สำหรับโหมด `aggressive` และ `ultra` ซึ่งมีประสิทธิภาพเป็นพิเศษสำหรับ:

- เซสชันการเขียนโค้ดที่ใช้เวลานาน
- การสนทนาหลายวัน
- เวิร์กโฟลว์แบบ Agentic ที่มีการเรียกใช้เครื่องมือหลายครั้ง

### โหมดเอาต์พุตแบบ Caveman

โมดูล `outputMode.ts` จะแทรก **คำสั่งพรอมต์ระบบ** เพื่อให้โมเดลสร้างเอาต์พุตที่บีบอัดและกระชับ (สไตล์ "caveman")

#### วิธีการทำงาน

แทนที่จะบีบอัดอินพุต โหมดนี้จะเพิ่มพรอมต์ระบบเช่น:

> "ตอบด้วยคำพูดให้น้อยที่สุด ข้ามคำทักทาย ใช้ประโยคสั้นๆ"

สิ่งนี้ทำงานได้ดีเป็นพิเศษสำหรับ:

- การสร้างโค้ด (เอาต์พุตที่กระชับ = โทเค็นน้อยลง)
- การถามตอบอย่างรวดเร็ว (ไม่จำเป็นต้องมีคำอธิบายที่ซับซ้อน)
- การประมวลผลแบบแบตช์ (เพิ่มปริมาณงานสูงสุด)

#### เวลาที่ใช้

โหมดเอาต์พุตแบบ Caveman เป็นแบบ **เลือกใช้** — ตั้งค่าผ่านการกำหนดค่าแบบคอมโบ:

```json
{
  "strategy": "auto",
  "config": {
    "auto": {
      "outputMode": "caveman"
    }
  }
}
```

### สไตล์เอาต์พุต (แค็ตตาล็อก)

โหมดเอาต์พุตแบบ Caveman ข้างต้นเป็น **เส้นทางสไตล์เดียวแบบเดิม** Phase 4 ได้ขยายให้เป็นแค็ตตาล็อกของสไตล์เอาต์พุตที่ประกอบรวมกันได้: `OUTPUT_STYLE_CATALOG` ใน `open-sse/services/compression/outputStyles/catalog.ts` แต่ละสไตล์เป็นคำสั่งพรอมต์ระบบที่ทำให้โมเดลสร้างเอาต์พุตที่ประหยัดกว่า สไตล์ต่างๆ สามารถเปิดใช้งานพร้อมกันได้และจะถูกแทรกตามลำดับในแค็ตตาล็อก

| Style                             | `id`          | What it does                                                                                                                                                                                                                          | Instruction languages                                      |
| :-------------------------------- | :------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :--------------------------------------------------------- |
| ร้อยแก้วกระชับ                    | `terse-prose` | ตัดคำฟุ่มเฟือย/คำนำหน้า/คำที่แสดงความไม่แน่ใจออก; คงเนื้อหาทางเทคนิคให้แม่นยำ. ข้อความเดียวกันกับโหมดเอาต์พุต caveman แบบเดิม (อ้างอิง ไม่ได้พิมพ์ซ้ำ).                                                                               | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi              |
| โค้ดน้อยลง                        | `less-code`   | หลัก YAGNI: การเปลี่ยนแปลงที่เล็กที่สุดที่ใช้งานได้, ไม่มีนามธรรมที่ไม่จำเป็น.                                                                                                                                                        | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi              |
| Ponytail (นักพัฒนาอาวุโสขี้เกียจ) | `ponytail`    | "โค้ดที่ดีที่สุดคือโค้ดที่ไม่เคยถูกเขียน": ใช้ซ้ำ > เขียนใหม่, สาเหตุหลัก > อาการ, diff ที่ทำงานได้สั้นที่สุด.                                                                                                                        | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi              |
| ฉันมี ADHD (เน้นการกระทำก่อน)     | `i-have-adhd` | เน้นการกระทำก่อน (คำสั่ง/พาธ/โค้ดสั้นๆ ก่อนร้อยแก้ว), ขั้นตอนที่มีหมายเลขและขอบเขต, ขั้นตอนถัดไปที่เป็นรูปธรรมเพียงหนึ่งเดียว, ไม่มีคำนำ/สรุป/คำลงท้าย. ดัดแปลงจาก [ayghri/i-have-adhd](https://github.com/ayghri/i-have-adhd) (MIT). | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi              |
| CJK กระชับ (文言)                 | `terse-cjk`   | สไตล์จีนโบราณที่กระชับเป็นพิเศษ.                                                                                                                                                                                                      | zh (จำกัดตามภาษา: เสนอให้เฉพาะเมื่อภาษาที่ถูกระบุคือ `zh`) |

ทุกสไตล์มาพร้อมกับสามระดับความเข้มข้น — `lite`, `full`, `ultra` — และทุกระดับจะปิดท้ายด้วยข้อกำหนดขอบเขตที่ใช้ร่วมกัน ซึ่งจะคงบล็อกโค้ด, พาธไฟล์, คำสั่ง, สตริงข้อผิดพลาด, URL และตัวระบุไว้ตามเดิม.

#### การทำงานของการฉีด

`applyOutputStyles()` (`open-sse/services/compression/outputStyles/apply.ts`) จะแก้ไขการเลือกเทียบกับแค็ตตาล็อก (ID ที่ไม่รู้จักและสไตล์ที่ไม่ตรงกับภาษาจะถูกละทิ้ง ไม่ถือเป็นข้อผิดพลาด), เชื่อมคำสั่งที่เลือกตามลำดับในแค็ตตาล็อก, เพิ่มข้อกำหนดขอบเขต **เพียงครั้งเดียว**, และโหลดผลลัพธ์เข้าสู่ system prompt ด้านหลังตัวระบุ idempotency ตัวเดียว (`[OmniRoute Output Styles]`) — การนำไปใช้ซ้ำจะไม่มีผล. เมื่อภาษาที่ตรวจพบของคำขอมีการแปล, คำสั่งที่แปลแล้วจะถูกฉีดเข้าไปแทนภาษาอังกฤษ.

#### วิธีเปิดใช้งาน

ในแดชบอร์ด: **Context → Settings → Compression** — หนึ่งแถวต่อหนึ่งสไตล์พร้อมปุ่มเปิด/ปิดและตัวเลือกระดับ. ในเชิงโปรแกรม, การกำหนดค่าการบีบอัดจะคงการเลือกไว้ดังนี้:

```json
{
  "outputStyles": [
    { "id": "i-have-adhd", "level": "full" },
    { "id": "less-code", "level": "lite" }
  ]
}
```

ความเข้ากันได้ย้อนหลัง: การตั้งค่าคอมโบ `outputMode: "caveman"` แบบเดิมยังคงใช้งานได้และแมปกับ `terse-prose` ซึ่งมีไบต์เหมือนกับการฉีดแบบเก่าในทุกภาษาเดิม.

การเลือกภาษา: เมื่อ `languageConfig.enabled` เปิดอยู่, `autoDetect` จะเลือกภาษาของข้อความผู้ใช้ล่าสุด (ตัวตรวจจับเดียวกับเอนจิ้นอินพุต); การปิด `autoDetect` จะตรึง `defaultLanguage` ไว้. ปิด → English.

เมทริกซ์สไตล์ × ภาษาถูกตรึงโดย `tests/unit/compression/output-styles-i18n-matrix.test.ts`: สไตล์ใหม่ไม่สามารถเผยแพร่ได้หากไม่มีการแปลอย่างน้อยเป็น pt-BR (หรือข้อยกเว้นที่ติดตามอย่างชัดเจน), และสไตล์ที่มีอยู่ไม่สามารถสูญเสียภาษาไปอย่างเงียบๆ. หากต้องการเพิ่มสไตล์, โปรดดูที่ [EXTENDING_COMPRESSION.md](./EXTENDING_COMPRESSION.md#adding-an-output-style).

### การบีบอัดผลลัพธ์เครื่องมือ

โมดูล `toolResultCompressor.ts` มี **5 กลยุทธ์การบีบอัดเฉพาะทาง** สำหรับผลลัพธ์เครื่องมือ (การเรียกใช้ฟังก์ชัน, เอาต์พุตของเอเจนต์, ผลการค้นหา ฯลฯ):

1.  **การบีบอัดผลการค้นหา** — ลบผลลัพธ์ที่ซ้ำซ้อน, เก็บ N อันดับแรก
2.  **การบีบอัดการอ่านไฟล์** — ตัดไฟล์ขนาดใหญ่, คงส่วนหัว/การนำเข้าไว้
3.  **การบีบอัดการดำเนินการโค้ด** — เก็บเฉพาะ stdout/stderr ที่จำเป็น
4.  **การบีบอัดการสอบถามฐานข้อมูล** — จำกัดจำนวนแถว, ลบเมตาดาตาที่ละเอียดเกินไป
5.  **การบีบอัดการตอบสนอง API** — ลบฟิลด์ที่เป็น null, ย่ออาร์เรย์

#### เมื่อใดควรใช้

การบีบอัดผลลัพธ์เครื่องมือจะ **เปิดใช้งานเสมอ** เมื่อมีการเรียกใช้เครื่องมือ. ไม่จำเป็นต้องมีการกำหนดค่า.

### ไปป์ไลน์แบบซ้อน

โหมดแบบซ้อนจะรัน **เอนจิ้นหลายตัวตามลำดับ** — โดยปกติคือ RTK ก่อน (ประหยัด 60-90% สำหรับเอาต์พุตเครื่องมือ), จากนั้น Caveman (ประหยัดเพิ่มอีก 30% สำหรับข้อความที่เหลือ). ซึ่งทำให้ได้ **การประหยัดรวม 78-95%**.

#### วิธีการทำงาน

```
อินพุต (1000 โทเค็น)
  → RTK (ตัวกรองที่รับรู้คำสั่ง) → 200 โทเค็น
    → Caveman (การลบคำฟุ่มเฟือย) → 140 โทเค็น
  → เอาต์พุต (140 โทเค็น, ประหยัด 86%)
```

#### เมื่อใดควรใช้

ใช้โหมดแบบซ้อนสำหรับ:

- เวิร์กโฟลว์ที่ใช้เครื่องมือหนัก (การเขียนโค้ดแบบเอเจนต์, การวิจัย)
- การประมวลผลแบบแบตช์ที่คำนึงถึงต้นทุน
- เมื่อคุณต้องการประหยัดโทเค็นสูงสุด

กำหนดค่าผ่านคอมโบ:

```json
{
  "strategy": "auto",
  "config": {
    "auto": {
      "modePack": "stacked"
    }
  }
}
```

---

## การแทนที่การบีบอัดสำหรับแต่ละคอมโบ

คุณสามารถแทนที่โหมดการบีบอัดส่วนกลาง **สำหรับแต่ละคอมโบ** เพื่อปรับแต่งลักษณะการทำงาน
ให้เหมาะกับกรณีการใช้งานต่างๆ:

```json
{
  "id": "coding-combo",
  "strategy": "priority",
  "config": {
    "auto": {
      "weights": { "taskFit": 0.5 },
      "modePack": "quality-first"
    }
  },
  "compressionOverride": {
    "mode": "aggressive",
    "stackedPipelines": ["rtk", "caveman"],
    "preserveToolDefinitions": true
  }
}
```

ฟีเจอร์นี้มีประโยชน์สำหรับ:

- **คอมโบสำหรับการเขียนโค้ด**: ใช้โหมด `aggressive` สำหรับเซสชันที่ยาวนาน
- **คอมโบถาม-ตอบแบบรวดเร็ว**: ใช้โหมด `lite` เพื่อให้ตอบกลับได้รวดเร็ว
- **คอมโบที่ใช้เครื่องมือจำนวนมาก**: ใช้โหมด `stacked` เพื่อประหยัดสูงสุด
- **คอมโบสำหรับระบบที่ใช้งานจริง**: ใช้โหมด `cache-aware` สำหรับผู้ให้บริการที่รองรับการแคช

---

## ดูเพิ่มเติม

- [การกำหนดค่าสภาพแวดล้อม](../reference/ENVIRONMENT.md) — ตัวแปรสภาพแวดล้อมสำหรับการบีบอัด
- [คู่มือสถาปัตยกรรม](../architecture/ARCHITECTURE.md) — การทำงานภายในของไปป์ไลน์การบีบอัด
- [คู่มือผู้ใช้](../guides/USER_GUIDE.md) — เริ่มต้นใช้งานการบีบอัด
- [การบีบอัดด้วย RTK](./RTK_COMPRESSION.md) — ตัวกรอง RTK, โมเดลความน่าเชื่อถือ, เกตตรวจสอบ และการกู้คืนเอาต์พุตดิบ
- [กลไกการบีบอัด](./COMPRESSION_ENGINES.md) — Caveman, RTK, การซ้อนกลไก, API, MCP และแดชบอร์ด
- [รูปแบบกฎการบีบอัด](./COMPRESSION_RULES_FORMAT.md) — รูปแบบแพ็กกฎ JSON
- [แพ็กภาษาสำหรับการบีบอัด](./COMPRESSION_LANGUAGE_PACKS.md) — กฎ Caveman ที่เฉพาะเจาะจงสำหรับแต่ละภาษา
