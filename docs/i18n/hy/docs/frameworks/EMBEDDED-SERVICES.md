# Embedded Services (Հայերեն)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Տարբերակը:** v3.8.44
> **Վերջին թարմացումը:** 2026-09-09
> **Լսարանը:** Ներկառուցված ծառայություններ (9Router, CLIProxyAPI, Mux, Bifrost, open-wa) ավելացնող, սպասարկող կամ կարգաբերող ինժեներներ։

Ներկառուցված ծառայությունները տեղականորեն տեղադրված պրոցեսի կողային գործիքներ են, որոնք OmniRoute-ը տեղադրում, վերահսկում և բացահայտում է որպես առաջնակարգ երթուղղման թիրախներ։ Ի տարբերություն արտաքին մատակարարների (որոնց հասանելի են ինտերնետի միջոցով՝ API բանալիների միջոցով), ներկառուցված ծառայություններն աշխատում են նույն մեքենայի վրա, ինչ OmniRoute-ը, և հաղորդակցվում են loopback-ի միջոցով։

---

## Բովանդակություն

1. [Ընդհանուր նկարագրություն](#1-overview)
2. [Ճարտարապետություն — 4 շերտ](#2-architecture--4-layers)
3. [Կենսափուլի վիճակների մեքենա](#3-lifecycle-state-machine)
4. [API տեղեկատու](#4-api-reference)
5. [Անվտանգություն](#5-security)
6. [Նոր ներկառուցված ծառայության ավելացում](#6-adding-a-new-embedded-service)
7. [Խնդիրների լուծում](#7-troubleshooting)
8. [ՀՏՀ](#8-faq)

---

## 1. Ընդհանուր ակնարկ

### Ինչու՞ ներկառուցված ծառայություններ:

Ներկառուցված են վեց ծառայություններ.

| Ծառայություն    | npm փաթեթ                          | Լռելյայն պորտ | Նպատակ                                                                                                                                                                                                                                   |
| --------------- | ---------------------------------- | :-----------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **9Router**     | `9router`                          |     20130     | AI երթուղիչ, որը OmniRoute-ը կարող է օգտագործել որպես ենթապրովայդեր: Մոդելները հասանելի են որպես `9router/{sub}/{model}`                                                                                                                 |
| **CLIProxyAPI** | GitHub release binary (`cliproxy`) |     8317      | Տեղական պրոքսի ադապտեր Anthropic CLI նույնականացման հոսքերի համար: Ապահովում է պահեստային երթուղի, երբ OAuth թոքենները ժամկետանց են լինում:                                                                                              |
| **Mux**         | `mux` (headless `mux server`)      |     8322      | Տեղական գործակալ-կազմակերպման դեմոն (coder/mux): Կյանքի ցիկլը կառավարվում է միայն՝ երթուղիչի թիրախ չէ (առանց LLM պրոքսիացիայի):                                                                                                          |
| **Bifrost**     | `@maximhq/bifrost`                 |     8080      | Go AI-դարպասի ռելեի բեքենդ: Գործարկման ժամանակ ավտոմատ կերպով ընտրվում է ռելեի երթուղու կողմից (`/v1/relay/`)                                                                                                                            |
| **Dario**       | `@askalf/dario`                    |     3456      | Claude-բաժանորդագրության պրոքսի՝ այլընտրանք/անսարքության դեպքում անցում CLIProxyAPI-ին Claude-Code-ի տեսքով տրաֆիկի համար. ներարկված բանալին դառնում է `DARIO_ADMIN_TOKEN`, որը սահմանափակում է նրա `/admin/*` OAuth կառավարման հարթակը: |
| **open-wa**     | `@open-wa/wa-automate`             |     8323      | WhatsApp Web ավտոմատացում (անգլուխ Chromium՝ Puppeteer-ի միջոցով): Կյանքի ցիկլը կառավարվում է միայն՝ երթուղիչի թիրախ չէ:                                                                                                                 |

Բոլոր վեցը հետևում են նույն վերահսկողական մոդելին.

- OmniRoute-ը դրանք տեղադրում է `DATA_DIR/services/{name}/` տակ (մեկուսացված OmniRoute-ի սեփական `package.json`-ից)
- OmniRoute-ը գործարկում և վերահսկում է դրանք որպես ենթապրոցեսներ
- OmniRoute-ը ժամանակավոր API բանալի է ներարկում ենթապրոցեսի միջավայր և պտտում է այն առանց ընդհատումների (որտեղ կիրառելի է)
- Բոլոր կառավարման երթուղիները (`/api/services/*`) **ՄԻԱՅՆ ՏԵՂԱԿԱՆ ԵՆ** — հասանելի են միայն լուփբեքից (խիստ կանոն #17)

### Հիմնական որոշումներ (դիզայնի պլանից)

| Որոշում                                        | Արժեք                                                                                                  |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Կառավարման վահանակի մուտք 9Router-ի բնիկ UI-ին | Հակադարձ պրոքսի `/dashboard/providers/services/9router/embed/*` հասցեում                               |
| Տեղադրման մեխանիզմ                             | ``npm install {package}`՝ `execFile`-ի միջոցով (առանց shell-ի ինտերպոլյացիայի)``                       |
| Սպառման ռեժիմ                                  | Պրովայդերը գրանցված է որպես `9router/{sub}/{model}` երթուղիչի շարժիչում                                |
| API բանալիների կառավարում                      | OmniRoute-ը ստեղծում, կոդավորում է պահեստավորման ժամանակ (AES-256-GCM) և ներարկում է միջավայրի միջոցով |
| Կառավարման վահանակի գտնվելու վայրը             | `/dashboard/providers/services` (երեք ներդիր)                                                          |
| Ավտոմատ մեկնարկ                                | Միացնել/անջատել յուրաքանչյուր ծառայության համար, լռելյայն ԱՆՋԱՏՎԱԾ է                                   |

---

## 2. Ճարտարապետություն — 4 շերտ

```
┌────────────────────────────────────────────────────────────────────┐
│  Շերտ 1 — Միջերես                                                  │
│  /dashboard/providers/services  (tabs: CLIProxyAPI | 9Router | Mux)│
│  Ուղիղ մուտքեր (SSE), Մեկնարկ/Դադարեցում/Վերագործարկում/Թարմացում, Կարգավորումներ, Տեղադրում     │
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Շել + ներդիրների երթուղի ?tab=-ով         │
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Շերտ 2 — API (LOCAL_ONLY — միայն լուփբեք)                        │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (հակադարձ HTTP + WebSocket պրոքսի → 9Router վերին հոսք)             │
│                                                                    │
│  Դարպաս. LOCAL_ONLY_API_PREFIXES-ը ներառում է "/api/services/" և       │
│        "/dashboard/providers/services/*/embed/"                    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ in-process calls
┌──────────────────────▼─────────────────────────────────────────────┐
│  Շերտ 3 — ServiceSupervisor (src/lib/services/)                   │
│                                                                    │
│  ServiceSupervisor.ts   Ընդհանուր վերահսկիչ (child_process.spawn)   │
│    ├── install:    execFile('npm', ['install', pkg, '--prefix'])    │
│    ├── start:      spawn(node, [entrypoint], {env, cwd})           │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── port:       20130 9Router-ի համար (կարգավորելի)                │
│    ├── logs:       stdio օղակաձև բուֆեր 5 ՄԲ → SSE իրադարձություններ             │
│    ├── health:     HTTP GET /health յուրաքանչյուր 2–5 վրկ, ծույլ վերականգնում     │
│    └── lifecycle:  SIGTERM 15 s → SIGKILL                          │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Գործարկում է բոլոր SERVICES[]-ը պրոցեսի մեկնարկին     │
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       Պարբերական GET /v1/models → service_models աղյուսակ │
│  ringBuffer.ts      Շրջանաձև մուտքերի բուֆեր (5 ՄԲ յուրաքանչյուր ծառայության համար)         │
│  healthCheck.ts     Հարցումային HTTP առողջության ստուգում                      │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (տեղադրողի ադապտերներ)                          │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ OpenAI-compatible HTTP (loopback)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Շերտ 4 — Պրովայդեր / Երթուղավորում                                      │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                 │
│    Յուրաքանչյուր հարցման համար նորից փնտրում է պորտը և API բանալին (առանց քեշավորման):          │
│    Հեռացնում է "9router/" նախածանցը մոդելի ID-ից՝ նախքան պրոքսիացումը:          │
│    Վերադարձնում է 503 service_not_running, եթե վերահսկիչը "running" վիճակում չէ: │
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Մուտք "9router"-ի համար. isEmbeddedService: true                    │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Մոդելները պահվում են որպես "9router/{sub}/{model}" (նախածանցով):            │
│    Համաժամեցվում է յուրաքանչյուր 5 րոպեն մեկ modelSync.ts-ի կողմից:                             │
│                                                                    │
│  Mux-ը կառավարվում է միայն կյանքի ցիկլով (Շերտեր 1-3) — այն գործակալների կառավարման դեմոն է, ոչ թե LLM պրոքսի, ուստի այն չունի Շերտ 4-ի կատարողի/պրովայդերի մուտք և երբեք երթուղավորման թիրախ չէ:            │
└────────────────────────────────────────────────────────────────────┘
```

### Հիմնական աղբյուրի ֆայլեր

| File                                        | Role                                                            |
| ------------------------------------------- | --------------------------------------------------------------- |
| `src/lib/services/ServiceSupervisor.ts`     | Հիմնական դաս՝ կյանքի ցիկլ, կողպեք, առողջություն, օղակաձև բուֆեր |
| `src/lib/services/bootstrap.ts`             | Գործընթացի մակարդակի գրանցում և ավտոմատ մեկնարկ                 |
| `src/lib/services/registry.ts`              | Սինգլտոն քարտեզ `գործիք → վերահսկիչ`                            |
| `src/lib/services/apiKey.ts`                | Բանալու ստեղծում, AES-256-GCM կոդավորում հանգիստ վիճակում       |
| `src/lib/services/modelSync.ts`             | Պարբերական մոդելի համաժամացում (5 րոպե) + ըստ պահանջի           |
| `src/lib/services/ringBuffer.ts`            | 5 ՄԲ օղակաձև մուտքի բուֆեր՝ SSE բաժանորդագրությամբ              |
| `src/lib/services/healthCheck.ts`           | HTTP առողջության զոնդ (կարգավորվող ինտերվալ)                    |
| `src/lib/services/installers/ninerouter.ts` | npm տեղադրում/թարմացում/հեռացում 9Router-ի համար                |
| `src/lib/services/installers/cliproxy.ts`   | npm տեղադրում/թարմացում/հեռացում CLIProxyAPI-ի համար            |
| `src/lib/services/installers/mux.ts`        | npm տեղադրում/թարմացում/հեռացում Mux-ի համար                    |
| `src/lib/services/installers/openwa.ts`     | npm տեղադրում/թարմացում/հեռացում open-wa-ի համար                |
| `src/app/api/services/9router/_lib.ts`      | `getOrInitSupervisor()` օգնական                                 |
| `src/app/api/services/[name]/logs/route.ts` | Ընդհանուր SSE մուտքերի վերջնակետ                                |
| `open-sse/executors/ninerouter.ts`          | Պրովայդերի կատարող (Շերտ 4)                                     |

---

## 3. Կենսացիկլի վիճակների մեքենա

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
        առողջության ստուգումը հաջող է │         խափանում / SIGTERM │
                               ┌────▼─────┐  (ելք 5 վ-ի ընթացքում) │
                               │ running  │──── խափանում ────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Վիճակները պահվում են `version_manager` ՏԲ աղյուսակում (`status` սյունակ) և արտացոլվում
են `ServiceSupervisor`-ի հիշողության վիճակում։ Հիշողության վիճակը գործարկվող
գործընթացի համար հեղինակավոր աղբյուրն է, իսկ ՏԲ վիճակը՝ բեռնման ժամանակ օգտագործվող մշտական պահուստային աղբյուրը։

### Վիճակների անցումներ

| Սկզբնական վիճակ | Իրադարձություն                             | Վերջնական վիճակ         |
| --------------- | ------------------------------------------ | ----------------------- |
| `not_installed` | `install()`-ը հաջողվում է                  | `stopped`               |
| `stopped`       | կանչվում է `start()`                       | `starting`              |
| `starting`      | առողջության ստուգումը վերադարձնում է 200   | `running`               |
| `starting`      | գործընթացն ավարտվում է մինչև առողջ դառնալը | `error`                 |
| `running`       | կանչվում է `stop()`                        | `stopping` → `stopped`  |
| `running`       | գործընթացն անսպասելի ավարտվում է (< 5 վ)   | `error` (արագ խափանում) |
| `running`       | գործընթացն անսպասելի ավարտվում է (> 5 վ)   | `error`                 |
| `error`         | կանչվում է `start()`                       | `starting`              |
| ցանկացած        | `stop()`՝ `stopping` վիճակում              | գործողություն չկա       |

### Գործողությունների կողպեք

`ServiceSupervisor`-ը կենսացիկլի գործողությունները հաջորդականացնում է ասինխրոն գործողությունների կողպեքի
(`withLock()`) միջոցով։ Նույն վերահսկիչի վրա միաժամանակյա `start()` կանչերը հանգեցնում են ճիշտ
մեկ գործընթացի ստեղծման․ երկրորդ կանչողը սպասում է և վերադարձնում առկա վիճակը։ Սա կանխում է
մրցակցային պայմանները, երբ, օրինակ, ավտոմեկնարկը և UI կոճակը գործարկվում են միաժամանակ։

---

## 4. API տեղեկատու

Բոլոր երթուղիները `/api/services/` հասցեի տակ հանդիսանում են **LOCAL_ONLY** (միայն loopback, խիստ կանոն #17):
Ոչ loopback հարցումները ստանում են `403 LOCAL_ONLY`՝ անկախ նույնականացման թոքենից:

### 4.1 9Router վերջնակետեր (11 երթուղի)

#### `POST /api/services/9router/install`

Տեղադրել 9Router-ը npm-ից: Ստեղծում է `DATA_DIR/services/9router/` թղթապանակը իր սեփական
`package.json` և `node_modules/` ֆայլերով: Չի հակասում OmniRoute-ի սեփական կախվածություններին (deps):

**Հարցման մարմին** (բոլորը կամընտիր).

```json
{ "version": "latest" }
```

| Դաշտ      | Տիպ      | Լռելյայն   | Նկարագրություն                               |
| --------- | -------- | ---------- | -------------------------------------------- |
| `version` | `string` | `"latest"` | npm տարբերակի թեգ կամ semver տեղադրման համար |

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն                                            |
| ---------- | --------------------------------------------------------- |
| `200`      | `{ ok: true, installedVersion: "x.y.z", path: "..." }`    |
| `400`      | Հարցման անվավեր մարմին (Zod վալիդացման ձախողում)          |
| `409`      | Արդեն տեղադրվում է (lock-ը զբաղված է)                     |
| `500`      | npm install-ը ձախողվեց — տես `message`-ը պարզ սխալի համար |

**Նշումներ.** Օգտագործում է `execFile('npm', [...])` — առանց shell-ի, առանց ինտերպոլյացիայի (խիստ կանոն #13):
EACCES սխալները ներկայացվում են որպես հասկանալի հաղորդագրություններ:

---

#### `POST /api/services/9router/start`

Գործարկել 9Router-ը: Գրանցում է supervisor, եթե այն դեռ գրանցված չէ, ապա կանչում է
`supervisor.start()`: Իդեմպոտենտ է, եթե արդեն աշխատում է:

**Հարցման մարմին.** չկա

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն                                        |
| ---------- | ----------------------------------------------------- |
| `200`      | `ServiceStatus` օբյեկտ (տես սխեման ստորև)             |
| `409`      | 9Router-ը տեղադրված չէ (`status: "not_installed"`)    |
| `503`      | Գործարկումը ձախողվեց (պրոցեսի սխալ — տես `lastError`) |

**ServiceStatus սխեմա.**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Բարեհամբույր կերպով կանգնեցնել 9Router-ը: Ուղարկում է SIGTERM, սպասում 15 վայրկյան, ապա SIGKILL, եթե դեռ ակտիվ է:
Իդեմպոտենտ է, եթե արդեն կանգնեցված է:

**Հարցման մարմին.** չկա

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն                     |
| ---------- | ---------------------------------- |
| `200`      | `ServiceStatus` (state: "stopped") |
| `503`      | Կանգնեցումը անսպասելիորեն ձախողվեց |

---

#### `POST /api/services/9router/restart`

Համարժեք է `stop()`-ին, որին հաջորդում է `start()`-ը գործողության lock-ի տակ:

**Հարցման մարմին.** չկա

**Պատասխաններ.** նույնն են, ինչ `start`-ի դեպքում (վերադարձնում է վերջնական `ServiceStatus`):

---

#### `POST /api/services/9router/update`

Թարմացնում է 9Router-ը npm-ի ավելի նոր տարբերակի: Եթե ծառայությունն աշխատում է, այն նախ կանգնեցվում է,
կատարվում է npm install (տեղում տեղադրելով նոր տարբերակը), ապա ծառայությունը վերագործարկվում է:

**Հարցման մարմին** (բոլորը կամընտիր).

```json
{ "version": "latest" }
```

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն                                                  |
| ---------- | --------------------------------------------------------------- |
| `200`      | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`      | Անվավեր մարմին                                                  |
| `500`      | npm update-ը ձախողվեց                                           |

---

#### `POST /api/services/9router/rotate-key`

Ստեղծում է նոր API բանալի 9Router-ի համար, վերջինս պահպանման պահին վերծանում է և վերագործարկում ծառայությունը
(եթե այն աշխատում է), որպեսզի այն վերցնի նոր բանալին իր միջավայրից: Հին բանալին անմիջապես անվավեր է դառնում:

**Հարցման մարմին.** չկա

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն                             |
| ---------- | ------------------------------------------ |
| `200`      | `{ keyRotated: true, restarted: boolean }` |
| `500`      | Ռոտացիան ձախողվեց                          |

**Անվտանգություն.** Նոր բանալին երբեք չի վերադարձվում պատասխանի մեջ (հավատարմագրերի արտահոսք չկա):
Այն պահվում է կոդավորված (AES-256-GCM) `version_manager` աղյուսակում:

---

#### `GET /api/services/9router/status`

Վերադարձնում է համակցված live + DB կարգավիճակը՝ ներառյալ տարբերակի մետատվյալները և API բանալու նախադիտումը:

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն                  |
| ---------- | ------------------------------- |
| `200`      | Տես սխեման ստորև                |
| `500`      | Կարգավիճակի ընթերցումը ձախողվեց |

**Պատասխանի սխեմա.**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Փոխում է ավտոմատ գործարկման (auto-start) դրոշակը: Երբ `enabled: true` է, ծառայությունը ավտոմատ կերպով կմեկնարկի
հաջորդ անգամ OmniRoute-ի բեռնման ժամանակ (եթե ծառայությունը տեղադրված է):

**Հարցման մարմին.**

```json
{ "enabled": true }
```

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն        |
| ---------- | --------------------- |
| `200`      | `{ autoStart: true }` |
| `400`      | Անվավեր մարմին        |

---

#### `GET /api/services/9router/logs`

SSE հոսք 9Router-ի stdout/stderr ring buffer-ի իրական ժամանակի լոգերից:

**Հարցման պարամետրեր.**

| Պարամետր | Տիպ       | Լռելյայն | Նկարագրություն                                                  |
| -------- | --------- | -------- | --------------------------------------------------------------- |
| `tail`   | `integer` | 200      | Քանի պատմական տող ուղարկել սկզբում (առավելագույնը 1000)         |
| `filter` | `string`  | չկա      | Ոչ ռեգիստրազգայուն ենթատողի ֆիլտր (առանց regex — ReDoS-անվտանգ) |

**SSE իրադարձություններ.**

| Իրադարձություն | Տվյալներ    | Նկարագրություն                |
| -------------- | ----------- | ----------------------------- |
| `snapshot`     | `LogLine[]` | Նախնական պատմական վերջնամաս   |
| `log`          | `LogLine`   | Իրական ժամանակի լոգի տող      |
| `heartbeat`    | `{}`        | Keep-alive յուրաքանչյուր 15 վ |

**LogLine սխեմա.**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Պատասխաններ.**

| Կարգավիճակ | Նկարագրություն                                   |
| ---------- | ------------------------------------------------ |
| `200`      | `text/event-stream`                              |
| `400`      | `filter` պարամետրը չափազանց երկար է (> 200 նիշ)  |
| `404`      | Ծառայությունը չի գտնվել (վերահսկիչը գրանցված չէ) |

---

### 4.2 CLIProxyAPI վերջնակետեր (10 երթուղի)

CLIProxyAPI-ն ունի նույն վերջնակետի ձևը, ինչ 9Router-ը, բացի `rotate-key`-ից, գումարած
`accounts`, `provider-expose` և `auto-restart-adopted`: Այն այժմ ստանում է
նվիրված տվյալների հարթակի API բանալի, որը ներարկվում է գործարկման ժամանակ (`needsApiKey: true`
`bootstrap.ts`-ում, օգտագործվում է մոդելի համաժամացման համար); `status`-ը ներառում է ավելի քիչ դաշտեր:

| Մեթոդ  | Ուղի                                | Նկարագրություն                                 |
| ------ | ----------------------------------- | ---------------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | Տեղադրել CLIProxyAPI-ը npm-ից                  |
| `POST` | `/api/services/cliproxy/start`      | Մեկնարկել CLIProxyAPI-ը                        |
| `POST` | `/api/services/cliproxy/stop`       | Դադարեցնել CLIProxyAPI-ը                       |
| `POST` | `/api/services/cliproxy/restart`    | Վերագործարկել CLIProxyAPI-ը                    |
| `POST` | `/api/services/cliproxy/update`     | Թարմացնել ավելի նոր տարբերակին                 |
| `GET`  | `/api/services/cliproxy/status`     | Կենդանի + ՏԲ կարգավիճակ (առանց `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | Միացնել/անջատել ավտոմատ մեկնարկը               |

Ընդհանուր `GET /api/services/{name}/logs` վերջնակետը (տես §4.1) աշխատում է բոլոր
չորս ծառայությունների համար՝ օգտագործելով `[name]` դինամիկ հատվածը:

---

### 4.3 Mux վերջնակետեր (8 երթուղի)

Mux-ն ունի նույն վերջնակետի ձևը, ինչ CLIProxyAPI-ն՝ API-ի մակերեսում `rotate-key` երթուղի չկա
(կրող թոքենը ստեղծվում է նույն կերպ, ինչ 9Router-ի միջոցով
`getOrCreateApiKey("mux")` և ներարկվում է `MUX_SERVER_AUTH_TOKEN` միջավայրի փոփոխականի միջոցով, բայց
դեռևս նվիրված ռոտացիայի վերջնակետ չկա): Mux-ը միայն կյանքի ցիկլով է կառավարվում. ի տարբերություն
9Router-ի, այն չունի Շերտ 4-ի կատարող և երբեք չի գրանցվում որպես երթուղային մատակարար:

| Մեթոդ  | Ուղի                           | Նկարագրություն                      |
| ------ | ------------------------------ | ----------------------------------- |
| `POST` | `/api/services/mux/install`    | Տեղադրել Mux-ը npm-ից (`npm i mux`) |
| `POST` | `/api/services/mux/start`      | Մեկնարկել Mux-ը (`mux server`)      |
| `POST` | `/api/services/mux/stop`       | Դադարեցնել Mux-ը                    |
| `POST` | `/api/services/mux/restart`    | Վերագործարկել Mux-ը                 |
| `POST` | `/api/services/mux/update`     | Թարմացնել ավելի նոր npm տարբերակին  |
| `GET`  | `/api/services/mux/status`     | Կենդանի + ՏԲ կարգավիճակ             |
| `POST` | `/api/services/mux/auto-start` | Միացնել/անջատել ավտոմատ մեկնարկը    |

---

### 4.4 Bifrost վերջնակետեր (8 երթուղի)

Bifrost-ը Go AI-gateway ռելեի բեքենդ է (`@maximhq/bifrost`): Այն օգտագործում է նույն
վերջնակետի ձևը, ինչ CLIProxyAPI-ն (առանց `rotate-key` – Bifrost-ը կառավարում է իր սեփական մատակարարի
բանալիները `config.json`-ում իր `-app-dir`-ի ներքո):

| Մեթոդ  | Ուղի                               | Նկարագրություն                                              |
| ------ | ---------------------------------- | ----------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | Տեղադրել Bifrost-ը npm-ից (`@maximhq/bifrost`)              |
| `POST` | `/api/services/bifrost/start`      | Մեկնարկել Bifrost-ը 8080 պորտում (լռելյայն)                 |
| `POST` | `/api/services/bifrost/stop`       | Դադարեցնել Bifrost-ը                                        |
| `POST` | `/api/services/bifrost/restart`    | Վերագործարկել Bifrost-ը                                     |
| `POST` | `/api/services/bifrost/update`     | Թարմացնել ավելի նոր տարբերակին                              |
| `GET`  | `/api/services/bifrost/status`     | Կենդանի + ՏԲ կարգավիճակ                                     |
| `POST` | `/api/services/bifrost/auto-start` | Միացնել/անջատել ավտոմատ մեկնարկը                            |
| `GET`  | `/api/services/bifrost/logs`       | SSE լոգերի հետևում (միջոցով `[name]/logs` դինամիկ երթուղու) |

**Երթուղային միացում.** Երբ `BIFROST_BASE_URL`-ը սահմանված չէ և վերահսկվող Bifrost
օրինակը աշխատում է, `getBifrostRoutingConfig()`-ը (`routingBackend.ts`-ում) ավտոմատ կերպով
օգտագործում է `http://127.0.0.1:{port}` որպես ռելեի հիմնական URL: Հստակ `BIFROST_BASE_URL` միջավայրի
փոփոխականը միշտ գերակայում է:

---

### 4.5 Dario վերջնակետեր (12 երթուղի)

Նույն կյանքի ցիկլի ձևը, ինչ մյուս ծառայությունները (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`) գումարած թոքենով պաշտպանված OAuth
կառավարման հարթակ `admin/`-ի ներքո. `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (բոլորը `DARIO_ADMIN_TOKEN`-ի հետևում):

### 4.6 open-wa վերջնակետեր (7 երթուղի)

open-wa (`@open-wa/wa-automate`) ղեկավարում է անգլուխ Chromium օրինակը (Puppeteer-ի միջոցով)
WhatsApp Web-ի ավտոմատացման համար: Այն օգտագործում է նույն վերջնակետի ձևը, ինչ Mux-ը (դեռևս `rotate-key` երթուղի չկա): Այն միայն կյանքի ցիկլով է կառավարվում՝ երթուղային թիրախ չէ,
Շերտ 4-ի կատարող/մատակարարի մուտք չկա:

| Method | Path                              | Description                                                           |
| ------ | --------------------------------- | --------------------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | Տեղադրել open-wa-ն npm-ից (`@open-wa/wa-automate`)                    |
| `POST` | `/api/services/openwa/start`      | Միացնել open-wa-ն 8323 պորտում (լռելյայն)                             |
| `POST` | `/api/services/openwa/stop`       | Անջատել open-wa-ն                                                     |
| `POST` | `/api/services/openwa/restart`    | Վերագործարկել open-wa-ն                                               |
| `POST` | `/api/services/openwa/update`     | Թարմացնել ավելի նոր տարբերակի                                         |
| `GET`  | `/api/services/openwa/status`     | Կենդանի + ՏԲ կարգավիճակ                                               |
| `POST` | `/api/services/openwa/auto-start` | Միացնել/անջատել ավտոմատ մեկնարկը                                      |
| `GET`  | `/api/services/openwa/logs`       | SSE լոգերի հետևում (ընդհանուր `[name]/logs` դինամիկ երթուղու միջոցով) |

**API բանալի:** ներարկվում է որպես `WA_KEY` — open-wa-ի ընդհանուր `WA_*` նախածանցով միջավայրի վերագրումը այն քարտեզագրում է `--key`/`-k` CLI ընտրանքի վրա (`dist/cli/setup.js::envArgs()`, ստուգված տեղադրված 4.76.0 փաթեթի դեմ): Նախածանցված է `ow_` երբ ստեղծվում է `generateServiceApiKey()`-ի կողմից: open-wa-ն բանալին հետ է կարդում `key`/`api_key` HTTP հեդերից (ոչ `Authorization: Bearer`); `/api-docs*`-ը բացահայտորեն ազատված է ստուգումից (`setupAuthenticationLayer` `dist/cli/server.js`-ում), ուստի առողջության զոնդին անհրաժեշտ չէ աուտենտիֆիկացիայի հեդեր:

**Զուգակցում:** open-wa-ն ոչ պաշտոնական է և կապ չունի WhatsApp-ի հետ — միացված համարը կրում է արգելքի ռիսկ WhatsApp-ի սեփական ավտոմատացման հայտնաբերումից: Առաջին մեկնարկի ժամանակ զուգակցման QR կոդը տպվում է stdout-ում և հայտնվում է գոյություն ունեցող Logs վահանակի/SSE հոսքի միջոցով — այս ինտեգրման մեջ դեռևս չկա հատուկ QR-պատկերի վերջնակետ:

---

### 4.7 Հակադարձ պրոքսի (9Router կառավարման վահանակի ներդրում)

Կառավարման վահանակը ներդնում է 9Router վեբ ինտերֆեյսը iframe-ի ներսում՝ ներքին հակադարձ պրոքսիի միջոցով հետևյալ հասցեում.

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Այս պրոքսին.

- Հարցումը փոխանցում է `http://127.0.0.1:{port}/{path}` հասցեին (միայն loopback)
- Հեռացնում է մուտքային `cookie` և `authorization` հեդերները (OmniRoute սեսիայի արտահոսք չկա)
- Ներարկում է `Authorization: Bearer {apiKey}` 9Router աուտենտիֆիկացիայի համար
- Հեռացնում է `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` հեդերները պատասխանից
- Վերագրում է HTML պատասխանները՝ ներարկելու `<base href>` և նորմալացնելու բացարձակ ուղիները (`/foo` → `/dashboard/.../embed/foo`)

Ներդրված կառավարման վահանակի համար WebSocket թարմացումները մշակվում են ուղեկցող սերվերի կողմից՝ հատուկ պորտում (տես `src/lib/services/embedWsProxy.ts`):

**Անվտանգություն:** Ներդրված պրոքսիի երթուղիները դասակարգվում են `LOCAL_ONLY_API_PREFIXES`-ի ներքո և հասանելի են միայն loopback-ից: Հարձակվողը, ով ստանում է JWT Cloudflare/Ngrok թունելի միջոցով, չի կարող պրոքսիացնել ներդրված ծառայություններ:

## 5. Անվտանգություն

### LOCAL_ONLY-ի կիրարկում (խիստ կանոն #17)

`/api/services/` և `/dashboard/providers/services/*/embed/` ուղիների ներքո գտնվող բոլոր երթուղիները
`src/server/authz/routeGuard.ts`-ում դասակարգված են որպես LOCAL_ONLY։ Հետադարձ կապի հասցեի ստուգումը
կատարվում է անվերապահորեն՝ նույնականացման ցանկացած ճյուղից առաջ․

```
հարցումը հասնում է
  → isLocalOnlyPath(path)?
      → ոչ հետադարձ կապի հասցե → 403 LOCAL_ONLY (միշտ՝ նույնականացման ստուգումից առաջ)
      → հետադարձ կապի հասցե    → անցում սովորական նույնականացմանը
```

Սա թույլ չի տալիս, որ արտահոսած JWT-ն (օրինակ՝ թունելի միջոցով) գործարկի `npm install`-ը կամ
պրոցեսների ստեղծումը։ Մակարդակների ամբողջական մատրիցը տե՛ս
`docs/security/ROUTE_GUARD_TIERS.md`-ում։

### API բանալու ներարկում

9Router-ը և Mux-ը սեփական HTTP վերջնակետերի համար պահանջում են API բանալի/կրող թոքեն։
OmniRoute-ը՝

1. Ստեղծում է բանալի `crypto.randomBytes(32).toString("base64url")`-ի միջոցով՝
   ծառայությանը հատուկ նախածանցով (`nr_`՝ 9Router-ի համար, `mx_`՝ Mux-ի համար)։
2. Պահպանման վիճակում կոդավորում է այն AES-256-GCM-ի միջոցով (նույն գաղտնագիրը, որն օգտագործվում է մատակարարի հավատարմագրերի համար)։
3. Ապակոդավորում և պրոցեսի ստեղծման պահին ներարկում է այն որպես միջավայրի փոփոխական՝
   `NINEROUTER_API_KEY`՝ 9Router-ի համար, `MUX_SERVER_AUTH_TOKEN`՝ Mux-ի համար (երբեք ոչ որպես CLI
   դրոշ, որպեսզի թոքենը երբեք չհայտնվի `ps`/պրոցեսների ցուցակներում)։
4. Երբեք որևէ HTTP պատասխանում չի վերադարձնում բանալու բաց տեքստը։

CLIProxyAPI-ն ստանում է տվյալների հարթակի հատուկ բանալի, որը ներարկվում է պրոցեսի ստեղծման պահին
(`needsApiKey: true` — օգտագործվում է ադապտերի հետ մոդելների համաժամացման համար)։

### Պաշտպանություն SSRF-ից

Հակադարձ HTTP պրոքսին (`/dashboard/.../embed/[...path]`) կոշտ կերպով կազմաձևված է հարցումները փոխանցելու
միայն `http://127.0.0.1:{port}` հասցեին։ Այն երբեք չի հետևում դեպի ոչ հետադարձ կապի
նպատակակետեր ուղղորդող վերահղումներին։ `ssrf-req-filter` գրադարանն օգտագործվում է՝ մերժելու վերին հոսքի ցանկացած URL, որը
որոշվում է հետադարձ կապի տիրույթից դուրս։

### Կեղևի անվտանգություն (խիստ կանոն #13)

`npm install`-ը կանչվում է `execFile('npm', ['install', pkg, '--prefix', dir])`-ի միջոցով՝
առանց ձևանմուշային տողերի, կեղևի կամ արտաքին ուղիները հրամանի տողի մեջ ներմուծելու։
Կատարման ժամանակի արժեքները (պորտեր, API բանալիներ) փոխանցվում են զավակ պրոցեսի `env` օբյեկտի միջոցով։

### Սխալների մաքրում (խիստ կանոն #12)

`/api/services/*`-ից ստացվող բոլոր սխալի պատասխաններն անցնում են `buildErrorBody()`-ի կամ
`sanitizeErrorMessage()`-ի միջով։ Չմշակված `err.stack`-ը և `err.message`-ը երբեք բառացիորեն չեն վերադարձվում
կանչողին։

---

## 6. Նոր ներկառուցված ծառայության ավելացում

Հետևեք այս 8 քայլերին։ Որպես կանոնական հղում՝ ուսումնասիրեք `src/lib/services/installers/`
և `src/app/api/services/` հասցեներում առկա իրականացումները։

### Քայլ 1 — Ստեղծեք տեղադրիչը

Ստեղծեք `src/lib/services/installers/{name}.ts`՝ որպես մոդել օգտագործելով `ninerouter.ts`-ը․

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // ընտրեք ազատ պորտ

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Օգտագործեք `runNpm(['install', NAME_PACKAGE, '--prefix', dir])`-ը `installers/utils.ts`-ից՝
երբեք մի օգտագործեք `execSync` կամ կեղևային միջարկում։

### Քայլ 2 — Գրանցեք սկզբնական մեկնարկում

`src/lib/services/bootstrap.ts`-ի `SERVICES` զանգվածում ավելացրեք `ServiceEntry`․

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false, եթե API բանալի պետք չէ
}
```

Ընդլայնեք `buildSpawnArgsFactory()`-ն այնպես, որ այն մշակի `cfg.tool === "myservice"` պայմանը։

#### Միացվող մատակարարի հավելման պայմանագիր (Փուլ 1, #7333)

`src/lib/services/providerPlugins/`-ը ներկայացնում է `ServiceProviderPlugin` պայմանագիր, որը
հետնամասի `bootstrap.ts`-ի `ServiceEntry` դաշտերը և `serviceBackends.ts`-ի
հայտագրի ձևանմուշի դաշտերը միավորում է մեկ օբյեկտի մեջ՝ փոխարենը նույն հետնամասի կառուցվածքը
երկու անկապ ֆայլերում առանձին արտահայտելու։ Այս գրության պահին **միայն `9router`-ն է
տեղափոխված**․ `bootstrap.ts`-ը ստանում է իր `SERVICES[]` գրառումը
`getServiceProviderPlugin("9router")`-ից (`src/lib/services/providerPlugins/registry.ts`)՝
գործարկման սխալ առաջացնելով, եթե հավելումը երբևէ բացակայի։ `cliproxy`-ն, `mux`-ը և `bifrost`-ը
մնում են նախկինում գոյություն ունեցող ներկառուցված `SERVICES[]` լիտերալներում՝ առանց փոփոխության։

`open-sse/config/providerPluginManifest.ts`-ը նաև ստացել է հավելող
`createServiceBackendManifestEntry(pluginId, template)` օգնական, որը
`SERVICE_BACKEND_MANIFEST_TEMPLATE` գրառումից կառուցում է պատշաճ ձևավորված
`ProviderPluginManifestEntry`․ այն **դեռ միացված չէ** որևէ գործող հարցման ուղու
(ո՛չ `generateProviderPluginManifestFromRegistry()`-ի,
ո՛չ `/v1/providers/[provider]/models`-ի)։ Դա մնում է որպես հետագա աշխատանք, երբ պայմանագիրը
փորձարկված լինի երկրորդ հետնամասի համար։

Հետաձգված է հետագա PR-ներին և հետևարկվում է #7333 խնդրի ներքո՝ `cliproxyapi`-ի տեղափոխումը նույն
ռեեստրի միջոցով, `mux`/`bifrost`-ի ընդհանրացումը `ServiceBackendPluginId` միության մեջ,
կատարողի երթուղավորման հատուկ մշակման (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) ներառումը հավելման պայմանագրում և
`createServiceBackendManifestEntry()`-ի միացումը գործող հայտագրի/մոդելների կոդային ուղուն։

### Քայլ 3 — Ավելացրեք միգրացիան և ՏԲ-ի սկզբնական տվյալը

Համոզվեք, որ ծառայությունն ունի տող `version_manager`-ում՝
`src/lib/db/migrations/`-ում գտնվող միգրացիայի միջոցով։ Տողը պետք է ունենա հետևյալ տեսքը․

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Քայլ 4 — Ստեղծեք API-ի 7 վերջնակետերը

`src/app/api/services/{name}/`-ի ներքո․

```
_lib.ts            getOrInitSupervisor() օգնական
install/route.ts   POST — կանչում է installer.install()
start/route.ts     POST — կանչում է supervisor.start()
stop/route.ts      POST — կանչում է supervisor.stop()
restart/route.ts   POST — կանչում է supervisor.restart()
update/route.ts    POST — կանչում է installer.update()
status/route.ts    GET  — միավորում է ընթացիկ + ՏԲ կարգավիճակները
auto-start/route.ts POST — փոխարկում է auto_start դրոշը
```

Համօգտագործվող `GET /api/services/[name]/logs` երթուղին արդեն միացված է․ այնտեղ փոփոխություններ
անհրաժեշտ չեն։

Բոլոր սխալների պատասխանները փոխանցեք `createErrorResponse()` / `buildErrorBody()` գործառույթների միջոցով։

### Քայլ 5 — Ավելացնել LOCAL_ONLY_API_PREFIXES-ում

`src/server/authz/routeGuard.ts`-ում ստուգեք, որ `/api/services/`-ն արդեն ներառված է։
Եթե ներմուծում եք նոր նախածանց (օրինակ՝ `/api/tools/`), այն ավելացրեք և՛
`LOCAL_ONLY_API_PREFIXES`-ում, և՛, եթե այն գործընթացներ է գործարկում, `SPAWN_CAPABLE_PREFIXES`-ում։
Ավելացրեք թեստ `tests/unit/authz/routeGuard.test.ts`-ում։

### Քայլ 6 — Ավելացնել UI ներդիրը

Ստեղծեք `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`։
Վերօգտագործեք համօգտագործվող բաղադրիչները․

- `ServiceStatusCard` — ընթացիկ վիճակ + աշխատունակության նշիչ
- `ServiceLifecycleButtons` — Գործարկել / Կանգնեցնել / Վերագործարկել / Թարմացնել
- `ServiceLogsPanel` — SSE մատյանների վերջնամաս (միանում է `/api/services/{name}/logs`-ին)
- `ApiKeyCard` — բանալու ցուցադրում + փոխարինում (եթե `needsApiKey: true`)

Գրանցեք ներդիրը `ServicesPageShell.tsx`-ում։

### Քայլ 7 — Ավելացնել մատակարարի գրառումը (եթե ծառայությունը երթուղավորման թիրախ է)

Եթե ներկառուցված ծառայությունը տրամադրում է OpenAI-ի հետ համատեղելի `/v1/chat/completions` վերջնակետ․

1. `src/shared/constants/providers.ts`-ում ավելացրեք մատակարարի գրառում՝ `isEmbeddedService: true`-ով։
2. Ստեղծեք `open-sse/executors/{name}.ts`, որը ընդլայնում է `BaseExecutor`-ը։ Յուրաքանչյուր հարցման համար
   կրկին որոնեք պորտը և API բանալին (երբեք մի պահեք կառուցչում)։ Վերադարձրեք `503 service_not_running`
   պատասխան, երբ վերահսկիչի վիճակը `"running"` չէ։
3. Գրանցեք մոդելները `open-sse/config/providerRegistry.ts`-ում՝ ծառայության նախածանցով
   (օրինակ՝ `myservice/sub/model`)։ `modelSync.ts`-ը դրանք արդիական կպահի։

### Քայլ 8 — Փաստաթղթավորել և թեստավորել

1. Թարմացրեք `docs/frameworks/EMBEDDED-SERVICES.md`-ը (այս ֆայլը)՝ ծառայությունն ավելացնելով
   §1-ի աղյուսակում, իսկ նոր վերջնակետերը՝ §4-ում։
2. Ավելացրեք միավորային թեստեր `tests/unit/services/`-ում (կենսացիկլ, տեղադրիչ, API-ի կառուցվածք)։
3. Ավելացրեք ինտեգրացիոն թեստ `tests/integration/services/`-ում (`RUN_SERVICES_INT=1`-ի հետևում)։
4. Թարմացրեք `docs/openapi.yaml`-ը՝ ներառելով նոր վերջնակետերը։

---

## 7. Խնդիրների լուծում

### Ծառայությունը չի մեկնարկում

**Ախտանիշներ․** Մեկնարկի կոճակը վերադարձնում է 503, վիճակը մնում է `"error"` կամ `"starting"`։

**Ստուգաթերթ․**

1. Ստուգեք `GET /api/services/{name}/logs`-ը (կամ կառավարման վահանակի Logs վահանակը)։ Փնտրեք
   այնպիսի տողեր, ինչպիսիք են՝ `Error: ENOENT`, `address already in use` կամ `Cannot find module`։
2. Համոզվեք, որ `npm`-ը PATH-ում է․ OmniRoute-ն աշխատեցնող նույն օգտահաշվից գործարկեք `which npm`։
3. Համոզվեք, որ ծառայությունը տեղադրված է․ `installedVersion`-ի առկայությունը ստուգեք
   `GET /api/services/{name}/status`-ում։ Եթե այն `null` է, նախ կատարեք տեղադրումը։
4. Ստուգեք, որ `DATA_DIR/services/{name}/node_modules/`-ը գոյություն ունի և դատարկ չէ։
5. Մաքրված ելքի պատճառը տեսնելու համար ստուգեք կարգավիճակի պատասխանի `lastError` դաշտը։

---

### Սառը մեկնարկը դանդաղ է (`running` վիճակին հասնելու համար պահանջվում է > 10 վ)

**Ախտանիշներ․** Վիճակը երկար ժամանակ մնում է `"starting"`, նախքան `"running"` կամ `"error"` դառնալը։

**Բացատրություն․** 9Router-ի սառը մեկնարկը ներառում է կախվածությունների մեծ ծառերի ներմուծում (DNS,
թունել, MITM մոդուլներ)։ Աշխատունակության ստուգման լռելյայն միջակայքը 2 վ է՝ 3 փորձով, որից հետո
վերահսկիչը հայտարարում է ժամանակի սպառում (սակայն շարունակում է հարցումները)։

**Լուծում․** `healthIntervalMs`-ը և `waitForHealthy`-ի ժամանակի սահմանաչափը
(`healthIntervalMs * 3`) կարգավորվում են `bootstrap.ts`-ում։ Ավելի երկար մեկնարկի
ժամանակ պահանջող ծառայությունների համար `healthIntervalMs`-ը մեծացրեք մինչև 5000, իսկ `stopTimeoutMs`-ը՝ մինչև 30 000։

---

### Պորտի բախում (`EADDRINUSE`)

**Ախտանիշներ․** Մատյաններում ցուցադրվում է `address already in use :::20130`։

**Պատճառներ․**

- Մեկ այլ գործընթաց արդեն օգտագործում է 20130 պորտը։
- 9Router-ի նախորդ գործընթացն ամբողջությամբ չի դադարեցվել (զոմբի PID)։

**Լուծում․**

1. Փոխեք լռելյայն պորտը `.env`-ի `NINEROUTER_PORT` միջավայրի փոփոխականի միջոցով։
2. Գտեք և հարկադիր ավարտեք բախում առաջացնող գործընթացը․ `lsof -ti :20130 | xargs kill -9`։
3. Պորտը յուրաքանչյուր ծառայության համար կարգավորվում է `bootstrap.ts`-ի `port` դաշտի միջոցով։

**Նշում․** 9Router-ը լռելյայն օգտագործում է հենց 20130 պորտը՝
OmniRoute-ի լռելյայն 20128 պորտի հետ բախումից խուսափելու համար։

---

### Տեղադրման ժամանակ թույլտվությունը մերժվում է (EACCES)

**Ախտանիշներ․** Տեղադրումը վերադարձնում է 500, մատյաններում ցուցադրվում է `EACCES` կամ `permission denied`։

**Պատճառներ․**

- OmniRoute-ի գործընթացը գրելու իրավունք չունի `DATA_DIR`-ում կամ դրա ծնող գրացուցակում։
- Աշխատեցվում է ոչ արմատային Docker միջավայրում՝ առանց կապակցված հատորում գրելու հասանելիության։

**Լուծում․**

1. Ստուգեք `DATA_DIR`-ը (լռելյայն՝ `~/.omniroute/`)․ `ls -la ~/.omniroute/`
2. Համոզվեք, որ գրացուցակը պատկանում է OmniRoute-ի գործընթացն աշխատեցնող օգտատիրոջը․ `chown -R $USER ~/.omniroute/`
3. Docker-ում համոզվեք, որ հատորի միացումն ունի կոնտեյների օգտատիրոջ համար ճիշտ թույլտվություններ։

---

### Թարմացումը ձախողվում է (`npm install`-ի ժամանակի սպառում կամ ցանցային սխալ)

**Ախտանիշներ․** Թարմացումը վերադարձնում է 500՝ `InstallError`-ով, իսկ մատյաններում ցուցադրվում է ցանցային ժամանակի սպառում։

**Ստուգաթերթ․**

1. Համոզվեք, որ npm ռեեստրը հասանելի է․ `npm ping`։
2. Ստուգեք կորպորատիվ միջնորդ սերվերի առկայությունը․ `npm config get proxy`, `npm config get https-proxy`։
3. Փորձեք տեղադրումը կատարել ձեռքով․ `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`։
4. Մեկուսացված ցանցում աշխատելու դեպքում նախապես ներբեռնեք tarball-ը և օգտագործեք `npm install /path/to/tarball.tgz`։

---

### Ծառայությունը մեկնարկից անմիջապես հետո ցուցադրում է `"error"` վիճակ (արագ խափանում)

**Ախտանիշներ․** Վիճակը 5 վայրկյանից պակաս ժամանակում `"starting"`-ից անցնում է `"error"`-ի։
`lastError`-ը ցուցադրում է `"Fast crash (exited with code 1)"`։

**Ստուգաթերթ․**

1. Կարդացեք մատյանի վերջին հատվածն ամբողջությամբ․ `GET /api/services/{name}/logs?tail=500`։
2. Տարածված պատճառ է ծառայության կողմից ակնկալվող միջավայրի փոփոխականների բացակայությունը։
3. 9Router-ի համար համոզվեք, որ `NINEROUTER_DISABLE_MITM=true`-ը և
   `NINEROUTER_DISABLE_TUNNEL=true`-ը փոխանցվում են գործարկման միջավայրում (տե՛ս
   `installers/ninerouter.ts`-ի `resolveSpawnArgs`-ը)։

---

## 8. ՀՏՀ

**Հ.՝ Կարո՞ղ եմ ներկառուցված ծառայությունների վերջնակետերը հասանելի դարձնել ոչ loopback հաճախորդների համար։**

Ոչ։ LOCAL_ONLY մակարդակը միտումնավոր է սահմանված (խիստ կանոն #17)։ Այն երթուղիները, որոնք կարող են կանչել
`npm install` կամ գործարկել `node` գործընթացներ, չպետք է հասանելի լինեն ոչ loopback
թրաֆիքից, քանի որ հակառակ դեպքում թունելի (Cloudflare, Ngrok, Tailscale) միջոցով արտահոսած JWT-ն
թույլ կտար կամայական գործընթացներ գործարկել։ Բացառման հնարավորություն
`/api/services/`-ի համար չկա․ ի տարբերություն `/api/mcp/`-ի՝ այն ներառված չէ manage-scope շրջանցման
ցանկում։ Տե՛ս `docs/security/ROUTE_GUARD_TIERS.md`։

---

**Հ.՝ 9Router-ը և CLIProxyAPI-ն հասանելի կլինե՞ն production/cloud տեղակայումներում։**

Այո։ Երկու ծառայություններն էլ հետևում են նույն՝ նախ տեղային օգտագործման մոդելին, ինչ հենց OmniRoute-ը։ Դրանք աշխատում են
նույն մեքենայի վրա և հաղորդակցվում են loopback-ի միջոցով։ Այստեղ «Production»-ը նշանակում է այն VPS-ը
կամ տեղային սերվերը, որտեղ տեղակայված է OmniRoute-ը, այլ ոչ թե հեռավոր ամպային մատակարար։

---

**Հ.՝ Ինչպե՞ս վրիպազերծել supervisor-ը։**

1. Հետևեք SSE մատյանների հոսքին՝ `curl -N http://localhost:20128/api/services/9router/logs`։
2. Ստուգեք OmniRoute-ի pino ելքում կառուցվածքավորված մատյանները՝ զտված
   `service:supervisor` անվանատարածքով։
3. Ստուգեք DB տողը՝ `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`։
4. Օգտագործեք `GET /api/services/9router/status`՝ մեկ կանչով տեսնելու ընթացիկ կենդանի վիճակը, PID-ը, առողջական վիճակը
   և `lastError`-ը։

---

**Հ.՝ Supervisor-ը ցույց է տալիս `health: "degraded"` կամ `health: "unknown"`, սակայն վիճակը `"running"` է։ Արդյո՞ք դա խնդիր է։**

`"degraded"`-ը նշանակում է, որ առողջական վիճակի ստուգումը վերադարձրել է ոչ 200 պատասխան։ `"unknown"`-ը նշանակում է, որ դեռևս ոչ մի
ստուգում չի ավարտվել (առաջին հարցման հետ մրցակցային վիճակ)։ Գործարկման ընթացքում երկուսն էլ ժամանակավոր են։
Եթե `"running"` վիճակին անցնելուց հետո առողջական վիճակը `"degraded"` է մնում ավելի քան `healthIntervalMs * 3` մվ,
ներկառուցված ծառայությունն աշխատում է, սակայն դրա HTTP API-ն չի արձագանքում։ Ստուգեք՝ արդյոք կարգավիճակի պատասխանում
նշված պորտը ճիշտ է, և արդյոք ծառայությունն իրականում
լսում է այդ պորտում։

---

**Հ.՝ Կարո՞ղ եմ փոխել 9Router API բանալին՝ առանց ամբողջական վերագործարկման։**

Ոչ։ API բանալին գործարկման պահին փոխանցվում է 9Router-ին միջավայրի փոփոխականի միջոցով։
Աշխատող գործընթացում միջավայրի փոփոխականները հնարավոր չէ փոխել։ `POST .../rotate-key`-ն
ինքնաշխատ կերպով կանգնեցնում և վերագործարկում է ծառայությունը՝ նոր բանալին կիրառելու համար։ Բանալու փոփոխությունն
ուժի մեջ է մտնում ծառայության `stopTimeoutMs`-ի (լռելյայն՝ 15 վ) և դրա գործարկման
ժամանակի հանրագումարի սահմաններում։

---

**Հ.՝ Ո՞րն է օղակաձև բուֆերի սահմանաչափը, և ի՞նչ է տեղի ունենում, երբ այն լցվում է։**

Յուրաքանչյուր ծառայություն ունի առանձին 5 ՄԲ օղակաձև բուֆեր։ Երբ բուֆերը լցվում է, ամենահին
մատյանային տողերը հեռացվում են՝ նորերի համար տեղ ազատելու նպատակով։ SSE `snapshot` իրադարձությունը վերադարձնում է
`tail` սահմանաչափի շրջանակում ամենավերջին տողերը։ Մատյանները սկավառակում չեն պահպանվում, եթե
DB տողում սահմանված չէ `logsBufferPath`-ը։

---

## Տե՛ս նաև

- `docs/security/ROUTE_GUARD_TIERS.md` — LOCAL_ONLY մակարդակի մանրամասներ
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 Ներկառուցված ծառայությունների մոդուլի համապատասխանեցում
- `docs/architecture/ARCHITECTURE.md` — համակարգային մակարդակի համատեքստ
- `docs/openapi.yaml` — մեքենայաընթեռնելի վերջնակետերի սահմանումներ
- `CLAUDE.md` §«Նոր ներկառուցված ծառայության ավելացում» — արագ հղման ստուգացանկ
