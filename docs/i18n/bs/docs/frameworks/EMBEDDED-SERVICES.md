# Embedded Services (Bosanski)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/EMBEDDED-SERVICES.md) · 🇪🇹 [am](../../../am/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇿 [az](../../../az/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇰 [da](../../../da/docs/frameworks/EMBEDDED-SERVICES.md) · 🇩🇪 [de](../../../de/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇷 [el](../../../el/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇸 [es](../../../es/docs/frameworks/EMBEDDED-SERVICES.md) · 🇪🇪 [et](../../../et/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇱 [he](../../../he/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/EMBEDDED-SERVICES.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇩 [id](../../../id/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇹 [it](../../../it/docs/frameworks/EMBEDDED-SERVICES.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/EMBEDDED-SERVICES.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇭 [km](../../../km/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇲🇲 [my](../../../my/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇴 [no](../../../no/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [or](../../../or/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/EMBEDDED-SERVICES.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/EMBEDDED-SERVICES.md) · 🇱🇰 [si](../../../si/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/EMBEDDED-SERVICES.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/EMBEDDED-SERVICES.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/EMBEDDED-SERVICES.md) · 🇮🇳 [te](../../../te/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇭 [th](../../../th/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/EMBEDDED-SERVICES.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/EMBEDDED-SERVICES.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/EMBEDDED-SERVICES.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/EMBEDDED-SERVICES.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/EMBEDDED-SERVICES.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/EMBEDDED-SERVICES.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/EMBEDDED-SERVICES.md)

---

> **Verzija:** v3.8.44
> **Posljednje ažuriranje:** 2026-09-09
> **Ciljna publika:** Inženjeri koji dodaju, održavaju ili otklanjaju greške u ugrađenim servisima (9Router, CLIProxyAPI, Mux, Bifrost, open-wa).

Ugrađeni servisi su lokalno instalirani prateći procesni alati koje OmniRoute instalira, nadzire i
izlaže kao punopravna odredišta za usmjeravanje. Za razliku od vanjskih pružalaca usluga (kojima se pristupa putem interneta
koristeći API ključeve), ugrađeni servisi pokreću se na istom računaru kao OmniRoute i komuniciraju putem loopback interfejsa.

---

## Sadržaj

1. [Pregled](#1-overview)
2. [Arhitektura — 4 sloja](#2-architecture--4-layers)
3. [Automat životnog ciklusa](#3-lifecycle-state-machine)
4. [API referenca](#4-api-reference)
5. [Sigurnost](#5-security)
6. [Dodavanje novog ugrađenog servisa](#6-adding-a-new-embedded-service)
7. [Otklanjanje poteškoća](#7-troubleshooting)
8. [Česta pitanja](#8-faq)

---

## 1. Pregled

### Zašto ugrađeni servisi?

Ugrađeno je šest servisa:

| Servis          | npm paket                                       | Zadani port | Namjena                                                                                                                                                                                                      |
| --------------- | ----------------------------------------------- | :---------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **9Router**     | `9router`                                       |    20130    | AI usmjerivač koji OmniRoute može koristiti kao podpružaoca. Modeli su izloženi kao `9router/{sub}/{model}`                                                                                                  |
| **CLIProxyAPI** | Binarna datoteka iz GitHub izdanja (`cliproxy`) |    8317     | Lokalni proxy adapter za tokove autentifikacije Anthropic CLI-ja. Omogućava rezervno usmjeravanje kada OAuth tokeni isteknu                                                                                  |
| **Mux**         | `mux` (bezgrafički `mux server`)                |    8322     | Lokalni demon za orkestraciju agenata (coder/mux). Upravlja se samo njegovim životnim ciklusom — nije odredište za usmjeravanje (nema LLM proxyja).                                                          |
| **Bifrost**     | `@maximhq/bifrost`                              |    8080     | Pozadinski Go relejni AI gateway. Kada je pokrenut, automatski ga odabire relejna ruta (`/v1/relay/`)                                                                                                        |
| **Dario**       | `@askalf/dario`                                 |    3456     | Proxy za Claude pretplatu — alternativa/rezervno rješenje za CLIProxyAPI za saobraćaj u formatu Claude Codea; umetnuti ključ postaje `DARIO_ADMIN_TOKEN` koji štiti njegovu OAuth kontrolnu ravan `/admin/*` |
| **open-wa**     | `@open-wa/wa-automate`                          |    8323     | Automatizacija WhatsApp Weba (bezgrafički Chromium putem Puppeteera). Upravlja se samo njegovim životnim ciklusom — nije odredište za usmjeravanje.                                                          |

Svih šest koristi isti model nadzora:

- OmniRoute ih instalira u `DATA_DIR/services/{name}/` (izolirano od vlastitog OmniRoute `package.json` fajla)
- OmniRoute ih pokreće i nadzire kao podređene procese
- OmniRoute ubacuje kratkotrajni API ključ u okruženje podređenog procesa i rotira ga bez prekida rada (gdje je primjenjivo)
- Sve upravljačke rute (`/api/services/*`) su **LOCAL_ONLY** — dostupne samo putem loopback interfejsa (strogo pravilo #17)

### Ključne odluke (iz plana dizajna)

| Odluka                                                   | Vrijednost                                                                                |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| Pristup izvornoj 9Router UI konzoli putem nadzorne ploče | Obrnuti proxy na `/dashboard/providers/services/9router/embed/*`                          |
| Mehanizam instalacije                                    | `npm install {package}` putem `execFile` (bez interpolacije ljuske)                       |
| Način korištenja                                         | Pružalac registriran kao `9router/{sub}/{model}` u mehanizmu za usmjeravanje              |
| Upravljanje API ključevima                               | OmniRoute generira, šifrira pri pohrani (AES-256-GCM) i ubacuje putem varijabli okruženja |
| Lokacija na nadzornoj ploči                              | `/dashboard/providers/services` (tri kartice)                                             |
| Automatsko pokretanje                                    | Prekidač za svaki servis, zadano ISKLJUČENO                                               |

---

## 2. Arhitektura — 4 sloja

```
┌────────────────────────────────────────────────────────────────────┐
│  Sloj 1 — Korisničko sučelje                                      │
│  /dashboard/providers/services  (kartice: CLIProxyAPI | 9Router | Mux)│
│  Dnevnici uživo (SSE), Pokretanje/Zaustavljanje/Ponovno pokretanje/Ažuriranje, Postavke, Instalacija│
│                                                                    │
│  src/app/(dashboard)/dashboard/providers/services/                 │
│    ├── page.tsx               Okvir + usmjeravanje kartica putem ?tab=│
│    ├── tabs/                  CliproxyServiceTab, NinerouterServiceTab,│
│    │                          MuxServiceTab                        │
│    └── components/            ServiceStatusCard, ServiceLifecycleButtons,│
│                               ServiceLogsPanel, ApiKeyCard, ...    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP (Next.js fetch)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Sloj 2 — API (LOCAL_ONLY — samo povratna petlja)                  │
│                                                                    │
│  /api/services/9router/{install|start|stop|restart|update|         │
│                          rotate-key|status|auto-start|logs}        │
│  /api/services/cliproxy/{install|start|stop|restart|update|        │
│                           status|auto-start|logs}                  │
│  /api/services/mux/{install|start|stop|restart|update|             │
│                      status|auto-start|logs}                       │
│  /dashboard/providers/services/9router/embed/[...path]             │
│    (obrnuti HTTP + WebSocket proxy → 9Router upstream)             │
│                                                                    │
│  Kontrola: LOCAL_ONLY_API_PREFIXES uključuje "/api/services/" i    │
│        "/dashboard/providers/services/*/embed/"                    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ pozivi unutar procesa
┌──────────────────────▼─────────────────────────────────────────────┐
│  Sloj 3 — ServiceSupervisor (src/lib/services/)                    │
│                                                                    │
│  ServiceSupervisor.ts   Generički nadzornik (child_process.spawn)  │
│    ├── instalacija: execFile('npm', ['install', pkg, '--prefix'])   │
│    ├── pokretanje:  spawn(node, [entrypoint], {env, cwd})          │
│    ├── api_key:    crypto.randomBytes(32) → env NINEROUTER_API_KEY  │
│    ├── port:       20130 za 9Router (podesivo)                     │
│    ├── dnevnici:   stdio kružni bafer od 5 MB → SSE događaji       │
│    ├── zdravlje:   HTTP GET /health svakih 2–5 s, lijeni oporavak  │
│    └── životni ciklus: SIGTERM 15 s → SIGKILL                      │
│                                                                    │
│  registry.ts        getSupervisor(name) / registerSupervisor()     │
│  bootstrap.ts       Inicijalizira sve SERVICES[] pri pokretanju procesa│
│  apiKey.ts          getOrCreateApiKey(), generateServiceApiKey()   │
│  modelSync.ts       Periodični GET /v1/models → tabela service_models│
│  ringBuffer.ts      Kružni bafer dnevnika (5 MB po servisu)        │
│  healthCheck.ts     Periodična HTTP provjera zdravlja              │
│  installers/        ninerouter.ts, cliproxy.ts, mux.ts, openwa.ts  │
│                      (adapteri instalacijskog programa)            │
└──────────────────────┬─────────────────────────────────────────────┘
                       │ HTTP kompatibilan s OpenAI-jem (povratna petlja)
┌──────────────────────▼─────────────────────────────────────────────┐
│  Sloj 4 — Pružalac usluga / Usmjeravanje                           │
│                                                                    │
│  open-sse/executors/ninerouter.ts                                  │
│    Ponovo traži port i API ključ za svaki zahtjev (bez keširanja). │
│    Uklanja prefiks "9router/" iz ID-a modela prije prosljeđivanja. │
│    Vraća 503 service_not_running ako nadzornik nije u stanju "running".│
│                                                                    │
│  src/shared/constants/providers.ts                                 │
│    Unos za "9router": isEmbeddedService: true                      │
│                                                                    │
│  open-sse/config/providerRegistry.ts                               │
│    Modeli se pohranjuju kao "9router/{sub}/{model}" (s prefiksom). │
│    Sinhronizira ih modelSync.ts svakih 5 min.                      │
│                                                                    │
│  Mux ima upravljanje SAMO životnim ciklusom (slojevi 1–3) — to je  │
│  daemon za orkestraciju agenata, a ne LLM proxy, pa nema izvršitelja│
│  niti unosa pružaoca usluga u sloju 4 i nikada nije cilj usmjeravanja.│
└────────────────────────────────────────────────────────────────────┘
```

### Ključne izvorne datoteke

| Datoteka                                    | Uloga                                                              |
| ------------------------------------------- | ------------------------------------------------------------------ |
| `src/lib/services/ServiceSupervisor.ts`     | Osnovna klasa: životni ciklus, zaključavanje, stanje, kružni bafer |
| `src/lib/services/bootstrap.ts`             | Registracija na nivou procesa i automatsko pokretanje              |
| `src/lib/services/registry.ts`              | Singleton mapa `alat → supervisor`                                 |
| `src/lib/services/apiKey.ts`                | Generisanje ključeva, AES-256-GCM šifriranje podataka u mirovanju  |
| `src/lib/services/modelSync.ts`             | Periodična sinhronizacija modela (5 min) + na zahtjev              |
| `src/lib/services/ringBuffer.ts`            | Kružni bafer zapisnika od 5 MB sa SSE pretplatom                   |
| `src/lib/services/healthCheck.ts`           | HTTP provjera stanja (podesivi interval)                           |
| `src/lib/services/installers/ninerouter.ts` | npm instaliranje/ažuriranje/deinstaliranje za 9Router              |
| `src/lib/services/installers/cliproxy.ts`   | npm instaliranje/ažuriranje/deinstaliranje za CLIProxyAPI          |
| `src/lib/services/installers/mux.ts`        | npm instaliranje/ažuriranje/deinstaliranje za Mux                  |
| `src/lib/services/installers/openwa.ts`     | npm instaliranje/ažuriranje/deinstaliranje za open-wa              |
| `src/app/api/services/9router/_lib.ts`      | Pomoćna funkcija `getOrInitSupervisor()`                           |
| `src/app/api/services/[name]/logs/route.ts` | Zajednička krajnja tačka za SSE zapisnike                          |
| `open-sse/executors/ninerouter.ts`          | Izvršilac pružaoca usluga (sloj 4)                                 |

---

## 3. Automat stanja životnog ciklusa

```
                    install()
  ┌─────────────┐ ──────────► ┌─────────────┐
  │ not_installed│             │   stopped   │◄──────────────────┐
  └─────────────┘             └──────┬──────┘                   │
                                     │ start()                   │
                                     ▼                           │ stop()
                               ┌──────────┐                      │
                               │ starting │                      │
                               └────┬─────┘                     │
        provjera stanja uspješna    │         pad / SIGTERM      │
                               ┌────▼─────┐  (izlaz unutar 5 s) │
                               │ running  │──── pad ─────────────►┤
                               └────┬─────┘                   ┌─▼────┐
                             stop() │                          │error │
                                    ▼                          └──────┘
                               ┌──────────┐
                               │ stopping │
                               └──────────┘
```

Stanja se pohranjuju u DB tabeli `version_manager` (kolona `status`) i preslikavaju
u stanje komponente `ServiceSupervisor` u memoriji. Stanje u memoriji je mjerodavno za
pokrenuti proces; stanje u bazi podataka predstavlja trajnu rezervnu vrijednost pri pokretanju sistema.

### Prijelazi stanja

| Iz              | Događaj                                   | U                      |
| --------------- | ----------------------------------------- | ---------------------- |
| `not_installed` | `install()` uspije                        | `stopped`              |
| `stopped`       | `start()` pozvan                          | `starting`             |
| `starting`      | provjera stanja vrati 200                 | `running`              |
| `starting`      | proces se završi prije nego postane zdrav | `error`                |
| `running`       | `stop()` pozvan                           | `stopping` → `stopped` |
| `running`       | proces se neočekivano završi (< 5 s)      | `error` (brzi pad)     |
| `running`       | proces se neočekivano završi (> 5 s)      | `error`                |
| `error`         | `start()` pozvan                          | `starting`             |
| bilo koje       | `stop()` tokom stanja `stopping`          | bez operacije          |

### Zaključavanje operacija

`ServiceSupervisor` serijalizira operacije životnog ciklusa pomoću asinhronog zaključavanja operacija
(`withLock()`). Istovremeni pozivi `start()` na istom nadzorniku rezultiraju tačno
jednim pokretanjem procesa; drugi pozivalac čeka i vraća postojeći status. Time se sprečavaju
uslovi utrke kada se, naprimjer, automatsko pokretanje i dugme u korisničkom interfejsu aktiviraju istovremeno.

---

## 4. API referenca

Sve rute pod `/api/services/` su **LOCAL_ONLY** (samo povratna petlja, strogo pravilo #17).
Zahtjevi koji ne dolaze preko povratne petlje dobijaju `403 LOCAL_ONLY`, bez obzira na token za autentifikaciju.

### 4.1 Krajnje tačke za 9Router (11 ruta)

#### `POST /api/services/9router/install`

Instalira 9Router iz npm-a. Kreira `DATA_DIR/services/9router/` s vlastitim
`package.json` i `node_modules/`. Ne dolazi u sukob s vlastitim zavisnostima OmniRoutea.

**Tijelo zahtjeva** (sve je opcionalno):

```json
{ "version": "latest" }
```

| Polje     | Tip      | Zadano     | Opis                                         |
| --------- | -------- | ---------- | -------------------------------------------- |
| `version` | `string` | `"latest"` | npm oznaka verzije ili semver za instalaciju |

**Odgovori:**

| Status | Opis                                                                              |
| ------ | --------------------------------------------------------------------------------- |
| `200`  | `{ ok: true, installedVersion: "x.y.z", path: "..." }`                            |
| `400`  | Nevažeće tijelo zahtjeva (neuspješna Zod validacija)                              |
| `409`  | Instalacija je već u toku (zaključavanje je aktivno)                              |
| `500`  | npm instalacija nije uspjela — pogledajte `message` za razumljivu poruku o grešci |

**Napomene:** Koristi `execFile('npm', [...])` — bez ljuske i bez interpolacije (strogo pravilo #13).
EACCES greške prikazuju se kao razumljive poruke.

---

#### `POST /api/services/9router/start`

Pokreće 9Router. Registruje nadzornika ako već nije registrovan, a zatim poziva
`supervisor.start()`. Idempotentno ako je već pokrenut.

**Tijelo zahtjeva:** nema

**Odgovori:**

| Status | Opis                                                              |
| ------ | ----------------------------------------------------------------- |
| `200`  | Objekt `ServiceStatus` (pogledajte shemu ispod)                   |
| `409`  | 9Router nije instaliran (`status: "not_installed"`)               |
| `503`  | Pokretanje nije uspjelo (greška procesa — pogledajte `lastError`) |

**Shema ServiceStatus:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null
}
```

---

#### `POST /api/services/9router/stop`

Kontrolisano zaustavlja 9Router. Šalje SIGTERM, čeka 15 s, a zatim šalje SIGKILL ako je proces još aktivan.
Idempotentno ako je već zaustavljen.

**Tijelo zahtjeva:** nema

**Odgovori:**

| Status | Opis                                   |
| ------ | -------------------------------------- |
| `200`  | `ServiceStatus` (stanje: "stopped")    |
| `503`  | Zaustavljanje je neočekivano neuspjelo |

---

#### `POST /api/services/9router/restart`

Ekvivalentno pozivu `stop()`, a zatim `start()` unutar zaključavanja operacije.

**Tijelo zahtjeva:** nema

**Odgovori:** isti kao za `start` (vraća konačni `ServiceStatus`).

---

#### `POST /api/services/9router/update`

Ažurira 9Router na noviju npm verziju. Ako je servis pokrenut, prvo se
zaustavlja, izvršava se npm instalacija (novija verzija se instalira na isto mjesto), a zatim se
servis ponovo pokreće.

**Tijelo zahtjeva** (sve je opcionalno):

```json
{ "version": "latest" }
```

**Odgovori:**

| Status | Opis                                                            |
| ------ | --------------------------------------------------------------- |
| `200`  | `{ ok: true, previousVersion: "...", installedVersion: "..." }` |
| `400`  | Nevažeće tijelo                                                 |
| `500`  | npm ažuriranje nije uspjelo                                     |

---

#### `POST /api/services/9router/rotate-key`

Generiše novi API ključ za 9Router, šifrira ga pri pohrani i ponovo pokreće servis
(ako je pokrenut) kako bi preuzeo novi ključ iz svog okruženja. Stari ključ se
odmah poništava.

**Tijelo zahtjeva:** nema

**Odgovori:**

| Status | Opis                                       |
| ------ | ------------------------------------------ |
| `200`  | `{ keyRotated: true, restarted: boolean }` |
| `500`  | Rotacija nije uspjela                      |

**Sigurnost:** Novi ključ se nikada ne vraća u odgovoru (nema curenja vjerodajnica).
Pohranjuje se šifriran (AES-256-GCM) u tabeli `version_manager`.

---

#### `GET /api/services/9router/status`

Vraća kombinovani status uživo i status iz baze podataka, uključujući metapodatke o verziji i pregled API ključa.

**Odgovori:**

| Status | Opis                         |
| ------ | ---------------------------- |
| `200`  | Pogledajte shemu u nastavku  |
| `500`  | Čitanje statusa nije uspjelo |

**Shema odgovora:**

```json
{
  "tool": "9router",
  "state": "running",
  "pid": 12345,
  "port": 20130,
  "health": "healthy",
  "startedAt": "2026-05-25T10:00:00.000Z",
  "lastError": null,
  "installedVersion": "1.2.3",
  "latestVersion": "1.2.4",
  "updateAvailable": true,
  "apiKeyMasked": "nr_****abcd",
  "autoStart": false,
  "providerExpose": false
}
```

---

#### `POST /api/services/9router/auto-start`

Uključuje ili isključuje zastavicu automatskog pokretanja. Kada je `enabled: true`, servis se automatski
pokreće sljedeći put kada se OmniRoute pokrene (ako je servis instaliran).

**Tijelo zahtjeva:**

```json
{ "enabled": true }
```

**Odgovori:**

| Status | Opis                  |
| ------ | --------------------- |
| `200`  | `{ autoStart: true }` |
| `400`  | Neispravno tijelo     |

---

#### `GET /api/services/9router/logs`

SSE tok zapisa uživo iz prstenastog bafera za stdout/stderr servisa 9Router.

**Parametri upita:**

| Parametar | Tip       | Zadano | Opis                                                                                |
| --------- | --------- | ------ | ----------------------------------------------------------------------------------- |
| `tail`    | `integer` | 200    | Broj historijskih redova koji se prvo šalju (maksimalno 1000)                       |
| `filter`  | `string`  | nema   | Filter podniza neosjetljiv na velika i mala slova (bez regexa — siguran od ReDoS-a) |

**SSE događaji:**

| Događaj     | Podaci      | Opis                               |
| ----------- | ----------- | ---------------------------------- |
| `snapshot`  | `LogLine[]` | Početni historijski završni redovi |
| `log`       | `LogLine`   | Red zapisa uživo                   |
| `heartbeat` | `{}`        | Održavanje veze svakih 15 s        |

**Shema LogLine:**

```json
{
  "ts": 1716633600000,
  "stream": "stdout",
  "line": "[9router] Listening on :20130"
}
```

**Odgovori:**

| Status | Opis                                               |
| ------ | -------------------------------------------------- |
| `200`  | `text/event-stream`                                |
| `400`  | Parametar `filter` je predug (> 200 znakova)       |
| `404`  | Servis nije pronađen (supervizor nije registrovan) |

---

### 4.2 Krajnje tačke CLIProxyAPI-ja (10 ruta)

CLIProxyAPI ima isti oblik krajnjih tačaka kao 9Router, bez `rotate-key`, uz
`accounts`, `provider-expose` i `auto-restart-adopted`. Sada prima
namjenski API ključ podatkovne ravni koji se umeće pri pokretanju (`needsApiKey: true` u
`bootstrap.ts`, koristi se za sinhronizaciju modela); `status` sadrži manje polja.

| Metoda | Putanja                             | Opis                                                        |
| ------ | ----------------------------------- | ----------------------------------------------------------- |
| `POST` | `/api/services/cliproxy/install`    | Instalira CLIProxyAPI iz npm-a                              |
| `POST` | `/api/services/cliproxy/start`      | Pokreće CLIProxyAPI                                         |
| `POST` | `/api/services/cliproxy/stop`       | Zaustavlja CLIProxyAPI                                      |
| `POST` | `/api/services/cliproxy/restart`    | Ponovo pokreće CLIProxyAPI                                  |
| `POST` | `/api/services/cliproxy/update`     | Ažurira na noviju verziju                                   |
| `GET`  | `/api/services/cliproxy/status`     | Status uživo + status iz baze podataka (bez `apiKeyMasked`) |
| `POST` | `/api/services/cliproxy/auto-start` | Uključuje ili isključuje automatsko pokretanje              |

Zajednička krajnja tačka `GET /api/services/{name}/logs` (pogledajte §4.1) radi za sva
četiri servisa koristeći dinamički segment `[name]`.

---

### 4.3 Krajnje tačke Muxa (8 ruta)

Mux ima isti oblik krajnjih tačaka kao CLIProxyAPI — bez rute `rotate-key` u API
površini (token nosioca generiše se na isti način kao i token 9Routera putem
`getOrCreateApiKey("mux")` i umeće se putem varijable okruženja `MUX_SERVER_AUTH_TOKEN`, ali
još nema namjenske krajnje tačke za rotaciju). Muxom se upravlja samo kroz životni ciklus: za razliku od
9Routera, nema izvršitelj sloja 4 i nikada se ne registruje kao pružalac usmjeravanja.

| Metoda | Putanja                        | Opis                                           |
| ------ | ------------------------------ | ---------------------------------------------- |
| `POST` | `/api/services/mux/install`    | Instalira Mux iz npm-a (`npm i mux`)           |
| `POST` | `/api/services/mux/start`      | Pokreće Mux (`mux server`)                     |
| `POST` | `/api/services/mux/stop`       | Zaustavlja Mux                                 |
| `POST` | `/api/services/mux/restart`    | Ponovo pokreće Mux                             |
| `POST` | `/api/services/mux/update`     | Ažurira na noviju npm verziju                  |
| `GET`  | `/api/services/mux/status`     | Status uživo + status iz baze podataka         |
| `POST` | `/api/services/mux/auto-start` | Uključuje ili isključuje automatsko pokretanje |

---

### 4.4 Krajnje tačke Bifrosta (8 ruta)

Bifrost je pozadinski relejni AI pristupnik napisan u Gou (`@maximhq/bifrost`). Koristi isti
oblik krajnjih tačaka kao CLIProxyAPI (bez `rotate-key` — Bifrost upravlja vlastitim ključevima
pružalaca u datoteci `config.json` unutar svog direktorija `-app-dir`).

| Metoda | Putanja                            | Opis                                                                   |
| ------ | ---------------------------------- | ---------------------------------------------------------------------- |
| `POST` | `/api/services/bifrost/install`    | Instalira Bifrost iz npm-a (`@maximhq/bifrost`)                        |
| `POST` | `/api/services/bifrost/start`      | Pokreće Bifrost na portu 8080 (zadano)                                 |
| `POST` | `/api/services/bifrost/stop`       | Zaustavlja Bifrost                                                     |
| `POST` | `/api/services/bifrost/restart`    | Ponovo pokreće Bifrost                                                 |
| `POST` | `/api/services/bifrost/update`     | Ažurira na noviju verziju                                              |
| `GET`  | `/api/services/bifrost/status`     | Status uživo + status baze podataka                                    |
| `POST` | `/api/services/bifrost/auto-start` | Uključuje/isključuje automatsko pokretanje                             |
| `GET`  | `/api/services/bifrost/logs`       | SSE praćenje zapisnika (putem zajedničke dinamičke rute `[name]/logs`) |

**Konfiguracija usmjeravanja:** Kada `BIFROST_BASE_URL` nije postavljen i nadzirana instanca
Bifrosta je pokrenuta, `getBifrostRoutingConfig()` (u `routingBackend.ts`) automatski
koristi `http://127.0.0.1:{port}` kao osnovni URL releja. Eksplicitno postavljena varijabla okruženja
`BIFROST_BASE_URL` uvijek ima prednost.

---

### 4.5 Dario krajnje tačke (12 ruta)

Isti oblik životnog ciklusa kao kod drugih servisa (`install`, `start`, `stop`, `restart`,
`update`, `status`, `auto-start`, `auto-restart-adopted`), uz OAuth
kontrolni sloj zaštićen tokenom pod `admin/`: `admin/accounts`, `admin/import-from-omniroute`,
`admin/login-start`, `admin/login-complete` (sve zaštićeno pomoću `DARIO_ADMIN_TOKEN`).

### 4.6 open-wa krajnje tačke (7 ruta)

open-wa (`@open-wa/wa-automate`) upravlja Chromium instancom bez grafičkog interfejsa (putem
Puppeteera) radi automatizacije WhatsApp Weba. Koristi isti oblik krajnjih tačaka kao Mux (još nema
rutu `rotate-key`). Njime se upravlja samo kroz životni ciklus — nije cilj usmjeravanja
i nema izvršnu/provider stavku sloja 4.

| Metoda | Putanja                           | Opis                                                                   |
| ------ | --------------------------------- | ---------------------------------------------------------------------- |
| `POST` | `/api/services/openwa/install`    | Instalira open-wa iz npm-a (`@open-wa/wa-automate`)                    |
| `POST` | `/api/services/openwa/start`      | Pokreće open-wa na portu 8323 (zadano)                                 |
| `POST` | `/api/services/openwa/stop`       | Zaustavlja open-wa                                                     |
| `POST` | `/api/services/openwa/restart`    | Ponovo pokreće open-wa                                                 |
| `POST` | `/api/services/openwa/update`     | Ažurira na noviju verziju                                              |
| `GET`  | `/api/services/openwa/status`     | Status uživo + status baze podataka                                    |
| `POST` | `/api/services/openwa/auto-start` | Uključuje/isključuje automatsko pokretanje                             |
| `GET`  | `/api/services/openwa/logs`       | SSE praćenje zapisnika (putem zajedničke dinamičke rute `[name]/logs`) |

**API ključ:** umeće se kao `WA_KEY` — open-wa generičko nadjačavanje putem varijabli okruženja
s prefiksom `WA_*` mapira ga na CLI opciju `--key`/`-k`
(`dist/cli/setup.js::envArgs()`, potvrđeno u odnosu na instalirani paket verzije 4.76.0).
Pri generiranju pomoću `generateServiceApiKey()` dodaje se prefiks `ow_`. open-wa
očitava ključ iz HTTP zaglavlja `key`/`api_key` (ne iz `Authorization:
Bearer`); `/api-docs*` je izričito izuzet iz provjere
(`setupAuthenticationLayer` u `dist/cli/server.js`), pa zdravstvena provjera
ne zahtijeva zaglavlje za autentifikaciju.

**Uparivanje:** open-wa je nezvaničan i nije povezan s WhatsAppom —
za povezani broj postoji rizik od zabrane zbog WhatsAppovog vlastitog otkrivanja automatizacije.
Pri prvom pokretanju QR kod za uparivanje ispisuje se na standardni izlaz i prikazuje putem
postojećeg panela zapisnika/SSE toka — u ovoj integraciji još nema namjenske krajnje tačke
za QR sliku.

---

### 4.7 Obrnuti proxy (ugrađeni 9Router dashboard)

Dashboard ugrađuje 9Router web interfejs u iframe putem internog obrnutog
proxyja na:

```
GET|POST|... /dashboard/providers/services/9router/embed/[...path]
```

Ovaj proxy:

- Prosljeđuje zahtjev na `http://127.0.0.1:{port}/{path}` (samo povratna petlja)
- Uklanja dolazna zaglavlja `cookie` i `authorization` (bez curenja OmniRoute sesije)
- Umeće `Authorization: Bearer {apiKey}` za 9Router autentifikaciju
- Uklanja `set-cookie`, `content-security-policy`, `x-frame-options`, `cross-origin-*` iz odgovora
- Prepravlja HTML odgovore radi umetanja `<base href>` i normalizacije apsolutnih putanja (`/foo` → `/dashboard/.../embed/foo`)

WebSocket nadogradnje za ugrađeni dashboard obrađuje prateći server na
namjenskom portu (pogledajte `src/lib/services/embedWsProxy.ts`).

**Sigurnost:** Rute proxyja za ugrađivanje klasificirane su pod `LOCAL_ONLY_API_PREFIXES`
i dostupne su samo putem povratne petlje. Napadač koji dobije JWT putem
Cloudflare/Ngrok tunela ne može koristiti proxy za pristup ugrađenim servisima.

---

## 5. Sigurnost

### Provođenje LOCAL_ONLY pravila (strogo pravilo #17)

Sve rute pod `/api/services/` i `/dashboard/providers/services/*/embed/`
klasificirane su kao LOCAL_ONLY u `src/server/authz/routeGuard.ts`. Provjera povratne petlje
izvršava se bezuslovno prije bilo koje grane autentifikacije:

```
zahtjev stiže
  → isLocalOnlyPath(path)?
      → nije povratna petlja → 403 LOCAL_ONLY (uvijek, prije provjere autentifikacije)
      → povratna petlja      → nastavi na uobičajenu autentifikaciju
```

Ovo sprečava da JWT koji je procurio (npr. putem tunela) pokrene `npm install` ili
stvaranje procesa. Pogledajte `docs/security/ROUTE_GUARD_TIERS.md` za potpunu matricu
nivoa.

### Umetanje API ključa

9Router i Mux zahtijevaju API ključ/bearer token za vlastite HTTP krajnje tačke.
OmniRoute:

1. Generira ključ putem `crypto.randomBytes(32).toString("base64url")` s
   prefiksom specifičnim za servis (`nr_` za 9Router, `mx_` za Mux).
2. Šifrira ga u mirovanju pomoću AES-256-GCM (iste šifre koja se koristi za pristupne podatke pružatelja).
3. Dešifrira ga i umeće kao varijablu okruženja prilikom pokretanja —
   `NINEROUTER_API_KEY` za 9Router, `MUX_SERVER_AUTH_TOKEN` za Mux (nikada kao CLI
   opciju, tako da se token nikada ne pojavljuje u `ps`/listama procesa).
4. Nikada ne vraća ključ u obliku običnog teksta ni u jednom HTTP odgovoru.

CLIProxyAPI prima namjenski ključ podatkovne ravni koji se umeće prilikom pokretanja
(`needsApiKey: true` — koristi se za sinhronizaciju modela s adapterom).

### Zaštita od SSRF-a

Obrnuti HTTP proxy (`/dashboard/.../embed/[...path]`) čvrsto je konfiguriran da prosljeđuje
samo na `http://127.0.0.1:{port}`. Nikada ne prati preusmjerenja na odredišta
izvan povratne petlje. Biblioteka `ssrf-req-filter` koristi se za odbijanje svakog nadređenog URL-a koji
se razrješava izvan raspona povratne petlje.

### Sigurnost ljuske (strogo pravilo #13)

`npm install` se poziva putem `execFile('npm', ['install', pkg, '--prefix', dir])` —
bez predložaka literala, bez ljuske i bez interpolacije vanjskih putanja u komandni
niz. Vrijednosti tokom izvođenja (portovi, API ključevi) prosljeđuju se putem objekta `env` podređenog procesa.

### Sanitizacija grešaka (strogo pravilo #12)

Svi odgovori o greškama iz `/api/services/*` prolaze kroz `buildErrorBody()` ili
`sanitizeErrorMessage()`. Neobrađeni `err.stack` i `err.message` nikada se ne vraćaju
pozivatelju u izvornom obliku.

---

## 6. Dodavanje novog ugrađenog servisa

Slijedite ovih 8 koraka. Pročitajte postojeće implementacije u `src/lib/services/installers/`
i `src/app/api/services/` kao mjerodavnu referencu.

### Korak 1 — Kreirajte instalacijski program

Kreirajte `src/lib/services/installers/{name}.ts` po uzoru na `ninerouter.ts`:

```typescript
export const NAME_PACKAGE = "your-npm-package";
export const NAME_DEFAULT_PORT = 20132; // odaberite slobodan port

export async function install(version = "latest"): Promise<InstallResult> { ... }
export async function update(version = "latest"): Promise<InstallResult> { ... }
export async function uninstall(): Promise<void> { ... }
export function resolveSpawnArgs(apiKey: string, port: number): SpawnArgs { ... }
export async function getInstalledVersion(): Promise<string | null> { ... }
export async function getLatestVersion(): Promise<string | null> { ... }
```

Koristite `runNpm(['install', NAME_PACKAGE, '--prefix', dir])` iz `installers/utils.ts`
— nikada `execSync` ili interpolaciju ljuske.

### Korak 2 — Registrirajte u pokretanju sistema

Dodajte `ServiceEntry` u niz `SERVICES` u `src/lib/services/bootstrap.ts`:

```typescript
{
  tool: "myservice",
  port: NAME_DEFAULT_PORT,
  healthPath: "/health",
  healthIntervalMs: 5_000,
  stopTimeoutMs: 15_000,
  logsBufferBytes: 5_242_880,
  needsApiKey: true, // false ako API ključ nije potreban
}
```

Proširite `buildSpawnArgsFactory()` da obrađuje `cfg.tool === "myservice"`.

#### Ugovor proširivog dodatka pružatelja (Faza 1, #7333)

`src/lib/services/providerPlugins/` uvodi ugovor `ServiceProviderPlugin` koji
objedinjuje polja `ServiceEntry` iz `bootstrap.ts` pozadinskog sistema i polja predloška manifesta iz
`serviceBackends.ts` u jedan objekt, umjesto da se oblik istog pozadinskog sistema
zasebno izražava u dvije nepovezane datoteke. U trenutku pisanja **samo je `9router`
migriran** — `bootstrap.ts` izvodi svoj unos u `SERVICES[]` iz
`getServiceProviderPlugin("9router")` (`src/lib/services/providerPlugins/registry.ts`),
uz izbacivanje greške pri pokretanju ako dodatak ikada nedostaje. `cliproxy`, `mux` i `bifrost`
ostaju nepromijenjeni na prethodno postojećim ugrađenim literalima `SERVICES[]`.

`open-sse/config/providerPluginManifest.ts` također je dobio dodatni pomoćnik
`createServiceBackendManifestEntry(pluginId, template)` koji gradi ispravno oblikovan
`ProviderPluginManifestEntry` iz unosa `SERVICE_BACKEND_MANIFEST_TEMPLATE` — on
**još nije** povezan ni s jednom aktivnom putanjom zahtjeva (ni s `generateProviderPluginManifestFromRegistry()`
ni s `/v1/providers/[provider]/models`); to ostaje naknadni zadatak nakon što ugovor bude
potvrđen za drugi pozadinski sistem.

Odgođeno za naknadne PR-ove, prati se pod problemom #7333: migracija `cliproxyapi` kroz isti
registar, generaliziranje `mux`/`bifrost` u uniju `ServiceBackendPluginId`,
uključivanje posebne obrade usmjeravanja izvršitelja (`open-sse/executors/index.ts`,
`open-sse/handlers/chatCore/executorProxy.ts`) u ugovor dodatka i povezivanje
`createServiceBackendManifestEntry()` s aktivnom putanjom koda za manifest/modele.

### Korak 3 — Dodajte migraciju i početne DB podatke

Osigurajte da servis ima red u `version_manager` putem migracije u
`src/lib/db/migrations/`. Red treba sadržavati:

```sql
INSERT OR IGNORE INTO version_manager (tool, status, auto_start, provider_expose)
VALUES ('myservice', 'not_installed', 0, 0);
```

### Korak 4 — Kreirajte 7 API krajnjih tačaka

Pod `src/app/api/services/{name}/`:

```
_lib.ts            pomoćnik getOrInitSupervisor()
install/route.ts   POST — poziva installer.install()
start/route.ts     POST — poziva supervisor.start()
stop/route.ts      POST — poziva supervisor.stop()
restart/route.ts   POST — poziva supervisor.restart()
update/route.ts    POST — poziva installer.update()
status/route.ts    GET  — spaja trenutno stanje + stanje iz DB-a
auto-start/route.ts POST — mijenja zastavicu auto_start
```

Dijeljena ruta `GET /api/services/[name]/logs` već je povezana — tamo nisu
potrebne nikakve izmjene.

Delegirajte sve odgovore o greškama kroz `createErrorResponse()` / `buildErrorBody()`.

### Korak 5 — Dodajte u LOCAL_ONLY_API_PREFIXES

U `src/server/authz/routeGuard.ts` provjerite je li `/api/services/` već naveden.
Ako uvedete novi prefiks (npr. `/api/tools/`), dodajte ga i u
`LOCAL_ONLY_API_PREFIXES` te, ako pokreće procese, u `SPAWN_CAPABLE_PREFIXES`.
Dodajte test u `tests/unit/authz/routeGuard.test.ts`.

### Korak 6 — Dodajte karticu korisničkog interfejsa

Kreirajte `src/app/(dashboard)/dashboard/providers/services/tabs/{Name}ServiceTab.tsx`.
Ponovno koristite dijeljene komponente:

- `ServiceStatusCard` — stanje uživo + oznaka ispravnosti
- `ServiceLifecycleButtons` — Pokreni / Zaustavi / Ponovo pokreni / Ažuriraj
- `ServiceLogsPanel` — SSE praćenje zapisa (povezuje se na `/api/services/{name}/logs`)
- `ApiKeyCard` — prikazivanje + rotiranje ključa (ako je `needsApiKey: true`)

Registrujte karticu u `ServicesPageShell.tsx`.

### Korak 7 — Dodajte unos pružaoca usluge (ako je servis cilj usmjeravanja)

Ako ugrađeni servis izlaže krajnju tačku `/v1/chat/completions` kompatibilnu s OpenAI-jem:

1. Dodajte unos pružaoca usluge u `src/shared/constants/providers.ts` s `isEmbeddedService: true`.
2. Kreirajte `open-sse/executors/{name}.ts` koji proširuje `BaseExecutor`. Za svaki zahtjev ponovo
   pronađite port i API ključ (nikada ih ne keširajte u konstruktoru). Vratite odgovor
   `503 service_not_running` kada stanje nadzornika nije `"running"`.
3. Registrujte modele u `open-sse/config/providerRegistry.ts` s prefiksom servisa
   (npr. `myservice/sub/model`). `modelSync.ts` će ih održavati ažurnim.

### Korak 8 — Dokumentujte i testirajte

1. Ažurirajte `docs/frameworks/EMBEDDED-SERVICES.md` (ovu datoteku) — dodajte servis u
   tabelu u §1 i sve nove krajnje tačke u §4.
2. Dodajte jedinične testove u `tests/unit/services/` (životni ciklus, instalacijski program, oblik API-ja).
3. Dodajte integracijski test u `tests/integration/services/` (iza `RUN_SERVICES_INT=1`).
4. Ažurirajte `docs/openapi.yaml` novim krajnjim tačkama.

---

## 7. Rješavanje problema

### Servis se ne pokreće

**Simptomi:** Dugme za pokretanje vraća 503, stanje ostaje `"error"` ili `"starting"`.

**Kontrolna lista:**

1. Provjerite `GET /api/services/{name}/logs` (ili panel Logs na nadzornoj ploči). Potražite
   redove poput `Error: ENOENT`, `address already in use` ili `Cannot find module`.
2. Provjerite je li `npm` u PATH-u: pokrenite `which npm` s istog korisničkog računa pod kojim je pokrenut OmniRoute.
3. Provjerite je li servis instaliran: provjerite `GET /api/services/{name}/status` i
   polje `installedVersion`. Ako je `null`, prvo pokrenite instalaciju.
4. Provjerite postoji li `DATA_DIR/services/{name}/node_modules/` i da nije prazan.
5. Provjerite polje `lastError` u odgovoru statusa radi sanitiziranog razloga završetka.

---

### Hladno pokretanje je sporo (> 10 s do dostizanja stanja `running`)

**Simptomi:** Stanje dugo ostaje `"starting"` prije prelaska u `"running"` ili `"error"`.

**Objašnjenje:** Hladno pokretanje 9Routera uključuje uvoz velikih stabala zavisnosti (DNS,
tunelski i MITM moduli). Zadani interval provjere zdravlja je 2 s, uz 3 pokušaja prije nego što
nadzornik proglasi istek vremena (ali nastavlja s provjerama).

**Rješenje:** `healthIntervalMs` i vremensko ograničenje za `waitForHealthy`
(`healthIntervalMs * 3`) mogu se konfigurirati u `bootstrap.ts`. Za servise kojima je potrebno više
vremena za pokretanje povećajte `healthIntervalMs` na 5000 i `stopTimeoutMs` na 30 000.

---

### Sukob porta (`EADDRINUSE`)

**Simptomi:** Zapisi prikazuju `address already in use :::20130`.

**Uzroci:**

- Drugi proces već koristi port 20130.
- Prethodni proces 9Routera nije u potpunosti zaustavljen (zombi PID).

**Rješenje:**

1. Promijenite zadani port putem varijable okruženja `NINEROUTER_PORT` u `.env`.
2. Pronađite i prekinite konfliktni proces: `lsof -ti :20130 | xargs kill -9`.
3. Port se može konfigurirati zasebno za svaki servis u `bootstrap.ts` putem polja `port`.

**Napomena:** 9Router zadano koristi port 20130 kako bi se izbjegao sukob sa
zadanim portom 20128 za OmniRoute.

---

### Odbijena dozvola (EACCES) prilikom instalacije

**Simptomi:** Instalacija vraća 500, a zapisi prikazuju `EACCES` ili `permission denied`.

**Uzroci:**

- OmniRoute proces nema dozvolu za pisanje u `DATA_DIR` ili njegov nadređeni direktorij.
- Pokretanje unutar Docker okruženja bez root ovlasti i bez pristupa za pisanje na mapirani volumen.

**Rješenje:**

1. Provjerite `DATA_DIR` (zadano: `~/.omniroute/`): `ls -la ~/.omniroute/`
2. Osigurajte da je korisnik OmniRoute procesa vlasnik direktorija: `chown -R $USER ~/.omniroute/`
3. U Dockeru osigurajte da montirani volumen ima odgovarajuće dozvole za korisnika kontejnera.

---

### Ažuriranje ne uspijeva (istek vremena za `npm install` ili mrežna greška)

**Simptomi:** Ažuriranje vraća 500 uz `InstallError`, a zapisi prikazuju istek vremena mrežne veze.

**Kontrolna lista:**

1. Potvrdite da je npm registar dostupan: `npm ping`.
2. Provjerite postoji li korporativni proxy: `npm config get proxy`, `npm config get https-proxy`.
3. Pokušajte ručno instalirati: `npm install {package}@latest --prefix ~/.omniroute/services/{name}/`.
4. Ako ste iza izolirane mreže, unaprijed preuzmite tarball i koristite `npm install /path/to/tarball.tgz`.

---

### Servis prikazuje stanje `"error"` odmah nakon pokretanja (brzo rušenje)

**Simptomi:** Stanje prelazi iz `"starting"` u `"error"` za manje od 5 sekundi.
`lastError` prikazuje `"Fast crash (exited with code 1)"`.

**Kontrolna lista:**

1. Pročitajte cijeli završni dio zapisa: `GET /api/services/{name}/logs?tail=500`.
2. Čest uzrok: nedostaju varijable okruženja koje servis očekuje.
3. Za 9Router: provjerite jesu li `NINEROUTER_DISABLE_MITM=true` i
   `NINEROUTER_DISABLE_TUNNEL=true` u okruženju proslijeđenom prilikom pokretanja (pogledajte
   `installers/ninerouter.ts` `resolveSpawnArgs`).

---

## 8. Često postavljana pitanja

**P: Mogu li krajnje tačke ugrađenih servisa izložiti klijentima koji nisu na loopback interfejsu?**

Ne. Nivo LOCAL_ONLY je namjeran (strogo pravilo #17). Rute koje mogu pozvati
`npm install` ili pokrenuti `node` procese ne smiju biti dostupne saobraćaju koji
ne dolazi preko loopback interfejsa, jer bi JWT kompromitovan putem tunela
(Cloudflare, Ngrok, Tailscale) u suprotnom omogućio proizvoljno pokretanje procesa.
Za `/api/services/` ne postoji izuzetak — za razliku od `/api/mcp/`, isključen je
iz liste za zaobilaženje provjere opsega manage. Pogledajte
`docs/security/ROUTE_GUARD_TIERS.md`.

---

**P: Hoće li 9Router i CLIProxyAPI biti dostupni u produkcijskim/cloud implementacijama?**

Da. Oba servisa slijede isti model prvenstveno lokalnog rada kao i sam OmniRoute.
Pokreću se na istom računaru i komuniciraju preko loopback interfejsa. „Produkcija“
ovdje znači VPS ili lokalni server na kojem je OmniRoute implementiran, a ne udaljenog
cloud pružaoca usluga.

---

**P: Kako mogu otkloniti greške u supervizoru?**

1. Pratite SSE tok logova: `curl -N http://localhost:20128/api/services/9router/logs`.
2. Provjerite strukturirane logove u OmniRoute pino izlazu, filtrirane prema
   prostoru imena `service:supervisor`.
3. Pregledajte red u bazi podataka: `sqlite3 ~/.omniroute/omniroute.db "SELECT * FROM version_manager WHERE tool='9router'"`.
4. Koristite `GET /api/services/9router/status` da biste u jednom pozivu vidjeli trenutno stanje uživo, PID, zdravlje
   i `lastError`.

---

**P: Supervizor prikazuje `health: "degraded"` ili `health: "unknown"`, ali stanje je `"running"`. Je li to problem?**

`"degraded"` znači da je provjera zdravlja vratila odgovor koji nije 200. `"unknown"`
znači da još nijedna provjera nije završena (vremenska kolizija s prvim ispitivanjem).
Oba stanja su privremena tokom pokretanja. Ako zdravlje ostane `"degraded"` duže od
`healthIntervalMs * 3` ms nakon stanja `"running"`, ugrađeni servis radi, ali njegov
HTTP API ne odgovara. Provjerite je li port u odgovoru o statusu ispravan i sluša li
servis zaista na tom portu.

---

**P: Mogu li promijeniti 9Router API ključ bez potpunog ponovnog pokretanja?**

Ne. API ključ se prosljeđuje servisu 9Router putem varijable okruženja u trenutku
pokretanja procesa. Varijable okruženja nije moguće promijeniti u procesu koji radi.
`POST .../rotate-key` automatski zaustavlja i ponovo pokreće servis kako bi primijenio
novi ključ. Rotacija ključa stupa na snagu unutar vrijednosti `stopTimeoutMs` servisa
(zadano 15 s), uvećane za vrijeme njegovog pokretanja.

---

**P: Koje je ograničenje kružnog bafera i šta se dešava kada se popuni?**

Svaki servis ima namjenski kružni bafer od 5 MB. Kada se bafer popuni, najstariji
redovi loga uklanjaju se kako bi se oslobodio prostor za nove. SSE događaj `snapshot`
vraća najnovije redove unutar ograničenja `tail`. Logovi se ne pohranjuju na disk osim
ako je `logsBufferPath` postavljen u redu baze podataka.

---

## Pogledajte također

- `docs/security/ROUTE_GUARD_TIERS.md` — detalji nivoa LOCAL_ONLY
- `docs/architecture/CODEBASE_DOCUMENTATION.md` — §3.2 mapiranje modula ugrađenih servisa
- `docs/architecture/ARCHITECTURE.md` — kontekst na nivou sistema
- `docs/openapi.yaml` — mašinski čitljive definicije krajnjih tačaka
- `CLAUDE.md` §„Dodavanje novog ugrađenog servisa“ — kontrolna lista za brzi pregled
