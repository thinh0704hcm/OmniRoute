# open-sse Architecture (Bosanski)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Ukratko**: `open-sse/` je osnovni mehanizam za streaming koji pokreće svaki LLM zahtjev u OmniRouteu. Sadrži oko 1.400 datoteka koje implementiraju tok obrade zahtjeva, izvršioce, servise, MCP server i sloj za prevođenje. Ovaj vodič objašnjava kako su svi dijelovi povezani.

**Izvor:** `open-sse/` (workspace paket, oko 1.440 `.ts` datoteka koje nisu testovi)

---

## Zašto zaseban workspace paket?

`open-sse/` je **samostalan workspace** u OmniRoute monorepozitoriju iz nekoliko razloga:

1. **Ponovna upotrebljivost** — `open-sse` se objavljuje kao `@omniroute/open-sse` na npm-u, tako da ga drugi projekti mogu koristiti nezavisno
2. **Jasne granice** — mehanizam za streaming odvojen je od UI/DB sloja specifičnog za OmniRoute
3. **Performanse** — mehanizam nema Next.js zavisnosti, što omogućava brže hladno pokretanje u CLI/serverless okruženjima
4. **Verzioniranje** — `open-sse` može objavljivati izdanja vlastitim tempom

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Struktura najvišeg nivoa

```
open-sse/
├── index.ts              # Javna ulazna tačka
├── types.d.ts            # Javni izvozi tipova
├── package.json          # @omniroute/open-sse
├── config/               # Konfiguracije pružalaca, konstante, registri
├── executors/            # HTTP izvršioci po pružaocu (67 + base.ts/index.ts)
├── handlers/             # Obrađivači zahtjeva (chatCore, responses itd.)
├── lib/                  # Interni pomoćni alati
├── mcp-server/           # Model Context Protocol server
├── services/             # Oko 298 servisnih modula
├── transformer/          # Transformator formata Responses API-ja
├── translator/           # Prevođenje formata (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Zajednički pomoćni alati (zapisivanje, greške, streaming itd.)
```

### Broj modula

| Direktorij | Datoteke | Namjena |
| `executors/` | 167 | HTTP izvršioci po pružaocu (objedinjeni putem DefaultExecutor factory obrasca) |
| `handlers/` | 157 | Ulazne tačke zahtjeva (chatCore, responses, embeddings) |
| `services/` | ~536 | Usmjeravanje, keširanje, ograničavanje brzine, osvježavanje itd. |
| `translator/` | 56 | Konverzija formata (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP alati i transporti |
| `utils/` | ~108 | Pomoćni alati koji se koriste kroz više slojeva (zapisivanje, greške, streaming) |
| `config/` | ~339 | Konfiguracije pružalaca, konstante, registri |

---

## Tok obrade zahtjeva

Svaki LLM zahtjev prolazi kroz **tok obrade od 5 faza**:

```
                              ┌──────────────┐
   HTTP zahtjev                │  1. USMJERI  │   razrješavanje kombinacije, odabir modela
   (Next.js ruta)              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. PREVEDI  │   konverzija formata (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. IZVRŠI   │   izvršilac pružaoca, HTTP, ponovni pokušaj, prekidač
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAM   │   SSE transformacija, povratni pritisak
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. ZABILJEŽI│   praćenje upotrebe, dnevnik poziva, klasifikacija grešaka
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP odgovor (SSE ili JSON)
```

### Faza 1: Usmjeravanje (services/combo.ts)

**Ulazna tačka**: `handleComboChat()` u `services/combo.ts`

Razrješava zahtjev u konkretan skup `(provider, model, account, credentials)`:

- Pronalazi kombinaciju prema ID-u (ili gradi virtuelnu kombinaciju za `auto/*` modele)
- Primjenjuje strategiju usmjeravanja (prioritet, ponderisano, kružno itd.)
- Filtrira pružaoce koji nisu ispravni (prekidač strujnog kola)
- Odabire sljedeće dostupno odredište

Za `auto/*` modele ova faza također:

- Pokreće algoritam **bodovanja prema 16 faktora** (`services/autoCombo/`)
- Odabire par `provider+model` na osnovu stanja, cijene, latencije itd.

### Faza 2: Prevođenje (translator/)

Ako se izvorni format (npr. OpenAI) razlikuje od ciljnog formata (npr. Claude), zahtjev se **prevodi**:

- Sistemski prompt → sistemska poruka
- Definicije alata → format alata specifičan za pružaoca
- Parametri zaključivanja/razmišljanja → ekvivalenti specifični za pružaoca
- Normalizacija uloga poruka (`developer` → `system` za sisteme koji nisu OpenAI)

`translator/index.ts` izlaže:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Faza 3: Izvršavanje (executors/)

**Ulazna tačka**: `getExecutor(providerId).execute(request, options)`

Svi pružaoci koriste `DefaultExecutor` (`executors/default.ts`) putem rezervne opcije `getExecutor()` factory obrasca. Izvršilac:

- Gradi upstream URL (`buildUrl()`)
- Dodaje zaglavlja specifična za pružaoca (`buildHeaders()`)
- Transformiše tijelo zahtjeva (`transformRequest()`)
- Šalje HTTP zahtjev uz ponovne pokušaje i eksponencijalno povećavanje intervala čekanja
- Po potrebi upravlja osvježavanjem autentifikacije (OAuth pružaoci)

Svi izvršioci proširuju `BaseExecutor` (`executors/base.ts`, 1170 LOC), koji pruža:

- Zajedničku logiku ponovnih pokušaja
- Integraciju proxyja
- Integraciju prekidača strujnog kola
- Hookove za bilježenje upotrebe

### Faza 4: Streaming (utils/stream.ts)

Za streaming odgovore izvršilac vraća **ReadableStream**. Obrađivač:

- Prosljeđuje podatke kroz SSE transformaciju (`createSSETransformStreamWithLogger`)
- Primjenjuje heartbeat pingove radi otkrivanja prekinutih veza
- Elegantno obrađuje prekid veze klijenta (`pipeWithDisconnect`)
- Transformiše SSE → JSON za klijente koji ne koriste streaming

Za odgovore bez streaminga izvršilac vraća parsirani JSON objekt koji se prosljeđuje bez izmjena.

### Faza 5: Bilježenje (services/usage.ts)

Nakon odgovora (uspješnog ili neuspješnog), bilježe se podaci o korištenju:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` iz odgovora
- `cost_usd` izračunat na osnovu podataka o cijenama
- `latency_ms`, `status`, `error_class` u slučaju neuspjeha
- Pohranjeno u tabelu `usage_history`

Artefakti evidencije poziva (ako su omogućeni) zapisuju se u `${DATA_DIR}/call_logs/`.

---

## Detaljan pregled ključnih datoteka

### chatCore.ts (5977 linija)

**Glavni obrađivač zahtjeva**. Uprkos svojoj veličini, ima jasnu strukturu:

```ts
// Pseudostruktura datoteke chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Autentifikacija + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Validacija tijela zahtjeva
  const body = await parseRequestBody(request);

  // 3. Prepoznavanje formata + prevođenje
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Kombinovano usmjeravanje
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Nastavi na sljedeće odredište
    }
  }

  // 5. Rezervna opcija za hitne slučajeve
  return await emergencyFallback(body);
}
```

Uprkos tome što je riječ o jednoj ogromnoj funkciji, organizovana je u **komentarisane odjeljke** koji odgovaraju cjevovodu od 5 faza.

### combo.ts (4456 linija koda)

**Mehanizam za usmjeravanje** koji razrješava kombinaciju u poredana odredišta.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Podržava **19 strategija usmjeravanja** (pogledajte `src/shared/constants/routingStrategies.ts`):

| Strategija          | Ponašanje                                                                        |
| ------------------- | -------------------------------------------------------------------------------- |
| `priority`          | Poredana lista s prvim odredištem kao prioritetom                                |
| `weighted`          | Vjerovatnosni odabir prema težini svakog odredišta                               |
| `round-robin`       | Kruženje kroz odredišta redom                                                    |
| `context-relay`     | Prenošenje konteksta između odredišta                                            |
| `fill-first`        | Popunjavanje kvote prije prelaska na sljedeće odredište                          |
| `p2c`               | Izbor između dvije opcije                                                        |
| `random`            | Ravnomjeran nasumični odabir                                                     |
| `least-used`        | Odabir odredišta s najmanje nedavnih korištenja                                  |
| `cost-optimized`    | Prvo najjeftinije dostupno odredište                                             |
| `reset-aware`       | Uzimanje u obzir perioda resetovanja pružaoca usluge                             |
| `reset-window`      | Usmjeravanje zasnovano na periodu resetovanja                                    |
| `headroom`          | Prvo odredište s najvećom preostalom rezervom kvote                              |
| `strict-random`     | Potpuno ravnomjeran odabir (bez ponderisanja prema kvalitetu)                    |
| `auto`              | Korištenje bodovanja sa 16 faktora (`autoCombo/`)                                |
| `lkgp`              | Posljednji poznati ispravni pružalac usluge ima prednost                         |
| `context-optimized` | Najbolje za zahtjeve s dugim kontekstom                                          |
| `fusion`            | Paralelno slanje panelu, zatim objedinjavanje pomoću procjenjivača (`fusion.ts`) |

### base.ts (1170 linija koda)

**Apstraktni izvršilac** koji proširuje svih 107 izvršilaca. Sadrži:

- `buildUrl()` — podrazumijevana konstrukcija URL-a (podklase je zamjenjuju prilagođenom implementacijom)
- `buildHeaders()` — podrazumijevana zaglavlja (autentifikacija, vrsta sadržaja)
- `transformRequest()` — podrazumijevano prosljeđivanje bez izmjena
- `execute()` — glavna HTTP petlja s ponovnim pokušajima, progresivnim čekanjem i prekidačem

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Obrađuje sve pružaoce usluga kompatibilne s OpenAI/Anthropic formatom
  // Pružaoci usluga registruju konfiguracije (URL, autentifikaciju, zaglavlja), ali dijele logiku izvršioca
}
```

Ponašanje specifično za pružaoca usluge (zaglavlja za autentifikaciju, osnovni URL, zaglavlja verzije) konfiguriše se putem registra pružalaca usluga, a ne zasebnih klasa izvršilaca.

````

---

## Servisi (117 modula)

Servisi su **fokusirani, jednonamjenski moduli** koje obrađivači kombinuju. Glavne kategorije su:

### Usmjeravanje i Combo

- `combo.ts` — ulazna tačka za zahtjeve usmjerene putem combo mehanizma
- `services/autoCombo/` — bodovanje prema 16 faktora, 8 strategija automatskog usmjeravanja
- `wildcardRouter.ts` — pronalazi podudaranja s rutama koje sadrže zamjenske znakove (`gpt-*`)
- `modelFamilyFallback.ts` — T5 rezervni izbor unutar porodice

### Ograničavanje brzine i kvota

- `rateLimitManager.ts` — spremnik tokena po ključu i pružaocu
- `usage.ts` — bilježenje korištenja
- `quotaCache.ts` — snimci kvota u memoriji

### Račun i token

- `tokenRefresh.ts` — OAuth osvježavanje pri odgovoru 401
- `accountFallback.ts` — prebacivanje na alternativni račun
- `sessionManager.ts` — stanje sesije kroz više interakcija

### Inteligencija

- `intentClassifier.ts` — klasificiranje namjere zahtjeva
- `taskAwareRouter.ts` — usmjeravanje prema vrsti zadatka
- `thinkingBudget.ts` — dodjeljivanje tokena za razmišljanje
- `contextManager.ts` — umetanje konteksta usmjeravanja

### Otpornost

- `resilience.ts` — orkestracija ponovnih pokušaja, odgode i prekidača
- `emergencyFallback.ts` — rezervni izbor u krajnjoj nuždi
- `modelDeprecation.ts` — automatsko usmjeravanje na modele nasljednike

### Stanje

- `signatureCache.ts` — uklanjanje duplikata prema potpisu zahtjeva
- `volumeDetector.ts` — rasterećenje pri velikom opterećenju
- `contextHandoff.ts` — serijalizacija sesije

### Kompresija

- `compression/` (poddirektorij) — potpuni cjevovod kompresije
- 39 datoteka koje obuhvataju mehanizme, pakete pravila i adaptere

### Vještine

- (obrađeno u [SKILLS.md](./SKILLS.md))

### Memorija

- (obrađeno u [MEMORY.md](./MEMORY.md))

---

## Izvršitelji (75+ datoteka)

Jedna datoteka po pružaocu. Svi proširuju `BaseExecutor` i nadjačavaju ono što se razlikuje.

### Uobičajeni obrasci

Pružaoci se razrješavaju putem `getExecutor(providerId)`, koji vraća konfigurirani izvršitelj. Pružaoci kompatibilni s OpenAI/Anthropic koriste `DefaultExecutor` (`executors/default.ts`). Ponašanje specifično za pružaoca (osnovni URL, zaglavlja za autentifikaciju, verzija API-ja) konfigurira se u `open-sse/config/providers/`, dok se transformacije tijela zahtjeva obrađuju u `open-sse/translator/`.

**Prilagođeni URL** postavlja se putem konfiguracije pružaoca:

```ts
// Konfiguracija pružaoca u open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Prilagođena autentifikacija** obrađuje se putem konfiguracije autentifikacije u registru pružalaca (API ključ, OAuth, profili zaglavlja).

Transformacije **prilagođenog tijela zahtjeva** (npr. Anthropicovo odvajanje `system` od `messages`) registriraju se za svakog pružaoca u `open-sse/translator/`.

````

### Fabrika izvršitelja

`executors/index.ts` izvozi `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Razrješavanje se odvija putem `ExecutorRegistry` (`executors/registry.ts`): svaki specijalizirani izvršitelj deklariran je u ugrađenoj tabeli datoteke `executors/index.ts` i registriran putem `registerExecutor(alias, instance)` prilikom učitavanja modula; `getExecutor()` provjerava registar i vraća memorirani `DefaultExecutor` za svakog pružaoca bez specijaliziranog unosa. Potpuno mapiranje alias → izvršitelj definirano je zlatnim testom `tests/unit/executor-map-golden.test.ts`.

---

## Prevodioci

Prevodite između **3 formata**: OpenAI, Anthropic, Gemini, kao i novi Responses API.

### Kada se vrši prevođenje

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Uobičajena prevođenja:

- `OpenAI → Anthropic`: zasebno polje `system`, zaglavlje `x-api-key`
- `OpenAI → Gemini`: `contents` umjesto `messages`, `systemInstruction`
- `OpenAI → Responses API`: niz `input`, stanje `previous_response_id`

### Obrađeni rubni slučajevi

- uloga `developer` → `system` za sisteme koji nisu OpenAI
- uloga `system` → spojena s prvom korisničkom porukom za GLM/ERNIE
- `json_schema` → Geminijevi `responseMimeType` + `responseSchema`
- `tools` → format alata specifičan za pružaoca
- Parametri razmišljanja (o1, Claude) → ekvivalenti specifični za pružaoca

---

## MCP server

`open-sse/mcp-server/` implementira server za **Model Context Protocol**:

- **110 alata** (upravljanje pružaocima, kombinacije, memorija, predmemorija, kompresija, proxy, vještine, gamifikacija, dodaci, Notion, Obsidian, lokalni korpus)
- **3 transporta**: stdio, SSE, Streamable HTTP
- **33 opsega** za detaljnu autorizaciju

### Registracija alata

Alati su registrovani kao samostalne datoteke u `open-sse/mcp-server/tools/`, pri čemu svaka izvozi naziv, shemu, rukovalac i opseg:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transporti

```ts
// stdio (upotreba putem CLI-ja)
startMcpStdio(server);

// SSE (streaming zasnovan na HTTP-u)
startMcpSse(server, port);

// Streamable HTTP (moderni MCP)
startMcpStreamable(server, port);
```

### Autorizacija

Svaki poziv alata prolazi kroz provjere opsega (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Transformatori

`open-sse/transformer/` vrši konverziju između formata **Chat Completions** i **Responses API**.

### Zašto zaseban transformator?

Responses API je novi OpenAI format sa **razgovorima sa stanjem** (`previous_response_id`). Kada klijent pošalje Responses zahtjev, OmniRoute:

1. Interno konvertuje Responses → Chat Completions
2. Šalje pružaocu (bilo kojem pružaocu koji podržava Chat Completions)
3. Konvertuje odgovor nazad u Responses format
4. Strimuje konvertovani odgovor klijentu

Transformator (`transformer/responsesTransformer.ts`) pruža:

```ts
createResponsesApiTransformStream(): TransformStream
```

On obrađuje:

- događaje `response.output_item.added`
- događaje `response.output_text.delta`
- događaj `response.completed`
- Mapiranje poziva alata (`function_call` ↔ `tool_calls`)

---

## Konfiguracija

`open-sse/config/` sadrži konfiguracijski sloj:

| Datoteka                      | Namjena                                                   |
| ----------------------------- | --------------------------------------------------------- |
| `providerRegistry.ts`         | Registar modela za razgovor nad katalogom od 352 pružaoca |
| `providerModels.ts`           | Aliasi modela, mapiranje formata                          |
| `constants.ts`                | Vremenska ograničenja, limiti, statusni kodovi            |
| `defaultThinkingSignature.ts` | Zadani Claude potpis razmišljanja                         |
| `modelStrip.ts` (u servisima) | Uklanjanje polja za svakog pružaoca                       |

### Shema registra pružalaca

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Zod validacija pri učitavanju modula osigurava da su sve konfiguracije pružalaca ispravne.

---

## Ograničenja performansi

Mehanizam za usmjeravanje ima strogo definirane ciljeve performansi:

| Operacija                                                   | Cilj  | Mjerenje                     |
| ----------------------------------------------------------- | ----- | ---------------------------- |
| Razrješavanje kombinacije                                   | <10ms | Za 50 odredišta              |
| Provjera ograničenja stope                                  | <1ms  | Memorijska kanta tokena      |
| Rezervni odabir porodice modela                             | <5ms  | Keširane definicije porodica |
| Prosljeđivanje zahtjeva                                     | <2ms  | Kritična putanja             |
| **Bez blokirajućeg I/O-a u kritičnoj putanji usmjeravanja** | —     | Sve je asinhrono             |

---

## Antipatterni

❌ **Sinhroni pozivi baze podataka u `combo.ts`** — unaprijed izračunajte i keširajte
❌ **Logika ponovnog pokušaja u rukovaocima** — koristite `retry()` iz servisa za otpornost
❌ **Direktan pristup konfiguraciji pružaoca** — koristite gettere iz `providerRegistry`
❌ **Tvrdo kodirani lanci rezervnih opcija** — definirajte ih u `modelFamilyFallback.ts`
❌ **Izmjene stanja među konkurentnim zahtjevima** — koristite samo kontekst ograničen na zahtjev

---

## Dodavanje nove komponente

### Dodavanje novog servisa

1. Kreirajte `open-sse/services/[serviceName].ts` s jasno definiranom odgovornošću
2. Izvezite glavnu funkciju rukovaoca i sve konstante
3. Dodajte jedinične testove u `tests/unit/services/[serviceName].test.mjs`
4. Integrirajte u proces obrade zahtjeva u `handlers/chatCore.ts` (ako je povezano s usmjeravanjem)
5. Ažurirajte logiku usmjeravanja u `combo.ts` ako servis utječe na odabir odredišta
6. Dokumentirajte u ovoj datoteci

### Dodavanje novog izvršioca

1. Kreirajte `open-sse/executors/[provider].ts` koji proširuje `BaseExecutor`
2. Registrirajte u `config/providerRegistry.ts`
3. Dodajte u tvornicu u `executors/index.ts`
4. Dodajte jedinične testove za izvršioca
5. Dokumentirajte u `docs/architecture/ARCHITECTURE.md`

### Dodavanje novog MCP alata

1. Kreirajte ili ažurirajte `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definirajte Zod shemu za ulazne podatke
3. Registrirajte alat u `mcp-server/index.ts`
4. Dodajte u matricu opsega u `mcp-server/auth/`
5. Dodajte jedinične testove

---

## Također pogledajte

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — arhitektura visokog nivoa
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — inženjerska referenca
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — pregled po direktorijima
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — bodovanje na osnovu 16 faktora
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP server
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A server
- Izvor: `open-sse/` (više od 400 datoteka, ~143K linija koda)
