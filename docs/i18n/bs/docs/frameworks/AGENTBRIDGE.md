# AgentBridge (Bosanski)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENTBRIDGE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENTBRIDGE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENTBRIDGE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENTBRIDGE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENTBRIDGE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENTBRIDGE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENTBRIDGE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENTBRIDGE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENTBRIDGE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENTBRIDGE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENTBRIDGE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENTBRIDGE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENTBRIDGE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENTBRIDGE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENTBRIDGE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENTBRIDGE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENTBRIDGE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENTBRIDGE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENTBRIDGE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENTBRIDGE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENTBRIDGE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENTBRIDGE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENTBRIDGE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENTBRIDGE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENTBRIDGE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENTBRIDGE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENTBRIDGE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENTBRIDGE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENTBRIDGE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENTBRIDGE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENTBRIDGE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENTBRIDGE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENTBRIDGE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENTBRIDGE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENTBRIDGE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENTBRIDGE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENTBRIDGE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENTBRIDGE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENTBRIDGE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENTBRIDGE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENTBRIDGE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENTBRIDGE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENTBRIDGE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENTBRIDGE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENTBRIDGE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENTBRIDGE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENTBRIDGE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENTBRIDGE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENTBRIDGE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENTBRIDGE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/AGENTBRIDGE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENTBRIDGE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENTBRIDGE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENTBRIDGE.md)

---

# AgentBridge

AgentBridge je OmniRoute-ov MITM (Man-in-the-Middle) proxy koji presreće HTTPS saobraćaj od IDE AI agenata i preusmjerava ga kroz OmniRoute-ov jedinstveni mehanizam za rutiranje. Podržava **10 IDE agenata** — Antigravity, Kiro, GitHub Copilot, GHE Copilot, OpenAI Codex, Cursor, Zed, Claude Code, Open Code i Trae (u fazi istraživanja) — čineći OmniRoute MITM proxyjem sa najširom pokrivenošću za AI asistente za kodiranje na tržištu.

**Lokacija kontrolne ploče (Dashboard):** `/dashboard/tools/agent-bridge`
**Grupa u bočnoj traci (Sidebar):** Tools (nakon Cloud Agents)
**Pogledajte također:** [`TRAFFIC_INSPECTOR.md`](./TRAFFIC_INSPECTOR.md) — nadgledajte sav presretnuti saobraćaj u realnom vremenu; `docs/security/MITM-TPROXY-DECRYPT.md` (git; nije kompajlirano u `/docs`) — Linux TPROXY transparentni-dešifrujući režim snimanja pokretan putem rute `/api/tools/agent-bridge/tproxy`.

---

## §1 Pregled

### Šta je AgentBridge?

Kada IDE agent (npr. GitHub Copilot, Cursor, Claude Code) napravi API poziv, on se direktno povezuje sa upstream AI provajderom (OpenAI, Anthropic, itd.). AgentBridge transparentno presreće tu konekciju na TLS nivou — bez potrebe za bilo kakvom promjenom konfiguracije agenta — i prepisuje zahtjev kroz OmniRoute.

To znači da možete:

- **Preusmjeriti bilo kojeg agenta na bilo kojeg provajdera**: Copilot komunicira sa OpenAI? Preusmjerite ga na Anthropic Claude, Gemini ili bilo kojeg od 352 OmniRoute provajdera.
- **Primijeniti mapiranja modela**: `gemini-3-flash` → `claude-sonnet-4.7` transparentno na nivou handlera.
- **Posmatrati sav saobraćaj agenta**: svaki presretnuti zahtjev se objavljuje u [Traffic Inspector](./TRAFFIC_INSPECTOR.md).
- **Primijeniti OmniRoute otpornost**: kombinovano rutiranje, circuit breakers, fallbacks i praćenje troškova rade i za saobraćaj IDE agenata.

### Pozicioniranje u odnosu na tržište

| Funkcionalnost         | 9router | anti-api | llm-interceptor | **OmniRoute AgentBridge** |
| ---------------------- | :-----: | :------: | :-------------: | :-----------------------: |
| Antigravity            |    ✓    |    ✓     |        —        |             ✓             |
| GitHub Copilot         |    ✓    |    ✓     |        —        |             ✓             |
| Kiro (AWS)             |    ✓    |    ✓     |        —        |             ✓             |
| OpenAI Codex           |    —    |    ✓     |        —        |             ✓             |
| Cursor IDE             |    ✓    |    ✓     |        —        |             ✓             |
| Zed Industries         |    —    |    ✓     |        —        |             ✓             |
| Claude Code            |    —    |    —     |        ✓        |             ✓             |
| Open Code              |    —    |    —     |        ✓        |             ✓             |
| Trae                   |    —    |    —     |        —        |      🔍 Istražuje se      |
| Dashboard UI           |    ✓    |    ✗     |        ✗        |             ✓             |
| Traffic Inspector      |    ✗    |    ✗     |        ✓        |             ✓             |
| OmniRoute rutiranje    |    ✗    |    ✗     |        ✗        |             ✓             |
| UI za mapiranje modela |    ✗    |    ✗     |        ✗        |             ✓             |
| Lista za zaobilaženje  |    ✗    |    ✗     |        ✓        |             ✓             |
| Upstream CA certifikat |    ✗    |    ✗     |        ✓        |             ✓             |

---

## §2 Arhitektura

### 2.1 Pregled komponenti

```
IDE Agent (VS Code / Cursor / itd.)
    │  HTTPS (port 443)
    ▼
/etc/hosts — 127.0.0.1 api.githubcopilot.com   ← DNS preusmjeravanje
    │
    ▼
src/mitm/server.cjs  (port 443, CJS podproces)
    │  razrješava cilj putem Host zaglavlja SNI
    │  generiše TLS certifikat po SNI potpisan od strane AgentBridge CA
    ├── Podudaranje sa listom zaobilaženja? → TCP prolaz (bez dekripcije)
    ├── Podudaranje sa ciljem? → fetch → OmniRoute ruter (port 20128)
    │       └── handler.intercept() — TypeScript
    │               ├── maskSecrets() na tijelu zahtjeva/zaglavljima
    │               ├── TrafficBuffer.push() — objavljuje u Traffic Inspector
    │               └── fetchRouter() → /v1/chat/completions
    └── Nema podudaranja? → TCP prolaz (bez dekripcije)
```

### 2.2 MITM server (`src/mitm/server.cjs`)

Osnovni MITM server radi kao Node.js CJS podproces (kako bi se izbjeglo prepisivanje postojećeg CJS koda). On:

- Sluša na portu 443 (zahtijeva privilegije ili `authbind`/`setcap`)
- Prima CONNECT tunele od OS-a (putem `/etc/hosts` DNS preusmjeravanja)
- Generiše TLS certifikate po SNI potpisane od strane AgentBridge CA (`DATA_DIR/mitm/ca.crt`)
- Razrješava ciljni agent putem Host zaglavlja kroz `targets/index.ts` registar
- Prosljeđuje na TypeScript sloj rukovaoca putem HTTP-a na `http://127.0.0.1:20128`

`TARGET_HOSTS` se učitava iz `DATA_DIR/mitm/targets.json` (koji `targets/index.ts` upisuje pri pokretanju), omogućavajući dinamička ažuriranja bez ponovnog pokretanja CJS servera.

> **Root-CA model (#6684).** Gore navedeni opis certifikata po SNI potpisanog od strane CA je trajni root-CA model dodan u #6684 (`src/mitm/cert/rootCa.ts` + `src/mitm/_internal/rootCaShim.cjs`, ponovno koristeći CA/leaf kriptografiju koja je već provjerena za TPROXY u `src/mitm/tproxy/dynamicCert.ts`) — on zamjenjuje stariji pojedinačni statički samopotpisani leaf (`src/mitm/cert/generate.ts`, koji je još uvijek ograničen samo na antigravity hostove) na šta ukazuje običan `server.crt`/`server.key` par na disku. **Ponašanje pri migraciji**: svježa instalacija (bez prethodnog `server.crt`) automatski dobija root-CA model; instalacija koja je već vjerovala starom statičkom leaf-u nastavlja ga koristiti sve dok operater ne postavi `MITM_ROOT_CA_ENABLED=true` i ponovo pokrene bridge (`src/mitm/cert/migration.ts` je čista funkcija odlučivanja — pouzdani MITM CA koji može potpisati leaf za **bilo koji** host je materijalno moćniji od starog leaf-a sa fiksnim SAN-om, tako da prebacivanje nikada nije tiho za već pouzdanu instalaciju). CA certifikat se instalira u isti `omniroute-mitm.crt` slot skladišta povjerenja koji je koristio stari leaf (`cert/install.ts::installCaCert`) — nije potrebno čišćenje dvostrukog povjerenja.

### 2.3 Osnova rukovaoca (`src/mitm/handlers/base.ts`)

Svi rukovaoci agenata proširuju `MitmHandlerBase`:

```ts
export abstract class MitmHandlerBase {
  abstract readonly agentId: AgentId;

  abstract intercept(
    req: IncomingMessage,
    res: ServerResponse,
    body: Buffer,
    mappedModel: string
  ): Promise<void>;

  // Zaštićeni pomoćnici: fetchRouter, pipeSSE, hookBufferStart, hookBufferUpdate
}
```

Svaki rukovalac poziva `hookBufferStart()` prije proksiranja i `hookBufferUpdate()` kada završi. Oni guraju `InterceptedRequest` unose u `globalTrafficBuffer` (pogledajte [Traffic Inspector](./TRAFFIC_INSPECTOR.md) §4).

### 2.4 Registar ciljeva (`src/mitm/targets/`)

Svaki agent ima deklarativnu datoteku cilja:

```ts
// src/mitm/targets/copilot.ts
export const COPILOT_TARGET: MitmTarget = {
  id: "copilot",
  name: "GitHub Copilot",
  hosts: ["api.githubcopilot.com", "copilot-proxy.githubusercontent.com"],
  port: 443,
  endpointPatterns: ["/chat/completions", "/v1/chat/completions"],
  defaultModels: [{ id: "gpt-4o", name: "GPT-4o", alias: "gpt-4o" }],
  handler: () => import("../handlers/copilot"),
  riskNoticeKey: "providers.riskNotice.oauth",
};
```

Registar (`targets/index.ts`) izvozi `ALL_TARGETS` i emituje `DATA_DIR/mitm/targets.json` pri pokretanju.

### 2.5 Prolaz i lista zaobilaženja (`src/mitm/passthrough.ts`)

**Lista zaobilaženja** (provjerava se prva, sa prioritetom nad podudaranjem cilja):

- Zadani obrasci: bankarski hostovi, `.gov.`, OAuth/SSO provajderi (Okta, Auth0), itd.
- Korisnički obrasci: pohranjeni u DB tabeli `agent_bridge_bypass`
- Zaobiđeni hostovi primaju transparentni TCP tunel — TLS se **nikada ne dekriptuje**

**Zadani prolaz** (nema podudaranja cilja i nije u zaobilaženju):

- Također prima TCP tunel — veze se nikada ne prekidaju
- Sprečava AgentBridge da ometa opći sistemski HTTPS saobraćaj

Prioritet rutiranja:

```
lista zaobilaženja → podudaranje cilja → prolaz
```

### 2.6 Upstream CA certifikat (`src/mitm/upstreamTrust.ts`)

Za korporativna mrežna okruženja sa prilagođenim CA:

```bash
AGENTBRIDGE_UPSTREAM_CA_CERT=/path/to/corporate-ca.pem
```

Kada je postavljeno, konfiguriše `undici` globalni dispečer sa dodatnim CA certifikatom, omogućavajući AgentBridge-u da dosegne upstream provajdere kroz korporativne TLS terminacione proxyje.

### 2.7 Maskiranje tajni (`src/mitm/maskSecrets.ts`)

Nezavisni clean-room skener se primjenjuje na tijela zahtjeva i zaglavlja vjerodajnica **prije** nego što uđu u Traffic Inspector bafer ili bilo koji log. On izvodi jedan linearni prolaz:

- `sk-` / `ak-` / `pk-` prefiksirani tokeni (OpenAI/Anthropic stil)
- RFC 6750 `Authorization: Bearer <token>` vjerodajnice, sa prioritetom cijelog tokena
- Generički dugi neprozirni tokeni (≥40 znakova), uključujući tačkaste i popunjene oblike

`sanitizeHeaders()` pretvara zadržana imena u mala slova, spaja vrijednosti niza deterministički, odbacuje dijeljenu hop-by-hop/framing crnu listu (uključujući proxy autentifikaciju), potpuno rediguje `cookie` i `set-cookie`, i delegira vrijednosti vjerodajnica skeneru.

---

## §3 Postavljanje

### 3.1 Pokretanje/zaustavljanje MITM servera

Koristite AgentBridge Server karticu na `/dashboard/tools/agent-bridge`:

| Akcija          | Opis                                                                               |
| --------------- | ---------------------------------------------------------------------------------- |
| Start Server    | Pokreće `src/mitm/server.cjs` na portu 443                                         |
| Stop Server     | Graciozno gasi podređeni proces                                                    |
| Restart Server  | Zaustavi + pokreni (preuzima promjene ciljeva)                                     |
| Trust Cert      | Instalira `DATA_DIR/mitm/ca.crt` u skladište pouzdanih certifikata OS-a            |
| Download Cert   | Preuzima `ca.crt` za ručnu instalaciju                                             |
| Regenerate Cert | Kreira novi CA par ključeva (svi postojeći certifikati po agentu postaju nevažeći) |

### 3.2 Povjerenje certifikatu

AgentBridge CA certifikat mora biti pouzdan od strane operativnog sistema prije nego što IDE-ovi prihvate MITM konekciju.

**Linux (NSS — Chrome/Firefox):**

```bash
certutil -A -d sql:$HOME/.pki/nssdb -n "OmniRoute AgentBridge" -t CT,, -i ~/.omniroute/mitm/ca.crt
```

**macOS (Keychain):**

```bash
sudo security add-trusted-cert -d -r trustRoot \
  -k /Library/Keychains/System.keychain ~/.omniroute/mitm/ca.crt
```

**Windows (certmgr):**

```powershell
certutil -addstore -f Root $env:USERPROFILE\.omniroute\mitm\ca.crt
```

Ili koristite dugme „Trust Cert“ na kontrolnoj tabli (pokreće odgovarajuću komandu za vaš OS, sa sudo upitom ako je potrebno).

#### IDE-ovi zasnovani na Electronu ignorišu skladište pouzdanih certifikata OS-a (`NODE_EXTRA_CA_CERTS`)

Neki IDE-ovi — posebno **Antigravity IDE**, i druge aplikacije izvedene iz Electrona / VS Code-a — uključuju sopstveno Node.js okruženje koje **ne konsultuje skladište pouzdanih certifikata OS-a** za odlazne `fetch`/HTTPS zahtjeve. Povjerenje CA certifikatu na nivou OS/NSS-a je dovoljno za izvorni **backend** IDE-a (npr. Go jezički server, koji koristi CA paket OS-a), ali **Electron frontend** će i dalje imati grešku pri TLS-u — to se manifestuje kao da je aplikacija _odjavljena_ ili prikazuje _"connection error"_ iako MITM log pokazuje da bootstrap pozivi backend-a vraćaju `200`. Potrebna su dva koraka, i oba su važna:

1. Eksplicitno usmjerite runtime na CA:
   ```bash
   export NODE_EXTRA_CA_CERTS=/path/to/omniroute-agentbridge-ca.crt
   ```
2. **Pokrenite IDE iz te ljuske (shell-a).** Pokretanje sa ikone na radnoj površini / Dock-a / Start menija **ne** nasljeđuje shell eksporte, a `~/.config/environment.d/*.conf` se primjenjuje tek nakon svježe grafičke prijave. Prvo potpuno zatvorite IDE — Electron-ov singleton lock znači da drugo pokretanje samo fokusira postojeći proces i novo okruženje se ignoriše.

Korak OS-trust + NSS iznad ostaje neophodan (Chromium mrežni stek koji koriste neki auth tokovi čita NSS skladište po korisniku, i ima svoje statičke pinove za `*.googleapis.com` koje lokalno pouzdani CA nadjačava). `NODE_EXTRA_CA_CERTS` pokriva Node `fetch` putanju povrh toga.

### 3.3 DNS rutiranje

Za svakog agenta kojeg želite presresti, njegov(i) API host(ovi) moraju biti razriješeni na `127.0.0.1`. AgentBridge automatski upravlja `/etc/hosts` unosima kada uključite DNS za agenta u čarobnjaku za postavljanje (Setup Wizard).

Primjer `/etc/hosts` unosa za GitHub Copilot:

```
127.0.0.1 api.githubcopilot.com
127.0.0.1 copilot-proxy.githubusercontent.com
```

### 3.4 Mapiranje modela

Koristite tabelu za mapiranje modela u svakoj kartici agenta da definišete mapiranja izvor → cilj:

| Izvorni model (izvorni za agenta) | Ciljni model (OmniRoute) |
| --------------------------------- | ------------------------ |
| `gpt-4o`                          | `claude-sonnet-4.7`      |
| `*` (džoker znak)                 | `claude-haiku-4.7`       |

Džoker znak `*` mapira bilo koji neprepoznati model na navedeni cilj. Trajno sačuvano u tabeli `agent_bridge_mappings`.

> **Savjet — otkrijte stvarne ID-ove modela agenta.** IDE može slati nazive modela koji se razlikuju od njegovih UI oznaka i koji se mijenjaju između glavnih verzija. Na primjer, **Antigravity 2** šalje `gemini-3.1-pro-low`, `gemini-pro-agent`, i `gemini-3.1-flash-lite` preko mreže — a ne `gemini-2.5-pro` prikazan u starijoj dokumentaciji. Pošaljite jedan chat bez postavljenog odgovarajućeg mapiranja: MITM loguje tačan dolazni `model:` i propušta zahtjev. Mapirajte tu doslovnu vrijednost, a zatim će sljedeći zahtjev biti presretnut i rutiran na vaš cilj.

### 3.5 Obavještenje o riziku

AgentBridge presreće akreditive (OAuth tokene, API ključeve) koje IDE koristi za autentifikaciju kod upstream provajdera. Oni su **maskirani prije logovanja** (pogledajte §2.7), ali su vidljivi MITM sloju OmniRoute-a. Prva aktivacija svakog agenta prikazuje modal sa obavještenjem o riziku koji se može zatvoriti.

### 3.6 Održavanje i dijagnostika

Kontrolna tabla izlaže karticu **Održavanje i dijagnostika** (`AgentBridgeMaintenanceCard`, u `src/app/(dashboard)/dashboard/tools/agent-bridge/components/`) koja prikazuje operativne MITM rute koje prethodno nisu imale UI. Njen podnaslov: _"Samotestirajte pipeline za hvatanje, poništite zaostalo stanje sistema i premjestite svoje postavljanje između mašina."_ Pomoćni klijentski alati kartice se nalaze u `src/lib/inspector/agentBridgeMaintenanceApi.ts`.

| Dugme             | Ruta                                   | Šta radi                                                                                                                                                                                           |
| ----------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Diagnose**      | `GET /api/tools/agent-bridge/diagnose` | Pokreće samotestiranje capture-pipeline-a i prikazuje izvještaj po provjeri (✓/✗ + savjet za sanaciju).                                                                                            |
| **Repair**        | `POST /api/tools/agent-bridge/repair`  | Poništava osirotjelo sistemsko stanje MITM-a (DNS spoof unosi, root CA, sistemski proxy) koje je ostalo nakon pada ili SIGKILL-a. Idempotentno — javlja „Ništa za popraviti” kada je stanje čisto. |
| **Remove CA**     | `DELETE /api/tools/agent-bridge/cert`  | Uklanja povjerenje i briše MITM root CA iz OS trust store-a (eksplicitno, idempotentno). Prikazuje se samo kada je CA trenutno pouzdan; zahtijeva inline potvrdu „Ukloni CA?”.                     |
| **Export config** | `GET /api/tools/agent-bridge/config`   | Preuzima prenosivi JSON konfiguracije (vidi §3.7).                                                                                                                                                 |
| **Import config** | `POST /api/tools/agent-bridge/config`  | Otprema prethodno izvezeni JSON konfiguracije (vidi §3.7).                                                                                                                                         |

**Dijagnostičke provjere** (`summarizeDiagnostics()` u `src/mitm/inspector/diagnostics.ts`). Ruta pokreće efektualnu sondu za svaku i unosi booleove vrijednosti u čisti summarizer; vraća se jedna `healthy` presuda plus savjet za svaki neuspjeh:

| Naziv provjere     | Šta verifikuje                                           | Savjet u slučaju neuspjeha                                                                                                                         |
| ------------------ | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server-running`   | Proces MITM servera je aktivan                           | "MITM server nije pokrenut. Pokrenite ga sa kartice AgentBridge."                                                                                  |
| `server-reachable` | MITM server prihvata konekcije na svom portu (TCP sonda) | "MITM server ne prihvata konekcije na svom portu. Provjerite da li je port slobodan i da li imate privilegije za njegovo vezivanje."               |
| `cert-exists`      | MITM certifikat je generisan na disku                    | "MITM certifikat još nije generisan. Generišite ga sa kartice AgentBridge."                                                                        |
| `cert-trusted`     | MITM root CA se nalazi u OS trust store-u                | "MITM root CA nije pouzdan od strane OS store-a, pa će TLS presretanje propasti. Vjerujte certifikatu sa kartice AgentBridge."                     |
| `dns-configured`   | Ciljana imena hostova su spoofovana u `/etc/hosts`       | "Ciljana imena hostova nisu spoofovana u /etc/hosts, pa saobraćaj nikada ne stiže do proxy-ja. Omogućite DNS za agenta(e) koje želite da snimite." |

**Baner za osirotjelo stanje:** kada stranica detektuje stanje koje je ostalo nakon pada (DNS spoof / CA / sistemski proxy), kartica prikazuje narandžasti baner — _"Prethodna sesija je ostavila sistemsko stanje (DNS spoof, CA ili sistemski proxy). Pokrenite Repair da ga očistite."_ — i ističe dugme **Repair**. `Repair` je analog aplikacionog sloja ProxyBridge-ovog `--cleanup` fleg-a (delegira na `repairMitm()` u `src/mitm/manager.ts`).

> MITM root CA ostaje instaliran tokom zaustavljanja/pokretanja kako bi se izbjegli ponovljeni sudo upiti (isto ponašanje kao kod mitmproxy/Charles), tako da je njegovo uklanjanje eksplicitna **Remove CA** radnja, a ne nešto što se dešava automatski pri zaustavljanju.

### 3.7 Portable config import/export

AgentBridge može serijalizovati stanje koje operater može podesiti u verzirani JSON blob tako da se podešavanje može replicirati na različitim mašinama. Serijalizator je `src/lib/inspector/configPortability.ts` (`exportConfig()` / `importConfig()`), validiran pomoću `AgentBridgeConfigSchema`.

Izvoz uključuje tačno tri dijela (ugrađene zadane vrijednosti namjerno NISU izvezene, tako da uvoz nikada ne duplicira niti se bori s njima):

| Polje            | Izvor                                                            | Napomene                                                                  |
| ---------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `bypassPatterns` | korisnički definisani bypass uzorci (`agent_bridge_bypass`)      | zadani bank/gov/okta uzorci su isključeni                                 |
| `customHosts`    | Traffic Inspector prilagođeni hostovi (`inspector_custom_hosts`) | svaki: `{ host, kind: "llm"\|"app"\|"custom", label? }`                   |
| `agentMappings`  | mapiranja modela po agentu (`agent_bridge_mappings`)             | `{ [agentId]: [{ source, target }] }` za svakog agenta koji ima mapiranja |

```jsonc
// GET /api/tools/agent-bridge/config
{
  "version": 1,
  "bypassPatterns": ["*.internal.example.com"],
  "customHosts": [{ "host": "api.example.com", "kind": "llm", "label": null }],
  "agentMappings": {
    "copilot": [{ "source": "gpt-4o", "target": "claude-sonnet-4.7" }],
  },
}
```

**Ponašanje uvoza** (`POST /api/tools/agent-bridge/config`): bypass uzorci i mapiranja po agentu se zamjenjuju u potpunosti; prilagođeni hostovi se dodaju idempotentno (`INSERT OR IGNORE`). Odgovor izvještava koliko je svakog primijenjeno:

```jsonc
{ "ok": true, "bypassPatterns": 1, "customHosts": 1, "agents": 1 }
```

Šta **NIJE** u konfiguraciji: status rada servera, putanje certifikata, DNS status po agentu, putanja do upstream CA i TPROXY postavke — to su stanje hosta/runtime-a, a ne prenosive postavke.

---

## §4 Referenca po agentu

| #   | Agent              | Status          | Presretnuti hostovi                                                | Tip autentifikacije |
| --- | ------------------ | --------------- | ------------------------------------------------------------------ | ------------------- |
| 1   | **Antigravity**    | ✅ Podržano     | `daily-cloudcode-pa.googleapis.com`, `cloudcode-pa.googleapis.com` | Firebase OAuth      |
| 2   | **Kiro (AWS)**     | ✅ Podržano     | `prod.kiro.aws`, `dev.kiro.aws`                                    | AWS SigV4           |
| 3   | **GitHub Copilot** | ✅ Podržano     | `api.githubcopilot.com`, `copilot-proxy.githubusercontent.com`     | GitHub OAuth        |
| 4   | **OpenAI Codex**   | ✅ Podržano     | `api.openai.com` (Codex putanje), `chatgpt.com`                    | OpenAI ključ        |
| 5   | **Cursor IDE**     | ✅ Podržano     | `api2.cursor.sh`, `api.cursor.sh`                                  | Cursor OAuth        |
| 6   | **Zed Industries** | ✅ Podržano     | `api.zed.dev`, `llm.zed.dev`                                       | Zed OAuth           |
| 7   | **Claude Code**    | ✅ Podržano     | `api.anthropic.com` (opt-in)                                       | Anthropic ključ     |
| 8   | **Open Code**      | ✅ Podržano     | `openrouter.ai`, `api.openai.com` (zen putanje)                    | API ključ           |
| 9   | **Trae**           | 🔍 Istražuje se | TBD — pogledajte §8                                                | TBD                 |

### Koraci čarobnjaka za podešavanje (po agentu)

Svaka kartica agenta ima čarobnjak za podešavanje u 3 koraka:

1. **Provjera preduslova** — Server radi? Certifikat pouzdan? IDE instaliran (automatski otkriven)?
2. **Omogući DNS** — Dodaje unose u `/etc/hosts` (zahtijeva sudo). Prikazuje tačno koje linije će biti dodate.
3. **Mapiraj modele** — Opciona tabela za mapiranje modela. Džokeri (wildcards) su prihvaćeni.

### Detekcija agenta

Za agente 1–8, AgentBridge pokušava automatski otkriti instalaciju IDE-a:

```ts
export async function detectAgent(agentId: AgentId): Promise<DetectionResult>;
// Vraća: { installed: boolean, version?: string, path?: string }
```

Detekcija koristi putanje specifične za OS i binarne provjere (npr. `code --list-extensions | grep github.copilot` za Copilot, `~/.config/antigravity/` za Antigravity).

---

## §5 Sigurnost

### Primijenjena stroga pravila

| Pravilo                           | Primjena                                                                                                 |
| --------------------------------- | -------------------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`    | Sve greške rukovaoca (handler) se sanitizuju prije odgovora ili unosa u bafer                            |
| **#13** Shell env-passing         | Izmjene `/etc/hosts` koriste `env` opciju — nema interpolacije stringova putanja                         |
| **#15 + #17** `isLocalOnlyPath()` | `/api/tools/agent-bridge/` je LOCAL_ONLY + SPAWN_CAPABLE — loopback se primjenjuje prije autentifikacije |

### Lista zaobilaženja za osjetljive hostove

Lista zaobilaženja osigurava da se finansijske institucije, OAuth/SSO provajderi i drugi osjetljivi hostovi **nikada ne dekriptuju**. Njihov TLS saobraćaj prolazi kao transparentni TCP tunel — OmniRoute nikada ne vidi običan tekst (plaintext).

Podrazumijevani obrasci za zaobilaženje uključuju:

- `*.bank.*`, `*.gov.*` (finansijski/vladini)
- `*.okta.com`, `*.auth0.com`, `*.microsoft.com` (SSO/identitet)
- `*.apple.com`, `*.icloud.com` (Apple sistemski servisi)

Obrasci za zaobilaženje koje dodaju korisnici se čuvaju u tabeli `agent_bridge_bypass` i imaju prioritet nad svime ostalim.

### Maskiranje tajni

`maskSecrets()` iz `src/mitm/maskSecrets.ts` se primjenjuje:

- Na svako tijelo zahtjeva prije `TrafficBuffer.push()`
- Na svako zaglavlje prije logovanja ili emitovanja

Obrasci: tokeni sa prefiksom `sk-`/`ak-`/`pk-`, `Bearer` tokeni i generički tokeni ≥40 karaktera.

### Upstream CA certifikat

Kada je `AGENTBRIDGE_UPSTREAM_CA_CERT` postavljen, fajl se čita pri pokretanju. Ako putanja postoji, ali fajl nije čitljiv, AgentBridge loguje jasnu grešku i odbija da se pokrene (sprječava tihe TLS greške u korporativnim okruženjima).

### Poznata ograničenja

- **Port 443 zahtijeva privilegije**: Na Linuxu, AgentBridge treba `setcap 'cap_net_bind_service=+ep'` na Node binarnom fajlu, ili pokretanje putem `authbind`. Čarobnjak za podešavanje prikazuje uputstva specifična za OS.
- **Potrebno ponovno pokretanje IDE-a**: Nakon DNS preusmjeravanja, IDE se mora ponovo pokrenuti da bi nova rezolucija hosta stupila na snagu.
- **Hardkodirani OAuth tokeni**: Neki agenti (Kiro, Antigravity) čuvaju OAuth refresh tokene lokalno. Oni su transparentni za AgentBridge — on vidi `Bearer` token u svakom zahtjevu, koji se maskira prije logovanja.
- **Electron frontendi trebaju `NODE_EXTRA_CA_CERTS`**: IDE-ovi čiji frontend radi na ugrađenom Node/Electron runtime-u ignorišu OS/NSS trust store i moraju se pokrenuti iz shell-a sa postavljenim `NODE_EXTRA_CA_CERTS` (pogledajte §3.2). Simptom kada nedostaje: IDE backend se autentifikuje (MITM pokazuje `200`s), ali UI ostaje odjavljen.
- **Višestruke instalacije istog IDE-a su nezavisne**: sistemska instalacija (npr. `/usr/share/antigravity/antigravity`) i korisnička "Full" instalacija (npr. `~/AntigravityIDE_Full/antigravity-ide`) su odvojeni procesi sa sopstvenim runtime-ovima — svaki se mora ponovo pokrenuti sa injektovanim CA. Identifikujte koji je pokrenut prema njegovoj binarnoj putanji prije ponovnog pokretanja.
- **Identitet postavlja sistemski prompt agenta, a ne rutirani model**: kada remapirate model agenta na drugog provajdera, odgovor i dalje tvrdi da je to izvorni identitet agenta (npr. Antigravity odgovara "I am powered by Gemini") jer IDE to ubacuje u sistemski prompt. Potvrdite stvarni backend u `call_logs` / `proxy_logs` (`provider`, `model`, `target_format`), a ne pitajući model ko je on.

---

## §6 Rješavanje problema

### Konflikt porta 443

Ako neki drugi proces već sluša na portu 443 (web server, VPN, itd.):

```bash
lsof -i :443          # pronađi proces
sudo fuser -k 443/tcp  # prisilno zaustavi (koristi s oprezom)
```

Alternativno, konfigurišite neprivilegovani port u postavkama AgentBridge-a i podesite `iptables` / `pf` pravila preusmjeravanja.

### Certifikat nije pouzdan

Ako IDE prikazuje TLS greške nakon pokretanja AgentBridge-a:

1. Provjerite da li je certifikat instaliran: `security find-certificate -c "OmniRoute AgentBridge"` (macOS) ili `certutil -L -d sql:$HOME/.pki/nssdb` (Linux/NSS)
2. Neke aplikacije održavaju vlastito skladište povjerenja (Firefox, Chrome na Linuxu). Ponovo pokrenite „Trust Cert“ i provjerite NSS/Firefox-specifično skladište certifikata.
3. Ponovo pokrenite IDE nakon dodavanja povjerenja — TLS sesije u toku koriste staro stanje povjerenja.

### IDE odjavljen / "greška u povezivanju" uprkos pouzdanom CA

Simptom: nakon preusmjeravanja DNS-a i dodavanja povjerenja CA, IDE zasnovan na Electronu (npr. Antigravity) se otvara **odjavljen** ili prikazuje grešku autentifikacije/povezivanja, iako MITM log pokazuje da bootstrap pozivi (`loadCodeAssist`, `fetchAvailableModels`, …) vraćaju `200`.

Uzrok: **ugrađeni Node/Electron runtime** IDE-a ignoriše skladište povjerenja operativnog sistema. Izvorni backend (Go jezički server) vjeruje OS CA i autentifikuje se, ali Electron frontend ne — pa UI vjeruje da je van mreže.

Rješenje (oba koraka): eksportujte `NODE_EXTRA_CA_CERTS=<ca.crt>` **i ponovo pokrenite IDE iz te ljuske (shell)**, a ne sa ikone na radnoj površini. Prvo potpuno zatvorite IDE — Electron-ovo zaključavanje singleton-a znači da drugo pokretanje samo fokusira postojeći proces i novo okruženje se ignoriše. Pogledajte §3.2. Ovo odražava otvoreni upstream izvještaj gdje samostalni agent radi kroz MITM, ali IDE varijanta ne uspijeva pod istim postavkama.

### DNS nije propagiran

Provjerite da li je `/etc/hosts` ažuriran:

```bash
grep "omniroute\|127.0.0.1.*github\|127.0.0.1.*cursor" /etc/hosts
```

Ispraznite DNS keš:

```bash
# macOS
sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder
# Linux (systemd-resolved)
sudo systemctl restart systemd-resolved
# Windows
ipconfig /flushdns
```

### IDE nije detektovan

Automatska detekcija koristi uobičajene putanje instalacije. Ako detekcija ne uspije, a IDE je instaliran:

- Provjerite da li se IDE binarna datoteka nalazi na nestandardnoj lokaciji
- Čarobnjak za podešavanje (Setup Wizard) i dalje radi — neuspjeh detekcije samo znači da značka neće prikazati putanju instalacije

### Greške rukovaoca (upstream fetch ne uspijeva)

Ako AgentBridge presreće, ali svi zahtjevi ne uspijevaju:

1. Provjerite da li je barem jedan provajder povezan na `/dashboard/providers`
2. Provjerite logove OmniRoute servera: `APP_LOG_LEVEL=debug` u `.env`
3. Provjerite da li `OMNIROUTE_BASE_URL` pokazuje na ispravnu krajnju tačku rutera (podrazumijevano: `http://127.0.0.1:20128`)

---

## §7 API referenca

Sve rute su `LOCAL_ONLY` (samo loopback, primjenjuje se prije autentifikacije) i `SPAWN_CAPABLE`. Pogledajte `src/server/authz/routeGuard.ts`.

Osnovna putanja: `/api/tools/agent-bridge/`

| Metoda              | Putanja                                        | Opis                                                                                                                                 |
| ------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| GET                 | `/api/tools/agent-bridge/state`                | Globalno stanje servera + detekcija/status po agentu                                                                                 |
| GET                 | `/api/tools/agent-bridge/agents`               | Lista registrovanih agenata (id, name, hosts, viability, state)                                                                      |
| GET                 | `/api/tools/agent-bridge/agents/{id}`          | Stanje jednog agenta (target config + detection + stored state)                                                                      |
| PATCH               | `/api/tools/agent-bridge/agents/{id}`          | Ažuriraj `setup_completed` za agenta                                                                                                 |
| GET                 | `/api/tools/agent-bridge/agents/{id}/detect`   | Pokreni detekcijsku sondu za agenta (installed, version?, path?)                                                                     |
| POST                | `/api/tools/agent-bridge/agents/{id}/dns`      | Omogući/onemogući DNS za agenta ({enabled: boolean})                                                                                 |
| GET                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Mapiranja modela za agenta                                                                                                           |
| PUT                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Zamijeni mapiranja modela                                                                                                            |
| POST                | `/api/tools/agent-bridge/server`               | Pokreni/zaustavi/restartuj server (action: "start"\|"stop"\|"restart"\|"trust-cert"\|"regenerate-cert")                              |
| GET                 | `/api/tools/agent-bridge/cert`                 | Status certifikata (exists, trusted, path)                                                                                           |
| POST                | `/api/tools/agent-bridge/cert`                 | Vjeruj (instaliraj) MITM root CA                                                                                                     |
| DELETE              | `/api/tools/agent-bridge/cert`                 | Ukloni povjerenje (ukloni) MITM root CA — idempotentno (vidi §3.6)                                                                   |
| POST                | `/api/tools/agent-bridge/cert/regenerate`      | Regeneriši samopotpisani MITM certifikat                                                                                             |
| GET                 | `/api/tools/agent-bridge/cert/download`        | Strimuj PEM certifikat za preuzimanje                                                                                                |
| GET                 | `/api/tools/agent-bridge/bypass`               | Lista obrazaca za zaobilaženje (default + user)                                                                                      |
| POST                | `/api/tools/agent-bridge/bypass`               | Zamijeni korisnički definisane obrasce za zaobilaženje u potpunosti                                                                  |
| DELETE              | `/api/tools/agent-bridge/bypass?pattern=...`   | Ukloni jedan korisnički definisan obrazac za zaobilaženje                                                                            |
| GET                 | `/api/tools/agent-bridge/diagnose`             | Samotestiranje cjevovoda za snimanje (vidi §3.6)                                                                                     |
| POST                | `/api/tools/agent-bridge/repair`               | Poništi osirotjelo stanje MITM sistema (vidi §3.6)                                                                                   |
| GET                 | `/api/tools/agent-bridge/config`               | Izvezi prenosivi konfiguracioni JSON (vidi §3.7)                                                                                     |
| POST                | `/api/tools/agent-bridge/config`               | Uvezi prenosivi konfiguracioni JSON (vidi §3.7)                                                                                      |
| GET                 | `/api/tools/agent-bridge/upstream-ca`          | Dobavi konfigurisanu putanju do upstream CA                                                                                          |
| POST                | `/api/tools/agent-bridge/upstream-ca`          | Validiraj + sačuvaj putanju do upstream CA                                                                                           |
| POST                | `/api/tools/agent-bridge/upstream-ca/test`     | Samo validacija (dry-run) putanje do upstream CA — ne čuva se                                                                        |
| GET / POST / DELETE | `/api/tools/agent-bridge/tproxy`               | TPROXY režim snimanja sa transparentnom dekripcijom — vidi `docs/security/MITM-TPROXY-DECRYPT.md` (git; nije kompajlirano u `/docs`) |

Puna OpenAPI šema: `docs/openapi.yaml` → oznaka `AgentBridge`.

---

## §8 Plan razvoja

### Istraživanje Trae

Trae je relativno novi AI asistent za kodiranje. Prije implementacije handlera:

1. Identifikujte binarnu datoteku/ekstenziju na VS Code / JetBrains marketima ili kao samostalnu aplikaciju
2. Uhvatite saobraćaj pomoću mitmproxy kako biste otkrili API hostove i oblike endpointa
3. Odredite mehanizam autentifikacije
4. Procijenite go/no-go na osnovu TOS-a i mogućnosti otkrivanja API-ja

Dok se istraživanje ne završi, kartica Trae na kontrolnoj tabli prikazuje značku "Investigating" sa linkom "Report viability". Handler stub na `src/mitm/handlers/trae.ts` baca strukturiranu grešku `Not yet implemented`.

### Backlog agenti (potreban MITM — nema podrške za prilagođeni base URL)

Sljedeći alati ne podržavaju prilagođene base URL-ove u svojim trenutnim verzijama, što čini MITM jedinom putanjom za presretanje. Procjena izvodljivosti je na čekanju:

- **Windsurf** (Codeium/Cognition)
- **Amp** (Sourcegraph)
- **Amazon Q / Kiro CLI** (AWS Bedrock — odvojeno od Kiro IDE)
- **Cowork** (Anthropic desktop)

Napomena: GitHub Copilot CLI ≥v1.0.19 podržava `COPILOT_PROVIDER_BASE_URL` — koristite direktnu konfiguraciju umjesto MITM-a za taj alat.
