# Claude Code CLI — Configuration with OmniRoute (Bosanski)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

# Claude Code CLI — Konfiguracija sa OmniRoute

Usmjerite **Claude Code** CLI (`claude`) na OmniRoute — lokalni ili udaljeni VPS — sa profilima po modelu, oponašajući Codex postavke.

---

## Brzi početak

```bash
# Pokrenite Claude Code protiv lokalnog OmniRoute-a (automatski detektuje aktivni kontekst)
omniroute launch

# Protiv udaljenog OmniRoute-a (nakon `omniroute connect <host>`, ovo je automatski)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Generišite profile po modelu, a zatim pokrenite jedan
omniroute setup-claude            # upisuje ~/.claude/profiles/<name>/settings.json
omniroute launch --profile glm52  # Claude Code koristi glm/glm-5.2 putem OmniRoute-a
```

---

## Kako se Claude Code povezuje na gateway

Claude Code komunicira putem **Anthropic Messages API**-ja i usmjeren je na prilagođenu krajnju tačku (endpoint) pomoću varijabli okruženja (nema `--base-url` zastavicu):

| Varijabla                                    | Svrha                                                                                              |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `ANTHROPIC_BASE_URL`                         | Root URL gateway-a (Claude Code dodaje `/v1/messages`). **Bez `/v1` sufiksa.**                     |
| `ANTHROPIC_AUTH_TOKEN`                       | Šalje se kao `Authorization: Bearer …` — koristite svoj OmniRoute pristupni token / API ključ      |
| `ANTHROPIC_API_KEY`                          | Alternativa: šalje se kao `x-api-key`. Ako su oba postavljena, `ANTHROPIC_AUTH_TOKEN` ima prednost |
| `ANTHROPIC_MODEL`                            | Forsirajte određeni model (nadjačava zadani `/model` birač)                                        |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | `1` → izvorni `/model` birač navodi `claude*`/`anthropic*` modele iz `/v1/models`                  |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | Ograničite izlazne tokene po odgovoru (npr. `65536`)                                               |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | Prag tokena za automatsko sažimanje                                                                |

> Varijable okruženja se čitaju **jednom pri pokretanju** — ponovo pokrenite Claude Code nakon što ih promijenite.

`omniroute launch` postavlja sve ovo za vas: razrješava base URL + token iz aktivnog konteksta (tako da `omniroute connect <vps>` pa `omniroute launch` jednostavno radi), provjerava zdravlje servera i izvršava `claude`.

---

## Alias-i za otkrivanje — prikažite modele koji nisu Claude u `/model` biraču

Otkrivanje modela gateway-a u Claude Code-u navodi samo ID-ove koji počinju sa `claude` ili `anthropic`, pa sa `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` izvorni `/model` birač obično prikazuje **samo** OmniRoute Claude/Anthropic modele — `kimi/kimi-k2.6` ili `glm/glm-5.2` se nikada ne pojavljuju, iako se ispravno rutiraju.

OmniRoute može preslikati bilo koji omogućeni model (i kombinaciju) pod `claude/…` ID-om tako da prođe taj filter i pojavi se u biraču:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<combo "custo-otimizado"> →  claude/combo/custo-otimizado
```

Kada odaberete jedan od ovih u Claude Code-u, OmniRoute uklanja `claude/` omotač nazad na pravi ID prije rutiranja — originalni `claude/<real-claude-model>` ID (stvarni Claude OAuth provajder) uvijek ostaje netaknut.

**Ovo je po defaultu isključeno** i kontrolisano je troslojnom kapijom (najspecifičnije pobjeđuje), tako da običan OmniRoute nikada ne duplira svoj katalog za klijente koji ne koriste Claude Code:

| Nivo      | Gdje                                                                                         |
| --------- | -------------------------------------------------------------------------------------------- |
| Model     | Stranica sa detaljima provajdera → prekidač "Expose in Claude Code" po modelu                |
| Provajder | Stranica sa detaljima provajdera → prekidač na nivou provajdera (pokriva sve njegove modele) |
| Globalno  | Postavke → Feature Flags → `EXPOSE_CC_DISCOVERY_ALIASES` (default isključeno)                |

Varijabla okruženja `EXPOSE_CC_DISCOVERY_ALIASES` forsira globalni nivo na uključeno i ima prednost nad nadjačavanjem na kontrolnoj tabli (ekran Feature Flags prikazuje napomenu "active via environment variable" kada se to desi). Prekidači po provajderu i po modelu se odatle preciziraju — npr. globalno isključeno + Kimi provajder uključen izlaže samo Kimi modele.

> ⚠️ **Nepodudaranje prozora na modelima koji nisu Claude.** Claude Code pretpostavlja prozor konteksta od 200K za bilo koji ID koji ne prepoznaje (ne može pročitati stvarni prozor iz `/v1/models`). Za model sa većim prozorom (npr. Kimi K2 od 256K), postavite `CLAUDE_CODE_AUTO_COMPACT_WINDOW` na vrijednost ispod stvarnog prozora modela kako se automatsko sažimanje ne bi prerano aktiviralo. Generisani profili iznad to već rade po modelu.

---

## Blok za onboarding na kontrolnoj tabli

Claude alatna kartica (**Dashboard → CLI Code**) prikazuje tačan `settings.json` fragment za ovu instancu, pored dugmeta za informacije o discovery-alias, sa dugmetom za kopiranje:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<your OmniRoute>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<your OmniRoute API key>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

Osnovni URL je onaj koji je kartica razriješila (uključujući prilagođeno preusmjeravanje koje ste unijeli), već normalizovan — bez `/v1` sufiksa, bez kose crte na kraju. **Ključ se nikada ne prikazuje**: blok isporučuje placeholder, tako da snimak ekrana ili zalijepljeni isječak ne mogu otkriti ključ. Zalijepite svoj ključ preko njega.

Dodajte `CLAUDE_CODE_AUTO_COMPACT_WINDOW` ispod istog `env` bloka za bilo koji model čiji stvarni prozor konteksta nije 200K — Claude Code pretpostavlja 200K za svaki ID koji ne prepoznaje, pa se automatsko sažimanje u suprotnom aktivira u pogrešnom trenutku (pogledajte upozorenje u prethodnom odjeljku). Generator isječaka takođe prihvata tu vrijednost, tako da pozivalac koji zna prozor ciljnog modela može direktno da ga emituje.

Izvor: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (čisti generator, jedinično testiran) kojeg renderuje `ClaudeGatewayOnboardingBlock`.

---

## Profili (CLAUDE_CONFIG_DIR)

Claude Code **nema izvorne datoteke profila** (za razliku od Codex-ovog `~/.codex/<name>.config.toml`). Idiomatski mehanizam je `CLAUDE_CONFIG_DIR` — poseban konfiguracioni direktorij po profilu, svaki sa svojim `settings.json`, akreditivima, historijom i kešom.

`omniroute setup-claude` preuzima katalog `/v1/models` uživo i piše jedan profil po modelu na `~/.claude/profiles/<name>/settings.json`, ponovo koristeći **ista imena kao `setup-codex`** (`glm52`, `kimi-k27`, `deepseek-pro`, …):

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **Autentifikacioni token se nikada ne upisuje u profil.** Pokrenite sa
> `omniroute launch --profile <name>` (on ubacuje `ANTHROPIC_AUTH_TOKEN` iz aktivnog konteksta), ili sami izvezite `ANTHROPIC_AUTH_TOKEN` i pokrenite `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude`.

**Automatska sinhronizacija nakon otkrivanja modela (opciono).** OmniRoute može automatski regenerisati ove iste `~/.claude/profiles/<name>/settings.json` datoteke kad god sinhronizacija modela provajdera promijeni katalog uživo — tako da novi/preimenovani modeli dobijaju profile bez ponovnog pokretanja komande. **Podrazumijevano je isključeno**: uključite ga sa **CLI Code kontrolne table** ("CLI profile auto-sync" → Claude Code), ili postavite `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` (takođe poštuje `CLI_ALLOW_CONFIG_WRITES`, što je podrazumijevano uključeno). Kada je omogućeno, samo upisuje datoteke profila; nikada ne mijenja vašu aktivnu/podrazumijevanu Claude konfiguraciju, autentifikaciju ili `~/.claude/settings.json`.

### Generisanje + korištenje profila

```bash
# Lokalni OmniRoute
omniroute setup-claude

# Udaljeni VPS (ugrađuje VPS URL u svaki profil)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Samo neki provajderi
omniroute setup-claude --only glm,kimi

# Pregled bez upisivanja
omniroute setup-claude --dry-run

# Pokretanje profila
omniroute launch --profile kimi-k27
```

---

## Nivoi modela (opciono)

Claude Code usmjerava na nivoe sposobnosti. Mapirajte svaki na OmniRoute model putem env / postavki ako želite različite provajdere po nivou:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

U suprotnom, jedan `ANTHROPIC_MODEL` (ono što profili postavljaju) se koristi za sve.

---

## Udaljeni režim

Nakon što pokrenete `omniroute connect <host>` (pogledajte [Udaljeni režim](./REMOTE-MODE.md)), `omniroute launch` i `omniroute setup-claude` automatski ciljaju taj udaljeni server i koriste njegov pristupni token sa ograničenim opsegom — nisu potrebni dodatni flagovi. Nadjačajte po pozivu sa `--remote` / `--api-key`.

---

## Rješavanje problema

**Claude Code ignoriše gateway** — potvrdite da `ANTHROPIC_BASE_URL` nema **`/v1`** i ponovo pokrenite `claude` (env se čita jednom pri pokretanju). `omniroute launch` ovo rješava umjesto vas.

**Birač `/model` je prazan / nedostaju gateway modeli** — zahtijeva Claude Code v2.1.219+ i `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`. Samo `claude*` / `anthropic*` ID-ovi modela se pojavljuju u biraču; prisilite bilo koji drugi model sa `ANTHROPIC_MODEL=<id>` (ovo je ono što profili rade).

**`400 Ambiguous model 'claude-…'`** — Claude Code uvijek šalje **neprefiksirane** ID-ove modela (npr. `claude-opus-4-8`), pa kada su povezani i Claude Code (`cc/…`) i Claude (`claude/…`) provajderi, goli ID odgovara dvjema rutama i OmniRoute odbija nagađati. Popravite to na bilo koji način: fiksirajte prefiksirani ID sa `ANTHROPIC_MODEL=cc/claude-opus-4-8`, ili omogućite **Prefer Claude Code for unprefixed Claude models** — prekidač na stranici Claude provajdera, ili `OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` (podrazumijevano isključeno; pogledajte [Okruženje](../reference/ENVIRONMENT.md)) — što usmjerava gole `claude-*` ID-ove na Claude Code umjesto toga. Eksplicitni prefiksi provajdera uvijek pobjeđuju.

**Greške pri autentifikaciji** — profil ne sadrži token. Koristite `omniroute launch --profile` (ubacuje ga) ili eksportujte `ANTHROPIC_AUTH_TOKEN`.

**Profili ne izoluju** — svaki profil je poseban `CLAUDE_CONFIG_DIR`; provjerite da li `echo $CLAUDE_CONFIG_DIR` unutar sesije pokazuje na `~/.claude/profiles/<name>`.
