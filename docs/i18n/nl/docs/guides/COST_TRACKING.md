# Cost & Spend Tracking (Nederlands)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/COST_TRACKING.md) · 🇪🇹 [am](../../../am/docs/guides/COST_TRACKING.md) · 🇸🇦 [ar](../../../ar/docs/guides/COST_TRACKING.md) · 🇦🇿 [az](../../../az/docs/guides/COST_TRACKING.md) · 🇧🇬 [bg](../../../bg/docs/guides/COST_TRACKING.md) · 🇧🇩 [bn](../../../bn/docs/guides/COST_TRACKING.md) · 🇧🇦 [bs](../../../bs/docs/guides/COST_TRACKING.md) · 🇨🇿 [cs](../../../cs/docs/guides/COST_TRACKING.md) · 🇩🇰 [da](../../../da/docs/guides/COST_TRACKING.md) · 🇩🇪 [de](../../../de/docs/guides/COST_TRACKING.md) · 🇬🇷 [el](../../../el/docs/guides/COST_TRACKING.md) · 🇪🇸 [es](../../../es/docs/guides/COST_TRACKING.md) · 🇪🇪 [et](../../../et/docs/guides/COST_TRACKING.md) · 🇮🇷 [fa](../../../fa/docs/guides/COST_TRACKING.md) · 🇫🇮 [fi](../../../fi/docs/guides/COST_TRACKING.md) · 🇫🇷 [fr](../../../fr/docs/guides/COST_TRACKING.md) · 🇮🇪 [ga](../../../ga/docs/guides/COST_TRACKING.md) · 🇮🇳 [gu](../../../gu/docs/guides/COST_TRACKING.md) · 🇳🇬 [ha](../../../ha/docs/guides/COST_TRACKING.md) · 🇮🇱 [he](../../../he/docs/guides/COST_TRACKING.md) · 🇮🇳 [hi](../../../hi/docs/guides/COST_TRACKING.md) · 🇭🇷 [hr](../../../hr/docs/guides/COST_TRACKING.md) · 🇭🇺 [hu](../../../hu/docs/guides/COST_TRACKING.md) · 🇦🇲 [hy](../../../hy/docs/guides/COST_TRACKING.md) · 🇮🇩 [id](../../../id/docs/guides/COST_TRACKING.md) · 🇳🇬 [ig](../../../ig/docs/guides/COST_TRACKING.md) · 🇮🇹 [it](../../../it/docs/guides/COST_TRACKING.md) · 🇯🇵 [ja](../../../ja/docs/guides/COST_TRACKING.md) · 🇬🇪 [ka](../../../ka/docs/guides/COST_TRACKING.md) · 🇰🇭 [km](../../../km/docs/guides/COST_TRACKING.md) · 🇮🇳 [kn](../../../kn/docs/guides/COST_TRACKING.md) · 🇰🇷 [ko](../../../ko/docs/guides/COST_TRACKING.md) · 🇱🇹 [lt](../../../lt/docs/guides/COST_TRACKING.md) · 🇱🇻 [lv](../../../lv/docs/guides/COST_TRACKING.md) · 🇮🇳 [ml](../../../ml/docs/guides/COST_TRACKING.md) · 🇮🇳 [mr](../../../mr/docs/guides/COST_TRACKING.md) · 🇲🇾 [ms](../../../ms/docs/guides/COST_TRACKING.md) · 🇲🇹 [mt](../../../mt/docs/guides/COST_TRACKING.md) · 🇲🇲 [my](../../../my/docs/guides/COST_TRACKING.md) · 🇳🇵 [ne](../../../ne/docs/guides/COST_TRACKING.md) · 🇳🇴 [no](../../../no/docs/guides/COST_TRACKING.md) · 🇮🇳 [or](../../../or/docs/guides/COST_TRACKING.md) · 🇮🇳 [pa](../../../pa/docs/guides/COST_TRACKING.md) · 🇵🇭 [phi](../../../phi/docs/guides/COST_TRACKING.md) · 🇵🇱 [pl](../../../pl/docs/guides/COST_TRACKING.md) · 🇵🇹 [pt](../../../pt/docs/guides/COST_TRACKING.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/COST_TRACKING.md) · 🇷🇴 [ro](../../../ro/docs/guides/COST_TRACKING.md) · 🇷🇺 [ru](../../../ru/docs/guides/COST_TRACKING.md) · 🇱🇰 [si](../../../si/docs/guides/COST_TRACKING.md) · 🇸🇰 [sk](../../../sk/docs/guides/COST_TRACKING.md) · 🇸🇮 [sl](../../../sl/docs/guides/COST_TRACKING.md) · 🇷🇸 [sr](../../../sr/docs/guides/COST_TRACKING.md) · 🇸🇪 [sv](../../../sv/docs/guides/COST_TRACKING.md) · 🇰🇪 [sw](../../../sw/docs/guides/COST_TRACKING.md) · 🇮🇳 [ta](../../../ta/docs/guides/COST_TRACKING.md) · 🇮🇳 [te](../../../te/docs/guides/COST_TRACKING.md) · 🇹🇭 [th](../../../th/docs/guides/COST_TRACKING.md) · 🇹🇷 [tr](../../../tr/docs/guides/COST_TRACKING.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/COST_TRACKING.md) · 🇵🇰 [ur](../../../ur/docs/guides/COST_TRACKING.md) · 🇺🇿 [uz](../../../uz/docs/guides/COST_TRACKING.md) · 🇻🇳 [vi](../../../vi/docs/guides/COST_TRACKING.md) · 🇳🇬 [yo](../../../yo/docs/guides/COST_TRACKING.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/COST_TRACKING.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/COST_TRACKING.md)

---

Hoe OmniRoute de kosten van elk verzoek schat, registreert en rapporteert — en waarom het
getal op het dashboard een **besparingstracker** is en geen factuur.

Zie ook: [Gebruikershandleiding](./USER_GUIDE.md) · [Functieoverzicht](./FEATURES.md)

---

## Wat het is (en wat het niet is)

OmniRoute kent aan elke voltooiing kosten per verzoek in USD toe door de aantallen tokens
te vermenigvuldigen met de tarieven van een model. Deze cijfers vormen de basis voor het
dashboard **Kosten**, de CLI-opdrachten `omniroute cost` / `omniroute usage`, CSV-/JSON-exports
en budgetten per API-sleutel.

> **De 'kosten' op het dashboard zijn een besparingstracker, geen factuur.** OmniRoute brengt
> je nooit kosten in rekening — het routeert je verzoeken naar providers die je al hebt
> gekoppeld (je eigen abonnementen, gratis niveaus en API-sleutels). Als er uitsluitend via
> gratis modellen "$290 totale kosten" zijn opgebouwd, betekent dit dat je ongeveer **$290
> _niet_ aan een betaalde API hebt uitgegeven**. Het bedrag is een _schatting_ van wat
> hetzelfde verkeer tegen standaardlijstprijzen zou hebben gekost, zodat je kunt zien waar
> je gebruik zich concentreert en hoeveel je bespaart door naar goedkopere/gratis providers
> te routeren.

Deze duiding wordt rechtstreeks vermeld in de [README](../../README.md) van het project
("de 'kosten' op het dashboard zijn een besparingstracker, geen factuur").

Omdat het bedrag een schatting is:

- Het hangt af van de prijstabel die OmniRoute voor elk model heeft. Een model zonder
  prijsvermelding draagt `0` aan de kosten bij (het wordt in de verkenner weergegeven als
  een rij "Verouderd / Gratis").
- Verkeer via gratis niveaus en abonnementen bouwt nog steeds _geschatte_ kosten op — dat
  is het bedrag dat je bespaart, niet een bedrag dat je verschuldigd bent.

---

## Hoe kosten worden geschat

### De prijsbron

Kosten zijn afkomstig uit een prijstabel die in deze volgorde van prioriteit wordt bepaald
([`src/lib/pricingSync.ts`](../../src/lib/pricingSync.ts)):

1. **Gebruikersoverschrijvingen** — prijzen die je instelt op het dashboard / via `PATCH /api/pricing`.
2. **Gesynchroniseerde externe prijzen** — opgehaald uit LiteLLM's openbare
   `model_prices_and_context_window.json` wanneer synchronisatie is ingeschakeld (opgeslagen
   in een afzonderlijke `pricing_synced`-naamruimte, zodat je overschrijvingen nooit worden
   overschreven).
3. **Hardgecodeerde standaardwaarden** — meegeleverd met OmniRoute.

Externe prijssynchronisatie is **optioneel** en standaard uitgeschakeld. Relevante
omgevingsvariabelen (zie [`.env.example`](../../.env.example)):

| Omgevingsvariabele      | Standaard | Doel                                                                                      |
| ----------------------- | --------- | ----------------------------------------------------------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`   | Schakel de LiteLLM-prijssynchronisatie op de achtergrond in bij het opstarten.            |
| `PRICING_SYNC_INTERVAL` | `86400`   | Synchronisatie-interval in **seconden** (standaard dagelijks).                            |
| `PRICING_SYNC_SOURCES`  | `litellm` | Door komma's gescheiden lijst met bronnen (momenteel wordt alleen `litellm` ondersteund). |

### De kostenformule

De kosten worden per verzoek berekend op basis van aantallen tokens en tarieven per miljoen
tokens in [`src/lib/usage/costCalculator.ts`](../../src/lib/usage/costCalculator.ts)
(`computeCostFromPricing` / `calculateCost`):

- **Invoertokens** (minus cachelezingen en tokens voor het aanmaken van de cache) × `input`-tarief.
- **Tokens uit cachelezingen** × `cached`-tarief (valt terug op het invoertarief).
- **Tokens voor het aanmaken van de cache** × `cache_creation`-tarief (valt terug op het invoertarief).
- **Uitvoertokens** × `output`-tarief.
- **Redeneertokens** × `reasoning`-tarief (valt terug op het uitvoertarief).

Alle tarieven worden geïnterpreteerd als USD per 1.000.000 tokens. Een Codex-serviceniveau
"fast"/"priority" of "flex" past een kostenvermenigvuldiger toe
(`getCodexFastCostMultiplier`) — flex wordt bijvoorbeeld gefactureerd met 50% korting op
tokens, wat op het dashboard wordt weergegeven als **flexbesparingen**.

Modelnamen worden eerst genormaliseerd (providerpadvoorvoegsels zoals `openai/` of
`accounts/fireworks/models/` worden verwijderd), zodat historische rijen nog steeds aan
een prijs kunnen worden gekoppeld.

### Hoe uitgaven worden geregistreerd

- De kosten per verzoek worden na het antwoord berekend en zonder op het resultaat te
  wachten geregistreerd, zodat dit nooit extra latentie voor de client veroorzaakt. Verbruik
  van gedeelde quota wordt ingepland voor de volgende iteratie van de eventloop via
  [`src/lib/quota/spendRecorder.ts`](../../src/lib/quota/spendRecorder.ts).
- Uitgaven via API-sleutels worden gebufferd en in batches weggeschreven door de
  [`SpendBatchWriter`](../../src/lib/spend/batchWriter.ts) (standaard een wegschrijfinterval
  van 60 seconden en een buffer van 1.000 vermeldingen). Instelbaar via:

  | Omgevingsvariabele                  | Standaard | Doel                                                           |
  | ----------------------------------- | --------- | -------------------------------------------------------------- |
  | `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` | `60000`   | Wegschrijfinterval in milliseconden.                           |
  | `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`   | `1000`    | Maximaal aantal gebufferde vermeldingen vóór het wegschrijven. |

De kostencijfers op het dashboard worden **niet** uit een opgeslagen dollarbedrag per rij
gelezen — ze worden telkens wanneer het analyse-eindpunt wordt uitgevoerd direct opnieuw
berekend op basis van de aantallen tokens en de huidige prijstabel. Dit betekent dat het
corrigeren van een onjuiste prijs (en opnieuw synchroniseren) historische kostenschattingen
met terugwerkende kracht bijwerkt.

---

## Dashboard: de pagina Kosten

De pagina **Kosten** bevindt zich op `/dashboard/costs`
(`src/app/(dashboard)/dashboard/costs/`).
De hoofdweergave is het tabblad **Kostenoverzicht**
(`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`),
dat alle gegevens laadt via `GET /api/usage/analytics`.

Wat wordt weergegeven:

- **Uitgaventegels** — geschatte uitgaven voor _Vandaag (1d)_, _7d_, _30d_ en de geselecteerde
  periode. Periodekiezer: `7d`, `30d`, `90d`, `all`.
- **Kerncijfers** — aanvragen binnen de periode, actieve providers, actieve modellen, gemiddelde
  kosten per aanvraag.
- **Kostenverkenner** — een sorteerbare/filterbare tabel, gegroepeerd op **provider**, **model**,
  **API-sleutel**, **account** of **serviceniveau**, met kosten, aanvragen, tokens, gemiddelde
  kosten per aanvraag en aandeel van het totaal in %.
- **Tokengebruik** — totaal aantal tokens / invoertokens / uitvoertokens en de verhouding invoer:uitvoer.
- **Routeringsefficiëntie** — aantal fallbacks, fallbackpercentage en dekking van aangevraagde modellen.
- **Maandprognose** — projecteert de uitgaven aan het einde van de maand op basis van het recente daggemiddelde.
- **Periodevergelijking** — procentuele verandering tussen de eerste en tweede helft van de periode.
- **Grafieken** — dagelijkse kostentrend, aandeel per provider (cirkeldiagram), belangrijkste providers, belangrijkste modellen, kosten
  per API-sleutel, kosten per account, wekelijks gebruikspatroon en een activiteitenheatmap.
- **Exporteren** — download de huidige periode als **CSV** of **JSON** (de knoppen verschijnen
  zodra er kostengegevens hoger dan nul zijn).

Wanneer er geen geprijsd verkeer is, worden rijen weergegeven met het label "Verouderd / Gratis" in plaats van `$0`,
in overeenstemming met het model voor het bijhouden van besparingen.

### Gerelateerde subpagina's van Kosten

Het gedeelte Kosten bevat ook (allemaal onder `/dashboard/costs/`):

- **Prijzen** (`/dashboard/costs/pricing`) — bekijk en overschrijf prijzen per model (geeft
  het gedeelde tabblad Prijzen weer).
- **Budget** (`/dashboard/costs/budget`) — stel uitgavenlimieten per bereik in (geeft het gedeelde
  tabblad Budget weer).
- **Quotumdeling** (`/dashboard/costs/quota-share`) — gedeelde quotumpools en weergaven van het
  verbruikstempo.

---

## API-endpoints

Al deze endpoints vereisen beheerauthenticatie (loopback/JWT, via `requireManagementAuth`), tenzij
anders vermeld.

### Gebruiks- en kostenanalyse

| Methode | Endpoint                 | Doel                                                                                                                                                                              |
| ------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`   | `/api/usage/analytics`   | Volledige kosten-/gebruiksanalyse: samenvatting, dagelijkse trend, per provider/model/API-sleutel/account/niveau. Query: `range`, `startDate`, `endDate`, `apiKeyIds`, `presets`. |
| `GET`   | `/api/usage/utilization` | Quotumgebruik per provider in de loop van de tijd. Query: `range` (`1h`/`24h`/`7d`/`30d`), `provider`.                                                                            |
| `GET`   | `/api/usage/history`     | Ruwe gebruikshistorieregels.                                                                                                                                                      |
| `GET`   | `/api/usage/call-logs`   | Aanroeplogboeken per aanvraag (model, tokens, kosten, latentie, status).                                                                                                          |
| `GET`   | `/api/usage/quota`       | Status van providerquotum.                                                                                                                                                        |
| `GET`   | `/api/usage/proxy-logs`  | Logboeken van proxy-aanvragen.                                                                                                                                                    |

### Budgetten

| Methode | Endpoint                 | Doel                                                                                                 |
| ------- | ------------------------ | ---------------------------------------------------------------------------------------------------- |
| `GET`   | `/api/usage/budget`      | Kostenoverzicht + budgetcontrole voor één API-sleutel (queryparameter `apiKeyId` vereist).           |
| `POST`  | `/api/usage/budget`      | Stel dagelijkse/wekelijkse/maandelijkse USD-limieten + waarschuwingsdrempel voor een API-sleutel in. |
| `GET`   | `/api/usage/budget/bulk` | Gecombineerde budgetoverzichten voor meerdere API-sleutels.                                          |

> De budget-API heeft een bereik per **API-sleutel** (`apiKeyId`). Limieten die worden geretourneerd door
> `GET /api/usage/budget` omvatten `dailyLimitUsd`, `weeklyLimitUsd`, `monthlyLimitUsd`,
> een `warningThreshold` en de lopende totalen (`totalCostToday`, `totalCostMonth`, …).

### Prijzen

| Methode  | Endpoint                | Doel                                                                                                                                  |
| -------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`    | `/api/pricing`          | Huidige samengevoegde prijzen (gebruiker + gesynchroniseerd + standaardwaarden). `?includeSources=1` om de bron per item te bekijken. |
| `PATCH`  | `/api/pricing`          | Overschrijf prijzen voor `{ provider: { model: { input, output, cached, … } } }`.                                                     |
| `DELETE` | `/api/pricing`          | Stel prijzen opnieuw in op de standaardwaarden (optioneel beperkt via `?provider=&model=`).                                           |
| `GET`    | `/api/pricing/defaults` | Toon standaard fallbacktarieven per 1 miljoen.                                                                                        |
| `GET`    | `/api/pricing/models`   | Prijzen geïndexeerd per model.                                                                                                        |
| `POST`   | `/api/pricing/sync`     | Start een handmatige synchronisatie vanuit externe bronnen (LiteLLM).                                                                 |
| `GET`    | `/api/pricing/sync`     | Huidige synchronisatiestatus.                                                                                                         |
| `DELETE` | `/api/pricing/sync`     | Wis alle gesynchroniseerde prijsgegevens.                                                                                             |

### Andere kostenrelevante endpoints

| Methode | Eindpunt                      | Doel                                                                                 |
| ------- | ----------------------------- | ------------------------------------------------------------------------------------ |
| `GET`   | `/api/free-tier/summary`      | Tokenaantallen voor gratis modellen, deze maand gebruikt en resterend gratis tegoed. |
| `GET`   | `/api/quota/pools/[id]/usage` | Gebruik voor een pool met een gedeeld quotum.                                        |

---

## CLI

De CLI van OmniRoute biedt opdrachten voor kosten, gebruik en prijzen (geregistreerd in
[`bin/cli/commands/registry.mjs`](../../bin/cli/commands/registry.mjs)).

### `omniroute cost`

Een kostenrapport dat is samengesteld uit `/api/usage/analytics`.

```bash
omniroute cost                          # afgelopen 30d, gegroepeerd op provider
omniroute cost --period 7d              # afgelopen 7 dagen
omniroute cost --group-by model         # groeperen op provider | model | combo | api-key | day
omniroute cost --since 2026-06-01 --until 2026-06-13
omniroute cost --api-key <key> --limit 50
```

Kolommen: groep, aanvragen, inkomende/uitgaande tokens, kosten (USD) en % van het totaal. Aan het einde
wordt een regel met het eindtotaal weergegeven (onderdrukt met `--quiet` of `--output json`).

### `omniroute usage`

```bash
omniroute usage analytics --period 30d [--provider <id>]   # kostenoverzicht per provider
omniroute usage logs [--limit 100] [--follow] [--api-key <k>] [--search <q>]
omniroute usage quota [--provider <id>] [--check]
omniroute usage utilization [--api-key <k>]
omniroute usage history [--limit 100]
omniroute usage proxy-logs [--limit 100]

# Budgetten
omniroute usage budget list
omniroute usage budget get [scope]
omniroute usage budget set <amount> [--scope global] [--period monthly]
omniroute usage budget reset [scope]
```

### `omniroute pricing`

```bash
omniroute pricing list [--provider <p>] [--model <m>] [--limit 200]
omniroute pricing get <model>
omniroute pricing sync [--provider <p>] [--force]   # POST /api/pricing/sync
omniroute pricing diff [--model <m>]
omniroute pricing defaults show
omniroute pricing defaults set [--input <p>] [--output <p>] [--cache-read <p>] [--cache-write <p>]
```

> `pricing defaults show` leest `GET /api/pricing/defaults`. Gebruik in plaats daarvan de dashboardpagina **Prijzen** of `PATCH /api/pricing`
> om prijzen van afzonderlijke modellen te bewerken.

---

## Probleemoplossing

- **Alle kosten worden weergegeven als $0 / "Legacy / Free".** De gebruikte modellen hebben geen prijsvermelding.
  Schakel externe synchronisatie in (`PRICING_SYNC_ENABLED=true`) en voer `omniroute pricing sync` uit, of
  stel prijzen handmatig in via de pagina Prijzen / `PATCH /api/pricing`.
- **Een historisch model heeft een onjuiste prijs.** Corrigeer de prijs (via een overschrijving of door opnieuw te synchroniseren) — de kosten worden
  bij elke analyse-uitlezing opnieuw berekend op basis van het aantal tokens, zodat schattingen met terugwerkende kracht worden bijgewerkt.
- **De uitgaven lopen achter op de actuele situatie.** Uitgaven per sleutel worden in batches verwerkt; verlaag
  `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` als u actuelere cijfers nodig hebt.

---

Zie de [Gebruikershandleiding](./USER_GUIDE.md) en
de [Functiegallerij](./FEATURES.md) om te zien hoe dit binnen het bredere dashboard past.
