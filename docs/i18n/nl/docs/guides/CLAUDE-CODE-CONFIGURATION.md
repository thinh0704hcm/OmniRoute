# Claude Code CLI — Configuration with OmniRoute (Nederlands)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇦 [bs](../../../bs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

Koppel de **Claude Code**-CLI (`claude`) aan OmniRoute — lokaal of op een externe VPS —
met profielen per model, vergelijkbaar met de Codex-configuratie.

---

## Snel aan de slag

```bash
# Start Claude Code met een lokale OmniRoute (detecteert automatisch de actieve context)
omniroute launch

# Met een externe OmniRoute (na `omniroute connect <host>` gebeurt dit automatisch)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Genereer profielen per model en start er vervolgens één
omniroute setup-claude            # schrijft ~/.claude/profiles/<name>/settings.json
omniroute launch --profile glm52  # Claude Code gebruikt glm/glm-5.2 via OmniRoute
```

---

## Hoe Claude Code verbinding maakt met een gateway

Claude Code communiceert via de **Anthropic Messages API** en wordt met omgevingsvariabelen
naar een aangepast endpoint verwezen (het heeft geen `--base-url`-vlag):

| Variabele                                    | Doel                                                                                                    |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `ANTHROPIC_BASE_URL`                         | Basis-URL van de gateway (Claude Code voegt `/v1/messages` toe). **Geen `/v1`-achtervoegsel.**          |
| `ANTHROPIC_AUTH_TOKEN`                       | Verzonden als `Authorization: Bearer …` — gebruik je OmniRoute-toegangstoken/API-sleutel                |
| `ANTHROPIC_API_KEY`                          | Alternatief: verzonden als `x-api-key`. Als beide zijn ingesteld, heeft `ANTHROPIC_AUTH_TOKEN` voorrang |
| `ANTHROPIC_MODEL`                            | Dwing een specifiek model af (overschrijft de standaardwaarde van de `/model`-kiezer)                   |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | `1` → de ingebouwde `/model`-kiezer toont `claude*`/`anthropic*`-modellen uit `/v1/models`              |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | Beperk het aantal uitvoertokens per antwoord (bijv. `65536`)                                            |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | Tokendrempel voor automatische compactie                                                                |

> Omgevingsvariabelen worden **eenmalig bij het opstarten** gelezen — start Claude Code opnieuw nadat je ze hebt gewijzigd.

`omniroute launch` stelt dit allemaal voor je in: het bepaalt de basis-URL en het token
op basis van de actieve context (zodat `omniroute connect <vps>` gevolgd door `omniroute launch`
direct werkt), voert een statuscontrole op de server uit en start `claude` via exec.

---

## Discovery-aliassen — toon niet-Claude-modellen in de `/model`-kiezer

De gatewaymodeldetectie van Claude Code toont alleen id's die beginnen met `claude`
of `anthropic`. Daarom toont de ingebouwde `/model`-kiezer met
`CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` normaal gesproken **alleen** de
Claude/Anthropic-modellen van OmniRoute — een `kimi/kimi-k2.6` of `glm/glm-5.2`
verschijnt nooit, hoewel de routering prima werkt.

OmniRoute kan elk ingeschakeld model (en elke combinatie) spiegelen onder een
`claude/…`-id, zodat het door dat filter komt en in de kiezer verschijnt:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<combo "custo-otimizado"> →  claude/combo/custo-otimizado
```

Wanneer je in Claude Code een van deze modellen kiest, verwijdert OmniRoute de
`claude/`-omsluiting vóór de routering, zodat het oorspronkelijke id wordt gebruikt —
een echt `claude/<real-claude-model>`-id (van de daadwerkelijke Claude OAuth-provider)
blijft altijd ongewijzigd.

**Dit is standaard uitgeschakeld** en wordt geregeld door een poort met drie niveaus
(het meest specifieke niveau heeft voorrang), zodat een standaardinstallatie van
OmniRoute de catalogus niet verdubbelt voor clients die Claude Code niet gebruiken:

| Niveau   | Waar                                                                                    |
| -------- | --------------------------------------------------------------------------------------- |
| Model    | Detailpagina van provider → schakelaar "Expose in Claude Code" per model                |
| Provider | Detailpagina van provider → schakelaar op providerniveau (geldt voor al diens modellen) |
| Globaal  | Settings → Feature Flags → `EXPOSE_CC_DISCOVERY_ALIASES` (standaard uit)                |

De omgevingsvariabele `EXPOSE_CC_DISCOVERY_ALIASES` schakelt het globale niveau
verplicht in en heeft voorrang op de overschrijving via het dashboard (het scherm
Feature Flags toont de melding "active via environment variable" wanneer dit het
geval is). Schakelaars per provider en per model verfijnen dit verder — zo worden
bijvoorbeeld met globaal uitgeschakeld + Kimi-provider ingeschakeld alleen de
modellen van Kimi beschikbaar gemaakt.

> ⚠️ **Niet-overeenkomend contextvenster bij niet-Claude-modellen.** Claude Code
> gaat uit van een contextvenster van 200K voor elk id dat het niet herkent (het
> kan het werkelijke venster niet uitlezen uit `/v1/models`). Stel voor een model
> met een groter venster (bijv. de 256K van Kimi K2)
> `CLAUDE_CODE_AUTO_COMPACT_WINDOW` in op een waarde onder het werkelijke venster
> van het model, zodat automatische compactie niet voortijdig wordt geactiveerd.
> De hierboven gegenereerde profielen doen dit al per model.

---

## Onboardingblok op het dashboard

De Claude-toolkaart (**Dashboard → CLI Code**) toont het exacte `settings.json`-fragment
voor deze instantie, naast de infoknop voor de detectie-alias en met een kopieerknop:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<jouw OmniRoute>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<jouw OmniRoute API-sleutel>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

De basis-URL is de URL die door de kaart is bepaald (inclusief een aangepaste overschrijving die je hebt ingevoerd) en is al
genormaliseerd — geen `/v1`-achtervoegsel en geen afsluitende slash. **De sleutel wordt nooit weergegeven**: het blok bevat een
tijdelijke aanduiding, zodat een schermafbeelding of een geplakt fragment geen sleutel kan lekken. Vervang deze door je sleutel.

Voeg `CLAUDE_CODE_AUTO_COMPACT_WINDOW` toe onder hetzelfde `env`-blok voor elk model waarvan het werkelijke
contextvenster niet 200K is — Claude Code gaat uit van 200K voor elke id die niet wordt herkend, waardoor
automatische compactie anders op het verkeerde moment wordt uitgevoerd (zie de waarschuwing in de vorige sectie).
De fragmentbouwer accepteert deze waarde ook, zodat een aanroeper die het contextvenster van het doelmodel kent
deze rechtstreeks kan uitvoeren.

Bron: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (zuivere
bouwer, getest met unittests), weergegeven door `ClaudeGatewayOnboardingBlock`.

---

## Profielen (`CLAUDE_CONFIG_DIR`)

Claude Code heeft **geen systeemeigen profielbestanden** (in tegenstelling tot `~/.codex/<name>.config.toml` van Codex).
Het gebruikelijke mechanisme is `CLAUDE_CONFIG_DIR` — een afzonderlijke configuratiemap per
profiel, elk met een eigen `settings.json`, referenties, geschiedenis en cache.

`omniroute setup-claude` haalt de actuele `/v1/models`-catalogus op en schrijft één
profiel per model naar `~/.claude/profiles/<name>/settings.json`, waarbij
**dezelfde namen als bij `setup-codex`** worden gebruikt (`glm52`, `kimi-k27`, `deepseek-pro`, …):

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **Het authenticatietoken wordt nooit naar het profiel geschreven.** Start met
> `omniroute launch --profile <name>` (dit injecteert `ANTHROPIC_AUTH_TOKEN` vanuit de
> actieve context), of exporteer zelf `ANTHROPIC_AUTH_TOKEN` en voer
> `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude` uit.

**Automatische synchronisatie na modeldetectie (optioneel).** OmniRoute kan dezelfde
`~/.claude/profiles/<name>/settings.json`-bestanden automatisch opnieuw genereren wanneer een synchronisatie van providermodellen
de actuele catalogus wijzigt — zodat nieuwe of hernoemde modellen profielen krijgen zonder dat de
opdracht opnieuw hoeft te worden uitgevoerd. Dit staat **standaard uit**: schakel het in via het **CLI Code-dashboard** ("Automatische synchronisatie van CLI-profielen" → Claude Code), of stel `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` in (hierbij wordt ook
`CLI_ALLOW_CONFIG_WRITES` gerespecteerd, dat standaard aanstaat). Als dit is ingeschakeld, worden alleen profielbestanden geschreven; je actieve/standaard Claude-configuratie, authenticatie of `~/.claude/settings.json` wordt nooit
gewijzigd.

### Profielen genereren en gebruiken

```bash
# Lokale OmniRoute
omniroute setup-claude

# Externe VPS (neemt de VPS-URL op in elk profiel)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Alleen bepaalde providers
omniroute setup-claude --only glm,kimi

# Voorbeeldweergave zonder te schrijven
omniroute setup-claude --dry-run

# Een profiel starten
omniroute launch --profile kimi-k27
```

---

## Modelniveaus (optioneel)

Claude Code routeert naar capaciteitsniveaus. Wijs elk niveau via omgevingsvariabelen /
instellingen toe aan een OmniRoute-model als je per niveau verschillende providers wilt gebruiken:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

Anders wordt één `ANTHROPIC_MODEL` (zoals ingesteld door profielen) voor alles gebruikt.

---

## Externe modus

Nadat je `omniroute connect <host>` hebt uitgevoerd (zie
[Externe modus](./REMOTE-MODE.md)), richten `omniroute launch` en `omniroute setup-claude`
zich automatisch op die externe server en gebruiken ze het bijbehorende toegangstoken — er zijn
geen extra opties nodig. Overschrijf dit per aanroep met `--remote` / `--api-key`.

---

## Probleemoplossing

**Claude Code negeert de gateway** — controleer of `ANTHROPIC_BASE_URL` **geen
`/v1`** bevat en start `claude` opnieuw (omgevingsvariabelen worden eenmaal bij het opstarten ingelezen). `omniroute launch`
regelt dit voor je.

**De `/model`-kiezer is leeg / gatewaymodellen ontbreken** — hiervoor zijn Claude Code
v2.1.219+ en `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1` vereist. Alleen model-ID's met `claude*` /
`anthropic*` verschijnen in de kiezer; forceer elk ander model met
`ANTHROPIC_MODEL=<id>` (dit is wat profielen doen).

**`400 Ambiguous model 'claude-…'`** — Claude Code verzendt altijd model-ID's **zonder voorvoegsel**
(bijv. `claude-opus-4-8`), dus wanneer zowel de Claude Code- (`cc/…`) als
Claude-provider (`claude/…`) verbonden zijn, komt de ID zonder voorvoegsel overeen met twee routes en
weigert OmniRoute te gokken. Los dit op een van beide manieren op: leg een ID met voorvoegsel vast met
`ANTHROPIC_MODEL=cc/claude-opus-4-8`, of schakel **Claude Code verkiezen voor
Claude-modellen zonder voorvoegsel** in — de schakelaar op de Claude-providerpagina, of
`OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` (standaard uitgeschakeld;
zie [Omgeving](../reference/ENVIRONMENT.md)) — waarmee `claude-*`-ID's zonder voorvoegsel
in plaats daarvan naar Claude Code worden gerouteerd. Expliciete providervoorvoegsels hebben altijd voorrang.

**Authenticatiefouten** — het profiel bevat geen token. Gebruik `omniroute launch --profile`
(injecteert het token) of exporteer `ANTHROPIC_AUTH_TOKEN`.

**Profielen zijn niet geïsoleerd** — elk profiel is een afzonderlijke `CLAUDE_CONFIG_DIR`;
controleer of `echo $CLAUDE_CONFIG_DIR` binnen de sessie naar
`~/.claude/profiles/<name>` verwijst.
