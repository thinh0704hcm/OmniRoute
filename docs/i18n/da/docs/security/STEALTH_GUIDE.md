# Stealth Guide (Dansk)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇮🇩 [id](../../../id/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇮🇹 [it](../../../it/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Sandhedskilde:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Senest opdateret:** 2026-09-02 — v3.8.51
> **Målgruppe:** Ingeniører, der vedligeholder udbyderspecifikke stealth-integrationer.

OmniRoute integrerer med udbydere, hvis edge-systemer aktivt identificerer uofficielle klienter via fingerprinting (TLS JA3/JA4, headerrækkefølge, JSON-bodystruktur og integritetstokens). Denne side dokumenterer de stealth-grænseflader, OmniRoute stiller til rådighed, og hvor de er implementeret.

## Juridisk og etisk meddelelse

Stealth-funktioner findes, så OmniRoute kan fungere som et kompatibilitetslag mellem brugerejede officielle konti (Claude Code CLI, Codex, Antigravity, Cursor osv.) og OmniRoutes samlede API. De er **ikke** beregnet til at omgå svindeldetektion, dele legitimationsoplysninger eller overtræde udbydernes servicevilkår. Vedligeholderne forventer, at operatører overholder de upstream-servicevilkår, de accepterede, da de oprettede deres konti.

---

## Lag til TLS-fingerprinting

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Vedvarende `wreq-js`-sessioner oprettes efter behov for hvert kontoomfang og hver resolveret proxy. Den procesomspændende `TlsClient`-pulje indeholder højst 128 sessioner, der efterligner **Chrome 124 på macOS**, for upstream-systemer bag Cloudflare. `TlsClient.fetch()` afviser som standard, når det native runtime-miljø ikke er tilgængeligt; en kalder kan eksplicit vælge en fallback uden for denne wrapper.

- Sessionsprofil: `browser: "chrome_124", os: "macos"`
- Proxy-resolvering (prioritet): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (også med små bogstaver)
- Timeout: `TLS_CLIENT_TIMEOUT_MS` (nedarves fra `FETCH_TIMEOUT_MS`, standardværdi 600000)
- `wreq-js` Response er fetch-kompatibel (`headers`, `text()`, `json()`, `clone()`, `body`).
- Watchdog for første byte (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()`
  resolver, så snart upstream-headerne ankommer, så `TLS_CLIENT_TIMEOUT_MS` alene kan ikke sætte en
  grænse for en body, der aldrig leverer en første byte. `guardTlsFirstByte()` lader bodyens første
  `read()` konkurrere mod `TLS_FIRST_BYTE_WATCHDOG_MS` (standardværdi `10000`, `0` deaktiverer den);
  en velfungerende body påvirkes ikke, mens en fastlåst body annullerer wreq-readeren og lader den
  eksisterende TLS-fallbacklogik i `proxyFetch` fortsætte til direct/proxy-dispatcheren (en anmodning,
  der ikke sikkert kan genafspilles, f.eks. en POST med en body, udløser stadig en fejl i stedet for
  at blive forsøgt igen uden varsel).

### Transport for webcookie-udbydere — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts` er den fælles adapter for de fem specialiserede
webcookie-transporter nedenfor. Hver enkel udbyder-wrapper vælger en browser-/OS-profil. Adapteren
bruger den fælles wreq-runtime-loader og transportpulje i `open-sse/utils/tlsClient.ts`, indekseret
efter profil + OS + resolveret proxy, mens hver anmodning bruger `cookieMode: "ephemeral"`. Konti og
anmodninger deler derfor forbindelser på transportniveau, men aldrig en wreq-session eller cookie jar.

| Udbyder    | Profil        | Emuleret OS | Politik for stream-EOF             |
| ---------- | ------------- | ----------- | ---------------------------------- |
| Claude     | `chrome_146`  | Linux       | medtag `[DONE]`                    |
| Perplexity | `firefox_148` | macOS       | medtag `event: end_of_stream`      |
| Grok       | `chrome_146`  | Linux       | udelad `[DONE]`                    |
| Notion     | `chrome_146`  | Windows     | medtag `[DONE]`                    |
| LMArena    | `chrome_146`  | Windows     | ingen sentinel; luk ved native EOF |

- Streaming læser den native respons' `ReadableStream` direkte; der oprettes ingen midlertidig fil
  eller sidecar.
- Op til 256 indledende bytes inspiceres, før en stream eksponeres. SSE-udbydere bufferlagrer fejl,
  der ikke er SSE; Grok/LMArena oversætter Cloudflare-udfordringer til `403` og HTML-mellemsider til
  `502`.
- Timeout for den native anmodning er fortsat omgivet af en absolut JS-hard deadline. Et stop
  ugyldiggør og lukker kun den berørte profil-/OS-/proxytransport, før den næste anmodning genskaber
  den.
- Prioriteten for proxy-resolvering er `proxyUrl` pr. kald → anmodningsafgrænset konto-/dashboardkontekst
  → `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (inklusive varianter med små bogstaver). Resolveringsfejl
  afvises som standard i stedet for at lække en direkte forbindelse. LMArena resolverer bevidst mod
  `arena.ai`.
- `byteResponse` returnerer en indholdstypet `data:`-URL uden UTF-8-korruption.
- Fejlene er `TlsClientUnavailableError` (pakke/addon er ikke tilgængelig), `TlsClientHangError`
  (deadline overskredet) og `WreqTransportCapacityError` (fejlkoden for delt sessionskapacitet),
  når alle 128 afgrænsede profil-/OS-/proxyslots er aktive eller ved at lukke.

Den generiske `TlsClient`-session ovenfor er fortsat specialiseret til vedvarende browserbaseret cookie state. Begge stier genbruger én cachelagret wreq-modulloader og process lifecycle-hook; deres puljer forbliver separate, fordi deres cookielevetider bevidst er forskellige.

Profilerne understøttes af den fastlåste pakkeversion, men reel WAF-accept kan ændre sig uafhængigt af lokale kontrakttests. Validér ændringer af fingerprints mod en eksplicit godkendt livekonto, før der hævdes paritet med en upstream-browser.

---

## Claude Code Stealth-pakke

Når `cliCompatMode` er slået til, omformer OmniRoute udgående Claude-anmodninger, så de ikke kan skelnes fra `claude-cli`-trafik. Tre moduler samarbejder:

### `claudeCodeFingerprint.ts`

Beregner det 3 tegn lange `cc_version`-fingeraftryk, der er indlejret i faktureringsheaderen:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (hardkodet; matcher den officielle klient)
- Input: tegn ved indeks 4, 7 og 20 i teksten i den første brugermeddelelse + versionsstreng
- Output: 3 tegn langt hex-præfiks

### `claudeCodeCCH.ts` (hash af klientindhold)

Integritetskontrol på serversiden, som den officielle Claude Code CLI beregner via Bun/Zig. OmniRoute genimplementerer den med `xxhash-wasm`:

1. Serialiser body med pladsholderen `cch=00000;`
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. 5 tegn langt hex med små bogstaver, udfyldt med foranstillede nuller
4. Erstat `cch=00000;` med det beregnede token

Konstanter:

- Seed: `0x6e52736ac806831e`
- Mønster: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Indsætter en Unicode-**zero-width joiner** (`U+200D`) efter det første tegn i "følsomme" klientnavne, så upstream-filtre ikke kan finde dem med grep. Standardordliste:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Anvendes på: `system`-blokke, alt `messages[].content` og `tools[].description` / `tools[].function.description`. Kan tilsidesættes af operatøren via `setSensitiveWords()`.

### `claudeCodeCompatible.ts` — `anthropic-compatible-cc-*`-udbydere

Til tredjeparts-Anthropic-relæer, der kun accepterer trafik fra "rigtig Claude Code":

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"` som standard
- Indstillingen "Enable redact-thinking beta" for hver forbindelse tilføjer `redact-thinking-2026-02-12`, når en CC Compatible-upstream specifikt kræver redigerede ræsonneringsstreams
- Indstillingen "Enable summarized thinking display" for hver forbindelse gemmer `providerSpecificData.requestDefaults.summarizeThinking` og føjer `display: "summarized"` til CC Compatible-ræsonneringsanmodninger, der ikke allerede har angivet en visningstilstand
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (Opus/Sonnet 4.x-familien)
- Standardsti: `/v1/messages?beta=true`

Relaterede moduler i samme pakke:

- `claudeCodeConstraints.ts` — regler for temperatur + cachekontrol
- `claudeCodeToolRemapper.ts` — omdannelse af værktøjsnavne
- `claudeCodeExtraRemap.ts` — yderligere normalisering af payload

---

## Antigravity Stealth

Antigravity-anmodninger bevarer kalderens tekst byte for byte. OmniRoute indsætter ikke tegn med nul bredde i prompts og omdøber eller injicerer ikke værktøjer for at efterligne en IDE-klient.

### `antigravityHeaderScrub.ts`

Fjerner Stainless SDK-markører (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`) før videresendelse.

### ⚠️ Risiko: `ANTIGRAVITY_CREDITS=always` (høj risiko for kontoblokering)

`ANTIGRAVITY_CREDITS=always` (anvendes af `open-sse/executors/antigravity.ts`) dirigerer **alle** anmodninger gennem Antigravity AI Credit Overages (betalte Google-kreditter) i stedet for at lade Googles gratis kvote begrænse forbruget. Dette er dokumenteret som en funktion, men det er **den absolut mest almindelige rapport om overtrædelse af servicevilkår, vi ser** — flere Google Ultra-konti er blevet blokeret med `403 / "service disabled for ToS violation" / insufficient_quota` efter at have kørt i nogle få timer med `=always`.

Upstream-håndhævelsen sker på **Googles side** og er ikke noget, OmniRoute kan forhindre. Miljøvariablens navn og den eksisterende dokumentation får det til at lyde som en sikker indstilling at aktivere; det er den ikke.

**Derfor udløser dette mere aggressiv registrering af misbrug end brug, der udelukkende benytter gratisniveauet:**

- Vedvarende automatiseret forbrug på en enkelt Google-konto markeres anderledes end brug af gratisniveauet, der rammer kvoten og stopper.
- Kreditoverskridelser har intet loft over anmodningsfrekvensen, så en forkert konfigureret klient kan bruge flere hundrede USD på få minutter og ligne videresalg af API-nøgler eller bottrafik.
- Flere OmniRoute-brugere, der parallelt anvender ekstra kreditter fra den samme eksterne IP-adresse, forstærker signalet.

**Anbefalet fremgangsmåde:**

1. Behold standardindstillingen `ANTIGRAVITY_CREDITS=off`, medmindre operatøren udtrykkeligt accepterer risikoen for betalte kreditter og håndhævelse mod kontoen. `retry` sender først den normale anmodning og injicerer højst kreditter én gang efter en kvalificerende kvote-429; `always` injicerer kreditter ved den første anmodning.
2. **Fordel belastningen på tværs af udbydere via Auto-Combo** (`model: "auto"` eller `kr/glm/etc`-combo) i stedet for at overbelaste en enkelt Antigravity-konto.
3. **Angiv RPM-grænser for hver forbindelse** på Antigravity-udbyderens redigeringsside (Dashboard → Providers → Antigravity → forbindelse → frekvensgrænse). 30–60 RPM er en forsvarlig øvre grænse for vedvarende brug.
4. **Brug stabil upstream-netværksinfrastruktur under operatørens kontrol**, og undgå at dele én konto mellem brugere eller arbejdsbelastninger, der ikke er relaterede.
5. **Ved blokering**: indsend en appel via `support.google.com` → "Restore Workspace/Account access" med den nøjagtige `quota_exceeded`- / `service disabled`-responsbody, som Google sendte. Gendannelse kan ikke garanteres.

Miljøreferencen dokumenterer konsekvenserne for konto og forbrug for hver kredittilstand.

Berøringspunkter:

- `open-sse/executors/antigravity.ts` — læser `process.env.ANTIGRAVITY_CREDITS`
- `src/lib/oauth/providers/antigravity.ts` — håndtering af legitimationsoplysninger
- Oprindelig hændelsesrapport: Diskussion [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## CLI-fingeraftryksregister — `open-sse/config/cliFingerprints.ts`

Tabel pr. udbyder, der fastholder den **præcise** rækkefølge af headers og JSON-bodyfelter registreret fra mitmproxy-sporinger af de officielle CLI'er. Aktuelt registreret: `codex`, `claude` samt runtime-afledte profiler i `providerHeaderProfiles.ts` for `antigravity` og `github`.

```ts
interface CliFingerprint {
  headerOrder: string[]; // skelner mellem store og små bogstaver
  bodyFieldOrder: string[]; // JSON-nøgler på øverste niveau
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Slå til eller fra pr. udbyder via miljøvariabler (se nedenfor). Når funktionen er deaktiveret, vises headers/body-nøgler i den rækkefølge, Node/JSON har givet dem — nemt at identificere via fingeraftryk.

---

## MITM-proxy (Antigravity, Linux/macOS/Windows)

For CLI'er, hvis binære filer ikke kan omdirigeres via `OPENAI_BASE_URL`, kører OmniRoute en lokal TLS-terminerende proxy. Endepunkterne findes under `src/app/api/cli-tools/antigravity-mitm/`.

| Metode | Endepunkt                               | Formål                                         |
| ------ | --------------------------------------- | ---------------------------------------------- |
| GET    | `/api/cli-tools/antigravity-mitm`       | Status — kører, pid, dnsConfigured, certExists |
| POST   | `/api/cli-tools/antigravity-mitm`       | Start MITM (kræver `apiKey` + `sudoPassword`)  |
| DELETE | `/api/cli-tools/antigravity-mitm`       | Stop MITM                                      |
| GET    | `/api/cli-tools/antigravity-mitm/alias` | Vis modelaliasser                              |
| PUT    | `/api/cli-tools/antigravity-mitm/alias` | Gem modelaliasser for et værktøj               |

Målvært, der opsnappes: **`daily-cloudcode-pa.googleapis.com`** (Antigravitys upstream).

### Startsekvens (`src/mitm/manager.ts::startMitm`)

1. Generér et selvsigneret certifikat via `selfsigned` (RSA-2048, SHA-256, 1 år) — `cert/generate.ts`
2. Installér certifikatet i systemets tillidslager — `cert/install.ts`
3. Tilføj værtsposten `127.0.0.1 daily-cloudcode-pa.googleapis.com` — `dns/dnsConfig.ts`
4. Start `src/mitm/server.cjs` med `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (standard `443`)
5. Gem PID i `<DATA_DIR>/mitm/.mitm.pid`

### Dynamisk registrering af tillidslager på Linux — `cert/install.ts`

`getLinuxCertConfig()` gennemgår en prioriteret liste og vælger den første eksisterende mappe:

| Distributionsfamilie     | Mappe                                       | Opdateringskommando      |
| ------------------------ | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu          | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS   | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                 | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Certifikatets filnavn: `omniroute-mitm.crt`. Fingeraftrykket matches via `getCertFingerprint()` (SHA-1 af DER).

Derudover installerer `updateNssDatabases()` i NSS-databaser for hver bruger, når `certutil` er tilgængelig: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb`, alle Firefox-profiler (inklusive snap) under kaldenavnet **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** PowerShell med administratorrettigheder → `certutil -addstore Root`

### Godkendelse

Alle MITM-endepunkter kræver administrationsgodkendelse (`requireCliToolsAuth`). Sudo-adgangskoden caches i modulets scope (aldrig `globalThis`) og ryddes ved `stopMitm()`.

---

## User-Agent-tilsidesættelser — miljøvariabler (`.env.example` afsnit 12)

| Variabel                 | Standardværdi                                                   |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.149.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

Bruges af `open-sse/executors/base.ts::buildHeaders()` via dynamisk opslag. **Opdatér disse, når udbyderne udgiver nye CLI-versioner** — forældede UA-strenge begynder at blive afvist som forældede klienter.

## Omskiftere for CLI-kompatibilitetstilstand (`.env.example` afsnit 13)

| Variabel                   | Effekt                           |
| -------------------------- | -------------------------------- |
| `CLI_COMPAT_CODEX=1`       | Codex-fingeraftryk               |
| `CLI_COMPAT_CLAUDE=1`      | claude-cli-fingeraftryk          |
| `CLI_COMPAT_GITHUB=1`      | GitHub Copilot Chat-fingeraftryk |
| `CLI_COMPAT_ANTIGRAVITY=1` | Antigravity-fingeraftryk         |
| `CLI_COMPAT_KIRO=1`        | Kiro                             |
| `CLI_COMPAT_CURSOR=1`      | Cursor                           |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                      |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                         |
| `CLI_COMPAT_CLINE=1`       | Cline                            |
| `CLI_COMPAT_ALL=1`         | Aktivér alle ovenstående         |

Udbyderens IP-adresse bevares **altid** — omskifteren ændrer kun anmodningens format på forbindelsen; den skifter ikke udgående IP-adresse.

---

## Rensning af indgående headers

OmniRoute fjerner indgående klientheaders før videresendelse, så en anmodning, der kommer fra Cursor, ikke lækker `User-Agent: Cursor/X.Y.Z` til en Claude-upstream. Se `src/shared/constants/upstreamHeaders.ts` for afvisningslisten, som holdes synkroniseret med Zod-skemaerne og enhedstestene.

---

## Opdatering af fingeraftryk, når en udbyder skifter dem

1. Opsnap officiel CLI-trafik med `mitmproxy` (TLS-opfangning + dump)
2. Udtræk JA3/JA4 og den bogstavelige headerrækkefølge
3. Opdatér den relevante `CLI_FINGERPRINTS[...]`-post
4. Opdatér den tilsvarende standardværdi for `*_USER_AGENT` i `.env.example`
5. Hvis selve TLS-handshaket er ændret, skal den relevante udbyder-wrapper eller wreq-js-indstillingen `browser:` opdateres
6. Kør de udbyderspecifikke TLS-tests og en manuel kanarietest mod liveudbyderen
7. Udgiv i en patchversion; dokumentér i `CHANGELOG.md`

---

## Tests

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — adfærd for den delte TLS-wrapper
- `tests/unit/anthropic-cache-fingerprint.test.ts` — deterministiske fingeraftryk
- `tests/unit/chatgpt-web-source-retirement.test.ts` — den fælles stealth-kilde til ChatGPT Web forbliver fraværende, mens Codex Web fortsat er til stede

---

## Se også

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — hvad der sker, når en skjult sti får en `403`
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — komplet reference til miljøvariabler
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — operatørens visning af MITM-arbejdsgangen
