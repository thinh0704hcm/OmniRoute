# Cost & Spend Tracking (Dansk)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/COST_TRACKING.md) · 🇪🇹 [am](../../../am/docs/guides/COST_TRACKING.md) · 🇸🇦 [ar](../../../ar/docs/guides/COST_TRACKING.md) · 🇦🇿 [az](../../../az/docs/guides/COST_TRACKING.md) · 🇧🇬 [bg](../../../bg/docs/guides/COST_TRACKING.md) · 🇧🇩 [bn](../../../bn/docs/guides/COST_TRACKING.md) · 🇧🇦 [bs](../../../bs/docs/guides/COST_TRACKING.md) · 🇨🇿 [cs](../../../cs/docs/guides/COST_TRACKING.md) · 🇩🇪 [de](../../../de/docs/guides/COST_TRACKING.md) · 🇬🇷 [el](../../../el/docs/guides/COST_TRACKING.md) · 🇪🇸 [es](../../../es/docs/guides/COST_TRACKING.md) · 🇪🇪 [et](../../../et/docs/guides/COST_TRACKING.md) · 🇮🇷 [fa](../../../fa/docs/guides/COST_TRACKING.md) · 🇫🇮 [fi](../../../fi/docs/guides/COST_TRACKING.md) · 🇫🇷 [fr](../../../fr/docs/guides/COST_TRACKING.md) · 🇮🇪 [ga](../../../ga/docs/guides/COST_TRACKING.md) · 🇮🇳 [gu](../../../gu/docs/guides/COST_TRACKING.md) · 🇳🇬 [ha](../../../ha/docs/guides/COST_TRACKING.md) · 🇮🇱 [he](../../../he/docs/guides/COST_TRACKING.md) · 🇮🇳 [hi](../../../hi/docs/guides/COST_TRACKING.md) · 🇭🇷 [hr](../../../hr/docs/guides/COST_TRACKING.md) · 🇭🇺 [hu](../../../hu/docs/guides/COST_TRACKING.md) · 🇦🇲 [hy](../../../hy/docs/guides/COST_TRACKING.md) · 🇮🇩 [id](../../../id/docs/guides/COST_TRACKING.md) · 🇳🇬 [ig](../../../ig/docs/guides/COST_TRACKING.md) · 🇮🇹 [it](../../../it/docs/guides/COST_TRACKING.md) · 🇯🇵 [ja](../../../ja/docs/guides/COST_TRACKING.md) · 🇬🇪 [ka](../../../ka/docs/guides/COST_TRACKING.md) · 🇰🇭 [km](../../../km/docs/guides/COST_TRACKING.md) · 🇮🇳 [kn](../../../kn/docs/guides/COST_TRACKING.md) · 🇰🇷 [ko](../../../ko/docs/guides/COST_TRACKING.md) · 🇱🇹 [lt](../../../lt/docs/guides/COST_TRACKING.md) · 🇱🇻 [lv](../../../lv/docs/guides/COST_TRACKING.md) · 🇮🇳 [ml](../../../ml/docs/guides/COST_TRACKING.md) · 🇮🇳 [mr](../../../mr/docs/guides/COST_TRACKING.md) · 🇲🇾 [ms](../../../ms/docs/guides/COST_TRACKING.md) · 🇲🇹 [mt](../../../mt/docs/guides/COST_TRACKING.md) · 🇲🇲 [my](../../../my/docs/guides/COST_TRACKING.md) · 🇳🇵 [ne](../../../ne/docs/guides/COST_TRACKING.md) · 🇳🇱 [nl](../../../nl/docs/guides/COST_TRACKING.md) · 🇳🇴 [no](../../../no/docs/guides/COST_TRACKING.md) · 🇮🇳 [or](../../../or/docs/guides/COST_TRACKING.md) · 🇮🇳 [pa](../../../pa/docs/guides/COST_TRACKING.md) · 🇵🇭 [phi](../../../phi/docs/guides/COST_TRACKING.md) · 🇵🇱 [pl](../../../pl/docs/guides/COST_TRACKING.md) · 🇵🇹 [pt](../../../pt/docs/guides/COST_TRACKING.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/COST_TRACKING.md) · 🇷🇴 [ro](../../../ro/docs/guides/COST_TRACKING.md) · 🇷🇺 [ru](../../../ru/docs/guides/COST_TRACKING.md) · 🇱🇰 [si](../../../si/docs/guides/COST_TRACKING.md) · 🇸🇰 [sk](../../../sk/docs/guides/COST_TRACKING.md) · 🇸🇮 [sl](../../../sl/docs/guides/COST_TRACKING.md) · 🇷🇸 [sr](../../../sr/docs/guides/COST_TRACKING.md) · 🇸🇪 [sv](../../../sv/docs/guides/COST_TRACKING.md) · 🇰🇪 [sw](../../../sw/docs/guides/COST_TRACKING.md) · 🇮🇳 [ta](../../../ta/docs/guides/COST_TRACKING.md) · 🇮🇳 [te](../../../te/docs/guides/COST_TRACKING.md) · 🇹🇭 [th](../../../th/docs/guides/COST_TRACKING.md) · 🇹🇷 [tr](../../../tr/docs/guides/COST_TRACKING.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/COST_TRACKING.md) · 🇵🇰 [ur](../../../ur/docs/guides/COST_TRACKING.md) · 🇺🇿 [uz](../../../uz/docs/guides/COST_TRACKING.md) · 🇻🇳 [vi](../../../vi/docs/guides/COST_TRACKING.md) · 🇳🇬 [yo](../../../yo/docs/guides/COST_TRACKING.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/COST_TRACKING.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/COST_TRACKING.md)

---

Sådan estimerer, registrerer og rapporterer OmniRoute omkostningerne for hver anmodning — og hvorfor tallet på
dashboardet er en **besparelsesmåler**, ikke en regning.

Se også: [Brugervejledning](./USER_GUIDE.md) · [Funktionsgalleri](./FEATURES.md)

---

## Hvad det er (og hvad det ikke er)

OmniRoute tildeler hver fuldførelse en omkostning i USD pr. anmodning ved at gange antallet
af tokens med en models priser. Disse tal driver dashboardet **Omkostninger**,
CLI-kommandoerne `omniroute cost` / `omniroute usage`, CSV/JSON-eksporter og budgetter pr. API-nøgle.

> **Dashboardets "omkostning" er en besparelsesmåler, ikke en regning.** OmniRoute opkræver aldrig betaling fra dig
> — det videresender dine anmodninger til udbydere, som du allerede har forbundet (dine egne
> abonnementer, gratis niveauer og API-nøgler). En "samlet omkostning på $290", der udelukkende er optjent på gratis
> modeller, betyder, at du omtrent **ikke betalte $290** til en betalt API. Tallet er et _estimat_
> af, hvad den samme trafik ville have kostet til standardlistepriser, så du kan se, hvor
> dit forbrug er koncentreret, og hvor meget du sparer ved at dirigere til billigere/gratis udbydere.

Denne fortolkning fremgår direkte af projektets [README](../../README.md) ("dashboardets
'omkostning' er en besparelsesmåler, ikke en regning").

Da tallet er et estimat:

- Det afhænger af den pristabel, OmniRoute har for hver model. En model uden en
  prispost bidrager med en omkostning på `0` (den vises som en række med "Ældre / Gratis" i oversigten).
- Trafik på gratis niveauer og via abonnementer påløber stadig en _estimeret_ omkostning — det er det
  beløb, du sparer, ikke et beløb, du skylder.

---

## Sådan estimeres omkostninger

### Priskilden

Omkostninger kommer fra en pristabel, der slås op i denne prioriterede rækkefølge
([`src/lib/pricingSync.ts`](../../src/lib/pricingSync.ts)):

1. **Brugertilsidesættelser** — priser, du angiver i dashboardet / via `PATCH /api/pricing`.
2. **Synkroniserede eksterne priser** — hentes fra LiteLLM's offentlige
   `model_prices_and_context_window.json`, når synkronisering er aktiveret (gemmes i et separat
   `pricing_synced`-navneområde, så det aldrig overskriver dine tilsidesættelser).
3. **Hardkodede standardværdier** — leveres med OmniRoute.

Ekstern prissynkronisering er **frivillig** og deaktiveret som standard. Relevante miljøvariabler
(se [`.env.example`](../../.env.example)):

| Miljøvariabel           | Standard  | Formål                                                               |
| ----------------------- | --------- | -------------------------------------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`   | Aktivér LiteLLM-prissynkronisering i baggrunden ved opstart.         |
| `PRICING_SYNC_INTERVAL` | `86400`   | Synkroniseringsinterval i **sekunder** (dagligt som standard).       |
| `PRICING_SYNC_SOURCES`  | `litellm` | Kommasepareret kildeliste (kun `litellm` understøttes i øjeblikket). |

### Omkostningsformlen

Omkostningen beregnes pr. anmodning ud fra tokenantal og priser pr. million tokens i
[`src/lib/usage/costCalculator.ts`](../../src/lib/usage/costCalculator.ts)
(`computeCostFromPricing` / `calculateCost`):

- **Inputtokens** (minus cachelæsninger og tokens til cacheoprettelse) × `input`-pris.
- **Cachelæsnings-tokens** × `cached`-pris (bruger inputprisen som reserve).
- **Cacheoprettelses-tokens** × `cache_creation`-pris (bruger inputprisen som reserve).
- **Outputtokens** × `output`-pris.
- **Ræsonneringstokens** × `reasoning`-pris (bruger outputprisen som reserve).

Alle priser fortolkes som USD pr. 1.000.000 tokens. Et Codex-serviceniveau af typen "fast"/"priority" eller
"flex" anvender en omkostningsmultiplikator (`getCodexFastCostMultiplier`) — f.eks. faktureres flex
med 50 % rabat på tokens, hvilket vises som **flex-besparelser** i dashboardet.

Modelnavne normaliseres først (udbyderstiers præfikser såsom `openai/` eller
`accounts/fireworks/models/` fjernes), så historiske rækker stadig matcher en pris.

### Sådan registreres forbrug

- Omkostningen pr. anmodning beregnes efter svaret og registreres asynkront uden at afvente resultatet, så det
  aldrig tilføjer forsinkelse for klienten. Forbrug af delt kvote planlægges på næste
  iteration af hændelsesløkken via [`src/lib/quota/spendRecorder.ts`](../../src/lib/quota/spendRecorder.ts).
- Forbrug pr. API-nøgle bufferlagres og skrives i batches af
  [`SpendBatchWriter`](../../src/lib/spend/batchWriter.ts) (som standard et skriveinterval på 60 sekunder
  og en buffer med 1.000 poster). Kan justeres via:

  | Miljøvariabel                       | Standard | Formål                                              |
  | ----------------------------------- | -------- | --------------------------------------------------- |
  | `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` | `60000`  | Skriveinterval i millisekunder.                     |
  | `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`   | `1000`   | Maksimalt antal bufferlagrede poster før skrivning. |

Dashboardets omkostningstal læses **ikke** fra et gemt dollarbeløb pr. række — de
genberegnes løbende ud fra tokenantal og den aktuelle pristabel, hver gang
analyse-endpointet køres. Det betyder, at rettelse af en forkert pris (og gensynkronisering)
opdaterer historiske omkostningsestimater med tilbagevirkende kraft.

---

## Dashboard: siden Omkostninger

Siden **Omkostninger** findes på `/dashboard/costs`
(`src/app/(dashboard)/dashboard/costs/`).
Dens primære visning er fanen **Omkostningsoversigt**
(`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`),
som indlæser alt fra `GET /api/usage/analytics`.

Det viser den:

- **Forbrugsfelter** — estimeret forbrug for _I dag (1d)_, _7d_, _30d_ og det valgte
  tidsinterval. Intervalvælger: `7d`, `30d`, `90d`, `all`.
- **Overordnede målinger** — anmodninger i tidsintervallet, aktive udbydere, aktive modeller og
  gennemsnitlig omkostning pr. anmodning.
- **Omkostningsoversigt** — en sorterbar og filtrerbar tabel grupperet efter **udbyder**, **model**,
  **API-nøgle**, **konto** eller **serviceniveau**, med omkostning, anmodninger, tokens,
  gennemsnitlig omkostning pr. anmodning og procentvis andel af totalen.
- **Tokenforbrug** — samlet antal tokens/inputtokens/outputtokens samt forholdet mellem input og output.
- **Routingeffektivitet** — antal fallbacks, fallback-rate og dækning for den anmodede model.
- **Månedlig prognose** — estimerer forbruget ved månedens udgang ud fra det seneste daglige gennemsnit.
- **Periodesammenligning** — procentvis ændring mellem første og anden halvdel af tidsintervallet.
- **Diagrammer** — daglig omkostningstendens, udbyderandel (cirkeldiagram), største udbydere, mest benyttede modeller, omkostning
  pr. API-nøgle, omkostning pr. konto, ugentligt brugsmønster og et aktivitets-heatmap.
- **Eksport** — download det aktuelle tidsinterval som **CSV** eller **JSON** (knapperne vises,
  når der findes omkostningsdata, som ikke er nul).

Når der ikke er nogen prissat trafik, vises rækker med etiketten "Ældre / Gratis" i stedet for `$0`,
hvilket afspejler modellen for registrering af besparelser.

### Relaterede undersider under Omkostninger

Området Omkostninger indeholder også følgende sider (alle under `/dashboard/costs/`):

- **Priser** (`/dashboard/costs/pricing`) — se og tilsidesæt priser pr. model (viser
  den fælles fane Priser).
- **Budget** (`/dashboard/costs/budget`) — angiv forbrugsgrænser pr. omfang (viser den fælles
  fane Budget).
- **Kvotedeling** (`/dashboard/costs/quota-share`) — puljer med delte kvoter og visninger af
  forbrugshastighed.

---

## API-slutpunkter

Alle disse kræver administrationsgodkendelse (loopback/JWT via `requireManagementAuth`), medmindre
andet er angivet.

### Brugs- og omkostningsanalyse

| Metode | Slutpunkt                | Formål                                                                                                                                                                          |
| ------ | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/analytics`   | Komplet omkostnings-/brugsanalyse: oversigt, daglig tendens, efter udbyder/model/API-nøgle/konto/niveau. Forespørgsel: `range`, `startDate`, `endDate`, `apiKeyIds`, `presets`. |
| `GET`  | `/api/usage/utilization` | Kvoteudnyttelse pr. udbyder over tid. Forespørgsel: `range` (`1h`/`24h`/`7d`/`30d`), `provider`.                                                                                |
| `GET`  | `/api/usage/history`     | Rækker med rå brugshistorik.                                                                                                                                                    |
| `GET`  | `/api/usage/call-logs`   | Kaldslogfiler pr. anmodning (model, tokens, omkostning, latenstid, status).                                                                                                     |
| `GET`  | `/api/usage/quota`       | Status for udbyderkvoter.                                                                                                                                                       |
| `GET`  | `/api/usage/proxy-logs`  | Logfiler for proxyanmodninger.                                                                                                                                                  |

### Budgetter

| Metode | Slutpunkt                | Formål                                                                                                   |
| ------ | ------------------------ | -------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/budget`      | Omkostningsoversigt og budgetkontrol for én API-nøgle (forespørgselsparameteren `apiKeyId` er påkrævet). |
| `POST` | `/api/usage/budget`      | Angiv daglige/ugentlige/månedlige USD-grænser og advarselstærskel for en API-nøgle.                      |
| `GET`  | `/api/usage/budget/bulk` | Samlede budgetoversigter på tværs af API-nøgler.                                                         |

> Budget-API'et er afgrænset pr. **API-nøgle** (`apiKeyId`). Grænser, der returneres af
> `GET /api/usage/budget`, omfatter `dailyLimitUsd`, `weeklyLimitUsd`, `monthlyLimitUsd`,
> en `warningThreshold` samt de løbende totaler (`totalCostToday`, `totalCostMonth`, …).

### Priser

| Metode   | Slutpunkt               | Formål                                                                                                                        |
| -------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `GET`    | `/api/pricing`          | Aktuelle sammenlagte priser (brugerdefinerede + synkroniserede + standarder). `?includeSources=1` viser kilden for hver post. |
| `PATCH`  | `/api/pricing`          | Tilsidesæt priser for `{ provider: { model: { input, output, cached, … } } }`.                                                |
| `DELETE` | `/api/pricing`          | Nulstil priser til standardværdier (kan valgfrit afgrænses med `?provider=&model=`).                                          |
| `GET`    | `/api/pricing/defaults` | Vis standardiserede fallback-priser pr. 1 mio.                                                                                |
| `GET`    | `/api/pricing/models`   | Priser indekseret efter model.                                                                                                |
| `POST`   | `/api/pricing/sync`     | Udløs en manuel synkronisering fra eksterne kilder (LiteLLM).                                                                 |
| `GET`    | `/api/pricing/sync`     | Aktuel synkroniseringsstatus.                                                                                                 |
| `DELETE` | `/api/pricing/sync`     | Ryd alle synkroniserede prisdata.                                                                                             |

### Andre omkostningsrelevante slutpunkter

| Metode | Slutpunkt                     | Formål                                                                                   |
| ------ | ----------------------------- | ---------------------------------------------------------------------------------------- |
| `GET`  | `/api/free-tier/summary`      | Samlet antal tokens for gratismodeller, forbrug i denne måned og resterende gratiskvote. |
| `GET`  | `/api/quota/pools/[id]/usage` | Forbrug for en pulje med delt kvote.                                                     |

---

## CLI

OmniRoutes CLI indeholder kommandoer til omkostninger, forbrug og priser (registreret i
[`bin/cli/commands/registry.mjs`](../../bin/cli/commands/registry.mjs)).

### `omniroute cost`

En omkostningsrapport, der er aggregeret fra `/api/usage/analytics`.

```bash
omniroute cost                          # seneste 30d, grupperet efter udbyder
omniroute cost --period 7d              # seneste 7 dage
omniroute cost --group-by model         # gruppér efter provider | model | combo | api-key | day
omniroute cost --since 2026-06-01 --until 2026-06-13
omniroute cost --api-key <key> --limit 50
```

Kolonner: gruppe, anmodninger, tokens ind/ud, omkostninger (USD) og procent af totalen. En linje med
den samlede total udskrives til sidst (udelades med `--quiet` eller `--output json`).

### `omniroute usage`

```bash
omniroute usage analytics --period 30d [--provider <id>]   # oversigt over omkostninger pr. udbyder
omniroute usage logs [--limit 100] [--follow] [--api-key <k>] [--search <q>]
omniroute usage quota [--provider <id>] [--check]
omniroute usage utilization [--api-key <k>]
omniroute usage history [--limit 100]
omniroute usage proxy-logs [--limit 100]

# Budgetter
omniroute usage budget list
omniroute usage budget get [scope]
omniroute usage budget set <amount> [--scope global] [--period monthly]
omniroute usage budget reset [scope]
```

### `omniroute pricing`

```bash
omniroute pricing list [--provider <p>] [--model <m>] [--limit 200]
omniroute pricing get <model>
omniroute pricing sync [--provider <p>] [--force]   # POST /api/pricing/sync
omniroute pricing diff [--model <m>]
omniroute pricing defaults show
omniroute pricing defaults set [--input <p>] [--output <p>] [--cache-read <p>] [--cache-write <p>]
```

> `pricing defaults show` læser `GET /api/pricing/defaults`. Hvis du i stedet vil redigere priserne
> for individuelle modeller, skal du bruge dashboardsiden **Pricing** eller `PATCH /api/pricing`.

---

## Fejlfinding

- **Alle omkostninger vises som $0 / "Legacy / Free".** De anvendte modeller har ingen prispost.
  Aktivér ekstern synkronisering (`PRICING_SYNC_ENABLED=true`), og kør `omniroute pricing sync`, eller
  angiv priser manuelt via siden Pricing / `PATCH /api/pricing`.
- **En historisk model har en forkert pris.** Ret prisen (tilsidesæt eller synkroniser igen) — omkostningen
  genberegnes ud fra antallet af tokens ved hver læsning af analysedata, så estimater opdateres med tilbagevirkende kraft.
- **Forbruget er forsinket i forhold til realtid.** Forbruget pr. nøgle behandles i batches; sænk
  `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS`, hvis du har brug for mere aktuelle tal.

---

Se [Brugervejledningen](./USER_GUIDE.md) og
[Funktionsgalleriet](./FEATURES.md) for at se, hvordan dette passer ind i det overordnede dashboard.
