# Claude Code CLI — Configuration with OmniRoute (Dansk)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇦 [ar](../../../ar/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

Peg **Claude Code**-CLI'en (`claude`) mod OmniRoute — lokalt eller på en ekstern VPS —
med profiler pr. model, der afspejler Codex-opsætningen.

---

## Hurtig start

```bash
# Start Claude Code mod en lokal OmniRoute (registrerer automatisk den aktive kontekst)
omniroute launch

# Mod en ekstern OmniRoute (efter `omniroute connect <host>` sker dette automatisk)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Generér profiler pr. model, og start derefter en af dem
omniroute setup-claude            # skriver ~/.claude/profiles/<name>/settings.json
omniroute launch --profile glm52  # Claude Code bruger glm/glm-5.2 via OmniRoute
```

---

## Sådan opretter Claude Code forbindelse til en gateway

Claude Code kommunikerer med **Anthropic Messages API** og peges mod et brugerdefineret
slutpunkt ved hjælp af miljøvariabler (det har ikke et `--base-url`-flag):

| Variabel                                     | Formål                                                                                        |
| -------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `ANTHROPIC_BASE_URL`                         | Gatewayens rod-URL (Claude Code tilføjer `/v1/messages`). **Intet `/v1`-suffiks.**            |
| `ANTHROPIC_AUTH_TOKEN`                       | Sendes som `Authorization: Bearer …` — brug dit OmniRoute-adgangstoken eller din API-nøgle    |
| `ANTHROPIC_API_KEY`                          | Alternativ: sendes som `x-api-key`. Hvis begge er angivet, har `ANTHROPIC_AUTH_TOKEN` forrang |
| `ANTHROPIC_MODEL`                            | Gennemtving en bestemt model (tilsidesætter standardvalget i `/model`)                        |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | `1` → den indbyggede `/model`-vælger viser `claude*`/`anthropic*`-modeller fra `/v1/models`   |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | Begræns antallet af outputtokens pr. svar (f.eks. `65536`)                                    |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | Tokengrænse for automatisk komprimering                                                       |

> Miljøvariabler læses **kun én gang ved opstart** — genstart Claude Code, når du har ændret dem.

`omniroute launch` angiver alle disse for dig: Kommandoen finder basis-URL'en og tokenet
fra den aktive kontekst (så `omniroute connect <vps>` efterfulgt af `omniroute launch`
bare virker), udfører et sundhedstjek af serveren og starter `claude` via exec.

---

## Discovery-aliasser — vis ikke-Claude-modeller i `/model`-vælgeren

Claude Codes modelregistrering via gateway viser kun id'er, der begynder med `claude`
eller `anthropic`, så når `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`, viser den
indbyggede `/model`-vælger normalt **kun** OmniRoutes Claude-/Anthropic-modeller — en
`kimi/kimi-k2.6` eller `glm/glm-5.2` vises aldrig, selvom den kan routes uden problemer.

OmniRoute kan spejle enhver aktiveret model (og kombination) under et `claude/…`-id, så den
passerer filteret og vises i vælgeren:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<kombination "custo-otimizado"> →  claude/combo/custo-otimizado
```

Når du vælger en af disse i Claude Code, fjerner OmniRoute `claude/`-omslaget
og gendanner det rigtige id før routing — et ægte `claude/<real-claude-model>`-id
(den faktiske Claude OAuth-udbyder) efterlades altid urørt.

**Dette er som standard deaktiveret** og styres af en portmekanisme på tre niveauer (det mest specifikke
har forrang), så en almindelig OmniRoute-installation aldrig fordobler sit katalog for klienter, der ikke bruger
Claude Code:

| Niveau  | Hvor                                                                                     |
| ------- | ---------------------------------------------------------------------------------------- |
| Model   | Udbyderens detaljeside → til/fra-knappen "Expose in Claude Code" pr. model               |
| Udbyder | Udbyderens detaljeside → til/fra-knap på udbyderniveau (omfatter alle dens modeller)     |
| Globalt | Indstillinger → Funktionsflag → `EXPOSE_CC_DISCOVERY_ALIASES` (deaktiveret som standard) |

Miljøvariablen `EXPOSE_CC_DISCOVERY_ALIASES` gennemtvinger aktivering på globalt niveau
og har forrang frem for tilsidesættelsen i kontrolpanelet (skærmen med funktionsflag viser
en bemærkning om "active via environment variable", når det er tilfældet). Til/fra-knapper pr. udbyder og
pr. model finjusterer derfra — f.eks. eksponerer globalt deaktiveret + Kimi-udbyder aktiveret
kun Kimis modeller.

> ⚠️ **Uoverensstemmelse i kontekstvindue for ikke-Claude-modeller.** Claude Code antager et kontekstvindue
> på 200K for ethvert id, det ikke genkender (det kan ikke læse det faktiske vindue fra
> `/v1/models`). For en model med et større vindue (f.eks. Kimi K2's 256K) skal du indstille
> `CLAUDE_CODE_AUTO_COMPACT_WINDOW` til en værdi under modellens faktiske vindue, så
> automatisk komprimering ikke udløses for tidligt. De genererede profiler ovenfor
> gør allerede dette pr. model.

---

## Onboarding-blok på dashboardet

Claude-værktøjskortet (**Dashboard → CLI Code**) viser det nøjagtige `settings.json`-fragment
for denne instans ved siden af infoknappen for discovery-aliasset sammen med en kopieringsknap:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<your OmniRoute>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<your OmniRoute API key>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

Basis-URL'en er den, som kortet har fundet (inklusive en brugerdefineret tilsidesættelse, du har indtastet), og er allerede
normaliseret — intet `/v1`-suffiks og ingen afsluttende skråstreg. **Nøglen vises aldrig**: blokken indeholder en
pladsholder, så et skærmbillede eller et indsat kodestykke ikke kan lække en nøgle. Indsæt din nøgle i stedet for den.

Tilføj `CLAUDE_CODE_AUTO_COMPACT_WINDOW` under den samme `env`-blok for enhver model, hvis reelle
kontekstvindue ikke er 200K — Claude Code antager 200K for hvert id, den ikke genkender, så
automatisk komprimering ellers aktiveres på det forkerte tidspunkt (se advarslen i forrige afsnit).
Kodestykke-generatoren accepterer også denne værdi, så en kalder, der kender målmodellens vindue,
kan generere den direkte.

Kilde: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (ren
generator, enhedstestet), vist af `ClaudeGatewayOnboardingBlock`.

---

## Profiler (`CLAUDE_CONFIG_DIR`)

Claude Code har **ingen indbyggede profilfiler** (i modsætning til Codex' `~/.codex/<name>.config.toml`).
Den anbefalede mekanisme er `CLAUDE_CONFIG_DIR` — en separat konfigurationsmappe for hver
profil, hver med sin egen `settings.json`, legitimationsoplysninger, historik og cache.

`omniroute setup-claude` henter det aktuelle `/v1/models`-katalog og skriver én
profil pr. model i `~/.claude/profiles/<name>/settings.json` ved at genbruge
**de samme navne som `setup-codex`** (`glm52`, `kimi-k27`, `deepseek-pro`, …):

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **Godkendelsestokenet skrives aldrig til profilen.** Start med
> `omniroute launch --profile <name>` (den injicerer `ANTHROPIC_AUTH_TOKEN` fra den
> aktive kontekst), eller eksportér selv `ANTHROPIC_AUTH_TOKEN`, og kør
> `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude`.

**Automatisk synkronisering efter modelregistrering (valgfrit).** OmniRoute kan automatisk regenerere de samme
`~/.claude/profiles/<name>/settings.json`-filer, hver gang en synkronisering af udbydermodeller
ændrer det aktuelle katalog — så nye eller omdøbte modeller får profiler, uden at kommandoen skal køres igen.
Det er **deaktiveret som standard**: slå det til fra **CLI Code-dashboardet** ("Automatisk synkronisering af
CLI-profiler" → Claude Code), eller indstil `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` (den respekterer også
`CLI_ALLOW_CONFIG_WRITES`, som er aktiveret som standard). Når funktionen er aktiveret, skriver den kun profilfiler; den ændrer aldrig
din aktive Claude-standardkonfiguration, godkendelse eller `~/.claude/settings.json`.

### Generering og brug af profiler

```bash
# Lokal OmniRoute
omniroute setup-claude

# Ekstern VPS (indlejrer VPS-URL'en i hver profil)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# Kun nogle udbydere
omniroute setup-claude --only glm,kimi

# Forhåndsvisning uden at skrive
omniroute setup-claude --dry-run

# Start en profil
omniroute launch --profile kimi-k27
```

---

## Modelniveauer (valgfrit)

Claude Code dirigerer til kapabilitetsniveauer. Knyt hvert niveau til en OmniRoute-model via miljøvariabler /
indstillinger, hvis du vil bruge forskellige udbydere pr. niveau:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

Ellers bruges én enkelt `ANTHROPIC_MODEL` (som profiler indstiller) til alt.

---

## Fjerntilstand

Når du har kørt `omniroute connect <host>` (se
[Fjerntilstand](./REMOTE-MODE.md)), målretter `omniroute launch` og `omniroute setup-claude`
automatisk den pågældende fjernserver og bruger dens adgangstoken med begrænset omfang — ingen
ekstra flag er nødvendige. Tilsidesæt for den enkelte kørsel med `--remote` / `--api-key`.

---

## Fejlfinding

**Claude Code ignorerer gatewayen** — bekræft, at `ANTHROPIC_BASE_URL` **ikke har
`/v1`**, og genstart `claude` (miljøvariabler indlæses kun én gang ved opstart). `omniroute launch`
håndterer dette for dig.

**`/model`-vælgeren er tom / mangler gatewaymodeller** — kræver Claude Code
v2.1.219+ og `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`. Kun model-id'er med `claude*` /
`anthropic*` vises i vælgeren; gennemtving enhver anden model med
`ANTHROPIC_MODEL=<id>` (det er det, profiler gør).

**`400 Ambiguous model 'claude-…'`** — Claude Code sender altid model-id'er **uden præfiks**
(f.eks. `claude-opus-4-8`), så når både Claude Code-udbyderen (`cc/…`) og
Claude-udbyderen (`claude/…`) er forbundet, matcher id'et uden præfiks to ruter, og
OmniRoute nægter at gætte. Løs det på en af følgende måder: Fastlås et id med præfiks via
`ANTHROPIC_MODEL=cc/claude-opus-4-8`, eller aktivér **Foretræk Claude Code for
Claude-modeller uden præfiks** — knappen på Claude-udbydersiden eller
`OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` (deaktiveret som standard;
se [Miljø](../reference/ENVIRONMENT.md)) — hvilket dirigerer `claude-*`-id'er uden præfiks
til Claude Code i stedet. Eksplicitte udbyderpræfikser har altid forrang.

**Godkendelsesfejl** — profilen indeholder intet token. Brug `omniroute launch --profile`
(indsætter det), eller eksportér `ANTHROPIC_AUTH_TOKEN`.

**Profiler er ikke isolerede** — hver profil er en separat `CLAUDE_CONFIG_DIR`;
kontrollér, at `echo $CLAUDE_CONFIG_DIR` inde i sessionen peger på
`~/.claude/profiles/<name>`.
