# OpenCode v2 plugin — install and configure (Português (Portugal))

🌐 **Languages:** 🇺🇸 [English](../../../../guides/OPENCODE-V2-PLUGIN.md) · 🇪🇹 [am](../../../am/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇸🇦 [ar](../../../ar/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇦🇿 [az](../../../az/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇧🇬 [bg](../../../bg/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇧🇩 [bn](../../../bn/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇧🇦 [bs](../../../bs/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇨🇿 [cs](../../../cs/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇩🇰 [da](../../../da/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇩🇪 [de](../../../de/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇬🇷 [el](../../../el/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇪🇸 [es](../../../es/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇪🇪 [et](../../../et/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇷 [fa](../../../fa/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇫🇮 [fi](../../../fi/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇫🇷 [fr](../../../fr/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇪 [ga](../../../ga/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [gu](../../../gu/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇳🇬 [ha](../../../ha/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇱 [he](../../../he/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [hi](../../../hi/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇭🇷 [hr](../../../hr/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇭🇺 [hu](../../../hu/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇦🇲 [hy](../../../hy/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇩 [id](../../../id/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇳🇬 [ig](../../../ig/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇹 [it](../../../it/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇯🇵 [ja](../../../ja/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇬🇪 [ka](../../../ka/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇰🇭 [km](../../../km/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [kn](../../../kn/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇰🇷 [ko](../../../ko/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇱🇹 [lt](../../../lt/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇱🇻 [lv](../../../lv/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [ml](../../../ml/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [mr](../../../mr/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇲🇾 [ms](../../../ms/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇲🇹 [mt](../../../mt/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇲🇲 [my](../../../my/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇳🇵 [ne](../../../ne/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇳🇱 [nl](../../../nl/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇳🇴 [no](../../../no/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [or](../../../or/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [pa](../../../pa/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇵🇭 [phi](../../../phi/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇵🇱 [pl](../../../pl/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇷🇴 [ro](../../../ro/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇷🇺 [ru](../../../ru/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇱🇰 [si](../../../si/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇸🇰 [sk](../../../sk/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇸🇮 [sl](../../../sl/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇷🇸 [sr](../../../sr/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇸🇪 [sv](../../../sv/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇰🇪 [sw](../../../sw/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [ta](../../../ta/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇮🇳 [te](../../../te/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇹🇭 [th](../../../th/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇹🇷 [tr](../../../tr/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇵🇰 [ur](../../../ur/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇺🇿 [uz](../../../uz/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇻🇳 [vi](../../../vi/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇳🇬 [yo](../../../yo/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/OPENCODE-V2-PLUGIN.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/OPENCODE-V2-PLUGIN.md)

---

`@omniroute/opencode-plugin-v2` disponibiliza todo o seu catálogo OmniRoute — modelos, combos e
auto-combos — no seletor de modelos do OpenCode v2, com nomes de apresentação, preços e limites
do nível gratuito.

É um pacote separado de `@omniroute/opencode-plugin`, porque o OpenCode v1 e o v2 carregam
plugins através de contratos diferentes. Escolha o que corresponde à versão principal do seu
OpenCode; nada é partilhado entre ambos, pelo que atualizar um nunca obriga a atualizar o outro.

## Requisitos

- OpenCode v2.
- Um gateway OmniRoute acessível (`http://localhost:20128` por predefinição).
- Node.js 22 ou 24.

## Instalação

Adicione o plugin a `opencode.json`:

```json
{
  "plugins": [
    {
      "package": "@omniroute/opencode-plugin-v2",
      "options": {
        "providerId": "omniroute",
        "baseURL": "http://localhost:20128"
      }
    }
  ]
}
```

Os modelos aparecem então como `omniroute/<provider>/<model>`; `providerId` determina esse prefixo e
o id da integração sob o qual o OpenCode armazena a credencial.

## Credenciais

O plugin procura uma chave do gateway em três locais, por esta ordem:

1. **A credencial que associou no OpenCode.** O plugin regista uma integração, pelo que
   o próprio fluxo de autenticação do OpenCode pode armazenar a chave. Nada é guardado em `opencode.json` — prefira esta opção.
2. `apiKey` nas opções do plugin, para uma substituição específica do projeto. Isto coloca a chave num ficheiro que
   poderá estar a incluir no repositório.
3. `OMNIROUTE_API_KEY` no ambiente.

Sem nenhuma das três, o catálogo fica vazio e o plugin informa-o uma vez no arranque, em vez de
deixar silenciosamente um seletor vazio.

### O token de gestão é uma chave diferente

Os combos, o estado dos fornecedores e o enriquecimento (nomes de apresentação, preços e limites do nível gratuito) provêm dos
endpoints `/api/*` do gateway, que a maioria das implementações protege com um token de **gestão**
em vez da chave de inferência:

```json
"options": {
  "baseURL": "http://localhost:20128",
  "managementReadToken": "<token de leitura de gestão>"
}
```

Se `managementReadToken` não estiver definido, é usado `apiKey` como alternativa. Quando um gateway rejeita essa alternativa,
o catálogo continua a ser publicado, mas com ids de modelos em bruto em vez de nomes de apresentação, sem
eliminação de duplicados por alias canónico, sem preços e sem combos. O plugin apresenta um aviso uma vez por endpoint quando isso acontece,
indicando o endpoint e o que foi perdido — para que um seletor degradado nunca seja um mistério.

## Opções

| Chave                            | Predefinição                                     | Notas                                                                                                                                         |
| -------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `providerId`                     | `"omniroute"`                                    | Id do fornecedor, id da integração e o prefixo sob o qual os modelos aparecem                                                                 |
| `baseURL`                        | obrigatório                                      | Raiz do gateway, apenas `http(s)`; o sufixo `/v1` é adicionado quando necessário                                                              |
| `apiKey`                         | credencial associada, depois `OMNIROUTE_API_KEY` | Chave de chat para `/v1/*`                                                                                                                    |
| `managementReadToken`            | recorre a `apiKey`                               | Chave para `/api/*` — normalmente **não** é a mesma                                                                                           |
| `displayName`                    | `"OmniRoute"`                                    | Nome do fornecedor no seletor                                                                                                                 |
| `timeoutMs`                      | `10000`                                          | Tempo limite de obtenção por endpoint (os auto-combos usam 5 s)                                                                               |
| `modelCacheTtlMs`                | `300000`                                         | TTL da cache do catálogo; uma captura em disco acelera os arranques a frio                                                                    |
| `timeouts`                       | recorre a `timeoutMs`                            | Limites por endpoint em ms: `models`, `combos`, `autoCombos`, `enrichment`                                                                    |
| `enrichment`                     | `true`                                           | Obtém nomes, preços e limites do nível gratuito                                                                                               |
| `providerTag`                    | `true`                                           | Adiciona ao nome de apresentação o prefixo do fornecedor a montante para o qual encaminha                                                     |
| `usableOnly`                     | `false`                                          | Mantém apenas os fornecedores que o gateway indica como aprovisionados                                                                        |
| `visibleModels` / `hiddenModels` | `[]`                                             | Listas de permissão por correspondência exata ou por sufixo; a negação prevalece                                                              |
| `geminiSanitization`             | `true`                                           | Remove dos esquemas de ferramentas as palavras-chave JSON Schema que o Gemini rejeita (as ferramentas `$ref` são encaminhadas sem alterações) |
| `apiFormat.allowAnthropic`       | `false`                                          | Encaminha os ids presentes na lista de permissões através do bloco da API Anthropic                                                           |
| `apiFormat.anthropicModels`      | `[]`                                             | Ids completos dos modelos encaminhados para a Anthropic                                                                                       |
| `logLevel` / `startupDebug`      | `warn` / `false`                                 | Nível de detalhe do registo                                                                                                                   |

## Como o catálogo se mantém atualizado

O catálogo é obtido de forma diferida e mantido em cache durante `modelCacheTtlMs`, e um instantâneo em disco mantém o
último catálogo conhecido disponível quando o gateway está inacessível — uma indisponibilidade apenas afeta a
atualidade. Os modelos e as combinações são publicados assim que chegam; as combinações automáticas, a lista de fornecedores
e a camada de enriquecimento são integradas quando ficam disponíveis, para que um endpoint lento não bloqueie
todo o seletor.

Os nomes incluem o que o gateway conhece: o fornecedor a montante para o qual um modelo é encaminhado, um marcador `[Free]`
e o orçamento associado — assim, duas ligações que disponibilizam o mesmo modelo continuam a ser
distinguíveis. Desative o prefixo com `"providerTag": false`.

Não existe um comando de atualização manual: os comandos do OpenCode v2 são modelos de prompts, não callbacks,
pelo que um plugin não pode disponibilizar um. As atualizações seguem o TTL; só é solicitado ao anfitrião que recarregue
quando o catálogo ou a camada de enriquecimento tiver realmente mudado, nunca a cada atualização.

## Chamadas de ferramentas no Gemini

O Gemini rejeita qualquer pedido cujas declarações de ferramentas incluam `$schema` ou
`additionalProperties`, respondendo com `400 INVALID_ARGUMENT`. O plugin remove essas palavras-chave dos
esquemas de ferramentas destinados a um modelo Gemini deste fornecedor e deixa todos os outros pedidos inalterados.
Uma ferramenta que inclua um `$ref` é reencaminhada sem alterações em vez de ser modificada, uma vez que remover a
referência alargaria o esquema para «aceitar qualquer coisa».
Defina `"geminiSanitization": false` para desativar esta funcionalidade.

## Resolução de problemas

| Sintoma                                             | Causa                                                                                                                     |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| O seletor não mostra nenhum modelo OmniRoute        | Não foi encontrada nenhuma chave (verifique o aviso no arranque) ou o gateway está inacessível                            |
| IDs de modelos em bruto, sem combinações nem preços | Os endpoints de gestão recusaram o token — defina `managementReadToken`                                                   |
| Uma sessão fixada em `opencode-<id>/…`              | O plugin v1 publicava `opencode-<id>`; a v2 publica apenas `<id>`, pelo que deve voltar a selecionar o modelo em `<id>/…` |

## Consulte também

- [CLI-INTEGRATIONS.md](CLI-INTEGRATIONS.md) — todas as integrações `setup-*` da CLI, incluindo a
  configuração OpenCode leve e compatível com a OpenAI.
- [REMOTE-MODE.md](REMOTE-MODE.md) — como direcionar uma CLI para um gateway remoto.
