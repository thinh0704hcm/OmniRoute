# open-sse Architecture (Português (Portugal))

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Resumindo**: `open-sse/` é o motor principal de streaming que processa todos os pedidos a LLM no OmniRoute. Contém ~1.400 ficheiros que implementam o pipeline de pedidos, executores, serviços, servidor MCP e camada de tradução. Este guia explica como as diferentes partes se integram.

**Fonte:** `open-sse/` (pacote do workspace, ~1.440 ficheiros `.ts` que não são de teste)

---

## Porquê um Pacote de Workspace Separado?

`open-sse/` é um **workspace autónomo** no monorepo do OmniRoute por vários motivos:

1. **Reutilização** — `open-sse` é publicado como `@omniroute/open-sse` no npm, pelo que outros projetos podem utilizá-lo de forma independente
2. **Limites bem definidos** — o motor de streaming está desacoplado da camada de UI/BD específica do OmniRoute
3. **Desempenho** — o motor não tem dependências do Next.js, permitindo arranques a frio mais rápidos em contextos de CLI/serverless
4. **Controlo de versões** — `open-sse` pode publicar versões segundo o seu próprio calendário

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Estrutura de Nível Superior

```
open-sse/
├── index.ts              # Ponto de entrada público
├── types.d.ts            # Exportações de tipos públicos
├── package.json          # @omniroute/open-sse
├── config/               # Configurações de fornecedores, constantes, registos
├── executors/            # Executores HTTP por fornecedor (67 + base.ts/index.ts)
├── handlers/             # Processadores de pedidos (chatCore, responses, etc.)
├── lib/                  # Utilitários internos
├── mcp-server/           # Servidor Model Context Protocol
├── services/             # ~298 módulos de serviço
├── transformer/          # Transformador de formato da Responses API
├── translator/           # Tradução de formatos (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Utilitários partilhados (registo, erros, streaming, etc.)
```

### Número de Módulos

| Diretório | Ficheiros | Finalidade |
| `executors/` | 167 | Executores HTTP por fornecedor (unificados através da fábrica DefaultExecutor) |
| `handlers/` | 157 | Pontos de entrada de pedidos (chatCore, responses, embeddings) |
| `services/` | ~536 | Encaminhamento, colocação em cache, limitação de taxa, atualização, etc. |
| `translator/` | 56 | Conversão de formatos (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Ferramentas e transportes MCP |
| `utils/` | ~108 | Utilitários transversais (registo, erros, streaming) |
| `config/` | ~339 | Configurações de fornecedores, constantes, registos |

---

## O Pipeline de Pedidos

Todos os pedidos a LLM passam por um **pipeline de 5 etapas**:

```
                              ┌──────────────┐
   Pedido HTTP                │  1. ENCAMINHAR│  resolução de combinações, seleção do modelo
   (rota do Next.js)          └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TRADUZIR │   conversão de formatos (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. EXECUTAR │   executor do fornecedor, HTTP, repetição, disjuntor
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAMING│   transformação SSE, contrapressão
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. REGISTAR │   acompanhamento da utilização, registo de chamadas, classificação de erros
                              └──────┬───────┘
                                     │
                                     ▼
                              Resposta HTTP (SSE ou JSON)
```

### Etapa 1: Encaminhar (services/combo.ts)

**Ponto de entrada**: `handleComboChat()` em `services/combo.ts`

Resolve o pedido para um tuplo concreto `(provider, model, account, credentials)`:

- Procura a combinação pelo ID (ou cria uma combinação virtual para modelos `auto/*`)
- Aplica a estratégia de encaminhamento (prioridade, ponderada, round-robin, etc.)
- Exclui fornecedores que não estejam operacionais (disjuntor)
- Escolhe o próximo destino viável

Para modelos `auto/*`, esta etapa também:

- Executa o algoritmo de **pontuação de 16 fatores** (`services/autoCombo/`)
- Seleciona um par `provider+model` com base no estado, custo, latência, etc.

### Etapa 2: Traduzir (translator/)

Se o formato de origem (por exemplo, OpenAI) for diferente do formato de destino (por exemplo, Claude), o pedido é **traduzido**:

- Prompt de sistema → mensagem de sistema
- Definições de ferramentas → formato de ferramentas específico do fornecedor
- Parâmetros de raciocínio/pensamento → equivalentes específicos do fornecedor
- Normalização das funções das mensagens (`developer` → `system` para sistemas que não sejam OpenAI)

O `translator/index.ts` expõe:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Etapa 3: Executar (executors/)

**Ponto de entrada**: `getExecutor(providerId).execute(request, options)`

Todos os fornecedores utilizam `DefaultExecutor` (`executors/default.ts`) através do recurso alternativo da fábrica `getExecutor()`. O executor:

- Constrói o URL a montante (`buildUrl()`)
- Adiciona cabeçalhos específicos do fornecedor (`buildHeaders()`)
- Transforma o corpo do pedido (`transformRequest()`)
- Envia o pedido HTTP com repetição + recuo exponencial
- Gere a atualização da autenticação, caso seja necessária (fornecedores OAuth)

Todos os executores estendem `BaseExecutor` (`executors/base.ts`, 1170 linhas de código), que disponibiliza:

- Lógica de repetição comum
- Integração com proxy
- Integração com disjuntor
- Hooks de registo da utilização

### Etapa 4: Streaming (utils/stream.ts)

Para respostas em streaming, o executor devolve um **ReadableStream**. O processador:

- Encaminha os dados através de uma transformação SSE (`createSSETransformStreamWithLogger`)
- Aplica pings periódicos para detetar ligações inativas
- Gere corretamente a desconexão do cliente (`pipeWithDisconnect`)
- Transforma SSE → JSON para clientes sem streaming

Para respostas sem streaming, o executor devolve um objeto JSON analisado, que é transmitido sem alterações.

### Etapa 5: Registar (services/usage.ts)

Após a resposta (com êxito ou falha), a utilização é registada:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` da resposta
- `cost_usd` calculado a partir dos dados de preços
- `latency_ms`, `status`, `error_class` em caso de falha
- Persistida na tabela `usage_history`

Os artefactos de registo de chamadas (se ativados) são escritos em `${DATA_DIR}/call_logs/`.

---

## Análise Detalhada dos Ficheiros Principais

### chatCore.ts (5977 linhas)

O **processador principal de pedidos**. Apesar da sua dimensão, tem uma estrutura clara:

```ts
// Pseudoestrutura de chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Autenticação + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Validação do corpo
  const body = await parseRequestBody(request);

  // 3. Deteção de formato + tradução
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Encaminhamento do combo
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Continuar para o alvo seguinte
    }
  }

  // 5. Alternativa de emergência
  return await emergencyFallback(body);
}
```

Apesar de ser uma única função gigante, está organizada em **secções comentadas** que correspondem ao pipeline de 5 etapas.

### combo.ts (4456 LOC)

O **motor de encaminhamento** que resolve um combo numa sequência ordenada de alvos.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Suporta **19 estratégias de encaminhamento** (consulte `src/shared/constants/routingStrategies.ts`):

| Estratégia          | Comportamento                                                                    |
| ------------------- | -------------------------------------------------------------------------------- |
| `priority`          | Lista ordenada com o primeiro alvo em primeiro lugar                             |
| `weighted`          | Probabilístico, de acordo com o peso de cada alvo                                |
| `round-robin`       | Percorre os alvos sequencialmente                                                |
| `context-relay`     | Transfere o contexto entre alvos                                                 |
| `fill-first`        | Preenche a quota antes de avançar para o alvo seguinte                           |
| `p2c`               | Potência de duas escolhas                                                        |
| `random`            | Aleatório uniforme                                                               |
| `least-used`        | Seleciona aquele com menos utilizações recentes                                  |
| `cost-optimized`    | Alvo saudável mais barato em primeiro lugar                                      |
| `reset-aware`       | Tem em conta as janelas de reposição do fornecedor                               |
| `reset-window`      | Encaminhamento baseado na janela de reposição                                    |
| `headroom`          | Maior margem de quota restante em primeiro lugar                                 |
| `strict-random`     | Verdadeiramente uniforme (sem ponderação de qualidade)                           |
| `auto`              | Utiliza uma pontuação de 16 fatores (`autoCombo/`)                               |
| `lkgp`              | Último fornecedor conhecido como funcional em primeiro lugar                     |
| `context-optimized` | Mais adequado para pedidos com contexto longo                                    |
| `fusion`            | Distribui em paralelo por um painel e sintetiza através de um juiz (`fusion.ts`) |

### base.ts (1170 LOC)

O **executor abstrato** que serve de base aos 107 executores. Contém:

- `buildUrl()` — construção predefinida do URL (as subclasses substituem-na para comportamentos personalizados)
- `buildHeaders()` — cabeçalhos predefinidos (autenticação, tipo de conteúdo)
- `transformRequest()` — passagem direta por predefinição
- `execute()` — o ciclo HTTP principal com novas tentativas/recuo/disjuntor

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Processa todos os fornecedores compatíveis com OpenAI/Anthropic
  // Os fornecedores registam configurações (URL, autenticação, cabeçalhos), mas partilham a lógica do executor
}
```

O comportamento específico de cada fornecedor (cabeçalhos de autenticação, URL base, cabeçalhos de versão) é configurado através do registo de fornecedores, e não de classes de executor separadas.

````

---

## Serviços (117 módulos)

Os serviços são **módulos focados e de finalidade única** que os handlers combinam. As principais categorias:

### Encaminhamento e Combo

- `combo.ts` — ponto de entrada para pedidos encaminhados por combo
- `services/autoCombo/` — pontuação baseada em 16 fatores, 8 estratégias de encaminhamento automático
- `wildcardRouter.ts` — corresponde a rotas com caracteres universais (`gpt-*`)
- `modelFamilyFallback.ts` — fallback T5 dentro da mesma família

### Limitação de Taxa e Quota

- `rateLimitManager.ts` — token bucket por chave+fornecedor
- `usage.ts` — registo de utilização
- `quotaCache.ts` — instantâneos de quota em memória

### Conta e Token

- `tokenRefresh.ts` — atualização OAuth em caso de 401
- `accountFallback.ts` — muda para uma conta alternativa
- `sessionManager.ts` — estado da sessão com vários turnos

### Inteligência

- `intentClassifier.ts` — classifica a intenção do pedido
- `taskAwareRouter.ts` — encaminha por tipo de tarefa
- `thinkingBudget.ts` — atribui tokens de raciocínio
- `contextManager.ts` — injeta contexto de encaminhamento

### Resiliência

- `resilience.ts` — orquestração de novas tentativas, backoff e circuit breaker
- `emergencyFallback.ts` — fallback de último recurso
- `modelDeprecation.ts` — encaminhamento automático para modelos sucessores

### Estado

- `signatureCache.ts` — desduplicação pela assinatura do pedido
- `volumeDetector.ts` — redução de carga
- `contextHandoff.ts` — serialização da sessão

### Compressão

- `compression/` (subdiretório) — pipeline completo de compressão
- 39 ficheiros que abrangem motores, conjuntos de regras e adaptadores

### Competências

- (abordadas em [SKILLS.md](./SKILLS.md))

### Memória

- (abordada em [MEMORY.md](./MEMORY.md))

---

## Executores (mais de 75 ficheiros)

Um ficheiro por fornecedor. Todos estendem `BaseExecutor` e substituem o que difere.

### Padrões Comuns

Os fornecedores são resolvidos através de `getExecutor(providerId)`, que devolve o executor configurado. Os fornecedores compatíveis com OpenAI/Anthropic utilizam `DefaultExecutor` (`executors/default.ts`). O comportamento específico de cada fornecedor (URL base, cabeçalhos de autenticação, versão da API) é configurado em `open-sse/config/providers/`, enquanto as transformações do corpo do pedido são tratadas em `open-sse/translator/`.

O **URL personalizado** é definido através da configuração do fornecedor:

```ts
// Configuração do fornecedor em open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

A **autenticação personalizada** é tratada através da configuração de autenticação do registo de fornecedores (chave de API, OAuth, perfis de cabeçalhos).

As transformações personalizadas do **corpo do pedido** (por exemplo, a separação de `system` de `messages` pela Anthropic) são registadas por fornecedor em `open-sse/translator/`.

````

### A Fábrica de Executores

`executors/index.ts` exporta `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

A resolução é efetuada através de `ExecutorRegistry` (`executors/registry.ts`): cada executor especializado é declarado na tabela incorporada de `executors/index.ts` e registado através de `registerExecutor(alias, instance)` durante o carregamento do módulo; `getExecutor()` consulta o registo e recorre a um `DefaultExecutor` memoizado para qualquer fornecedor sem uma entrada especializada. O mapeamento completo entre alias → executor é caracterizado pelo teste golden `tests/unit/executor-map-golden.test.ts`.

---

## Tradutores

Traduza entre **3 formatos**: OpenAI, Anthropic, Gemini, além da nova Responses API.

### Quando é efetuada a tradução

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Traduções comuns:

- `OpenAI → Anthropic`: campo `system` separado, cabeçalho `x-api-key`
- `OpenAI → Gemini`: `contents` em vez de `messages`, `systemInstruction`
- `OpenAI → Responses API`: matriz `input`, estado `previous_response_id`

### Casos especiais tratados

- Papel `developer` → `system` para formatos que não sejam OpenAI
- Papel `system` → incorporado na primeira mensagem do utilizador para GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` do Gemini
- `tools` → formato de ferramentas específico do fornecedor
- Parâmetros de raciocínio (o1, Claude) → equivalentes específicos do fornecedor

---

## Servidor MCP

`open-sse/mcp-server/` implementa o servidor **Model Context Protocol**:

- **110 ferramentas** (gestão de fornecedores, combinações, memória, cache, compressão, proxy, competências, gamificação, plugins, Notion, Obsidian, corpus local)
- **3 transportes**: stdio, SSE, Streamable HTTP
- **33 âmbitos** para autorização granular

### Registo de ferramentas

As ferramentas são registadas como ficheiros autónomos em `open-sse/mcp-server/tools/`, cada um exportando um nome, esquema, processador e âmbito:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transportes

```ts
// stdio (utilização na CLI)
startMcpStdio(server);

// SSE (transmissão baseada em HTTP)
startMcpSse(server, port);

// Streamable HTTP (MCP moderno)
startMcpStreamable(server, port);
```

### Autorização

Todas as chamadas de ferramentas passam por verificações de âmbito (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Transformadores

`open-sse/transformer/` converte entre os formatos **Chat Completions** e **Responses API**.

### Porquê um transformador separado?

A Responses API é o novo formato da OpenAI com **conversas com estado** (`previous_response_id`). Quando um cliente envia um pedido Responses, o OmniRoute:

1. Converte internamente Responses → Chat Completions
2. Envia para o fornecedor (qualquer fornecedor que suporte Chat Completions)
3. Converte a resposta novamente para o formato Responses
4. Transmite a resposta convertida ao cliente

O transformador (`transformer/responsesTransformer.ts`) fornece:

```ts
createResponsesApiTransformStream(): TransformStream
```

Este processa:

- Eventos `response.output_item.added`
- Eventos `response.output_text.delta`
- Evento `response.completed`
- Mapeamento de chamadas de ferramentas (`function_call` ↔ `tool_calls`)

---

## Configuração

`open-sse/config/` contém a camada de configuração:

| Ficheiro                       | Finalidade                                                      |
| ------------------------------ | --------------------------------------------------------------- |
| `providerRegistry.ts`          | Registo de modelos de chat sobre o catálogo de 352 fornecedores |
| `providerModels.ts`            | Aliases de modelos, mapeamento de formatos                      |
| `constants.ts`                 | Tempos limite, limites, códigos de estado                       |
| `defaultThinkingSignature.ts`  | Assinatura de raciocínio predefinida do Claude                  |
| `modelStrip.ts` (nos serviços) | Remoção de campos por fornecedor                                |

### Esquema do registo de fornecedores

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

A validação Zod durante o carregamento do módulo garante que todas as configurações de fornecedores são válidas.

---

## Restrições de Desempenho

O motor de encaminhamento tem limites de desempenho rigorosos:

| Operação                                                    | Objetivo | Medição                         |
| ----------------------------------------------------------- | -------- | ------------------------------- |
| Resolução de combinações                                    | <10ms    | Para 50 destinos                |
| Verificação do limite de taxa                               | <1ms     | Token bucket em memória         |
| Fallback da família de modelos                              | <5ms     | Definições de famílias em cache |
| Despacho do encaminhamento de pedidos                       | <2ms     | Caminho crítico                 |
| **Sem I/O bloqueante no caminho crítico do encaminhamento** | —        | Tudo assíncrono                 |

---

## Antipadrões

❌ **Chamadas síncronas à BD em `combo.ts`** — pré-calcular e colocar em cache
❌ **Lógica de repetição nos handlers** — utilizar `retry()` do serviço de resiliência
❌ **Acesso direto à configuração do fornecedor** — utilizar os getters de `providerRegistry`
❌ **Cadeias de fallback codificadas diretamente** — definir em `modelFamilyFallback.ts`
❌ **Mutações de estado entre pedidos concorrentes** — utilizar apenas contexto ao nível do pedido

---

## Adicionar um Novo Componente

### Adicionar um Novo Serviço

1. Criar `open-sse/services/[serviceName].ts` com uma responsabilidade específica
2. Exportar a função principal do handler e quaisquer constantes
3. Adicionar testes unitários em `tests/unit/services/[serviceName].test.mjs`
4. Integrar no pipeline de pedidos em `handlers/chatCore.ts` (se estiver relacionado com o encaminhamento)
5. Atualizar a lógica de encaminhamento em `combo.ts` se o serviço afetar a seleção de destinos
6. Documentar neste ficheiro

### Adicionar um Novo Executor

1. Criar `open-sse/executors/[provider].ts` que estenda `BaseExecutor`
2. Registar em `config/providerRegistry.ts`
3. Adicionar à factory de `executors/index.ts`
4. Adicionar testes unitários para o executor
5. Documentar em `docs/architecture/ARCHITECTURE.md`

### Adicionar uma Nova Ferramenta MCP

1. Criar ou atualizar `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definir o esquema Zod para os dados de entrada
3. Registar a ferramenta em `mcp-server/index.ts`
4. Adicionar à matriz de âmbitos em `mcp-server/auth/`
5. Adicionar testes unitários

---

## Ver Também

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — arquitetura de alto nível
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — referência de engenharia
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — descrição diretório a diretório
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — pontuação baseada em 16 fatores
- [MCP-SERVER.md](./MCP-SERVER.md) — servidor MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — servidor A2A
- Código-fonte: `open-sse/` (mais de 400 ficheiros, ~143 mil linhas de código)
