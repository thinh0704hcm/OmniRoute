# open-sse Architecture (Igbo)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Na nkenke**: `open-sse/` bụ injin nkwanye data bụ isi nke na-akwado arịrịọ LLM niile dị na OmniRoute. Ọ nwere ihe dị ka faịlụ 1,400 ndị na-arụpụta usoro nhazi arịrịọ, ndị mmebe arịrịọ, ọrụ, sava MCP, na oyi akwa ntụgharị. Ntuziaka a na-akọwa etu akụkụ ndị a si arụkọ ọrụ.

**Isi mmalite:** `open-sse/` (ngwugwu workspace, ihe dị ka faịlụ `.ts` 1,440 na-abụghị nke nnwale)

---

## Gịnị Mere E Ji Nwee Ngwugwu Workspace Dị Iche?

`open-sse/` bụ **workspace kwụụrụ onwe ya** n'ime monorepo OmniRoute n'ihi ọtụtụ ihe:

1. **Enwere ike iji ya ọzọ** — a na-ebipụta `open-sse` dị ka `@omniroute/open-sse` na npm, ya mere ọrụ ndị ọzọ nwere ike iji ya n'onwe ha
2. **Oke doro anya** — e kewapụrụ injin nkwanye data ahụ na oyi akwa UI/DB nke OmniRoute
3. **Ọsọ arụmọrụ** — injin ahụ enweghị ndabere Next.js, nke na-eme ka mmalite oyi dị ngwa karịa n'ọnọdụ CLI/serverless
4. **Njikwa ụdị** — `open-sse` nwere ike iwepụta ụdị ọhụrụ dịka usoro oge nke ya si dị

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Nhazi Ọkwa Elu

```
open-sse/
├── index.ts              # Ebe ntinye ọha
├── types.d.ts            # Mbupụ ụdị maka ọha
├── package.json          # @omniroute/open-sse
├── config/               # Nhazi ndị na-eweta ọrụ, constants, na registries
├── executors/            # Ndị mmebe HTTP maka onye na-eweta ọrụ ọ bụla (67 + base.ts/index.ts)
├── handlers/             # Ndị njikwa arịrịọ (chatCore, responses, wdg.)
├── lib/                  # Ngwa enyemaka ime
├── mcp-server/           # Sava Model Context Protocol
├── services/             # Ihe dị ka modulu ọrụ 298
├── transformer/          # Ihe ntụgharị usoro Responses API
├── translator/           # Ntụgharị usoro (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Ngwa enyemaka ndị a na-ekekọrịta (ndekọ, njehie, nkwanye data, wdg.)
```

### Ọnụọgụ Modulu

| Ndekọ | Faịlụ | Ebumnuche |
| `executors/` | 167 | Ndị mmebe HTTP maka onye na-eweta ọrụ ọ bụla (ejikọrọ site na factory DefaultExecutor) |
| `handlers/` | 157 | Ebe ntinye arịrịọ (chatCore, responses, embeddings) |
| `services/` | ~536 | Ndugharị ụzọ, caching, mmachi ọsọ, mmegharị ọhụrụ, wdg. |
| `translator/` | 56 | Ntụgharị usoro (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | Ngwa ọrụ na ụzọ mbufe MCP |
| `utils/` | ~108 | Ngwa enyemaka na-emetụta akụkụ dị iche iche (ndekọ, njehie, nkwanye data) |
| `config/` | ~339 | Nhazi ndị na-eweta ọrụ, constants, na registries |

---

## Usoro Nhazi Arịrịọ

Arịrịọ LLM ọ bụla na-agafe **usoro nwere ọkwa 5**:

```
                              ┌──────────────┐
   Arịrịọ HTTP                 │  1. ROUTE    │   idozi combo, ịhọrọ model
   (ụzọ Next.js)               └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TRANSLATE│   ntụgharị usoro (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. EXECUTE  │   executor onye na-eweta ọrụ, HTTP, nnwale ọzọ, breaker
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STREAM   │   mgbanwe SSE, backpressure
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. RECORD   │   nsuso ojiji, ndekọ oku, nhazi njehie
                              └──────┬───────┘
                                     │
                                     ▼
                              Nzaghachi HTTP (SSE ma ọ bụ JSON)
```

### Ọkwa 1: Duzie Ụzọ (services/combo.ts)

**Ebe ntinye**: `handleComboChat()` dị na `services/combo.ts`

Ọ na-edozi arịrịọ ahụ ka ọ bụrụ tuple `(provider, model, account, credentials)` doro anya:

- Chọta combo site na ID (ma ọ bụ wuo combo mebere maka model `auto/*`)
- Tinye atụmatụ ndugharị ụzọ n'ọrụ (mkpa, ịdị arọ, round-robin, wdg.)
- Wepụ ndị na-eweta ọrụ na-adịghị arụ ọrụ nke ọma (circuit breaker)
- Họrọ ebe ọzọ nwere ike ịrụ ọrụ

Maka model `auto/*`, ọkwa a na-emekwa ihe ndị a:

- Na-agba algọridim **ntule ihe 16** (`services/autoCombo/`)
- Na-ahọrọ ụzọ abụọ `provider+model` dabere na ahụike, ọnụ ahịa, latency, wdg.

### Ọkwa 2: Tụgharịa (translator/)

Ọ bụrụ na usoro isi mmalite (dịka ọmụmaatụ, OpenAI) dị iche na usoro ebe a na-aga (dịka ọmụmaatụ, Claude), a na-**atụgharị** arịrịọ ahụ:

- System prompt → system message
- Nkọwa ngwa ọrụ → usoro ngwa ọrụ pụrụ iche nke onye na-eweta ọrụ
- Paramita reasoning/thinking → ihe kwekọrọ na ha nke onye na-eweta ọrụ
- Ịhazigharị role ozi (`developer` → `system` maka ndị na-abụghị OpenAI)

`translator/index.ts` na-ekpughe:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Ọkwa 3: Mee (executors/)

**Ebe ntinye**: `getExecutor(providerId).execute(request, options)`

Ndị na-eweta ọrụ niile na-eji `DefaultExecutor` (`executors/default.ts`) site na fallback nke factory `getExecutor()`. Executor ahụ:

- Na-ewu URL upstream (`buildUrl()`)
- Na-agbakwunye headers pụrụ iche nke onye na-eweta ọrụ (`buildHeaders()`)
- Na-agbanwe body arịrịọ (`transformRequest()`)
- Na-eziga arịrịọ HTTP yana nnwale ọzọ + exponential backoff
- Na-ahụ maka auth refresh ma ọ bụrụ na ọ dị mkpa (ndị na-eweta ọrụ OAuth)

Executor niile na-eketa `BaseExecutor` (`executors/base.ts`, ahịrị koodu 1170), nke na-enye:

- Usoro nnwale ọzọ nkịtị
- Njikọ proxy
- Njikọ circuit breaker
- Hooks maka idekọ ojiji

### Ọkwa 4: Kwasaa Data (utils/stream.ts)

Maka nzaghachi ndị a na-akwasa, executor na-eweghachi **ReadableStream**. Handler ahụ:

- Na-ebufe ya site na mgbanwe SSE (`createSSETransformStreamWithLogger`)
- Na-etinye heartbeat pings iji chọpụta njikọ ndị kwụsịrị
- Na-ejikwa nkwụsị njikọ client nke ọma (`pipeWithDisconnect`)
- Na-agbanwe SSE → JSON maka client ndị na-anaghị eji nkwasa data

Maka nzaghachi ndị anaghị eji nkwasa data, executor na-eweghachi object JSON a haziri, nke a na-ebufe n'enweghị mgbanwe.

### Ọkwa 5: Dekọọ (services/usage.ts)

Mgbe nzaghachi gasịrị (ma ọ gara nke ọma ma ọ bụ daa), a na-edekọ ojiji:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` sitere na nzaghachi ahụ
- `cost_usd` nke a gbakọrọ site na data ọnụahịa
- `latency_ms`, `status`, `error_class` ma ọ bụrụ na ọ daa
- A na-echekwa ya na tebụl `usage_history`

A na-ede ihe ndekọ oku (ọ bụrụ na agbanyere ya) na `${DATA_DIR}/call_logs/`.

---

## Nnyocha Miri Emi nke Faịlụ Ndị Dị Mkpa

### chatCore.ts (ahịrị 5977)

**Isi njikwa arịrịọ**. N'agbanyeghị nha ya, ọ nwere nhazi doro anya:

```ts
// Ọdịdị nhazi nke chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Nyocha njirimara + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Nkwado izi ezi nke body
  const body = await parseRequestBody(request);

  // 3. Nchọpụta usoro + ntụgharị
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Nhazi ụzọ combo
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Gaa n'ihu na target na-esote
    }
  }

  // 5. Usoro ndabere maka ọnọdụ mberede
  return await emergencyFallback(body);
}
```

N'agbanyeghị na ọ bụ otu nnukwu function, a haziri ya n'ime **ngalaba nwere nkọwa** ndị kwekọrọ na pipeline nke nwere usoro 5.

### combo.ts (4456 LOC)

**Injin nhazi ụzọ** nke na-akọwapụta otu combo gaa na targets ndị ahaziri n'usoro.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Ọ na-akwado **atụmatụ nhazi ụzọ 19** (lee `src/shared/constants/routingStrategies.ts`):

| Atụmatụ             | Omume                                                                    |
| ------------------- | ------------------------------------------------------------------------ |
| `priority`          | Ndepụta ahaziri nke target mbụ na-ebute ụzọ                              |
| `weighted`          | Nhọrọ dabere na puru omume dịka ịdị arọ nke target ọ bụla                |
| `round-robin`       | Gagharịa na targets n'usoro                                              |
| `context-relay`     | Nyefee context n'etiti targets                                           |
| `fill-first`        | Jupụta quota tupu ịgafe na nke na-esote                                  |
| `p2c`               | Ike nke nhọrọ abụọ                                                       |
| `random`            | Nhọrọ random nwere nkesa nha anya                                        |
| `least-used`        | Họrọ nke e ji mee ihe ugboro ole na ole kacha nta n'oge na-adịbeghị anya |
| `cost-optimized`    | Target dị ọnụ ala ma dị mma na-ebute ụzọ                                 |
| `reset-aware`       | Na-eburu oge reset nke provider n'uche                                   |
| `reset-window`      | Nhazi ụzọ dabere na oge reset                                            |
| `headroom`          | Nke nwere ohere quota fọdụrụ kacha ukwuu na-ebute ụzọ                    |
| `strict-random`     | Nkesa nha anya n'ezie (enweghị weighting dabere na quality)              |
| `auto`              | Jiri scoring nke ihe 16 (`autoCombo/`)                                   |
| `lkgp`              | Provider ikpeazụ a maara na ọ dị mma na-ebute ụzọ                        |
| `context-optimized` | Nke kacha mma maka arịrịọ nwere context dị ogologo                       |
| `fusion`            | Ziga n'otu oge na panel, wee jiri judge chịkọta azịza (`fusion.ts`)      |

### base.ts (1170 LOC)

**Executor abstract** nke executors 107 niile na-extend. Ọ nwere:

- `buildUrl()` — nrụpụta URL ndabara (subclasses na-override ya maka nhazi pụrụ iche)
- `buildHeaders()` — headers ndabara (auth, content-type)
- `transformRequest()` — pass-through na ndabara
- `execute()` — isi loop HTTP nwere retry/backoff/breaker

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Na-ejikwa providers niile dakọtara na OpenAI/Anthropic
  // Providers na-edebanye configurations (URL, auth, headers) mana ha na-ekekọrịta logic executor
}
```

A na-ahazi omume pụrụ iche nke provider (auth headers, base URL, version headers) site na registry provider, ọ bụghị site na classes executor dị iche iche.

````

---

## Ọrụ (modul 117)

Ọrụ bụ **modul ndị lekwasịrị anya n'otu ebumnuche** nke ndị njikwa na-ejikọta. Otu ndị kachasị ukwuu bụ:

### Nduzi & Ngwakọta

- `combo.ts` — ebe mbata maka arịrịọ ndị e ji ngwakọta duzie
- `services/autoCombo/` — inye akara dabere n'ihe 16, atụmatụ nduzi akpaaka 8
- `wildcardRouter.ts` — na-achọta ụzọ wildcard ndị dabara (`gpt-*`)
- `modelFamilyFallback.ts` — fallback T5 n'ime otu ezinụlọ

### Mmachi Ọsọ & Oke Ojiji

- `rateLimitManager.ts` — token bucket maka key+provider ọ bụla
- `usage.ts` — ndekọ ojiji
- `quotaCache.ts` — snapshot oke ojiji dị na ebe nchekwa

### Akaụntụ & Token

- `tokenRefresh.ts` — mmegharị OAuth mgbe 401 mere
- `accountFallback.ts` — ịgbanwe gaa na akaụntụ ọzọ
- `sessionManager.ts` — steeti nnọkọ nwere ọtụtụ ntụgharị

### Ọgụgụ Isi

- `intentClassifier.ts` — ikewa ebumnuche arịrịọ
- `taskAwareRouter.ts` — iduzi dabere n'ụdị ọrụ
- `thinkingBudget.ts` — ikesa token echiche
- `contextManager.ts` — itinye ọnọdụ nduzi

### Nkwụsi Ike

- `resilience.ts` — nhazi ịnwale ọzọ, backoff, na breaker
- `emergencyFallback.ts` — fallback nke nhọrọ ikpeazụ
- `modelDeprecation.ts` — iduzi na-akpaghị aka gaa na model nọchiri anya ya

### Steeti

- `signatureCache.ts` — iwepụ oyiri site na mbinye aka arịrịọ
- `volumeDetector.ts` — ibelata ibu
- `contextHandoff.ts` — serialization nke nnọkọ

### Mkpakọ

- `compression/` (subdirectory) — pipeline mkpakọ zuru ezu
- Faịlụ 39 na-ekpuchi engine, ngwugwu iwu, na adapter

### Nkà

- (akọwara na [SKILLS.md](./SKILLS.md))

### Ebe Ncheta

- (akọwara na [MEMORY.md](./MEMORY.md))

---

## Ndị Mmezu (faịlụ 75+)

Otu faịlụ maka provider ọ bụla. Ha niile na-agbatị `BaseExecutor` ma na-edegharị ihe ndị dị iche.

### Ụkpụrụ Ndị A Na-ahụkarị

A na-achọpụta provider site na `getExecutor(providerId)`, nke na-eweghachi executor ahaziri. Ndị provider dakọtara na OpenAI/Anthropic na-eji `DefaultExecutor` (`executors/default.ts`). A na-ahazi omume pụrụ iche nke provider (URL ntọala, header njirimara, ụdị API) na `open-sse/config/providers/`, ebe a na-ahazi mgbanwe request body na `open-sse/translator/`.

A na-esite na nhazi provider tọọ **URL ahaziri iche**:

```ts
// Nhazi provider na open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

A na-ahazi **njirimara ahaziri iche** site na nhazi njirimara nke ndekọ provider (API key, OAuth, profaịlụ header).

A na-edebanye mgbanwe **request body ahaziri iche** (dịka ọmụmaatụ, Anthropic ikewapụ `system` na `messages`) maka provider ọ bụla na `open-sse/translator/`.

````

### Factory nke Executor

`executors/index.ts` na-ebupụ `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Mkpebi na-agafe na `ExecutorRegistry` (`executors/registry.ts`): a na-ekwupụta executor pụrụ iche ọ bụla na tebụl arụnyere n'ime `executors/index.ts`, ma debanye ya site na `registerExecutor(alias, instance)` mgbe modul na-eburu; `getExecutor()` na-enyocha ndekọ ahụ ma jiri `DefaultExecutor` echekwara maka provider ọ bụla na-enweghị ntinye pụrụ iche dịka fallback. Golden test `tests/unit/executor-map-golden.test.ts` na-akọwapụta mapping alias → executor zuru ezu.

---

## Ndị ntụgharị

Tụgharịa n'etiti **usoro 3**: OpenAI, Anthropic, Gemini, tinyere Responses API ọhụrụ.

### Mgbe Ntughari Na-eme

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Ntughari ndị a na-ahụkarị:

- `OpenAI → Anthropic`: kewapụ ubi `system`, jiri nkụnyeisi `x-api-key`
- `OpenAI → Gemini`: jiri `contents` kama `messages`, `systemInstruction`
- `OpenAI → Responses API`: n'usoro n'usoro `input`, steeti `previous_response_id`

### Ọnọdụ Pụrụ Iche A Na-ahụ Maka Ha

- Ọrụ `developer` → `system` maka ndị na-abụghị OpenAI
- Ọrụ `system` → jikọta ya n'ime ozi onye ọrụ mbụ maka GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` nke Gemini
- `tools` → usoro ngwa ọrụ akọwapụtara maka onye na-enye ọrụ ọ bụla
- Paramita echiche (o1, Claude) → ihe ha na ya hà nke onye na-enye ọrụ ọ bụla

---

## Sava MCP

`open-sse/mcp-server/` na-emejuputa sava **Model Context Protocol**:

- **Ngwa ọrụ 110** (njikwa ndị na-enye ọrụ, ngwakọta, ebe nchekwa, cache, mkpakọ, proxy, nkà, ime ka ọ dị ka egwuregwu, plugins, Notion, Obsidian, corpus mpaghara)
- **Ụzọ mbufe 3**: stdio, SSE, Streamable HTTP
- **Scopes 33** maka ikike nwere njikwa zuru ezu

### Ndebanye Aha Ngwa Ọrụ

A na-edebanye ngwa ọrụ dị ka faịlụ kwụụrụ onwe ha n'ime `open-sse/mcp-server/tools/`, nke ọ bụla na-ebupụ aha, schema, handler, na scope:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Get system health snapshot",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Ụzọ Mbufe

```ts
// stdio (ojiji CLI)
startMcpStdio(server);

// SSE (nkwanye data dabere na HTTP)
startMcpSse(server, port);

// Streamable HTTP (MCP ọgbara ọhụrụ)
startMcpStreamable(server, port);
```

### Inye Ikike

Oku ngwa ọrụ ọ bụla na-agafe nyocha scope (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Insufficient scope");
}
```

---

## Ndị Ntụgharị Ọdịdị

`open-sse/transformer/` na-atụgharị n'etiti usoro **Chat Completions** na **Responses API**.

### Gịnị Mere E Ji Nwee Transformer Dị Iche?

Responses API bụ usoro ọhụrụ nke OpenAI nwere **mkparịta ụka na-edobe steeti** (`previous_response_id`). Mgbe onye ahịa zipụrụ arịrịọ Responses, OmniRoute:

1. Na-atụgharị Responses → Chat Completions n'ime sistemụ
2. Na-ezigara ya onye na-enye ọrụ (onye na-enye ọrụ ọ bụla na-akwado Chat Completions)
3. Na-atụgharị nzaghachi ahụ laghachi n'usoro Responses
4. Na-ezigara onye ahịa nzaghachi a tụgharịrị dị ka iyi data

Transformer (`transformer/responsesTransformer.ts`) na-enye:

```ts
createResponsesApiTransformStream(): TransformStream
```

Nke a na-ahụ maka:

- Ihe omume `response.output_item.added`
- Ihe omume `response.output_text.delta`
- Ihe omume `response.completed`
- Nhazi oku ngwa ọrụ (`function_call` ↔ `tool_calls`)

---

## Nhazi

`open-sse/config/` nwere oyi akwa nhazi:

| Faịlụ                            | Ebumnuche                                              |
| -------------------------------- | ------------------------------------------------------ |
| `providerRegistry.ts`            | Ndebanye ụdịdị Chat n'elu katalọgụ ndị na-enye ọrụ 352 |
| `providerModels.ts`              | Aha ọzọ nke ụdịdị, nhazi usoro                         |
| `constants.ts`                   | Oge nkwụsị, oke, koodu ọnọdụ                           |
| `defaultThinkingSignature.ts`    | Mbinye aka echiche Claude nke ndabara                  |
| `modelStrip.ts` (n'ime services) | Iwepụ ubi dịka onye na-enye ọrụ si dị                  |

### Schema Ndebanye Ndị Na-enye Ọrụ

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Nkwado Zod n'oge modulu na-eburu na-eme ka o doo anya na nhazi ndị na-enye ọrụ niile ziri ezi.

---

## Mmachi Ịrụ Ọrụ

Injin nhazi ụzọ nwere oke arụmọrụ siri ike:

| Ọrụ                                     | Ebumnuche | Ntụle                         |
| --------------------------------------- | --------- | ----------------------------- |
| Mkpebi ngwakọta                         | <10ms     | Maka ebe ezubere 50           |
| Nnyocha oke ọsọ                         | <1ms      | Bọket token dị na ebe nchekwa |
| Nlaghachi azụ nke ezinụlọ model         | <5ms      | Nkọwa ezinụlọ echekwara       |
| Mgbasa nhazi ụzọ arịrịọ                 | <2ms      | Ụzọ na-arụsi ọrụ ike          |
| **Enweghị I/O na-egbochi na ụzọ nhazi** | —         | Ha niile bụ async             |

---

## Ụkpụrụ Ndị A Ga-ezere

❌ **Oku DB synchronous na `combo.ts`** — gbakọọ tupu oge eruo ma chekwaa na cache
❌ **Usoro ịnwa ọzọ n'ime handlers** — jiri `retry()` sitere na ọrụ resilience
❌ **Ịnweta nhazi provider ozugbo** — jiri getters nke `providerRegistry`
❌ **Usoro fallback edere ozugbo** — kọwaa ha na `modelFamilyFallback.ts`
❌ **Mgbanwe state n'etiti arịrịọ ndị na-aga n'otu oge** — jiri naanị context nke metụtara arịrịọ ahụ

---

## Ịgbakwunye Komponentị Ọhụrụ

### Ịgbakwunye Ọrụ Ọhụrụ

1. Mepụta `open-sse/services/[serviceName].ts` nwere ọrụ a kapịrị ọnụ
2. Bupụ function handler bụ isi na constants ọ bụla
3. Tinye unit tests na `tests/unit/services/[serviceName].test.mjs`
4. Tinye ya n'ime pipeline arịrịọ na `handlers/chatCore.ts` (ọ bụrụ na ọ metụtara nhazi ụzọ)
5. Melite usoro nhazi ụzọ na `combo.ts` ma ọ bụrụ na ọrụ ahụ na-emetụta nhọrọ ebe ezubere
6. Detuo ya n'ime faịlụ a

### Ịgbakwunye Executor Ọhụrụ

1. Mepụta `open-sse/executors/[provider].ts` nke na-agbatị `BaseExecutor`
2. Debanye aha ya na `config/providerRegistry.ts`
3. Tinye ya na factory nke `executors/index.ts`
4. Tinye unit tests maka executor ahụ
5. Detuo ya na `docs/architecture/ARCHITECTURE.md`

### Ịgbakwunye Ngwaọrụ MCP Ọhụrụ

1. Mepụta ma ọ bụ melite `open-sse/mcp-server/tools/[category]Tools.ts`
2. Kọwaa schema Zod maka inputs
3. Debanye aha ngwaọrụ ahụ na `mcp-server/index.ts`
4. Tinye ya na matriks scope dị na `mcp-server/auth/`
5. Tinye unit tests

---

## Hụkwa

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — nhazi ụlọ n'ogo dị elu
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — ntụaka injinịa
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — nkọwa ndekọ n'otu n'otu
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — akara ihe 16
- [MCP-SERVER.md](./MCP-SERVER.md) — sava MCP
- [A2A-SERVER.md](./A2A-SERVER.md) — sava A2A
- Isi mmalite: `open-sse/` (faịlụ 400+, ~143K LOC)
