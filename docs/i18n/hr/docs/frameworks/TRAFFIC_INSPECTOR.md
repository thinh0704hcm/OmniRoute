# Traffic Inspector (Hrvatski)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇹 [am](../../../am/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇿 [az](../../../az/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇰 [da](../../../da/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇩🇪 [de](../../../de/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇷 [el](../../../el/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇸 [es](../../../es/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇪🇪 [et](../../../et/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇱 [he](../../../he/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇩 [id](../../../id/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇹 [it](../../../it/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇭 [km](../../../km/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇲🇲 [my](../../../my/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇴 [no](../../../no/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [or](../../../or/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇱🇰 [si](../../../si/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇮🇳 [te](../../../te/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇭 [th](../../../th/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/TRAFFIC_INSPECTOR.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/TRAFFIC_INSPECTOR.md)

---

Traffic Inspector je ugrađeni OmniRouteov alat za otklanjanje pogrešaka u HTTPS prometu — alat nalik na Charles Proxy / mitmweb / HTTP Toolkit koji **prepoznaje LLM-ove** i **agente**. Nalazi se na `/dashboard/tools/traffic-inspector` i prima promet uživo iz najviše 5 istodobnih izvora snimanja.

**Lokacija na nadzornoj ploči:** `/dashboard/tools/traffic-inspector`
**Grupa na bočnoj traci:** Alati (nakon AgentBridgea)
**Vidi također:** [`AGENTBRIDGE.md`](./AGENTBRIDGE.md) — AgentBridge je 1. način snimanja.

---

## §1 Pregled

### Po čemu je Traffic Inspector jedinstven

| Značajka                                                                                       |  mitmweb   | Charles |  Fiddler   | **OmniRoute Traffic Inspector** |
| ---------------------------------------------------------------------------------------------- | :--------: | :-----: | :--------: | :-----------------------------: |
| Temeljen na webu                                                                               |     ✓      |    ✗    |     ✗      |                ✓                |
| Otvorenog koda                                                                                 |     ✓      |    ✗    | djelomično |                ✓                |
| **Prepoznaje agente** (zna dolazi li zahtjev iz Antigravityja/Copilota/itd.)                   |     ✗      |    ✗    |     ✗      |                ✓                |
| **Prepoznaje LLM-ove** (raščlanjuje strukturu OpenAI-ja/Anthropica/Geminija, tokene, model)    |     ✗      |    ✗    |     ✗      |                ✓                |
| **Vidljivo mapiranje modela** (gemini-3-flash → claude-sonnet-4.7)                             |     ✗      |    ✗    |     ✗      |                ✓                |
| **Odvojena latencija proxyja/uzvodnog poslužitelja**                                           | djelomično |    ✗    |     ✗      |                ✓                |
| **Integriran s OmniRouteovim** usmjeravanjem, rezervnim odabirom i troškovima                  |     ✗      |    ✗    |     ✗      |                ✓                |
| **Otklanjanje pogrešaka proxyja na razini cijelog sustava** (bilo koja aplikacija na računalu) |     ✓      |    ✓    |     ✓      |                ✓                |
| **Snimanje prilagođenog hosta** (DNS preusmjeravanje po hostu)                                 |     ✓      |    ✓    |     ✓      |                ✓                |
| **Način rada s okruženjem HTTP_PROXY**                                                         |     ✓      |    ✓    |     ✓      |                ✓                |
| **Prikaz razgovora** (oblačići s više poruka, tool_use/tool_result)                            |     ✗      |    ✗    |     ✗      |                ✓                |
| **Spajanje SSE tokova** (rekonstrukcija iz događaja razlika)                                   |     ✗      |    ✗    |     ✗      |                ✓                |
| **Snimanje sesije** (imenovane sesije koje se mogu izvesti kao .har/.jsonl)                    |     ✗      |    ✓    |     ✓      |                ✓                |

### Arhitektura u jednom odlomku

`TrafficBuffer` (`src/mitm/inspector/buffer.ts`) zajednički je kružni međuspremnik u memoriji (zadano 1000 unosa, podesivo putem `INSPECTOR_BUFFER_SIZE`). Svi izvori snimanja zapisuju u njega putem `push()`. Klasa međuspremnika klasificira svaki unos pomoću `kindDetector.ts` (utvrđuje je li riječ o LLM zahtjevu), izračunava `contextKey` (SHA-256 otisak sistemskog upita) i šalje podatke svim WebSocket pretplatnicima putem `globalTrafficBuffer.subscribe()`. Nadzorna ploča povezuje se putem `GET /api/tools/traffic-inspector/ws` te nakon povezivanja prima snimku trenutačnog stanja, a zatim događaje `new`/`update`/`clear`.

---

## §2 Načini snimanja

Traffic Inspector podržava **5 istodobnih izvora snimanja**. Svaki se može neovisno uključiti ili isključiti. Polje `source` u svakom objektu `InterceptedRequest` (`src/mitm/inspector/types.ts`) ima jednu od vrijednosti `"agent-bridge"`, `"custom-host"`, `"http-proxy"`, `"system-proxy"` ili `"tproxy"`.

### Način 1 — AgentBridge (zadano, uvijek uključen)

**Izvor:** AgentBridge rukovatelji (`src/mitm/handlers/base.ts`)
**Mehanizam:** Svaki poziv `intercept()` u `MitmHandlerBase` poziva `hookBufferStart()` prije prosljeđivanja i `hookBufferUpdate()` po završetku. Nije potrebna nikakva dodatna konfiguracija — radi čim se AgentBridge pokrene.
**Doseg:** 9 IDE agenata konfiguriranih u AgentBridgeu
**Napomena:** Polje `source` u objektu `InterceptedRequest` = `"agent-bridge"`

### Način 2 — Prilagođeni hostovi (DNS preusmjeravanje)

**Izvor:** Korisnički definiran popis hostova (tablica `inspector_custom_hosts`)
**Mehanizam:** Dodavanjem hosta putem korisničkog sučelja dodaje se `127.0.0.1 <host>` u `/etc/hosts` (zahtijeva sudo). Postojeći AgentBridge MITM poslužitelj (port 443) dinamički generira SNI certifikat za novi host.
**Doseg:** Bilo koja aplikacija koja koristi dodani host — nije potrebna promjena konfiguracije aplikacije
**Napomena:** `source` = `"custom-host"`

Primjeri slučajeva upotrebe:

- Nadziranje `api.openai.com` iz Python skripti
- Otklanjanje pogrešaka za `my-internal-llm.company.com`
- Snimanje prometa s mobilnih uređaja na istoj mreži (putem ARP lažiranja — napredno)

### Način 3 — HTTP_PROXY slušatelj (port 8080)

**Izvor:** Aplikacije koje koriste varijable okruženja `HTTP_PROXY`/`HTTPS_PROXY`
**Mehanizam:** Sekundarni slušatelj na portu 8080 (`src/mitm/inspector/httpProxyServer.ts`) koji djeluje kao standardni eksplicitni HTTP/HTTPS proxy. Prihvaća `CONNECT` tunele (HTTPS) i izravne HTTP zahtjeve.
**Doseg:** Bilo koja aplikacija koja poštuje varijablu okruženja `HTTP_PROXY` — bez promjene DNS-a i bez naredbe sudo
**Napomena:** `source` = `"http-proxy"`

```bash
# Brzo snimanje za jednu naredbu:
HTTPS_PROXY=http://127.0.0.1:8080 curl https://api.openai.com/v1/models

# Trajno snimanje unutar sesije ljuske:
export HTTP_PROXY=http://127.0.0.1:8080
export HTTPS_PROXY=http://127.0.0.1:8080
```

**Ograničenje TLS-a:** HTTPS `CONNECT` tuneli snimaju se samo kao metapodaci (host, port, vremenski podaci) — TLS tijelo prema zadanim se postavkama ne dešifrira. Za potpuni pregled tijela uključite opciju „Dešifriraj HTTPS u proxy načinu rada” (izborno, zahtijeva da certifikat AgentBridgea bude pouzdan).

**Sukob porta:** Ako se port 8080 već upotrebljava, AgentBridge vraća status 409 sa strukturiranom pogreškom. Promijenite port putem varijable okruženja `INSPECTOR_HTTP_PROXY_PORT`.

### Način 4 — Proxy na razini cijelog sustava (napredno, izborno)

**Izvor:** Postavke proxyja na razini operacijskog sustava (primjenjuju se na sve aplikacije na računalu)
**Mehanizam:** Upotrebljava API-je operacijskog sustava za preusmjeravanje cjelokupnog HTTP/HTTPS prometa kroz HTTP_PROXY slušatelj:

- **macOS:** `networksetup -setwebproxy / -setsecurewebproxy`
- **Linux:** `gsettings set org.gnome.system.proxy` + `/etc/environment`
- **Windows:** `netsh winhttp set proxy 127.0.0.1:8080`
  **Doseg:** Svaka aplikacija na računalu koja poštuje postavke proxyja sustava
  **Napomena:** `source` = `"system-proxy"`

**Sigurnosni mehanizmi:**

- Mjerač vremena za automatsko isključivanje (zadano 30 min, može se konfigurirati putem `INSPECTOR_SYSTEM_PROXY_GUARD_MINUTES`)
- Prethodno stanje proxyja sustava sprema se u bazu podataka i vraća pri poništavanju
- Nadzorna ploča prikazuje upit „Poništavanje proxyja sustava” ako korisnik napusti stranicu dok je proxy aktivan
- Korisničko sučelje prikazuje oznaku `⚠ Napredno` + eksplicitni potvrdni okvir

### Način 5 — Transparentno TPROXY dešifriranje (Linux, root, izborno)

**Izvor:** TPROXY jezgre + usmjeravanje prema pravilima (`src/mitm/tproxy/`)
**Mehanizam:** Označava nove lokalne odlazne TCP veze prema ciljnom portu (zadano `443`) u lancu `mangle OUTPUT`, pravilo `ip rule` preusmjerava označene pakete na lokalnu isporuku, a cilj `TPROXY` lanca `mangle PREROUTING` predaje ih transparentnom (**IP_TRANSPARENT**) slušatelju (zadani port `8443`). Slušatelj prekida TLS pomoću krajnjeg certifikata koji dinamički CA izdaje **na zahtjev za svaki SNI naziv hosta**, snima dešifriranu razmjenu i prosljeđuje zahtjev ponovno šifriran prema izvornom odredištu.
**Doseg:** **Proizvoljni** odredišni hostovi na ciljnom portu — bez lažiranja datoteke `/etc/hosts`, bez varijable okruženja `HTTP_PROXY` i bez izmjene proxyja na razini cijelog sustava. Presretnutom procesu nije potrebna promjena konfiguracije, ali mora vjerovati dinamičkom CA-u.
**Napomena:** `source` = `"tproxy"`

**Zahtjevi:** Samo Linux (**IP_TRANSPARENT** dostupan je samo na Linuxu), mogućnost **CAP_NET_ADMIN** (root) i izvorni N-API dodatak koji se mora izgraditi pomoću lanca C alata (`npm run build:native:tproxy`). Kada nije dostupan, prekidač na nadzornoj ploči onemogućen je uz opis alata „TPROXY dešifriranje zahtijeva Linux + root + izvorni dodatak”. Pravila vatrozida primjenjuju se i poništavaju transakcijski (pad sustava nikada ne ostavlja pravilo `mangle`) te se brišu pri ponovnom pokretanju. Mehanizam protiv petlje temeljen na SO_MARK sprječava ponovno presretanje vlastitog ponovno šifriranog prosljeđivanja proxyja.

Ovo je opsežan podsustav s vlastitim namjenskim vodičem za operatere — pogledajte `docs/security/MITM-TPROXY-DECRYPT.md` (git; nije preveden u `/docs`) za cjelovit recept vatrozida, dinamički CA po SNI-ju + instalacijski program za spremište pouzdanih certifikata, isključivo lokalnu rutu, pojedinosti o sprječavanju petlje i konfiguracijsku shemu. Prekidačem se upravlja putem `GET / POST / DELETE /api/tools/agent-bridge/tproxy` (napomena: ruta se nalazi pod prefiksom AgentBridgea, a ne pod prefiksom Traffic Inspectora).

### Usporedba načina snimanja

| Način rada             | Postavljanje                           |            Sudo?             | Doseg                                  | Napomene                                                                                                                            |
| ---------------------- | -------------------------------------- | :--------------------------: | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1. AgentBridge         | Automatsko                             |  Jednom (certifikat+hosts)   | 9 IDE agenata                          | Zadano uključeno                                                                                                                    |
| 2. Prilagođeni hostovi | Unos za svaki host                     |     Da (datoteka hosts)      | Svaka aplikacija koja koristi taj host | Spremljeno u bazi podataka                                                                                                          |
| 3. HTTP_PROXY          | `export HTTPS_PROXY=...`               |              Ne              | Aplikacije koje poštuju env            | Port 8080, bez TLS dešifriranja prema zadanim postavkama                                                                            |
| 4. Na razini sustava   | Prebacivanje + potvrda                 |              Da              | Sve aplikacije na računalu             | Automatsko isključivanje za 30 min                                                                                                  |
| 5. TPROXY dešifriranje | Prebacivanje (Linux + izvorni dodatak) | Da (root + instalacija CA-a) | Bilo koji host na ciljnom portu        | Dešifrira proizvoljne hostove; zadano isključeno — pogledajte `docs/security/MITM-TPROXY-DECRYPT.md` (git; nije ugrađeno u `/docs`) |

---

## §3 Korisničko sučelje

### 3.1 Raspored

```
┌─ Inspektor prometa ────────────────────────────────────────────────────┐
│ ┌─ Alatna traka izvora snimanja ──────────────────────────────────┐   │
│ │ [✓ AgentBridge]  [✓ Prilagođeni hostovi (3)] [○ HTTP_PROXY] [○ Sustav]│
│ └─────────────────────────────────────────────────────────────────────┘  │
│ ┌─ Traka filtara/kontrola ────────────────────────────────────────┐   │
│ │ Profil: (●) Samo LLM  (○) Prilagođeno  (○) Sve                  │   │
│ │ [⎉ Pauziraj] [🗑 Očisti] [⬇ .har] [● SNIMI sesiju] ● uživo 482/1k│   │
│ └─────────────────────────────────────────────────────────────────────┘  │
├══◀▶══════════════════════════════╬══════════════════════════════════════╤╡
│ POPIS ZAHTJEVA (promjenjiv)      ║ OKNO S POJEDINOSTIMA                ▲ │
│ ────────────────────────────── │ ║ [Razgovor][Zaglavlja][Zahtjev]      │ │
│ ▎ 14:32 POST 200 12k AG openai ║ [Odgovor][Vrijeme][LLM][Statistika] │ │
│ ▎ 14:31 POST 200 8k  CP openai ║                                     ▼ │
│ ▎ 14:31 POST 503 ⚠   KR ...   ║                                       │
│ ▎ 14:30 GET  200 3k  🌐 prilag. ║                                       │
└══════════════════════════════════╝══════════════════════════════════════╝
```

### 3.2 Popis zahtjeva (lijevo okno)

- **Virtualiziran** (`useVirtualList` + `ResizeObserver`): obrađuje 1000 stavki bez zamrzavanja
- **Automatsko pomicanje** s prekidačem za pauziranje tijekom pregleda
- **Status označen bojama**: zeleno (2xx), žuto (3xx), crveno (4xx/5xx), sivo (u tijeku)
- **Emoji agenta**: 🔵 Antigravity, 🟢 Copilot, 🟠 Kiro, 🟣 Codex, 🔷 Cursor, 🟤 Zed, 🟡 Claude Code, ⚫ Open Code, 🌐 prilagođeni host
- **Traka boje konteksta**: lijevi obrub od 1 px obojen prema `contextKey` (SHA-256 sistemskog upita) — vizualno grupira povezane razgovore
- **Lijeno učitavanje tijela**: samo se tijelo odabranog zahtjeva materijalizira u karticama s pojedinostima (izbjegava iscrtavanje 1000 × 1 MB tijela)

### 3.3 Okno s pojedinostima — 7 kartica

| Kartica               | Sadržaj                                                                                         | Napomene                                                                                            |
| --------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| **Razgovor**          | Oblačići razgovora s više izmjena (system/user/assistant + tool_use/tool_result)                | Normalizirano iz formata bilo kojeg pružatelja; prikazuje se samo za `detectedKind === "llm"`       |
| **Zaglavlja**         | Tablice zaglavlja zahtjeva i odgovora                                                           | Osjetljiva zaglavlja (Authorization, Cookie, api-key) zadano su maskirana; prekidač „Prikaži tajne” |
| **Zahtjev**           | Neobrađeno tijelo, prikaz JSON stabla, oznaka polja modela                                      | Lijepo oblikovan JSON ili neobrađeni tekst                                                          |
| **Odgovor**           | Neobrađeno tijelo ili popis SSE događaja; prekidač „Neobrađeno ↔ Spojeno”                       | SSE spajač rekonstruira završnu poruku iz delta događaja                                            |
| **Vrijeme**           | Slapni prikaz: dodatno vrijeme proxyja u odnosu na latenciju uzvodnog poslužitelja              | Ukupno, TTFB i veličina                                                                             |
| **Pojedinosti LLM-a** | Pružatelj, model, broj poruka, ulazni/izlazni tokeni, procjena troška, mapirano odredište       | Prikazuje se samo za LLM zahtjeve                                                                   |
| **Statistika**        | Recharts: vremenski tijek latencije, trakasti grafikon tokena, dijagram raspršenja poziva alata | Prikazuje se samo kada je učitana snimljena sesija                                                  |

### 3.4 Kontrole alatne trake

| Kontrola         | Radnja                                                                              |
| ---------------- | ----------------------------------------------------------------------------------- |
| ⎉ Pauziraj       | Zaustavlja iscrtavanje novih zahtjeva; oznaka „X novih” nastavlja se povećavati     |
| 🗑 Očisti         | Čisti popis u korisničkom sučelju (ne utječe na međuspremnik poslužitelja)          |
| ⬇ Izvezi .har    | Preuzima trenutačni filtrirani popis kao HAR datoteku                               |
| ● Snimi sesiju   | Pokreće imenovanu sesiju snimanja                                                   |
| Odabir profila   | Samo LLM / Prilagođeni hostovi / Sve                                                |
| Filtar hostova   | Podudaranje podniza u polju `host`                                                  |
| Filtar agenata   | Padajući izbornik: Svi / prema agentu                                               |
| Filtar statusa   | Sve / 2xx / 3xx / 4xx / 5xx / pogreška                                              |
| Filtar izvora    | Sve / agent-bridge / custom-host / http-proxy / system-proxy / tproxy               |
| Filtar **Uživo** | Prikazuje samo zahtjeve u tijeku (otvorene) — prekidač `liveOnly` (pogledajte §4.6) |

### 3.5 Okna promjenjive veličine

- Popis i okno s pojedinostima odvojeni su ručicom za povlačenje
- Širina popisa: najmanje 280 px, najviše 720 px, pohranjena u `localStorage` (`inspector.listWidth`)
- Može se sažeti na traku od 48 px (samo ikone); kliknite redak u traci kako biste je proširili

---

## §4 Značajke prilagođene LLM-ovima

### 4.1 Detektor vrste (`src/mitm/inspector/kindDetector.ts`)

Klasificira svaki zahtjev kao `"llm"`, `"app"` ili `"unknown"` koristeći 4 signala:

1. **Registar poslužitelja** — ~18 poznatih naziva poslužitelja LLM API-ja (OpenAI, Anthropic, Gemini, Groq, Mistral, Together, Fireworks, Cohere, Perplexity, Hugging Face, OpenRouter, xAI, Moonshot itd.)
2. **Uzorci putanja** — `/v1/chat/completions`, `/v1/messages`, `/generateContent`, `/v1/responses` itd.
3. **Struktura tijela** — otkriva polja `messages[]` (OpenAI/Claude), `contents[]` (Gemini), `prompt`, `input`
4. **Naznake korisničkog agenta** — `codex`, `claude`, `gemini`, `antigravity`, `kiro`, `copilot`, `cursor` u UA nizu

Prilagođeni poslužitelji dodani putem načina rada 2 nasljeđuju svoj `kind` iz unosa u obrascu (zadana je vrijednost `"custom"`).

### 4.2 Spajanje SSE događaja (`src/mitm/inspector/sseMerger.ts`)

**Neovisna implementacija razvijena od nule.** Raščlanjivanje događaja slijedi
[WHATWG algoritam za događaje koje šalje poslužitelj](https://html.spec.whatwg.org/multipage/server-sent-events.html#parsing-an-event-stream),
dok rekonstrukcija slijedi javne sheme strujanja za [OpenAI](https://platform.openai.com/docs/api-reference/chat/create),
[Anthropic](https://platform.claude.com/docs/en/build-with-claude/streaming) i
[Gemini](https://ai.google.dev/api/generate-content#method:-models.streamgeneratecontent).

Rekonstruira konačnu poruku asistenta iz neobrađenih SSE delta događaja:

- **Anthropic**: akumulira `content_block_delta` prema indeksu; obrađuje `text_delta`, `input_json_delta` (pozive alata), `thinking_delta`
- **OpenAI**: akumulira odabire/pozive alata sučelja Chat Completions i izlazne stavke sučelja Responses API
  prema indeksu
- **Gemini**: akumulira `candidates[i].content.parts`
- **Nepoznato**: vraća neobrađene događaje bez izmjena

Kartica Odgovor prikazuje preklopnik: **„Neobrađeni događaji ↔ Spojeno”**.

### 4.3 Normalizator razgovora (`src/mitm/inspector/conversationNormalizer.ts`)

**Neovisna implementacija razvijena od nule.** Normalizacija je definirana lokalnim
ugovorima crne kutije i javnim shemama poruka za OpenAI, Anthropic i Gemini; ne
upotrebljava se izvorni kôd uzvodne implementacije.

Pretvara formate poruka OpenAI-ja, Anthropica i Geminija u jedinstveni `NormalizedConversation` prije iscrtavanja:

```ts
interface NormalizedConversation {
  request: NormalizedTurn[]; // poruke / sadržaji / upit iz tijela zahtjeva
  response: NormalizedTurn[]; // odgovor asistenta (spojen putem sseMerger)
  contextKey: string | null; // SHA-256 otisak sistemskog upita
}
```

Vrste blokova: `text`, `tool_use`, `tool_result`. Kartica Razgovor upotrebljava ovu strukturu neovisno o pružatelju usluge.

### 4.4 Bojanje ključa konteksta (`src/mitm/inspector/contextKey.ts`)

- Izračunava `SHA-256` sistemskog upita (prva poruka `role:system`, polje `system` ili Geminijev `systemInstruction`)
- Vraća heksadecimalni prefiks od 12 znakova (`"a3f9c2..."`)
- Sučelje preslikava ključ u determinističku HSL boju trake lijevog obruba
- **Filtar „isti kontekst”**: klik na oznaku `ctx #a3f` dodaje filtar koji prikazuje samo zahtjeve s istim otiskom

To olakšava vizualno razlikovanje različitih „persona” ili zadataka koji se izvode u istoj sesiji agenta.

### 4.5 Izdvajanje metapodataka LLM-a

Za LLM zahtjeve kartica Pojedinosti LLM-a izdvaja:

```ts
interface LlmMetadata {
  provider: string | null; // "openai" | "anthropic" | "gemini" | ...
  apiKind: string | null; // "chat.completions" | "messages" | "embeddings" | ...
  model: string | null; // iz tijela zahtjeva ili odgovora
  messages: number; // broj izmjena
  tokensIn: number | null; // usage.prompt_tokens / usage.input_tokens
  tokensOut: number | null; // usage.completion_tokens / usage.output_tokens
  streamed: boolean; // true ako je odgovor SSE
  mappedTo: string | null; // zaglavlje x-omniroute-mapped
  costEstimateUsd: number | null; // procijenjeni trošak na temelju cijena OmniRoutea
}
```

### 4.6 Filtar aktivnih zahtjeva u stvarnom vremenu

Polje `status` zahtjeva ima tip `number | "in-flight" | "error"` — unos se
dodaje kao `"in-flight"` čim zahtjev započne i **ažurira na mjestu**
kada stigne odgovor (ili pogreška). Preklopnik **„Uživo”** na alatnoj traci
(`liveOnly`, i18n ključ `trafficInspector.liveOnly`) ograničava popis na unose
čiji je `status === "in-flight"`, omogućujući praćenje otvorenih veza u stvarnom vremenu.

Filtar je čisti predikat na klijentskoj strani u
`src/lib/inspector/matchesTrafficFilter.ts`:

```ts
if (f.liveOnly && req.status !== "in-flight") return false;
```

Stanje preklopnika nalazi se u `useTrafficFilters` (hookovima nadzorne ploče inspektora) i
kombinira se s ostalim filtrima (profil, poslužitelj, agent, izvor, status, kontekst).

### 4.7 Pripisivanje procesa (Linux)

Na Linuxu se svaki presretnuti zahtjev može pripisati **izvornom lokalnom
procesu**. U `InterceptedRequest` dodaju se dva neobavezna polja:

```ts
pid?: number;          // identifikator izvornog procesa (samo Linux)
processName?: string;  // naziv izvornog procesa (samo Linux)
```

`src/mitm/inspector/processAttribution.ts` preslikava privremeni port _klijenta_
veze u PID + naziv na sljedeći način:

1. Čita `/proc/net/tcp` i `/proc/net/tcp6` kako bi pronašao inode utičnice za
   port (`parseProcNetTcpForInode`, čisti parser koji se može testirati pomoću testnih podataka).
2. Pretražuje `/proc/<pid>/fd/` radi simboličke poveznice na `socket:[<inode>]`.
3. Čita naziv procesa iz `/proc/<pid>/comm`.

Predmemorija s TTL-om od 1 sekunde ograničava trošak pregledavanja procfs-a pod opterećenjem. Pripisivanje se
izvodi **prema najboljoj mogućnosti** — svaki se neuspjeh razrješava u `null` i nikada ne blokira snimanje. Na
sustavima macOS/Windows funkcija vraća `null` (privremena implementacija; podrška za `lsof`/`GetExtendedTcpTable`
bit će dodana naknadno).

---

## §5 Sesije

### 5.1 Snimanje sesije

1. Kliknite **"● Snimi sesiju"** na alatnoj traci → unesite naziv (neobavezno)
2. Praćenje uživo nastavlja se uobičajeno; crveni pulsirajući indikator prikazuje `◉ SNIMANJE · <naziv> · 00:42 · 23 zahtjeva`
3. Kliknite **"⏹ Zaustavi"** → snimka sesije sprema se u `inspector_sessions` + `inspector_session_requests`

### 5.2 Pregled snimljene sesije

Padajući izbornik **Sesije** na alatnoj traci prikazuje spremljene sesije. Odabirom sesije:

- Učitava se snimka sesije (zamrznuto stanje)
- Natpis prikazuje: `Pregled snimljene sesije "<naziv>" — [Natrag na prikaz uživo]`
- Kartica Statistika postaje dostupna s agregatima biblioteke Recharts

### 5.3 Formati izvoza

Svaka sesija može se izvesti u sljedećim formatima:

| Format                     | Upotreba                                                                                |
| -------------------------- | --------------------------------------------------------------------------------------- |
| **HAR** (HTTP Archive 1.2) | Kompatibilno s alatima Chrome DevTools, Charles i Fiddler — uvoz za izvanmrežnu analizu |
| **JSONL**                  | Jedan `InterceptedRequest` po retku — kompatibilno s formatom `llm-interceptor`         |

Izvezite putem `GET /api/tools/traffic-inspector/sessions/{id}/export.har` ili gumba ⬇ u padajućem izborniku Sesije.

---

## §6 Sigurnost

Traffic Inspector prikazuje **sav presretnuti HTTPS promet**, uključujući autorizacijska zaglavlja i tijela zahtjeva. Primjenjuju se sljedeće kontrole:

| Kontrola                        | Pojedinosti                                                                                                                                                                                                                                 |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **LOCAL_ONLY**                  | Sve rute i WebSocket krajnja točka dostupne su samo putem povratne petlje (provodi se u `routeGuard.ts` prije autentifikacije)                                                                                                              |
| **Maskiranje tajni**            | Linearni skener `maskSecret()` redigira vjerodajnice Bearer prema RFC-u 6750, ključeve s prefiksom pružatelja i duge neprozirne tokene prije `TrafficBuffer.push()`                                                                         |
| **Ograničenje veličine tijela** | Tijela > `INSPECTOR_MAX_BODY_KB` (zadano 1024 KB) skraćuju se uz obavijest `"(skraćeno radi boljih performansi)"`                                                                                                                           |
| **Sanitizacija zaglavlja**      | Nazivi se pretvaraju u mala slova; zaglavlja za uokvirivanje, ona ograničena na jedan skok i zaglavlja autentifikacije proxyja odbacuju se; kolačići se potpuno redigiraju; vrijednosti vjerodajnica prosljeđuju se funkciji `maskSecret()` |
| **CSP**                         | Stroga pravila Content Security Policy na stranicama alata Traffic Inspector radi sprječavanja XSS-a putem umetnutih tijela odgovora                                                                                                        |
| **Bez zadane trajne pohrane**   | `TrafficBuffer` se nalazi u memoriji i gubi se pri ponovnom pokretanju poslužitelja. Sesije se trajno pohranjuju samo kada se izričito snimaju                                                                                              |

### Primijenjena stroga pravila

| Pravilo                           | Primjena                                                                                   |
| --------------------------------- | ------------------------------------------------------------------------------------------ |
| **#12** `sanitizeErrorMessage`    | Sanitiziraju se svi HTTP odgovori o pogrešci iz ruta alata Traffic Inspector               |
| **#15 + #17** `isLocalOnlyPath()` | `/api/tools/traffic-inspector/` je LOCAL_ONLY + SPAWN_CAPABLE (naredbe sistemskog proxyja) |

### Poznata ograničenja

- **Način sistemskog proxyja** utječe na sve aplikacije na računalu, uključujući VPN klijente i SSO. Uvijek ga upotrebljavajte s mjeračem vremena za automatsko onemogućavanje. Nemojte ga upotrebljavati na dijeljenim računalima.
- **HTTPS putem tunela CONNECT**: Način rada 3 (HTTP_PROXY) bilježi samo metapodatke tunela za HTTPS odredišta, osim ako je omogućeno presretanje TLS-a. To je namjerno — transparentno bilježenje bez pouzdanog certifikata AgentBridge prekinulo bi provjeru TLS-a za te aplikacije.
- **Tvrdo kodirani nizovi u nekim komponentama**: Neke komponente korisničkog sučelja (F7/F8) imaju mali broj tvrdo kodiranih nizova koji još nisu obuhvaćeni i18n ključevima. Dokumentirani su kao poznato ograničenje u izvješću o nedostacima i18n-a; migrirat će se u naknadnom prolazu. Obuhvaćeni nizovi ukrasne su oznake korisničkog sučelja koje nije potrebno prevesti za funkcionalnu upotrebu.

---

## §7 Rješavanje problema

### Prekid WebSocket veze

Ako praćenje uživo prikazuje "Veza je prekinuta":

1. Provjerite radi li poslužitelj još uvijek: `GET /api/tools/traffic-inspector/capture-modes`
2. Ponovno učitajte stranicu — WebSocket se ponovno povezuje i prima novu snimku
3. Ako je poslužitelj ponovno pokrenut, međuspremnik u memoriji je izbrisan — stari zapisi više nisu dostupni osim ako je sesija bila snimljena

### Sukob na priključku 8080

Ako se način rada HTTP_PROXY ne uspije pokrenuti:

```bash
lsof -i :8080    # pronađite proces
```

Promijenite priključak:

```bash
# .env
INSPECTOR_HTTP_PROXY_PORT=8888
```

### Sistemski proxy nije vraćen na prethodne postavke

Ako se OmniRoute sruši dok je aktivan način rada sistemskog proxyja:

**macOS:**

```bash
networksetup -setwebproxystate Wi-Fi off
networksetup -setsecurewebproxystate Wi-Fi off
```

**Linux (GNOME):**

```bash
gsettings set org.gnome.system.proxy mode 'none'
```

**Windows:**

```cmd
netsh winhttp reset proxy
```

Nadzorna ploča također će pri sljedećem učitavanju ponuditi opciju "Vrati postavke sistemskog proxyja" ako otkrije da stanje baze podataka pokazuje da je proxy bio aktivan.

### Pun međuspremnik

Kada međuspremnik dosegne `INSPECTOR_BUFFER_SIZE` (zadano 1000), novi zapisi potiskuju najstarije. Ako se važni zahtjevi gube:

- Povećajte `INSPECTOR_BUFFER_SIZE` (npr. 5000) — time se povećava potrošnja memorije radi duljeg zadržavanja
- Snimite sesiju kako biste relevantno razdoblje trajno pohranili u bazu podataka

---

## §8 Referenca API-ja

Sve su rute `LOCAL_ONLY` (samo povratna petlja) i `SPAWN_CAPABLE` (naredbe sistemskog proxyja). Pogledajte `src/server/authz/routeGuard.ts`.

Osnovna putanja: `/api/tools/traffic-inspector/`

### Upravljanje zahtjevima

| Metoda | Putanja                     | Opis                                                                                       |
| ------ | --------------------------- | ------------------------------------------------------------------------------------------ |
| GET    | `/requests`                 | Popis zahtjeva (moguće filtrirati: `?profile=llm&host=&agent=&status=&source=&sessionId=`) |
| GET    | `/requests/{id}`            | Pojedinosti pojedinačnog zahtjeva                                                          |
| DELETE | `/requests`                 | Brisanje međuspremnika u memoriji                                                          |
| POST   | `/requests/{id}/replay`     | Ponovno izvršavanje istog zahtjeva putem usmjerivača OmniRoute                             |
| PUT    | `/requests/{id}/annotation` | Spremanje ili ažuriranje bilješke uz zahtjev                                               |

### WebSocket

| Metoda | Putanja | Opis                                                                                                |
| ------ | ------- | --------------------------------------------------------------------------------------------------- |
| GET    | `/ws`   | WebSocket prijenos uživo. Pri povezivanju šalje `snapshot`, a zatim događaje `new`/`update`/`clear` |

### Izvoz

| Metoda | Putanja       | Opis                                          |
| ------ | ------------- | --------------------------------------------- |
| GET    | `/export.har` | Izvoz trenutačno filtriranog popisa u HAR 1.2 |

### Prilagođeni hostovi

| Metoda | Putanja         | Opis                                              |
| ------ | --------------- | ------------------------------------------------- |
| GET    | `/hosts`        | Popis prilagođenih hostova                        |
| POST   | `/hosts`        | Dodavanje hosta (automatski uređuje `/etc/hosts`) |
| DELETE | `/hosts/{host}` | Uklanjanje hosta                                  |
| PATCH  | `/hosts/{host}` | Uključivanje ili isključivanje `enabled`          |

### Načini snimanja

| Metoda | Putanja                        | Opis                                                                                                      |
| ------ | ------------------------------ | --------------------------------------------------------------------------------------------------------- |
| GET    | `/capture-modes`               | Stanje načina AgentBridge / prilagođeni hostovi / HTTP_PROXY / sistemski proxy + prekidač `tls-intercept` |
| POST   | `/capture-modes/http-proxy`    | Pokretanje/zaustavljanje HTTP_PROXY slušatelja (`{action: "start"\|"stop"}`)                              |
| POST   | `/capture-modes/system-proxy`  | Primjena/vraćanje sistemskog proxyja (`{action: "apply"\|"revert"}`)                                      |
| POST   | `/capture-modes/tls-intercept` | Uključivanje ili isključivanje dešifriranja HTTPS tijela u proxy načinu (`{enabled: boolean}`)            |

> **TPROXY decrypt** (način snimanja 5) pokreće se putem **zasebne** rute pod
> prefiksom AgentBridge — `GET / POST / DELETE /api/tools/agent-bridge/tproxy` — a ne
> pod `/api/tools/traffic-inspector/`. Pogledajte
> `docs/security/MITM-TPROXY-DECRYPT.md` (git; nije ugrađeno u `/docs`).

### Sesije

| Metoda | Putanja                     | Opis                                                                          |
| ------ | --------------------------- | ----------------------------------------------------------------------------- |
| POST   | `/sessions`                 | Početak snimanja (`{name?: string}`)                                          |
| PATCH  | `/sessions/{id}`            | Zaustavljanje ili preimenovanje (`{action: "stop"\|"rename", name?: string}`) |
| GET    | `/sessions`                 | Popis svih spremljenih sesija                                                 |
| GET    | `/sessions/{id}`            | Snimka sesije (svi zahtjevi)                                                  |
| DELETE | `/sessions/{id}`            | Brisanje sesije                                                               |
| GET    | `/sessions/{id}/export.har` | Izvoz sesije u HAR 1.2                                                        |

### Interni unos (D4 pričuvni mehanizam)

| Metoda | Putanja            | Opis                                                                                                                 |
| ------ | ------------------ | -------------------------------------------------------------------------------------------------------------------- |
| POST   | `/internal/ingest` | Prihvaća presretnuti zahtjev iz prolazne putanje `server.cjs`; zahtijeva zaglavlje `INSPECTOR_INTERNAL_INGEST_TOKEN` |

Potpune OpenAPI sheme: `docs/openapi.yaml` → oznaka `Traffic Inspector`.
