# open-sse Architecture (Hrvatski)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Ukratko**: `open-sse/` je temeljni mehanizam za strujanje koji pokreće svaki LLM zahtjev u OmniRouteu. Sadrži oko 1.400 datoteka koje implementiraju tijek obrade zahtjeva, izvršitelje, usluge, MCP poslužitelj i sloj za prevođenje formata. Ovaj vodič objašnjava kako se svi dijelovi međusobno povezuju.

**Izvor:** `open-sse/` (paket radnog prostora, oko 1.440 `.ts` datoteka koje nisu testne)

---

## Zašto zaseban paket radnog prostora?

`open-sse/` je **samostalan radni prostor** u OmniRoute monorepozitoriju iz nekoliko razloga:

1. **Ponovna upotrebljivost** — `open-sse` se objavljuje kao `@omniroute/open-sse` na npm-u, pa ga drugi projekti mogu koristiti neovisno
2. **Jasne granice** — mehanizam za strujanje odvojen je od sloja korisničkog sučelja i baze podataka specifičnog za OmniRoute
3. **Performanse** — mehanizam nema ovisnosti o Next.js-u, što omogućuje brže hladno pokretanje u CLI i serverless okruženjima
4. **Verzioniranje** — `open-sse` može objavljivati izdanja vlastitim tempom

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Struktura najviše razine

```
open-sse/
├── index.ts              # Javna ulazna točka
├── types.d.ts            # Javni izvozi tipova
├── package.json          # @omniroute/open-sse
├── config/               # Konfiguracije pružatelja, konstante, registri
├── executors/            # HTTP izvršitelji za pojedinačne pružatelje (67 + base.ts/index.ts)
├── handlers/             # Rukovatelji zahtjevima (chatCore, responses itd.)
├── lib/                  # Interni pomoćni alati
├── mcp-server/           # Poslužitelj protokola Model Context Protocol
├── services/             # Oko 298 modula usluga
├── transformer/          # Transformator formata Responses API-ja
├── translator/           # Prevođenje formata (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Zajednički pomoćni alati (zapisivanje, pogreške, strujanje itd.)
```

### Broj modula

| Direktorij | Datoteke | Namjena |
| `executors/` | 167 | HTTP izvršitelji za pojedinačne pružatelje (objedinjeni putem tvornice DefaultExecutor) |
| `handlers/` | 157 | Ulazne točke zahtjeva (chatCore, responses, embeddings) |
| `services/` | ~536 | Usmjeravanje, predmemoriranje, ograničavanje brzine, osvježavanje itd. |
| `translator/` | 56 | Pretvorba formata (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP alati i prijenosi |
| `utils/` | ~108 | Višenamjenski pomoćni alati (zapisivanje, pogreške, strujanje) |
| `config/` | ~339 | Konfiguracije pružatelja, konstante, registri |

---

## Tijek obrade zahtjeva

Svaki LLM zahtjev prolazi kroz **tijek obrade od 5 faza**:

```
                              ┌──────────────┐
   HTTP zahtjev                │  1. USMJERI  │   razrješavanje kombinacije, odabir modela
   (Next.js ruta)              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. PREVEDI  │   pretvorba formata (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. IZVRŠI   │   izvršitelj pružatelja, HTTP, ponovni pokušaj, prekidač
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. STRUJANJE│   SSE transformacija, povratni pritisak
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. ZABILJEŽI│   praćenje upotrebe, zapisnik poziva, klasifikacija pogrešaka
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP odgovor (SSE ili JSON)
```

### Faza 1: Usmjeravanje (services/combo.ts)

**Ulazna točka**: `handleComboChat()` u `services/combo.ts`

Razrješava zahtjev u konkretan skup `(provider, model, account, credentials)`:

- Pronalazi kombinaciju prema ID-u (ili izrađuje virtualnu kombinaciju za modele `auto/*`)
- Primjenjuje strategiju usmjeravanja (prioritetnu, ponderiranu, kružnu itd.)
- Filtrira pružatelje koji nisu ispravni (prekidač strujnog kruga)
- Odabire sljedeće dostupno odredište

Za modele `auto/*` ova faza također:

- Pokreće algoritam **bodovanja prema 16 čimbenika** (`services/autoCombo/`)
- Odabire par `provider+model` na temelju ispravnosti, cijene, latencije itd.

### Faza 2: Prevođenje (translator/)

Ako se izvorni format (npr. OpenAI) razlikuje od ciljnog formata (npr. Claude), zahtjev se **prevodi**:

- Sistemski prompt → sistemska poruka
- Definicije alata → format alata specifičan za pružatelja
- Parametri zaključivanja/razmišljanja → ekvivalenti specifični za pružatelja
- Normalizacija uloga poruka (`developer` → `system` za sustave koji nisu OpenAI)

`translator/index.ts` izlaže:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Faza 3: Izvršavanje (executors/)

**Ulazna točka**: `getExecutor(providerId).execute(request, options)`

Svi pružatelji koriste `DefaultExecutor` (`executors/default.ts`) putem pričuvnog mehanizma tvornice `getExecutor()`. Izvršitelj:

- Izrađuje URL nadređene usluge (`buildUrl()`)
- Dodaje zaglavlja specifična za pružatelja (`buildHeaders()`)
- Transformira tijelo zahtjeva (`transformRequest()`)
- Šalje HTTP zahtjev uz ponovne pokušaje i eksponencijalno povećavanje razmaka
- Po potrebi upravlja osvježavanjem autentikacije (OAuth pružatelji)

Svi izvršitelji proširuju `BaseExecutor` (`executors/base.ts`, 1170 redaka koda), koji pruža:

- Zajedničku logiku ponovnih pokušaja
- Integraciju s proxyjem
- Integraciju s prekidačem strujnog kruga
- Priključke za bilježenje upotrebe

### Faza 4: Strujanje (utils/stream.ts)

Za strujane odgovore izvršitelj vraća **ReadableStream**. Rukovatelj:

- Prosljeđuje podatke kroz SSE transformaciju (`createSSETransformStreamWithLogger`)
- Primjenjuje periodične signalne poruke radi otkrivanja prekinutih veza
- Elegantno obrađuje prekid veze klijenta (`pipeWithDisconnect`)
- Transformira SSE → JSON za klijente koji ne upotrebljavaju strujanje

Za odgovore bez strujanja izvršitelj vraća raščlanjeni JSON objekt koji se prosljeđuje bez promjena.

### Faza 5: Bilježenje (services/usage.ts)

Nakon odgovora (uspješnog ili neuspješnog) bilježi se upotreba:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` iz odgovora
- `cost_usd` izračunat iz podataka o cijenama
- `latency_ms`, `status`, `error_class` u slučaju neuspjeha
- Spremljeno u tablicu `usage_history`

Artefakti zapisnika poziva (ako su omogućeni) zapisuju se u `${DATA_DIR}/call_logs/`.

---

## Detaljan pregled ključnih datoteka

### chatCore.ts (5977 redaka)

**Glavni obrađivač zahtjeva**. Unatoč veličini, ima jasnu strukturu:

```ts
// Pseudostruktura datoteke chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Autentifikacija + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Provjera valjanosti tijela
  const body = await parseRequestBody(request);

  // 3. Otkrivanje formata + prevođenje
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Usmjeravanje kombinacije
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Nastavi na sljedeći cilj
    }
  }

  // 5. Rezervna opcija za hitne slučajeve
  return await emergencyFallback(body);
}
```

Unatoč tome što je riječ o jednoj golemoj funkciji, organizirana je u **komentirane odjeljke** koji odgovaraju procesu od 5 faza.

### combo.ts (4456 redaka koda)

**Mehanizam usmjeravanja** koji kombinaciju razrješava u poredane ciljeve.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

Podržava **19 strategija usmjeravanja** (pogledajte `src/shared/constants/routingStrategies.ts`):

| Strategija          | Ponašanje                                                                     |
| ------------------- | ----------------------------------------------------------------------------- |
| `priority`          | Poredani popis u kojem prvi cilj ima prednost                                 |
| `weighted`          | Vjerojatnosni odabir prema težinskom faktoru svakog cilja                     |
| `round-robin`       | Kružno prolazi kroz ciljeve zadanim redoslijedom                              |
| `context-relay`     | Prosljeđuje kontekst između ciljeva                                           |
| `fill-first`        | Ispunjava kvotu prije prelaska na sljedeći cilj                               |
| `p2c`               | Odabir između dvije mogućnosti                                                |
| `random`            | Ravnomjeran nasumični odabir                                                  |
| `least-used`        | Odabire cilj s najmanje nedavnih upotreba                                     |
| `cost-optimized`    | Najprije odabire najjeftiniji dostupan cilj                                   |
| `reset-aware`       | Uzima u obzir razdoblja poništavanja pružatelja                               |
| `reset-window`      | Usmjeravanje na temelju razdoblja poništavanja                                |
| `headroom`          | Najprije odabire cilj s najvećom preostalom kvotom                            |
| `strict-random`     | Uistinu ravnomjeran odabir (bez ponderiranja prema kvaliteti)                 |
| `auto`              | Koristi bodovanje sa 16 faktora (`autoCombo/`)                                |
| `lkgp`              | Najprije odabire posljednjeg poznatog ispravnog pružatelja                    |
| `context-optimized` | Najprikladniji za zahtjeve s dugim kontekstom                                 |
| `fusion`            | Paralelno šalje zahtjeve panelu, a zatim sintetizira putem suca (`fusion.ts`) |

### base.ts (1170 redaka koda)

**Apstraktni izvršitelj** koji proširuje svih 107 izvršitelja. Sadrži:

- `buildUrl()` — zadana izgradnja URL-a (podklase je nadjačavaju za prilagođeno ponašanje)
- `buildHeaders()` — zadana zaglavlja (autentifikacija, vrsta sadržaja)
- `transformRequest()` — zadano prosljeđivanje bez izmjena
- `execute()` — glavna HTTP petlja s ponovnim pokušajima, postupnim povećanjem čekanja i prekidačem

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Obrađuje sve pružatelje kompatibilne s OpenAI/Anthropic
  // Pružatelji registriraju konfiguracije (URL, autentifikaciju, zaglavlja), ali dijele logiku izvršitelja
}
```

Ponašanje specifično za pružatelja (autentifikacijska zaglavlja, osnovni URL, zaglavlja verzije) konfigurira se putem registra pružatelja, a ne zasebnim klasama izvršitelja.

````

---

## Usluge (117 modula)

Usluge su **usmjereni, jednonamjenski moduli** koje obrađivači kombiniraju. Glavne kategorije:

### Usmjeravanje i kombinacije

- `combo.ts` — ulazna točka za zahtjeve usmjerene kombinacijama
- `services/autoCombo/` — bodovanje prema 16 čimbenika, 8 strategija automatskog usmjeravanja
- `wildcardRouter.ts` — pronalazi podudaranja s rutama koje sadržavaju zamjenske znakove (`gpt-*`)
- `modelFamilyFallback.ts` — T5 rezervna opcija unutar obitelji

### Ograničavanje brzine i kvota

- `rateLimitManager.ts` — spremnik tokena po ključu i pružatelju
- `usage.ts` — bilježenje upotrebe
- `quotaCache.ts` — snimke kvota u memoriji

### Račun i token

- `tokenRefresh.ts` — osvježavanje OAuth tokena pri odgovoru 401
- `accountFallback.ts` — prebacivanje na alternativni račun
- `sessionManager.ts` — stanje višekoračne sesije

### Inteligencija

- `intentClassifier.ts` — klasifikacija namjere zahtjeva
- `taskAwareRouter.ts` — usmjeravanje prema vrsti zadatka
- `thinkingBudget.ts` — dodjeljivanje tokena za razmišljanje
- `contextManager.ts` — umetanje konteksta usmjeravanja

### Otpornost

- `resilience.ts` — orkestracija ponovnih pokušaja, vremenskog odmaka i prekidača
- `emergencyFallback.ts` — krajnja rezervna opcija
- `modelDeprecation.ts` — automatsko usmjeravanje na modele nasljednike

### Stanje

- `signatureCache.ts` — deduplikacija prema potpisu zahtjeva
- `volumeDetector.ts` — rasterećenje opterećenja
- `contextHandoff.ts` — serijalizacija sesije

### Kompresija

- `compression/` (poddirektorij) — cjelovit cjevovod kompresije
- 39 datoteka koje obuhvaćaju pogonske mehanizme, pakete pravila i prilagodnike

### Vještine

- (obrađeno u [SKILLS.md](./SKILLS.md))

### Memorija

- (obrađeno u [MEMORY.md](./MEMORY.md))

---

## Izvršitelji (75+ datoteka)

Jedna datoteka po pružatelju. Svi proširuju `BaseExecutor` i nadjačavaju ono što se razlikuje.

### Uobičajeni obrasci

Pružatelji se razrješavaju putem `getExecutor(providerId)`, koji vraća konfigurirani izvršitelj. Pružatelji kompatibilni s OpenAI-jem/Anthropicom upotrebljavaju `DefaultExecutor` (`executors/default.ts`). Ponašanje specifično za pružatelja (osnovni URL, zaglavlja za autentifikaciju, verzija API-ja) konfigurira se u `open-sse/config/providers/`, dok se transformacije tijela zahtjeva obrađuju u `open-sse/translator/`.

**Prilagođeni URL** postavlja se putem konfiguracije pružatelja:

```ts
// Konfiguracija pružatelja u open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Prilagođena autentifikacija** obrađuje se putem konfiguracije autentifikacije u registru pružatelja (API ključ, OAuth, profili zaglavlja).

Transformacije **prilagođenog tijela zahtjeva** (npr. Anthropicovo odvajanje `system` od `messages`) registriraju se zasebno za svakog pružatelja u `open-sse/translator/`.

````

### Tvornica izvršitelja

`executors/index.ts` izvozi `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Razrješavanje se odvija putem `ExecutorRegistry` (`executors/registry.ts`): svaki specijalizirani izvršitelj deklariran je u ugrađenoj tablici datoteke `executors/index.ts` i registriran putem `registerExecutor(alias, instance)` pri učitavanju modula; `getExecutor()` provjerava registar i vraća memoizirani `DefaultExecutor` za svakog pružatelja bez specijaliziranog unosa. Cjelovito mapiranje pseudonima na izvršitelje opisano je referentnim testom `tests/unit/executor-map-golden.test.ts`.

---

## Prevoditelji

Prevodite između **3 formata**: OpenAI, Anthropic, Gemini te novog Responses API-ja.

### Kada se izvodi prevođenje

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Uobičajena prevođenja:

- `OpenAI → Anthropic`: zasebno polje `system`, zaglavlje `x-api-key`
- `OpenAI → Gemini`: `contents` umjesto `messages`, `systemInstruction`
- `OpenAI → Responses API`: polje `input`, stanje `previous_response_id`

### Obrađeni rubni slučajevi

- Uloga `developer` → `system` za sustave koji nisu OpenAI
- Uloga `system` → spaja se s prvom korisničkom porukom za GLM/ERNIE
- `json_schema` → Geminijevi `responseMimeType` + `responseSchema`
- `tools` → format alata specifičan za pružatelja
- Parametri razmišljanja (o1, Claude) → ekvivalenti specifični za pružatelja

---

## MCP poslužitelj

`open-sse/mcp-server/` implementira poslužitelj za **Model Context Protocol**:

- **110 alata** (upravljanje pružateljima, kombinacije, memorija, predmemorija, kompresija, proxy, vještine, gamifikacija, dodaci, Notion, Obsidian, lokalni korpus)
- **3 prijenosna mehanizma**: stdio, SSE, Streamable HTTP
- **33 opsega** za preciznu autorizaciju

### Registracija alata

Alati se registriraju kao samostalne datoteke u `open-sse/mcp-server/tools/`, a svaka izvozi naziv, shemu, rukovatelja i opseg:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Dohvati snimku stanja sustava",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Prijenosni mehanizmi

```ts
// stdio (uporaba putem CLI-ja)
startMcpStdio(server);

// SSE (strujanje temeljeno na HTTP-u)
startMcpSse(server, port);

// Streamable HTTP (suvremeni MCP)
startMcpStreamable(server, port);
```

### Autorizacija

Svaki poziv alata prolazi kroz provjere opsega (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Nedovoljan opseg");
}
```

---

## Transformatori

`open-sse/transformer/` pretvara između formata **Chat Completions** i **Responses API**.

### Zašto zaseban transformator?

Responses API novi je OpenAI-jev format s **razgovorima koji čuvaju stanje** (`previous_response_id`). Kada klijent pošalje Responses zahtjev, OmniRoute:

1. Interno pretvara Responses → Chat Completions
2. Šalje pružatelju (bilo kojem pružatelju koji podržava Chat Completions)
3. Pretvara odgovor natrag u Responses format
4. Strujanjem šalje pretvoreni odgovor klijentu

Transformator (`transformer/responsesTransformer.ts`) pruža:

```ts
createResponsesApiTransformStream(): TransformStream
```

Time se obrađuju:

- Događaji `response.output_item.added`
- Događaji `response.output_text.delta`
- Događaj `response.completed`
- Mapiranje poziva alata (`function_call` ↔ `tool_calls`)

---

## Konfiguracija

`open-sse/config/` sadrži konfiguracijski sloj:

| Datoteka                      | Namjena                                                |
| ----------------------------- | ------------------------------------------------------ |
| `providerRegistry.ts`         | Registar modela za razgovor iz kataloga 352 pružatelja |
| `providerModels.ts`           | Aliasi modela, mapiranje formata                       |
| `constants.ts`                | Vremenska ograničenja, limiti, statusni kodovi         |
| `defaultThinkingSignature.ts` | Zadani Claudeov potpis razmišljanja                    |
| `modelStrip.ts` (u servisima) | Uklanjanje polja za pojedinog pružatelja               |

### Shema registra pružatelja

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Zod provjera valjanosti pri učitavanju modula osigurava da su sve konfiguracije pružatelja valjane.

---

## Ograničenja performansi

Mehanizam usmjeravanja ima strogo definirane ciljeve performansi:

| Operacija                                                   | Cilj  | Mjerenje                           |
| ----------------------------------------------------------- | ----- | ---------------------------------- |
| Razrješavanje kombinacije                                   | <10ms | Za 50 ciljeva                      |
| Provjera ograničenja stope                                  | <1ms  | Spremnik tokena u memoriji         |
| Zamjenski model iz porodice                                 | <5ms  | Predmemorirane definicije porodica |
| Prosljeđivanje zahtjeva za usmjeravanje                     | <2ms  | Kritična putanja                   |
| **Bez blokirajućeg I/O-a u kritičnoj putanji usmjeravanja** | —     | Sve je asinkrono                   |

---

## Antipatterni

❌ **Sinkroni pozivi bazi podataka u `combo.ts`** — unaprijed izračunajte i predmemorirajte
❌ **Logika ponovnih pokušaja u rukovateljima** — koristite `retry()` iz usluge otpornosti
❌ **Izravan pristup konfiguraciji pružatelja** — koristite gettere registra `providerRegistry`
❌ **Fiksno kodirani lanci zamjenskih opcija** — definirajte ih u `modelFamilyFallback.ts`
❌ **Promjene stanja između istodobnih zahtjeva** — koristite samo kontekst ograničen na zahtjev

---

## Dodavanje nove komponente

### Dodavanje nove usluge

1. Izradite `open-sse/services/[serviceName].ts` s jasno usmjerenom odgovornošću
2. Izvezite glavnu funkciju rukovatelja i sve konstante
3. Dodajte jedinične testove u `tests/unit/services/[serviceName].test.mjs`
4. Integrirajte u cjevovod zahtjeva u `handlers/chatCore.ts` (ako je povezano s usmjeravanjem)
5. Ažurirajte logiku usmjeravanja u `combo.ts` ako usluga utječe na odabir cilja
6. Dokumentirajte u ovoj datoteci

### Dodavanje novog izvršitelja

1. Izradite `open-sse/executors/[provider].ts` koji proširuje `BaseExecutor`
2. Registrirajte ga u `config/providerRegistry.ts`
3. Dodajte ga u tvornicu `executors/index.ts`
4. Dodajte jedinične testove za izvršitelja
5. Dokumentirajte u `docs/architecture/ARCHITECTURE.md`

### Dodavanje novog MCP alata

1. Izradite ili ažurirajte `open-sse/mcp-server/tools/[category]Tools.ts`
2. Definirajte Zod shemu za ulazne podatke
3. Registrirajte alat u `mcp-server/index.ts`
4. Dodajte ga u matricu opsega u `mcp-server/auth/`
5. Dodajte jedinične testove

---

## Pogledajte i

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — arhitektura visoke razine
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — inženjerska referenca
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — pregled svakog direktorija
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — bodovanje prema 16 faktora
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP poslužitelj
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A poslužitelj
- Izvor: `open-sse/` (više od 400 datoteka, ~143K redaka koda)
