# Termux Headless Setup (Hrvatski)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/TERMUX_GUIDE.md) · 🇪🇹 [am](../../../am/docs/guides/TERMUX_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/guides/TERMUX_GUIDE.md) · 🇦🇿 [az](../../../az/docs/guides/TERMUX_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/guides/TERMUX_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/guides/TERMUX_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/guides/TERMUX_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/guides/TERMUX_GUIDE.md) · 🇩🇰 [da](../../../da/docs/guides/TERMUX_GUIDE.md) · 🇩🇪 [de](../../../de/docs/guides/TERMUX_GUIDE.md) · 🇬🇷 [el](../../../el/docs/guides/TERMUX_GUIDE.md) · 🇪🇸 [es](../../../es/docs/guides/TERMUX_GUIDE.md) · 🇪🇪 [et](../../../et/docs/guides/TERMUX_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/guides/TERMUX_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/guides/TERMUX_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/guides/TERMUX_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/guides/TERMUX_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/guides/TERMUX_GUIDE.md) · 🇮🇱 [he](../../../he/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/guides/TERMUX_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/guides/TERMUX_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/guides/TERMUX_GUIDE.md) · 🇮🇩 [id](../../../id/docs/guides/TERMUX_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/guides/TERMUX_GUIDE.md) · 🇮🇹 [it](../../../it/docs/guides/TERMUX_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/guides/TERMUX_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/guides/TERMUX_GUIDE.md) · 🇰🇭 [km](../../../km/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/guides/TERMUX_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/guides/TERMUX_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/guides/TERMUX_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/guides/TERMUX_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/guides/TERMUX_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/guides/TERMUX_GUIDE.md) · 🇲🇲 [my](../../../my/docs/guides/TERMUX_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/guides/TERMUX_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/guides/TERMUX_GUIDE.md) · 🇳🇴 [no](../../../no/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [or](../../../or/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/guides/TERMUX_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/guides/TERMUX_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/guides/TERMUX_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/guides/TERMUX_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/TERMUX_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/guides/TERMUX_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/guides/TERMUX_GUIDE.md) · 🇱🇰 [si](../../../si/docs/guides/TERMUX_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/guides/TERMUX_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/guides/TERMUX_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/guides/TERMUX_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/guides/TERMUX_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/guides/TERMUX_GUIDE.md) · 🇮🇳 [te](../../../te/docs/guides/TERMUX_GUIDE.md) · 🇹🇭 [th](../../../th/docs/guides/TERMUX_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/guides/TERMUX_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/TERMUX_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/guides/TERMUX_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/guides/TERMUX_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/guides/TERMUX_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/guides/TERMUX_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/TERMUX_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/TERMUX_GUIDE.md)

---

OmniRoute može raditi kao poslužitelj bez grafičkog sučelja na Androidu putem Termuxa. Electron aplikacija za stolna računala nije podržana u Termuxu, ali web-nadzorna ploča i API kompatibilan s OpenAI-jem rade u lokalnom pregledniku ili na drugim uređajima na istoj mreži.

## Preduvjeti

Instalirajte Termux iz F-Droida ili putem izdanja na GitHubu, zatim ažurirajte pakete i instalirajte alate za izgradnju potrebne izvornim ovisnostima kao što je `better-sqlite3`.

```bash
pkg update
pkg upgrade
pkg install nodejs python build-essential git
```

> **Verzija Node.js-a:** OmniRoute zahtijeva Node `>=22.22.2 <23 || >=24.0.0 <27` (odgovara vrijednostima `engines` u `package.json` / `SUPPORTED_NODE_RANGE`). Termuxov `nodejs-lts` obično isporučuje Node 20 LTS, koji **više nije podržan** — umjesto toga instalirajte `pkg install nodejs` (trenutačna verzija) i provjerite prikazuje li `node --version` verziju iz linije 22.x/24.x+.

Ako kompiliranje izvornih paketa ne uspije, ponovno pokrenite prethodno navedenu naredbu `pkg install`, a zatim ponovno pokušajte instalirati OmniRoute.

## Instalacija

Izravno pokrenite najnoviji objavljeni paket:

```bash
npx -y omniroute@latest
```

Možete ga instalirati i globalno:

```bash
npm install -g omniroute
omniroute
```

## Pokretanje

Pokrenite OmniRoute u načinu rada poslužitelja bez grafičkog sučelja:

```bash
omniroute
```

ili:

```bash
npx omniroute
```

Nadzorna ploča dostupna je na:

```text
http://localhost:20128
```

Otvorite taj URL u pregledniku na Androidu. Ako klijente pokrećete unutar Termuxa, upotrijebite isti host i port kao osnovni URL kompatibilan s OpenAI-jem.

## Izvršavanje u pozadini

Za jednostavan proces u pozadini:

```bash
nohup omniroute > omniroute.log 2>&1 &
```

Za njegovo zaustavljanje:

```bash
pkill -f omniroute
```

Za automatsko pokretanje nakon uključivanja uređaja instalirajte dodatak Termux:Boot i izradite skriptu za pokretanje:

```bash
mkdir -p ~/.termux/boot
cat > ~/.termux/boot/omniroute.sh <<'EOF'
#!/data/data/com.termux/files/usr/bin/sh
cd "$HOME"
nohup omniroute > "$HOME/omniroute.log" 2>&1 &
EOF
chmod +x ~/.termux/boot/omniroute.sh
```

Androidova optimizacija baterije može zaustaviti dugotrajne pozadinske procese. Onemogućite optimizaciju baterije za Termux ako poslužitelj treba ostati dostupan.

## Pristup s drugih uređaja

Pronađite IP adresu telefona na WiFi mreži:

```bash
ip addr show wlan0
```

Zatim otvorite nadzornu ploču s drugog uređaja:

```text
http://PHONE_IP:20128
```

Na primjer:

```text
http://192.168.1.50:20128
```

Telefon i klijent držite na istoj pouzdanoj mreži. Ako OmniRoute učinite dostupnim izvan telefona, omogućite API ključeve i provjeru autentičnosti nadzorne ploče.

## Direktorij podataka

OmniRoute prema zadanim postavkama pohranjuje podatke u Termuxovu početnom direktoriju, slijedeći isto ponašanje putanje podataka na strani poslužitelja koje se upotrebljava na Linuxu. Da biste bazu podataka smjestili na izričito određeno mjesto:

```bash
export DATA_DIR="$HOME/.omniroute"
omniroute
```

## Ograničenja

- Electron se ne može izvoditi u Termuxu.
- Nema programske trake sustava ni integracije sa stolnim okruženjem.
- Ova je konfiguracija namijenjena samo poslužitelju: upotrebljavajte nadzornu ploču u pregledniku.
- Izvorne ovisnosti možda će trebati lokalno kompilirati.
- Na Android uređajima s malo memorije možda će trebati smanjiti broj istodobnih zahtjeva.
- Značajke MITM-a/sistemskih certifikata mogu zahtijevati rad sa spremištem pouzdanih certifikata na razini Androida, izvan Termuxa.

## Rješavanje problema

### Nepodržana platforma: android (svaki zahtjev vraća HTTP 500)

**Simptom:** `omniroute` / `omniroute serve` ispisuje `✔ OmniRoute is running!`, ali svaki zahtjev nadzorne ploče ili API-ja vraća samo `500 Internal Server Error`. `~/.omniroute/logs/application/app.log` ostaje prazan, `APP_LOG_LEVEL=debug` ne ispisuje ništa korisno, a tijelo odgovora običan je tekst (`Internal Server Error`) bez pojedinosti u JSON-u.

**Uzrok:** Neke verzije Termuxa/Nodea prijavljuju `process.platform === "android"`. Next.js `getCacheDirectory()` ne podržava tu platformu: zahtijeva da `~/.cache` (ili generički privremeni direktorij) _već_ postoji, inače učitavanje kuke za instrumentaciju ne uspijeva uz poruku:

```text
Error: An error occurred while loading instrumentation hook: Unsupported platform: android
```

Budući da se kuka nikad ne učita, bilježenje zapisnika nikad se ne pokreće — pogreška 500 izgleda kao da ju je potpuno nemoguće dijagnosticirati. OmniRoute stvara `~/.cache` (i postavlja `XDG_CACHE_HOME` ako nije postavljen) u ulaznoj točki CLI-ja prije pokretanja Next.js-a kako bi ova provjera uspjela na Androidu/Termuxu.

**Podržano rješenje (bez izmjene paketa):**

```bash
mkdir -p ~/.cache
omniroute serve
```

U trenutačnim verzijama OmniRoutea CLI to automatski radi na Androidu/Termuxu — nakon svježeg pokretanja `npx -y omniroute@latest` / globalne instalacije ovaj ručni korak ne bi trebao biti potreban. Ako i nakon nadogradnje i dalje vidite pogrešku, jednom izradite `~/.cache` kao što je prethodno prikazano i ponovno pokrenite aplikaciju.

**Nemojte** mijenjati `dist/server.js` kako biste prisilno postavili `process.platform = "linux"`. Takva se izmjena paketa prepisuje pri svakoj ponovnoj instalaciji/nadogradnji i nepotrebna je nakon što direktorij predmemorije postoji.

### Pogreške pri izgradnji paketa better-sqlite3

Instalirajte Termuxov skup alata za izgradnju:

```bash
pkg install nodejs python build-essential
```

Zatim ponovno pokrenite:

```bash
npx -y omniroute@latest
```

### Port se već upotrebljava

Provjerite što sluša na zadanom portu:

```bash
ss -ltnp | grep 20128
```

Zaustavite stari proces:

```bash
pkill -f omniroute
```

### Nadzorna ploča nije dostupna s drugog uređaja

Provjerite jesu li oba uređaja na istoj WiFi mreži, a zatim provedite test iz Termuxa:

```bash
curl http://localhost:20128
```

Ako lokalni pristup radi, ali pristup putem LAN-a ne radi, provjerite izolaciju pristupne točke/WiFi mreže na Androidu te sve profile vatrozida ili VPN-a na telefonu.
