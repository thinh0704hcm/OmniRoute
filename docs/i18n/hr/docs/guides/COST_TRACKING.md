# Cost & Spend Tracking (Hrvatski)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/COST_TRACKING.md) · 🇪🇹 [am](../../../am/docs/guides/COST_TRACKING.md) · 🇸🇦 [ar](../../../ar/docs/guides/COST_TRACKING.md) · 🇦🇿 [az](../../../az/docs/guides/COST_TRACKING.md) · 🇧🇬 [bg](../../../bg/docs/guides/COST_TRACKING.md) · 🇧🇩 [bn](../../../bn/docs/guides/COST_TRACKING.md) · 🇧🇦 [bs](../../../bs/docs/guides/COST_TRACKING.md) · 🇨🇿 [cs](../../../cs/docs/guides/COST_TRACKING.md) · 🇩🇰 [da](../../../da/docs/guides/COST_TRACKING.md) · 🇩🇪 [de](../../../de/docs/guides/COST_TRACKING.md) · 🇬🇷 [el](../../../el/docs/guides/COST_TRACKING.md) · 🇪🇸 [es](../../../es/docs/guides/COST_TRACKING.md) · 🇪🇪 [et](../../../et/docs/guides/COST_TRACKING.md) · 🇮🇷 [fa](../../../fa/docs/guides/COST_TRACKING.md) · 🇫🇮 [fi](../../../fi/docs/guides/COST_TRACKING.md) · 🇫🇷 [fr](../../../fr/docs/guides/COST_TRACKING.md) · 🇮🇪 [ga](../../../ga/docs/guides/COST_TRACKING.md) · 🇮🇳 [gu](../../../gu/docs/guides/COST_TRACKING.md) · 🇳🇬 [ha](../../../ha/docs/guides/COST_TRACKING.md) · 🇮🇱 [he](../../../he/docs/guides/COST_TRACKING.md) · 🇮🇳 [hi](../../../hi/docs/guides/COST_TRACKING.md) · 🇭🇺 [hu](../../../hu/docs/guides/COST_TRACKING.md) · 🇦🇲 [hy](../../../hy/docs/guides/COST_TRACKING.md) · 🇮🇩 [id](../../../id/docs/guides/COST_TRACKING.md) · 🇳🇬 [ig](../../../ig/docs/guides/COST_TRACKING.md) · 🇮🇹 [it](../../../it/docs/guides/COST_TRACKING.md) · 🇯🇵 [ja](../../../ja/docs/guides/COST_TRACKING.md) · 🇬🇪 [ka](../../../ka/docs/guides/COST_TRACKING.md) · 🇰🇭 [km](../../../km/docs/guides/COST_TRACKING.md) · 🇮🇳 [kn](../../../kn/docs/guides/COST_TRACKING.md) · 🇰🇷 [ko](../../../ko/docs/guides/COST_TRACKING.md) · 🇱🇹 [lt](../../../lt/docs/guides/COST_TRACKING.md) · 🇱🇻 [lv](../../../lv/docs/guides/COST_TRACKING.md) · 🇮🇳 [ml](../../../ml/docs/guides/COST_TRACKING.md) · 🇮🇳 [mr](../../../mr/docs/guides/COST_TRACKING.md) · 🇲🇾 [ms](../../../ms/docs/guides/COST_TRACKING.md) · 🇲🇹 [mt](../../../mt/docs/guides/COST_TRACKING.md) · 🇲🇲 [my](../../../my/docs/guides/COST_TRACKING.md) · 🇳🇵 [ne](../../../ne/docs/guides/COST_TRACKING.md) · 🇳🇱 [nl](../../../nl/docs/guides/COST_TRACKING.md) · 🇳🇴 [no](../../../no/docs/guides/COST_TRACKING.md) · 🇮🇳 [or](../../../or/docs/guides/COST_TRACKING.md) · 🇮🇳 [pa](../../../pa/docs/guides/COST_TRACKING.md) · 🇵🇭 [phi](../../../phi/docs/guides/COST_TRACKING.md) · 🇵🇱 [pl](../../../pl/docs/guides/COST_TRACKING.md) · 🇵🇹 [pt](../../../pt/docs/guides/COST_TRACKING.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/COST_TRACKING.md) · 🇷🇴 [ro](../../../ro/docs/guides/COST_TRACKING.md) · 🇷🇺 [ru](../../../ru/docs/guides/COST_TRACKING.md) · 🇱🇰 [si](../../../si/docs/guides/COST_TRACKING.md) · 🇸🇰 [sk](../../../sk/docs/guides/COST_TRACKING.md) · 🇸🇮 [sl](../../../sl/docs/guides/COST_TRACKING.md) · 🇷🇸 [sr](../../../sr/docs/guides/COST_TRACKING.md) · 🇸🇪 [sv](../../../sv/docs/guides/COST_TRACKING.md) · 🇰🇪 [sw](../../../sw/docs/guides/COST_TRACKING.md) · 🇮🇳 [ta](../../../ta/docs/guides/COST_TRACKING.md) · 🇮🇳 [te](../../../te/docs/guides/COST_TRACKING.md) · 🇹🇭 [th](../../../th/docs/guides/COST_TRACKING.md) · 🇹🇷 [tr](../../../tr/docs/guides/COST_TRACKING.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/COST_TRACKING.md) · 🇵🇰 [ur](../../../ur/docs/guides/COST_TRACKING.md) · 🇺🇿 [uz](../../../uz/docs/guides/COST_TRACKING.md) · 🇻🇳 [vi](../../../vi/docs/guides/COST_TRACKING.md) · 🇳🇬 [yo](../../../yo/docs/guides/COST_TRACKING.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/COST_TRACKING.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/COST_TRACKING.md)

---

Kako OmniRoute procjenjuje, bilježi i prijavljuje trošak svakog zahtjeva — i zašto broj na
nadzornoj ploči predstavlja **praćenje uštede**, a ne račun.

Pogledajte i: [Korisnički vodič](./USER_GUIDE.md) · [Galerija značajki](./FEATURES.md)

---

## Što jest (i što nije)

OmniRoute svakom dovršetku pripisuje trošak u USD po zahtjevu množenjem broja
tokena s cijenama modela. Ti brojevi koriste se na nadzornoj ploči **Troškovi**, u
CLI naredbama `omniroute cost` / `omniroute usage`, CSV/JSON izvozima i proračunima po API ključu.

> **„Trošak” na nadzornoj ploči predstavlja praćenje uštede, a ne račun.** OmniRoute vam
> nikada ništa ne naplaćuje — usmjerava vaše zahtjeve pružateljima koje ste već povezali
> (vlastite pretplate, besplatne razine i API ključevi). „Ukupni trošak od $290” ostvaren
> isključivo na besplatnim modelima znači da je to približno **$290 koje _niste_ platili**
> API-ju koji se plaća. Iznos je _procjena_ koliko bi isti promet koštao prema standardnim
> cijenama, kako biste mogli vidjeti gdje je koncentrirana vaša upotreba i koliko štedite
> usmjeravanjem prema jeftinijim ili besplatnim pružateljima.

Ovo je tumačenje izravno navedeno u projektnom dokumentu [README](../../README.md)
(„'trošak' na nadzornoj ploči predstavlja praćenje uštede, a ne račun”).

Budući da je broj procjena:

- Ovisi o tablici cijena koju OmniRoute ima za svaki model. Model bez zapisa o cijeni
  pridonosi trošku s `0` (u pregledniku se prikazuje kao redak „Naslijeđeno / besplatno”).
- Promet putem besplatne razine i pretplate i dalje ostvaruje _procijenjeni_ trošak — to je
  iznos koji štedite, a ne iznos koji dugujete.

---

## Kako se troškovi procjenjuju

### Izvor cijena

Troškovi dolaze iz tablice cijena koja se određuje prema sljedećem redoslijedu prvenstva
([`src/lib/pricingSync.ts`](../../src/lib/pricingSync.ts)):

1. **Korisničke izmjene** — cijene koje postavite na nadzornoj ploči / putem `PATCH /api/pricing`.
2. **Sinkronizirane vanjske cijene** — dohvaćaju se iz javne LiteLLM-ove datoteke
   `model_prices_and_context_window.json` kada je sinkronizacija omogućena (pohranjuju se
   u zasebnom prostoru naziva `pricing_synced` kako nikada ne bi prebrisale vaše izmjene).
3. **Zadane vrijednosti ugrađene u kod** — isporučuju se s OmniRouteom.

Sinkronizacija vanjskih cijena zahtijeva **izričito uključivanje** i prema zadanim je
postavkama onemogućena. Relevantne varijable okruženja
(pogledajte [`.env.example`](../../.env.example)):

| Varijabla okruženja     | Zadana vrijednost | Svrha                                                                   |
| ----------------------- | ----------------- | ----------------------------------------------------------------------- |
| `PRICING_SYNC_ENABLED`  | `false`           | Omogućuje pozadinsku sinkronizaciju cijena iz LiteLLM-a pri pokretanju. |
| `PRICING_SYNC_INTERVAL` | `86400`           | Interval sinkronizacije u **sekundama** (zadano jednom dnevno).         |
| `PRICING_SYNC_SOURCES`  | `litellm`         | Popis izvora odvojenih zarezima (trenutačno je podržan samo `litellm`). |

### Formula troška

Trošak se izračunava za svaki zahtjev iz broja tokena i cijena po milijun tokena u
[`src/lib/usage/costCalculator.ts`](../../src/lib/usage/costCalculator.ts)
(`computeCostFromPricing` / `calculateCost`):

- **Ulazni tokeni** (umanjeni za čitanja iz predmemorije i tokene stvaranja predmemorije) × cijena `input`.
- **Tokeni pročitani iz predmemorije** × cijena `cached` (ako nije dostupna, koristi se cijena ulaznih tokena).
- **Tokeni stvaranja predmemorije** × cijena `cache_creation` (ako nije dostupna, koristi se cijena ulaznih tokena).
- **Izlazni tokeni** × cijena `output`.
- **Tokeni zaključivanja** × cijena `reasoning` (ako nije dostupna, koristi se cijena izlaznih tokena).

Sve cijene tumače se kao USD za 1.000.000 tokena. Razina usluge Codex „fast”/„priority”
ili „flex” primjenjuje množitelj troška (`getCodexFastCostMultiplier`) — primjerice, flex
se obračunava uz 50-postotni popust na tokene, što se na nadzornoj ploči prikazuje kao
**flex ušteda**.

Nazivi modela najprije se normaliziraju (uklanjaju se prefiksi putanja pružatelja kao što
su `openai/` ili `accounts/fireworks/models/`) kako bi povijesni retci i dalje odgovarali
cijeni.

### Kako se potrošnja bilježi

- Trošak po zahtjevu izračunava se nakon odgovora i bilježi bez čekanja na dovršetak kako
  klijentu nikada ne bi povećao latenciju. Potrošnja zajedničke kvote raspoređuje se u
  sljedećem ciklusu petlje događaja putem datoteke
  [`src/lib/quota/spendRecorder.ts`](../../src/lib/quota/spendRecorder.ts).
- Potrošnja API ključa privremeno se sprema i skupno zapisuje putem komponente
  [`SpendBatchWriter`](../../src/lib/spend/batchWriter.ts) (zadani interval zapisivanja
  od 60 s, međuspremnik od 1.000 zapisa). Može se prilagoditi putem:

  | Varijabla okruženja                 | Zadana vrijednost | Svrha                                                 |
  | ----------------------------------- | ----------------- | ----------------------------------------------------- |
  | `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` | `60000`           | Interval pražnjenja međuspremnika u milisekundama.    |
  | `OMNIROUTE_SPEND_MAX_BUFFER_SIZE`   | `1000`            | Najveći broj zapisa u međuspremniku prije pražnjenja. |

Podaci o troškovima na nadzornoj ploči **ne** čitaju se iz pohranjenog dolarskog iznosa
za svaki redak — iznova se izračunavaju iz broja tokena i trenutačne tablice cijena svaki
put kada se pokrene krajnja točka za analitiku. To znači da ispravljanje pogrešne cijene
(i ponovna sinkronizacija) retroaktivno ažurira povijesne procjene troškova.

---

## Nadzorna ploča: stranica Troškovi

Stranica **Troškovi** nalazi se na `/dashboard/costs`
(`src/app/(dashboard)/dashboard/costs/`).
Njezin je glavni prikaz kartica **Pregled troškova**
(`src/app/(dashboard)/dashboard/costs/CostOverviewTab.tsx`),
koja sve podatke učitava putem `GET /api/usage/analytics`.

Što prikazuje:

- **Kartice potrošnje** — procijenjena potrošnja za _Danas (1d)_, _7d_, _30d_ i odabrano
  razdoblje. Odabir raspona: `7d`, `30d`, `90d`, `all`.
- **Glavne metrike** — zahtjevi u razdoblju, aktivni pružatelji, aktivni modeli, prosječni
  trošak po zahtjevu.
- **Istraživač troškova** — tablica s mogućnošću sortiranja i filtriranja, grupirana prema **pružatelju**, **modelu**,
  **API ključu**, **računu** ili **razini usluge**, s troškom, zahtjevima, tokenima, prosječnim
  troškom po zahtjevu i postotnim udjelom u ukupnom iznosu.
- **Upotreba tokena** — ukupan broj / ulazni / izlazni tokeni i omjer ulaznih i izlaznih tokena.
- **Učinkovitost usmjeravanja** — broj prebacivanja na rezervnu opciju, stopa prebacivanja i pokrivenost zatraženog modela.
- **Mjesečna prognoza** — predviđa potrošnju do kraja mjeseca na temelju nedavnog dnevnog prosjeka.
- **Usporedba razdoblja** — postotna promjena između prve i druge polovice razdoblja.
- **Grafikoni** — dnevni trend troškova, udio pružatelja (kružni grafikon), vodeći pružatelji, vodeći modeli, trošak
  po API ključu, trošak po računu, tjedni obrazac upotrebe i toplinska karta aktivnosti.
- **Izvoz** — preuzimanje trenutačnog razdoblja kao **CSV** ili **JSON** (gumbi se prikazuju
  kada postoje podaci s troškom većim od nule).

Kada nema tarifiranog prometa, retci se prikazuju s oznakom „Naslijeđeno / Besplatno” umjesto `$0`,
u skladu s modelom praćenja ušteda.

### Povezane podstranice Troškova

Područje Troškovi također sadržava sljedeće stranice (sve pod `/dashboard/costs/`):

- **Cijene** (`/dashboard/costs/pricing`) — pregled i nadjačavanje cijena po modelu (prikazuje
  zajedničku karticu Cijene).
- **Proračun** (`/dashboard/costs/budget`) — postavljanje ograničenja potrošnje po opsegu (prikazuje zajedničku
  karticu Proračun).
- **Dijeljenje kvote** (`/dashboard/costs/quota-share`) — skupovi dijeljenih kvota i prikazi
  stope potrošnje.

---

## API krajnje točke

Sve zahtijevaju autentifikaciju za upravljanje (povratna petlja/JWT, putem `requireManagementAuth`), osim ako
nije drukčije navedeno.

### Analitika upotrebe i troškova

| Metoda | Krajnja točka            | Svrha                                                                                                                                                                        |
| ------ | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/analytics`   | Potpuna analitika troškova/upotrebe: sažetak, dnevni trend, prema pružatelju/modelu/API ključu/računu/razini. Upit: `range`, `startDate`, `endDate`, `apiKeyIds`, `presets`. |
| `GET`  | `/api/usage/utilization` | Iskorištenost kvote po pružatelju tijekom vremena. Upit: `range` (`1h`/`24h`/`7d`/`30d`), `provider`.                                                                        |
| `GET`  | `/api/usage/history`     | Neobrađeni retci povijesti upotrebe.                                                                                                                                         |
| `GET`  | `/api/usage/call-logs`   | Zapisi poziva po zahtjevu (model, tokeni, trošak, latencija, status).                                                                                                        |
| `GET`  | `/api/usage/quota`       | Status kvote pružatelja.                                                                                                                                                     |
| `GET`  | `/api/usage/proxy-logs`  | Zapisi proxy zahtjeva.                                                                                                                                                       |

### Proračuni

| Metoda | Krajnja točka            | Svrha                                                                                           |
| ------ | ------------------------ | ----------------------------------------------------------------------------------------------- |
| `GET`  | `/api/usage/budget`      | Sažetak troškova + provjera proračuna za jedan API ključ (parametar upita `apiKeyId` obavezan). |
| `POST` | `/api/usage/budget`      | Postavljanje dnevnih/tjednih/mjesečnih ograničenja u USD-u + praga upozorenja za API ključ.     |
| `GET`  | `/api/usage/budget/bulk` | Skupni sažeci proračuna za više API ključeva.                                                   |

> API za proračun ograničen je na pojedinačni **API ključ** (`apiKeyId`). Ograničenja koja vraća
> `GET /api/usage/budget` uključuju `dailyLimitUsd`, `weeklyLimitUsd`, `monthlyLimitUsd`,
> `warningThreshold` i tekuće ukupne iznose (`totalCostToday`, `totalCostMonth`, …).

### Cijene

| Metoda   | Krajnja točka           | Svrha                                                                                                                    |
| -------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `GET`    | `/api/pricing`          | Trenutačne objedinjene cijene (korisničke + sinkronizirane + zadane). `?includeSources=1` prikazuje izvor za svaki unos. |
| `PATCH`  | `/api/pricing`          | Nadjačavanje cijena za `{ provider: { model: { input, output, cached, … } } }`.                                          |
| `DELETE` | `/api/pricing`          | Vraćanje cijena na zadane vrijednosti (opcionalno ograničeno putem `?provider=&model=`).                                 |
| `GET`    | `/api/pricing/defaults` | Prikaz zadanih rezervnih cijena po 1M.                                                                                   |
| `GET`    | `/api/pricing/models`   | Cijene indeksirane prema modelu.                                                                                         |
| `POST`   | `/api/pricing/sync`     | Pokretanje ručne sinkronizacije iz vanjskih izvora (LiteLLM).                                                            |
| `GET`    | `/api/pricing/sync`     | Trenutačni status sinkronizacije.                                                                                        |
| `DELETE` | `/api/pricing/sync`     | Brisanje svih sinkroniziranih podataka o cijenama.                                                                       |

### Ostale krajnje točke povezane s troškovima

| Metoda | Krajnja točka                 | Svrha                                                                           |
| ------ | ----------------------------- | ------------------------------------------------------------------------------- |
| `GET`  | `/api/free-tier/summary`      | Ukupan broj tokena besplatnog modela, potrošnja ovog mjeseca i preostala kvota. |
| `GET`  | `/api/quota/pools/[id]/usage` | Potrošnja za skup dijeljene kvote.                                              |

---

## CLI

OmniRouteov CLI pruža naredbe za troškove, upotrebu i cijene (registrirane u
[`bin/cli/commands/registry.mjs`](../../bin/cli/commands/registry.mjs)).

### `omniroute cost`

Izvješće o troškovima agregirano iz `/api/usage/analytics`.

```bash
omniroute cost                          # posljednjih 30 dana, grupirano prema pružatelju
omniroute cost --period 7d              # posljednjih 7 dana
omniroute cost --group-by model         # grupiranje prema provider | model | combo | api-key | day
omniroute cost --since 2026-06-01 --until 2026-06-13
omniroute cost --api-key <key> --limit 50
```

Stupci: grupa, zahtjevi, ulazni/izlazni tokeni, trošak (USD) i % ukupnog iznosa. Redak sa sveukupnim iznosom
ispisuje se na kraju (ne prikazuje se uz `--quiet` ili `--output json`).

### `omniroute usage`

```bash
omniroute usage analytics --period 30d [--provider <id>]   # sažetak troškova po pružatelju
omniroute usage logs [--limit 100] [--follow] [--api-key <k>] [--search <q>]
omniroute usage quota [--provider <id>] [--check]
omniroute usage utilization [--api-key <k>]
omniroute usage history [--limit 100]
omniroute usage proxy-logs [--limit 100]

# Proračuni
omniroute usage budget list
omniroute usage budget get [scope]
omniroute usage budget set <amount> [--scope global] [--period monthly]
omniroute usage budget reset [scope]
```

### `omniroute pricing`

```bash
omniroute pricing list [--provider <p>] [--model <m>] [--limit 200]
omniroute pricing get <model>
omniroute pricing sync [--provider <p>] [--force]   # POST /api/pricing/sync
omniroute pricing diff [--model <m>]
omniroute pricing defaults show
omniroute pricing defaults set [--input <p>] [--output <p>] [--cache-read <p>] [--cache-write <p>]
```

> `pricing defaults show` čita `GET /api/pricing/defaults`. Za uređivanje cijena pojedinačnih modela
> umjesto toga upotrijebite stranicu **Cijene** na nadzornoj ploči ili `PATCH /api/pricing`.

---

## Rješavanje problema

- **Svi troškovi prikazuju $0 / „Legacy / Free”.** Modeli koji se upotrebljavaju nemaju zapis o cijeni.
  Omogućite vanjsku sinkronizaciju (`PRICING_SYNC_ENABLED=true`) i pokrenite `omniroute pricing sync` ili
  ručno postavite cijene putem stranice Cijene / `PATCH /api/pricing`.
- **Povijesni model ima pogrešnu cijenu.** Ispravite cijenu (nadjačavanjem ili ponovnom sinkronizacijom) — trošak se
  ponovno izračunava iz broja tokena pri svakom čitanju analitike, pa se procjene retroaktivno ažuriraju.
- **Potrošnja kasni za stvarnim vremenom.** Potrošnja po ključu obrađuje se skupno; smanjite
  `OMNIROUTE_SPEND_FLUSH_INTERVAL_MS` ako su vam potrebni ažurniji podaci.

---

Za informacije o tome kako se ovo uklapa u širu nadzornu ploču pogledajte [Korisnički vodič](./USER_GUIDE.md) i
[Galeriju značajki](./FEATURES.md).
