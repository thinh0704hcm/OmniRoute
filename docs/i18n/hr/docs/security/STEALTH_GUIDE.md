# Stealth Guide (Hrvatski)

🌐 **Languages:** 🇺🇸 [English](../../../../security/STEALTH_GUIDE.md) · 🇪🇹 [am](../../../am/docs/security/STEALTH_GUIDE.md) · 🇸🇦 [ar](../../../ar/docs/security/STEALTH_GUIDE.md) · 🇦🇿 [az](../../../az/docs/security/STEALTH_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/security/STEALTH_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/security/STEALTH_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/security/STEALTH_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/security/STEALTH_GUIDE.md) · 🇩🇰 [da](../../../da/docs/security/STEALTH_GUIDE.md) · 🇩🇪 [de](../../../de/docs/security/STEALTH_GUIDE.md) · 🇬🇷 [el](../../../el/docs/security/STEALTH_GUIDE.md) · 🇪🇸 [es](../../../es/docs/security/STEALTH_GUIDE.md) · 🇪🇪 [et](../../../et/docs/security/STEALTH_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/security/STEALTH_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/security/STEALTH_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/security/STEALTH_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/security/STEALTH_GUIDE.md) · 🇮🇱 [he](../../../he/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/security/STEALTH_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/security/STEALTH_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/security/STEALTH_GUIDE.md) · 🇮🇩 [id](../../../id/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/security/STEALTH_GUIDE.md) · 🇮🇹 [it](../../../it/docs/security/STEALTH_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/security/STEALTH_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/security/STEALTH_GUIDE.md) · 🇰🇭 [km](../../../km/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/security/STEALTH_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/security/STEALTH_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/security/STEALTH_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/security/STEALTH_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/security/STEALTH_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/security/STEALTH_GUIDE.md) · 🇲🇲 [my](../../../my/docs/security/STEALTH_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/security/STEALTH_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/security/STEALTH_GUIDE.md) · 🇳🇴 [no](../../../no/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [or](../../../or/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/security/STEALTH_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/security/STEALTH_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/security/STEALTH_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/security/STEALTH_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/security/STEALTH_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/security/STEALTH_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/security/STEALTH_GUIDE.md) · 🇱🇰 [si](../../../si/docs/security/STEALTH_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/security/STEALTH_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/security/STEALTH_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/security/STEALTH_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/security/STEALTH_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/security/STEALTH_GUIDE.md) · 🇮🇳 [te](../../../te/docs/security/STEALTH_GUIDE.md) · 🇹🇭 [th](../../../th/docs/security/STEALTH_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/security/STEALTH_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/security/STEALTH_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/security/STEALTH_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/security/STEALTH_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/security/STEALTH_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/security/STEALTH_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/security/STEALTH_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/security/STEALTH_GUIDE.md)

---

> **Izvor istine:** `open-sse/utils/tlsClient.ts`, `open-sse/services/{tlsClientBase,claudeTlsClient,perplexityTlsClient,grokTlsClient,notionTlsClient,lmarenaTlsClient,claudeCodeCCH,claudeCodeFingerprint,claudeCodeObfuscation,claudeCodeCompatible}.ts`, `open-sse/config/cliFingerprints.ts`, `src/mitm/`
> **Posljednje ažuriranje:** 2026-09-02 — v3.8.51
> **Publika:** Inženjeri koji održavaju prikrivene integracije specifične za pružatelje usluga.

OmniRoute se integrira s pružateljima usluga čiji rubni sustavi aktivno utvrđuju otiske neslužbenih klijenata (TLS JA3/JA4, redoslijed zaglavlja, struktura JSON tijela, tokeni integriteta). Ova stranica dokumentira prikrivene površine koje OmniRoute izlaže i mjesta njihove implementacije.

## Pravna i etička napomena

Prikrivene značajke postoje kako bi OmniRoute mogao djelovati kao sloj kompatibilnosti između službenih računa u vlasništvu korisnika (Claude Code CLI, Codex, Antigravity, Cursor itd.) i objedinjenog API-ja sustava OmniRoute. One **nisu** namijenjene izbjegavanju otkrivanja prijevara, dijeljenju vjerodajnica ili kršenju Uvjeta pružanja usluge. Održavatelji očekuju da operatori poštuju uvjete korištenja nadređenih usluga koje su prihvatili prilikom izrade računa.

---

## Sloj za utvrđivanje TLS otiska

### `open-sse/utils/tlsClient.ts` — wreq-js (Chrome 124)

Trajne sesije paketa `wreq-js` stvaraju se lijeno za svaki opseg računa i razriješeni proxy. Skup
objekata `TlsClient` na razini procesa sadrži najviše 128 sesija koje oponašaju **Chrome 124 na sustavu macOS** za
nadređene sustave iza Cloudflarea. `TlsClient.fetch()` završava neuspjehom bez zamjenskog ponašanja kada izvorno izvršno okruženje
nije dostupno; pozivatelj može izričito odabrati zamjensku mogućnost izvan ovog omotača.

- Profil sesije: `browser: "chrome_124", os: "macos"`
- Razrješavanje proxyja (redoslijed prioriteta): `HTTPS_PROXY` → `HTTP_PROXY` → `ALL_PROXY` (također malim slovima)
- Istek vremena: `TLS_CLIENT_TIMEOUT_MS` (nasljeđuje se iz `FETCH_TIMEOUT_MS`, zadano 600000)
- Odgovor paketa `wreq-js` kompatibilan je s funkcijom fetch (`headers`, `text()`, `json()`, `clone()`, `body`).
- Nadzornik prvog bajta (`open-sse/utils/tlsFirstByteWatchdog.ts`, #12656): `TlsClient.fetch()`
  razrješava se čim stignu zaglavlja nadređenog sustava, pa sam `TLS_CLIENT_TIMEOUT_MS` ne može ograničiti
  tijelo koje nikada ne isporuči prvi bajt. `guardTlsFirstByte()` stavlja prvo `read()` tijela u utrku
  s `TLS_FIRST_BYTE_WATCHDOG_MS` (zadano `10000`, `0` ga onemogućuje); na ispravno tijelo to
  ne utječe, dok zastalo tijelo otkazuje wreq čitač i dopušta da postojeća
  TLS zamjenska logika funkcije `proxyFetch` prijeđe na izravni/proxy dispečer (zahtjev koji nije sigurno ponovno poslati, npr.
  POST s tijelom, i dalje izaziva pogrešku umjesto tihog ponovnog pokušaja).

### Prijenos pružatelja usluga s web-kolačićima — wreq-js 3.2.0

`open-sse/services/tlsClientBase.ts` zajednički je prilagodnik za pet specijaliziranih
prijenosa s web-kolačićima navedenih u nastavku. Svaki tanki omotač pružatelja odabire profil preglednika/OS-a. Prilagodnik
koristi jedinstveni učitavač izvršnog okruženja wreq i skup prijenosa u `open-sse/utils/tlsClient.ts`, indeksiran prema
profilu + OS-u + razriješenom proxyju, dok svaki zahtjev upotrebljava `cookieMode: "ephemeral"`. Računi i
zahtjevi stoga dijele veze na razini prijenosa, ali nikada wreq sesiju ili spremnik kolačića.

| Pružatelj usluge | Profil        | Emulirani OS | Pravilo za EOF toka                    |
| ---------------- | ------------- | ------------ | -------------------------------------- |
| Claude           | `chrome_146`  | Linux        | uključi `[DONE]`                       |
| Perplexity       | `firefox_148` | macOS        | uključi `event: end_of_stream`         |
| Grok             | `chrome_146`  | Linux        | isključi `[DONE]`                      |
| Notion           | `chrome_146`  | Windows      | uključi `[DONE]`                       |
| LMArena          | `chrome_146`  | Windows      | bez oznake; zatvori pri izvornom EOF-u |

- Strujanje izravno troši izvorni `ReadableStream` odgovora; ne stvara se privremena datoteka ni
  pomoćni proces.
- Prije izlaganja toka pregledava se do 256 početnih bajtova. SSE pružatelji međuspremnički pohranjuju pogreške koje nisu SSE;
  Grok/LMArena mapiraju Cloudflare izazove na `403`, a HTML međustranice na `502`.
- Izvorni istek vremena zahtjeva i dalje je obuhvaćen apsolutnim tvrdim JS rokom. Zastoj poništava valjanost
  i zatvara samo zahvaćeni prijenos profila/OS-a/proxyja prije nego što ga sljedeći zahtjev ponovno stvori.
- Redoslijed prioriteta za razrješavanje proxyja je `proxyUrl` pojedinačnog poziva → kontekst računa/nadzorne ploče ograničen na zahtjev →
  `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` (uključujući inačice malim slovima). Pogreške razrješavanja završavaju
  neuspjehom bez zamjenskog ponašanja umjesto propuštanja izravne veze. LMArena se namjerno razrješava prema `arena.ai`.
- `byteResponse` vraća `data:` URL s vrstom sadržaja bez oštećenja UTF-8 kodiranja.
- Pogreške su `TlsClientUnavailableError` (paket/dodatak nije dostupan), `TlsClientHangError`
  (rok prekoračen) i `WreqTransportCapacityError` (kod pogreške kapaciteta zajedničke sesije)
  kada je svih 128 ograničenih mjesta za profil/OS/proxy aktivno ili u postupku zatvaranja.

Prethodno navedena generička sesija objekta `TlsClient` ostaje specijalizirana za trajno stanje kolačića podržano preglednikom.
Oba puta ponovno upotrebljavaju jedan predmemorirani učitavač modula wreq i kuku životnog ciklusa procesa; njihovi skupovi ostaju
odvojeni jer su njihova trajanja kolačića namjerno različita.

Profile podržava prikvačeni paket, ali se stvarno prihvaćanje WAF-a može promijeniti neovisno
o lokalnim ugovornim testovima. Provjerite promjene otiska pomoću izričito odobrenog aktivnog računa
prije nego što ustvrdite jednakost s nadređenim preglednikom.

---

## Claude Code prikriveni paket

Kada je `cliCompatMode` uključen, OmniRoute preoblikuje odlazne Claude zahtjeve tako da ih nije moguće razlikovati od `claude-cli` prometa. Tri modula surađuju:

### `claudeCodeFingerprint.ts`

Izračunava troznamenkasti `cc_version` otisak ugrađen u zaglavlje za naplatu:

```
SHA256(SALT + msg[4] + msg[7] + msg[20] + version)[:3]
```

- `FINGERPRINT_SALT = "59cf53e54c78"` (tvrdo kodirano; odgovara službenom klijentu)
- Ulazi: znakovi na indeksima 4, 7 i 20 teksta prve korisničke poruke + niz verzije
- Izlaz: troznamenkasti heksadekadski prefiks

### `claudeCodeCCH.ts` (kontrolni sažetak sadržaja klijenta)

Provjera integriteta na strani poslužitelja koju službeni Claude Code CLI izračunava putem Bun/Zig. OmniRoute je ponovno implementira s pomoću `xxhash-wasm`:

1. Serijalizirajte tijelo s rezerviranim mjestom `cch=00000;`
2. `xxhash64(bytes, seed) & 0xFFFFF`
3. Heksadekadska vrijednost od 5 malih znakova, dopunjena nulama
4. Zamijenite `cch=00000;` izračunatim tokenom

Konstante:

- Početna vrijednost: `0x6e52736ac806831e`
- Uzorak: `/\bcch=([0-9a-f]{5});/`

### `claudeCodeObfuscation.ts`

Umeće Unicode **spojnik nulte širine** (`U+200D`) nakon prvog znaka „osjetljivih” naziva klijenata kako ih filtri uzlaznog sustava ne bi mogli pronaći naredbom grep. Zadani popis riječi:

```
opencode, open-code, cline, roo-cline, roo_cline, cursor, windsurf,
aider, continue.dev, copilot, avante, codecompanion
```

Primjenjuje se na: blokove `system`, sav `messages[].content` te `tools[].description` / `tools[].function.description`. Operator ga može nadjačati putem `setSensitiveWords()`.

### `claudeCodeCompatible.ts` — pružatelji `anthropic-compatible-cc-*`

Za Anthropic posrednike trećih strana koji prihvaćaju samo promet „pravog Claude Codea”:

- `CLAUDE_CODE_COMPATIBLE_USER_AGENT = "claude-cli/2.1.258 (external, sdk-cli)"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_PACKAGE_VERSION = "0.112.1"`
- `CLAUDE_CODE_COMPATIBLE_STAINLESS_RUNTIME_VERSION = "v26.3.0"`
- `anthropic-beta = "claude-code-20250219,interleaved-thinking-2025-05-14,effort-2025-11-24"` prema zadanim postavkama
- Prekidač „Omogući beta-verziju redigiranog razmišljanja” za pojedinu vezu dodaje `redact-thinking-2026-02-12` kada CC Compatible uzlazni sustav izričito zahtijeva redigirane tokove razmišljanja
- Prekidač „Omogući prikaz sažetog razmišljanja” za pojedinu vezu pohranjuje `providerSpecificData.requestDefaults.summarizeThinking` i dodaje `display: "summarized"` CC Compatible zahtjevima za razmišljanje koji još nemaju postavljen način prikaza
- `CONTEXT_1M_BETA_HEADER = "context-1m-2025-08-07"` (obitelj Opus/Sonnet 4.x)
- Zadana putanja: `/v1/messages?beta=true`

Srodni moduli u istom paketu:

- `claudeCodeConstraints.ts` — pravila temperature + upravljanja predmemorijom
- `claudeCodeToolRemapper.ts` — ponovno mapiranje naziva alata
- `claudeCodeExtraRemap.ts` — dodatna normalizacija korisnog sadržaja

---

## Antigravity prikrivanje

Antigravity zahtjevi čuvaju tekst pozivatelja bajt po bajt. OmniRoute ne umeće znakove nulte širine u upite niti preimenuje/umeće alate radi oponašanja IDE klijenta.

### `antigravityHeaderScrub.ts`

Uklanja oznake Stainless SDK-a (`x-stainless-lang`, `x-stainless-package-version`, `x-stainless-os`, `x-stainless-arch`, `x-stainless-runtime`, `x-stainless-runtime-version`, `x-stainless-timeout`, `x-stainless-retry-count`, `x-stainless-helper-method`) prije prosljeđivanja.

### ⚠️ Rizik: `ANTIGRAVITY_CREDITS=always` (kritična točka za zabranu računa)

`ANTIGRAVITY_CREDITS=always` (koristi ga `open-sse/executors/antigravity.ts`) usmjerava **svaki** zahtjev kroz prekoračenja Antigravity AI kredita (plaćeni Google krediti), umjesto da dopusti kvoti Googleove besplatne razine da ograniči uporabu. To je dokumentirano kao značajka, ali predstavlja **najčešću prijavu kršenja Uvjeta pružanja usluge koju susrećemo** — više Google Ultra računa zabranjeno je uz `403 / "service disabled for ToS violation" / insufficient_quota` nakon nekoliko sati rada s postavkom `=always`.

Provedba pravila u uzlaznom sustavu odvija se na **Googleovoj strani** i OmniRoute je ne može spriječiti. Naziv varijable okruženja i postojeća dokumentacija ostavljaju dojam da je riječ o sigurnoj postavci koju možete uključiti; nije.

**Zašto ovo agresivnije aktivira otkrivanje zlouporabe od korištenja samo besplatne razine:**

- Kontinuirana automatizirana potrošnja na jednom Google računu označava se drukčije od slučaja u kojem besplatna razina dosegne kvotu i zaustavi se.
- Prekoračenja kredita nemaju gornju granicu učestalosti, pa pogrešno konfiguriran klijent može potrošiti nekoliko stotina USD u nekoliko minuta i izgledati kao preprodaja API ključeva ili botski promet.
- Ako više korisnika OmniRoutea paralelno troši kredite za prekoračenje s iste vanjske IP adrese, signal se dodatno pojačava.

**Preporučeni pristup:**

1. Zadržite zadanu postavku `ANTIGRAVITY_CREDITS=off`, osim ako operator izričito prihvaća rizik plaćenih kredita i sankcioniranja računa. `retry` najprije šalje uobičajeni zahtjev i umeće kredite najviše jednom nakon odgovarajuće pogreške kvote 429; `always` umeće kredite pri prvom zahtjevu.
2. **Raspodijelite opterećenje među pružateljima putem Auto-Combo** (`model: "auto"` ili kombinacija `kr/glm/etc`) umjesto preopterećivanja jednog Antigravity računa.
3. **Postavite ograničenja RPM-a za pojedinu vezu** na stranici za uređivanje pružatelja Antigravity (Nadzorna ploča → Pružatelji → Antigravity → veza → ograničenje učestalosti). 30–60 RPM razumna je gornja granica za kontinuiranu uporabu.
4. **Koristite stabilnu mrežnu infrastrukturu uzlaznog sustava kojom upravlja operator** i izbjegavajte dijeljenje jednog računa među nepovezanim korisnicima ili radnim opterećenjima.
5. **U slučaju zabrane**: podnesite žalbu putem `support.google.com` → „Vrati pristup Workspaceu/računu” s točnim tijelom odgovora `quota_exceeded` / `service disabled` koje je Google poslao. Vraćanje pristupa nije zajamčeno.

Referenca varijabli okruženja dokumentira posljedice svakog načina rada kredita za račun i potrošnju.

Dodirne točke:

- `open-sse/executors/antigravity.ts` — čita `process.env.ANTIGRAVITY_CREDITS`
- `src/lib/oauth/providers/antigravity.ts` — prosljeđivanje vjerodajnica
- Izvorna prijava incidenta: rasprava [#1183](https://github.com/diegosouzapw/OmniRoute/discussions/1183)

---

## Registar CLI otisaka — `open-sse/config/cliFingerprints.ts`

Tablica po pružatelju koja fiksira **točan** redoslijed zaglavlja i redoslijed polja JSON tijela zabilježen u mitmproxy tragovima službenih CLI-jeva. Trenutačno su registrirani: `codex`, `claude`, kao i profili izvedeni tijekom izvođenja u datoteci `providerHeaderProfiles.ts` za `antigravity` i `github`.

```ts
interface CliFingerprint {
  headerOrder: string[]; // razlikuje velika i mala slova
  bodyFieldOrder: string[]; // JSON ključevi najviše razine
  userAgent?: string | (() => string);
  extraHeaders?: Record<string, string>;
}
```

Uključuje se ili isključuje zasebno za svakog pružatelja putem varijabli okruženja (pogledajte u nastavku). Kada je onemogućeno, zaglavlja/ključevi tijela pojavljuju se redoslijedom kojim su ih Node/JSON vratili — što omogućuje jednostavno utvrđivanje otiska.

---

## MITM proxy (Antigravity, Linux/macOS/Windows)

Za CLI-jeve čije se binarne datoteke ne mogu preusmjeriti putem `OPENAI_BASE_URL`, OmniRoute pokreće lokalni proxy koji terminira TLS. Krajnje točke nalaze se pod `src/app/api/cli-tools/antigravity-mitm/`.

| Metoda | Krajnja točka                           | Namjena                                                 |
| ------ | --------------------------------------- | ------------------------------------------------------- |
| GET    | `/api/cli-tools/antigravity-mitm`       | Status — radi li, pid, dnsConfigured, certExists        |
| POST   | `/api/cli-tools/antigravity-mitm`       | Pokretanje MITM-a (zahtijeva `apiKey` + `sudoPassword`) |
| DELETE | `/api/cli-tools/antigravity-mitm`       | Zaustavljanje MITM-a                                    |
| GET    | `/api/cli-tools/antigravity-mitm/alias` | Popis pseudonima modela                                 |
| PUT    | `/api/cli-tools/antigravity-mitm/alias` | Spremanje pseudonima modela za alat                     |

Ciljni presretnuti host: **`daily-cloudcode-pa.googleapis.com`** (Antigravityjev nadređeni poslužitelj).

### Slijed pokretanja (`src/mitm/manager.ts::startMitm`)

1. Generiranje samopotpisanog certifikata putem `selfsigned` (RSA-2048, SHA-256, 1 godina) — `cert/generate.ts`
2. Instaliranje certifikata u sistemsko spremište pouzdanih certifikata — `cert/install.ts`
3. Dodavanje zapisa `127.0.0.1 daily-cloudcode-pa.googleapis.com` u hosts datoteku — `dns/dnsConfig.ts`
4. Pokretanje procesa `src/mitm/server.cjs` uz `ROUTER_API_KEY` + `MITM_LOCAL_PORT` (zadano `443`)
5. Trajno spremanje PID-a u `<DATA_DIR>/mitm/.mitm.pid`

### Dinamičko otkrivanje spremišta pouzdanih certifikata na Linuxu — `cert/install.ts`

`getLinuxCertConfig()` prolazi popis prioriteta i odabire prvi postojeći direktorij:

| Obitelj distribucija     | Direktorij                                  | Naredba za ažuriranje    |
| ------------------------ | ------------------------------------------- | ------------------------ |
| Debian / Ubuntu          | `/usr/local/share/ca-certificates`          | `update-ca-certificates` |
| Arch / CachyOS / Manjaro | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust`        |
| Fedora / RHEL / CentOS   | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust`        |
| openSUSE                 | `/etc/pki/trust/anchors`                    | `update-ca-certificates` |

Naziv datoteke certifikata: `omniroute-mitm.crt`. Podudaranje otiska putem `getCertFingerprint()` (SHA-1 DER-a).

Osim toga, `updateNssDatabases()` instalira certifikat u NSS baze podataka pojedinačnih korisnika kada je `certutil` dostupan: `~/.pki/nssdb`, `~/snap/chromium/.../nssdb`, svi Firefox profili (uključujući snap), pod nadimkom **`OmniRoute MITM Root CA`**.

### macOS / Windows

- **macOS:** `security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain`
- **Windows:** PowerShell s povišenim ovlastima → `certutil -addstore Root`

### Autentifikacija

Sve MITM krajnje točke zahtijevaju autentifikaciju za upravljanje (`requireCliToolsAuth`). Sudo lozinka predmemorira se u opsegu modula (nikada u `globalThis`) i briše se pri pozivu `stopMitm()`.

## Nadjačavanja User-Agenta — varijable okruženja (`.env.example`, odjeljak 12)

| Varijabla                | Zadana vrijednost                                               |
| ------------------------ | --------------------------------------------------------------- |
| `CLAUDE_USER_AGENT`      | `claude-cli/2.1.258 (external, cli)`                            |
| `CODEX_USER_AGENT`       | `codex-cli/0.149.0 (Windows 10.0.26200; x64)`                   |
| `GITHUB_USER_AGENT`      | `GitHubCopilotChat/0.54.0`                                      |
| `ANTIGRAVITY_USER_AGENT` | `antigravity/2.0.1 linux/arm64 google-api-nodejs-client/10.3.0` |
| `KIRO_USER_AGENT`        | `AWS-SDK-JS/3.0.0 kiro-ide/1.0.0`                               |
| `QODER_USER_AGENT`       | `Qoder-Cli`                                                     |
| `CURSOR_USER_AGENT`      | `Cursor/3.4`                                                    |

Upotrebljava ih `open-sse/executors/base.ts::buildHeaders()` putem dinamičkog dohvata. **Ažurirajte ih kada pružatelji objave nove verzije CLI-ja** — zastarjeli UA nizovi počinju se odbijati kao zastarjeli klijenti.

## Prekidači načina kompatibilnosti CLI-ja (`.env.example`, odjeljak 13)

| Varijabla                  | Učinak                           |
| -------------------------- | -------------------------------- |
| `CLI_COMPAT_CODEX=1`       | Codexov otisak                   |
| `CLI_COMPAT_CLAUDE=1`      | Otisak claude-cli-ja             |
| `CLI_COMPAT_GITHUB=1`      | Otisak GitHub Copilot Chata      |
| `CLI_COMPAT_ANTIGRAVITY=1` | Antigravityjev otisak            |
| `CLI_COMPAT_KIRO=1`        | Kiro                             |
| `CLI_COMPAT_CURSOR=1`      | Cursor                           |
| `CLI_COMPAT_KIMI_CODING=1` | Kimi Coding                      |
| `CLI_COMPAT_KILOCODE=1`    | KiloCode                         |
| `CLI_COMPAT_CLINE=1`       | Cline                            |
| `CLI_COMPAT_ALL=1`         | Omogućuje sve prethodno navedeno |

IP adresa pružatelja **uvijek se zadržava** — prekidač samo preoblikuje mrežni izgled zahtjeva i ne mijenja izlaznu IP adresu.

---

## Sanitizacija dolaznih zaglavlja

OmniRoute uklanja dolazna klijentska zaglavlja prije prosljeđivanja kako zahtjev koji stigne iz Cursora ne bi otkrio `User-Agent: Cursor/X.Y.Z` nadređenom Claudeovu poslužitelju. Popis zabranjenih stavki nalazi se u `src/shared/constants/upstreamHeaders.ts` i održava se usklađenim sa Zod shemama i jediničnim testovima.

---

## Ažuriranje otisaka kada ih pružatelj promijeni

1. Snimite službeni CLI promet pomoću `mitmproxy` (presretanje TLS-a + ispis)
2. Izdvojite JA3/JA4 i doslovni redoslijed zaglavlja
3. Ažurirajte odgovarajući unos `CLI_FINGERPRINTS[...]`
4. Ažurirajte zadanu vrijednost odgovarajućeg `*_USER_AGENT` u `.env.example`
5. Ako se promijenilo samo TLS rukovanje, ažurirajte odgovarajući omotač pružatelja ili opciju `browser:` biblioteke wreq-js
6. Pokrenite TLS testove specifične za pružatelja i ručnu provjeru na aktivnom pružatelju
7. Objavite kao zakrpu; dokumentirajte u `CHANGELOG.md`

---

## Testovi

- `open-sse/services/__tests__/claudeTlsClient.test.ts` — ponašanje zajedničkog TLS omotača
- `tests/unit/anthropic-cache-fingerprint.test.ts` — determinizam otiska
- `tests/unit/chatgpt-web-source-retirement.test.ts` — zajednički prikriveni izvor ChatGPT Weba ostaje odsutan, dok Codex Web ostaje prisutan

---

## Vidi također

- [RESILIENCE_GUIDE.md](../architecture/RESILIENCE_GUIDE.md) — što se događa kada prikrivena putanja dobije odgovor `403`
- [TROUBLESHOOTING.md](../guides/TROUBLESHOOTING.md)
- [ENVIRONMENT.md](../reference/ENVIRONMENT.md) — potpuna referenca varijabli okruženja
- [CLI-TOOLS.md](../reference/CLI-TOOLS.md) — prikaz MITM tijeka rada za operatere
