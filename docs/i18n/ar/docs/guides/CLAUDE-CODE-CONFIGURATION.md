# Claude Code CLI — Configuration with OmniRoute (العربية)

🌐 **Languages:** 🇺🇸 [English](../../../../guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇹 [am](../../../am/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇿 [az](../../../az/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇬 [bg](../../../bg/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇩 [bn](../../../bn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇿 [cs](../../../cs/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇰 [da](../../../da/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇩🇪 [de](../../../de/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇷 [el](../../../el/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇸 [es](../../../es/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇪🇪 [et](../../../et/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇷 [fa](../../../fa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇮 [fi](../../../fi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇫🇷 [fr](../../../fr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇪 [ga](../../../ga/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [gu](../../../gu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ha](../../../ha/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇱 [he](../../../he/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [hi](../../../hi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇷 [hr](../../../hr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇭🇺 [hu](../../../hu/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇦🇲 [hy](../../../hy/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇩 [id](../../../id/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [ig](../../../ig/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇹 [it](../../../it/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇯🇵 [ja](../../../ja/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇬🇪 [ka](../../../ka/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇭 [km](../../../km/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [kn](../../../kn/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇷 [ko](../../../ko/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇹 [lt](../../../lt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇻 [lv](../../../lv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ml](../../../ml/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [mr](../../../mr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇾 [ms](../../../ms/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇹 [mt](../../../mt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇲🇲 [my](../../../my/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇵 [ne](../../../ne/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇱 [nl](../../../nl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇴 [no](../../../no/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [or](../../../or/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [pa](../../../pa/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇭 [phi](../../../phi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇱 [pl](../../../pl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇹 [pt](../../../pt/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇴 [ro](../../../ro/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇺 [ru](../../../ru/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇱🇰 [si](../../../si/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇰 [sk](../../../sk/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇮 [sl](../../../sl/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇷🇸 [sr](../../../sr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇸🇪 [sv](../../../sv/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇰🇪 [sw](../../../sw/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [ta](../../../ta/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇮🇳 [te](../../../te/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇭 [th](../../../th/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇷 [tr](../../../tr/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇵🇰 [ur](../../../ur/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇺🇿 [uz](../../../uz/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇻🇳 [vi](../../../vi/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇳🇬 [yo](../../../yo/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/guides/CLAUDE-CODE-CONFIGURATION.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/guides/CLAUDE-CODE-CONFIGURATION.md)

---

وجّه واجهة سطر الأوامر **Claude Code** ‏(`claude`) إلى OmniRoute — محليًا أو على خادم VPS بعيد —
باستخدام ملفات تعريف مخصّصة لكل نموذج، على غرار إعداد Codex.

---

## البدء السريع

```bash
# تشغيل Claude Code مع OmniRoute محلي (يكتشف السياق النشط تلقائيًا)
omniroute launch

# مع OmniRoute بعيد (يصبح هذا تلقائيًا بعد `omniroute connect <host>`)
omniroute launch --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# إنشاء ملفات تعريف لكل نموذج، ثم تشغيل أحدها
omniroute setup-claude            # يكتب ~/.claude/profiles/<name>/settings.json
omniroute launch --profile glm52  # استخدام Claude Code للنموذج glm/glm-5.2 عبر OmniRoute
```

---

## كيفية اتصال Claude Code ببوابة

يتواصل Claude Code باستخدام **Anthropic Messages API**، ويُوجَّه إلى نقطة نهاية
مخصّصة عبر متغيرات البيئة (إذ لا يوفّر الخيار `--base-url`):

| المتغير                                      | الغرض                                                                                         |
| -------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `ANTHROPIC_BASE_URL`                         | عنوان URL الجذري للبوابة (يُلحق Claude Code المسار `/v1/messages`). **من دون اللاحقة `/v1`.** |
| `ANTHROPIC_AUTH_TOKEN`                       | يُرسل بصيغة `Authorization: Bearer …` — استخدم رمز وصول OmniRoute / مفتاح API الخاص بك        |
| `ANTHROPIC_API_KEY`                          | بديل: يُرسل بصيغة `x-api-key`. إذا ضُبط كلاهما، تكون الأولوية لـ `ANTHROPIC_AUTH_TOKEN`       |
| `ANTHROPIC_MODEL`                            | فرض استخدام نموذج محدد (يتجاوز القيمة الافتراضية لمنتقي `/model`)                             |
| `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` | تؤدي القيمة `1` إلى عرض منتقي `/model` الأصلي لنماذج `claude*`/`anthropic*` من `/v1/models`   |
| `CLAUDE_CODE_MAX_OUTPUT_TOKENS`              | تحديد الحد الأقصى لرموز الإخراج لكل استجابة (مثل `65536`)                                     |
| `CLAUDE_CODE_AUTO_COMPACT_WINDOW`            | حدّ الرموز للضغط التلقائي                                                                     |

> تُقرأ متغيرات البيئة **مرة واحدة عند بدء التشغيل** — أعد تشغيل Claude Code بعد تغييرها.

يضبط `omniroute launch` جميع هذه المتغيرات نيابةً عنك: فهو يستخرج عنوان URL الأساسي والرمز
من السياق النشط (وبذلك يعمل `omniroute connect <vps>` ثم `omniroute launch`
مباشرةً)، ويتحقق من سلامة الخادم، ثم يشغّل `claude` عبر `exec`.

---

## أسماء الاكتشاف البديلة — إظهار النماذج غير التابعة لـ Claude في منتقي `/model`

لا يعرض اكتشاف نماذج البوابة في Claude Code سوى المعرّفات التي تبدأ بـ `claude`
أو `anthropic`، ولذلك عند استخدام `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`، يعرض
منتقي `/model` الأصلي عادةً **فقط** نماذج Claude/Anthropic في OmniRoute — ولن يظهر
`kimi/kimi-k2.6` أو `glm/glm-5.2` مطلقًا، رغم أن توجيههما يعمل بصورة صحيحة.

يمكن لـ OmniRoute إنشاء نسخة بديلة من أي نموذج مفعّل (وأي تركيبة) تحت معرّف `claude/…` كي
يجتاز ذلك المرشح ويظهر في المنتقي:

```
kimi/kimi-k2.6            →  claude/kimi/kimi-k2.6      "Kimi K2.6 (OmniRoute)"
glm/glm-5.2              →  claude/glm/glm-5.2         "GLM 5.2 (OmniRoute)"
<التركيبة "custo-otimizado"> →  claude/combo/custo-otimizado
```

عند اختيار أحد هذه المعرّفات في Claude Code، يزيل OmniRoute الغلاف `claude/`
ويعيده إلى المعرّف الحقيقي قبل التوجيه — بينما يبقى المعرّف الأصلي `claude/<real-claude-model>`
(موفّر Claude OAuth الفعلي) دون أي تغيير دائمًا.

**هذه الميزة معطّلة افتراضيًا** وتتحكم بها بوابة ذات ثلاثة مستويات (الأكثر
تحديدًا له الأولوية)، ولذلك لا يضاعف OmniRoute العادي فهرسه للعملاء الذين لا يستخدمون
Claude Code:

| المستوى | المكان                                                                     |
| ------- | -------------------------------------------------------------------------- |
| النموذج | صفحة تفاصيل الموفّر ← مفتاح "Expose in Claude Code" لكل نموذج              |
| الموفّر | صفحة تفاصيل الموفّر ← مفتاح على مستوى الموفّر (يشمل جميع نماذجه)           |
| العام   | Settings ← Feature Flags ← `EXPOSE_CC_DISCOVERY_ALIASES` (معطّل افتراضيًا) |

يفرض متغير البيئة `EXPOSE_CC_DISCOVERY_ALIASES` تفعيل المستوى العام،
وتكون له الأولوية على التجاوز المحدد في لوحة المعلومات (تعرض شاشة Feature Flags
ملاحظة "active via environment variable" عندما يحدث ذلك). ثم تضبط مفاتيح كل موفّر
وكل نموذج النتيجة بمزيد من الدقة — فعلى سبيل المثال، يؤدي تعطيل المستوى العام مع تفعيل موفّر Kimi
إلى إظهار نماذج Kimi فقط.

> ⚠️ **عدم تطابق النافذة في النماذج غير التابعة لـ Claude.** يفترض Claude Code نافذة سياق
> بحجم 200K لأي معرّف لا يتعرّف عليه (إذ لا يمكنه قراءة حجم النافذة الحقيقي من
> `/v1/models`). بالنسبة إلى نموذج ذي نافذة أكبر (مثل نافذة Kimi K2 بحجم 256K)، اضبط
> `CLAUDE_CODE_AUTO_COMPACT_WINDOW` على قيمة أقل من حجم النافذة الحقيقي للنموذج حتى
> لا يبدأ الضغط التلقائي قبل أوانه. ملفات التعريف المُنشأة أعلاه تضبط ذلك بالفعل
> لكل نموذج.

---

## كتلة الإعداد الأولي في لوحة المعلومات

تعرض بطاقة أداة Claude (**لوحة المعلومات → CLI Code**) جزء `settings.json` الدقيق
لهذا المثيل، بجوار زر معلومات الاسم المستعار للاكتشاف، مع زر للنسخ:

```jsonc
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://<OmniRoute الخاص بك>:20128",
    "ANTHROPIC_AUTH_TOKEN": "<مفتاح OmniRoute API الخاص بك>",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
  },
}
```

عنوان URL الأساسي هو العنوان الذي تحدده البطاقة (بما في ذلك أي تجاوز مخصص أدخلته)، وقد جرى
تطبيعه بالفعل — دون اللاحقة `/v1` ودون شرطة مائلة ختامية. **لا يُعرض المفتاح مطلقًا**: تتضمن الكتلة
عنصرًا نائبًا، لذا لا يمكن للقطة شاشة أو لمقتطف ملصق أن يسرّبه. الصق مفتاحك مكانه.

أضف `CLAUDE_CODE_AUTO_COMPACT_WINDOW` ضمن كتلة `env` نفسها لأي نموذج لا تبلغ نافذة
سياقه الفعلية 200K — يفترض Claude Code قيمة 200K لكل معرّف لا يتعرف عليه، ولذلك
يعمل الضغط التلقائي عند نقطة خاطئة بخلاف ذلك (راجع التحذير في القسم السابق).
تقبل أداة إنشاء المقتطفات هذه القيمة أيضًا، لذا يمكن للمستدعي الذي يعرف نافذة النموذج المستهدف
إصدارها مباشرةً.

المصدر: `src/shared/services/claudeCliConfig.ts::buildClaudeDiscoverySettingsSnippet` (أداة إنشاء
بحتة وخاضعة لاختبارات الوحدة)، ويعرضها `ClaudeGatewayOnboardingBlock`.

---

## الملفات التعريفية (`CLAUDE_CONFIG_DIR`)

لا يحتوي Claude Code على **ملفات تعريفية أصلية** (بخلاف `~/.codex/<name>.config.toml` في Codex).
الآلية المتعارف عليها هي `CLAUDE_CONFIG_DIR` — دليل إعدادات منفصل لكل
ملف تعريفي، ولكل منها ملف `settings.json` وبيانات اعتماد وسجل وذاكرة تخزين مؤقت خاصة به.

يجلب `omniroute setup-claude` الكتالوج المباشر من `/v1/models` ويكتب ملفًا
تعريفيًا واحدًا لكل نموذج في `~/.claude/profiles/<name>/settings.json`، مع إعادة استخدام
**الأسماء نفسها التي يستخدمها `setup-codex`** (`glm52` و`kimi-k27` و`deepseek-pro`، …):

```jsonc
// ~/.claude/profiles/glm52/settings.json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "model": "glm/glm-5.2",
  "effortLevel": "xhigh",
  "env": {
    "ANTHROPIC_BASE_URL": "http://192.168.0.15:20128",
    "ANTHROPIC_MODEL": "glm/glm-5.2",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "190000",
  },
}
```

> **لا يُكتب رمز المصادقة المميز في الملف التعريفي مطلقًا.** شغّله باستخدام
> `omniroute launch --profile <name>` (إذ يحقن `ANTHROPIC_AUTH_TOKEN` من
> السياق النشط)، أو صدّر `ANTHROPIC_AUTH_TOKEN` بنفسك ثم شغّل
> `CLAUDE_CONFIG_DIR=~/.claude/profiles/<name> claude`.

**المزامنة التلقائية بعد اكتشاف النماذج (اختيارية).** يمكن لـ OmniRoute إعادة إنشاء ملفات
`~/.claude/profiles/<name>/settings.json` نفسها تلقائيًا كلما غيّرت مزامنة نماذج أحد المزوّدين
الكتالوج المباشر — بحيث تحصل النماذج الجديدة أو المُعاد تسميتها على ملفات تعريفية دون إعادة تشغيل
الأمر. تكون هذه الميزة **معطّلة افتراضيًا**: فعّلها من **لوحة معلومات CLI Code** ("المزامنة التلقائية
لملفات CLI التعريفية" → Claude Code)، أو عيّن `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES=true` (كما أنها تحترم
`CLI_ALLOW_CONFIG_WRITES`، وهو مفعّل افتراضيًا). عند تمكينها، لا تكتب سوى ملفات التعريف؛ ولا تغيّر مطلقًا
إعدادات Claude النشطة أو الافتراضية، أو المصادقة، أو `~/.claude/settings.json`.

### إنشاء الملفات التعريفية واستخدامها

```bash
# OmniRoute محلي
omniroute setup-claude

# خادم VPS بعيد (يضمّن عنوان URL الخاص بخادم VPS في كل ملف تعريفي)
omniroute setup-claude --remote http://192.168.0.15:20128 --api-key oma_live_xxx

# بعض المزوّدين فقط
omniroute setup-claude --only glm,kimi

# معاينة دون كتابة
omniroute setup-claude --dry-run

# تشغيل ملف تعريفي
omniroute launch --profile kimi-k27
```

---

## مستويات النماذج (اختياري)

يوجّه Claude Code الطلبات إلى مستويات قدرات مختلفة. اربط كل مستوى بنموذج OmniRoute عبر متغيرات البيئة /
الإعدادات إذا كنت تريد استخدام مزوّدين مختلفين لكل مستوى:

```bash
export ANTHROPIC_DEFAULT_OPUS_MODEL="glm/glm-5.2"
export ANTHROPIC_DEFAULT_SONNET_MODEL="kmc/kimi-k2.6"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="glm/glm-4.7-flash"
```

وإلا فسيُستخدم متغير `ANTHROPIC_MODEL` واحد (وهو ما تضبطه ملفات التعريف) لكل شيء.

---

## الوضع البعيد

بمجرد تشغيل `omniroute connect <host>` (راجع
[الوضع البعيد](./REMOTE-MODE.md))، يستهدف الأمران `omniroute launch` و`omniroute setup-claude`
ذلك الخادم البعيد تلقائيًا ويستخدمان رمز الوصول محدد النطاق الخاص به — من دون الحاجة إلى
خيارات إضافية. يمكنك تجاوز ذلك لكل استدعاء باستخدام `--remote` / `--api-key`.

---

## استكشاف الأخطاء وإصلاحها

**يتجاهل Claude Code البوابة** — تأكد من أن `ANTHROPIC_BASE_URL` **لا يحتوي على
`/v1`**، ثم أعد تشغيل `claude` (تُقرأ متغيرات البيئة مرة واحدة عند بدء التشغيل). يتولى `omniroute launch`
ذلك نيابةً عنك.

**قائمة اختيار `/model` فارغة / نماذج البوابة مفقودة** — يتطلب ذلك Claude Code
v2.1.219+ وضبط `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY=1`. لا تظهر في قائمة الاختيار سوى معرّفات النماذج التي تبدأ بـ `claude*` /
`anthropic*`؛ لفرض استخدام أي نموذج آخر، استخدم
`ANTHROPIC_MODEL=<id>` (وهذا ما تفعله ملفات التعريف).

**`400 Ambiguous model 'claude-…'`** — يرسل Claude Code دائمًا معرّفات نماذج **من دون بادئة**
(مثل `claude-opus-4-8`)، ولذلك عندما يكون كل من مزوّد Claude Code (`cc/…`) ومزوّد
Claude (`claude/…`) متصلين، يطابق المعرّف غير المسبوق ببادئة مسارين، ويرفض
OmniRoute التخمين. أصلح ذلك بإحدى الطريقتين: ثبّت معرّفًا مسبوقًا ببادئة باستخدام
`ANTHROPIC_MODEL=cc/claude-opus-4-8`، أو فعّل **تفضيل Claude Code لنماذج Claude
غير المسبوقة ببادئة** — من مفتاح التبديل في صفحة مزوّد Claude، أو باستخدام
`OMNIROUTE_PREFER_CLAUDE_CODE_FOR_UNPREFIXED_CLAUDE_MODELS=true` (معطّل افتراضيًا؛
راجع [البيئة](../reference/ENVIRONMENT.md)) — ما يوجّه معرّفات `claude-*`
غير المسبوقة ببادئة إلى Claude Code بدلًا من ذلك. تكون الأولوية دائمًا لبادئات المزوّد الصريحة.

**أخطاء المصادقة** — لا يحتوي ملف التعريف على أي رمز. استخدم `omniroute launch --profile`
(لحقنه) أو صدّر `ANTHROPIC_AUTH_TOKEN`.

**ملفات التعريف غير معزولة** — لكل ملف تعريف `CLAUDE_CONFIG_DIR` مستقل؛
تحقق من أن `echo $CLAUDE_CONFIG_DIR` داخل الجلسة يشير إلى
`~/.claude/profiles/<name>`.
