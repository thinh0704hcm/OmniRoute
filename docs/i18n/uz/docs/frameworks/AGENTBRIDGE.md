# AgentBridge (Oʻzbekcha)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/AGENTBRIDGE.md) · 🇪🇹 [am](../../../am/docs/frameworks/AGENTBRIDGE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/AGENTBRIDGE.md) · 🇦🇿 [az](../../../az/docs/frameworks/AGENTBRIDGE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/AGENTBRIDGE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/AGENTBRIDGE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/AGENTBRIDGE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/AGENTBRIDGE.md) · 🇩🇰 [da](../../../da/docs/frameworks/AGENTBRIDGE.md) · 🇩🇪 [de](../../../de/docs/frameworks/AGENTBRIDGE.md) · 🇬🇷 [el](../../../el/docs/frameworks/AGENTBRIDGE.md) · 🇪🇸 [es](../../../es/docs/frameworks/AGENTBRIDGE.md) · 🇪🇪 [et](../../../et/docs/frameworks/AGENTBRIDGE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/AGENTBRIDGE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/AGENTBRIDGE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/AGENTBRIDGE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/AGENTBRIDGE.md) · 🇮🇱 [he](../../../he/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/AGENTBRIDGE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/AGENTBRIDGE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/AGENTBRIDGE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/AGENTBRIDGE.md) · 🇮🇩 [id](../../../id/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/AGENTBRIDGE.md) · 🇮🇹 [it](../../../it/docs/frameworks/AGENTBRIDGE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/AGENTBRIDGE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/AGENTBRIDGE.md) · 🇰🇭 [km](../../../km/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/AGENTBRIDGE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/AGENTBRIDGE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/AGENTBRIDGE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/AGENTBRIDGE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/AGENTBRIDGE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/AGENTBRIDGE.md) · 🇲🇲 [my](../../../my/docs/frameworks/AGENTBRIDGE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/AGENTBRIDGE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/AGENTBRIDGE.md) · 🇳🇴 [no](../../../no/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [or](../../../or/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/AGENTBRIDGE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/AGENTBRIDGE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/AGENTBRIDGE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/AGENTBRIDGE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/AGENTBRIDGE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/AGENTBRIDGE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/AGENTBRIDGE.md) · 🇱🇰 [si](../../../si/docs/frameworks/AGENTBRIDGE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/AGENTBRIDGE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/AGENTBRIDGE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/AGENTBRIDGE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/AGENTBRIDGE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/AGENTBRIDGE.md) · 🇮🇳 [te](../../../te/docs/frameworks/AGENTBRIDGE.md) · 🇹🇭 [th](../../../th/docs/frameworks/AGENTBRIDGE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/AGENTBRIDGE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/AGENTBRIDGE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/AGENTBRIDGE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/AGENTBRIDGE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/AGENTBRIDGE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/AGENTBRIDGE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/AGENTBRIDGE.md)

---

AgentBridge — OmniRoute’ning MITM (Man-in-the-Middle) proksisi bo‘lib, IDE AI agentlarining HTTPS trafigini ushlab qoladi va uni OmniRoute’ning yagona marshrutlash mexanizmi orqali qayta yo‘naltiradi. U **10 ta IDE agentini** — Antigravity, Kiro, GitHub Copilot, GHE Copilot, OpenAI Codex, Cursor, Zed, Claude Code, Open Code va Trae (o‘rganilmoqda) — qo‘llab-quvvatlaydi, bu esa OmniRoute’ni bozordagi AI dasturlash yordamchilari uchun eng keng qamrovli MITM proksisiga aylantiradi.

**Boshqaruv panelidagi joylashuvi:** `/dashboard/tools/agent-bridge`
**Yon panel guruhi:** Vositalar (Cloud Agents’dan keyin)
**Shuningdek qarang:** [`TRAFFIC_INSPECTOR.md`](./TRAFFIC_INSPECTOR.md) — barcha ushlangan trafikni real vaqt rejimida kuzatish; `docs/security/MITM-TPROXY-DECRYPT.md` (git; `/docs` tarkibiga kompilyatsiya qilinmaydi) — `/api/tools/agent-bridge/tproxy` marshruti orqali boshqariladigan Linux TPROXY shaffof deshifrlash va yozib olish rejimi.

---

## §1 Umumiy ma’lumot

### AgentBridge nima?

IDE agenti (masalan, GitHub Copilot, Cursor, Claude Code) API chaqiruvini amalga oshirganda, u to‘g‘ridan-to‘g‘ri yuqori oqimdagi AI provayderiga (OpenAI, Anthropic va boshqalar) ulanadi. AgentBridge ushbu ulanishni TLS darajasida shaffof tarzda — agent konfiguratsiyasini o‘zgartirishni talab qilmasdan — ushlab qoladi va so‘rovni OmniRoute orqali qayta yo‘naltiradi.

Bu sizga quyidagilarni amalga oshirish imkonini beradi:

- **Istalgan agentni istalgan provayderga qayta yo‘naltirish**: Copilot OpenAI bilan ishlayaptimi? Uni Anthropic Claude, Gemini yoki OmniRoute’dagi 352 ta provayderdan istalgan biriga yo‘naltiring.
- **Model mosliklarini qo‘llash**: `gemini-3-flash` → `claude-sonnet-4.7` mosligini ishlov beruvchi darajasida shaffof tarzda qo‘llang.
- **Barcha agent trafigini kuzatish**: har bir ushlangan so‘rov [Traffic Inspector](./TRAFFIC_INSPECTOR.md) xizmatiga yuboriladi.
- **OmniRoute barqarorlik imkoniyatlarini qo‘llash**: kombinatsiyalangan marshrutlash, avtomatik uzgichlar, zaxira yo‘nalishlar va xarajatlarni kuzatish IDE agentlari trafigi uchun ham ishlaydi.

### Bozordagi muqobillarga nisbatan o‘rni

| Xususiyat                   | 9router | anti-api | llm-interceptor | **OmniRoute AgentBridge** |
| --------------------------- | :-----: | :------: | :-------------: | :-----------------------: |
| Antigravity                 |    ✓    |    ✓     |        —        |             ✓             |
| GitHub Copilot              |    ✓    |    ✓     |        —        |             ✓             |
| Kiro (AWS)                  |    ✓    |    ✓     |        —        |             ✓             |
| OpenAI Codex                |    —    |    ✓     |        —        |             ✓             |
| Cursor IDE                  |    ✓    |    ✓     |        —        |             ✓             |
| Zed Industries              |    —    |    ✓     |        —        |             ✓             |
| Claude Code                 |    —    |    —     |        ✓        |             ✓             |
| Open Code                   |    —    |    —     |        ✓        |             ✓             |
| Trae                        |    —    |    —     |        —        |     🔍 O‘rganilmoqda      |
| Boshqaruv paneli interfeysi |    ✓    |    ✗     |        ✗        |             ✓             |
| Trafik inspektori           |    ✗    |    ✗     |        ✓        |             ✓             |
| OmniRoute marshrutlashi     |    ✗    |    ✗     |        ✗        |             ✓             |
| Model mosliklari interfeysi |    ✗    |    ✗     |        ✗        |             ✓             |
| Chetlab o‘tish ro‘yxati     |    ✗    |    ✗     |        ✓        |             ✓             |
| Yuqori oqim CA sertifikati  |    ✗    |    ✗     |        ✓        |             ✓             |

---

## §2 Arxitektura

### 2.1 Komponentlar haqida umumiy maʼlumot

```
IDE agenti (VS Code / Cursor / va boshqalar)
    │  HTTPS (443-port)
    ▼
/etc/hosts — 127.0.0.1 api.githubcopilot.com   ← DNS qayta yoʻnaltirishi
    │
    ▼
src/mitm/server.cjs  (443-port, CJS yordamchi jarayoni)
    │  nishonni Host sarlavhasidagi SNI orqali aniqlaydi
    │  AgentBridge CA tomonidan imzolangan har bir SNI uchun TLS sertifikatini yaratadi
    ├── Chetlab oʻtish roʻyxatiga mosmi? → TCP orqali bevosita oʻtkazish (shifrdan yechilmaydi)
    ├── Nishonga mosmi? → fetch → OmniRoute marshrutizatori (20128-port)
    │       └── handler.intercept() — TypeScript
    │               ├── soʻrov tanasi/sarlavhalarida maskSecrets()
    │               ├── TrafficBuffer.push() — Traffic Inspector xizmatiga yuboradi
    │               └── fetchRouter() → /v1/chat/completions
    └── Moslik yoʻqmi? → TCP orqali bevosita oʻtkazish (shifrdan yechilmaydi)
```

### 2.2 MITM serveri (`src/mitm/server.cjs`)

Asosiy MITM serveri Node.js CJS yordamchi jarayoni sifatida ishlaydi (mavjud CJS kod bazasini qayta yozmaslik uchun). U:

- 443-portni tinglaydi (imtiyoz yoki `authbind`/`setcap` talab qilinadi)
- Operatsion tizimdan CONNECT tunnellarini qabul qiladi (`/etc/hosts` DNS qayta yoʻnaltirishi orqali)
- AgentBridge CA (`DATA_DIR/mitm/ca.crt`) tomonidan imzolangan har bir SNI uchun TLS sertifikatlarini yaratadi
- `targets/index.ts` reyestri orqali Host sarlavhasi asosida nishon agentini aniqlaydi
- HTTP orqali TypeScript ishlov beruvchi qatlamiga `http://127.0.0.1:20128` manzilida uzatadi

`TARGET_HOSTS` `DATA_DIR/mitm/targets.json` faylidan yuklanadi (ishga tushirish vaqtida `targets/index.ts` tomonidan yoziladi), bu CJS serverini qayta ishga tushirmasdan dinamik yangilash imkonini beradi.

> **Root-CA modeli (#6684).** Yuqoridagi CA tomonidan imzolangan har bir SNI uchun sertifikat tavsifi
> #6684 da qoʻshilgan doimiy root-CA modelidir (`src/mitm/cert/rootCa.ts` +
> `src/mitm/_internal/rootCaShim.cjs`, bunda `src/mitm/tproxy/dynamicCert.ts` faylida
> TPROXY uchun allaqachon sinalgan CA/barg kriptografiyasi qayta ishlatiladi) — u diskdagi oddiy
> `server.crt`/`server.key` juftligi mavjudligini bildiruvchi, faqat antigravity hostlari doirasida
> qoladigan eski yagona statik, oʻzi imzolagan barg sertifikatini
> (`src/mitm/cert/generate.ts`) almashtiradi. **Migratsiya xatti-harakati**: yangi oʻrnatish
> (avvalgi `server.crt` mavjud boʻlmasa) root-CA modelini avtomatik ravishda oladi; eski statik barg
> sertifikatiga allaqachon ishongan oʻrnatish operator `MITM_ROOT_CA_ENABLED=true` qiymatini
> oʻrnatib, koʻprikni qayta ishga tushirmaguncha undan foydalanishda davom etadi
> (`src/mitm/cert/migration.ts` — sof qaror qabul qilish funksiyasi; **istalgan** host uchun
> barg sertifikatini imzolay oladigan ishonchli MITM CA eski, qatʼiy SAN barg sertifikatidan
> ancha kuchliroq, shuning uchun allaqachon ishonch bildirilgan oʻrnatishda oʻtish hech qachon
> yashirin amalga oshirilmaydi). CA sertifikati eski barg sertifikati foydalangan ayni
> `omniroute-mitm.crt` ishonch ombori uyasiga oʻrnatiladi
> (`cert/install.ts::installCaCert`) — ikkita ishonch yozuvini tozalash talab qilinmaydi.

### 2.3 Ishlov beruvchi asosi (`src/mitm/handlers/base.ts`)

Barcha agent ishlov beruvchilari `MitmHandlerBase` klassini kengaytiradi:

```ts
export abstract class MitmHandlerBase {
  abstract readonly agentId: AgentId;

  abstract intercept(
    req: IncomingMessage,
    res: ServerResponse,
    body: Buffer,
    mappedModel: string
  ): Promise<void>;

  // Himoyalangan yordamchilar: fetchRouter, pipeSSE, hookBufferStart, hookBufferUpdate
}
```

Har bir ishlov beruvchi proksilashdan oldin `hookBufferStart()` funksiyasini va jarayon tugagach `hookBufferUpdate()` funksiyasini chaqiradi. Ular `InterceptedRequest` yozuvlarini `globalTrafficBuffer` buferiga yuboradi ([Traffic Inspector](./TRAFFIC_INSPECTOR.md) §4 bo‘limiga qarang).

### 2.4 Nishonlar reyestri (`src/mitm/targets/`)

Har bir agent deklarativ nishon fayliga ega:

```ts
// src/mitm/targets/copilot.ts
export const COPILOT_TARGET: MitmTarget = {
  id: "copilot",
  name: "GitHub Copilot",
  hosts: ["api.githubcopilot.com", "copilot-proxy.githubusercontent.com"],
  port: 443,
  endpointPatterns: ["/chat/completions", "/v1/chat/completions"],
  defaultModels: [{ id: "gpt-4o", name: "GPT-4o", alias: "gpt-4o" }],
  handler: () => import("../handlers/copilot"),
  riskNoticeKey: "providers.riskNotice.oauth",
};
```

Reyestr (`targets/index.ts`) `ALL_TARGETS` ni eksport qiladi va ishga tushirish vaqtida `DATA_DIR/mitm/targets.json` faylini yaratadi.

### 2.5 Bevosita oʻtkazish va chetlab oʻtish roʻyxati (`src/mitm/passthrough.ts`)

**Chetlab oʻtish roʻyxati** (birinchi boʻlib tekshiriladi va nishonga moslikdan ustun turadi):

- Standart andozalar: bank hostlari, `.gov.`, OAuth/SSO provayderlari (Okta, Auth0) va boshqalar
- Foydalanuvchi andozalari: `agent_bridge_bypass` maʼlumotlar bazasi jadvalida saqlanadi
- Chetlab oʻtiladigan hostlar shaffof TCP tunnelini oladi — TLS **hech qachon shifrdan yechilmaydi**

**Standart bevosita oʻtkazish** (nishonga moslik yoʻq va chetlab oʻtish roʻyxatida emas):

- U ham TCP tunnelini oladi — ulanishlar hech qachon uzilmaydi
- AgentBridge umumiy tizim HTTPS trafigiga xalaqit berishining oldini oladi

Marshrutlash ustuvorligi:

```
chetlab oʻtish roʻyxati → nishonga moslik → bevosita oʻtkazish
```

### 2.6 Yuqori oqim CA sertifikati (`src/mitm/upstreamTrust.ts`)

Maxsus CA ishlatiladigan korporativ tarmoq muhitlari uchun:

```bash
AGENTBRIDGE_UPSTREAM_CA_CERT=/path/to/corporate-ca.pem
```

Bu qiymat oʻrnatilganda, `undici` global dispetcheri qoʻshimcha CA sertifikati bilan sozlanadi va AgentBridge korporativ TLS terminatsiya proksilari orqali yuqori oqim provayderlariga ulana oladi.

### 2.7 Maxfiy maʼlumotlarni niqoblash (`src/mitm/maskSecrets.ts`)

Mustaqil, toza muhitda yaratilgan skaner soʻrov tanalari va hisobga olish maʼlumotlari sarlavhalariga
ular Traffic Inspector buferiga yoki biror jurnalga kirishidan **oldin** qoʻllanadi. U bitta chiziqli oʻtishni amalga oshiradi:

- `sk-` / `ak-` / `pk-` prefiksli tokenlar (OpenAI/Anthropic uslubida)
- RFC 6750 `Authorization: Bearer <token>` hisobga olish maʼlumotlari, bunda butun token ustuvorlikka ega
- Umumiy uzun shaffof boʻlmagan tokenlar (≥40 ta belgi), jumladan nuqtali va toʻldirilgan shakllar

`sanitizeHeaders()` saqlab qolinadigan nomlarni kichik harflarga oʻtkazadi, massiv qiymatlarini deterministik tarzda birlashtiradi, umumiy hop-by-hop/freyming taqiqlash roʻyxatidagi sarlavhalarni (jumladan proksi autentifikatsiyasini) olib tashlaydi, `cookie` va `set-cookie` qiymatlarini toʻliq yashiradi hamda hisobga olish maʼlumotlari qiymatlarini skanerga topshiradi.

---

## §3 Sozlash

### 3.1 MITM serverini ishga tushirish/toʻxtatish

`/dashboard/tools/agent-bridge` manzilidagi AgentBridge Server kartasidan foydalaning:

| Amal                           | Tavsif                                                                                                         |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| Serverni ishga tushirish       | 443-portda `src/mitm/server.cjs` jarayonini ishga tushiradi                                                    |
| Serverni toʻxtatish            | Quyi jarayonni xavfsiz tarzda toʻxtatadi                                                                       |
| Serverni qayta ishga tushirish | Toʻxtatish + ishga tushirish (maqsad oʻzgarishlarini qoʻllaydi)                                                |
| Sertifikatga ishonish          | `DATA_DIR/mitm/ca.crt` faylini OT ishonchli sertifikatlar omboriga oʻrnatadi                                   |
| Sertifikatni yuklab olish      | Qoʻlda oʻrnatish uchun `ca.crt` faylini yuklab oladi                                                           |
| Sertifikatni qayta yaratish    | Yangi CA kalitlar juftligini yaratadi (har bir agent uchun mavjud barcha sertifikatlar yaroqsiz boʻlib qoladi) |

### 3.2 Sertifikatga ishonish

IDElar MITM ulanishini qabul qilishi uchun AgentBridge CA sertifikati operatsion tizim tomonidan ishonchli deb tan olinishi kerak.

**Linux (NSS — Chrome/Firefox):**

```bash
certutil -A -d sql:$HOME/.pki/nssdb -n "OmniRoute AgentBridge" -t CT,, -i ~/.omniroute/mitm/ca.crt
```

**macOS (Keychain):**

```bash
sudo security add-trusted-cert -d -r trustRoot \
  -k /Library/Keychains/System.keychain ~/.omniroute/mitm/ca.crt
```

**Windows (certmgr):**

```powershell
certutil -addstore -f Root $env:USERPROFILE\.omniroute\mitm\ca.crt
```

Yoki boshqaruv panelidagi "Sertifikatga ishonish" tugmasidan foydalaning (u operatsion tizimingizga mos buyruqni bajaradi va zarur boʻlsa, sudo soʻrovini koʻrsatadi).

#### Electron asosidagi IDElar OT ishonchli sertifikatlar omborini eʼtiborsiz qoldiradi (`NODE_EXTRA_CA_CERTS`)

Baʼzi IDElar — xususan, **Antigravity IDE** hamda Electron / VS Code asosidagi boshqa ilovalar —
chiquvchi `fetch`/HTTPS soʻrovlari uchun **OT ishonchli sertifikatlar omboridan foydalanmaydigan**
oʻz Node.js ish muhitini oʻz ichiga oladi. CA sertifikatiga OT/NSS darajasida ishonish IDEning
mahalliy **backend** qismi (masalan, OT CA toʻplamidan foydalanadigan Go til serveri) uchun yetarli,
ammo **Electron frontend** qismida TLS baribir ishlamaydi — MITM jurnalida backendning boshlangʻich
soʻrovlari `200` qaytarayotgani koʻrinsa ham, bu ilovada tizimdan _chiqib ketilgan_ holat yoki
_"ulanish xatosi"_ sifatida namoyon boʻladi. Ikki qadam talab qilinadi va ikkalasi ham muhim:

1. Ish muhitiga CA manzilini bevosita koʻrsating:
   ```bash
   export NODE_EXTRA_CA_CERTS=/path/to/omniroute-agentbridge-ca.crt
   ```
2. **IDEni shu shell ichidan ishga tushiring.** Uni ish stoli belgisi / Dock / Start menyusi orqali
   ishga tushirish shell eksportlarini **meros qilib olmaydi**, `~/.config/environment.d/*.conf` esa
   faqat grafik tizimga qaytadan kirgandan keyin qoʻllanadi. Avval IDEni toʻliq yoping — Electronning
   yagona nusxa qulfi sababli ikkinchi marta ishga tushirish faqat mavjud jarayonni fokuslaydi va yangi
   muhit eʼtiborsiz qoldiriladi.

Yuqoridagi OT ishonchi + NSS qadami hamon zarur (ayrim autentifikatsiya jarayonlarida ishlatiladigan
Chromium tarmoq steki har bir foydalanuvchining NSS omborini oʻqiydi va `*.googleapis.com` uchun
mahalliy ishonchli CA bekor qiladigan oʻz statik pinlariga ega). `NODE_EXTRA_CA_CERTS` bunga qoʻshimcha
ravishda Node `fetch` yoʻlini qamrab oladi.

### 3.3 DNS yoʻnaltirish

Tutib qolmoqchi boʻlgan har bir agent uchun uning API hosti yoki hostlari `127.0.0.1` manziliga mos kelishi kerak. Sozlash ustasida agent uchun DNSni yoqqaningizda, AgentBridge `/etc/hosts` yozuvlarini avtomatik ravishda boshqaradi.

GitHub Copilot uchun `/etc/hosts` yozuvlariga misol:

```
127.0.0.1 api.githubcopilot.com
127.0.0.1 copilot-proxy.githubusercontent.com
```

### 3.4 Modelni moslashtirish

Manba → maqsad mosliklarini belgilash uchun har bir agent kartasidagi Model Mapping jadvalidan foydalaning:

| Manba modeli (agentga xos) | Maqsad modeli (OmniRoute) |
| -------------------------- | ------------------------- |
| `gpt-4o`                   | `claude-sonnet-4.7`       |
| `*` (universal belgi)      | `claude-haiku-4.7`        |

`*` universal belgisi har qanday tanilmagan modelni koʻrsatilgan maqsadga moslaydi. Maʼlumotlar `agent_bridge_mappings` jadvalida saqlanadi.

> **Maslahat — agentning haqiqiy model IDlarini aniqlang.** IDE oʻz interfeysidagi yorliqlardan farq
> qiladigan va asosiy versiyalar orasida oʻzgaradigan model nomlarini yuborishi mumkin. Masalan,
> **Antigravity 2** tarmoq orqali eski hujjatlarda koʻrsatilgan `gemini-2.5-pro` emas, balki
> `gemini-3.1-pro-low`, `gemini-pro-agent` va `gemini-3.1-flash-lite` nomlarini yuboradi. Mos keluvchi
> mapping mavjud boʻlmagan holda bitta chat yuboring: MITM aniq kiruvchi `model:` qiymatini jurnalga
> yozadi va soʻrovni oʻzgartirmasdan oʻtkazib yuboradi. Aynan shu qiymatni moslang, shunda keyingi
> soʻrov tutib qolinib, maqsadingizga yoʻnaltiriladi.

### 3.5 Xavf haqida ogohlantirish

AgentBridge IDE yuqori oqim provayderlarida autentifikatsiyadan oʻtish uchun ishlatadigan hisob maʼlumotlarini (OAuth tokenlari, API kalitlari) tutib qoladi. Ular **jurnalga yozilishidan oldin niqoblanadi** (§2.7 ga qarang), ammo OmniRoute MITM qatlamiga koʻrinadi. Har bir agent birinchi marta faollashtirilganda yopish mumkin boʻlgan xavf haqida ogohlantirish modal oynasi koʻrsatiladi.

### 3.6 Texnik xizmat va diagnostika

Boshqaruv panelida ilgari foydalanuvchi interfeysiga ega boʻlmagan operatsion MITM yoʻnalishlarini koʻrsatuvchi **Texnik xizmat va diagnostika** kartasi (`AgentBridgeMaintenanceCard`, `src/app/(dashboard)/dashboard/tools/agent-bridge/components/` ichida) mavjud. Uning ostsarlavhasi: _"Tutib olish konveyerini mustaqil sinovdan oʻtkazing, tizimda qolib ketgan holatlarni bekor qiling va sozlamalaringizni qurilmalar oʻrtasida koʻchiring."_ Karta mijoz yordamchilari `src/lib/inspector/agentBridgeMaintenanceApi.ts` ichida joylashgan.

| Tugma                               | Yoʻnalish                              | Vazifasi                                                                                                                                                                                                                                           |
| ----------------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Diagnostika**                     | `GET /api/tools/agent-bridge/diagnose` | Maʼlumotlarni ushlash konveyerining oʻz-oʻzini tekshirishini ishga tushiradi va har bir tekshiruv boʻyicha hisobotni koʻrsatadi (✓/✗ + tuzatish boʻyicha maslahat).                                                                                |
| **Tuzatish**                        | `POST /api/tools/agent-bridge/repair`  | Nosozlik yoki SIGKILL tufayli qolib ketgan, egasiz MITM tizim holatini (DNS spoof yozuvlari, ildiz CA, tizim proksisi) bekor qiladi. Idempotent — holat toza boʻlsa, "Tuzatish uchun hech narsa yoʻq" deb xabar beradi.                            |
| **CAʼni olib tashlash**             | `DELETE /api/tools/agent-bridge/cert`  | MITM ildiz CA sertifikatiga ishonchni bekor qiladi va uni OS ishonchli sertifikatlar omboridan olib tashlaydi (aniq, idempotent). Faqat CA hozirda ishonchli boʻlganda koʻrsatiladi; qator ichida "CA olib tashlansinmi?" tasdigʻini talab qiladi. |
| **Konfiguratsiyani eksport qilish** | `GET /api/tools/agent-bridge/config`   | Koʻchma konfiguratsiya JSON faylini yuklab oladi (§3.7 ga qarang).                                                                                                                                                                                 |
| **Konfiguratsiyani import qilish**  | `POST /api/tools/agent-bridge/config`  | Oldin eksport qilingan konfiguratsiya JSON faylini yuklaydi (§3.7 ga qarang).                                                                                                                                                                      |

**Diagnostika tekshiruvlari** (`src/mitm/inspector/diagnostics.ts` ichidagi `summarizeDiagnostics()`). Yoʻnalish har biri uchun yon taʼsirli tekshiruvni ishga tushiradi va mantiqiy qiymatlarni sof umumlashtiruvchiga uzatadi; yagona `healthy` xulosasi hamda har bir nosozlik uchun maslahat qaytariladi:

| Tekshiruv nomi     | Nimani tekshiradi                                                      | Muvaffaqiyatsizlikdagi maslahat                                                                                                                                |
| ------------------ | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server-running`   | MITM server jarayoni faol ekanini                                      | "MITM serveri ishlamayapti. Uni AgentBridge ichki oynasidan ishga tushiring."                                                                                  |
| `server-reachable` | MITM serveri oʻz portida ulanishlarni qabul qilishini (TCP tekshiruvi) | "MITM serveri oʻz portida ulanishlarni qabul qilmayapti. Port boʻshligini va uni bogʻlash uchun vakolatingiz borligini tekshiring."                            |
| `cert-exists`      | MITM sertifikati diskda yaratilganini                                  | "Hali MITM sertifikati yaratilmagan. Uni AgentBridge ichki oynasidan yarating."                                                                                |
| `cert-trusted`     | MITM ildiz CA sertifikati OS ishonchli sertifikatlar omborida ekanini  | "MITM ildiz CA sertifikati OS omborida ishonchli emas, shu sababli TLS tutib qolish ishlamaydi. Sertifikatga AgentBridge ichki oynasidan ishonch bildiring."   |
| `dns-configured`   | Maqsadli xost nomlari `/etc/hosts` ichida soxtalashtirilganini         | "Maqsadli xost nomlari /etc/hosts ichida soxtalashtirilmagan, shu sababli trafik proksiga yetib bormaydi. Ushlamoqchi boʻlgan agent(lar) uchun DNSʼni yoqing." |

**Yetim holat banneri:** sahifa nosozlik tufayli qolib ketgan holatni (DNS spoof / CA / tizim proksisi) aniqlasa, kartada sariq banner — _"Oldingi seans tizim holatini qoldirib ketgan (DNS spoof, CA yoki tizim proksisi). Uni tozalash uchun Tuzatishni ishga tushiring."_ — koʻrsatiladi va **Tuzatish** tugmasi ajratib koʻrsatiladi. `Repair` — ProxyBridgeʼning `--cleanup` bayrogʻining ilova qatlamidagi analogi (u vazifani `src/mitm/manager.ts` ichidagi `repairMitm()` ga topshiradi).

> MITM ildiz CA sertifikati qayta-qayta sudo soʻrovlarini oldini olish uchun
> toʻxtatish/ishga tushirish davomida oʻrnatilgan holda saqlanadi (mitmproxy/Charles bilan bir xil xatti-harakat),
> shuning uchun uni olib tashlash toʻxtatish paytida avtomatik yuz beradigan amal emas, balki aniq
> **CAʼni olib tashlash** amalidir.

### 3.7 Koʻchma konfiguratsiyani import/eksport qilish

AgentBridge sozlamani bir nechta kompyuterda takrorlash uchun **operator sozlashi mumkin boʻlgan** holatni versiyalangan JSON obyektiga serializatsiya qila oladi. Serializator — `src/lib/inspector/configPortability.ts` (`exportConfig()` / `importConfig()`), u `AgentBridgeConfigSchema` tomonidan tekshiriladi.

Eksport aynan uchta qismni oʻz ichiga oladi (ichki standart qiymatlar ataylab eksport qilinmaydi, shuning uchun import qilish ularni hech qachon takrorlamaydi yoki ular bilan ziddiyatga kirishmaydi):

| Maydon           | Manba                                                                      | Izohlar                                                                  |
| ---------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `bypassPatterns` | foydalanuvchi belgilagan chetlab oʻtish namunalari (`agent_bridge_bypass`) | standart bank/gov/okta namunalari chiqarib tashlanadi                    |
| `customHosts`    | Traffic Inspector maxsus xostlari (`inspector_custom_hosts`)               | har biri: `{ host, kind: "llm"\|"app"\|"custom", label? }`               |
| `agentMappings`  | har bir agent uchun model mosliklari (`agent_bridge_mappings`)             | mosliklari bor har bir agent uchun `{ [agentId]: [{ source, target }] }` |

```jsonc
// GET /api/tools/agent-bridge/config
{
  "version": 1,
  "bypassPatterns": ["*.internal.example.com"],
  "customHosts": [{ "host": "api.example.com", "kind": "llm", "label": null }],
  "agentMappings": {
    "copilot": [{ "source": "gpt-4o", "target": "claude-sonnet-4.7" }],
  },
}
```

**Import qilish xatti-harakati** (`POST /api/tools/agent-bridge/config`): chetlab oʻtish namunalari va har bir agent uchun mosliklar **toʻliq almashtiriladi**; maxsus xostlar **idempotent tarzda** qoʻshiladi (`INSERT OR IGNORE`). Javobda har biridan nechtasi qoʻllangani koʻrsatiladi:

```jsonc
{ "ok": true, "bypassPatterns": 1, "customHosts": 1, "agents": 1 }
```

Konfiguratsiyaga **KIRMAYDIGAN** narsalar: serverning ishlash holati, sertifikat yoʻllari, har bir agentning DNS holati, yuqori oqim CA yoʻli va TPROXY sozlamalari — bular koʻchma parametrlar emas, balki xost/ishlash muhiti holatidir.

---

## §4 Har bir agent bo‘yicha ma’lumotnoma

| #   | Agent              | Holat                   | Ushlab qolinadigan hostlar                                         | Autentifikatsiya turi |
| --- | ------------------ | ----------------------- | ------------------------------------------------------------------ | --------------------- |
| 1   | **Antigravity**    | ✅ Qo‘llab-quvvatlanadi | `daily-cloudcode-pa.googleapis.com`, `cloudcode-pa.googleapis.com` | Firebase OAuth        |
| 2   | **Kiro (AWS)**     | ✅ Qo‘llab-quvvatlanadi | `prod.kiro.aws`, `dev.kiro.aws`                                    | AWS SigV4             |
| 3   | **GitHub Copilot** | ✅ Qo‘llab-quvvatlanadi | `api.githubcopilot.com`, `copilot-proxy.githubusercontent.com`     | GitHub OAuth          |
| 4   | **OpenAI Codex**   | ✅ Qo‘llab-quvvatlanadi | `api.openai.com` (Codex yo‘llari), `chatgpt.com`                   | OpenAI kaliti         |
| 5   | **Cursor IDE**     | ✅ Qo‘llab-quvvatlanadi | `api2.cursor.sh`, `api.cursor.sh`                                  | Cursor OAuth          |
| 6   | **Zed Industries** | ✅ Qo‘llab-quvvatlanadi | `api.zed.dev`, `llm.zed.dev`                                       | Zed OAuth             |
| 7   | **Claude Code**    | ✅ Qo‘llab-quvvatlanadi | `api.anthropic.com` (ixtiyoriy ravishda yoqiladi)                  | Anthropic kaliti      |
| 8   | **Open Code**      | ✅ Qo‘llab-quvvatlanadi | `openrouter.ai`, `api.openai.com` (zen yo‘llari)                   | API kaliti            |
| 9   | **Trae**           | 🔍 O‘rganilmoqda        | Aniqlanadi — §8 ga qarang                                          | Aniqlanadi            |

### Sozlash ustasi bosqichlari (har bir agent uchun)

Har bir agent kartasida 3 bosqichli sozlash ustasi mavjud:

1. **Talablarni tekshirish** — Server ishlayaptimi? Sertifikat ishonchli deb belgilanganmi? IDE o‘rnatilganmi (avtomatik aniqlanadi)?
2. **DNS’ni yoqish** — `/etc/hosts` yozuvlarini qo‘shadi (sudo talab qilinadi). Aynan qaysi qatorlar qo‘shilishini ko‘rsatadi.
3. **Modellarni moslashtirish** — Ixtiyoriy model moslashtirish jadvali. Joker belgilar qabul qilinadi.

### Agentni aniqlash

1–8-agentlar uchun AgentBridge IDE o‘rnatilganini avtomatik aniqlashga urinadi:

```ts
export async function detectAgent(agentId: AgentId): Promise<DetectionResult>;
// Qaytaradi: { installed: boolean, version?: string, path?: string }
```

Aniqlashda operatsion tizimga xos yo‘llar va binar fayl tekshiruvlaridan foydalaniladi (masalan, Copilot uchun `code --list-extensions | grep github.copilot`, Antigravity uchun `~/.config/antigravity/`).

---

## §5 Xavfsizlik

### Qo‘llaniladigan qat’iy qoidalar

| Qoida                             | Qo‘llanishi                                                                                                       |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| **#12** `sanitizeErrorMessage`    | Barcha ishlov beruvchi xatolari javob yoki bufer yozuviga kiritilishidan oldin tozalanadi                         |
| **#13** Shell muhitini uzatish    | `/etc/hosts` tahrirlari `env` parametridan foydalanadi — yo‘llar satrga interpolyatsiya qilinmaydi                |
| **#15 + #17** `isLocalOnlyPath()` | `/api/tools/agent-bridge/` — LOCAL_ONLY + SPAWN_CAPABLE; autentifikatsiyadan oldin loopback majburiy tekshiriladi |

### Maxfiy hostlar uchun chetlab o‘tish ro‘yxati

Chetlab o‘tish ro‘yxati moliyaviy muassasalar, OAuth/SSO provayderlari va boshqa maxfiy hostlar **hech qachon deshifrlanmasligini** ta’minlaydi. Ularning TLS trafigi shaffof TCP tunneli orqali o‘tadi — OmniRoute hech qachon ochiq matnni ko‘rmaydi.

Standart chetlab o‘tish andozalariga quyidagilar kiradi:

- `*.bank.*`, `*.gov.*` (moliya/hukumat)
- `*.okta.com`, `*.auth0.com`, `*.microsoft.com` (SSO/identifikatsiya)
- `*.apple.com`, `*.icloud.com` (Apple tizim xizmatlari)

Foydalanuvchi qo‘shgan chetlab o‘tish andozalari `agent_bridge_bypass` jadvalida saqlanadi va barcha boshqa qoidalardan ustun turadi.

### Sirlarni niqoblash

`src/mitm/maskSecrets.ts` faylidagi `maskSecrets()` quyidagi hollarda qo‘llaniladi:

- Har bir so‘rov tanasiga `TrafficBuffer.push()` dan oldin
- Har bir sarlavhaga jurnalga yozish yoki uzatishdan oldin

Andozalar: `sk-`/`ak-`/`pk-` prefiksli tokenlar, `Bearer` tokenlari va uzunligi ≥40 belgidan iborat umumiy tokenlar.

### Yuqori oqim CA sertifikati

`AGENTBRIDGE_UPSTREAM_CA_CERT` o‘rnatilganda, fayl ishga tushirish vaqtida o‘qiladi. Agar yo‘l mavjud bo‘lsa-yu, faylni o‘qib bo‘lmasa, AgentBridge aniq xatoni jurnalga yozadi va ishga tushishni rad etadi (korporativ muhitlarda yashirin TLS nosozliklarining oldini oladi).

### Ma’lum cheklovlar

- **443-port imtiyoz talab qiladi**: Linux’da AgentBridge Node binar fayli uchun `setcap 'cap_net_bind_service=+ep'` talab qiladi yoki `authbind` orqali ishga tushirilishi kerak. Sozlash ustasi operatsion tizimga xos ko‘rsatmalarni namoyish etadi.
- **IDE’ni qayta ishga tushirish talab qilinadi**: DNS qayta yo‘naltirilgandan keyin yangi host aniqlanishi kuchga kirishi uchun IDE qayta ishga tushirilishi kerak.
- **Qat’iy belgilangan OAuth tokenlari**: Ayrim agentlar (Kiro, Antigravity) OAuth yangilash tokenlarini lokal saqlaydi. Bular AgentBridge uchun shaffof — u har bir so‘rovdagi Bearer tokenini ko‘radi va jurnalga yozishdan oldin uni niqoblaydi.
- **Electron frontendlariga `NODE_EXTRA_CA_CERTS` kerak**: Frontendi ichki Node/Electron muhitida ishlaydigan IDE’lar operatsion tizim/NSS ishonchli sertifikatlar omborini e’tiborsiz qoldiradi va `NODE_EXTRA_CA_CERTS` o‘rnatilgan shell orqali ishga tushirilishi kerak (§3.2 ga qarang). U mavjud bo‘lmagandagi alomat: IDE backendi autentifikatsiyadan o‘tadi (MITM `200` javoblarini ko‘rsatadi), ammo UI tizimga kirmagan holatda qoladi.
- **Bitta IDE’ning bir nechta o‘rnatilishi mustaqildir**: tizim darajasidagi o‘rnatish (masalan, `/usr/share/antigravity/antigravity`) va foydalanuvchi uchun lokal «Full» o‘rnatish (masalan, `~/AntigravityIDE_Full/antigravity-ide`) o‘z bajarish muhitlariga ega alohida jarayonlardir — har biri CA kiritilgan holda qayta ishga tushirilishi kerak. Qayta ishga tushirishdan oldin binar fayl yo‘li orqali aynan qaysi biri ishlayotganini aniqlang.
- **Identifikatsiyani yo‘naltirilgan model emas, agentning tizim prompti belgilaydi**: agent modelini boshqa provayderga moslashtirganingizda ham javob agentning asl identifikatsiyasini da’vo qiladi (masalan, Antigravity «Meni Gemini quvvatlantiradi» deb javob beradi), chunki IDE buni tizim promptiga kiritadi. Haqiqiy backendni modeldan uning kimligini so‘rab emas, `call_logs` / `proxy_logs` ichidagi (`provider`, `model`, `target_format`) orqali tasdiqlang.

---

## §6 Muammolarni bartaraf etish

### 443-port ziddiyati

Agar boshqa jarayon allaqachon 443-portni tinglayotgan boʻlsa (veb-server, VPN va hokazo):

```bash
lsof -i :443          # jarayonni topish
sudo fuser -k 443/tcp  # majburan toʻxtatish (ehtiyotkorlik bilan ishlating)
```

Muqobil ravishda, AgentBridge sozlamalarida imtiyozsiz portni sozlang va `iptables` / `pf` qayta yoʻnaltirish qoidalarini oʻrnating.

### Sertifikat ishonchli deb topilmadi

Agar AgentBridge ishga tushirilgandan keyin IDE TLS xatolarini koʻrsatsa:

1. Sertifikat oʻrnatilganini tekshiring: `security find-certificate -c "OmniRoute AgentBridge"` (macOS) yoki `certutil -L -d sql:$HOME/.pki/nssdb` (Linux/NSS)
2. Ayrim ilovalar oʻz ishonchli sertifikatlar omborini yuritadi (Firefox, Linux’dagi Chrome). "Trust Cert" amalini yana bajaring va NSS/Firefox’ga xos sertifikatlar omborini tekshiring.
3. Ishonch oʻrnatilgandan keyin IDE’ni qayta ishga tushiring — davom etayotgan TLS seanslari eski ishonch holatidan foydalanadi.

### Ishonchli CA mavjudligiga qaramay, IDE tizimdan chiqib ketgan / "connection error"

Alomat: DNS qayta yoʻnaltirilib, CA’ga ishonch oʻrnatilgandan keyin Electron asosidagi IDE (masalan, Antigravity)
**tizimdan chiqilgan** holatda ochiladi yoki autentifikatsiya/ulanish xatosini koʻrsatadi, ammo MITM jurnalida
boshlangʻich chaqiruvlar (`loadCodeAssist`, `fetchAvailableModels`, …) `200` qaytarayotgani koʻrinadi.

Sabab: IDE bilan birga taqdim etilgan **Node/Electron bajarilish muhiti operatsion tizimning ishonchli sertifikatlar omborini eʼtiborsiz qoldiradi**. Mahalliy
bekend (Go til serveri) operatsion tizim CA sertifikatiga ishonadi va autentifikatsiyadan oʻtadi, biroq Electron frontendi
ishonmaydi — shu sababli UI oʻzini oflayn deb hisoblaydi.

Yechim (har ikki qadam): `NODE_EXTRA_CA_CERTS=<ca.crt>` ni eksport qiling **va IDE’ni ish stoli
belgisidan emas, aynan shu qobiqdan qayta ishga tushiring**. Avval IDE’dan toʻliq chiqing — Electron’ning yagona nusxa qulfi
sababli ikkinchi ishga tushirish mavjud jarayonni shunchaki fokuslaydi va yangi muhit eʼtiborsiz qoldiriladi. §3.2 ga qarang.
Bu mustaqil agent MITM orqali ishlaydigan, ammo IDE
varianti ayni sozlama ostida ishlamay qoladigan ochiq yuqori oqim hisobotidagi holatni takrorlaydi.

### DNS tarqalmadi

`/etc/hosts` yangilanganini tekshiring:

```bash
grep "omniroute\|127.0.0.1.*github\|127.0.0.1.*cursor" /etc/hosts
```

DNS keshini tozalang:

```bash
# macOS
sudo dscacheutil -flushcache && sudo killall -HUP mDNSResponder
# Linux (systemd-resolved)
sudo systemctl restart systemd-resolved
# Windows
ipconfig /flushdns
```

### IDE aniqlanmadi

Avtomatik aniqlash keng tarqalgan oʻrnatish yoʻllaridan foydalanadi. Agar aniqlash muvaffaqiyatsiz tugasa, ammo IDE oʻrnatilgan boʻlsa:

- IDE binar fayli nostandart joylashuvda ekanini tekshiring
- Setup Wizard ishlashda davom etadi — aniqlashdagi muvaffaqiyatsizlik shunchaki nishonchada oʻrnatish yoʻli koʻrsatilmasligini anglatadi

### Handler xatolari (yuqori oqimdan olish muvaffaqiyatsiz)

Agar AgentBridge soʻrovlarni tutib olsa-yu, lekin barcha soʻrovlar muvaffaqiyatsiz tugasa:

1. `/dashboard/providers` manzilida kamida bitta provayder ulanganini tekshiring
2. OmniRoute server jurnallarini tekshiring: `.env` ichida `APP_LOG_LEVEL=debug`
3. `OMNIROUTE_BASE_URL` toʻgʻri marshrutizator soʻnggi nuqtasiga ishora qilayotganini tekshiring (standart: `http://127.0.0.1:20128`)

---

## §7 API maʼlumotnomasi

Barcha yoʻnalishlar `LOCAL_ONLY` (faqat loopback, autentifikatsiyadan oldin majburiy tarzda qoʻllanadi) va `SPAWN_CAPABLE` hisoblanadi. `src/server/authz/routeGuard.ts` ga qarang.

Asosiy yoʻl: `/api/tools/agent-bridge/`

| Metod               | Yo‘l                                           | Tavsif                                                                                                                                                       |
| ------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| GET                 | `/api/tools/agent-bridge/state`                | Global server holati + har bir agent uchun aniqlash/holat                                                                                                    |
| GET                 | `/api/tools/agent-bridge/agents`               | Ro‘yxatdan o‘tgan agentlar ro‘yxati (id, nom, hostlar, yaroqlilik, holat)                                                                                    |
| GET                 | `/api/tools/agent-bridge/agents/{id}`          | Bitta agentning holati (maqsad konfiguratsiyasi + aniqlash + saqlangan holat)                                                                                |
| PATCH               | `/api/tools/agent-bridge/agents/{id}`          | Agent uchun `setup_completed` qiymatini yangilash                                                                                                            |
| GET                 | `/api/tools/agent-bridge/agents/{id}/detect`   | Agent uchun aniqlash tekshiruvini ishga tushirish (`installed`, `version?`, `path?`)                                                                         |
| POST                | `/api/tools/agent-bridge/agents/{id}/dns`      | Agent uchun DNS’ni yoqish/o‘chirish (`{enabled: boolean}`)                                                                                                   |
| GET                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Agent uchun model mosliklari                                                                                                                                 |
| PUT                 | `/api/tools/agent-bridge/agents/{id}/mappings` | Model mosliklarini almashtirish                                                                                                                              |
| POST                | `/api/tools/agent-bridge/server`               | Serverni ishga tushirish/to‘xtatish/qayta ishga tushirish (`action: "start"\|"stop"\|"restart"\|"trust-cert"\|"regenerate-cert"`)                            |
| GET                 | `/api/tools/agent-bridge/cert`                 | Sertifikat holati (`exists`, `trusted`, `path`)                                                                                                              |
| POST                | `/api/tools/agent-bridge/cert`                 | MITM ildiz CA sertifikatiga ishonish (o‘rnatish)                                                                                                             |
| DELETE              | `/api/tools/agent-bridge/cert`                 | MITM ildiz CA sertifikatiga ishonchni bekor qilish (olib tashlash) — idempotent (§3.6 ga qarang)                                                             |
| POST                | `/api/tools/agent-bridge/cert/regenerate`      | O‘z-o‘zidan imzolangan MITM sertifikatini qayta yaratish                                                                                                     |
| GET                 | `/api/tools/agent-bridge/cert/download`        | Yuklab olish uchun PEM sertifikatini oqim orqali uzatish                                                                                                     |
| GET                 | `/api/tools/agent-bridge/bypass`               | Chetlab o‘tish andozalari ro‘yxati (`default` + `user`)                                                                                                      |
| POST                | `/api/tools/agent-bridge/bypass`               | Foydalanuvchi belgilagan chetlab o‘tish andozalarini to‘liq almashtirish                                                                                     |
| DELETE              | `/api/tools/agent-bridge/bypass?pattern=...`   | Foydalanuvchi belgilagan bitta chetlab o‘tish andozasini olib tashlash                                                                                       |
| GET                 | `/api/tools/agent-bridge/diagnose`             | Tutib olish konveyerining o‘z-o‘zini tekshirishi (§3.6 ga qarang)                                                                                            |
| POST                | `/api/tools/agent-bridge/repair`               | Yetim qolgan MITM tizim holatini bekor qilish (§3.6 ga qarang)                                                                                               |
| GET                 | `/api/tools/agent-bridge/config`               | Ko‘chma JSON konfiguratsiyasini eksport qilish (§3.7 ga qarang)                                                                                              |
| POST                | `/api/tools/agent-bridge/config`               | Ko‘chma JSON konfiguratsiyasini import qilish (§3.7 ga qarang)                                                                                               |
| GET                 | `/api/tools/agent-bridge/upstream-ca`          | Sozlangan yuqori oqim CA yo‘lini olish                                                                                                                       |
| POST                | `/api/tools/agent-bridge/upstream-ca`          | Yuqori oqim CA yo‘lini tekshirish + saqlash                                                                                                                  |
| POST                | `/api/tools/agent-bridge/upstream-ca/test`     | Yuqori oqim CA yo‘lini faqat tekshirish (sinov tariqasida) — saqlanmaydi                                                                                     |
| GET / POST / DELETE | `/api/tools/agent-bridge/tproxy`               | TPROXY shaffof-shifrni ochish orqali tutib olish rejimi — `docs/security/MITM-TPROXY-DECRYPT.md` ga qarang (git; `/docs` tarkibiga kompilyatsiya qilinmagan) |

To‘liq OpenAPI sxemalari: `docs/openapi.yaml` → `AgentBridge` tegi.

---

## §8 Yo‘l xaritasi

### Trae’ni o‘rganish

Trae — nisbatan yangi AI kod yozish yordamchisi. Handler’ni amalga oshirishdan oldin:

1. VS Code / JetBrains marketpleyslarida yoki mustaqil ilova sifatida binar fayl/kengaytmani aniqlash
2. API hostlari va endpoint tuzilmalarini aniqlash uchun mitmproxy yordamida trafikni yozib olish
3. Autentifikatsiya mexanizmini aniqlash
4. TOS va API’ni aniqlash imkoniyatiga asoslanib, davom etish yoki etmaslikni baholash

O‘rganish yakunlangunga qadar boshqaruv panelidagi Trae kartasida "Yaroqliligini bildirish" havolasi bilan "O‘rganilmoqda" belgisi ko‘rsatiladi. `src/mitm/handlers/trae.ts` dagi handler zagotovkasi tuzilmaviy `Hali amalga oshirilmagan` xatosini chiqaradi.

### Rejadagi agentlar (MITM talab qilinadi — maxsus asosiy URL’ni qo‘llab-quvvatlamaydi)

Quyidagi vositalarning joriy versiyalari maxsus asosiy URL’larni qo‘llab-quvvatlamaydi, shu sababli MITM trafikni tutib olishning yagona usuli hisoblanadi. Yaroqlilikni baholash kutilmoqda:

- **Windsurf** (Codeium/Cognition)
- **Amp** (Sourcegraph)
- **Amazon Q / Kiro CLI** (AWS Bedrock — Kiro IDE’dan alohida)
- **Cowork** (Anthropic desktop)

Eslatma: GitHub Copilot CLI ≥v1.0.19 `COPILOT_PROVIDER_BASE_URL`’ni qo‘llab-quvvatlaydi — bu vosita uchun MITM o‘rniga bevosita konfiguratsiyadan foydalaning.
