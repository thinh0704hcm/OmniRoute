# open-sse Architecture (Oʻzbekcha)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇬 [bg](../../../bg/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Qisqacha**: `open-sse/` — OmniRoute’dagi har bir LLM soʻrovini boshqaradigan asosiy oqimli uzatish mexanizmi. U soʻrov konveyeri, ijrochilar, xizmatlar, MCP serveri va tarjima qatlamini amalga oshiruvchi ~1 400 ta faylni oʻz ichiga oladi. Ushbu qoʻllanma bu qismlar qanday birgalikda ishlashini tushuntiradi.

**Manba:** `open-sse/` (ish maydoni paketi, testga oid boʻlmagan ~1 440 ta `.ts` fayl)

---

## Nima uchun alohida ish maydoni paketi?

`open-sse/` bir nechta sabablarga koʻra OmniRoute monoreposida **mustaqil ish maydoni** hisoblanadi:

1. **Qayta foydalanish imkoniyati** — `open-sse` npm’da `@omniroute/open-sse` sifatida chop etiladi, shuning uchun boshqa loyihalar undan mustaqil ravishda foydalanishi mumkin
2. **Aniq chegaralar** — oqimli uzatish mexanizmi OmniRoute’ga xos UI/DB qatlamidan ajratilgan
3. **Unumdorlik** — mexanizmda Next.js bogʻliqliklari yoʻq, bu CLI/serverless muhitlarida sovuq ishga tushishni tezlashtiradi
4. **Versiyalash** — `open-sse` oʻz jadvali boʻyicha reliz chiqarishi mumkin

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Yuqori darajadagi tuzilma

```
open-sse/
├── index.ts              # Ommaviy kirish nuqtasi
├── types.d.ts            # Ommaviy tur eksportlari
├── package.json          # @omniroute/open-sse
├── config/               # Provayder konfiguratsiyalari, konstantalar, registrlar
├── executors/            # Har bir provayder uchun HTTP ijrochilari (67 + base.ts/index.ts)
├── handlers/             # Soʻrov ishlovchilari (chatCore, responses va boshqalar)
├── lib/                  # Ichki yordamchi vositalar
├── mcp-server/           # Model Context Protocol serveri
├── services/             # ~298 ta xizmat moduli
├── transformer/          # Responses API formati transformatori
├── translator/           # Format tarjimasi (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Umumiy yordamchi vositalar (jurnallash, xato, oqim va boshqalar)
```

### Modullar soni

| Katalog | Fayllar | Maqsad |
| `executors/` | 167 | Har bir provayder uchun HTTP ijrochilari (DefaultExecutor fabrikasi orqali birlashtirilgan) |
| `handlers/` | 157 | Soʻrov kirish nuqtalari (chatCore, responses, embeddings) |
| `services/` | ~536 | Yoʻnaltirish, keshlash, tezlikni cheklash, yangilash va boshqalar |
| `translator/` | 56 | Formatni oʻzgartirish (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP vositalari va transportlari |
| `utils/` | ~108 | Tizim boʻylab qoʻllaniladigan yordamchi vositalar (jurnallash, xato, oqim) |
| `config/` | ~339 | Provayder konfiguratsiyalari, konstantalar, registrlar |

---

## Soʻrov konveyeri

Har bir LLM soʻrovi **5 bosqichli konveyer** orqali oʻtadi:

```
                              ┌──────────────┐
   HTTP soʻrovi                │  1. YOʻNALTIRISH│   kombinatsiyani aniqlash, modelni tanlash
   (Next.js marshruti)         └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  2. TARJIMA  │   formatni oʻzgartirish (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  3. BAJARISH │   provayder ijrochisi, HTTP, qayta urinish, uzgich
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  4. OQIM     │   SSE transformatsiyasi, teskari bosim
                              └──────┬───────┘
                                     │
                                     ▼
                              ┌──────────────┐
                              │  5. QAYD     │   foydalanishni kuzatish, chaqiruv jurnali, xatoni tasniflash
                              └──────┬───────┘
                                     │
                                     ▼
                              HTTP javobi (SSE yoki JSON)
```

### 1-bosqich: Yoʻnaltirish (services/combo.ts)

**Kirish nuqtasi**: `services/combo.ts` ichidagi `handleComboChat()`

Soʻrovni aniq `(provider, model, account, credentials)` kortejiga moslaydi:

- Kombinatsiyani ID boʻyicha topish (yoki `auto/*` modellari uchun virtual kombinatsiya yaratish)
- Yoʻnaltirish strategiyasini qoʻllash (ustuvorlik, vaznli, navbatma-navbat va boshqalar)
- Nosogʻlom provayderlarni chiqarib tashlash (avtomatik uzgich)
- Keyingi yaroqli nishonni tanlash

`auto/*` modellari uchun bu bosqich quyidagilarni ham bajaradi:

- **16 omilli baholash** algoritmini ishga tushiradi (`services/autoCombo/`)
- Sogʻlomlik, xarajat, kechikish va boshqa omillar asosida `provider+model` juftligini tanlaydi

### 2-bosqich: Tarjima (translator/)

Agar manba formati (masalan, OpenAI) maqsad formatidan (masalan, Claude) farq qilsa, soʻrov **tarjima qilinadi**:

- Tizim prompti → tizim xabari
- Vosita taʼriflari → provayderga xos vosita formati
- Mulohaza/yuritmali fikrlash parametrlari → provayderga xos muqobillar
- Xabar rollarini meʼyorlashtirish (OpenAI boʻlmaganlar uchun `developer` → `system`)

`translator/index.ts` quyidagilarni taqdim etadi:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### 3-bosqich: Bajarish (executors/)

**Kirish nuqtasi**: `getExecutor(providerId).execute(request, options)`

Barcha provayderlar `getExecutor()` fabrikasining zaxira varianti orqali `DefaultExecutor` (`executors/default.ts`)dan foydalanadi. Ijrochi:

- Yuqori oqim URL manzilini tuzadi (`buildUrl()`)
- Provayderga xos sarlavhalarni qoʻshadi (`buildHeaders()`)
- Soʻrov tanasini oʻzgartiradi (`transformRequest()`)
- HTTP soʻrovini qayta urinish va eksponensial kechiktirish bilan yuboradi
- Zarur boʻlsa, autentifikatsiyani yangilaydi (OAuth provayderlari)

Barcha ijrochilar `BaseExecutor` (`executors/base.ts`, 1170 qator kod)ni kengaytiradi, u quyidagilarni taqdim etadi:

- Umumiy qayta urinish mantigʻi
- Proksi integratsiyasi
- Avtomatik uzgich integratsiyasi
- Foydalanishni qayd etish ilgaklari

### 4-bosqich: Oqim (utils/stream.ts)

Oqimli javoblar uchun ijrochi **ReadableStream** qaytaradi. Ishlovchi:

- SSE transformatsiyasi orqali oʻtkazadi (`createSSETransformStreamWithLogger`)
- Uzilib qolgan ulanishlarni aniqlash uchun davriy signal pinglarini qoʻllaydi
- Mijoz uzilishini muammosiz boshqaradi (`pipeWithDisconnect`)
- Oqimsiz mijozlar uchun SSE → JSON oʻzgartirishini bajaradi

Oqimsiz javoblar uchun ijrochi tahlil qilingan JSON obyektini qaytaradi va u oʻzgartirilmasdan uzatiladi.

### 5-bosqich: Qayd etish (services/usage.ts)

Javobdan soʻng (muvaffaqiyatli yoki muvaffaqiyatsiz), foydalanish qayd etiladi:

- Javobdan olingan `prompt_tokens`, `completion_tokens`, `cached_tokens`
- Narxlash maʼlumotlari asosida hisoblangan `cost_usd`
- Muvaffaqiyatsiz boʻlsa, `latency_ms`, `status`, `error_class`
- `usage_history` jadvaliga saqlanadi

Chaqiruv jurnali artefaktlari (agar yoqilgan boʻlsa) `${DATA_DIR}/call_logs/` manziliga yoziladi.

---

## Asosiy fayllarni chuqur tahlil qilish

### chatCore.ts (5977 qator)

**Asosiy soʻrov ishlovchisi**. Hajmiga qaramay, u aniq tuzilishga ega:

```ts
// chatCore.ts faylining psevdotuzilmasi
export async function handleChat(request: NextRequest) {
  // 1. Autentifikatsiya + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Soʻrov tanasini tekshirish
  const body = await parseRequestBody(request);

  // 3. Formatni aniqlash + konvertatsiya
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Kombo marshrutlash
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Keyingi nishonga oʻtish
    }
  }

  // 5. Favqulodda zaxira variant
  return await emergencyFallback(body);
}
```

Bitta ulkan funksiyadan iborat boʻlishiga qaramay, u 5 bosqichli konveyerga mos keladigan **izohlangan boʻlimlar** asosida tashkil etilgan.

### combo.ts (4456 LOC)

Komboni tartiblangan nishonlarga aylantiradigan **marshrutlash mexanizmi**.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("target failed, trying next", { target, err });
    }
  }
  throw new ComboExhaustedError("All targets failed");
}
```

**19 ta marshrutlash strategiyasini** qoʻllab-quvvatlaydi (`src/shared/constants/routingStrategies.ts` fayliga qarang):

| Strategiya          | Xatti-harakat                                                                       |
| ------------------- | ----------------------------------------------------------------------------------- |
| `priority`          | Birinchi nishon ustuvor boʻlgan tartiblangan roʻyxat                                |
| `weighted`          | Har bir nishon vazniga asoslangan ehtimollik                                        |
| `round-robin`       | Nishonlarni tartib boʻyicha navbatma-navbat aylanish                                |
| `context-relay`     | Kontekstni nishonlar orasida uzatish                                                |
| `fill-first`        | Keyingisiga oʻtishdan oldin kvotani toʻldirish                                      |
| `p2c`               | Ikki tanlov quvvati                                                                 |
| `random`            | Bir tekis tasodifiy tanlov                                                          |
| `least-used`        | Yaqinda eng kam ishlatilganini tanlash                                              |
| `cost-optimized`    | Avval eng arzon, sogʻlom nishonni tanlash                                           |
| `reset-aware`       | Provayderning tiklash vaqt oraligʻini hisobga olish                                 |
| `reset-window`      | Tiklash vaqt oraligʻiga asoslangan marshrutlash                                     |
| `headroom`          | Qolgan kvota zaxirasi eng katta boʻlganini birinchi tanlash                         |
| `strict-random`     | Haqiqiy bir tekis tanlov (sifatga asoslangan vaznlashsiz)                           |
| `auto`              | 16 omilli baholashdan foydalanish (`autoCombo/`)                                    |
| `lkgp`              | Oxirgi maʼlum boʻlgan yaxshi provayderni birinchi tanlash                           |
| `context-optimized` | Uzun kontekstli soʻrovlar uchun eng maqbul                                          |
| `fusion`            | Panelga parallel ravishda tarqatish, soʻng hakam orqali sintez qilish (`fusion.ts`) |

### base.ts (1170 LOC)

Barcha 107 ta ijrochi kengaytiradigan **abstrakt ijrochi**. U quyidagilarni oʻz ichiga oladi:

- `buildUrl()` — standart URL yaratish (quyi sinflar maxsus holatlar uchun qayta belgilaydi)
- `buildHeaders()` — standart sarlavhalar (autentifikatsiya, kontent turi)
- `transformRequest()` — sukut boʻyicha oʻzgarishsiz uzatish
- `execute()` — qayta urinish/kechiktirish/uzgich bilan asosiy HTTP sikli

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Barcha OpenAI/Anthropic bilan mos provayderlarga ishlov beradi
  // Provayderlar konfiguratsiyalarni (URL, autentifikatsiya, sarlavhalar) roʻyxatdan oʻtkazadi, ammo ijrochi mantigʻi umumiy boʻladi
}
```

Provayderga xos xatti-harakatlar (autentifikatsiya sarlavhalari, asosiy URL, versiya sarlavhalari) alohida ijrochi sinflari orqali emas, balki provayderlar reyestri orqali sozlanadi.

````

---

## Servislar (117 modul)

Servislar — ishlov beruvchilar birlashtiradigan **aniq yoʻnaltirilgan, bitta maqsadga moʻljallangan modullar**. Asosiy toifalar:

### Yoʻnaltirish va Combo

- `combo.ts` — combo orqali yoʻnaltirilgan soʻrovlar uchun kirish nuqtasi
- `services/autoCombo/` — 16 omilli baholash, 8 ta avtomatik yoʻnaltirish strategiyasi
- `wildcardRouter.ts` — joker belgili yoʻnalishlarni moslashtiradi (`gpt-*`)
- `modelFamilyFallback.ts` — T5 oilasi ichidagi zaxira variant

### Tezlikni cheklash va kvota

- `rateLimitManager.ts` — har bir kalit+provayder uchun token savati
- `usage.ts` — foydalanishni qayd etish
- `quotaCache.ts` — xotiradagi kvota holatlari

### Hisob va token

- `tokenRefresh.ts` — 401 holatida OAuth yangilanishi
- `accountFallback.ts` — muqobil hisobga oʻtish
- `sessionManager.ts` — koʻp bosqichli seans holati

### Intellekt

- `intentClassifier.ts` — soʻrov maqsadini tasniflash
- `taskAwareRouter.ts` — vazifa turi boʻyicha yoʻnaltirish
- `thinkingBudget.ts` — fikrlash tokenlarini taqsimlash
- `contextManager.ts` — yoʻnaltirish kontekstini kiritish

### Bardoshlilik

- `resilience.ts` — qayta urinish, kechiktirish va uzgichni muvofiqlashtirish
- `emergencyFallback.ts` — soʻnggi chora sifatidagi zaxira variant
- `modelDeprecation.ts` — voris modellarga avtomatik yoʻnaltirish

### Holat

- `signatureCache.ts` — soʻrov imzosi boʻyicha takrorlarni yoʻqotish
- `volumeDetector.ts` — yuklamani kamaytirish
- `contextHandoff.ts` — seansni serializatsiya qilish

### Siqish

- `compression/` (quyi katalog) — toʻliq siqish konveyeri
- Mexanizmlar, qoidalar toʻplamlari va adapterlarni qamrab oluvchi 39 ta fayl

### Koʻnikmalar

- ([SKILLS.md](./SKILLS.md) faylida yoritilgan)

### Xotira

- ([MEMORY.md](./MEMORY.md) faylida yoritilgan)

---

## Ijrochilar (75+ fayl)

Har bir provayder uchun bittadan fayl. Ularning barchasi `BaseExecutor` sinfini kengaytiradi va farq qiluvchi qismlarni qayta belgilaydi.

### Umumiy andozalar

Provayderlar sozlangan ijrochini qaytaradigan `getExecutor(providerId)` orqali aniqlanadi. OpenAI/Anthropic bilan mos provayderlar `DefaultExecutor` (`executors/default.ts`) dan foydalanadi. Provayderga xos xatti-harakatlar (asosiy URL, autentifikatsiya sarlavhalari, API versiyasi) `open-sse/config/providers/` ichida sozlanadi, soʻrov tanasini oʻzgartirish esa `open-sse/translator/` ichida amalga oshiriladi.

**Maxsus URL** provayder konfiguratsiyasi orqali oʻrnatiladi:

```ts
// open-sse/config/providers/ ichidagi provayder konfiguratsiyasi
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Maxsus autentifikatsiya** provayder reyestrining autentifikatsiya konfiguratsiyasi (API kaliti, OAuth, sarlavha profillari) orqali boshqariladi.

**Maxsus soʻrov tanasi** oʻzgartirishlari (masalan, Anthropic tomonidan `system` ning `messages` dan ajratilishi) `open-sse/translator/` ichida har bir provayder uchun alohida roʻyxatdan oʻtkaziladi.

````

### Ijrochi fabrikasi

`executors/index.ts` fayli `getExecutor(providerId)` ni eksport qiladi:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Aniqlash `ExecutorRegistry` (`executors/registry.ts`) orqali amalga oshiriladi: har bir maxsus ijrochi `executors/index.ts` ning ichki jadvalida eʼlon qilinadi va modul yuklanayotganda `registerExecutor(alias, instance)` orqali roʻyxatdan oʻtkaziladi; `getExecutor()` reyestrni tekshiradi va maxsus yozuvga ega boʻlmagan har qanday provayder uchun xotirada saqlanuvchi `DefaultExecutor` ga qaytadi. Toʻliq taxallus → ijrochi mosligi `tests/unit/executor-map-golden.test.ts` etalon testi orqali tavsiflanadi.

---

## Tarjimonlar

**3 ta format** o‘rtasida tarjima qiladi: OpenAI, Anthropic, Gemini, shuningdek yangi Responses API.

### Tarjima qachon amalga oshiriladi

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Keng tarqalgan tarjimalar:

- `OpenAI → Anthropic`: alohida `system` maydoni, `x-api-key` sarlavhasi
- `OpenAI → Gemini`: `messages` o‘rniga `contents`, `systemInstruction`
- `OpenAI → Responses API`: `input` massivi, `previous_response_id` holati

### Qo‘llab-quvvatlanadigan chekka holatlar

- `developer` roli → OpenAI bo‘lmagan tizimlar uchun `system`
- `system` roli → GLM/ERNIE uchun birinchi foydalanuvchi xabariga birlashtiriladi
- `json_schema` → Gemini’ning `responseMimeType` + `responseSchema`
- `tools` → provayderga xos vosita formati
- Fikrlash parametrlari (o1, Claude) → provayderga xos muqobillar

---

## MCP serveri

`open-sse/mcp-server/` **Model Context Protocol** serverini amalga oshiradi:

- **110 ta vosita** (provayderlarni boshqarish, kombinatsiyalar, xotira, kesh, siqish, proksi, ko‘nikmalar, geymifikatsiya, plaginlar, Notion, Obsidian, mahalliy korpus)
- **3 ta transport**: stdio, SSE, Streamable HTTP
- Batafsil avtorizatsiya uchun **33 ta doira**

### Vositalarni ro‘yxatdan o‘tkazish

Vositalar `open-sse/mcp-server/tools/` ichida alohida fayllar sifatida ro‘yxatdan o‘tkaziladi, ularning har biri nom, sxema, ishlov beruvchi va doirani eksport qiladi:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Tizim holatining joriy tasvirini olish",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Transportlar

```ts
// stdio (CLI orqali foydalanish)
startMcpStdio(server);

// SSE (HTTP asosidagi oqimli uzatish)
startMcpSse(server, port);

// Streamable HTTP (zamonaviy MCP)
startMcpStreamable(server, port);
```

### Avtorizatsiya

Har bir vosita chaqiruvi doira tekshiruvlaridan o‘tadi (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Doira yetarli emas");
}
```

---

## Transformatorlar

`open-sse/transformer/` **Chat Completions** va **Responses API** formatlari o‘rtasida o‘zgartirishni amalga oshiradi.

### Nima uchun alohida transformator kerak?

Responses API — OpenAI’ning **holatni saqlovchi suhbatlar** (`previous_response_id`) imkoniyatiga ega yangi formati. Mijoz Responses so‘rovini yuborganda, OmniRoute:

1. Responses formatini ichki tarzda Chat Completions formatiga o‘zgartiradi
2. Provayderga yuboradi (Chat Completions’ni qo‘llab-quvvatlaydigan istalgan provayder)
3. Javobni qayta Responses formatiga o‘zgartiradi
4. O‘zgartirilgan javobni mijozga oqim tarzida uzatadi

Transformator (`transformer/responsesTransformer.ts`) quyidagini taqdim etadi:

```ts
createResponsesApiTransformStream(): TransformStream
```

U quyidagilarni boshqaradi:

- `response.output_item.added` hodisalari
- `response.output_text.delta` hodisalari
- `response.completed` hodisasi
- Vosita chaqiruvlarini moslashtirish (`function_call` ↔ `tool_calls`)

---

## Konfiguratsiya

`open-sse/config/` konfiguratsiya qatlamini saqlaydi:

| Fayl                          | Maqsadi                                                     |
| ----------------------------- | ----------------------------------------------------------- |
| `providerRegistry.ts`         | 352 ta provayder katalogi asosidagi chat modellari reyestri |
| `providerModels.ts`           | Model taxalluslari, formatlarni moslashtirish               |
| `constants.ts`                | Vaqt tugashlari, cheklovlar, holat kodlari                  |
| `defaultThinkingSignature.ts` | Standart Claude fikrlash imzosi                             |
| `modelStrip.ts` (xizmatlarda) | Har bir provayder uchun maydonlarni olib tashlash           |

### Provayder reyestri sxemasi

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Modul yuklanishidagi Zod tekshiruvi barcha provayder konfiguratsiyalarining yaroqliligini ta’minlaydi.

---

## Unumdorlik cheklovlari

Marshrutlash mexanizmi qatʼiy unumdorlik limitlariga ega:

| Operatsiya                                              | Maqsad | Oʻlchov                    |
| ------------------------------------------------------- | ------ | -------------------------- |
| Kombinatsiyani aniqlash                                 | <10ms  | 50 ta maqsad uchun         |
| Tezlik cheklovini tekshirish                            | <1ms   | Xotiradagi tokenlar savati |
| Model oilasining zaxira variantiga oʻtish               | <5ms   | Keshlangan oila taʼriflari |
| Soʻrovni marshrutlashga uzatish                         | <2ms   | Kritik yoʻl                |
| **Marshrutlashning kritik yoʻlida bloklovchi I/O yoʻq** | —      | Barchasi asinxron          |

---

## Notoʻgʻri yondashuvlar

❌ **`combo.ts` ichidagi sinxron DB chaqiruvlari** — oldindan hisoblang va keshlang
❌ **Ishlov beruvchilardagi qayta urinish mantigʻi** — barqarorlik xizmatidagi `retry()` funksiyasidan foydalaning
❌ **Provayder konfiguratsiyasiga bevosita kirish** — `providerRegistry` getterlaridan foydalaning
❌ **Qattiq kodlangan zaxira zanjirlari** — `modelFamilyFallback.ts` ichida belgilang
❌ **Parallel soʻrovlar orasidagi holat oʻzgarishlari** — faqat soʻrov doirasidagi kontekstdan foydalaning

---

## Yangi komponent qoʻshish

### Yangi xizmat qoʻshish

1. Aniq bir vazifaga yoʻnaltirilgan `open-sse/services/[serviceName].ts` faylini yarating
2. Asosiy ishlov beruvchi funksiya va barcha konstantalarni eksport qiling
3. `tests/unit/services/[serviceName].test.mjs` ichiga modul testlarini qoʻshing
4. Soʻrovlar konveyeriga `handlers/chatCore.ts` ichida integratsiya qiling (agar marshrutlash bilan bogʻliq boʻlsa)
5. Agar xizmat maqsad tanlashga taʼsir qilsa, `combo.ts` ichidagi marshrutlash mantigʻini yangilang
6. Ushbu faylda hujjatlashtiring

### Yangi ijrochini qoʻshish

1. `BaseExecutor`ni kengaytiruvchi `open-sse/executors/[provider].ts` faylini yarating
2. `config/providerRegistry.ts` ichida roʻyxatdan oʻtkazing
3. `executors/index.ts` fabrikasiga qoʻshing
4. Ijrochi uchun modul testlarini qoʻshing
5. `docs/architecture/ARCHITECTURE.md` ichida hujjatlashtiring

### Yangi MCP vositasini qoʻshish

1. `open-sse/mcp-server/tools/[category]Tools.ts` faylini yarating yoki yangilang
2. Kirish maʼlumotlari uchun Zod sxemasini belgilang
3. Vositaning `mcp-server/index.ts` ichida roʻyxatdan oʻtkazing
4. `mcp-server/auth/` ichidagi qamrov matritsasiga qoʻshing
5. Modul testlarini qoʻshing

---

## Shuningdek qarang

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — yuqori darajadagi arxitektura
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — muhandislik maʼlumotnomasi
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — kataloglar boʻyicha tavsif
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — 16 omilli baholash
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP serveri
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A serveri
- Manba: `open-sse/` (400+ ta fayl, ~143K kod qatori)
