# open-sse Architecture (Български)

🌐 **Languages:** 🇺🇸 [English](../../../../frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇹 [am](../../../am/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇦 [ar](../../../ar/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇿 [az](../../../az/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇩 [bn](../../../bn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇦 [bs](../../../bs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇿 [cs](../../../cs/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇰 [da](../../../da/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇩🇪 [de](../../../de/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇷 [el](../../../el/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇸 [es](../../../es/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇪🇪 [et](../../../et/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇷 [fa](../../../fa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇮 [fi](../../../fi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇫🇷 [fr](../../../fr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇪 [ga](../../../ga/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [gu](../../../gu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ha](../../../ha/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇱 [he](../../../he/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [hi](../../../hi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇷 [hr](../../../hr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇭🇺 [hu](../../../hu/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇦🇲 [hy](../../../hy/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇩 [id](../../../id/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [ig](../../../ig/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇹 [it](../../../it/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇯🇵 [ja](../../../ja/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇬🇪 [ka](../../../ka/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇭 [km](../../../km/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [kn](../../../kn/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇷 [ko](../../../ko/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇹 [lt](../../../lt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇻 [lv](../../../lv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ml](../../../ml/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [mr](../../../mr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇾 [ms](../../../ms/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇹 [mt](../../../mt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇲🇲 [my](../../../my/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇵 [ne](../../../ne/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇱 [nl](../../../nl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇴 [no](../../../no/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [or](../../../or/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [pa](../../../pa/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇭 [phi](../../../phi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇱 [pl](../../../pl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇹 [pt](../../../pt/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇴 [ro](../../../ro/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇺 [ru](../../../ru/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇱🇰 [si](../../../si/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇰 [sk](../../../sk/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇮 [sl](../../../sl/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇷🇸 [sr](../../../sr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇸🇪 [sv](../../../sv/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇰🇪 [sw](../../../sw/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [ta](../../../ta/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇮🇳 [te](../../../te/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇭 [th](../../../th/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇷 [tr](../../../tr/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇵🇰 [ur](../../../ur/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇺🇿 [uz](../../../uz/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇻🇳 [vi](../../../vi/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇳🇬 [yo](../../../yo/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/frameworks/OPEN_SSE_ARCHITECTURE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/frameworks/OPEN_SSE_ARCHITECTURE.md)

---

> **Накратко**: `open-sse/` е основният стрийминг механизъм, който обслужва всяка LLM заявка в OmniRoute. Той съдържа ~1 400 файла, реализиращи конвейера за заявки, изпълнителите, услугите, MCP сървъра и слоя за преобразуване. Това ръководство обяснява как отделните компоненти работят заедно.

**Изходен код:** `open-sse/` (пакет в работното пространство, ~1 440 `.ts` файла без тестовете)

---

## Защо отделен пакет в работното пространство?

`open-sse/` е **самостоятелно работно пространство** в монорепото на OmniRoute поради няколко причини:

1. **Повторна употреба** — `open-sse` се публикува като `@omniroute/open-sse` в npm, така че други проекти могат да го използват независимо
2. **Ясни граници** — стрийминг механизмът е отделен от специфичния за OmniRoute слой за потребителски интерфейс/база данни
3. **Производителност** — механизмът няма зависимости от Next.js, което позволява по-бързо студено стартиране в CLI/сървърлес среди
4. **Версиониране** — `open-sse` може да публикува версии по собствен график

```json
// package.json
"workspaces": ["open-sse"]
```

---

## Структура на най-високо ниво

```
open-sse/
├── index.ts              # Публична входна точка
├── types.d.ts            # Публично експортирани типове
├── package.json          # @omniroute/open-sse
├── config/               # Конфигурации на доставчици, константи, регистри
├── executors/            # HTTP изпълнители за отделните доставчици (67 + base.ts/index.ts)
├── handlers/             # Обработчици на заявки (chatCore, responses и др.)
├── lib/                  # Вътрешни помощни инструменти
├── mcp-server/           # Сървър за Model Context Protocol
├── services/             # ~298 модула с услуги
├── transformer/          # Преобразувател на формата на Responses API
├── translator/           # Преобразуване на формати (OpenAI ↔ Claude ↔ Gemini)
└── utils/                # Споделени помощни инструменти (регистриране, грешки, потоци и др.)
```

### Брой модули

| Директория | Файлове | Предназначение |
| `executors/` | 167 | HTTP изпълнители за отделните доставчици (унифицирани чрез фабриката DefaultExecutor) |
| `handlers/` | 157 | Входни точки за заявки (chatCore, responses, embeddings) |
| `services/` | ~536 | Маршрутизиране, кеширане, ограничаване на честотата, опресняване и др. |
| `translator/` | 56 | Преобразуване на формати (OpenAI ↔ Claude ↔ Gemini) |
| `mcp-server/` | 44 | MCP инструменти и транспортни механизми |
| `utils/` | ~108 | Помощни инструменти с общо предназначение (регистриране, грешки, потоци) |
| `config/` | ~339 | Конфигурации на доставчици, константи, регистри |

---

## Конвейерът за заявки

Всяка LLM заявка преминава през **5-етапен конвейер**:

```
                              ┌───────────────┐
   HTTP заявка                 │  1. МАРШРУТ   │   разрешаване на комбинация, избор на модел
   (Next.js маршрут)           └──────┬────────┘
                                     │
                                     ▼
                              ┌───────────────┐
                              │  2. ПРЕВОД    │   преобразуване на формат (OpenAI ↔ Claude ↔ Gemini)
                              └──────┬────────┘
                                     │
                                     ▼
                              ┌───────────────┐
                              │  3. ИЗПЪЛНЕНИЕ│   изпълнител на доставчика, HTTP, повторен опит, прекъсвач
                              └──────┬────────┘
                                     │
                                     ▼
                              ┌───────────────┐
                              │  4. ПОТОК     │   SSE преобразуване, обратен натиск
                              └──────┬────────┘
                                     │
                                     ▼
                              ┌───────────────┐
                              │  5. ЗАПИС     │   проследяване на употребата, дневник на извикванията, класификация на грешките
                              └──────┬────────┘
                                     │
                                     ▼
                              HTTP отговор (SSE или JSON)
```

### Етап 1: Маршрутизиране (services/combo.ts)

**Входна точка**: `handleComboChat()` в `services/combo.ts`

Свързва заявката с конкретен кортеж `(provider, model, account, credentials)`:

- Намира комбинацията по ID (или създава виртуална комбинация за модели `auto/*`)
- Прилага стратегията за маршрутизиране (приоритетна, претеглена, циклична и др.)
- Филтрира неизправните доставчици (прекъсвач на веригата)
- Избира следващата подходяща цел

За модели `auto/*` този етап също:

- Изпълнява алгоритъма за **оценяване по 16 фактора** (`services/autoCombo/`)
- Избира двойка `provider+model` въз основа на изправност, цена, латентност и др.

### Етап 2: Преобразуване (translator/)

Ако изходният формат (напр. OpenAI) се различава от целевия формат (напр. Claude), заявката се **преобразува**:

- Системна подкана → системно съобщение
- Дефиниции на инструменти → специфичен за доставчика формат на инструментите
- Параметри за разсъждение/мисловен процес → специфични за доставчика еквиваленти
- Нормализиране на ролите в съобщенията (`developer` → `system` за системи, различни от OpenAI)

`translator/index.ts` предоставя:

```ts
translateRequest(body, sourceFormat, targetFormat): TranslatedRequest
needsTranslation(source, target): boolean
```

### Етап 3: Изпълнение (executors/)

**Входна точка**: `getExecutor(providerId).execute(request, options)`

Всички доставчици използват `DefaultExecutor` (`executors/default.ts`) чрез резервния механизъм на фабриката `getExecutor()`. Изпълнителят:

- Създава URL адреса към услугата нагоре по веригата (`buildUrl()`)
- Добавя специфични за доставчика заглавки (`buildHeaders()`)
- Преобразува тялото на заявката (`transformRequest()`)
- Изпраща HTTP заявката с повторни опити и експоненциално изчакване
- Обработва опресняването на удостоверяването при необходимост (за OAuth доставчици)

Всички изпълнители разширяват `BaseExecutor` (`executors/base.ts`, 1170 реда код), който предоставя:

- Обща логика за повторни опити
- Интеграция с прокси сървъри
- Интеграция с прекъсвач на веригата
- Механизми за записване на употребата

### Етап 4: Поточно предаване (utils/stream.ts)

За поточни отговори изпълнителят връща **ReadableStream**. Обработчикът:

- Прекарва потока през SSE преобразуване (`createSSETransformStreamWithLogger`)
- Прилага периодични сигнали за проверка с цел откриване на прекъснати връзки
- Обработва коректно прекъсването на връзката от клиента (`pipeWithDisconnect`)
- Преобразува SSE → JSON за клиенти без поддръжка на поточно предаване

За отговори без поточно предаване изпълнителят връща анализиран JSON обект, който се предава без промени.

### Етап 5: Записване (services/usage.ts)

След отговора (успешен или неуспешен) се записват данни за използването:

- `prompt_tokens`, `completion_tokens`, `cached_tokens` от отговора
- `cost_usd`, изчислена въз основа на ценовите данни
- `latency_ms`, `status`, `error_class` при неуспех
- Данните се съхраняват в таблицата `usage_history`

Артефактите от регистрите на извикванията (ако са активирани) се записват в `${DATA_DIR}/call_logs/`.

---

## Подробен преглед на ключовите файлове

### chatCore.ts (5977 реда)

**Основният обработчик на заявки**. Въпреки размера си, той има ясна структура:

```ts
// Псевдоструктура на chatCore.ts
export async function handleChat(request: NextRequest) {
  // 1. Удостоверяване + CORS
  await authenticateRequest(request);
  applyCorsHeaders(response);

  // 2. Валидиране на тялото
  const body = await parseRequestBody(request);

  // 3. Разпознаване на формата + преобразуване
  const sourceFormat = detectFormat(request);
  const targetFormat = getTargetFormat(providerId);
  if (needsTranslation(sourceFormat, targetFormat)) {
    body = translateRequest(body, sourceFormat, targetFormat);
  }

  // 4. Комбинирано маршрутизиране
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      const result = await executeOnTarget(target, body);
      await recordUsage(result);
      return result;
    } catch (err) {
      // Продължаване към следващата цел
    }
  }

  // 5. Аварийна резервна опция
  return await emergencyFallback(body);
}
```

Въпреки че представлява една огромна функция, тя е организирана в **секции с коментари**, които съответстват на 5-етапния конвейер.

### combo.ts (4456 LOC)

**Механизмът за маршрутизиране**, който преобразува дадена комбинация в подредени цели.

```ts
// services/combo.ts
export async function handleComboChat(body, comboId): Promise<ChatResult> {
  const targets = await resolveComboTargets(comboId, body);
  for (const target of targets) {
    try {
      return await handleSingleModel(target, body);
    } catch (err) {
      log.warn("целта е неуспешна, опитва се следващата", { target, err });
    }
  }
  throw new ComboExhaustedError("Всички цели са неуспешни");
}
```

Поддържа **19 стратегии за маршрутизиране** (вижте `src/shared/constants/routingStrategies.ts`):

| Стратегия           | Поведение                                                                  |
| ------------------- | -------------------------------------------------------------------------- |
| `priority`          | Подреден списък, започващ с първата цел                                    |
| `weighted`          | Вероятностен избор според теглото на всяка цел                             |
| `round-robin`       | Циклично преминаване през целите по ред                                    |
| `context-relay`     | Предаване на контекста между целите                                        |
| `fill-first`        | Запълване на квотата преди преминаване към следващата цел                  |
| `p2c`               | Избор между две възможности                                                |
| `random`            | Равномерно случаен избор                                                   |
| `least-used`        | Избира целта с най-малко скорошни използвания                              |
| `cost-optimized`    | Първо най-евтината изправна цел                                            |
| `reset-aware`       | Съобразява се с периодите за нулиране на доставчика                        |
| `reset-window`      | Маршрутизиране въз основа на периода за нулиране                           |
| `headroom`          | Първо целта с най-голям оставащ резерв от квотата                          |
| `strict-random`     | Напълно равномерен избор (без претегляне според качеството)                |
| `auto`              | Използва оценяване по 16 фактора (`autoCombo/`)                            |
| `lkgp`              | Първо последният известен работещ доставчик                                |
| `context-optimized` | Най-подходяща за заявки с дълъг контекст                                   |
| `fusion`            | Изпраща паралелно към панел, след което синтезира чрез съдия (`fusion.ts`) |

### base.ts (1170 LOC)

**Абстрактният изпълнител**, който се разширява от всички 107 изпълнители. Той съдържа:

- `buildUrl()` — конструиране на URL по подразбиране (подкласовете го заменят при специфични нужди)
- `buildHeaders()` — заглавки по подразбиране (удостоверяване, тип съдържание)
- `transformRequest()` — предаване без промени по подразбиране
- `execute()` — основният HTTP цикъл с повторни опити, изчакване и прекъсвач

```ts
// open-sse/executors/default.ts
export class DefaultExecutor extends BaseExecutor {
  // Обработва всички доставчици, съвместими с OpenAI/Anthropic
  // Доставчиците регистрират конфигурации (URL, удостоверяване, заглавки), но споделят логиката на изпълнителя
}
```

Специфичното за доставчика поведение (заглавки за удостоверяване, базов URL, заглавки за версията) се конфигурира чрез регистъра на доставчиците, а не чрез отделни класове изпълнители.

````

---

## Услуги (117 модула)

Услугите са **специализирани модули с една конкретна цел**, които обработчиците комбинират. Основните категории са:

### Маршрутизиране и комбиниране

- `combo.ts` — входна точка за заявки с комбинирано маршрутизиране
- `services/autoCombo/` — оценяване по 16 фактора, 8 стратегии за автоматично маршрутизиране
- `wildcardRouter.ts` — съпоставя маршрути със заместващи символи (`gpt-*`)
- `modelFamilyFallback.ts` — T5 резервен вариант в рамките на същото семейство

### Ограничаване на честотата и квоти

- `rateLimitManager.ts` — кошница с токени за всяка комбинация ключ+доставчик
- `usage.ts` — записване на потреблението
- `quotaCache.ts` — моментни снимки на квотите в паметта

### Акаунти и токени

- `tokenRefresh.ts` — обновяване чрез OAuth при 401
- `accountFallback.ts` — превключване към алтернативен акаунт
- `sessionManager.ts` — състояние на сесията за диалози с множество ходове

### Интелигентност

- `intentClassifier.ts` — класифицира намерението на заявката
- `taskAwareRouter.ts` — маршрутизира според типа задача
- `thinkingBudget.ts` — разпределя токени за разсъждение
- `contextManager.ts` — добавя контекст за маршрутизиране

### Устойчивост

- `resilience.ts` — оркестрация на повторните опити, отложените повторения и прекъсвачите
- `emergencyFallback.ts` — краен резервен вариант
- `modelDeprecation.ts` — автоматично маршрутизиране към модели наследници

### Състояние

- `signatureCache.ts` — премахване на дубликати по сигнатура на заявката
- `volumeDetector.ts` — разтоварване при натоварване
- `contextHandoff.ts` — сериализиране на сесии

### Компресия

- `compression/` (поддиректория) — пълен конвейер за компресия
- 39 файла, обхващащи механизми, пакети с правила и адаптери

### Умения

- (описани в [SKILLS.md](./SKILLS.md))

### Памет

- (описана в [MEMORY.md](./MEMORY.md))

---

## Изпълнители (75+ файла)

По един файл за всеки доставчик. Всички разширяват `BaseExecutor` и предефинират различаващото се поведение.

### Общи шаблони

Доставчиците се определят чрез `getExecutor(providerId)`, която връща конфигурирания изпълнител. Доставчиците, съвместими с OpenAI/Anthropic, използват `DefaultExecutor` (`executors/default.ts`). Специфичното за доставчика поведение (основен URL адрес, заглавки за удостоверяване, версия на API) се конфигурира в `open-sse/config/providers/`, а трансформациите на тялото на заявката се обработват в `open-sse/translator/`.

**Персонализираният URL адрес** се задава чрез конфигурацията на доставчика:

```ts
// Конфигурация на доставчика в open-sse/config/providers/
export default {
  id: "together",
  baseURL: "https://api.together.xyz/v1/chat/completions",
}
````

**Персонализираното удостоверяване** се обработва чрез конфигурацията за удостоверяване в регистъра на доставчиците (API ключ, OAuth, профили на заглавки).

Трансформациите на **персонализираното тяло на заявката** (напр. отделянето на `system` от `messages` при Anthropic) се регистрират за всеки доставчик в `open-sse/translator/`.

````

### Фабриката за изпълнители

`executors/index.ts` експортира `getExecutor(providerId)`:

```ts
import { getExecutor } from "@omniroute/open-sse/executors";

const executor = getExecutor("anthropic");
const result = await executor.execute({
  model: "claude-sonnet-4-5",
  messages: [...],
});
````

Определянето преминава през `ExecutorRegistry` (`executors/registry.ts`): всеки специализиран изпълнител е деклариран във вградената таблица на `executors/index.ts` и се регистрира чрез `registerExecutor(alias, instance)` при зареждането на модула; `getExecutor()` прави справка в регистъра и използва мемоизиран `DefaultExecutor` като резервен вариант за всеки доставчик без специализиран запис. Пълното съпоставяне псевдоним → изпълнител е описано чрез еталонния тест `tests/unit/executor-map-golden.test.ts`.

---

## Преобразуватели

Преобразуват между **3 формата**: OpenAI, Anthropic, Gemini, както и новия Responses API.

### Кога се извършва преобразуването

```ts
import { needsTranslation, translateRequest } from "@omniroute/open-sse/translator";

if (needsTranslation(sourceFormat, targetFormat)) {
  body = translateRequest(body, sourceFormat, targetFormat);
}
```

Често срещани преобразувания:

- `OpenAI → Anthropic`: отделно поле `system`, заглавка `x-api-key`
- `OpenAI → Gemini`: `contents` вместо `messages`, `systemInstruction`
- `OpenAI → Responses API`: масив `input`, състояние `previous_response_id`

### Обработвани гранични случаи

- Роля `developer` → `system` за формати, различни от OpenAI
- Роля `system` → обединява се с първото потребителско съобщение за GLM/ERNIE
- `json_schema` → `responseMimeType` + `responseSchema` на Gemini
- `tools` → специфичен за доставчика формат на инструментите
- Параметри за мисловен процес (o1, Claude) → специфични за доставчика еквиваленти

---

## MCP сървър

`open-sse/mcp-server/` реализира сървъра за **Model Context Protocol**:

- **110 инструмента** (управление на доставчици, комбинации, памет, кеш, компресиране, прокси, умения, геймификация, приставки, Notion, Obsidian, локален корпус)
- **3 транспорта**: stdio, SSE, Streamable HTTP
- **33 обхвата** за детайлно управление на оторизацията

### Регистриране на инструменти

Инструментите се регистрират като самостоятелни файлове в `open-sse/mcp-server/tools/`, като всеки експортира име, схема, обработчик и обхват:

```ts
// open-sse/mcp-server/tools/getHealth.ts
import { z } from "zod";
export default {
  name: "omniroute_get_health",
  description: "Получаване на моментна снимка на състоянието на системата",
  scope: "read:health",
  inputSchema: z.object({}),
  handler: async (_args, ctx) => {
    return await getSystemHealth();
  },
};
```

### Транспорти

```ts
// stdio (използване чрез CLI)
startMcpStdio(server);

// SSE (поточно предаване чрез HTTP)
startMcpSse(server, port);

// Streamable HTTP (съвременен MCP)
startMcpStreamable(server, port);
```

### Оторизация

Всяко извикване на инструмент преминава през проверки на обхвата (`open-sse/mcp-server/auth/`):

```ts
if (!hasScope(apiKey, "providers:read")) {
  throw new Error("Недостатъчен обхват");
}
```

---

## Трансформатори

`open-sse/transformer/` преобразува между форматите **Chat Completions** и **Responses API**.

### Защо е необходим отделен трансформатор?

Responses API е новият формат на OpenAI с **разговори със състояние** (`previous_response_id`). Когато клиент изпрати заявка във формат Responses, OmniRoute:

1. Вътрешно преобразува Responses → Chat Completions
2. Изпраща заявката до доставчика (който и да е доставчик, поддържащ Chat Completions)
3. Преобразува отговора обратно във формат Responses
4. Предава поточно преобразувания отговор към клиента

Трансформаторът (`transformer/responsesTransformer.ts`) предоставя:

```ts
createResponsesApiTransformStream(): TransformStream
```

Той обработва:

- Събития `response.output_item.added`
- Събития `response.output_text.delta`
- Събитие `response.completed`
- Съпоставяне на извикванията на инструменти (`function_call` ↔ `tool_calls`)

---

## Конфигурация

`open-sse/config/` съдържа конфигурационния слой:

| Файл                          | Предназначение                                         |
| ----------------------------- | ------------------------------------------------------ |
| `providerRegistry.ts`         | Регистър на чат модели върху каталога с 352 доставчика |
| `providerModels.ts`           | Псевдоними на модели, съпоставяне на формати           |
| `constants.ts`                | Времеви ограничения, лимити, кодове на състоянието     |
| `defaultThinkingSignature.ts` | Подпис по подразбиране за мисловния процес на Claude   |
| `modelStrip.ts` (в services)  | Премахване на полета според доставчика                 |

### Схема на регистъра на доставчици

```ts
interface ProviderConfig {
  id: string;
  name: string;
  baseUrl: string;
  authType: "bearer" | "api-key" | "oauth" | "cookie";
  executorClass: string;
  defaultModel: string;
  capabilities: ProviderCapabilities;
  models: ModelDefinition[];
}
```

Валидирането чрез Zod при зареждане на модула гарантира, че всички конфигурации на доставчици са валидни.

---

## Ограничения за производителността

Маршрутизиращият механизъм има строги ограничения за производителността:

| Операция                                                 | Целева стойност | Измерване                       |
| -------------------------------------------------------- | --------------- | ------------------------------- |
| Разрешаване на комбинация                                | <10ms           | За 50 цели                      |
| Проверка на ограничението на честотата                   | <1ms            | In-memory token bucket          |
| Резервен избор в рамките на семейство от модели          | <5ms            | Кеширани дефиниции на семейства |
| Насочване на заявка                                      | <2ms            | Критичен път                    |
| **Без блокиращ I/O в критичния път на маршрутизирането** | —               | Изцяло асинхронно               |

---

## Антипатерни

❌ **Синхронни извиквания към БД в `combo.ts`** — изчислявайте предварително и кеширайте
❌ **Логика за повторни опити в обработчиците** — използвайте `retry()` от услугата за устойчивост
❌ **Директен достъп до конфигурацията на доставчика** — използвайте getter методите на `providerRegistry`
❌ **Твърдо кодирани вериги за резервен избор** — дефинирайте ги в `modelFamilyFallback.ts`
❌ **Промени на състоянието между паралелни заявки** — използвайте само контекст с обхват на заявката

---

## Добавяне на нов компонент

### Добавяне на нова услуга

1. Създайте `open-sse/services/[serviceName].ts` с ясно фокусирана отговорност
2. Експортирайте основната функция за обработка и всички константи
3. Добавете модулни тестове в `tests/unit/services/[serviceName].test.mjs`
4. Интегрирайте в конвейера за обработка на заявки в `handlers/chatCore.ts` (ако е свързано с маршрутизирането)
5. Актуализирайте логиката за маршрутизиране в `combo.ts`, ако услугата влияе върху избора на цел
6. Документирайте в този файл

### Добавяне на нов изпълнител

1. Създайте `open-sse/executors/[provider].ts`, който разширява `BaseExecutor`
2. Регистрирайте го в `config/providerRegistry.ts`
3. Добавете го към фабриката в `executors/index.ts`
4. Добавете модулни тестове за изпълнителя
5. Документирайте в `docs/architecture/ARCHITECTURE.md`

### Добавяне на нов MCP инструмент

1. Създайте или актуализирайте `open-sse/mcp-server/tools/[category]Tools.ts`
2. Дефинирайте Zod схема за входните данни
3. Регистрирайте инструмента в `mcp-server/index.ts`
4. Добавете го към матрицата на обхватите в `mcp-server/auth/`
5. Добавете модулни тестове

---

## Вижте също

- [ARCHITECTURE.md](../architecture/ARCHITECTURE.md) — архитектура на високо ниво
- [CODEBASE_DOCUMENTATION.md](../architecture/CODEBASE_DOCUMENTATION.md) — инженерна справка
- [REPOSITORY_MAP.md](../architecture/REPOSITORY_MAP.md) — описание по директории
- [AUTO-COMBO.md](../routing/AUTO-COMBO.md) — оценяване по 16 фактора
- [MCP-SERVER.md](./MCP-SERVER.md) — MCP сървър
- [A2A-SERVER.md](./A2A-SERVER.md) — A2A сървър
- Изходен код: `open-sse/` (над 400 файла, ~143K реда код)
