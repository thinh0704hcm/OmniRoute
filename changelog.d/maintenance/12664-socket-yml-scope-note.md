- **docs(dependencies):** clarify that `socket.yml` only shapes Socket.dev's registry-side
  post-publish scan of the published npm artifact — it is not an enforced CI/PR merge gate
  ([#12664](https://github.com/diegosouzapw/OmniRoute/pull/12664) — thanks @toor11).
