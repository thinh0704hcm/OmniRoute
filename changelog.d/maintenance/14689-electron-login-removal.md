- **fix(electron):** Remove the dead Electron web-cookie login path (`login:*` IPC handlers, preload exposure, `loginManager.js`, `lib/loginHeaderCapture.js`). Its only persist step required a TypeScript path from plain CJS, so every successful extraction reported failure (#14660), and no renderer code ever called the exposed API. Cookie-provider login stays on the server-side `inAppLoginService`. ([#14689](https://github.com/diegosouzapw/OmniRoute/pull/14689), reported in [#14705](https://github.com/diegosouzapw/OmniRoute/issues/14705) — thanks @gonisulaimann)
