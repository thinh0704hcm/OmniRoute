- **test(batches):** boot the batch rate-limit E2E suite through the peer-stamped server (`run-next.mjs`) instead of bare `next dev`. Without the TCP-peer stamp the open-bootstrap management calls that seed its provider node and API key failed closed with 401, so the suite never reached an assertion. It is now pinned by a unit source-contract test (#11535, #14403 fixed the same trap in two sibling harnesses).
