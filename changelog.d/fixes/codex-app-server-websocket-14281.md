- **fix(providers):** the `codex-app-server` registry factory now wires the app-server executor with `websocketFn: getCodexAppServerWebsocketTransport()`, the same transport the internal codex fallback path uses, so the executor no longer runs without a WebSocket transport when reached through the registry ([#14277](https://github.com/diegosouzapw/OmniRoute/issues/14277)). Reasoning effort resolution also accepts `reasoning_effort` on the request body, and an explicit model reasoning suffix (e.g. `gpt-5.5-high`) now takes precedence over client-injected body defaults.
