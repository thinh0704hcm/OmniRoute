- **fix(proxies):** the health sweep now tells a refused destination apart from a dead proxy (a stalled handshake is observed separately from a frank connection failure, and a 451 or proven 403 carries its cause alongside the verdict without touching any proxy status), and an opt-in bounded pass re-tests a proxy the sweep saw refused before its set-aside expires — `PROXY_HEALTH_RECOVERY_ENABLED=true` (default off: no extra probe runs) ([#14233](https://github.com/diegosouzapw/OmniRoute/pull/14233)) — thanks @maxmad64bis
