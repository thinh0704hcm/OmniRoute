- fix(sse): stop an intermittent empty Claude stream from emptying a whole combo — forward the auto mode `safeguards` field only when its paired `dangerous-tool-use-2026-09-03` beta travels with it (Anthropic rejects the field alone with a 400), exempt OmniRoute's own synthesized empty-response 502 from the per-model lockout that was pulling healthy accounts out of rotation for 60s, and surface the real upstream streaming error instead of masking every failure as "Claude returned an empty response (no content block)"
