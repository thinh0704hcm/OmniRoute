- **fix(sse):** an OpenCode free-tier 403 (`FreeTierError`) is now request-scoped for combo routing — it no longer records model lockout or connection cooldown; the synthetic `noauth` path is paused for a short TTL after such a refusal so auto-combo stops re-picking it every turn; dashboard copy and `FREE_TIERS.md` document that the free tier can only be used from within OpenCode ([#14313](https://github.com/diegosouzapw/OmniRoute/issues/14313))
