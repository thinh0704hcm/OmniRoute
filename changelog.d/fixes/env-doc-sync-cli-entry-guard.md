- **fix(scripts):** the env/docs contract gate now actually runs its CLI entry when the checkout path contains characters that file URLs percent-encode (spaces, non-ASCII). The raw `file://${argv[1]}` comparison never matched `import.meta.url` in those checkouts, so `npm run check:env-doc-sync` exited 0 without scanning anything; the entry guard now encodes the path with `pathToFileURL`, matching the other gate scripts.
