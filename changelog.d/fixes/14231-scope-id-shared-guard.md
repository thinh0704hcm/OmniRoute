- **fix(proxies):** non-global proxy scopes now share one validation rule, so a missing `scopeId` is rejected the same way everywhere with the same messages as before. Behaviour change: blank `scopeId` values (whitespace only) on stored assignments are now rejected instead of accepted; clearing a non-global scope without `scopeId` is now rejected as well. ([#14231](https://github.com/diegosouzapw/OmniRoute/pull/14231)) — thanks @maxmad64bis
