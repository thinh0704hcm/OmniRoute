- **Resilience (#14815):** a request that fails while being translated (locally, before any upstream call — e.g. "Maximum call stack size exceeded" on one large image) no longer puts the account into cooldown or counts toward the provider circuit breaker. The same body fails identically on every account, so the error now goes straight back to the client instead of making a single-account install report "all accounts rate limited".
