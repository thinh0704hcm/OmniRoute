- **fix(db):** the Bun SQLite adapter now returns `undefined` instead of `null` for a `.get()` with no matching row, matching better-sqlite3, node:sqlite and sql.js. Under Bun the exclusive-lease guard used to report every connection as leased, so connection tests returned `LEASE_ACTIVE`, usage refresh was deferred and model sync found no connections ([#14203](https://github.com/diegosouzapw/OmniRoute/pull/14203)) — thanks @Ercaner1988
