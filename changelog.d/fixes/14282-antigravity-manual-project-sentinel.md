- **fix(antigravity):** never persist or send the `__REQUIRES_GCP_PROJECT__` manual-project sentinel as a projectId; a connection that already holds it now behaves as "no project" (typed 422, dropped by the selector when a healthy sibling exists) instead of failing every request with HTTP 400 and being preferred over healthy accounts ([#14282](https://github.com/diegosouzapw/OmniRoute/pull/14282)) — thanks @bufftop25
