- **fix(auth):** `parseStringList` and `parseAllowedModels` keep string entries when the API-key row arrives already decoded as an array instead of JSON text, so a key's allowed models and allowed combos are no longer dropped before policy evaluation ([#14500](https://github.com/diegosouzapw/OmniRoute/issues/14500)). `parseAllowedConnections` is covered by [#14698](https://github.com/diegosouzapw/OmniRoute/pull/14698).
